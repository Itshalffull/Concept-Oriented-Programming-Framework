// generated: ProcessEvent/Adapter.swift

import Foundation

class ProcessEventAdapter: ConceptTransport {
    private let handler: any ProcessEventHandler
    private let storage: ConceptStorage

    init(handler: any ProcessEventHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "append":
            let input = try decoder.decode(ProcessEventAppendInput.self, from: invocation.inputData)
            let output = try await handler.append(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "query":
            let input = try decoder.decode(ProcessEventQueryInput.self, from: invocation.inputData)
            let output = try await handler.query(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "queryByType":
            let input = try decoder.decode(ProcessEventQueryByTypeInput.self, from: invocation.inputData)
            let output = try await handler.queryByType(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "getCursor":
            let input = try decoder.decode(ProcessEventGetCursorInput.self, from: invocation.inputData)
            let output = try await handler.getCursor(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
