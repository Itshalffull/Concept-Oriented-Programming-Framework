// generated: LLMCall/Adapter.swift

import Foundation

class LLMCallAdapter: ConceptTransport {
    private let handler: any LLMCallHandler
    private let storage: ConceptStorage

    init(handler: any LLMCallHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "request":
            let input = try decoder.decode(LLMCallRequestInput.self, from: invocation.inputData)
            let output = try await handler.request(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "recordResponse":
            let input = try decoder.decode(LLMCallRecordResponseInput.self, from: invocation.inputData)
            let output = try await handler.recordResponse(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "validate":
            let input = try decoder.decode(LLMCallValidateInput.self, from: invocation.inputData)
            let output = try await handler.validate(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "repair":
            let input = try decoder.decode(LLMCallRepairInput.self, from: invocation.inputData)
            let output = try await handler.repair(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "accept":
            let input = try decoder.decode(LLMCallAcceptInput.self, from: invocation.inputData)
            let output = try await handler.accept(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "reject":
            let input = try decoder.decode(LLMCallRejectInput.self, from: invocation.inputData)
            let output = try await handler.reject(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
