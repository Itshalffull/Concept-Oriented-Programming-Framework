// generated: ProcessMetric/Adapter.swift

import Foundation

class ProcessMetricAdapter: ConceptTransport {
    private let handler: any ProcessMetricHandler
    private let storage: ConceptStorage

    init(handler: any ProcessMetricHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "record":
            let input = try decoder.decode(ProcessMetricRecordInput.self, from: invocation.inputData)
            let output = try await handler.record(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "query":
            let input = try decoder.decode(ProcessMetricQueryInput.self, from: invocation.inputData)
            let output = try await handler.query(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "aggregate":
            let input = try decoder.decode(ProcessMetricAggregateInput.self, from: invocation.inputData)
            let output = try await handler.aggregate(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
