// generated: FlowToken/Adapter.swift

import Foundation

class FlowTokenAdapter: ConceptTransport {
    private let handler: any FlowTokenHandler
    private let storage: ConceptStorage

    init(handler: any FlowTokenHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "emit":
            let input = try decoder.decode(FlowTokenEmitInput.self, from: invocation.inputData)
            let output = try await handler.emit(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "consume":
            let input = try decoder.decode(FlowTokenConsumeInput.self, from: invocation.inputData)
            let output = try await handler.consume(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "kill":
            let input = try decoder.decode(FlowTokenKillInput.self, from: invocation.inputData)
            let output = try await handler.kill(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "countActive":
            let input = try decoder.decode(FlowTokenCountActiveInput.self, from: invocation.inputData)
            let output = try await handler.countActive(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "listActive":
            let input = try decoder.decode(FlowTokenListActiveInput.self, from: invocation.inputData)
            let output = try await handler.listActive(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
