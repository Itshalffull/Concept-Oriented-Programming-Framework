// generated: ProcessRun/Adapter.swift

import Foundation

class ProcessRunAdapter: ConceptTransport {
    private let handler: any ProcessRunHandler
    private let storage: ConceptStorage

    init(handler: any ProcessRunHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "start":
            let input = try decoder.decode(ProcessRunStartInput.self, from: invocation.inputData)
            let output = try await handler.start(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "startChild":
            let input = try decoder.decode(ProcessRunStartChildInput.self, from: invocation.inputData)
            let output = try await handler.startChild(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "complete":
            let input = try decoder.decode(ProcessRunCompleteInput.self, from: invocation.inputData)
            let output = try await handler.complete(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "fail":
            let input = try decoder.decode(ProcessRunFailInput.self, from: invocation.inputData)
            let output = try await handler.fail(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "cancel":
            let input = try decoder.decode(ProcessRunCancelInput.self, from: invocation.inputData)
            let output = try await handler.cancel(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "suspend":
            let input = try decoder.decode(ProcessRunSuspendInput.self, from: invocation.inputData)
            let output = try await handler.suspend(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "resume":
            let input = try decoder.decode(ProcessRunResumeInput.self, from: invocation.inputData)
            let output = try await handler.resume(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "getStatus":
            let input = try decoder.decode(ProcessRunGetStatusInput.self, from: invocation.inputData)
            let output = try await handler.getStatus(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
