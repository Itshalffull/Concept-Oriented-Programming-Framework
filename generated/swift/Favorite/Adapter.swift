// generated: Favorite/Adapter.swift

import Foundation

class FavoriteAdapter: ConceptTransport {
    private let handler: any FavoriteHandler
    private let storage: ConceptStorage

    init(handler: any FavoriteHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "favorite":
            let input = try decoder.decode(FavoriteFavoriteInput.self, from: invocation.inputData)
            let output = try await handler.favorite(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "unfavorite":
            let input = try decoder.decode(FavoriteUnfavoriteInput.self, from: invocation.inputData)
            let output = try await handler.unfavorite(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "isFavorited":
            let input = try decoder.decode(FavoriteIsFavoritedInput.self, from: invocation.inputData)
            let output = try await handler.isFavorited(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "count":
            let input = try decoder.decode(FavoriteCountInput.self, from: invocation.inputData)
            let output = try await handler.count(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
