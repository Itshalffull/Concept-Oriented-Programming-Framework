// generated: ProcessVariable/Adapter.swift

import Foundation

class ProcessVariableAdapter: ConceptTransport {
    private let handler: any ProcessVariableHandler
    private let storage: ConceptStorage

    init(handler: any ProcessVariableHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "set":
            let input = try decoder.decode(ProcessVariableSetInput.self, from: invocation.inputData)
            let output = try await handler.set(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "get":
            let input = try decoder.decode(ProcessVariableGetInput.self, from: invocation.inputData)
            let output = try await handler.get(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "merge":
            let input = try decoder.decode(ProcessVariableMergeInput.self, from: invocation.inputData)
            let output = try await handler.merge(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "delete":
            let input = try decoder.decode(ProcessVariableDeleteInput.self, from: invocation.inputData)
            let output = try await handler.delete(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "list":
            let input = try decoder.decode(ProcessVariableListInput.self, from: invocation.inputData)
            let output = try await handler.list(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "snapshot":
            let input = try decoder.decode(ProcessVariableSnapshotInput.self, from: invocation.inputData)
            let output = try await handler.snapshot(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
