// generated: Evidence/Adapter.swift

import Foundation

class EvidenceAdapter: ConceptTransport {
    private let handler: any EvidenceHandler
    private let storage: ConceptStorage

    init(handler: any EvidenceHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "record":
            let input = try decoder.decode(EvidenceRecordInput.self, from: invocation.inputData)
            let output = try await handler.record(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "validate":
            let input = try decoder.decode(EvidenceValidateInput.self, from: invocation.inputData)
            let output = try await handler.validate(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "retrieve":
            let input = try decoder.decode(EvidenceRetrieveInput.self, from: invocation.inputData)
            let output = try await handler.retrieve(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "compare":
            let input = try decoder.decode(EvidenceCompareInput.self, from: invocation.inputData)
            let output = try await handler.compare(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "minimize":
            let input = try decoder.decode(EvidenceMinimizeInput.self, from: invocation.inputData)
            let output = try await handler.minimize(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "list":
            let input = try decoder.decode(EvidenceListInput.self, from: invocation.inputData)
            let output = try await handler.list(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}