// generated: Approval/Adapter.swift

import Foundation

class ApprovalAdapter: ConceptTransport {
    private let handler: any ApprovalHandler
    private let storage: ConceptStorage

    init(handler: any ApprovalHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "request":
            let input = try decoder.decode(ApprovalRequestInput.self, from: invocation.inputData)
            let output = try await handler.request(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "approve":
            let input = try decoder.decode(ApprovalApproveInput.self, from: invocation.inputData)
            let output = try await handler.approve(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "deny":
            let input = try decoder.decode(ApprovalDenyInput.self, from: invocation.inputData)
            let output = try await handler.deny(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "requestChanges":
            let input = try decoder.decode(ApprovalRequestChangesInput.self, from: invocation.inputData)
            let output = try await handler.requestChanges(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "timeout":
            let input = try decoder.decode(ApprovalTimeoutInput.self, from: invocation.inputData)
            let output = try await handler.timeout(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "getStatus":
            let input = try decoder.decode(ApprovalGetStatusInput.self, from: invocation.inputData)
            let output = try await handler.getStatus(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
