// generated: ConnectorCall/Adapter.swift

import Foundation

class ConnectorCallAdapter: ConceptTransport {
    private let handler: any ConnectorCallHandler
    private let storage: ConceptStorage

    init(handler: any ConnectorCallHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "invoke":
            let input = try decoder.decode(ConnectorCallInvokeInput.self, from: invocation.inputData)
            let output = try await handler.invoke(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "markSuccess":
            let input = try decoder.decode(ConnectorCallMarkSuccessInput.self, from: invocation.inputData)
            let output = try await handler.markSuccess(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "markFailure":
            let input = try decoder.decode(ConnectorCallMarkFailureInput.self, from: invocation.inputData)
            let output = try await handler.markFailure(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "getResult":
            let input = try decoder.decode(ConnectorCallGetResultInput.self, from: invocation.inputData)
            let output = try await handler.getResult(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
