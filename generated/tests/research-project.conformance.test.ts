// ResearchProject Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { researchProjectHandler } from '../../handlers/ts/app/research-project.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ResearchProject functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof researchProjectHandler.create !== 'function') return;
      const result = await interpret(researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "market_analysis" -> ok', async () => {
      if (typeof researchProjectHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "fact_check" -> ok', async () => {
      if (typeof researchProjectHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(researchProjectHandler.create({ query: "Does intermittent fasting improve metabolic health markers?", deliverable_type: "fact_check", constraints: "", perspectives: "nutritionist,endocrinologist", budget: "{\"max_tokens\":200000,\"max_search_calls\":40,\"max_duration_minutes\":15}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_query" -> error', async () => {
      if (typeof researchProjectHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(researchProjectHandler.create({ query: "", deliverable_type: "report", constraints: "", perspectives: "analyst", budget: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "invalid_deliverable" -> error', async () => {
      if (typeof researchProjectHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(researchProjectHandler.create({ query: "Valid query", deliverable_type: "invalid_type", constraints: "", perspectives: "analyst", budget: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "bad_budget_json" -> error', async () => {
      if (typeof researchProjectHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(researchProjectHandler.create({ query: "Valid query", deliverable_type: "report", constraints: "", perspectives: "analyst", budget: "not-json" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('updateBudgetUsage', () => {
    it('builds a valid StorageProgram', () => {
      const program = researchProjectHandler.updateBudgetUsage({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, tokens_delta: "5000", search_calls_delta: "2" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = researchProjectHandler.updateBudgetUsage({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, tokens_delta: "5000", search_calls_delta: "2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = researchProjectHandler.updateBudgetUsage({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, tokens_delta: "5000", search_calls_delta: "2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = researchProjectHandler.updateBudgetUsage({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, tokens_delta: "5000", search_calls_delta: "2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = researchProjectHandler.updateBudgetUsage({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, tokens_delta: "5000", search_calls_delta: "2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof researchProjectHandler.updateBudgetUsage !== 'function') return;
      const result = await interpret(researchProjectHandler.updateBudgetUsage({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, tokens_delta: "5000", search_calls_delta: "2" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "usage_ok" -> ok', async () => {
      if (typeof researchProjectHandler.updateBudgetUsage !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_market_analysis = await interpret(researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" }), storage);
      const result = await interpret(researchProjectHandler.updateBudgetUsage({ project: afterResult_market_analysis?.output?.["project"], tokens_delta: "5000", search_calls_delta: "2" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "usage_missing" -> notfound', async () => {
      if (typeof researchProjectHandler.updateBudgetUsage !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(researchProjectHandler.updateBudgetUsage({ project: "nonexistent", tokens_delta: "100", search_calls_delta: "1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('transition', () => {
    it('builds a valid StorageProgram', () => {
      const program = researchProjectHandler.transition({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, new_status: "planning" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = researchProjectHandler.transition({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, new_status: "planning" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = researchProjectHandler.transition({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, new_status: "planning" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = researchProjectHandler.transition({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, new_status: "planning" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = researchProjectHandler.transition({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, new_status: "planning" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof researchProjectHandler.transition !== 'function') return;
      const result = await interpret(researchProjectHandler.transition({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, new_status: "planning" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "to_planning" -> ok', async () => {
      if (typeof researchProjectHandler.transition !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_market_analysis = await interpret(researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" }), storage);
      const result = await interpret(researchProjectHandler.transition({ project: afterResult_market_analysis?.output?.["project"], new_status: "planning" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "to_cancelled" -> ok', async () => {
      if (typeof researchProjectHandler.transition !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_fact_check = await interpret(researchProjectHandler.create({ query: "Does intermittent fasting improve metabolic health markers?", deliverable_type: "fact_check", constraints: "", perspectives: "nutritionist,endocrinologist", budget: "{\"max_tokens\":200000,\"max_search_calls\":40,\"max_duration_minutes\":15}" }), storage);
      const result = await interpret(researchProjectHandler.transition({ project: afterResult_fact_check?.output?.["project"], new_status: "cancelled" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "skip_transition" -> invalid_transition', async () => {
      if (typeof researchProjectHandler.transition !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_market_analysis = await interpret(researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" }), storage);
      const result = await interpret(researchProjectHandler.transition({ project: afterResult_market_analysis?.output?.["project"], new_status: "completed" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_transition'));
    });

    it('fixture "transition_missing" -> notfound', async () => {
      if (typeof researchProjectHandler.transition !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(researchProjectHandler.transition({ project: "nonexistent", new_status: "planning" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('linkReport', () => {
    it('builds a valid StorageProgram', () => {
      const program = researchProjectHandler.linkReport({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, report_entity_id: "report-page-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = researchProjectHandler.linkReport({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, report_entity_id: "report-page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = researchProjectHandler.linkReport({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, report_entity_id: "report-page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = researchProjectHandler.linkReport({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, report_entity_id: "report-page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = researchProjectHandler.linkReport({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, report_entity_id: "report-page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof researchProjectHandler.linkReport !== 'function') return;
      const result = await interpret(researchProjectHandler.linkReport({ project: {"type":"ref","fixture":"market_analysis","field":"project"}, report_entity_id: "report-page-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "link_report_ok" -> ok', async () => {
      if (typeof researchProjectHandler.linkReport !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_market_analysis = await interpret(researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" }), storage);
      const result = await interpret(researchProjectHandler.linkReport({ project: afterResult_market_analysis?.output?.["project"], report_entity_id: "report-page-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "link_report_missing" -> notfound', async () => {
      if (typeof researchProjectHandler.linkReport !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(researchProjectHandler.linkReport({ project: "nonexistent", report_entity_id: "report-page-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = researchProjectHandler.get({ project: {"type":"ref","fixture":"market_analysis","field":"project"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = researchProjectHandler.get({ project: {"type":"ref","fixture":"market_analysis","field":"project"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = researchProjectHandler.get({ project: {"type":"ref","fixture":"market_analysis","field":"project"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = researchProjectHandler.get({ project: {"type":"ref","fixture":"market_analysis","field":"project"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = researchProjectHandler.get({ project: {"type":"ref","fixture":"market_analysis","field":"project"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof researchProjectHandler.get !== 'function') return;
      const result = await interpret(researchProjectHandler.get({ project: {"type":"ref","fixture":"market_analysis","field":"project"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof researchProjectHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_market_analysis = await interpret(researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" }), storage);
      const result = await interpret(researchProjectHandler.get({ project: afterResult_market_analysis?.output?.["project"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof researchProjectHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(researchProjectHandler.get({ project: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = researchProjectHandler.list({ status_filter: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = researchProjectHandler.list({ status_filter: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = researchProjectHandler.list({ status_filter: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = researchProjectHandler.list({ status_filter: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = researchProjectHandler.list({ status_filter: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof researchProjectHandler.list !== 'function') return;
      const result = await interpret(researchProjectHandler.list({ status_filter: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof researchProjectHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_market_analysis = await interpret(researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" }), storage);
      const _pool = Object.assign({}, (afterResult_market_analysis?.output ?? {}));
      const _fixtureInput = { status_filter: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(researchProjectHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_draft" -> ok', async () => {
      if (typeof researchProjectHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_market_analysis = await interpret(researchProjectHandler.create({ query: "Compare cloud GPU pricing across AWS, GCP, and Azure for LLM fine-tuning workloads", deliverable_type: "comparison", constraints: "Focus on H100 and A100 instances, include spot pricing", perspectives: "cost analyst,ML engineer,infrastructure architect", budget: "{\"max_tokens\":500000,\"max_search_calls\":80,\"max_duration_minutes\":30}" }), storage);
      const _pool = Object.assign({}, (afterResult_market_analysis?.output ?? {}));
      const _fixtureInput = { status_filter: "draft" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(researchProjectHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof researchProjectHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = researchProjectHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ResearchProject');
    });
  });

  describe('invariant examples', () => {
    it("full lifecycle", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(researchProjectHandler.create({ query: "Compare X vs Y", deliverable_type: "comparison", constraints: "", perspectives: "analyst", budget: "{\"max_tokens\":100000,\"max_search_calls\":50,\"max_duration_minutes\":15}" }), storage);
      expect(createResult0.variant).toBe("ok");
      let project = createResult0.output["project"];
      let p = project;
      const thenResult0 = await interpret(researchProjectHandler.transition({ project: p, new_status: "planning" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(researchProjectHandler.transition({ project: p, new_status: "executing" }), storage);
      expect(thenResult1.variant).toBe("ok");
      const thenResult2 = await interpret(researchProjectHandler.transition({ project: p, new_status: "reviewing" }), storage);
      expect(thenResult2.variant).toBe("ok");
      const thenResult3 = await interpret(researchProjectHandler.transition({ project: p, new_status: "completed" }), storage);
      expect(thenResult3.variant).toBe("ok");
    });

    it("budget tracking", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(researchProjectHandler.create({ query: "Test query", deliverable_type: "report", constraints: "", perspectives: "analyst", budget: "{\"max_tokens\":1000,\"max_search_calls\":5,\"max_duration_minutes\":5}" }), storage);
      expect(createResult0.variant).toBe("ok");
      let project = createResult0.output["project"];
      let p = project;
      const thenResult0 = await interpret(researchProjectHandler.updateBudgetUsage({ project: p, tokens_delta: 500, search_calls_delta: 2 }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(researchProjectHandler.get({ project: p }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid status', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ query: fc.string({ minLength: 1, maxLength: 50 }), deliverable_type: fc.string({ minLength: 1, maxLength: 50 }), constraints: fc.string({ minLength: 1, maxLength: 50 }), perspectives: fc.string({ minLength: 1, maxLength: 50 }), budget: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('updateBudgetUsage'), input: fc.record({ project: fc.string(), tokens_delta: fc.integer({ min: 1, max: 1000 }), search_calls_delta: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('transition'), input: fc.record({ project: fc.string(), new_status: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('linkReport'), input: fc.record({ project: fc.string(), report_entity_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ project: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ status_filter: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = researchProjectHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(researchProjectHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: usage non-negative', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ query: fc.string({ minLength: 1, maxLength: 50 }), deliverable_type: fc.string({ minLength: 1, maxLength: 50 }), constraints: fc.string({ minLength: 1, maxLength: 50 }), perspectives: fc.string({ minLength: 1, maxLength: 50 }), budget: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('updateBudgetUsage'), input: fc.record({ project: fc.string(), tokens_delta: fc.integer({ min: 1, max: 1000 }), search_calls_delta: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('transition'), input: fc.record({ project: fc.string(), new_status: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('linkReport'), input: fc.record({ project: fc.string(), report_entity_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ project: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ status_filter: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = researchProjectHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(researchProjectHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty query', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ query: fc.string({ minLength: 1, maxLength: 50 }), deliverable_type: fc.string({ minLength: 1, maxLength: 50 }), constraints: fc.string({ minLength: 1, maxLength: 50 }), perspectives: fc.string({ minLength: 1, maxLength: 50 }), budget: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('updateBudgetUsage'), input: fc.record({ project: fc.string(), tokens_delta: fc.integer({ min: 1, max: 1000 }), search_calls_delta: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('transition'), input: fc.record({ project: fc.string(), new_status: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('linkReport'), input: fc.record({ project: fc.string(), report_entity_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ project: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ status_filter: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = researchProjectHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(researchProjectHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty query
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
