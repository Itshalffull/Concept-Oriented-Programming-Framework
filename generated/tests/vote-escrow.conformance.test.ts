// VoteEscrow Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { voteEscrowHandler } from '../../handlers/ts/governance-structure/weight-sources/vote-escrow.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('VoteEscrow functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof voteEscrowHandler.configure !== 'function') return;
      const result = await interpret(voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "configure_default" -> ok', async () => {
      if (typeof voteEscrowHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "configure_no_token" -> error', async () => {
      if (typeof voteEscrowHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(voteEscrowHandler.configure({ token: "", maxLockYears: "4.0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('lock', () => {
    it('builds a valid StorageProgram', () => {
      const program = voteEscrowHandler.lock({ config: {"type":"ref","fixture":"configure_default","field":"id"}, locker: "alice", amount: "1000.0", lockYears: "4.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = voteEscrowHandler.lock({ config: {"type":"ref","fixture":"configure_default","field":"id"}, locker: "alice", amount: "1000.0", lockYears: "4.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = voteEscrowHandler.lock({ config: {"type":"ref","fixture":"configure_default","field":"id"}, locker: "alice", amount: "1000.0", lockYears: "4.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = voteEscrowHandler.lock({ config: {"type":"ref","fixture":"configure_default","field":"id"}, locker: "alice", amount: "1000.0", lockYears: "4.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = voteEscrowHandler.lock({ config: {"type":"ref","fixture":"configure_default","field":"id"}, locker: "alice", amount: "1000.0", lockYears: "4.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof voteEscrowHandler.lock !== 'function') return;
      const result = await interpret(voteEscrowHandler.lock({ config: {"type":"ref","fixture":"configure_default","field":"id"}, locker: "alice", amount: "1000.0", lockYears: "4.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "lock_full_term" -> ok', async () => {
      if (typeof voteEscrowHandler.lock !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_default = await interpret(voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" }), storage);
      const result = await interpret(voteEscrowHandler.lock({ config: afterResult_configure_default?.output?.["id"], locker: "alice", amount: "1000.0", lockYears: "4.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "lock_zero_amount" -> error', async () => {
      if (typeof voteEscrowHandler.lock !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(voteEscrowHandler.lock({ config: "ve-cfg-001", locker: "alice", amount: "0.0", lockYears: "4.0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('extendLock', () => {
    it('builds a valid StorageProgram', () => {
      const program = voteEscrowHandler.extendLock({ lock: {"type":"ref","fixture":"configure_default","field":"id"}, additionalYears: "1.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = voteEscrowHandler.extendLock({ lock: {"type":"ref","fixture":"configure_default","field":"id"}, additionalYears: "1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = voteEscrowHandler.extendLock({ lock: {"type":"ref","fixture":"configure_default","field":"id"}, additionalYears: "1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = voteEscrowHandler.extendLock({ lock: {"type":"ref","fixture":"configure_default","field":"id"}, additionalYears: "1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = voteEscrowHandler.extendLock({ lock: {"type":"ref","fixture":"configure_default","field":"id"}, additionalYears: "1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof voteEscrowHandler.extendLock !== 'function') return;
      const result = await interpret(voteEscrowHandler.extendLock({ lock: {"type":"ref","fixture":"configure_default","field":"id"}, additionalYears: "1.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "extend_by_one" -> ok', async () => {
      if (typeof voteEscrowHandler.extendLock !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_default = await interpret(voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" }), storage);
      const result = await interpret(voteEscrowHandler.extendLock({ lock: afterResult_configure_default?.output?.["id"], additionalYears: "1.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "extend_missing_lock" -> error', async () => {
      if (typeof voteEscrowHandler.extendLock !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(voteEscrowHandler.extendLock({ lock: "lock-missing", additionalYears: "1.0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('getWeight', () => {
    it('builds a valid StorageProgram', () => {
      const program = voteEscrowHandler.getWeight({ config: {"type":"ref","fixture":"configure_default","field":"id"}, participant: "alice" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = voteEscrowHandler.getWeight({ config: {"type":"ref","fixture":"configure_default","field":"id"}, participant: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = voteEscrowHandler.getWeight({ config: {"type":"ref","fixture":"configure_default","field":"id"}, participant: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = voteEscrowHandler.getWeight({ config: {"type":"ref","fixture":"configure_default","field":"id"}, participant: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = voteEscrowHandler.getWeight({ config: {"type":"ref","fixture":"configure_default","field":"id"}, participant: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof voteEscrowHandler.getWeight !== 'function') return;
      const result = await interpret(voteEscrowHandler.getWeight({ config: {"type":"ref","fixture":"configure_default","field":"id"}, participant: "alice" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_weight_alice" -> ok', async () => {
      if (typeof voteEscrowHandler.getWeight !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_default = await interpret(voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" }), storage);
      const result = await interpret(voteEscrowHandler.getWeight({ config: afterResult_configure_default?.output?.["id"], participant: "alice" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_weight_unknown" -> error', async () => {
      if (typeof voteEscrowHandler.getWeight !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(voteEscrowHandler.getWeight({ config: "ve-cfg-001", participant: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('withdraw', () => {
    it('builds a valid StorageProgram', () => {
      const program = voteEscrowHandler.withdraw({ lock: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = voteEscrowHandler.withdraw({ lock: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = voteEscrowHandler.withdraw({ lock: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = voteEscrowHandler.withdraw({ lock: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = voteEscrowHandler.withdraw({ lock: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof voteEscrowHandler.withdraw !== 'function') return;
      const result = await interpret(voteEscrowHandler.withdraw({ lock: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "withdraw_expired" -> error', async () => {
      if (typeof voteEscrowHandler.withdraw !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_default = await interpret(voteEscrowHandler.configure({ token: "GOV", maxLockYears: "4.0" }), storage);
      const _pool = Object.assign({}, (afterResult_configure_default?.output ?? {}));
      const _fixtureInput = { lock: "lock-expired" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(voteEscrowHandler.withdraw({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "withdraw_still_locked" -> error', async () => {
      if (typeof voteEscrowHandler.withdraw !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(voteEscrowHandler.withdraw({ lock: "lock-active" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof voteEscrowHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = voteEscrowHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('VoteEscrow');
    });
  });

  describe('invariant examples', () => {
    it("configure-then-getWeight", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(voteEscrowHandler.configure({ token: "GOV", maxLockYears: 4 }), storage);
      expect(configureResult0.variant).toBe("ok");
      let config = configureResult0.output["config"];
      let cfg = config;
      const thenResult0 = await interpret(voteEscrowHandler.lock({ config: cfg, locker: "test-p", amount: 100, lockYears: 4 }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(voteEscrowHandler.getWeight({ config: cfg, participant: "test-p" }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-locker', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ token: fc.string({ minLength: 1, maxLength: 50 }), maxLockYears: fc.string() }) }),
              fc.record({ action: fc.constant('lock'), input: fc.record({ config: fc.string(), locker: fc.string({ minLength: 1, maxLength: 50 }), amount: fc.string(), lockYears: fc.string() }) }),
              fc.record({ action: fc.constant('extendLock'), input: fc.record({ lock: fc.string(), additionalYears: fc.string() }) }),
              fc.record({ action: fc.constant('getWeight'), input: fc.record({ config: fc.string(), participant: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('withdraw'), input: fc.record({ lock: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = voteEscrowHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(voteEscrowHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-amount', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ token: fc.string({ minLength: 1, maxLength: 50 }), maxLockYears: fc.string() }) }),
              fc.record({ action: fc.constant('lock'), input: fc.record({ config: fc.string(), locker: fc.string({ minLength: 1, maxLength: 50 }), amount: fc.string(), lockYears: fc.string() }) }),
              fc.record({ action: fc.constant('extendLock'), input: fc.record({ lock: fc.string(), additionalYears: fc.string() }) }),
              fc.record({ action: fc.constant('getWeight'), input: fc.record({ config: fc.string(), participant: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('withdraw'), input: fc.record({ lock: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = voteEscrowHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(voteEscrowHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-amount
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof voteEscrowHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(voteEscrowHandler.configure({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('configure ensures on ok: ', async () => {
      if (typeof voteEscrowHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ token: fc.string({ minLength: 1, maxLength: 50 }), maxLockYears: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = voteEscrowHandler.configure(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
