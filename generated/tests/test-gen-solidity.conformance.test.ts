// TestGenSolidity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { testGenSolidityHandler } from '../../handlers/ts/framework/test/test-gen-solidity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('TestGenSolidity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('render', () => {
    it('builds a valid StorageProgram', () => {
      const program = testGenSolidityHandler.render({ test_plan: "{\"conceptName\":\"Counter\",\"actions\":[{\"name\":\"increment\",\"params\":[],\"variants\":[\"ok\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}", output_path: "generated/tests/counter.test.solidity" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = testGenSolidityHandler.render({ test_plan: "{\"conceptName\":\"Counter\",\"actions\":[{\"name\":\"increment\",\"params\":[],\"variants\":[\"ok\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}", output_path: "generated/tests/counter.test.solidity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = testGenSolidityHandler.render({ test_plan: "{\"conceptName\":\"Counter\",\"actions\":[{\"name\":\"increment\",\"params\":[],\"variants\":[\"ok\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}", output_path: "generated/tests/counter.test.solidity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = testGenSolidityHandler.render({ test_plan: "{\"conceptName\":\"Counter\",\"actions\":[{\"name\":\"increment\",\"params\":[],\"variants\":[\"ok\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}", output_path: "generated/tests/counter.test.solidity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = testGenSolidityHandler.render({ test_plan: "{\"conceptName\":\"Counter\",\"actions\":[{\"name\":\"increment\",\"params\":[],\"variants\":[\"ok\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}", output_path: "generated/tests/counter.test.solidity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof testGenSolidityHandler.render !== 'function') return;
      const result = await interpret(testGenSolidityHandler.render({ test_plan: "{\"conceptName\":\"Counter\",\"actions\":[{\"name\":\"increment\",\"params\":[],\"variants\":[\"ok\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}", output_path: "generated/tests/counter.test.solidity" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "render_valid" -> ok', async () => {
      if (typeof testGenSolidityHandler.render !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_batch_valid = await interpret(testGenSolidityHandler.renderBatch({ test_plans: "[{\"test_plan\":\"{}\",\"output_path\":\"a.ts\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_batch_valid?.output ?? {}));
      const _fixtureInput = { test_plan: "{\"conceptName\":\"Counter\",\"actions\":[{\"name\":\"increment\",\"params\":[],\"variants\":[\"ok\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}", output_path: "generated/tests/counter.test.solidity" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenSolidityHandler.render({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "render_invalid_json" -> error', async () => {
      if (typeof testGenSolidityHandler.render !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(testGenSolidityHandler.render({ test_plan: "not json", output_path: "test.ts" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('renderBatch', () => {
    it('builds a valid StorageProgram', () => {
      const program = testGenSolidityHandler.renderBatch({ test_plans: "[{\"test_plan\":\"{}\",\"output_path\":\"a.ts\"}]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = testGenSolidityHandler.renderBatch({ test_plans: "[{\"test_plan\":\"{}\",\"output_path\":\"a.ts\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = testGenSolidityHandler.renderBatch({ test_plans: "[{\"test_plan\":\"{}\",\"output_path\":\"a.ts\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = testGenSolidityHandler.renderBatch({ test_plans: "[{\"test_plan\":\"{}\",\"output_path\":\"a.ts\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = testGenSolidityHandler.renderBatch({ test_plans: "[{\"test_plan\":\"{}\",\"output_path\":\"a.ts\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof testGenSolidityHandler.renderBatch !== 'function') return;
      const result = await interpret(testGenSolidityHandler.renderBatch({ test_plans: "[{\"test_plan\":\"{}\",\"output_path\":\"a.ts\"}]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "batch_valid" -> ok', async () => {
      if (typeof testGenSolidityHandler.renderBatch !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_render_valid = await interpret(testGenSolidityHandler.render({ test_plan: "{\"conceptName\":\"Counter\",\"actions\":[{\"name\":\"increment\",\"params\":[],\"variants\":[\"ok\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}", output_path: "generated/tests/counter.test.solidity" }), storage);
      const _pool = Object.assign({}, (afterResult_render_valid?.output ?? {}));
      const _fixtureInput = { test_plans: "[{\"test_plan\":\"{}\",\"output_path\":\"a.ts\"}]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenSolidityHandler.renderBatch({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "batch_empty" -> error', async () => {
      if (typeof testGenSolidityHandler.renderBatch !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(testGenSolidityHandler.renderBatch({ test_plans: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listRendered', () => {
    it('builds a valid StorageProgram', () => {
      const program = testGenSolidityHandler.listRendered({ concept_ref: "clef/concept/Counter" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = testGenSolidityHandler.listRendered({ concept_ref: "clef/concept/Counter" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = testGenSolidityHandler.listRendered({ concept_ref: "clef/concept/Counter" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = testGenSolidityHandler.listRendered({ concept_ref: "clef/concept/Counter" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = testGenSolidityHandler.listRendered({ concept_ref: "clef/concept/Counter" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof testGenSolidityHandler.listRendered !== 'function') return;
      const result = await interpret(testGenSolidityHandler.listRendered({ concept_ref: "clef/concept/Counter" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_valid" -> ok', async () => {
      if (typeof testGenSolidityHandler.listRendered !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_render_valid = await interpret(testGenSolidityHandler.render({ test_plan: "{\"conceptName\":\"Counter\",\"actions\":[{\"name\":\"increment\",\"params\":[],\"variants\":[\"ok\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}", output_path: "generated/tests/counter.test.solidity" }), storage);
      const _pool = Object.assign({}, (afterResult_render_valid?.output ?? {}));
      const _fixtureInput = { concept_ref: "clef/concept/Counter" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenSolidityHandler.listRendered({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "list_empty" -> error', async () => {
      if (typeof testGenSolidityHandler.listRendered !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(testGenSolidityHandler.listRendered({ concept_ref: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof testGenSolidityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = testGenSolidityHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('TestGenSolidity');
    });
  });

  describe('invariant examples', () => {
    it("render produces Foundry test code", async () => {
      const storage = createInMemoryStorage();
      const renderResult0 = await interpret(testGenSolidityHandler.render({ test_plan: {"type":"literal","value":"{\"conceptName\":\"Vault\",\"conceptRef\":\"clef/concept/Vault\",\"handlerPath\":\"codegen/solidity/src/Vault.sol\",\"actions\":[{\"name\":\"deposit\",\"params\":[{\"name\":\"amount\",\"type\":\"Int\"}],\"variants\":[\"ok\",\"error\"]}],\"examples\":[],\"properties\":[],\"stateInvariants\":[],\"liveness\":[],\"contracts\":[]}"}, output_path: {"type":"literal","value":"generated/tests/Vault.conformance.t.sol"} }), storage);
      const _isErr0 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr0(renderResult0.variant), `step 0: expected success but got '${renderResult0.variant}'`).toBe(false);
      let result = renderResult0.output["result"];
      let rendered_code = renderResult0.output["rendered_code"];
      let file_path = renderResult0.output["file_path"];
      let test_count = renderResult0.output["test_count"];
    });

    it("invalid JSON rejected", async () => {
      const storage = createInMemoryStorage();
      const renderResult0 = await interpret(testGenSolidityHandler.render({ test_plan: {"type":"literal","value":"[]"}, output_path: {"type":"literal","value":"test.sol"} }), storage);
      expect(renderResult0.variant).toBe("invalid");
      let message = renderResult0.output["message"];
    });

  });

});
