// Z3SolverEndpoint Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { z3SolverEndpointHandler } from '../../handlers/ts/execution/instances/z3-solver-endpoint.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Z3SolverEndpoint functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof z3SolverEndpointHandler.register !== 'function') return;
      const result = await interpret(z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "z3_default" -> ok', async () => {
      if (typeof z3SolverEndpointHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "z3_custom_path" -> ok', async () => {
      if (typeof z3SolverEndpointHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(z3SolverEndpointHandler.register({ name: "z3-custom", binaryPath: "/opt/z3/bin/z3", timeout: "60000", options: "-smt2 -T:60" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_name" -> error', async () => {
      if (typeof z3SolverEndpointHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(z3SolverEndpointHandler.register({ name: "", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('solve', () => {
    it('builds a valid StorageProgram', () => {
      const program = z3SolverEndpointHandler.solve({ name: "z3-local", formula: "(declare-const x Int) (assert (> x 0)) (check-sat)", logic: "QF_LIA" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = z3SolverEndpointHandler.solve({ name: "z3-local", formula: "(declare-const x Int) (assert (> x 0)) (check-sat)", logic: "QF_LIA" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = z3SolverEndpointHandler.solve({ name: "z3-local", formula: "(declare-const x Int) (assert (> x 0)) (check-sat)", logic: "QF_LIA" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = z3SolverEndpointHandler.solve({ name: "z3-local", formula: "(declare-const x Int) (assert (> x 0)) (check-sat)", logic: "QF_LIA" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = z3SolverEndpointHandler.solve({ name: "z3-local", formula: "(declare-const x Int) (assert (> x 0)) (check-sat)", logic: "QF_LIA" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof z3SolverEndpointHandler.solve !== 'function') return;
      const result = await interpret(z3SolverEndpointHandler.solve({ name: "z3-local", formula: "(declare-const x Int) (assert (> x 0)) (check-sat)", logic: "QF_LIA" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "solve_sat" -> ok', async () => {
      if (typeof z3SolverEndpointHandler.solve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_z3_default = await interpret(z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" }), storage);
      const _pool = Object.assign({}, (afterResult_z3_default?.output ?? {}));
      const _fixtureInput = { name: "z3-local", formula: "(declare-const x Int) (assert (> x 0)) (check-sat)", logic: "QF_LIA" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(z3SolverEndpointHandler.solve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "solve_missing_endpoint" -> error', async () => {
      if (typeof z3SolverEndpointHandler.solve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(z3SolverEndpointHandler.solve({ name: "nonexistent", formula: "(check-sat)", logic: "ALL" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = z3SolverEndpointHandler.resolve({ name: "z3-local" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = z3SolverEndpointHandler.resolve({ name: "z3-local" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = z3SolverEndpointHandler.resolve({ name: "z3-local" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = z3SolverEndpointHandler.resolve({ name: "z3-local" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = z3SolverEndpointHandler.resolve({ name: "z3-local" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof z3SolverEndpointHandler.resolve !== 'function') return;
      const result = await interpret(z3SolverEndpointHandler.resolve({ name: "z3-local" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_existing" -> ok', async () => {
      if (typeof z3SolverEndpointHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_z3_default = await interpret(z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: "30000", options: "-smt2" }), storage);
      const _pool = Object.assign({}, (afterResult_z3_default?.output ?? {}));
      const _fixtureInput = { name: "z3-local" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(z3SolverEndpointHandler.resolve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_missing" -> error', async () => {
      if (typeof z3SolverEndpointHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(z3SolverEndpointHandler.resolve({ name: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("solve after register", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(z3SolverEndpointHandler.register({ name: "z3-local", binaryPath: "/usr/bin/z3", timeout: 30000, options: "-smt2" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let endpoint = registerResult0.output["endpoint"];
      let z = endpoint;
      const thenResult0 = await interpret(z3SolverEndpointHandler.resolve({ name: "z3-local" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: endpoints have valid name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), binaryPath: fc.string({ minLength: 1, maxLength: 50 }), timeout: fc.integer({ min: 1, max: 1000 }), options: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('solve'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), formula: fc.string({ minLength: 1, maxLength: 50 }), logic: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = z3SolverEndpointHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(z3SolverEndpointHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: unregistered solve', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), binaryPath: fc.string({ minLength: 1, maxLength: 50 }), timeout: fc.integer({ min: 1, max: 1000 }), options: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('solve'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), formula: fc.string({ minLength: 1, maxLength: 50 }), logic: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = z3SolverEndpointHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(z3SolverEndpointHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: unregistered solve
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof z3SolverEndpointHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(z3SolverEndpointHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof z3SolverEndpointHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), binaryPath: fc.string({ minLength: 1, maxLength: 50 }), timeout: fc.integer({ min: 1, max: 1000 }), options: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = z3SolverEndpointHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
