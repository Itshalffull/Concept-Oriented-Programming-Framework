// ProcessConversation Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { processConversationHandler } from '../../handlers/ts/process-conversation.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ProcessConversation functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('bind', () => {
    it('builds a valid StorageProgram', () => {
      const program = processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processConversationHandler.bind !== 'function') return;
      const result = await interpret(processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof processConversationHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_advance_missing = await interpret(processConversationHandler.advance({ pc: "nonexistent", to_step: "step-1" }), storage);
      const _pool = Object.assign({}, (afterResult_advance_missing?.output ?? {}));
      const _fixtureInput = { run: "run-001", conversation: "conv-001", mode: "guided" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(processConversationHandler.bind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "freeform" -> ok', async () => {
      if (typeof processConversationHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_advance_missing = await interpret(processConversationHandler.advance({ pc: "nonexistent", to_step: "step-1" }), storage);
      const _pool = Object.assign({}, (afterResult_advance_missing?.output ?? {}));
      const _fixtureInput = { run: "run-002", conversation: "conv-002", mode: "freeform" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(processConversationHandler.bind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "duplicate_run" -> fail', async () => {
      if (typeof processConversationHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(processConversationHandler.bind({ run: "run-001", conversation: "conv-003", mode: "guided" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('fail'));
    });

  });

  describe('advance', () => {
    it('builds a valid StorageProgram', () => {
      const program = processConversationHandler.advance({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processConversationHandler.advance({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processConversationHandler.advance({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processConversationHandler.advance({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processConversationHandler.advance({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processConversationHandler.advance !== 'function') return;
      const result = await interpret(processConversationHandler.advance({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "advance_ok" -> ok', async () => {
      if (typeof processConversationHandler.advance !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_advance_missing = await interpret(processConversationHandler.advance({ pc: "nonexistent", to_step: "step-1" }), storage);
      const afterResult_valid = await interpret(processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" }), storage);
      const result = await interpret(processConversationHandler.advance({ pc: afterResult_valid?.output?.["pc"], to_step: "step-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "advance_missing" -> ok', async () => {
      if (typeof processConversationHandler.advance !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(processConversationHandler.advance({ pc: "nonexistent", to_step: "step-1" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('rewind', () => {
    it('builds a valid StorageProgram', () => {
      const program = processConversationHandler.rewind({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processConversationHandler.rewind({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processConversationHandler.rewind({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processConversationHandler.rewind({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processConversationHandler.rewind({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processConversationHandler.rewind !== 'function') return;
      const result = await interpret(processConversationHandler.rewind({ pc: {"type":"ref","fixture":"valid","field":"pc"}, to_step: "step-0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "rewind_ok" -> ok', async () => {
      if (typeof processConversationHandler.rewind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_advance_missing = await interpret(processConversationHandler.advance({ pc: "nonexistent", to_step: "step-1" }), storage);
      const afterResult_valid = await interpret(processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" }), storage);
      const result = await interpret(processConversationHandler.rewind({ pc: afterResult_valid?.output?.["pc"], to_step: "step-0" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('delegate_edit', () => {
    it('builds a valid StorageProgram', () => {
      const program = processConversationHandler.delegate_edit({ pc: {"type":"ref","fixture":"valid","field":"pc"}, action_ref: "edit-field", params: "{\"field\":\"name\",\"value\":\"Alice\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processConversationHandler.delegate_edit({ pc: {"type":"ref","fixture":"valid","field":"pc"}, action_ref: "edit-field", params: "{\"field\":\"name\",\"value\":\"Alice\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processConversationHandler.delegate_edit({ pc: {"type":"ref","fixture":"valid","field":"pc"}, action_ref: "edit-field", params: "{\"field\":\"name\",\"value\":\"Alice\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processConversationHandler.delegate_edit({ pc: {"type":"ref","fixture":"valid","field":"pc"}, action_ref: "edit-field", params: "{\"field\":\"name\",\"value\":\"Alice\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processConversationHandler.delegate_edit({ pc: {"type":"ref","fixture":"valid","field":"pc"}, action_ref: "edit-field", params: "{\"field\":\"name\",\"value\":\"Alice\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processConversationHandler.delegate_edit !== 'function') return;
      const result = await interpret(processConversationHandler.delegate_edit({ pc: {"type":"ref","fixture":"valid","field":"pc"}, action_ref: "edit-field", params: "{\"field\":\"name\",\"value\":\"Alice\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "delegate_ok" -> ok', async () => {
      if (typeof processConversationHandler.delegate_edit !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_advance_missing = await interpret(processConversationHandler.advance({ pc: "nonexistent", to_step: "step-1" }), storage);
      const afterResult_valid = await interpret(processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" }), storage);
      const result = await interpret(processConversationHandler.delegate_edit({ pc: afterResult_valid?.output?.["pc"], action_ref: "edit-field", params: "{\"field\":\"name\",\"value\":\"Alice\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "delegate_denied" -> denied', async () => {
      if (typeof processConversationHandler.delegate_edit !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(processConversationHandler.delegate_edit({ pc: {"type":"ref","fixture":"valid","field":"pc"}, action_ref: "delete-record", params: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('denied'));
    });

  });

  describe('complete_step', () => {
    it('builds a valid StorageProgram', () => {
      const program = processConversationHandler.complete_step({ pc: {"type":"ref","fixture":"valid","field":"pc"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processConversationHandler.complete_step({ pc: {"type":"ref","fixture":"valid","field":"pc"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processConversationHandler.complete_step({ pc: {"type":"ref","fixture":"valid","field":"pc"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processConversationHandler.complete_step({ pc: {"type":"ref","fixture":"valid","field":"pc"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processConversationHandler.complete_step({ pc: {"type":"ref","fixture":"valid","field":"pc"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processConversationHandler.complete_step !== 'function') return;
      const result = await interpret(processConversationHandler.complete_step({ pc: {"type":"ref","fixture":"valid","field":"pc"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "complete_ok" -> ok', async () => {
      if (typeof processConversationHandler.complete_step !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_advance_missing = await interpret(processConversationHandler.advance({ pc: "nonexistent", to_step: "step-1" }), storage);
      const afterResult_valid = await interpret(processConversationHandler.bind({ run: "run-001", conversation: "conv-001", mode: "guided" }), storage);
      const result = await interpret(processConversationHandler.complete_step({ pc: afterResult_valid?.output?.["pc"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "complete_fail" -> fail', async () => {
      if (typeof processConversationHandler.complete_step !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(processConversationHandler.complete_step({ pc: {"type":"ref","fixture":"valid","field":"pc"} }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('fail'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof processConversationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = processConversationHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ProcessConversation');
    });
  });

  describe('invariant examples', () => {
    it("bind then advance", async () => {
      const storage = createInMemoryStorage();
      const bindResult0 = await interpret(processConversationHandler.bind({ run: "run-100", conversation: "conv-100", mode: "guided" }), storage);
      expect(bindResult0.variant).toBe("ok");
      let pc = bindResult0.output["pc"];
      let x = pc;
      const thenResult0 = await interpret(processConversationHandler.advance({ pc: x, to_step: "step-1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("bind then complete step", async () => {
      const storage = createInMemoryStorage();
      const bindResult0 = await interpret(processConversationHandler.bind({ run: "run-200", conversation: "conv-200", mode: "guided" }), storage);
      expect(bindResult0.variant).toBe("ok");
      let pc = bindResult0.output["pc"];
      let y = pc;
      const thenResult0 = await interpret(processConversationHandler.complete_step({ pc: y }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: orphan binding', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('bind'), input: fc.record({ run: fc.string({ minLength: 1, maxLength: 50 }), conversation: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('advance'), input: fc.record({ pc: fc.string(), to_step: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rewind'), input: fc.record({ pc: fc.string(), to_step: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delegate_edit'), input: fc.record({ pc: fc.string(), action_ref: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('complete_step'), input: fc.record({ pc: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = processConversationHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(processConversationHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphan binding
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: mode is valid', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('bind'), input: fc.record({ run: fc.string({ minLength: 1, maxLength: 50 }), conversation: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('advance'), input: fc.record({ pc: fc.string(), to_step: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rewind'), input: fc.record({ pc: fc.string(), to_step: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delegate_edit'), input: fc.record({ pc: fc.string(), action_ref: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('complete_step'), input: fc.record({ pc: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = processConversationHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(processConversationHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
