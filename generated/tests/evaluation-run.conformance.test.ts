// EvaluationRun Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { evaluationRunHandler } from '../../handlers/ts/process-llm/evaluation-run.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('EvaluationRun functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('run_eval', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationRunHandler.run_eval({ step_ref: 'test-step_ref', evaluator_type: 'test-evaluator_type', input: 'test', threshold: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationRunHandler.run_eval({ step_ref: 'test-step_ref', evaluator_type: 'test-evaluator_type', input: 'test', threshold: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationRunHandler.run_eval({ step_ref: 'test-step_ref', evaluator_type: 'test-evaluator_type', input: 'test', threshold: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationRunHandler.run_eval({ step_ref: 'test-step_ref', evaluator_type: 'test-evaluator_type', input: 'test', threshold: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationRunHandler.run_eval({ step_ref: 'test-step_ref', evaluator_type: 'test-evaluator_type', input: 'test', threshold: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationRunHandler.run_eval !== 'function') return;
      const result = await interpret(evaluationRunHandler.run_eval({ step_ref: 'test-step_ref', evaluator_type: 'test-evaluator_type', input: 'test', threshold: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('log_metric', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationRunHandler.log_metric({ eval: 'test', metric_name: 'test-metric_name', metric_value: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationRunHandler.log_metric({ eval: 'test', metric_name: 'test-metric_name', metric_value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationRunHandler.log_metric({ eval: 'test', metric_name: 'test-metric_name', metric_value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationRunHandler.log_metric({ eval: 'test', metric_name: 'test-metric_name', metric_value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationRunHandler.log_metric({ eval: 'test', metric_name: 'test-metric_name', metric_value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationRunHandler.log_metric !== 'function') return;
      const result = await interpret(evaluationRunHandler.log_metric({ eval: 'test', metric_name: 'test-metric_name', metric_value: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('pass', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationRunHandler.pass({ eval: 'test', score: 1, feedback: 'test-feedback' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationRunHandler.pass({ eval: 'test', score: 1, feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationRunHandler.pass({ eval: 'test', score: 1, feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationRunHandler.pass({ eval: 'test', score: 1, feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationRunHandler.pass({ eval: 'test', score: 1, feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationRunHandler.pass !== 'function') return;
      const result = await interpret(evaluationRunHandler.pass({ eval: 'test', score: 1, feedback: 'test-feedback' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('fail', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationRunHandler.fail({ eval: 'test', score: 1, feedback: 'test-feedback' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationRunHandler.fail({ eval: 'test', score: 1, feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationRunHandler.fail({ eval: 'test', score: 1, feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationRunHandler.fail({ eval: 'test', score: 1, feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationRunHandler.fail({ eval: 'test', score: 1, feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationRunHandler.fail !== 'function') return;
      const result = await interpret(evaluationRunHandler.fail({ eval: 'test', score: 1, feedback: 'test-feedback' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('get_result', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationRunHandler.get_result({ eval: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationRunHandler.get_result({ eval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationRunHandler.get_result({ eval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationRunHandler.get_result({ eval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationRunHandler.get_result({ eval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationRunHandler.get_result !== 'function') return;
      const result = await interpret(evaluationRunHandler.get_result({ eval: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof evaluationRunHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = evaluationRunHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('EvaluationRun');
    });
  });

  describe('state invariants (stateful PBT)', () => {
    it('always: runs have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('run_eval'), input: fc.record({ step_ref: fc.string({ minLength: 1, maxLength: 50 }), evaluator_type: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string(), threshold: fc.string() }) }),
              fc.record({ action: fc.constant('log_metric'), input: fc.record({ eval: fc.string(), metric_name: fc.string({ minLength: 1, maxLength: 50 }), metric_value: fc.string() }) }),
              fc.record({ action: fc.constant('pass'), input: fc.record({ eval: fc.string(), score: fc.string(), feedback: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('fail'), input: fc.record({ eval: fc.string(), score: fc.string(), feedback: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get_result'), input: fc.record({ eval: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = evaluationRunHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(evaluationRunHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in runs', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('run_eval'), input: fc.record({ step_ref: fc.string({ minLength: 1, maxLength: 50 }), evaluator_type: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string(), threshold: fc.string() }) }),
              fc.record({ action: fc.constant('log_metric'), input: fc.record({ eval: fc.string(), metric_name: fc.string({ minLength: 1, maxLength: 50 }), metric_value: fc.string() }) }),
              fc.record({ action: fc.constant('pass'), input: fc.record({ eval: fc.string(), score: fc.string(), feedback: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('fail'), input: fc.record({ eval: fc.string(), score: fc.string(), feedback: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get_result'), input: fc.record({ eval: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = evaluationRunHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(evaluationRunHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in runs
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('run_eval handles empty input: ', async () => {
      if (typeof evaluationRunHandler.run_eval !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(evaluationRunHandler.run_eval({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('run_eval ensures on ok: ', async () => {
      if (typeof evaluationRunHandler.run_eval !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ step_ref: fc.string({ minLength: 1, maxLength: 50 }), evaluator_type: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string(), threshold: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = evaluationRunHandler.run_eval(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('log_metric handles empty input: ', async () => {
      if (typeof evaluationRunHandler.log_metric !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(evaluationRunHandler.log_metric({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('log_metric ensures on ok: ', async () => {
      if (typeof evaluationRunHandler.log_metric !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ eval: fc.string(), metric_name: fc.string({ minLength: 1, maxLength: 50 }), metric_value: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = evaluationRunHandler.log_metric(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('pass handles empty input: ', async () => {
      if (typeof evaluationRunHandler.pass !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(evaluationRunHandler.pass({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('pass ensures on ok: ', async () => {
      if (typeof evaluationRunHandler.pass !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ eval: fc.string(), score: fc.string(), feedback: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = evaluationRunHandler.pass(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
