// PaginationSpec Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { paginationSpecHandler } from '../../handlers/ts/view/pagination-spec.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('PaginationSpec functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      const result = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "default_offset" -> ok', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "cursor_spec" -> ok', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.create({ name: "cursor-1", mode: "cursor", pageSize: "50", maxSize: "200" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "keyset_spec" -> ok', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.create({ name: "keyset-1", mode: "keyset", pageSize: "10", maxSize: "50" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "duplicate" -> duplicate', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "oversized" -> error', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.create({ name: "big", mode: "offset", pageSize: "200", maxSize: "100" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "invalid_mode" -> error', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.create({ name: "bad-mode", mode: "stream", pageSize: "10", maxSize: "100" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "zero_pagesize" -> error', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.create({ name: "zero", mode: "offset", pageSize: "0", maxSize: "100" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.get({ name: {"type":"ref","fixture":"default_offset","field":"spec"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.get({ name: {"type":"ref","fixture":"default_offset","field":"spec"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.get({ name: {"type":"ref","fixture":"default_offset","field":"spec"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.get({ name: {"type":"ref","fixture":"default_offset","field":"spec"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.get({ name: {"type":"ref","fixture":"default_offset","field":"spec"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.get !== 'function') return;
      const result = await interpret(paginationSpecHandler.get({ name: {"type":"ref","fixture":"default_offset","field":"spec"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof paginationSpecHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const result = await interpret(paginationSpecHandler.get({ name: afterResult_default_offset?.output?.["spec"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> not_found', async () => {
      if (typeof paginationSpecHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.get({ name: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('advance', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.advance({ name: "page-1", nextPosition: "1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.advance({ name: "page-1", nextPosition: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.advance({ name: "page-1", nextPosition: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.advance({ name: "page-1", nextPosition: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.advance({ name: "page-1", nextPosition: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.advance !== 'function') return;
      const result = await interpret(paginationSpecHandler.advance({ name: "page-1", nextPosition: "1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "advance_ok" -> ok', async () => {
      if (typeof paginationSpecHandler.advance !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { name: "page-1", nextPosition: "1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.advance({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "advance_missing" -> not_found', async () => {
      if (typeof paginationSpecHandler.advance !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.advance({ name: "nonexistent", nextPosition: "1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('retreat', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.retreat({ name: "page-1", prevPosition: "0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.retreat({ name: "page-1", prevPosition: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.retreat({ name: "page-1", prevPosition: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.retreat({ name: "page-1", prevPosition: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.retreat({ name: "page-1", prevPosition: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.retreat !== 'function') return;
      const result = await interpret(paginationSpecHandler.retreat({ name: "page-1", prevPosition: "0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "retreat_ok" -> ok', async () => {
      if (typeof paginationSpecHandler.retreat !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const afterResult_advance_ok = await interpret(paginationSpecHandler.advance({ name: "page-1", nextPosition: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}), (afterResult_advance_ok?.output ?? {}));
      const _fixtureInput = { name: "page-1", prevPosition: "0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.retreat({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "retreat_at_start" -> at_start', async () => {
      if (typeof paginationSpecHandler.retreat !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { name: "page-1", prevPosition: "0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.retreat({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('at_start'));
    });

    it('fixture "retreat_missing" -> not_found', async () => {
      if (typeof paginationSpecHandler.retreat !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.retreat({ name: "nonexistent", prevPosition: "0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('reset', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.reset({ name: "page-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.reset({ name: "page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.reset({ name: "page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.reset({ name: "page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.reset({ name: "page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.reset !== 'function') return;
      const result = await interpret(paginationSpecHandler.reset({ name: "page-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "reset_ok" -> ok', async () => {
      if (typeof paginationSpecHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const afterResult_advance_ok = await interpret(paginationSpecHandler.advance({ name: "page-1", nextPosition: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}), (afterResult_advance_ok?.output ?? {}));
      const _fixtureInput = { name: "page-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.reset({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reset_missing" -> not_found', async () => {
      if (typeof paginationSpecHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.reset({ name: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('resize', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.resize({ name: "page-1", pageSize: "10" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.resize({ name: "page-1", pageSize: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.resize({ name: "page-1", pageSize: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.resize({ name: "page-1", pageSize: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.resize({ name: "page-1", pageSize: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.resize !== 'function') return;
      const result = await interpret(paginationSpecHandler.resize({ name: "page-1", pageSize: "10" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resize_ok" -> ok', async () => {
      if (typeof paginationSpecHandler.resize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { name: "page-1", pageSize: "10" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.resize({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resize_exceeds" -> exceeds_max', async () => {
      if (typeof paginationSpecHandler.resize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { name: "page-1", pageSize: "500" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.resize({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('exceeds_max'));
    });

    it('fixture "resize_zero" -> error', async () => {
      if (typeof paginationSpecHandler.resize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { name: "page-1", pageSize: "0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.resize({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "resize_missing" -> not_found', async () => {
      if (typeof paginationSpecHandler.resize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.resize({ name: "nonexistent", pageSize: "10" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('updateCount', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.updateCount({ name: "page-1", totalCount: "150", hasMore: "true" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.updateCount({ name: "page-1", totalCount: "150", hasMore: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.updateCount({ name: "page-1", totalCount: "150", hasMore: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.updateCount({ name: "page-1", totalCount: "150", hasMore: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.updateCount({ name: "page-1", totalCount: "150", hasMore: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.updateCount !== 'function') return;
      const result = await interpret(paginationSpecHandler.updateCount({ name: "page-1", totalCount: "150", hasMore: "true" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "update_count_ok" -> ok', async () => {
      if (typeof paginationSpecHandler.updateCount !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { name: "page-1", totalCount: "150", hasMore: "true" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.updateCount({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "update_count_missing" -> not_found', async () => {
      if (typeof paginationSpecHandler.updateCount !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.updateCount({ name: "nonexistent", totalCount: "0", hasMore: "false" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('remove', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.remove({ name: "page-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.remove({ name: "page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.remove({ name: "page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.remove({ name: "page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.remove({ name: "page-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.remove !== 'function') return;
      const result = await interpret(paginationSpecHandler.remove({ name: "page-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_ok" -> ok', async () => {
      if (typeof paginationSpecHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { name: "page-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.remove({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_missing" -> not_found', async () => {
      if (typeof paginationSpecHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.remove({ name: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.list !== 'function') return;
      const result = await interpret(paginationSpecHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof paginationSpecHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(paginationSpecHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('evaluate', () => {
    it('builds a valid StorageProgram', () => {
      const program = paginationSpecHandler.evaluate({ name: "page-1", offset: "0", cursor: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paginationSpecHandler.evaluate({ name: "page-1", offset: "0", cursor: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paginationSpecHandler.evaluate({ name: "page-1", offset: "0", cursor: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paginationSpecHandler.evaluate({ name: "page-1", offset: "0", cursor: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paginationSpecHandler.evaluate({ name: "page-1", offset: "0", cursor: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paginationSpecHandler.evaluate !== 'function') return;
      const result = await interpret(paginationSpecHandler.evaluate({ name: "page-1", offset: "0", cursor: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "evaluate_offset" -> ok', async () => {
      if (typeof paginationSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_default_offset = await interpret(paginationSpecHandler.create({ name: "page-1", mode: "offset", pageSize: "25", maxSize: "100" }), storage);
      const _pool = Object.assign({}, (afterResult_default_offset?.output ?? {}));
      const _fixtureInput = { name: "page-1", offset: "0", cursor: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.evaluate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "evaluate_cursor" -> ok', async () => {
      if (typeof paginationSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_cursor_spec = await interpret(paginationSpecHandler.create({ name: "cursor-1", mode: "cursor", pageSize: "50", maxSize: "200" }), storage);
      const _pool = Object.assign({}, (afterResult_cursor_spec?.output ?? {}));
      const _fixtureInput = { name: "cursor-1", offset: "0", cursor: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paginationSpecHandler.evaluate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "evaluate_missing" -> not_found', async () => {
      if (typeof paginationSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paginationSpecHandler.evaluate({ name: "nonexistent", offset: "0", cursor: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof paginationSpecHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = paginationSpecHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('PaginationSpec');
    });
  });

  describe('invariant examples', () => {
    it("create then get returns spec", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(paginationSpecHandler.create({ name: "p1", mode: "offset", pageSize: 25, maxSize: 100 }), storage);
      expect(createResult0.variant).toBe("ok");
      let spec = createResult0.output["spec"];
      let s = spec;
      const thenResult0 = await interpret(paginationSpecHandler.get({ name: "p1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("advance then retreat returns to original position", async () => {
      const storage = createInMemoryStorage();
    });

    it("resize resets position to zero", async () => {
      const storage = createInMemoryStorage();
    });

    it("duplicate create is rejected", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(paginationSpecHandler.create({ name: "p4", mode: "offset", pageSize: 25, maxSize: 100 }), storage);
      expect(createResult0.variant).toBe("ok");
      const thenResult0 = await interpret(paginationSpecHandler.create({ name: "p4", mode: "cursor", pageSize: 10, maxSize: 100 }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

    it("evaluate offset returns correct limit and offset", async () => {
      const storage = createInMemoryStorage();
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: page size within bounds', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }), pageSize: fc.integer({ min: 1, max: 1000 }), maxSize: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('advance'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), nextPosition: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('retreat'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), prevPosition: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resize'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), pageSize: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('updateCount'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), totalCount: fc.integer({ min: 1, max: 1000 }), hasMore: fc.boolean() }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), offset: fc.integer({ min: 1, max: 1000 }), cursor: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paginationSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paginationSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: mode is valid', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }), pageSize: fc.integer({ min: 1, max: 1000 }), maxSize: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('advance'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), nextPosition: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('retreat'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), prevPosition: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resize'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), pageSize: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('updateCount'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), totalCount: fc.integer({ min: 1, max: 1000 }), hasMore: fc.boolean() }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), offset: fc.integer({ min: 1, max: 1000 }), cursor: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paginationSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paginationSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphan position state', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }), pageSize: fc.integer({ min: 1, max: 1000 }), maxSize: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('advance'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), nextPosition: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('retreat'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), prevPosition: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resize'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), pageSize: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('updateCount'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), totalCount: fc.integer({ min: 1, max: 1000 }), hasMore: fc.boolean() }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), offset: fc.integer({ min: 1, max: 1000 }), cursor: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paginationSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paginationSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphan position state
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(paginationSpecHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof paginationSpecHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }), pageSize: fc.integer({ min: 1, max: 1000 }), maxSize: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = paginationSpecHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('resize handles empty input: ', async () => {
      if (typeof paginationSpecHandler.resize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(paginationSpecHandler.resize({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('resize ensures on ok: ', async () => {
      if (typeof paginationSpecHandler.resize !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), pageSize: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = paginationSpecHandler.resize(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
