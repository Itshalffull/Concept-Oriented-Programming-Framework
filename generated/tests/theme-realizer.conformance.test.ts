// ThemeRealizer Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { themeRealizerHandler } from '../../handlers/ts/surface/theme-realizer.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ThemeRealizer functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof themeRealizerHandler.register !== 'function') return;
      const result = await interpret(themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "css_realizer" -> ok', async () => {
      if (typeof themeRealizerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "swiftui_realizer" -> ok', async () => {
      if (typeof themeRealizerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(themeRealizerHandler.register({ realizer: "realizer-swiftui", target: "swiftui", outputKinds: ["ContractManifest","SwiftUiStyles"], contractSupport: ["field-control","page-section"], capabilities: ["environment-keys","state-modifiers"], description: "Generate SwiftUI modifier and environment bindings." }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "duplicate_realizer" -> duplicate', async () => {
      if (typeof themeRealizerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_css_realizer = await interpret(themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." }), storage);
      const _pool = Object.assign({}, (afterResult_css_realizer?.output ?? {}));
      const _fixtureInput = { realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest"], contractSupport: ["field-control"], capabilities: ["density-variants"], description: "Duplicate registration should fail." } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(themeRealizerHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "missing_target" -> error', async () => {
      if (typeof themeRealizerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(themeRealizerHandler.register({ realizer: "broken-realizer", target: "", outputKinds: ["ContractManifest"], contractSupport: ["field-control"], capabilities: ["state-selectors"], description: "Missing target should fail." }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('realize', () => {
    it('builds a valid StorageProgram', () => {
      const program = themeRealizerHandler.realize({ realizer: "realizer-css", theme: "{\"id\":\"light\"}", contracts: "{\"contracts\":[]}", lenses: "{\"lenses\":[]}", context: "{\"density\":\"compact\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = themeRealizerHandler.realize({ realizer: "realizer-css", theme: "{\"id\":\"light\"}", contracts: "{\"contracts\":[]}", lenses: "{\"lenses\":[]}", context: "{\"density\":\"compact\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = themeRealizerHandler.realize({ realizer: "realizer-css", theme: "{\"id\":\"light\"}", contracts: "{\"contracts\":[]}", lenses: "{\"lenses\":[]}", context: "{\"density\":\"compact\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = themeRealizerHandler.realize({ realizer: "realizer-css", theme: "{\"id\":\"light\"}", contracts: "{\"contracts\":[]}", lenses: "{\"lenses\":[]}", context: "{\"density\":\"compact\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = themeRealizerHandler.realize({ realizer: "realizer-css", theme: "{\"id\":\"light\"}", contracts: "{\"contracts\":[]}", lenses: "{\"lenses\":[]}", context: "{\"density\":\"compact\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof themeRealizerHandler.realize !== 'function') return;
      const result = await interpret(themeRealizerHandler.realize({ realizer: "realizer-css", theme: "{\"id\":\"light\"}", contracts: "{\"contracts\":[]}", lenses: "{\"lenses\":[]}", context: "{\"density\":\"compact\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "realize_css" -> ok', async () => {
      if (typeof themeRealizerHandler.realize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_css_realizer = await interpret(themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." }), storage);
      const _pool = Object.assign({}, (afterResult_css_realizer?.output ?? {}));
      const _fixtureInput = { realizer: "realizer-css", theme: "{\"id\":\"light\"}", contracts: "{\"contracts\":[]}", lenses: "{\"lenses\":[]}", context: "{\"density\":\"compact\"}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(themeRealizerHandler.realize({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "realize_unknown" -> notfound', async () => {
      if (typeof themeRealizerHandler.realize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_css_realizer = await interpret(themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." }), storage);
      const _pool = Object.assign({}, (afterResult_css_realizer?.output ?? {}));
      const _fixtureInput = { realizer: "missing-realizer", theme: "{}", contracts: "{}", lenses: "{}", context: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(themeRealizerHandler.realize({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "realize_empty_theme" -> error', async () => {
      if (typeof themeRealizerHandler.realize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_css_realizer = await interpret(themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." }), storage);
      const _pool = Object.assign({}, (afterResult_css_realizer?.output ?? {}));
      const _fixtureInput = { realizer: "realizer-css", theme: "", contracts: "{\"contracts\":[]}", lenses: "{\"lenses\":[]}", context: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(themeRealizerHandler.realize({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "realize_bad_contracts" -> ok', async () => {
      if (typeof themeRealizerHandler.realize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_css_realizer = await interpret(themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." }), storage);
      const _pool = Object.assign({}, (afterResult_css_realizer?.output ?? {}));
      const _fixtureInput = { realizer: "realizer-css", theme: "{\"id\":\"light\"}", contracts: "not-json", lenses: "{\"lenses\":[]}", context: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(themeRealizerHandler.realize({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = themeRealizerHandler.get({ realizer: {"type":"ref","fixture":"css_realizer","field":"realizer"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = themeRealizerHandler.get({ realizer: {"type":"ref","fixture":"css_realizer","field":"realizer"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = themeRealizerHandler.get({ realizer: {"type":"ref","fixture":"css_realizer","field":"realizer"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = themeRealizerHandler.get({ realizer: {"type":"ref","fixture":"css_realizer","field":"realizer"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = themeRealizerHandler.get({ realizer: {"type":"ref","fixture":"css_realizer","field":"realizer"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof themeRealizerHandler.get !== 'function') return;
      const result = await interpret(themeRealizerHandler.get({ realizer: {"type":"ref","fixture":"css_realizer","field":"realizer"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "existing_realizer" -> ok', async () => {
      if (typeof themeRealizerHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_css_realizer = await interpret(themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." }), storage);
      const result = await interpret(themeRealizerHandler.get({ realizer: afterResult_css_realizer?.output?.["realizer"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unknown_realizer" -> notfound', async () => {
      if (typeof themeRealizerHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(themeRealizerHandler.get({ realizer: "missing-realizer" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('listTargets', () => {
    it('builds a valid StorageProgram', () => {
      const program = themeRealizerHandler.listTargets({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = themeRealizerHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = themeRealizerHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = themeRealizerHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = themeRealizerHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof themeRealizerHandler.listTargets !== 'function') return;
      const result = await interpret(themeRealizerHandler.listTargets({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof themeRealizerHandler.listTargets !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_css_realizer = await interpret(themeRealizerHandler.register({ realizer: "realizer-css", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control","floating-panel","menu-item"], capabilities: ["density-variants","state-selectors"], description: "Generate contract CSS and manifest output for web targets." }), storage);
      const _pool = Object.assign({}, (afterResult_css_realizer?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(themeRealizerHandler.listTargets({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then realize succeeds", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(themeRealizerHandler.register({ realizer: "test-r", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control"], capabilities: ["density-variants"], description: "Generate contract CSS and manifest output." }), storage);
      expect(registerResult0.variant).toBe("ok");
      let realizer = registerResult0.output["realizer"];
      let r = realizer;
      const thenResult0 = await interpret(themeRealizerHandler.realize({ realizer: r, theme: "{\"id\":\"light\"}", contracts: "{\"contracts\":[]}", lenses: "{\"lenses\":[]}", context: "{}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate register returns duplicate", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(themeRealizerHandler.register({ realizer: "test-r", target: "web-css", outputKinds: ["ContractManifest"], contractSupport: ["field-control"], capabilities: ["density-variants"], description: "Generate contract manifest output." }), storage);
      expect(registerResult0.variant).toBe("ok");
      let realizer = registerResult0.output["realizer"];
      let r = realizer;
      const thenResult0 = await interpret(themeRealizerHandler.register({ realizer: r, target: "web-css", outputKinds: ["ContractManifest"], contractSupport: ["field-control"], capabilities: ["density-variants"], description: "Generate contract manifest output." }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

    it("register then get returns metadata", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(themeRealizerHandler.register({ realizer: "test-r", target: "web-css", outputKinds: ["ContractManifest","ContractCss"], contractSupport: ["field-control"], capabilities: ["density-variants"], description: "Generate contract CSS." }), storage);
      expect(registerResult0.variant).toBe("ok");
      let realizer = registerResult0.output["realizer"];
      let r = realizer;
      const thenResult0 = await interpret(themeRealizerHandler.get({ realizer: r }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("listTargets reflects registrations", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(themeRealizerHandler.register({ realizer: "test-r", target: "web-css", outputKinds: ["ContractManifest"], contractSupport: ["field-control"], capabilities: ["density-variants"], description: "CSS output." }), storage);
      expect(registerResult0.variant).toBe("ok");
      let realizer = registerResult0.output["realizer"];
      let r = realizer;
      const thenResult0 = await interpret(themeRealizerHandler.listTargets({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: registered realizers have targets', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ realizer: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }), outputKinds: fc.string(), contractSupport: fc.string(), capabilities: fc.string(), description: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('realize'), input: fc.record({ realizer: fc.string(), theme: fc.string({ minLength: 1, maxLength: 50 }), contracts: fc.string({ minLength: 1, maxLength: 50 }), lenses: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ realizer: fc.string() }) }),
              fc.record({ action: fc.constant('listTargets'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = themeRealizerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(themeRealizerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: registered realizers declare outputs', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ realizer: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }), outputKinds: fc.string(), contractSupport: fc.string(), capabilities: fc.string(), description: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('realize'), input: fc.record({ realizer: fc.string(), theme: fc.string({ minLength: 1, maxLength: 50 }), contracts: fc.string({ minLength: 1, maxLength: 50 }), lenses: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ realizer: fc.string() }) }),
              fc.record({ action: fc.constant('listTargets'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = themeRealizerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(themeRealizerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: registered realizers have descriptions', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ realizer: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }), outputKinds: fc.string(), contractSupport: fc.string(), capabilities: fc.string(), description: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('realize'), input: fc.record({ realizer: fc.string(), theme: fc.string({ minLength: 1, maxLength: 50 }), contracts: fc.string({ minLength: 1, maxLength: 50 }), lenses: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ realizer: fc.string() }) }),
              fc.record({ action: fc.constant('listTargets'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = themeRealizerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(themeRealizerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphan realizer without target', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ realizer: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }), outputKinds: fc.string(), contractSupport: fc.string(), capabilities: fc.string(), description: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('realize'), input: fc.record({ realizer: fc.string(), theme: fc.string({ minLength: 1, maxLength: 50 }), contracts: fc.string({ minLength: 1, maxLength: 50 }), lenses: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ realizer: fc.string() }) }),
              fc.record({ action: fc.constant('listTargets'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = themeRealizerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(themeRealizerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphan realizer without target
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
