// DeadBranchProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { deadBranchProviderHandler } from '../../handlers/ts/monadic/providers/dead-branch-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('DeadBranchProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('analyze', () => {
    it('builds a valid StorageProgram', () => {
      const program = deadBranchProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"branch\",\"condition\":false,\"thenBranch\":{\"instructions\":[]},\"elseBranch\":{\"instructions\":[]}}]}", constraints: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deadBranchProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"branch\",\"condition\":false,\"thenBranch\":{\"instructions\":[]},\"elseBranch\":{\"instructions\":[]}}]}", constraints: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deadBranchProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"branch\",\"condition\":false,\"thenBranch\":{\"instructions\":[]},\"elseBranch\":{\"instructions\":[]}}]}", constraints: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deadBranchProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"branch\",\"condition\":false,\"thenBranch\":{\"instructions\":[]},\"elseBranch\":{\"instructions\":[]}}]}", constraints: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deadBranchProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"branch\",\"condition\":false,\"thenBranch\":{\"instructions\":[]},\"elseBranch\":{\"instructions\":[]}}]}", constraints: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deadBranchProviderHandler.analyze !== 'function') return;
      const result = await interpret(deadBranchProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"branch\",\"condition\":false,\"thenBranch\":{\"instructions\":[]},\"elseBranch\":{\"instructions\":[]}}]}", constraints: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "false_condition" -> ok', async () => {
      if (typeof deadBranchProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deadBranchProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"branch\",\"condition\":false,\"thenBranch\":{\"instructions\":[]},\"elseBranch\":{\"instructions\":[]}}]}", constraints: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "no_branches" -> ok', async () => {
      if (typeof deadBranchProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deadBranchProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"get\",\"relation\":\"users\",\"key\":\"u1\",\"bindAs\":\"user\"}]}", constraints: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_program" -> error', async () => {
      if (typeof deadBranchProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deadBranchProviderHandler.analyze({ program: "", constraints: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof deadBranchProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = deadBranchProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('DeadBranchProvider');
    });
  });

  describe('invariant examples', () => {
    it("false condition produces one dead branch", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(deadBranchProviderHandler.analyze({ program: "branch(false, thenP, elseP)", constraints: "{}" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let result = analyzeResult0.output["result"];
      let d = result;
      let deadBranches = analyzeResult0.output["deadBranches"];
      let db = deadBranches;
      let reachableCount = analyzeResult0.output["reachableCount"];
      let rc = reachableCount;
      let totalCount = analyzeResult0.output["totalCount"];
      let tc = totalCount;
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: reachable count does not exceed total', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('analyze'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), constraints: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = deadBranchProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(deadBranchProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: dead branches exceed total branches', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('analyze'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), constraints: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = deadBranchProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(deadBranchProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: dead branches exceed total branches
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('analyze handles empty input: ', async () => {
      if (typeof deadBranchProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(deadBranchProviderHandler.analyze({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('analyze ensures on ok: ', async () => {
      if (typeof deadBranchProviderHandler.analyze !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), constraints: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = deadBranchProviderHandler.analyze(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
