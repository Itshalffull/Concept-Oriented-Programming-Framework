// Retriever Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { retrieverHandler } from '../../handlers/ts/repertoire/llm-rag/retriever.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Retriever functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = retrieverHandler.create({ retriever_type: 'test-retriever_type', source_ids: 'test', top_k: 1, score_threshold: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retrieverHandler.create({ retriever_type: 'test-retriever_type', source_ids: 'test', top_k: 1, score_threshold: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retrieverHandler.create({ retriever_type: 'test-retriever_type', source_ids: 'test', top_k: 1, score_threshold: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retrieverHandler.create({ retriever_type: 'test-retriever_type', source_ids: 'test', top_k: 1, score_threshold: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retrieverHandler.create({ retriever_type: 'test-retriever_type', source_ids: 'test', top_k: 1, score_threshold: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retrieverHandler.create !== 'function') return;
      const result = await interpret(retrieverHandler.create({ retriever_type: 'test-retriever_type', source_ids: 'test', top_k: 1, score_threshold: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('retrieve', () => {
    it('builds a valid StorageProgram', () => {
      const program = retrieverHandler.retrieve({ retriever: 'test', query: 'test-query' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retrieverHandler.retrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retrieverHandler.retrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retrieverHandler.retrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retrieverHandler.retrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retrieverHandler.retrieve !== 'function') return;
      const result = await interpret(retrieverHandler.retrieve({ retriever: 'test', query: 'test-query' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('multiQueryRetrieve', () => {
    it('builds a valid StorageProgram', () => {
      const program = retrieverHandler.multiQueryRetrieve({ retriever: 'test', query: 'test-query' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retrieverHandler.multiQueryRetrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retrieverHandler.multiQueryRetrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retrieverHandler.multiQueryRetrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retrieverHandler.multiQueryRetrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retrieverHandler.multiQueryRetrieve !== 'function') return;
      const result = await interpret(retrieverHandler.multiQueryRetrieve({ retriever: 'test', query: 'test-query' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('selfQueryRetrieve', () => {
    it('builds a valid StorageProgram', () => {
      const program = retrieverHandler.selfQueryRetrieve({ retriever: 'test', query: 'test-query' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retrieverHandler.selfQueryRetrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retrieverHandler.selfQueryRetrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retrieverHandler.selfQueryRetrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retrieverHandler.selfQueryRetrieve({ retriever: 'test', query: 'test-query' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retrieverHandler.selfQueryRetrieve !== 'function') return;
      const result = await interpret(retrieverHandler.selfQueryRetrieve({ retriever: 'test', query: 'test-query' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('rerank', () => {
    it('builds a valid StorageProgram', () => {
      const program = retrieverHandler.rerank({ retriever: 'test', query: 'test-query', documents: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retrieverHandler.rerank({ retriever: 'test', query: 'test-query', documents: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retrieverHandler.rerank({ retriever: 'test', query: 'test-query', documents: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retrieverHandler.rerank({ retriever: 'test', query: 'test-query', documents: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retrieverHandler.rerank({ retriever: 'test', query: 'test-query', documents: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retrieverHandler.rerank !== 'function') return;
      const result = await interpret(retrieverHandler.rerank({ retriever: 'test', query: 'test-query', documents: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('compress', () => {
    it('builds a valid StorageProgram', () => {
      const program = retrieverHandler.compress({ retriever: 'test', query: 'test-query', documents: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retrieverHandler.compress({ retriever: 'test', query: 'test-query', documents: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retrieverHandler.compress({ retriever: 'test', query: 'test-query', documents: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retrieverHandler.compress({ retriever: 'test', query: 'test-query', documents: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retrieverHandler.compress({ retriever: 'test', query: 'test-query', documents: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retrieverHandler.compress !== 'function') return;
      const result = await interpret(retrieverHandler.compress({ retriever: 'test', query: 'test-query', documents: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('setReranker', () => {
    it('builds a valid StorageProgram', () => {
      const program = retrieverHandler.setReranker({ retriever: 'test', model: 'test-model', top_n: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retrieverHandler.setReranker({ retriever: 'test', model: 'test-model', top_n: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retrieverHandler.setReranker({ retriever: 'test', model: 'test-model', top_n: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retrieverHandler.setReranker({ retriever: 'test', model: 'test-model', top_n: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retrieverHandler.setReranker({ retriever: 'test', model: 'test-model', top_n: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retrieverHandler.setReranker !== 'function') return;
      const result = await interpret(retrieverHandler.setReranker({ retriever: 'test', model: 'test-model', top_n: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof retrieverHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = retrieverHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Retriever');
    });
  });

  describe('invariant examples', () => {
    it("create-then-retrieve", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(retrieverHandler.create({ retriever_type: "vector", source_ids: ["idx1"], top_k: 5, score_threshold: "test-_" }), storage);
      expect(createResult0.variant).toBe("ok");
      let retriever = createResult0.output["retriever"];
      let r = retriever;
      const thenResult0 = await interpret(retrieverHandler.retrieve({ retriever: r, query: "test" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-retriever_type', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ retriever_type: fc.string({ minLength: 1, maxLength: 50 }), source_ids: fc.string(), top_k: fc.integer({ min: 1, max: 1000 }), score_threshold: fc.string() }) }),
              fc.record({ action: fc.constant('retrieve'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('multiQueryRetrieve'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('selfQueryRetrieve'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rerank'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }), documents: fc.string() }) }),
              fc.record({ action: fc.constant('compress'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }), documents: fc.string() }) }),
              fc.record({ action: fc.constant('setReranker'), input: fc.record({ retriever: fc.string(), model: fc.string({ minLength: 1, maxLength: 50 }), top_n: fc.integer({ min: 1, max: 1000 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = retrieverHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(retrieverHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-source_ids', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ retriever_type: fc.string({ minLength: 1, maxLength: 50 }), source_ids: fc.string(), top_k: fc.integer({ min: 1, max: 1000 }), score_threshold: fc.string() }) }),
              fc.record({ action: fc.constant('retrieve'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('multiQueryRetrieve'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('selfQueryRetrieve'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rerank'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }), documents: fc.string() }) }),
              fc.record({ action: fc.constant('compress'), input: fc.record({ retriever: fc.string(), query: fc.string({ minLength: 1, maxLength: 50 }), documents: fc.string() }) }),
              fc.record({ action: fc.constant('setReranker'), input: fc.record({ retriever: fc.string(), model: fc.string({ minLength: 1, maxLength: 50 }), top_n: fc.integer({ min: 1, max: 1000 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = retrieverHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(retrieverHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-source_ids
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof retrieverHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(retrieverHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof retrieverHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ retriever_type: fc.string({ minLength: 1, maxLength: 50 }), source_ids: fc.string(), top_k: fc.integer({ min: 1, max: 1000 }), score_threshold: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = retrieverHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
