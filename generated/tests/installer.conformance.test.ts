// Installer Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { installerHandler } from '../../handlers/ts/installer.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('Installer imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('stage', () => {
    it('executes successfully', async () => {
      if (typeof installerHandler.stage !== 'function') return;
      const result = await installerHandler.stage({ lockfile_entries: 'test', project_root: 'test-project_root' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('activate', () => {
    it('executes successfully', async () => {
      if (typeof installerHandler.activate !== 'function') return;
      const result = await installerHandler.activate({ installation: 'test' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('rollback', () => {
    it('executes successfully', async () => {
      if (typeof installerHandler.rollback !== 'function') return;
      const result = await installerHandler.rollback({ installation: 'test' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('clean', () => {
    it('executes successfully', async () => {
      if (typeof installerHandler.clean !== 'function') return;
      const result = await installerHandler.clean({ keep_generations: 1 }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invariant examples', () => {
    it("stage lifecycle", async () => {
      const storage = createInMemoryStorage();
      const stageResult0 = await installerHandler.stage({ lockfile_entries: {"type":"variable","name":"entries"}, project_root: {"type":"variable","name":"root"} }, storage);
      expect(stageResult0.variant).toBe("ok");
      const installation = stageResult0.output["installation"];
      expect(iResult.output["active"]).toBe({"type":"literal","value":false});
      expect(iResult.output["generation"]).toBeGreaterThan({"type":"literal","value":0});
    });

    it("stage then activate", async () => {
      const storage = createInMemoryStorage();
      const stageResult0 = await installerHandler.stage({ lockfile_entries: {"type":"variable","name":"entries"}, project_root: {"type":"variable","name":"root"} }, storage);
      expect(stageResult0.variant).toBe("ok");
      const installation = stageResult0.output["installation"];
      const thenResult0 = await installerHandler.activate({ installation: {"type":"variable","name":"i"} }, storage);
      expect(thenResult0.variant).toBe("ok");
      expect(iResult.output["active"]).toBe({"type":"literal","value":true});
      const thenResult2 = await installerHandler.rollback({ installation: {"type":"variable","name":"i"} }, storage);
      expect(thenResult2.variant).toBe("ok");
      expect(prevResult.output["active"]).toBe({"type":"literal","value":true});
      expect(iResult.output["active"]).toBe({"type":"literal","value":false});
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: installations have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('stage'), input: fc.record({ lockfile_entries: fc.string(), project_root: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('activate'), input: fc.record({ installation: fc.string() }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ installation: fc.string() }) }),
              fc.record({ action: fc.constant('clean'), input: fc.record({ keep_generations: fc.integer({ min: 0, max: 1000 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = installerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await actionFn.call(installerHandler, step.input as Record<string, unknown>, storage);
                expect(result.variant).toBeDefined();
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in installations', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('stage'), input: fc.record({ lockfile_entries: fc.string(), project_root: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('activate'), input: fc.record({ installation: fc.string() }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ installation: fc.string() }) }),
              fc.record({ action: fc.constant('clean'), input: fc.record({ keep_generations: fc.integer({ min: 0, max: 1000 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = installerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await actionFn.call(installerHandler, step.input as Record<string, unknown>, storage);
                expect(result.variant).toBeDefined();
                // Never: orphaned entry in installations
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('stage requires: ', async () => {
      const storage = createInMemoryStorage();
      const result = await installerHandler.stage({  }, storage);
      expect(['error', 'invalid', 'missing', 'notFound']).toContain(result.variant);
    });

    it('stage ensures on ok: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ lockfile_entries: fc.string(), project_root: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await installerHandler.stage(input as Record<string, unknown>, storage);
            fc.pre(result.variant === "ok");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

    it('activate ensures on ok: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ installation: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await installerHandler.activate(input as Record<string, unknown>, storage);
            fc.pre(result.variant === "ok");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

    it('rollback ensures on ok: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ installation: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await installerHandler.rollback(input as Record<string, unknown>, storage);
            fc.pre(result.variant === "ok");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
