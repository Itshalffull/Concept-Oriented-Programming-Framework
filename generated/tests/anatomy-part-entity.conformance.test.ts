// AnatomyPartEntity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { anatomyPartEntityHandler } from '../../handlers/ts/score/anatomy-part-entity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('AnatomyPartEntity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof anatomyPartEntityHandler.register !== 'function') return;
      const result = await interpret(anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_dialog_root" -> ok', async () => {
      if (typeof anatomyPartEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_widget" -> ok', async () => {
      if (typeof anatomyPartEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(anatomyPartEntityHandler.register({ widget: "", name: "root", role: "container", required: "true" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('findByRole', () => {
    it('builds a valid StorageProgram', () => {
      const program = anatomyPartEntityHandler.findByRole({ role: "container" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = anatomyPartEntityHandler.findByRole({ role: "container" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = anatomyPartEntityHandler.findByRole({ role: "container" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = anatomyPartEntityHandler.findByRole({ role: "container" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = anatomyPartEntityHandler.findByRole({ role: "container" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof anatomyPartEntityHandler.findByRole !== 'function') return;
      const result = await interpret(anatomyPartEntityHandler.findByRole({ role: "container" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_containers" -> ok', async () => {
      if (typeof anatomyPartEntityHandler.findByRole !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_dialog_root = await interpret(anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" }), storage);
      const _pool = Object.assign({}, (afterResult_register_dialog_root?.output ?? {}));
      const _fixtureInput = { role: "container" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(anatomyPartEntityHandler.findByRole({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unknown_role" -> error', async () => {
      if (typeof anatomyPartEntityHandler.findByRole !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(anatomyPartEntityHandler.findByRole({ role: "nonexistent-role" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findBoundToField', () => {
    it('builds a valid StorageProgram', () => {
      const program = anatomyPartEntityHandler.findBoundToField({ field: "title" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = anatomyPartEntityHandler.findBoundToField({ field: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = anatomyPartEntityHandler.findBoundToField({ field: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = anatomyPartEntityHandler.findBoundToField({ field: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = anatomyPartEntityHandler.findBoundToField({ field: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof anatomyPartEntityHandler.findBoundToField !== 'function') return;
      const result = await interpret(anatomyPartEntityHandler.findBoundToField({ field: "title" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_title_bindings" -> ok', async () => {
      if (typeof anatomyPartEntityHandler.findBoundToField !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_dialog_root = await interpret(anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" }), storage);
      const _pool = Object.assign({}, (afterResult_register_dialog_root?.output ?? {}));
      const _fixtureInput = { field: "title" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(anatomyPartEntityHandler.findBoundToField({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unbound_field" -> error', async () => {
      if (typeof anatomyPartEntityHandler.findBoundToField !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(anatomyPartEntityHandler.findBoundToField({ field: "nonexistent-field" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findBoundToAction', () => {
    it('builds a valid StorageProgram', () => {
      const program = anatomyPartEntityHandler.findBoundToAction({ action: "submit" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = anatomyPartEntityHandler.findBoundToAction({ action: "submit" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = anatomyPartEntityHandler.findBoundToAction({ action: "submit" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = anatomyPartEntityHandler.findBoundToAction({ action: "submit" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = anatomyPartEntityHandler.findBoundToAction({ action: "submit" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof anatomyPartEntityHandler.findBoundToAction !== 'function') return;
      const result = await interpret(anatomyPartEntityHandler.findBoundToAction({ action: "submit" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_submit_bindings" -> ok', async () => {
      if (typeof anatomyPartEntityHandler.findBoundToAction !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_dialog_root = await interpret(anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" }), storage);
      const _pool = Object.assign({}, (afterResult_register_dialog_root?.output ?? {}));
      const _fixtureInput = { action: "submit" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(anatomyPartEntityHandler.findBoundToAction({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unbound_action" -> error', async () => {
      if (typeof anatomyPartEntityHandler.findBoundToAction !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(anatomyPartEntityHandler.findBoundToAction({ action: "nonexistent-action" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = anatomyPartEntityHandler.get({ part: {"type":"ref","fixture":"register_dialog_root","field":"part"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = anatomyPartEntityHandler.get({ part: {"type":"ref","fixture":"register_dialog_root","field":"part"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = anatomyPartEntityHandler.get({ part: {"type":"ref","fixture":"register_dialog_root","field":"part"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = anatomyPartEntityHandler.get({ part: {"type":"ref","fixture":"register_dialog_root","field":"part"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = anatomyPartEntityHandler.get({ part: {"type":"ref","fixture":"register_dialog_root","field":"part"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof anatomyPartEntityHandler.get !== 'function') return;
      const result = await interpret(anatomyPartEntityHandler.get({ part: {"type":"ref","fixture":"register_dialog_root","field":"part"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing_part" -> ok', async () => {
      if (typeof anatomyPartEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_dialog_root = await interpret(anatomyPartEntityHandler.register({ widget: "dialog", name: "root", role: "container", required: "true" }), storage);
      const result = await interpret(anatomyPartEntityHandler.get({ part: afterResult_register_dialog_root?.output?.["part"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_nonexistent_part" -> error', async () => {
      if (typeof anatomyPartEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(anatomyPartEntityHandler.get({ part: "nonexistent-part" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(anatomyPartEntityHandler.register({ widget: {"type":"literal","value":"dialog"}, name: {"type":"literal","value":"root"}, role: {"type":"literal","value":"container"}, required: {"type":"literal","value":"true"} }), storage);
      expect(registerResult0.variant).toBe("ok");
      let part = registerResult0.output["part"];
      const thenResult0 = await interpret(anatomyPartEntityHandler.get({ part: {"type":"variable","name":"a"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every parts entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), role: fc.string({ minLength: 1, maxLength: 50 }), required: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByRole'), input: fc.record({ role: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBoundToField'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBoundToAction'), input: fc.record({ action: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ part: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = anatomyPartEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(anatomyPartEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in parts', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), role: fc.string({ minLength: 1, maxLength: 50 }), required: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByRole'), input: fc.record({ role: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBoundToField'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBoundToAction'), input: fc.record({ action: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ part: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = anatomyPartEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(anatomyPartEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty name in parts
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof anatomyPartEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(anatomyPartEntityHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof anatomyPartEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), role: fc.string({ minLength: 1, maxLength: 50 }), required: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = anatomyPartEntityHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
