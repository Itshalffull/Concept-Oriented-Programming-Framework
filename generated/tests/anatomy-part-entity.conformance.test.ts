// AnatomyPartEntity Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { anatomyPartEntityHandler } from '../../handlers/ts/score/anatomy-part-entity.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('AnatomyPartEntity imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('executes successfully', async () => {
      if (typeof anatomyPartEntityHandler.register !== 'function') return;
      const result = await anatomyPartEntityHandler.register({ widget: 'test-widget', name: 'test-name', role: 'test-role', required: 'test-required' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('findByRole', () => {
    it('executes successfully', async () => {
      if (typeof anatomyPartEntityHandler.findByRole !== 'function') return;
      const result = await anatomyPartEntityHandler.findByRole({ role: 'test-role' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('findBoundToField', () => {
    it('executes successfully', async () => {
      if (typeof anatomyPartEntityHandler.findBoundToField !== 'function') return;
      const result = await anatomyPartEntityHandler.findBoundToField({ field: 'test-field' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('findBoundToAction', () => {
    it('executes successfully', async () => {
      if (typeof anatomyPartEntityHandler.findBoundToAction !== 'function') return;
      const result = await anatomyPartEntityHandler.findBoundToAction({ action: 'test-action' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('get', () => {
    it('executes successfully', async () => {
      if (typeof anatomyPartEntityHandler.get !== 'function') return;
      const result = await anatomyPartEntityHandler.get({ part: 'test' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await anatomyPartEntityHandler.register({ widget: {"type":"literal","value":"dialog"}, name: {"type":"literal","value":"root"}, role: {"type":"literal","value":"container"}, required: {"type":"literal","value":"true"} }, storage);
      expect(registerResult0.variant).toBe("ok");
      const part = registerResult0.output["part"];
      const thenResult0 = await anatomyPartEntityHandler.get({ part: {"type":"variable","name":"a"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every parts entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 20 }), name: fc.string({ minLength: 1, maxLength: 20 }), role: fc.string({ minLength: 1, maxLength: 20 }), required: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findByRole'), input: fc.record({ role: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findBoundToField'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findBoundToAction'), input: fc.record({ action: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ part: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = anatomyPartEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await actionFn.call(anatomyPartEntityHandler, step.input as Record<string, unknown>, storage);
                expect(result.variant).toBeDefined();
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in parts', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 20 }), name: fc.string({ minLength: 1, maxLength: 20 }), role: fc.string({ minLength: 1, maxLength: 20 }), required: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findByRole'), input: fc.record({ role: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findBoundToField'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findBoundToAction'), input: fc.record({ action: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ part: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = anatomyPartEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await actionFn.call(anatomyPartEntityHandler, step.input as Record<string, unknown>, storage);
                expect(result.variant).toBeDefined();
                // Never: empty name in parts
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register requires: ', async () => {
      const storage = createInMemoryStorage();
      const result = await anatomyPartEntityHandler.register({  }, storage);
      expect(['error', 'invalid', 'missing', 'notFound']).toContain(result.variant);
    });

    it('register ensures on ok: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), role: fc.string({ minLength: 1, maxLength: 50 }), required: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await anatomyPartEntityHandler.register(input as Record<string, unknown>, storage);
            fc.pre(result.variant === "ok");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
