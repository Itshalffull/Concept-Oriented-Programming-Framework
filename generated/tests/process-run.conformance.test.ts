// ProcessRun Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { processRunHandler } from '../../handlers/ts/process-foundation/process-run.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ProcessRun functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('start', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.start({ spec_ref: 'test-spec_ref', spec_version: 1, input: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.start({ spec_ref: 'test-spec_ref', spec_version: 1, input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.start({ spec_ref: 'test-spec_ref', spec_version: 1, input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.start({ spec_ref: 'test-spec_ref', spec_version: 1, input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.start({ spec_ref: 'test-spec_ref', spec_version: 1, input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processRunHandler.start !== 'function') return;
      const result = await interpret(processRunHandler.start({ spec_ref: 'test-spec_ref', spec_version: 1, input: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('start_child', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.start_child({ spec_ref: 'test-spec_ref', spec_version: 1, parent_run: 'test-parent_run', input: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.start_child({ spec_ref: 'test-spec_ref', spec_version: 1, parent_run: 'test-parent_run', input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.start_child({ spec_ref: 'test-spec_ref', spec_version: 1, parent_run: 'test-parent_run', input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.start_child({ spec_ref: 'test-spec_ref', spec_version: 1, parent_run: 'test-parent_run', input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.start_child({ spec_ref: 'test-spec_ref', spec_version: 1, parent_run: 'test-parent_run', input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processRunHandler.start_child !== 'function') return;
      const result = await interpret(processRunHandler.start_child({ spec_ref: 'test-spec_ref', spec_version: 1, parent_run: 'test-parent_run', input: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('complete', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.complete({ run: 'test', output: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.complete({ run: 'test', output: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.complete({ run: 'test', output: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.complete({ run: 'test', output: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.complete({ run: 'test', output: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processRunHandler.complete !== 'function') return;
      const result = await interpret(processRunHandler.complete({ run: 'test', output: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('fail', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.fail({ run: 'test', error: 'test-error' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.fail({ run: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.fail({ run: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.fail({ run: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.fail({ run: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processRunHandler.fail !== 'function') return;
      const result = await interpret(processRunHandler.fail({ run: 'test', error: 'test-error' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('cancel', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.cancel({ run: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.cancel({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.cancel({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.cancel({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.cancel({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processRunHandler.cancel !== 'function') return;
      const result = await interpret(processRunHandler.cancel({ run: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('suspend', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.suspend({ run: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.suspend({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.suspend({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.suspend({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.suspend({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processRunHandler.suspend !== 'function') return;
      const result = await interpret(processRunHandler.suspend({ run: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('resume', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.resume({ run: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.resume({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.resume({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.resume({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.resume({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processRunHandler.resume !== 'function') return;
      const result = await interpret(processRunHandler.resume({ run: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('get_status', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.get_status({ run: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.get_status({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.get_status({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.get_status({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.get_status({ run: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof processRunHandler.get_status !== 'function') return;
      const result = await interpret(processRunHandler.get_status({ run: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('replay', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.replay({ original_run: 'test-run', start_from_step: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.replay({ original_run: 'test-run', start_from_step: null });
      if (!program?.instructions) return;
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.replay({ original_run: 'test-run', start_from_step: null });
      if (!program?.instructions) return;
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.replay({ original_run: 'test-run', start_from_step: null });
      if (!program?.instructions) return;
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.replay({ original_run: 'test-run', start_from_step: null });
      if (!program?.instructions) return;
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('returns error for missing original_run', async () => {
      const result = await interpret(
        processRunHandler.replay({ original_run: 'nonexistent-run', start_from_step: null }),
        storage,
      );
      expect(result.variant).toBe('error');
    });

    it('returns not_terminal when run is still active', async () => {
      // Start a run (it will be in running status)
      const startResult = await interpret(
        processRunHandler.start({ spec_ref: 'pipe', spec_version: 1, input: 'e30=' }),
        storage,
      );
      expect(startResult.variant).toBe('ok');
      const runId = startResult.output?.run ?? startResult.run;
      // Attempt replay on a non-terminal run
      const result = await interpret(
        processRunHandler.replay({ original_run: runId, start_from_step: null }),
        storage,
      );
      expect(result.variant).toBe('not_terminal');
    });

    it('returns ok when replaying a completed run', async () => {
      // Start then complete a run
      const startResult = await interpret(
        processRunHandler.start({ spec_ref: 'pipe', spec_version: 1, input: 'e30=' }),
        storage,
      );
      expect(startResult.variant).toBe('ok');
      const runId = startResult.output?.run ?? startResult.run;
      const completeResult = await interpret(
        processRunHandler.complete({ run: runId, output: 'cmpl' }),
        storage,
      );
      expect(completeResult.variant).toBe('ok');
      // Now replay the completed run
      const replayResult = await interpret(
        processRunHandler.replay({ original_run: runId, start_from_step: null }),
        storage,
      );
      expect(replayResult.variant).toBe('ok');
      expect(replayResult.output?.run ?? replayResult.run).toBeDefined();
    });

  });

  describe('attachContext', () => {
    it('builds a valid StorageProgram', () => {
      const program = processRunHandler.attachContext({ run: 'test-run', principal: 'svc@example.com', context: 'e30=' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = processRunHandler.attachContext({ run: 'test-run', principal: 'svc@example.com', context: 'e30=' });
      if (!program?.instructions) return;
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = processRunHandler.attachContext({ run: 'test-run', principal: 'svc@example.com', context: 'e30=' });
      if (!program?.instructions) return;
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = processRunHandler.attachContext({ run: 'test-run', principal: 'svc@example.com', context: 'e30=' });
      if (!program?.instructions) return;
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = processRunHandler.attachContext({ run: 'test-run', principal: 'svc@example.com', context: 'e30=' });
      if (!program?.instructions) return;
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('returns error for empty principal', async () => {
      const startResult = await interpret(
        processRunHandler.start({ spec_ref: 'pipe', spec_version: 1, input: 'e30=' }),
        storage,
      );
      const runId = startResult.output?.run ?? startResult.run;
      const result = await interpret(
        processRunHandler.attachContext({ run: runId, principal: '', context: 'e30=' }),
        storage,
      );
      expect(result.variant).toBe('error');
    });

    it('returns not_found for nonexistent run', async () => {
      const result = await interpret(
        processRunHandler.attachContext({ run: 'nonexistent-run', principal: 'svc@example.com', context: 'e30=' }),
        storage,
      );
      expect(result.variant).toBe('not_found');
    });

    it('returns ok and records principal on existing run', async () => {
      const startResult = await interpret(
        processRunHandler.start({ spec_ref: 'pipe', spec_version: 1, input: 'e30=' }),
        storage,
      );
      expect(startResult.variant).toBe('ok');
      const runId = startResult.output?.run ?? startResult.run;
      const result = await interpret(
        processRunHandler.attachContext({ run: runId, principal: 'svc-account@example.com', context: 'eyJrZXkiOiJ2YWwifQ==' }),
        storage,
      );
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof processRunHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = processRunHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ProcessRun');
    });
  });

  describe('invariant examples', () => {
    it("start then get_status", async () => {
      const storage = createInMemoryStorage();
      const startResult0 = await interpret(processRunHandler.start({ spec_ref: "onboard", spec_version: 1, input: "test-d" }), storage);
      expect(startResult0.variant).toBe("ok");
      let run = startResult0.output["run"];
      let r = run;
      let spec_ref = startResult0.output["spec_ref"];
      const thenResult0 = await interpret(processRunHandler.get_status({ run: r }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(processRunHandler.complete({ run: r, output: "test-o" }), storage);
      expect(thenResult1.variant).toBe("ok");
      const thenResult2 = await interpret(processRunHandler.get_status({ run: r }), storage);
      expect(thenResult2.variant).toBe("ok");
    });

    it("replay a completed run", async () => {
      const storage = createInMemoryStorage();
      const startResult = await interpret(processRunHandler.start({ spec_ref: "pipeline", spec_version: 2, input: "test-inp" }), storage);
      expect(startResult.variant).toBe("ok");
      const r = startResult.output?.run ?? startResult.run;
      const completeResult = await interpret(processRunHandler.complete({ run: r, output: "test-out" }), storage);
      expect(completeResult.variant).toBe("ok");
      const replayResult = await interpret(processRunHandler.replay({ original_run: r, start_from_step: null }), storage);
      expect(replayResult.variant).toBe("ok");
      const r2 = replayResult.output?.run ?? replayResult.run;
      expect(r2).toBeDefined();
      expect(r2).not.toBe(r);
    });

    it("attachContext records principal", async () => {
      const storage = createInMemoryStorage();
      const startResult = await interpret(processRunHandler.start({ spec_ref: "pipeline", spec_version: 1, input: "test-inp" }), storage);
      expect(startResult.variant).toBe("ok");
      const r = startResult.output?.run ?? startResult.run;
      const attachResult = await interpret(
        processRunHandler.attachContext({ run: r, principal: "svc@example.com", context: "test-ctx" }),
        storage,
      );
      expect(attachResult.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: runs have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ spec_ref: fc.string({ minLength: 1, maxLength: 50 }), spec_version: fc.integer({ min: 1, max: 1000 }), input: fc.string() }) }),
              fc.record({ action: fc.constant('start_child'), input: fc.record({ spec_ref: fc.string({ minLength: 1, maxLength: 50 }), spec_version: fc.integer({ min: 1, max: 1000 }), parent_run: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string() }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ run: fc.string(), output: fc.string() }) }),
              fc.record({ action: fc.constant('fail'), input: fc.record({ run: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('cancel'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('suspend'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('resume'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('get_status'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('replay'), input: fc.record({ original_run: fc.string(), start_from_step: fc.option(fc.string()) }) }),
              fc.record({ action: fc.constant('attachContext'), input: fc.record({ run: fc.string(), principal: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = processRunHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(processRunHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in runs', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ spec_ref: fc.string({ minLength: 1, maxLength: 50 }), spec_version: fc.integer({ min: 1, max: 1000 }), input: fc.string() }) }),
              fc.record({ action: fc.constant('start_child'), input: fc.record({ spec_ref: fc.string({ minLength: 1, maxLength: 50 }), spec_version: fc.integer({ min: 1, max: 1000 }), parent_run: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string() }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ run: fc.string(), output: fc.string() }) }),
              fc.record({ action: fc.constant('fail'), input: fc.record({ run: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('cancel'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('suspend'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('resume'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('get_status'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('replay'), input: fc.record({ original_run: fc.string(), start_from_step: fc.option(fc.string()) }) }),
              fc.record({ action: fc.constant('attachContext'), input: fc.record({ run: fc.string(), principal: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = processRunHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(processRunHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in runs
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('start handles empty input: ', async () => {
      if (typeof processRunHandler.start !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(processRunHandler.start({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('start ensures on ok: ', async () => {
      if (typeof processRunHandler.start !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ spec_ref: fc.string({ minLength: 1, maxLength: 50 }), spec_version: fc.integer({ min: 1, max: 1000 }), input: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = processRunHandler.start(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('start_child handles empty input: ', async () => {
      if (typeof processRunHandler.start_child !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(processRunHandler.start_child({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('start_child ensures on ok: ', async () => {
      if (typeof processRunHandler.start_child !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ spec_ref: fc.string({ minLength: 1, maxLength: 50 }), spec_version: fc.integer({ min: 1, max: 1000 }), parent_run: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = processRunHandler.start_child(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('complete ensures on ok: ', async () => {
      if (typeof processRunHandler.complete !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ run: fc.string(), output: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = processRunHandler.complete(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
