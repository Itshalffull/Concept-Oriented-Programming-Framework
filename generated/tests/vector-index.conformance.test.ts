// VectorIndex Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { vectorIndexHandler } from '../../handlers/ts/repertoire/llm-rag/vector-index.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('VectorIndex functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = vectorIndexHandler.create({ dimensions: 1, distance_metric: 'test-distance_metric', index_type: 'test-index_type', backend: 'test-backend', embedding_model: 'test-embedding_model' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vectorIndexHandler.create({ dimensions: 1, distance_metric: 'test-distance_metric', index_type: 'test-index_type', backend: 'test-backend', embedding_model: 'test-embedding_model' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vectorIndexHandler.create({ dimensions: 1, distance_metric: 'test-distance_metric', index_type: 'test-index_type', backend: 'test-backend', embedding_model: 'test-embedding_model' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vectorIndexHandler.create({ dimensions: 1, distance_metric: 'test-distance_metric', index_type: 'test-index_type', backend: 'test-backend', embedding_model: 'test-embedding_model' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vectorIndexHandler.create({ dimensions: 1, distance_metric: 'test-distance_metric', index_type: 'test-index_type', backend: 'test-backend', embedding_model: 'test-embedding_model' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vectorIndexHandler.create !== 'function') return;
      const result = await interpret(vectorIndexHandler.create({ dimensions: 1, distance_metric: 'test-distance_metric', index_type: 'test-index_type', backend: 'test-backend', embedding_model: 'test-embedding_model' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('embed', () => {
    it('builds a valid StorageProgram', () => {
      const program = vectorIndexHandler.embed({ index: 'test', text: 'test-text' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vectorIndexHandler.embed({ index: 'test', text: 'test-text' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vectorIndexHandler.embed({ index: 'test', text: 'test-text' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vectorIndexHandler.embed({ index: 'test', text: 'test-text' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vectorIndexHandler.embed({ index: 'test', text: 'test-text' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vectorIndexHandler.embed !== 'function') return;
      const result = await interpret(vectorIndexHandler.embed({ index: 'test', text: 'test-text' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('embedBatch', () => {
    it('builds a valid StorageProgram', () => {
      const program = vectorIndexHandler.embedBatch({ index: 'test', texts: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vectorIndexHandler.embedBatch({ index: 'test', texts: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vectorIndexHandler.embedBatch({ index: 'test', texts: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vectorIndexHandler.embedBatch({ index: 'test', texts: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vectorIndexHandler.embedBatch({ index: 'test', texts: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vectorIndexHandler.embedBatch !== 'function') return;
      const result = await interpret(vectorIndexHandler.embedBatch({ index: 'test', texts: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('add', () => {
    it('builds a valid StorageProgram', () => {
      const program = vectorIndexHandler.add({ index: 'test', id: 'test-id', vector: 'test', metadata: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vectorIndexHandler.add({ index: 'test', id: 'test-id', vector: 'test', metadata: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vectorIndexHandler.add({ index: 'test', id: 'test-id', vector: 'test', metadata: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vectorIndexHandler.add({ index: 'test', id: 'test-id', vector: 'test', metadata: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vectorIndexHandler.add({ index: 'test', id: 'test-id', vector: 'test', metadata: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vectorIndexHandler.add !== 'function') return;
      const result = await interpret(vectorIndexHandler.add({ index: 'test', id: 'test-id', vector: 'test', metadata: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('addBatch', () => {
    it('builds a valid StorageProgram', () => {
      const program = vectorIndexHandler.addBatch({ index: 'test', items: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vectorIndexHandler.addBatch({ index: 'test', items: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vectorIndexHandler.addBatch({ index: 'test', items: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vectorIndexHandler.addBatch({ index: 'test', items: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vectorIndexHandler.addBatch({ index: 'test', items: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vectorIndexHandler.addBatch !== 'function') return;
      const result = await interpret(vectorIndexHandler.addBatch({ index: 'test', items: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('search', () => {
    it('builds a valid StorageProgram', () => {
      const program = vectorIndexHandler.search({ index: 'test', query_vector: 'test', k: 1, filters: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vectorIndexHandler.search({ index: 'test', query_vector: 'test', k: 1, filters: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vectorIndexHandler.search({ index: 'test', query_vector: 'test', k: 1, filters: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vectorIndexHandler.search({ index: 'test', query_vector: 'test', k: 1, filters: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vectorIndexHandler.search({ index: 'test', query_vector: 'test', k: 1, filters: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vectorIndexHandler.search !== 'function') return;
      const result = await interpret(vectorIndexHandler.search({ index: 'test', query_vector: 'test', k: 1, filters: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('hybridSearch', () => {
    it('builds a valid StorageProgram', () => {
      const program = vectorIndexHandler.hybridSearch({ index: 'test', query_vector: 'test', keyword_query: 'test-keyword_query', vector_weight: 1, k: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vectorIndexHandler.hybridSearch({ index: 'test', query_vector: 'test', keyword_query: 'test-keyword_query', vector_weight: 1, k: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vectorIndexHandler.hybridSearch({ index: 'test', query_vector: 'test', keyword_query: 'test-keyword_query', vector_weight: 1, k: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vectorIndexHandler.hybridSearch({ index: 'test', query_vector: 'test', keyword_query: 'test-keyword_query', vector_weight: 1, k: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vectorIndexHandler.hybridSearch({ index: 'test', query_vector: 'test', keyword_query: 'test-keyword_query', vector_weight: 1, k: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vectorIndexHandler.hybridSearch !== 'function') return;
      const result = await interpret(vectorIndexHandler.hybridSearch({ index: 'test', query_vector: 'test', keyword_query: 'test-keyword_query', vector_weight: 1, k: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('mmrSearch', () => {
    it('builds a valid StorageProgram', () => {
      const program = vectorIndexHandler.mmrSearch({ index: 'test', query_vector: 'test', k: 1, diversity: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vectorIndexHandler.mmrSearch({ index: 'test', query_vector: 'test', k: 1, diversity: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vectorIndexHandler.mmrSearch({ index: 'test', query_vector: 'test', k: 1, diversity: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vectorIndexHandler.mmrSearch({ index: 'test', query_vector: 'test', k: 1, diversity: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vectorIndexHandler.mmrSearch({ index: 'test', query_vector: 'test', k: 1, diversity: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vectorIndexHandler.mmrSearch !== 'function') return;
      const result = await interpret(vectorIndexHandler.mmrSearch({ index: 'test', query_vector: 'test', k: 1, diversity: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('delete', () => {
    it('builds a valid StorageProgram', () => {
      const program = vectorIndexHandler.delete({ index: 'test', ids: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vectorIndexHandler.delete({ index: 'test', ids: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vectorIndexHandler.delete({ index: 'test', ids: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vectorIndexHandler.delete({ index: 'test', ids: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vectorIndexHandler.delete({ index: 'test', ids: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vectorIndexHandler.delete !== 'function') return;
      const result = await interpret(vectorIndexHandler.delete({ index: 'test', ids: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof vectorIndexHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = vectorIndexHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('VectorIndex');
    });
  });

  describe('invariant examples', () => {
    it("create-then-search", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(vectorIndexHandler.create({ dimensions: 1536, distance_metric: "cosine", index_type: "hnsw", backend: "qdrant", embedding_model: "text-embedding-3-small" }), storage);
      expect(createResult0.variant).toBe("ok");
      let index = createResult0.output["index"];
      let x = index;
      const addResult1 = await interpret(vectorIndexHandler.add({ index: x, id: "doc1", vector: "test-v", metadata: "test-_" }), storage);
      expect(addResult1.variant).toBe("ok");
      const thenResult0 = await interpret(vectorIndexHandler.search({ index: x, query_vector: "test-v", k: 1, filters: "test-_" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-distance_metric', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ dimensions: fc.integer({ min: 1, max: 1000 }), distance_metric: fc.string({ minLength: 1, maxLength: 50 }), index_type: fc.string({ minLength: 1, maxLength: 50 }), backend: fc.string({ minLength: 1, maxLength: 50 }), embedding_model: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('embed'), input: fc.record({ index: fc.string(), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('embedBatch'), input: fc.record({ index: fc.string(), texts: fc.string() }) }),
              fc.record({ action: fc.constant('add'), input: fc.record({ index: fc.string(), id: fc.string({ minLength: 1, maxLength: 50 }), vector: fc.string(), metadata: fc.string() }) }),
              fc.record({ action: fc.constant('addBatch'), input: fc.record({ index: fc.string(), items: fc.string() }) }),
              fc.record({ action: fc.constant('search'), input: fc.record({ index: fc.string(), query_vector: fc.string(), k: fc.integer({ min: 1, max: 1000 }), filters: fc.string() }) }),
              fc.record({ action: fc.constant('hybridSearch'), input: fc.record({ index: fc.string(), query_vector: fc.string(), keyword_query: fc.string({ minLength: 1, maxLength: 50 }), vector_weight: fc.string(), k: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('mmrSearch'), input: fc.record({ index: fc.string(), query_vector: fc.string(), k: fc.integer({ min: 1, max: 1000 }), diversity: fc.string() }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ index: fc.string(), ids: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = vectorIndexHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(vectorIndexHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-dimensions', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ dimensions: fc.integer({ min: 1, max: 1000 }), distance_metric: fc.string({ minLength: 1, maxLength: 50 }), index_type: fc.string({ minLength: 1, maxLength: 50 }), backend: fc.string({ minLength: 1, maxLength: 50 }), embedding_model: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('embed'), input: fc.record({ index: fc.string(), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('embedBatch'), input: fc.record({ index: fc.string(), texts: fc.string() }) }),
              fc.record({ action: fc.constant('add'), input: fc.record({ index: fc.string(), id: fc.string({ minLength: 1, maxLength: 50 }), vector: fc.string(), metadata: fc.string() }) }),
              fc.record({ action: fc.constant('addBatch'), input: fc.record({ index: fc.string(), items: fc.string() }) }),
              fc.record({ action: fc.constant('search'), input: fc.record({ index: fc.string(), query_vector: fc.string(), k: fc.integer({ min: 1, max: 1000 }), filters: fc.string() }) }),
              fc.record({ action: fc.constant('hybridSearch'), input: fc.record({ index: fc.string(), query_vector: fc.string(), keyword_query: fc.string({ minLength: 1, maxLength: 50 }), vector_weight: fc.string(), k: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('mmrSearch'), input: fc.record({ index: fc.string(), query_vector: fc.string(), k: fc.integer({ min: 1, max: 1000 }), diversity: fc.string() }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ index: fc.string(), ids: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = vectorIndexHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(vectorIndexHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-dimensions
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof vectorIndexHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(vectorIndexHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof vectorIndexHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ dimensions: fc.integer({ min: 1, max: 1000 }), distance_metric: fc.string({ minLength: 1, maxLength: 50 }), index_type: fc.string({ minLength: 1, maxLength: 50 }), backend: fc.string({ minLength: 1, maxLength: 50 }), embedding_model: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = vectorIndexHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
