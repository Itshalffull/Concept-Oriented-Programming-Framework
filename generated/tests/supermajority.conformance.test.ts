// Supermajority Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { supermajorityHandler } from '../../handlers/ts/app/governance/supermajority.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('Supermajority functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = supermajorityHandler.configure({ threshold: 1, roundingMode: 'test-roundingMode', abstentionsCount: true });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = supermajorityHandler.configure({ threshold: 1, roundingMode: 'test-roundingMode', abstentionsCount: true });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = supermajorityHandler.configure({ threshold: 1, roundingMode: 'test-roundingMode', abstentionsCount: true });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = supermajorityHandler.configure({ threshold: 1, roundingMode: 'test-roundingMode', abstentionsCount: true });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = supermajorityHandler.configure({ threshold: 1, roundingMode: 'test-roundingMode', abstentionsCount: true });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof supermajorityHandler.configure !== 'function') return;
      try {
        const result = await interpret(supermajorityHandler.configure({ threshold: 1, roundingMode: 'test-roundingMode', abstentionsCount: true }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('count', () => {
    it('builds a valid StorageProgram', () => {
      const program = supermajorityHandler.count({ config: 'test', ballots: 'test-ballots', weights: 'test-weights' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = supermajorityHandler.count({ config: 'test', ballots: 'test-ballots', weights: 'test-weights' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = supermajorityHandler.count({ config: 'test', ballots: 'test-ballots', weights: 'test-weights' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = supermajorityHandler.count({ config: 'test', ballots: 'test-ballots', weights: 'test-weights' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = supermajorityHandler.count({ config: 'test', ballots: 'test-ballots', weights: 'test-weights' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof supermajorityHandler.count !== 'function') return;
      try {
        const result = await interpret(supermajorityHandler.count({ config: 'test', ballots: 'test-ballots', weights: 'test-weights' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("configure-then-count", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(supermajorityHandler.configure({ threshold: {"type":"literal","value":0.6667}, roundingMode: {"type":"literal","value":"Ceil"}, abstentionsCount: {"type":"literal","value":false} }), storage);
      expect(configureResult0.variant).toBe("configured");
      const config = configureResult0.output["config"];
      const thenResult0 = await interpret(supermajorityHandler.count({ config: {"type":"variable","name":"sm"}, ballots: {"type":"variable","name":"_"}, weights: {"type":"variable","name":"_"} }), storage);
      expect(thenResult0.variant).toBe("winner");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-threshold', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ threshold: fc.string(), roundingMode: fc.string({ minLength: 1, maxLength: 20 }), abstentionsCount: fc.boolean() }) }),
              fc.record({ action: fc.constant('count'), input: fc.record({ config: fc.string(), ballots: fc.string({ minLength: 1, maxLength: 20 }), weights: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = supermajorityHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(supermajorityHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-threshold', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ threshold: fc.string(), roundingMode: fc.string({ minLength: 1, maxLength: 20 }), abstentionsCount: fc.boolean() }) }),
              fc.record({ action: fc.constant('count'), input: fc.record({ config: fc.string(), ballots: fc.string({ minLength: 1, maxLength: 20 }), weights: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = supermajorityHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(supermajorityHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                  // Never: orphaned-threshold
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof supermajorityHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(supermajorityHandler.configure({  }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('configure ensures on configured: ', async () => {
      if (typeof supermajorityHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ threshold: fc.string(), roundingMode: fc.string({ minLength: 1, maxLength: 50 }), abstentionsCount: fc.boolean() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = supermajorityHandler.configure(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            if (result.variant === "configured") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
