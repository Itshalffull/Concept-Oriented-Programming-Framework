// E2EEProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { e2eeProviderHandler } from '../../handlers/ts/view/e2ee-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('E2EEProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('decrypt', () => {
    it('builds a valid StorageProgram', () => {
      const program = e2eeProviderHandler.decrypt({ rows: [{"ciphertext":"dGVzdA=="}], keyRef: "vault-key-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = e2eeProviderHandler.decrypt({ rows: [{"ciphertext":"dGVzdA=="}], keyRef: "vault-key-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = e2eeProviderHandler.decrypt({ rows: [{"ciphertext":"dGVzdA=="}], keyRef: "vault-key-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = e2eeProviderHandler.decrypt({ rows: [{"ciphertext":"dGVzdA=="}], keyRef: "vault-key-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = e2eeProviderHandler.decrypt({ rows: [{"ciphertext":"dGVzdA=="}], keyRef: "vault-key-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof e2eeProviderHandler.decrypt !== 'function') return;
      const result = await interpret(e2eeProviderHandler.decrypt({ rows: [{"ciphertext":"dGVzdA=="}], keyRef: "vault-key-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "decrypt_ok" -> ok', async () => {
      if (typeof e2eeProviderHandler.decrypt !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_load_key = await interpret(e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" }), storage);
      const _pool = Object.assign({}, (afterResult_load_key?.output ?? {}));
      const _fixtureInput = { rows: [{"ciphertext":"dGVzdA=="}], keyRef: "vault-key-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(e2eeProviderHandler.decrypt({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "decrypt_missing_key" -> notfound', async () => {
      if (typeof e2eeProviderHandler.decrypt !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(e2eeProviderHandler.decrypt({ rows: [{"ciphertext":"dGVzdA=="}], keyRef: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "decrypt_invalid_rows" -> error', async () => {
      if (typeof e2eeProviderHandler.decrypt !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(e2eeProviderHandler.decrypt({ rows: "not-json", keyRef: "vault-key-1" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('loadKey', () => {
    it('builds a valid StorageProgram', () => {
      const program = e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof e2eeProviderHandler.loadKey !== 'function') return;
      const result = await interpret(e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "load_key" -> ok', async () => {
      if (typeof e2eeProviderHandler.loadKey !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "load_duplicate" -> duplicate', async () => {
      if (typeof e2eeProviderHandler.loadKey !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_load_key = await interpret(e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" }), storage);
      const _pool = Object.assign({}, (afterResult_load_key?.output ?? {}));
      const _fixtureInput = { keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(e2eeProviderHandler.loadKey({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "load_empty_ref" -> error', async () => {
      if (typeof e2eeProviderHandler.loadKey !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(e2eeProviderHandler.loadKey({ keyRef: "", keyMaterial: "abc" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "load_empty_material" -> error', async () => {
      if (typeof e2eeProviderHandler.loadKey !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('unloadKey', () => {
    it('builds a valid StorageProgram', () => {
      const program = e2eeProviderHandler.unloadKey({ keyRef: "vault-key-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = e2eeProviderHandler.unloadKey({ keyRef: "vault-key-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = e2eeProviderHandler.unloadKey({ keyRef: "vault-key-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = e2eeProviderHandler.unloadKey({ keyRef: "vault-key-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = e2eeProviderHandler.unloadKey({ keyRef: "vault-key-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof e2eeProviderHandler.unloadKey !== 'function') return;
      const result = await interpret(e2eeProviderHandler.unloadKey({ keyRef: "vault-key-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "unload_ok" -> ok', async () => {
      if (typeof e2eeProviderHandler.unloadKey !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_load_key = await interpret(e2eeProviderHandler.loadKey({ keyRef: "vault-key-1", keyMaterial: "c2VjcmV0LWtleS1tYXRlcmlhbA==" }), storage);
      const _pool = Object.assign({}, (afterResult_load_key?.output ?? {}));
      const _fixtureInput = { keyRef: "vault-key-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(e2eeProviderHandler.unloadKey({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unload_missing" -> notfound', async () => {
      if (typeof e2eeProviderHandler.unloadKey !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(e2eeProviderHandler.unloadKey({ keyRef: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof e2eeProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = e2eeProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('E2EEProvider');
    });
  });

  describe('invariant examples', () => {
    it("loadKey then decrypt succeeds", async () => {
      const storage = createInMemoryStorage();
    });

    it("decrypt without key returns notfound", async () => {
      const storage = createInMemoryStorage();
    });

    it("unload removes key from decrypt path", async () => {
      const storage = createInMemoryStorage();
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: loaded keys have non-empty refs', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('decrypt'), input: fc.record({ rows: fc.string({ minLength: 1, maxLength: 50 }), keyRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('loadKey'), input: fc.record({ keyRef: fc.string({ minLength: 1, maxLength: 50 }), keyMaterial: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unloadKey'), input: fc.record({ keyRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = e2eeProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(e2eeProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
