// Replica Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { replicaHandler } from '../../handlers/ts/replica.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Replica functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('localUpdate', () => {
    it('builds a valid StorageProgram', () => {
      const program = replicaHandler.localUpdate({ op: "insert:hello-world" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = replicaHandler.localUpdate({ op: "insert:hello-world" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = replicaHandler.localUpdate({ op: "insert:hello-world" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = replicaHandler.localUpdate({ op: "insert:hello-world" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = replicaHandler.localUpdate({ op: "insert:hello-world" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof replicaHandler.localUpdate !== 'function') return;
      const result = await interpret(replicaHandler.localUpdate({ op: "insert:hello-world" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "update_insert_op" -> ok', async () => {
      if (typeof replicaHandler.localUpdate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(replicaHandler.localUpdate({ op: "insert:hello-world" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "update_empty_op" -> error', async () => {
      if (typeof replicaHandler.localUpdate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(replicaHandler.localUpdate({ op: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('receiveRemote', () => {
    it('builds a valid StorageProgram', () => {
      const program = replicaHandler.receiveRemote({ op: "update:field=value", fromReplica: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = replicaHandler.receiveRemote({ op: "update:field=value", fromReplica: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = replicaHandler.receiveRemote({ op: "update:field=value", fromReplica: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = replicaHandler.receiveRemote({ op: "update:field=value", fromReplica: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = replicaHandler.receiveRemote({ op: "update:field=value", fromReplica: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof replicaHandler.receiveRemote !== 'function') return;
      const result = await interpret(replicaHandler.receiveRemote({ op: "update:field=value", fromReplica: {"type":"ref","fixture":"update_insert_op","field":"newState"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "receive_from_known_peer" -> ok', async () => {
      if (typeof replicaHandler.receiveRemote !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_update_insert_op = await interpret(replicaHandler.localUpdate({ op: "insert:hello-world" }), storage);
      const result = await interpret(replicaHandler.receiveRemote({ op: "update:field=value", fromReplica: afterResult_update_insert_op?.output?.["newState"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "receive_from_unknown_peer" -> error', async () => {
      if (typeof replicaHandler.receiveRemote !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(replicaHandler.receiveRemote({ op: "update:field=value", fromReplica: "unknown-replica-999" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('sync', () => {
    it('builds a valid StorageProgram', () => {
      const program = replicaHandler.sync({ peer: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = replicaHandler.sync({ peer: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = replicaHandler.sync({ peer: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = replicaHandler.sync({ peer: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = replicaHandler.sync({ peer: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof replicaHandler.sync !== 'function') return;
      const result = await interpret(replicaHandler.sync({ peer: {"type":"ref","fixture":"update_insert_op","field":"newState"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "sync_known_peer" -> ok', async () => {
      if (typeof replicaHandler.sync !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_update_insert_op = await interpret(replicaHandler.localUpdate({ op: "insert:hello-world" }), storage);
      const result = await interpret(replicaHandler.sync({ peer: afterResult_update_insert_op?.output?.["newState"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "sync_unknown_peer" -> error', async () => {
      if (typeof replicaHandler.sync !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(replicaHandler.sync({ peer: "unknown-peer-999" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('getState', () => {
    it('builds a valid StorageProgram', () => {
      const program = replicaHandler.getState({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = replicaHandler.getState({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = replicaHandler.getState({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = replicaHandler.getState({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = replicaHandler.getState({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof replicaHandler.getState !== 'function') return;
      const result = await interpret(replicaHandler.getState({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof replicaHandler.getState !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_update_insert_op = await interpret(replicaHandler.localUpdate({ op: "insert:hello-world" }), storage);
      const _pool = Object.assign({}, (afterResult_update_insert_op?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(replicaHandler.getState({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('fork', () => {
    it('builds a valid StorageProgram', () => {
      const program = replicaHandler.fork({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = replicaHandler.fork({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = replicaHandler.fork({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = replicaHandler.fork({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = replicaHandler.fork({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof replicaHandler.fork !== 'function') return;
      const result = await interpret(replicaHandler.fork({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof replicaHandler.fork !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_update_insert_op = await interpret(replicaHandler.localUpdate({ op: "insert:hello-world" }), storage);
      const _pool = Object.assign({}, (afterResult_update_insert_op?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(replicaHandler.fork({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('addPeer', () => {
    it('builds a valid StorageProgram', () => {
      const program = replicaHandler.addPeer({ peerId: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = replicaHandler.addPeer({ peerId: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = replicaHandler.addPeer({ peerId: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = replicaHandler.addPeer({ peerId: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = replicaHandler.addPeer({ peerId: {"type":"ref","fixture":"update_insert_op","field":"newState"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof replicaHandler.addPeer !== 'function') return;
      const result = await interpret(replicaHandler.addPeer({ peerId: {"type":"ref","fixture":"update_insert_op","field":"newState"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "add_new_peer" -> ok', async () => {
      if (typeof replicaHandler.addPeer !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_update_insert_op = await interpret(replicaHandler.localUpdate({ op: "insert:hello-world" }), storage);
      const result = await interpret(replicaHandler.addPeer({ peerId: afterResult_update_insert_op?.output?.["newState"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_empty_peer" -> error', async () => {
      if (typeof replicaHandler.addPeer !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_update_insert_op = await interpret(replicaHandler.localUpdate({ op: "insert:hello-world" }), storage);
      const _pool = Object.assign({}, (afterResult_update_insert_op?.output ?? {}));
      const _fixtureInput = { peerId: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(replicaHandler.addPeer({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof replicaHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = replicaHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Replica');
    });
  });

  describe('invariant examples', () => {
    it("localUpdate-then-getState", async () => {
      const storage = createInMemoryStorage();
      const localUpdateResult0 = await interpret(replicaHandler.localUpdate({ op: "test-o" }), storage);
      expect(localUpdateResult0.variant).toBe("ok");
      let newState = localUpdateResult0.output["newState"];
      let n = newState;
      const thenResult0 = await interpret(replicaHandler.getState({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-replicaId', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('localUpdate'), input: fc.record({ op: fc.string() }) }),
              fc.record({ action: fc.constant('receiveRemote'), input: fc.record({ op: fc.string(), fromReplica: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sync'), input: fc.record({ peer: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getState'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('fork'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('addPeer'), input: fc.record({ peerId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = replicaHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(replicaHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('addPeer handles empty input: ', async () => {
      if (typeof replicaHandler.addPeer !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(replicaHandler.addPeer({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('addPeer ensures on ok: ', async () => {
      if (typeof replicaHandler.addPeer !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ peerId: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = replicaHandler.addPeer(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
