// Auditor Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { auditorHandler } from '../../handlers/ts/auditor.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Auditor functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('audit', () => {
    it('builds a valid StorageProgram', () => {
      const program = auditorHandler.audit({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = auditorHandler.audit({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = auditorHandler.audit({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = auditorHandler.audit({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = auditorHandler.audit({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof auditorHandler.audit !== 'function') return;
      const result = await interpret(auditorHandler.audit({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "audit_clean_deps" -> ok', async () => {
      if (typeof auditorHandler.audit !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_diff_same_audit = await interpret(auditorHandler.diff({ old_audit: "audit-1", new_audit: "audit-1" }), storage);
      const _pool = Object.assign({}, (afterResult_diff_same_audit?.output ?? {}));
      const _fixtureInput = { lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(auditorHandler.audit({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "audit_empty_entries" -> error', async () => {
      if (typeof auditorHandler.audit !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(auditorHandler.audit({ lockfile_entries: [] }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('checkPolicy', () => {
    it('builds a valid StorageProgram', () => {
      const program = auditorHandler.checkPolicy({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"}], policy: {"allowed_licenses":["MIT","Apache-2.0","ISC"],"denied_namespaces":[],"max_severity":"critical"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = auditorHandler.checkPolicy({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"}], policy: {"allowed_licenses":["MIT","Apache-2.0","ISC"],"denied_namespaces":[],"max_severity":"critical"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = auditorHandler.checkPolicy({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"}], policy: {"allowed_licenses":["MIT","Apache-2.0","ISC"],"denied_namespaces":[],"max_severity":"critical"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = auditorHandler.checkPolicy({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"}], policy: {"allowed_licenses":["MIT","Apache-2.0","ISC"],"denied_namespaces":[],"max_severity":"critical"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = auditorHandler.checkPolicy({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"}], policy: {"allowed_licenses":["MIT","Apache-2.0","ISC"],"denied_namespaces":[],"max_severity":"critical"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof auditorHandler.checkPolicy !== 'function') return;
      const result = await interpret(auditorHandler.checkPolicy({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"}], policy: {"allowed_licenses":["MIT","Apache-2.0","ISC"],"denied_namespaces":[],"max_severity":"critical"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "check_permissive_policy" -> ok', async () => {
      if (typeof auditorHandler.checkPolicy !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_audit_clean_deps = await interpret(auditorHandler.audit({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] }), storage);
      const _pool = Object.assign({}, (afterResult_audit_clean_deps?.output ?? {}));
      const _fixtureInput = { lockfile_entries: [{"module_id":"lodash","version":"4.17.21"}], policy: {"allowed_licenses":["MIT","Apache-2.0","ISC"],"denied_namespaces":[],"max_severity":"critical"} } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(auditorHandler.checkPolicy({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_restrictive_policy" -> error', async () => {
      if (typeof auditorHandler.checkPolicy !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_audit_clean_deps = await interpret(auditorHandler.audit({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] }), storage);
      const _pool = Object.assign({}, (afterResult_audit_clean_deps?.output ?? {}));
      const _fixtureInput = { lockfile_entries: [{"module_id":"@evil/malware","version":"0.1.0"}], policy: {"allowed_licenses":["MIT"],"denied_namespaces":["@evil"],"max_severity":"low"} } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(auditorHandler.checkPolicy({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('diff', () => {
    it('builds a valid StorageProgram', () => {
      const program = auditorHandler.diff({ old_audit: "audit-1", new_audit: "audit-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = auditorHandler.diff({ old_audit: "audit-1", new_audit: "audit-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = auditorHandler.diff({ old_audit: "audit-1", new_audit: "audit-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = auditorHandler.diff({ old_audit: "audit-1", new_audit: "audit-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = auditorHandler.diff({ old_audit: "audit-1", new_audit: "audit-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof auditorHandler.diff !== 'function') return;
      const result = await interpret(auditorHandler.diff({ old_audit: "audit-1", new_audit: "audit-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "diff_same_audit" -> ok', async () => {
      if (typeof auditorHandler.diff !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_audit_clean_deps = await interpret(auditorHandler.audit({ lockfile_entries: [{"module_id":"lodash","version":"4.17.21"},{"module_id":"express","version":"4.18.2"}] }), storage);
      const _pool = Object.assign({}, (afterResult_audit_clean_deps?.output ?? {}));
      const _fixtureInput = { old_audit: "audit-1", new_audit: "audit-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(auditorHandler.diff({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "diff_missing_old" -> error', async () => {
      if (typeof auditorHandler.diff !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(auditorHandler.diff({ old_audit: "audit-nonexistent", new_audit: "audit-1" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof auditorHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = auditorHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Auditor');
    });
  });

  describe('invariant examples', () => {
    it("audit then diff", async () => {
      const storage = createInMemoryStorage();
      const auditResult0 = await interpret(auditorHandler.audit({ lockfile_entries: "test-entries" }), storage);
      expect(auditResult0.variant).toBe("ok");
      let audit = auditResult0.output["audit"];
      const thenResult0 = await interpret(auditorHandler.diff({ old_audit: "test-a", new_audit: "test-a" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("checkPolicy lifecycle", async () => {
      const storage = createInMemoryStorage();
      const checkPolicyResult0 = await interpret(auditorHandler.checkPolicy({ lockfile_entries: "test-entries", policy: {"type":"record","fields":[{"name":"allowed_licenses","value":{"type":"list","items":[{"type":"literal","value":"MIT"},{"type":"literal","value":"Apache-2.0"}]}},{"name":"denied_namespaces","value":{"type":"list","items":[]}},{"name":"max_severity","value":{"type":"literal","value":"critical"}}]} }), storage);
      expect(checkPolicyResult0.variant).toBe("ok");
      let audit = checkPolicyResult0.output["audit"];
      // Note: variable 'a' not found in step outputs
      expect(a).toBe({"type":"list","items":[]});
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: audits have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('audit'), input: fc.record({ lockfile_entries: fc.string() }) }),
              fc.record({ action: fc.constant('checkPolicy'), input: fc.record({ lockfile_entries: fc.string(), policy: fc.string() }) }),
              fc.record({ action: fc.constant('diff'), input: fc.record({ old_audit: fc.string(), new_audit: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = auditorHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(auditorHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in audits', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('audit'), input: fc.record({ lockfile_entries: fc.string() }) }),
              fc.record({ action: fc.constant('checkPolicy'), input: fc.record({ lockfile_entries: fc.string(), policy: fc.string() }) }),
              fc.record({ action: fc.constant('diff'), input: fc.record({ old_audit: fc.string(), new_audit: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = auditorHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(auditorHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in audits
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('diff ensures on ok: ', async () => {
      if (typeof auditorHandler.diff !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ old_audit: fc.string(), new_audit: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = auditorHandler.diff(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
