// ApprovalCounting Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { approvalCountingHandler } from '../../handlers/ts/governance-decision/approval-counting.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ApprovalCounting functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof approvalCountingHandler.configure !== 'function') return;
      const result = await interpret(approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "single_winner_unlimited" -> ok', async () => {
      if (typeof approvalCountingHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "multi_winner_capped" -> ok', async () => {
      if (typeof approvalCountingHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(approvalCountingHandler.configure({ maxApprovals: "3", winnerCount: "2" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "zero_winners" -> error', async () => {
      if (typeof approvalCountingHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('count', () => {
    it('builds a valid StorageProgram', () => {
      const program = approvalCountingHandler.count({ config: {"type":"ref","fixture":"single_winner_unlimited","field":"id"}, approvalSets: "[{\"voter\":\"alice\",\"approvals\":[\"A\",\"B\"]},{\"voter\":\"bob\",\"approvals\":[\"B\",\"C\"]}]", weights: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = approvalCountingHandler.count({ config: {"type":"ref","fixture":"single_winner_unlimited","field":"id"}, approvalSets: "[{\"voter\":\"alice\",\"approvals\":[\"A\",\"B\"]},{\"voter\":\"bob\",\"approvals\":[\"B\",\"C\"]}]", weights: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = approvalCountingHandler.count({ config: {"type":"ref","fixture":"single_winner_unlimited","field":"id"}, approvalSets: "[{\"voter\":\"alice\",\"approvals\":[\"A\",\"B\"]},{\"voter\":\"bob\",\"approvals\":[\"B\",\"C\"]}]", weights: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = approvalCountingHandler.count({ config: {"type":"ref","fixture":"single_winner_unlimited","field":"id"}, approvalSets: "[{\"voter\":\"alice\",\"approvals\":[\"A\",\"B\"]},{\"voter\":\"bob\",\"approvals\":[\"B\",\"C\"]}]", weights: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = approvalCountingHandler.count({ config: {"type":"ref","fixture":"single_winner_unlimited","field":"id"}, approvalSets: "[{\"voter\":\"alice\",\"approvals\":[\"A\",\"B\"]},{\"voter\":\"bob\",\"approvals\":[\"B\",\"C\"]}]", weights: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof approvalCountingHandler.count !== 'function') return;
      const result = await interpret(approvalCountingHandler.count({ config: {"type":"ref","fixture":"single_winner_unlimited","field":"id"}, approvalSets: "[{\"voter\":\"alice\",\"approvals\":[\"A\",\"B\"]},{\"voter\":\"bob\",\"approvals\":[\"B\",\"C\"]}]", weights: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "count_approvals" -> ok', async () => {
      if (typeof approvalCountingHandler.count !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_single_winner_unlimited = await interpret(approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "1" }), storage);
      const result = await interpret(approvalCountingHandler.count({ config: afterResult_single_winner_unlimited?.output?.["id"], approvalSets: "[{\"voter\":\"alice\",\"approvals\":[\"A\",\"B\"]},{\"voter\":\"bob\",\"approvals\":[\"B\",\"C\"]}]", weights: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "count_no_ballots" -> error', async () => {
      if (typeof approvalCountingHandler.count !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_single_winner_unlimited = await interpret(approvalCountingHandler.configure({ maxApprovals: null, winnerCount: "1" }), storage);
      const result = await interpret(approvalCountingHandler.count({ config: afterResult_single_winner_unlimited?.output?.["id"], approvalSets: "[]", weights: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof approvalCountingHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = approvalCountingHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ApprovalCounting');
    });
  });

  describe('invariant examples', () => {
    it("configure-then-count", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(approvalCountingHandler.configure({ maxApprovals: "test-_", winnerCount: 1 }), storage);
      expect(configureResult0.variant).toBe("ok");
      let config = configureResult0.output["config"];
      let ac = config;
      const thenResult0 = await interpret(approvalCountingHandler.count({ config: ac, approvalSets: "test-_", weights: "test-_" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-maxApprovals', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ maxApprovals: fc.string(), winnerCount: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('count'), input: fc.record({ config: fc.string(), approvalSets: fc.string({ minLength: 1, maxLength: 50 }), weights: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = approvalCountingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(approvalCountingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-winnerCount', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ maxApprovals: fc.string(), winnerCount: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('count'), input: fc.record({ config: fc.string(), approvalSets: fc.string({ minLength: 1, maxLength: 50 }), weights: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = approvalCountingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(approvalCountingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-winnerCount
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof approvalCountingHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(approvalCountingHandler.configure({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('configure ensures on ok: ', async () => {
      if (typeof approvalCountingHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ maxApprovals: fc.string(), winnerCount: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = approvalCountingHandler.configure(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
