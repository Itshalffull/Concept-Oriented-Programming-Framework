// Subject Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { subjectHandler } from '../../handlers/ts/app/identity/subject.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Subject functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof subjectHandler.register !== 'function') return;
      const result = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_human" -> ok', async () => {
      if (typeof subjectHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_agent" -> ok', async () => {
      if (typeof subjectHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.register({ subject: "subject-agent-001", kind: "agent", backingRef: "agent:agent-research-001", ownerRef: "subject-admin-001" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof subjectHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const result = await interpret(subjectHandler.register({ subject: afterResult_register_human?.output?.["subject"], kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "register_empty_kind" -> invalid', async () => {
      if (typeof subjectHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.register({ subject: "subject-bad-001", kind: "", backingRef: "user:bob", ownerRef: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "register_empty_ref" -> invalid', async () => {
      if (typeof subjectHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.register({ subject: "subject-bad-002", kind: "human", backingRef: "", ownerRef: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "register_bad_kind" -> invalid', async () => {
      if (typeof subjectHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.register({ subject: "subject-bad-003", kind: "robot", backingRef: "robot:r2d2", ownerRef: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('unify', () => {
    it('builds a valid StorageProgram', () => {
      const program = subjectHandler.unify({ subject: "subject-svc-001", kind: "service", backingRef: "service:api-gateway", ownerRef: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = subjectHandler.unify({ subject: "subject-svc-001", kind: "service", backingRef: "service:api-gateway", ownerRef: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = subjectHandler.unify({ subject: "subject-svc-001", kind: "service", backingRef: "service:api-gateway", ownerRef: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = subjectHandler.unify({ subject: "subject-svc-001", kind: "service", backingRef: "service:api-gateway", ownerRef: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = subjectHandler.unify({ subject: "subject-svc-001", kind: "service", backingRef: "service:api-gateway", ownerRef: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof subjectHandler.unify !== 'function') return;
      const result = await interpret(subjectHandler.unify({ subject: "subject-svc-001", kind: "service", backingRef: "service:api-gateway", ownerRef: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "unify_new" -> ok', async () => {
      if (typeof subjectHandler.unify !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const _pool = Object.assign({}, (afterResult_register_human?.output ?? {}));
      const _fixtureInput = { subject: "subject-svc-001", kind: "service", backingRef: "service:api-gateway", ownerRef: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(subjectHandler.unify({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unify_idempotent" -> ok', async () => {
      if (typeof subjectHandler.unify !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const afterResult_unify_new = await interpret(subjectHandler.unify({ subject: "subject-svc-001", kind: "service", backingRef: "service:api-gateway", ownerRef: null }), storage);
      const result = await interpret(subjectHandler.unify({ subject: afterResult_unify_new?.output?.["subject"], kind: "service", backingRef: "service:api-gateway", ownerRef: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unify_invalid_kind" -> invalid', async () => {
      if (typeof subjectHandler.unify !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.unify({ subject: "subject-bad-004", kind: "", backingRef: "service:x", ownerRef: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('updateStatus', () => {
    it('builds a valid StorageProgram', () => {
      const program = subjectHandler.updateStatus({ id: {"type":"ref","fixture":"register_human","field":"subject"}, status: "disabled" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = subjectHandler.updateStatus({ id: {"type":"ref","fixture":"register_human","field":"subject"}, status: "disabled" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = subjectHandler.updateStatus({ id: {"type":"ref","fixture":"register_human","field":"subject"}, status: "disabled" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = subjectHandler.updateStatus({ id: {"type":"ref","fixture":"register_human","field":"subject"}, status: "disabled" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = subjectHandler.updateStatus({ id: {"type":"ref","fixture":"register_human","field":"subject"}, status: "disabled" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof subjectHandler.updateStatus !== 'function') return;
      const result = await interpret(subjectHandler.updateStatus({ id: {"type":"ref","fixture":"register_human","field":"subject"}, status: "disabled" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "update_status_ok" -> ok', async () => {
      if (typeof subjectHandler.updateStatus !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const result = await interpret(subjectHandler.updateStatus({ id: afterResult_register_human?.output?.["subject"], status: "disabled" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "update_status_missing" -> notfound', async () => {
      if (typeof subjectHandler.updateStatus !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.updateStatus({ id: "subject-nonexistent", status: "disabled" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "update_status_empty" -> invalid', async () => {
      if (typeof subjectHandler.updateStatus !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const result = await interpret(subjectHandler.updateStatus({ id: afterResult_register_human?.output?.["subject"], status: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "update_status_bad" -> invalid', async () => {
      if (typeof subjectHandler.updateStatus !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const result = await interpret(subjectHandler.updateStatus({ id: afterResult_register_human?.output?.["subject"], status: "suspended" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('updateOwner', () => {
    it('builds a valid StorageProgram', () => {
      const program = subjectHandler.updateOwner({ id: {"type":"ref","fixture":"register_agent","field":"subject"}, ownerRef: "subject-admin-002" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = subjectHandler.updateOwner({ id: {"type":"ref","fixture":"register_agent","field":"subject"}, ownerRef: "subject-admin-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = subjectHandler.updateOwner({ id: {"type":"ref","fixture":"register_agent","field":"subject"}, ownerRef: "subject-admin-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = subjectHandler.updateOwner({ id: {"type":"ref","fixture":"register_agent","field":"subject"}, ownerRef: "subject-admin-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = subjectHandler.updateOwner({ id: {"type":"ref","fixture":"register_agent","field":"subject"}, ownerRef: "subject-admin-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof subjectHandler.updateOwner !== 'function') return;
      const result = await interpret(subjectHandler.updateOwner({ id: {"type":"ref","fixture":"register_agent","field":"subject"}, ownerRef: "subject-admin-002" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "update_owner_ok" -> ok', async () => {
      if (typeof subjectHandler.updateOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_agent = await interpret(subjectHandler.register({ subject: "subject-agent-001", kind: "agent", backingRef: "agent:agent-research-001", ownerRef: "subject-admin-001" }), storage);
      const result = await interpret(subjectHandler.updateOwner({ id: afterResult_register_agent?.output?.["subject"], ownerRef: "subject-admin-002" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "update_owner_missing" -> notfound', async () => {
      if (typeof subjectHandler.updateOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.updateOwner({ id: "subject-nonexistent", ownerRef: "subject-admin-001" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "update_owner_empty" -> invalid', async () => {
      if (typeof subjectHandler.updateOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_agent = await interpret(subjectHandler.register({ subject: "subject-agent-001", kind: "agent", backingRef: "agent:agent-research-001", ownerRef: "subject-admin-001" }), storage);
      const result = await interpret(subjectHandler.updateOwner({ id: afterResult_register_agent?.output?.["subject"], ownerRef: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = subjectHandler.get({ subject: {"type":"ref","fixture":"register_human","field":"subject"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = subjectHandler.get({ subject: {"type":"ref","fixture":"register_human","field":"subject"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = subjectHandler.get({ subject: {"type":"ref","fixture":"register_human","field":"subject"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = subjectHandler.get({ subject: {"type":"ref","fixture":"register_human","field":"subject"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = subjectHandler.get({ subject: {"type":"ref","fixture":"register_human","field":"subject"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof subjectHandler.get !== 'function') return;
      const result = await interpret(subjectHandler.get({ subject: {"type":"ref","fixture":"register_human","field":"subject"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof subjectHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const result = await interpret(subjectHandler.get({ subject: afterResult_register_human?.output?.["subject"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof subjectHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.get({ subject: "subject-nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = subjectHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = subjectHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = subjectHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = subjectHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = subjectHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof subjectHandler.list !== 'function') return;
      const result = await interpret(subjectHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_ok" -> ok', async () => {
      if (typeof subjectHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const _pool = Object.assign({}, (afterResult_register_human?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(subjectHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof subjectHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const _pool = Object.assign({}, (afterResult_register_human?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(subjectHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('listByKind', () => {
    it('builds a valid StorageProgram', () => {
      const program = subjectHandler.listByKind({ kind: "human" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = subjectHandler.listByKind({ kind: "human" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = subjectHandler.listByKind({ kind: "human" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = subjectHandler.listByKind({ kind: "human" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = subjectHandler.listByKind({ kind: "human" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof subjectHandler.listByKind !== 'function') return;
      const result = await interpret(subjectHandler.listByKind({ kind: "human" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_by_kind_ok" -> ok', async () => {
      if (typeof subjectHandler.listByKind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const _pool = Object.assign({}, (afterResult_register_human?.output ?? {}));
      const _fixtureInput = { kind: "human" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(subjectHandler.listByKind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_kind_empty" -> ok', async () => {
      if (typeof subjectHandler.listByKind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_human = await interpret(subjectHandler.register({ subject: "subject-admin-001", kind: "human", backingRef: "user:alice", ownerRef: null }), storage);
      const _pool = Object.assign({}, (afterResult_register_human?.output ?? {}));
      const _fixtureInput = { kind: "service" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(subjectHandler.listByKind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_kind_invalid" -> invalid', async () => {
      if (typeof subjectHandler.listByKind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(subjectHandler.listByKind({ kind: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });


  describe('invariant examples', () => {
    it("register-then-get", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(subjectHandler.register({ subject: "test-s", kind: "human", backingRef: "user:alice", ownerRef: false }), storage);
      expect(registerResult0.variant).toBe("ok");
      let subject = registerResult0.output["subject"];
      let s = subject;
      const thenResult0 = await interpret(subjectHandler.get({ subject: s }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("unify-is-idempotent", async () => {
      const storage = createInMemoryStorage();
      const unifyResult0 = await interpret(subjectHandler.unify({ subject: "test-s", kind: "agent", backingRef: "agent:r1", ownerRef: false }), storage);
      expect(unifyResult0.variant).toBe("ok");
      let subject = unifyResult0.output["subject"];
      let s = subject;
      const thenResult0 = await interpret(subjectHandler.unify({ subject: s, kind: "agent", backingRef: "agent:r1", ownerRef: false }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("status-update-visible", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(subjectHandler.register({ subject: "test-s", kind: "human", backingRef: "user:bob", ownerRef: false }), storage);
      expect(registerResult0.variant).toBe("ok");
      let subject = registerResult0.output["subject"];
      let s = subject;
      const updateStatusResult1 = await interpret(subjectHandler.updateStatus({ id: s, status: "disabled" }), storage);
      expect(updateStatusResult1.variant).toBe("ok");
      subject = updateStatusResult1.output["subject"];
      let status = updateStatusResult1.output["status"];
      const thenResult0 = await interpret(subjectHandler.get({ subject: s }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: subjects-have-valid-kind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ subject: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }), backingRef: fc.string({ minLength: 1, maxLength: 50 }), ownerRef: fc.string() }) }),
              fc.record({ action: fc.constant('unify'), input: fc.record({ subject: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }), backingRef: fc.string({ minLength: 1, maxLength: 50 }), ownerRef: fc.string() }) }),
              fc.record({ action: fc.constant('updateStatus'), input: fc.record({ id: fc.string(), status: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('updateOwner'), input: fc.record({ id: fc.string(), ownerRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ subject: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('listByKind'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = subjectHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(subjectHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: subjects-have-valid-status', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ subject: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }), backingRef: fc.string({ minLength: 1, maxLength: 50 }), ownerRef: fc.string() }) }),
              fc.record({ action: fc.constant('unify'), input: fc.record({ subject: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }), backingRef: fc.string({ minLength: 1, maxLength: 50 }), ownerRef: fc.string() }) }),
              fc.record({ action: fc.constant('updateStatus'), input: fc.record({ id: fc.string(), status: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('updateOwner'), input: fc.record({ id: fc.string(), ownerRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ subject: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('listByKind'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = subjectHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(subjectHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: subjects-have-empty-backing-ref', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ subject: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }), backingRef: fc.string({ minLength: 1, maxLength: 50 }), ownerRef: fc.string() }) }),
              fc.record({ action: fc.constant('unify'), input: fc.record({ subject: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }), backingRef: fc.string({ minLength: 1, maxLength: 50 }), ownerRef: fc.string() }) }),
              fc.record({ action: fc.constant('updateStatus'), input: fc.record({ id: fc.string(), status: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('updateOwner'), input: fc.record({ id: fc.string(), ownerRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ subject: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('listByKind'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = subjectHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(subjectHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: subjects-have-empty-backing-ref
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof subjectHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(subjectHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof subjectHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ subject: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }), backingRef: fc.string({ minLength: 1, maxLength: 50 }), ownerRef: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = subjectHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('unify handles empty input: ', async () => {
      if (typeof subjectHandler.unify !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(subjectHandler.unify({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('unify ensures on ok: ', async () => {
      if (typeof subjectHandler.unify !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ subject: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }), backingRef: fc.string({ minLength: 1, maxLength: 50 }), ownerRef: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = subjectHandler.unify(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
