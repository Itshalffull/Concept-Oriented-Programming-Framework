// InvariantExtractionProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { invariantExtractionProviderHandler } from '../../handlers/ts/monadic/providers/invariant-extraction-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('InvariantExtractionProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('extract', () => {
    it('builds a valid StorageProgram', () => {
      const program = invariantExtractionProviderHandler.extract({ program: "{\"instructions\":[{\"tag\":\"put\",\"relation\":\"users\",\"key\":\"u1\",\"value\":{\"name\":\"Alice\"}}]}", conceptSpec: "{\"state\":{\"users\":\"set U\"}}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = invariantExtractionProviderHandler.extract({ program: "{\"instructions\":[{\"tag\":\"put\",\"relation\":\"users\",\"key\":\"u1\",\"value\":{\"name\":\"Alice\"}}]}", conceptSpec: "{\"state\":{\"users\":\"set U\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = invariantExtractionProviderHandler.extract({ program: "{\"instructions\":[{\"tag\":\"put\",\"relation\":\"users\",\"key\":\"u1\",\"value\":{\"name\":\"Alice\"}}]}", conceptSpec: "{\"state\":{\"users\":\"set U\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = invariantExtractionProviderHandler.extract({ program: "{\"instructions\":[{\"tag\":\"put\",\"relation\":\"users\",\"key\":\"u1\",\"value\":{\"name\":\"Alice\"}}]}", conceptSpec: "{\"state\":{\"users\":\"set U\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = invariantExtractionProviderHandler.extract({ program: "{\"instructions\":[{\"tag\":\"put\",\"relation\":\"users\",\"key\":\"u1\",\"value\":{\"name\":\"Alice\"}}]}", conceptSpec: "{\"state\":{\"users\":\"set U\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof invariantExtractionProviderHandler.extract !== 'function') return;
      const result = await interpret(invariantExtractionProviderHandler.extract({ program: "{\"instructions\":[{\"tag\":\"put\",\"relation\":\"users\",\"key\":\"u1\",\"value\":{\"name\":\"Alice\"}}]}", conceptSpec: "{\"state\":{\"users\":\"set U\"}}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "extract_with_put" -> ok', async () => {
      if (typeof invariantExtractionProviderHandler.extract !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(invariantExtractionProviderHandler.extract({ program: "{\"instructions\":[{\"tag\":\"put\",\"relation\":\"users\",\"key\":\"u1\",\"value\":{\"name\":\"Alice\"}}]}", conceptSpec: "{\"state\":{\"users\":\"set U\"}}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "extract_with_get_only" -> ok', async () => {
      if (typeof invariantExtractionProviderHandler.extract !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(invariantExtractionProviderHandler.extract({ program: "get(users, u1); put(users, u1, data)", conceptSpec: "User { state { users: set U } }" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "extract_empty_program" -> error', async () => {
      if (typeof invariantExtractionProviderHandler.extract !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(invariantExtractionProviderHandler.extract({ program: "", conceptSpec: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof invariantExtractionProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = invariantExtractionProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('InvariantExtractionProvider');
    });
  });

  describe('invariant examples', () => {
    it("extraction produces non-empty properties", async () => {
      const storage = createInMemoryStorage();
      const extractResult0 = await interpret(invariantExtractionProviderHandler.extract({ program: {"type":"literal","value":"get(users, u1); put(users, u1, data)"}, conceptSpec: {"type":"literal","value":"User { state { users: set U } }"} }), storage);
      expect(extractResult0.variant).toBe("ok");
      let result = extractResult0.output["result"];
      let properties = extractResult0.output["properties"];
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: results reference a concept', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('extract'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), conceptSpec: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = invariantExtractionProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(invariantExtractionProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('extract handles empty input: ', async () => {
      if (typeof invariantExtractionProviderHandler.extract !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(invariantExtractionProviderHandler.extract({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('extract ensures on ok: ', async () => {
      if (typeof invariantExtractionProviderHandler.extract !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), conceptSpec: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = invariantExtractionProviderHandler.extract(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
