// DerivedEntity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { derivedEntityHandler } from '../../handlers/ts/score/derived-entity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('DerivedEntity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof derivedEntityHandler.register !== 'function') return;
      const result = await interpret(derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_trash" -> ok', async () => {
      if (typeof derivedEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_minimal" -> ok', async () => {
      if (typeof derivedEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(derivedEntityHandler.register({ name: "Inbox", source: "specs/inbox.derived", ast: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_name" -> error', async () => {
      if (typeof derivedEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(derivedEntityHandler.register({ name: "", source: "specs/empty.derived", ast: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = derivedEntityHandler.get({ name: "Trash" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = derivedEntityHandler.get({ name: "Trash" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = derivedEntityHandler.get({ name: "Trash" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = derivedEntityHandler.get({ name: "Trash" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = derivedEntityHandler.get({ name: "Trash" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof derivedEntityHandler.get !== 'function') return;
      const result = await interpret(derivedEntityHandler.get({ name: "Trash" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_trash" -> ok', async () => {
      if (typeof derivedEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_trash = await interpret(derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_trash?.output ?? {}));
      const _fixtureInput = { name: "Trash" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(derivedEntityHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> error', async () => {
      if (typeof derivedEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(derivedEntityHandler.get({ name: "NonexistentDerived" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByComposedConcept', () => {
    it('builds a valid StorageProgram', () => {
      const program = derivedEntityHandler.findByComposedConcept({ concept: "Article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = derivedEntityHandler.findByComposedConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = derivedEntityHandler.findByComposedConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = derivedEntityHandler.findByComposedConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = derivedEntityHandler.findByComposedConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof derivedEntityHandler.findByComposedConcept !== 'function') return;
      const result = await interpret(derivedEntityHandler.findByComposedConcept({ concept: "Article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_by_article" -> ok', async () => {
      if (typeof derivedEntityHandler.findByComposedConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_trash = await interpret(derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_trash?.output ?? {}));
      const _fixtureInput = { concept: "Article" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(derivedEntityHandler.findByComposedConcept({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_by_empty" -> error', async () => {
      if (typeof derivedEntityHandler.findByComposedConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(derivedEntityHandler.findByComposedConcept({ concept: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findBySync', () => {
    it('builds a valid StorageProgram', () => {
      const program = derivedEntityHandler.findBySync({ syncName: "trash-on-delete" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = derivedEntityHandler.findBySync({ syncName: "trash-on-delete" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = derivedEntityHandler.findBySync({ syncName: "trash-on-delete" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = derivedEntityHandler.findBySync({ syncName: "trash-on-delete" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = derivedEntityHandler.findBySync({ syncName: "trash-on-delete" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof derivedEntityHandler.findBySync !== 'function') return;
      const result = await interpret(derivedEntityHandler.findBySync({ syncName: "trash-on-delete" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_by_sync" -> ok', async () => {
      if (typeof derivedEntityHandler.findBySync !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_trash = await interpret(derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_trash?.output ?? {}));
      const _fixtureInput = { syncName: "trash-on-delete" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(derivedEntityHandler.findBySync({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_by_sync_empty" -> error', async () => {
      if (typeof derivedEntityHandler.findBySync !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(derivedEntityHandler.findBySync({ syncName: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('compositionTree', () => {
    it('builds a valid StorageProgram', () => {
      const program = derivedEntityHandler.compositionTree({ entity: "derived-entity-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = derivedEntityHandler.compositionTree({ entity: "derived-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = derivedEntityHandler.compositionTree({ entity: "derived-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = derivedEntityHandler.compositionTree({ entity: "derived-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = derivedEntityHandler.compositionTree({ entity: "derived-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof derivedEntityHandler.compositionTree !== 'function') return;
      const result = await interpret(derivedEntityHandler.compositionTree({ entity: "derived-entity-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "tree_valid" -> ok', async () => {
      if (typeof derivedEntityHandler.compositionTree !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_trash = await interpret(derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_trash?.output ?? {}));
      const _fixtureInput = { entity: "derived-entity-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(derivedEntityHandler.compositionTree({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "tree_missing" -> error', async () => {
      if (typeof derivedEntityHandler.compositionTree !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(derivedEntityHandler.compositionTree({ entity: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('traceRollup', () => {
    it('builds a valid StorageProgram', () => {
      const program = derivedEntityHandler.traceRollup({ entity: "derived-entity-1", flowId: "flow-abc-123" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = derivedEntityHandler.traceRollup({ entity: "derived-entity-1", flowId: "flow-abc-123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = derivedEntityHandler.traceRollup({ entity: "derived-entity-1", flowId: "flow-abc-123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = derivedEntityHandler.traceRollup({ entity: "derived-entity-1", flowId: "flow-abc-123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = derivedEntityHandler.traceRollup({ entity: "derived-entity-1", flowId: "flow-abc-123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof derivedEntityHandler.traceRollup !== 'function') return;
      const result = await interpret(derivedEntityHandler.traceRollup({ entity: "derived-entity-1", flowId: "flow-abc-123" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "rollup_valid" -> ok', async () => {
      if (typeof derivedEntityHandler.traceRollup !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_trash = await interpret(derivedEntityHandler.register({ name: "Trash", source: "specs/trash.derived", ast: "{\"composes\":[\"Article\",\"Label\"],\"syncs\":{\"required\":[\"trash-on-delete\"]}}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_trash?.output ?? {}));
      const _fixtureInput = { entity: "derived-entity-1", flowId: "flow-abc-123" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(derivedEntityHandler.traceRollup({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "rollup_missing_entity" -> error', async () => {
      if (typeof derivedEntityHandler.traceRollup !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(derivedEntityHandler.traceRollup({ entity: "nonexistent-id", flowId: "flow-abc-123" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(derivedEntityHandler.register({ name: {"type":"literal","value":"Trash"}, source: {"type":"literal","value":"specs/trash.derived"}, ast: {"type":"literal","value":"{}"} }), storage);
      expect(registerResult0.variant).toBe("ok");
      let entity = registerResult0.output["entity"];
      const thenResult0 = await interpret(derivedEntityHandler.get({ name: {"type":"literal","value":"Trash"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate registration returns existing", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(derivedEntityHandler.register({ name: {"type":"literal","value":"Trash"}, source: {"type":"literal","value":"specs/trash.derived"}, ast: {"type":"literal","value":"{}"} }), storage);
      expect(registerResult0.variant).toBe("ok");
      let entity = registerResult0.output["entity"];
      const thenResult0 = await interpret(derivedEntityHandler.register({ name: {"type":"literal","value":"Trash"}, source: {"type":"literal","value":"specs/trash.derived"}, ast: {"type":"literal","value":"{}"} }), storage);
      expect(thenResult0.variant).toBe("alreadyRegistered");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every entities entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), ast: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByComposedConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBySync'), input: fc.record({ syncName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compositionTree'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('traceRollup'), input: fc.record({ entity: fc.string(), flowId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = derivedEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(derivedEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in entities', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), ast: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByComposedConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBySync'), input: fc.record({ syncName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compositionTree'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('traceRollup'), input: fc.record({ entity: fc.string(), flowId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = derivedEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(derivedEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty name in entities
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof derivedEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(derivedEntityHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof derivedEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), ast: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = derivedEntityHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
