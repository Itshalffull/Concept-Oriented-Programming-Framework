// SortSpec Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { sortSpecHandler } from '../../handlers/ts/view/sort-spec.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SortSpec functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof sortSpecHandler.create !== 'function') return;
      const result = await interpret(sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_by_name" -> ok', async () => {
      if (typeof sortSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_by_date_desc" -> ok', async () => {
      if (typeof sortSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(sortSpecHandler.create({ name: "by-date", keys: "[{\"field\":\"createdAt\",\"direction\":\"desc\",\"nulls\":\"last\"}]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_identity" -> ok', async () => {
      if (typeof sortSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(sortSpecHandler.create({ name: "natural-order", keys: "[]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_duplicate" -> duplicate', async () => {
      if (typeof sortSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_by_name = await interpret(sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_create_by_name?.output ?? {}));
      const _fixtureInput = { name: "by-name", keys: "[]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(sortSpecHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = sortSpecHandler.get({ name: "by-name" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = sortSpecHandler.get({ name: "by-name" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = sortSpecHandler.get({ name: "by-name" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = sortSpecHandler.get({ name: "by-name" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = sortSpecHandler.get({ name: "by-name" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof sortSpecHandler.get !== 'function') return;
      const result = await interpret(sortSpecHandler.get({ name: "by-name" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof sortSpecHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_by_name = await interpret(sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_create_by_name?.output ?? {}));
      const _fixtureInput = { name: "by-name" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(sortSpecHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof sortSpecHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(sortSpecHandler.get({ name: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('compose', () => {
    it('builds a valid StorageProgram', () => {
      const program = sortSpecHandler.compose({ primary: "by-name", secondary: "by-date" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = sortSpecHandler.compose({ primary: "by-name", secondary: "by-date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = sortSpecHandler.compose({ primary: "by-name", secondary: "by-date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = sortSpecHandler.compose({ primary: "by-name", secondary: "by-date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = sortSpecHandler.compose({ primary: "by-name", secondary: "by-date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof sortSpecHandler.compose !== 'function') return;
      const result = await interpret(sortSpecHandler.compose({ primary: "by-name", secondary: "by-date" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compose_two" -> ok', async () => {
      if (typeof sortSpecHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_by_name = await interpret(sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" }), storage);
      const afterResult_create_by_date_desc = await interpret(sortSpecHandler.create({ name: "by-date", keys: "[{\"field\":\"createdAt\",\"direction\":\"desc\",\"nulls\":\"last\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_create_by_name?.output ?? {}), (afterResult_create_by_date_desc?.output ?? {}));
      const _fixtureInput = { primary: "by-name", secondary: "by-date" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(sortSpecHandler.compose({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compose_missing" -> notfound', async () => {
      if (typeof sortSpecHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(sortSpecHandler.compose({ primary: "nonexistent", secondary: "by-name" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('evaluate', () => {
    it('builds a valid StorageProgram', () => {
      const program = sortSpecHandler.evaluate({ name: "by-name", rows: "[{\"node\":\"B\"},{\"node\":\"A\"}]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = sortSpecHandler.evaluate({ name: "by-name", rows: "[{\"node\":\"B\"},{\"node\":\"A\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = sortSpecHandler.evaluate({ name: "by-name", rows: "[{\"node\":\"B\"},{\"node\":\"A\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = sortSpecHandler.evaluate({ name: "by-name", rows: "[{\"node\":\"B\"},{\"node\":\"A\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = sortSpecHandler.evaluate({ name: "by-name", rows: "[{\"node\":\"B\"},{\"node\":\"A\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof sortSpecHandler.evaluate !== 'function') return;
      const result = await interpret(sortSpecHandler.evaluate({ name: "by-name", rows: "[{\"node\":\"B\"},{\"node\":\"A\"}]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "evaluate_sort" -> ok', async () => {
      if (typeof sortSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_by_name = await interpret(sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_create_by_name?.output ?? {}));
      const _fixtureInput = { name: "by-name", rows: "[{\"node\":\"B\"},{\"node\":\"A\"}]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(sortSpecHandler.evaluate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "evaluate_missing" -> notfound', async () => {
      if (typeof sortSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(sortSpecHandler.evaluate({ name: "nonexistent", rows: "[]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "evaluate_bad_rows" -> error', async () => {
      if (typeof sortSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(sortSpecHandler.evaluate({ name: "by-name", rows: "not-json" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = sortSpecHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = sortSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = sortSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = sortSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = sortSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof sortSpecHandler.list !== 'function') return;
      const result = await interpret(sortSpecHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof sortSpecHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_by_name = await interpret(sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_create_by_name?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(sortSpecHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof sortSpecHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = sortSpecHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('SortSpec');
    });
  });

  describe('invariant examples', () => {
    it("create then get returns sort", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(sortSpecHandler.create({ name: "by-name", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" }), storage);
      expect(createResult0.variant).toBe("ok");
      let sort = createResult0.output["sort"];
      let s = sort;
      const thenResult0 = await interpret(sortSpecHandler.get({ name: "by-name" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("compose concatenates keys", async () => {
      const storage = createInMemoryStorage();
    });

    it("evaluate sorts rows", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(sortSpecHandler.create({ name: "by-node", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]" }), storage);
      expect(createResult0.variant).toBe("ok");
      const thenResult0 = await interpret(sortSpecHandler.evaluate({ name: "by-node", rows: "[{\"node\":\"B\"},{\"node\":\"A\"}]" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("compose is not commutative", async () => {
      const storage = createInMemoryStorage();
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: sorts have non-empty names', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), keys: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ primary: fc.string({ minLength: 1, maxLength: 50 }), secondary: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), rows: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = sortSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(sortSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: compose with identity changes keys', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), keys: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ primary: fc.string({ minLength: 1, maxLength: 50 }), secondary: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), rows: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = sortSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(sortSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: compose with identity changes keys
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
