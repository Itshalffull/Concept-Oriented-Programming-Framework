// ConnectorPort Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { connectorPortHandler } from '../../handlers/ts/connector-port.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ConnectorPort functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('addPort', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectorPortHandler.addPort !== 'function') return;
      const result = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "add_data_output" -> ok', async () => {
      if (typeof connectorPortHandler.addPort !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_validate_compatible = await interpret(connectorPortHandler.validateConnection({ source_port: "port-1", target_port: "port-2" }), storage);
      const _pool = Object.assign({}, (afterResult_validate_compatible?.output ?? {}));
      const _fixtureInput = { owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(connectorPortHandler.addPort({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_center_input" -> ok', async () => {
      if (typeof connectorPortHandler.addPort !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_validate_compatible = await interpret(connectorPortHandler.validateConnection({ source_port: "port-1", target_port: "port-2" }), storage);
      const _pool = Object.assign({}, (afterResult_validate_compatible?.output ?? {}));
      const _fixtureInput = { owner: "node-2", side: "center", offset: "0.0", direction: "in", port_type: "signal", label: "Trigger", max_connections: "1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(connectorPortHandler.addPort({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invalid_side" -> error', async () => {
      if (typeof connectorPortHandler.addPort !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "diagonal", offset: "0.5", direction: "out", port_type: "data", label: "Bad", max_connections: "1" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "invalid_direction" -> error', async () => {
      if (typeof connectorPortHandler.addPort !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "top", offset: "0.5", direction: "lateral", port_type: "data", label: "Bad", max_connections: "1" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('removePort', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectorPortHandler.removePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectorPortHandler.removePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectorPortHandler.removePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectorPortHandler.removePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectorPortHandler.removePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectorPortHandler.removePort !== 'function') return;
      const result = await interpret(connectorPortHandler.removePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_existing" -> ok', async () => {
      if (typeof connectorPortHandler.removePort !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_add_data_output = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" }), storage);
      const result = await interpret(connectorPortHandler.removePort({ port: afterResult_add_data_output?.output?.["port"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_missing" -> notfound', async () => {
      if (typeof connectorPortHandler.removePort !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectorPortHandler.removePort({ port: "port-999" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('movePort', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectorPortHandler.movePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"}, side: "left", offset: "0.3" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectorPortHandler.movePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"}, side: "left", offset: "0.3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectorPortHandler.movePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"}, side: "left", offset: "0.3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectorPortHandler.movePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"}, side: "left", offset: "0.3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectorPortHandler.movePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"}, side: "left", offset: "0.3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectorPortHandler.movePort !== 'function') return;
      const result = await interpret(connectorPortHandler.movePort({ port: {"type":"ref","fixture":"add_data_output","field":"port"}, side: "left", offset: "0.3" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "move_to_left" -> ok', async () => {
      if (typeof connectorPortHandler.movePort !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_add_data_output = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" }), storage);
      const result = await interpret(connectorPortHandler.movePort({ port: afterResult_add_data_output?.output?.["port"], side: "left", offset: "0.3" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "move_missing" -> notfound', async () => {
      if (typeof connectorPortHandler.movePort !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectorPortHandler.movePort({ port: "port-999", side: "top", offset: "0.5" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('validateConnection', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectorPortHandler.validateConnection({ source_port: "port-1", target_port: "port-2" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectorPortHandler.validateConnection({ source_port: "port-1", target_port: "port-2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectorPortHandler.validateConnection({ source_port: "port-1", target_port: "port-2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectorPortHandler.validateConnection({ source_port: "port-1", target_port: "port-2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectorPortHandler.validateConnection({ source_port: "port-1", target_port: "port-2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectorPortHandler.validateConnection !== 'function') return;
      const result = await interpret(connectorPortHandler.validateConnection({ source_port: "port-1", target_port: "port-2" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "validate_compatible" -> ok', async () => {
      if (typeof connectorPortHandler.validateConnection !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_add_data_output = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" }), storage);
      const _pool = Object.assign({}, (afterResult_add_data_output?.output ?? {}));
      const _fixtureInput = { source_port: "port-1", target_port: "port-2" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(connectorPortHandler.validateConnection({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_self" -> incompatible', async () => {
      if (typeof connectorPortHandler.validateConnection !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectorPortHandler.validateConnection({ source_port: "port-1", target_port: "port-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('incompatible'));
    });

  });

  describe('incrementConnection', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectorPortHandler.incrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectorPortHandler.incrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectorPortHandler.incrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectorPortHandler.incrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectorPortHandler.incrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectorPortHandler.incrementConnection !== 'function') return;
      const result = await interpret(connectorPortHandler.incrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "increment_ok" -> ok', async () => {
      if (typeof connectorPortHandler.incrementConnection !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_add_data_output = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" }), storage);
      const result = await interpret(connectorPortHandler.incrementConnection({ port: afterResult_add_data_output?.output?.["port"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "increment_missing" -> error', async () => {
      if (typeof connectorPortHandler.incrementConnection !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectorPortHandler.incrementConnection({ port: "port-999" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('decrementConnection', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectorPortHandler.decrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectorPortHandler.decrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectorPortHandler.decrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectorPortHandler.decrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectorPortHandler.decrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectorPortHandler.decrementConnection !== 'function') return;
      const result = await interpret(connectorPortHandler.decrementConnection({ port: {"type":"ref","fixture":"add_data_output","field":"port"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "decrement_ok" -> ok', async () => {
      if (typeof connectorPortHandler.decrementConnection !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_add_data_output = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" }), storage);
      const result = await interpret(connectorPortHandler.decrementConnection({ port: afterResult_add_data_output?.output?.["port"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "decrement_missing" -> error', async () => {
      if (typeof connectorPortHandler.decrementConnection !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectorPortHandler.decrementConnection({ port: "port-999" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('getPortsForOwner', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectorPortHandler.getPortsForOwner({ owner: {"type":"ref","fixture":"add_data_output","field":"port"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectorPortHandler.getPortsForOwner({ owner: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectorPortHandler.getPortsForOwner({ owner: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectorPortHandler.getPortsForOwner({ owner: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectorPortHandler.getPortsForOwner({ owner: {"type":"ref","fixture":"add_data_output","field":"port"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectorPortHandler.getPortsForOwner !== 'function') return;
      const result = await interpret(connectorPortHandler.getPortsForOwner({ owner: {"type":"ref","fixture":"add_data_output","field":"port"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ports_for_node" -> ok', async () => {
      if (typeof connectorPortHandler.getPortsForOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_add_data_output = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" }), storage);
      const result = await interpret(connectorPortHandler.getPortsForOwner({ owner: afterResult_add_data_output?.output?.["port"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_ports_for_unknown" -> error', async () => {
      if (typeof connectorPortHandler.getPortsForOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_add_data_output = await interpret(connectorPortHandler.addPort({ owner: "node-1", side: "right", offset: "0.5", direction: "out", port_type: "data", label: "Output", max_connections: "3" }), storage);
      const _pool = Object.assign({}, (afterResult_add_data_output?.output ?? {}));
      const _fixtureInput = { owner: "node-unknown" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(connectorPortHandler.getPortsForOwner({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof connectorPortHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = connectorPortHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ConnectorPort');
    });
  });

  describe('invariant examples', () => {
    it("addPort-then-validateConnection", async () => {
      const storage = createInMemoryStorage();
      const addPortResult0 = await interpret(connectorPortHandler.addPort({ owner: "test-o", side: "right", offset: 0.5, direction: "out", port_type: "data", label: "Output", max_connections: 1 }), storage);
      expect(addPortResult0.variant).toBe("ok");
      let port = addPortResult0.output["port"];
      let p = port;
      const thenResult0 = await interpret(connectorPortHandler.validateConnection({ source_port: p, target_port: p }), storage);
      expect(thenResult0.variant).toBe("incompatible");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-owner', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('addPort'), input: fc.record({ owner: fc.string(), side: fc.string({ minLength: 1, maxLength: 50 }), offset: fc.string(), direction: fc.string({ minLength: 1, maxLength: 50 }), port_type: fc.string(), label: fc.string(), max_connections: fc.string() }) }),
              fc.record({ action: fc.constant('removePort'), input: fc.record({ port: fc.string() }) }),
              fc.record({ action: fc.constant('movePort'), input: fc.record({ port: fc.string(), side: fc.string({ minLength: 1, maxLength: 50 }), offset: fc.string() }) }),
              fc.record({ action: fc.constant('validateConnection'), input: fc.record({ source_port: fc.string(), target_port: fc.string() }) }),
              fc.record({ action: fc.constant('incrementConnection'), input: fc.record({ port: fc.string() }) }),
              fc.record({ action: fc.constant('decrementConnection'), input: fc.record({ port: fc.string() }) }),
              fc.record({ action: fc.constant('getPortsForOwner'), input: fc.record({ owner: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = connectorPortHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(connectorPortHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-owner', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('addPort'), input: fc.record({ owner: fc.string(), side: fc.string({ minLength: 1, maxLength: 50 }), offset: fc.string(), direction: fc.string({ minLength: 1, maxLength: 50 }), port_type: fc.string(), label: fc.string(), max_connections: fc.string() }) }),
              fc.record({ action: fc.constant('removePort'), input: fc.record({ port: fc.string() }) }),
              fc.record({ action: fc.constant('movePort'), input: fc.record({ port: fc.string(), side: fc.string({ minLength: 1, maxLength: 50 }), offset: fc.string() }) }),
              fc.record({ action: fc.constant('validateConnection'), input: fc.record({ source_port: fc.string(), target_port: fc.string() }) }),
              fc.record({ action: fc.constant('incrementConnection'), input: fc.record({ port: fc.string() }) }),
              fc.record({ action: fc.constant('decrementConnection'), input: fc.record({ port: fc.string() }) }),
              fc.record({ action: fc.constant('getPortsForOwner'), input: fc.record({ owner: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = connectorPortHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(connectorPortHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-owner
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('addPort handles empty input: ', async () => {
      if (typeof connectorPortHandler.addPort !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(connectorPortHandler.addPort({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('addPort ensures on ok: ', async () => {
      if (typeof connectorPortHandler.addPort !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ owner: fc.string(), side: fc.string({ minLength: 1, maxLength: 50 }), offset: fc.string(), direction: fc.string({ minLength: 1, maxLength: 50 }), port_type: fc.string(), label: fc.string(), max_connections: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = connectorPortHandler.addPort(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
