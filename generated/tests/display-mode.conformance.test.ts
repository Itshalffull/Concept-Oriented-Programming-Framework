// DisplayMode Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { displayModeHandler } from '../../handlers/ts/app/display-mode.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('DisplayMode functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.create !== 'function') return;
      const result = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_article_full" -> ok', async () => {
      if (typeof displayModeHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_empty_schema" -> error', async () => {
      if (typeof displayModeHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.create({ schema: "", mode_id: "full", name: "Full Page" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.resolve({ schema: "Article", mode_id: "full" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.resolve({ schema: "Article", mode_id: "full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.resolve({ schema: "Article", mode_id: "full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.resolve({ schema: "Article", mode_id: "full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.resolve({ schema: "Article", mode_id: "full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.resolve !== 'function') return;
      const result = await interpret(displayModeHandler.resolve({ schema: "Article", mode_id: "full" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_article" -> ok', async () => {
      if (typeof displayModeHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { schema: "Article", mode_id: "full" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(displayModeHandler.resolve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_nonexistent" -> error', async () => {
      if (typeof displayModeHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.resolve({ schema: "Article", mode_id: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('set_layout', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.set_layout({ mode: "Article:full", layout: "triple-zone-default" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.set_layout({ mode: "Article:full", layout: "triple-zone-default" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.set_layout({ mode: "Article:full", layout: "triple-zone-default" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.set_layout({ mode: "Article:full", layout: "triple-zone-default" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.set_layout({ mode: "Article:full", layout: "triple-zone-default" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.set_layout !== 'function') return;
      const result = await interpret(displayModeHandler.set_layout({ mode: "Article:full", layout: "triple-zone-default" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_layout_triple" -> ok', async () => {
      if (typeof displayModeHandler.set_layout !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { mode: "Article:full", layout: "triple-zone-default" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(displayModeHandler.set_layout({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_layout_missing_mode" -> error', async () => {
      if (typeof displayModeHandler.set_layout !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.set_layout({ mode: "Missing:mode", layout: "two-column" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('clear_layout', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.clear_layout({ mode: "Article:full" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.clear_layout({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.clear_layout({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.clear_layout({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.clear_layout({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.clear_layout !== 'function') return;
      const result = await interpret(displayModeHandler.clear_layout({ mode: "Article:full" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "clear_existing_layout" -> ok', async () => {
      if (typeof displayModeHandler.clear_layout !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { mode: "Article:full" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(displayModeHandler.clear_layout({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "clear_layout_missing" -> error', async () => {
      if (typeof displayModeHandler.clear_layout !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.clear_layout({ mode: "Missing:mode" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('set_component_mapping', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.set_component_mapping({ mode: "Article:full", mapping: "article-hero-card" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.set_component_mapping({ mode: "Article:full", mapping: "article-hero-card" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.set_component_mapping({ mode: "Article:full", mapping: "article-hero-card" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.set_component_mapping({ mode: "Article:full", mapping: "article-hero-card" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.set_component_mapping({ mode: "Article:full", mapping: "article-hero-card" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.set_component_mapping !== 'function') return;
      const result = await interpret(displayModeHandler.set_component_mapping({ mode: "Article:full", mapping: "article-hero-card" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_mapping_hero" -> ok', async () => {
      if (typeof displayModeHandler.set_component_mapping !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { mode: "Article:full", mapping: "article-hero-card" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(displayModeHandler.set_component_mapping({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_mapping_missing" -> error', async () => {
      if (typeof displayModeHandler.set_component_mapping !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.set_component_mapping({ mode: "Missing:mode", mapping: "widget" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('clear_component_mapping', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.clear_component_mapping({ mode: "Article:full" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.clear_component_mapping({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.clear_component_mapping({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.clear_component_mapping({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.clear_component_mapping({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.clear_component_mapping !== 'function') return;
      const result = await interpret(displayModeHandler.clear_component_mapping({ mode: "Article:full" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "clear_comp_mapping" -> ok', async () => {
      if (typeof displayModeHandler.clear_component_mapping !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { mode: "Article:full" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(displayModeHandler.clear_component_mapping({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "clear_comp_missing" -> error', async () => {
      if (typeof displayModeHandler.clear_component_mapping !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.clear_component_mapping({ mode: "Missing:mode" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('set_flat_fields', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.set_flat_fields({ mode: "Article:full", placements: "[\"fp-title\",\"fp-body\"]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.set_flat_fields({ mode: "Article:full", placements: "[\"fp-title\",\"fp-body\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.set_flat_fields({ mode: "Article:full", placements: "[\"fp-title\",\"fp-body\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.set_flat_fields({ mode: "Article:full", placements: "[\"fp-title\",\"fp-body\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.set_flat_fields({ mode: "Article:full", placements: "[\"fp-title\",\"fp-body\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.set_flat_fields !== 'function') return;
      const result = await interpret(displayModeHandler.set_flat_fields({ mode: "Article:full", placements: "[\"fp-title\",\"fp-body\"]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_placements" -> ok', async () => {
      if (typeof displayModeHandler.set_flat_fields !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { mode: "Article:full", placements: "[\"fp-title\",\"fp-body\"]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(displayModeHandler.set_flat_fields({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_placements_missing" -> error', async () => {
      if (typeof displayModeHandler.set_flat_fields !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.set_flat_fields({ mode: "Missing:mode", placements: "[]" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.get({ mode: "Article:full" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.get({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.get({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.get({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.get({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.get !== 'function') return;
      const result = await interpret(displayModeHandler.get({ mode: "Article:full" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing_mode" -> ok', async () => {
      if (typeof displayModeHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { mode: "Article:full" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(displayModeHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing_mode" -> error', async () => {
      if (typeof displayModeHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.get({ mode: "Missing:mode" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('delete', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.delete({ mode: "Article:full" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.delete({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.delete({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.delete({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.delete({ mode: "Article:full" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.delete !== 'function') return;
      const result = await interpret(displayModeHandler.delete({ mode: "Article:full" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "delete_existing" -> ok', async () => {
      if (typeof displayModeHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { mode: "Article:full" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(displayModeHandler.delete({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "delete_missing" -> error', async () => {
      if (typeof displayModeHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.delete({ mode: "Missing:mode" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('list_for_schema', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.list_for_schema({ schema: "Article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.list_for_schema({ schema: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.list_for_schema({ schema: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.list_for_schema({ schema: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.list_for_schema({ schema: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.list_for_schema !== 'function') return;
      const result = await interpret(displayModeHandler.list_for_schema({ schema: "Article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_article_modes" -> ok', async () => {
      if (typeof displayModeHandler.list_for_schema !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { schema: "Article" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(displayModeHandler.list_for_schema({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_empty_schema" -> error', async () => {
      if (typeof displayModeHandler.list_for_schema !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(displayModeHandler.list_for_schema({ schema: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = displayModeHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = displayModeHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = displayModeHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = displayModeHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = displayModeHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof displayModeHandler.list !== 'function') return;
      const result = await interpret(displayModeHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof displayModeHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_full = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_full?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(displayModeHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof displayModeHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = displayModeHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('DisplayMode');
    });
  });

  describe('invariant examples', () => {
    it("create then resolve", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      expect(createResult0.variant).toBe("ok");
      let mode = createResult0.output["mode"];
      let m = mode;
      const thenResult0 = await interpret(displayModeHandler.resolve({ schema: "Article", mode_id: "full" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("create then list_for_schema", async () => {
      const storage = createInMemoryStorage();
    });

    it("create then get", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      expect(createResult0.variant).toBe("ok");
      let mode = createResult0.output["mode"];
      let m = mode;
      const set_layoutResult1 = await interpret(displayModeHandler.set_layout({ mode: m, layout: "triple-zone-default" }), storage);
      expect(set_layoutResult1.variant).toBe("ok");
      mode = set_layoutResult1.output["mode"];
      const thenResult0 = await interpret(displayModeHandler.get({ mode: m }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("create then get", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(displayModeHandler.create({ schema: "Article", mode_id: "full", name: "Full Page" }), storage);
      expect(createResult0.variant).toBe("ok");
      let mode = createResult0.output["mode"];
      let m = mode;
      const set_component_mappingResult1 = await interpret(displayModeHandler.set_component_mapping({ mode: m, mapping: "article-hero-card" }), storage);
      expect(set_component_mappingResult1.variant).toBe("ok");
      mode = set_component_mappingResult1.output["mode"];
      const thenResult0 = await interpret(displayModeHandler.get({ mode: m }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: modes have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), mode_id: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), mode_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('set_layout'), input: fc.record({ mode: fc.string(), layout: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clear_layout'), input: fc.record({ mode: fc.string() }) }),
              fc.record({ action: fc.constant('set_component_mapping'), input: fc.record({ mode: fc.string(), mapping: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clear_component_mapping'), input: fc.record({ mode: fc.string() }) }),
              fc.record({ action: fc.constant('set_flat_fields'), input: fc.record({ mode: fc.string(), placements: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ mode: fc.string() }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ mode: fc.string() }) }),
              fc.record({ action: fc.constant('list_for_schema'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = displayModeHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(displayModeHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in modes', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), mode_id: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), mode_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('set_layout'), input: fc.record({ mode: fc.string(), layout: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clear_layout'), input: fc.record({ mode: fc.string() }) }),
              fc.record({ action: fc.constant('set_component_mapping'), input: fc.record({ mode: fc.string(), mapping: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clear_component_mapping'), input: fc.record({ mode: fc.string() }) }),
              fc.record({ action: fc.constant('set_flat_fields'), input: fc.record({ mode: fc.string(), placements: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ mode: fc.string() }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ mode: fc.string() }) }),
              fc.record({ action: fc.constant('list_for_schema'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = displayModeHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(displayModeHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in modes
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof displayModeHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(displayModeHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof displayModeHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), mode_id: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = displayModeHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('resolve handles empty input: ', async () => {
      if (typeof displayModeHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(displayModeHandler.resolve({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('resolve ensures on ok: ', async () => {
      if (typeof displayModeHandler.resolve !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), mode_id: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = displayModeHandler.resolve(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('set_layout handles empty input: ', async () => {
      if (typeof displayModeHandler.set_layout !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(displayModeHandler.set_layout({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('set_layout ensures on ok: ', async () => {
      if (typeof displayModeHandler.set_layout !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ mode: fc.string(), layout: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = displayModeHandler.set_layout(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
