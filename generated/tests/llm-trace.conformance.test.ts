// LLMTrace Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { llmTraceHandler } from '../../handlers/ts/repertoire/llm-safety/llm-trace.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('LLMTrace functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('startTrace', () => {
    it('builds a valid StorageProgram', () => {
      const program = llmTraceHandler.startTrace({ name: 'test-name', tags: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = llmTraceHandler.startTrace({ name: 'test-name', tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = llmTraceHandler.startTrace({ name: 'test-name', tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = llmTraceHandler.startTrace({ name: 'test-name', tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = llmTraceHandler.startTrace({ name: 'test-name', tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof llmTraceHandler.startTrace !== 'function') return;
      const result = await interpret(llmTraceHandler.startTrace({ name: 'test-name', tags: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('startSpan', () => {
    it('builds a valid StorageProgram', () => {
      const program = llmTraceHandler.startSpan({ trace: 'test', operation: 'test-operation', parent_span_id: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = llmTraceHandler.startSpan({ trace: 'test', operation: 'test-operation', parent_span_id: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = llmTraceHandler.startSpan({ trace: 'test', operation: 'test-operation', parent_span_id: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = llmTraceHandler.startSpan({ trace: 'test', operation: 'test-operation', parent_span_id: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = llmTraceHandler.startSpan({ trace: 'test', operation: 'test-operation', parent_span_id: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof llmTraceHandler.startSpan !== 'function') return;
      const result = await interpret(llmTraceHandler.startSpan({ trace: 'test', operation: 'test-operation', parent_span_id: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('endSpan', () => {
    it('builds a valid StorageProgram', () => {
      const program = llmTraceHandler.endSpan({ trace: 'test', span_id: 'test-span_id', status: 'test-status', metrics: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = llmTraceHandler.endSpan({ trace: 'test', span_id: 'test-span_id', status: 'test-status', metrics: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = llmTraceHandler.endSpan({ trace: 'test', span_id: 'test-span_id', status: 'test-status', metrics: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = llmTraceHandler.endSpan({ trace: 'test', span_id: 'test-span_id', status: 'test-status', metrics: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = llmTraceHandler.endSpan({ trace: 'test', span_id: 'test-span_id', status: 'test-status', metrics: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof llmTraceHandler.endSpan !== 'function') return;
      const result = await interpret(llmTraceHandler.endSpan({ trace: 'test', span_id: 'test-span_id', status: 'test-status', metrics: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('addMetric', () => {
    it('builds a valid StorageProgram', () => {
      const program = llmTraceHandler.addMetric({ trace: 'test', span_id: 'test-span_id', key: 'test-key', value: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = llmTraceHandler.addMetric({ trace: 'test', span_id: 'test-span_id', key: 'test-key', value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = llmTraceHandler.addMetric({ trace: 'test', span_id: 'test-span_id', key: 'test-key', value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = llmTraceHandler.addMetric({ trace: 'test', span_id: 'test-span_id', key: 'test-key', value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = llmTraceHandler.addMetric({ trace: 'test', span_id: 'test-span_id', key: 'test-key', value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof llmTraceHandler.addMetric !== 'function') return;
      const result = await interpret(llmTraceHandler.addMetric({ trace: 'test', span_id: 'test-span_id', key: 'test-key', value: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('getCost', () => {
    it('builds a valid StorageProgram', () => {
      const program = llmTraceHandler.getCost({ trace: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = llmTraceHandler.getCost({ trace: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = llmTraceHandler.getCost({ trace: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = llmTraceHandler.getCost({ trace: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = llmTraceHandler.getCost({ trace: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof llmTraceHandler.getCost !== 'function') return;
      const result = await interpret(llmTraceHandler.getCost({ trace: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('getTrace', () => {
    it('builds a valid StorageProgram', () => {
      const program = llmTraceHandler.getTrace({ trace: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = llmTraceHandler.getTrace({ trace: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = llmTraceHandler.getTrace({ trace: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = llmTraceHandler.getTrace({ trace: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = llmTraceHandler.getTrace({ trace: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof llmTraceHandler.getTrace !== 'function') return;
      const result = await interpret(llmTraceHandler.getTrace({ trace: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('export', () => {
    it('builds a valid StorageProgram', () => {
      const program = llmTraceHandler.export({ trace: 'test', format: 'test-format' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = llmTraceHandler.export({ trace: 'test', format: 'test-format' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = llmTraceHandler.export({ trace: 'test', format: 'test-format' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = llmTraceHandler.export({ trace: 'test', format: 'test-format' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = llmTraceHandler.export({ trace: 'test', format: 'test-format' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof llmTraceHandler.export !== 'function') return;
      const result = await interpret(llmTraceHandler.export({ trace: 'test', format: 'test-format' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof llmTraceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = llmTraceHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('LLMTrace');
    });
  });

  describe('invariant examples', () => {
    it("startTrace-then-endSpan", async () => {
      const storage = createInMemoryStorage();
      const startTraceResult0 = await interpret(llmTraceHandler.startTrace({ name: "test", tags: [] }), storage);
      expect(startTraceResult0.variant).toBe("ok");
      let trace = startTraceResult0.output["trace"];
      let z = trace;
      const startSpanResult1 = await interpret(llmTraceHandler.startSpan({ trace: z, operation: "llm.generate", parent_span_id: "test-_" }), storage);
      expect(startSpanResult1.variant).toBe("ok");
      let span_id = startSpanResult1.output["span_id"];
      let s = span_id;
      const thenResult0 = await interpret(llmTraceHandler.endSpan({ trace: z, span_id: s, status: "ok", metrics: {"tokens":100,"cost":0.01,"latency_ms":500} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('startTrace'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), tags: fc.string() }) }),
              fc.record({ action: fc.constant('startSpan'), input: fc.record({ trace: fc.string(), operation: fc.string({ minLength: 1, maxLength: 50 }), parent_span_id: fc.string() }) }),
              fc.record({ action: fc.constant('endSpan'), input: fc.record({ trace: fc.string(), span_id: fc.string({ minLength: 1, maxLength: 50 }), status: fc.string({ minLength: 1, maxLength: 50 }), metrics: fc.string() }) }),
              fc.record({ action: fc.constant('addMetric'), input: fc.record({ trace: fc.string(), span_id: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), value: fc.string() }) }),
              fc.record({ action: fc.constant('getCost'), input: fc.record({ trace: fc.string() }) }),
              fc.record({ action: fc.constant('getTrace'), input: fc.record({ trace: fc.string() }) }),
              fc.record({ action: fc.constant('export'), input: fc.record({ trace: fc.string(), format: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = llmTraceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(llmTraceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('startTrace'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), tags: fc.string() }) }),
              fc.record({ action: fc.constant('startSpan'), input: fc.record({ trace: fc.string(), operation: fc.string({ minLength: 1, maxLength: 50 }), parent_span_id: fc.string() }) }),
              fc.record({ action: fc.constant('endSpan'), input: fc.record({ trace: fc.string(), span_id: fc.string({ minLength: 1, maxLength: 50 }), status: fc.string({ minLength: 1, maxLength: 50 }), metrics: fc.string() }) }),
              fc.record({ action: fc.constant('addMetric'), input: fc.record({ trace: fc.string(), span_id: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), value: fc.string() }) }),
              fc.record({ action: fc.constant('getCost'), input: fc.record({ trace: fc.string() }) }),
              fc.record({ action: fc.constant('getTrace'), input: fc.record({ trace: fc.string() }) }),
              fc.record({ action: fc.constant('export'), input: fc.record({ trace: fc.string(), format: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = llmTraceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(llmTraceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-name
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('addMetric handles empty input: ', async () => {
      if (typeof llmTraceHandler.addMetric !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(llmTraceHandler.addMetric({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('addMetric ensures on ok: ', async () => {
      if (typeof llmTraceHandler.addMetric !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ trace: fc.string(), span_id: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), value: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = llmTraceHandler.addMetric(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
