// Version Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { versionHandler } from '../../handlers/ts/app/version.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Version functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('snapshot', () => {
    it('builds a valid StorageProgram', () => {
      const program = versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof versionHandler.snapshot !== 'function') return;
      const result = await interpret(versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_snapshot" -> ok', async () => {
      if (typeof versionHandler.snapshot !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_diff = await interpret(versionHandler.diff({ versionA: "v1", versionB: "v2" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_diff?.output ?? {}));
      const _fixtureInput = { version: "v1", entity: "doc-1", data: "initial content", author: "alice" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(versionHandler.snapshot({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "another_snapshot" -> ok', async () => {
      if (typeof versionHandler.snapshot !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_diff = await interpret(versionHandler.diff({ versionA: "v1", versionB: "v2" }), storage);
      const afterResult_valid_snapshot = await interpret(versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" }), storage);
      const result = await interpret(versionHandler.snapshot({ version: "v2", entity: afterResult_valid_snapshot?.output?.["version"], data: "updated content", author: "bob" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('listVersions', () => {
    it('builds a valid StorageProgram', () => {
      const program = versionHandler.listVersions({ entity: {"type":"ref","fixture":"valid_snapshot","field":"version"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = versionHandler.listVersions({ entity: {"type":"ref","fixture":"valid_snapshot","field":"version"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = versionHandler.listVersions({ entity: {"type":"ref","fixture":"valid_snapshot","field":"version"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = versionHandler.listVersions({ entity: {"type":"ref","fixture":"valid_snapshot","field":"version"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = versionHandler.listVersions({ entity: {"type":"ref","fixture":"valid_snapshot","field":"version"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof versionHandler.listVersions !== 'function') return;
      const result = await interpret(versionHandler.listVersions({ entity: {"type":"ref","fixture":"valid_snapshot","field":"version"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_list" -> ok', async () => {
      if (typeof versionHandler.listVersions !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_diff = await interpret(versionHandler.diff({ versionA: "v1", versionB: "v2" }), storage);
      const afterResult_valid_snapshot = await interpret(versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" }), storage);
      const result = await interpret(versionHandler.listVersions({ entity: afterResult_valid_snapshot?.output?.["version"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_unknown_entity" -> error', async () => {
      if (typeof versionHandler.listVersions !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_diff = await interpret(versionHandler.diff({ versionA: "v1", versionB: "v2" }), storage);
      const afterResult_valid_snapshot = await interpret(versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_diff?.output ?? {}), (afterResult_valid_snapshot?.output ?? {}));
      const _fixtureInput = { entity: "nonexistent" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(versionHandler.listVersions({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('rollback', () => {
    it('builds a valid StorageProgram', () => {
      const program = versionHandler.rollback({ version: "v1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = versionHandler.rollback({ version: "v1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = versionHandler.rollback({ version: "v1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = versionHandler.rollback({ version: "v1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = versionHandler.rollback({ version: "v1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof versionHandler.rollback !== 'function') return;
      const result = await interpret(versionHandler.rollback({ version: "v1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_rollback" -> ok', async () => {
      if (typeof versionHandler.rollback !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_diff = await interpret(versionHandler.diff({ versionA: "v1", versionB: "v2" }), storage);
      const afterResult_valid_snapshot = await interpret(versionHandler.snapshot({ version: "v1", entity: "doc-1", data: "initial content", author: "alice" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_diff?.output ?? {}), (afterResult_valid_snapshot?.output ?? {}));
      const _fixtureInput = { version: "v1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(versionHandler.rollback({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "rollback_missing" -> notfound', async () => {
      if (typeof versionHandler.rollback !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(versionHandler.rollback({ version: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('diff', () => {
    it('builds a valid StorageProgram', () => {
      const program = versionHandler.diff({ versionA: "v1", versionB: "v2" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = versionHandler.diff({ versionA: "v1", versionB: "v2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = versionHandler.diff({ versionA: "v1", versionB: "v2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = versionHandler.diff({ versionA: "v1", versionB: "v2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = versionHandler.diff({ versionA: "v1", versionB: "v2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof versionHandler.diff !== 'function') return;
      const result = await interpret(versionHandler.diff({ versionA: "v1", versionB: "v2" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_diff" -> ok', async () => {
      if (typeof versionHandler.diff !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(versionHandler.diff({ versionA: "v1", versionB: "v2" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "diff_missing" -> notfound', async () => {
      if (typeof versionHandler.diff !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(versionHandler.diff({ versionA: "v1", versionB: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof versionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = versionHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Version');
    });
  });

  describe('invariant examples', () => {
    it("snapshot-then-rollback", async () => {
      const storage = createInMemoryStorage();
      const snapshotResult0 = await interpret(versionHandler.snapshot({ version: "test-v1", entity: "doc", data: "original", author: "alice" }), storage);
      expect(snapshotResult0.variant).toBe("ok");
      let version = snapshotResult0.output["version"];
      let v1 = version;
      const thenResult0 = await interpret(versionHandler.listVersions({ entity: "doc" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(versionHandler.rollback({ version: v1 }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-entity', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('snapshot'), input: fc.record({ version: fc.string(), entity: fc.string({ minLength: 1, maxLength: 50 }), data: fc.string({ minLength: 1, maxLength: 50 }), author: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listVersions'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ version: fc.string() }) }),
              fc.record({ action: fc.constant('diff'), input: fc.record({ versionA: fc.string(), versionB: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = versionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(versionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-snapshot', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('snapshot'), input: fc.record({ version: fc.string(), entity: fc.string({ minLength: 1, maxLength: 50 }), data: fc.string({ minLength: 1, maxLength: 50 }), author: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listVersions'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ version: fc.string() }) }),
              fc.record({ action: fc.constant('diff'), input: fc.record({ versionA: fc.string(), versionB: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = versionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(versionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-snapshot
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('snapshot handles empty input: ', async () => {
      if (typeof versionHandler.snapshot !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(versionHandler.snapshot({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('snapshot ensures on ok: ', async () => {
      if (typeof versionHandler.snapshot !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ version: fc.string(), entity: fc.string({ minLength: 1, maxLength: 50 }), data: fc.string({ minLength: 1, maxLength: 50 }), author: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = versionHandler.snapshot(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
