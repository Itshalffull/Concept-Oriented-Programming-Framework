// DeploymentEntity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { deploymentEntityHandler } from '../../handlers/ts/score/deployment-entity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('DeploymentEntity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.register !== 'function') return;
      const result = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_prod" -> ok', async () => {
      if (typeof deploymentEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_staging" -> ok', async () => {
      if (typeof deploymentEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deploymentEntityHandler.register({ name: "conduit-staging", source: "deploy/conduit-staging.deploy.yaml", manifest: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_name" -> error', async () => {
      if (typeof deploymentEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deploymentEntityHandler.register({ name: "", source: "deploy/empty.deploy.yaml", manifest: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.get({ name: "conduit-prod" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.get({ name: "conduit-prod" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.get({ name: "conduit-prod" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.get({ name: "conduit-prod" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.get({ name: "conduit-prod" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.get !== 'function') return;
      const result = await interpret(deploymentEntityHandler.get({ name: "conduit-prod" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof deploymentEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_prod?.output ?? {}));
      const _fixtureInput = { name: "conduit-prod" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(deploymentEntityHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_nonexistent" -> error', async () => {
      if (typeof deploymentEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deploymentEntityHandler.get({ name: "no-such-deployment" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listRuntimes', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.listRuntimes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.listRuntimes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.listRuntimes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.listRuntimes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.listRuntimes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.listRuntimes !== 'function') return;
      const result = await interpret(deploymentEntityHandler.listRuntimes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_runtimes" -> ok', async () => {
      if (typeof deploymentEntityHandler.listRuntimes !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const result = await interpret(deploymentEntityHandler.listRuntimes({ deployment: afterResult_register_prod?.output?.["deployment"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_runtimes_missing" -> ok', async () => {
      if (typeof deploymentEntityHandler.listRuntimes !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_prod?.output ?? {}));
      const _fixtureInput = { deployment: "deploy-missing" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(deploymentEntityHandler.listRuntimes({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('findConceptRuntime', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.findConceptRuntime({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, concept: "User" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.findConceptRuntime({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.findConceptRuntime({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.findConceptRuntime({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.findConceptRuntime({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.findConceptRuntime !== 'function') return;
      const result = await interpret(deploymentEntityHandler.findConceptRuntime({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, concept: "User" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_user_runtime" -> ok', async () => {
      if (typeof deploymentEntityHandler.findConceptRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const result = await interpret(deploymentEntityHandler.findConceptRuntime({ deployment: afterResult_register_prod?.output?.["deployment"], concept: "User" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unassigned" -> error', async () => {
      if (typeof deploymentEntityHandler.findConceptRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deploymentEntityHandler.findConceptRuntime({ deployment: "deploy-001", concept: "UnknownConcept" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findSyncEngine', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.findSyncEngine({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, sync: "ArticlePublishSync" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.findSyncEngine({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, sync: "ArticlePublishSync" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.findSyncEngine({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, sync: "ArticlePublishSync" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.findSyncEngine({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, sync: "ArticlePublishSync" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.findSyncEngine({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, sync: "ArticlePublishSync" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.findSyncEngine !== 'function') return;
      const result = await interpret(deploymentEntityHandler.findSyncEngine({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, sync: "ArticlePublishSync" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_article_sync" -> ok', async () => {
      if (typeof deploymentEntityHandler.findSyncEngine !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const result = await interpret(deploymentEntityHandler.findSyncEngine({ deployment: afterResult_register_prod?.output?.["deployment"], sync: "ArticlePublishSync" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unassigned_sync" -> error', async () => {
      if (typeof deploymentEntityHandler.findSyncEngine !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deploymentEntityHandler.findSyncEngine({ deployment: "deploy-001", sync: "NonexistentSync" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('topology', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.topology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.topology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.topology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.topology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.topology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.topology !== 'function') return;
      const result = await interpret(deploymentEntityHandler.topology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "topology_existing" -> ok', async () => {
      if (typeof deploymentEntityHandler.topology !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const result = await interpret(deploymentEntityHandler.topology({ deployment: afterResult_register_prod?.output?.["deployment"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "topology_missing" -> ok', async () => {
      if (typeof deploymentEntityHandler.topology !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_prod?.output ?? {}));
      const _fixtureInput = { deployment: "deploy-missing" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(deploymentEntityHandler.topology({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('transportRoutes', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.transportRoutes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, fromConcept: "User", toConcept: "Notification" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.transportRoutes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, fromConcept: "User", toConcept: "Notification" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.transportRoutes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, fromConcept: "User", toConcept: "Notification" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.transportRoutes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, fromConcept: "User", toConcept: "Notification" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.transportRoutes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, fromConcept: "User", toConcept: "Notification" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.transportRoutes !== 'function') return;
      const result = await interpret(deploymentEntityHandler.transportRoutes({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"}, fromConcept: "User", toConcept: "Notification" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "route_cross_runtime" -> ok', async () => {
      if (typeof deploymentEntityHandler.transportRoutes !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const result = await interpret(deploymentEntityHandler.transportRoutes({ deployment: afterResult_register_prod?.output?.["deployment"], fromConcept: "User", toConcept: "Notification" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "route_same_runtime" -> error', async () => {
      if (typeof deploymentEntityHandler.transportRoutes !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deploymentEntityHandler.transportRoutes({ deployment: "deploy-001", fromConcept: "User", toConcept: "Session" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('storageTopology', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.storageTopology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.storageTopology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.storageTopology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.storageTopology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.storageTopology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.storageTopology !== 'function') return;
      const result = await interpret(deploymentEntityHandler.storageTopology({ deployment: {"type":"ref","fixture":"register_prod","field":"deployment"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "storage_topology" -> ok', async () => {
      if (typeof deploymentEntityHandler.storageTopology !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const result = await interpret(deploymentEntityHandler.storageTopology({ deployment: afterResult_register_prod?.output?.["deployment"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "storage_topology_missing" -> ok', async () => {
      if (typeof deploymentEntityHandler.storageTopology !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_prod?.output ?? {}));
      const _fixtureInput = { deployment: "deploy-missing" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(deploymentEntityHandler.storageTopology({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('environmentDiff', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.environmentDiff({ deploymentA: "deploy-prod", deploymentB: "deploy-staging" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.environmentDiff({ deploymentA: "deploy-prod", deploymentB: "deploy-staging" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.environmentDiff({ deploymentA: "deploy-prod", deploymentB: "deploy-staging" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.environmentDiff({ deploymentA: "deploy-prod", deploymentB: "deploy-staging" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.environmentDiff({ deploymentA: "deploy-prod", deploymentB: "deploy-staging" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.environmentDiff !== 'function') return;
      const result = await interpret(deploymentEntityHandler.environmentDiff({ deploymentA: "deploy-prod", deploymentB: "deploy-staging" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "diff_prod_staging" -> ok', async () => {
      if (typeof deploymentEntityHandler.environmentDiff !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_prod?.output ?? {}));
      const _fixtureInput = { deploymentA: "deploy-prod", deploymentB: "deploy-staging" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(deploymentEntityHandler.environmentDiff({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "diff_same" -> ok', async () => {
      if (typeof deploymentEntityHandler.environmentDiff !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_prod = await interpret(deploymentEntityHandler.register({ name: "conduit-prod", source: "deploy/conduit.deploy.yaml", manifest: "{\"app\":{\"name\":\"conduit\",\"version\":\"1.0\"},\"runtimes\":[{\"name\":\"api-server\",\"type\":\"http\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_prod?.output ?? {}));
      const _fixtureInput = { deploymentA: "deploy-prod", deploymentB: "deploy-prod" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(deploymentEntityHandler.environmentDiff({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('validateAgainstSpecs', () => {
    it('builds a valid StorageProgram', () => {
      const program = deploymentEntityHandler.validateAgainstSpecs({ deployment: "deploy-001" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = deploymentEntityHandler.validateAgainstSpecs({ deployment: "deploy-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = deploymentEntityHandler.validateAgainstSpecs({ deployment: "deploy-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = deploymentEntityHandler.validateAgainstSpecs({ deployment: "deploy-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = deploymentEntityHandler.validateAgainstSpecs({ deployment: "deploy-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof deploymentEntityHandler.validateAgainstSpecs !== 'function') return;
      const result = await interpret(deploymentEntityHandler.validateAgainstSpecs({ deployment: "deploy-001" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "validate_existing" -> ok', async () => {
      if (typeof deploymentEntityHandler.validateAgainstSpecs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deploymentEntityHandler.validateAgainstSpecs({ deployment: "deploy-001" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_missing" -> ok', async () => {
      if (typeof deploymentEntityHandler.validateAgainstSpecs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(deploymentEntityHandler.validateAgainstSpecs({ deployment: "deploy-missing" }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(deploymentEntityHandler.register({ name: {"type":"literal","value":"conduit-prod"}, source: {"type":"literal","value":"deploy/conduit.deploy.yaml"}, manifest: {"type":"literal","value":"{}"} }), storage);
      expect(registerResult0.variant).toBe("ok");
      let deployment = registerResult0.output["deployment"];
      const thenResult0 = await interpret(deploymentEntityHandler.get({ name: {"type":"literal","value":"conduit-prod"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every deployments entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), manifest: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listRuntimes'), input: fc.record({ deployment: fc.string() }) }),
              fc.record({ action: fc.constant('findConceptRuntime'), input: fc.record({ deployment: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findSyncEngine'), input: fc.record({ deployment: fc.string(), sync: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('topology'), input: fc.record({ deployment: fc.string() }) }),
              fc.record({ action: fc.constant('transportRoutes'), input: fc.record({ deployment: fc.string(), fromConcept: fc.string({ minLength: 1, maxLength: 50 }), toConcept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('storageTopology'), input: fc.record({ deployment: fc.string() }) }),
              fc.record({ action: fc.constant('environmentDiff'), input: fc.record({ deploymentA: fc.string(), deploymentB: fc.string() }) }),
              fc.record({ action: fc.constant('validateAgainstSpecs'), input: fc.record({ deployment: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = deploymentEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(deploymentEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in deployments', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), manifest: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listRuntimes'), input: fc.record({ deployment: fc.string() }) }),
              fc.record({ action: fc.constant('findConceptRuntime'), input: fc.record({ deployment: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findSyncEngine'), input: fc.record({ deployment: fc.string(), sync: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('topology'), input: fc.record({ deployment: fc.string() }) }),
              fc.record({ action: fc.constant('transportRoutes'), input: fc.record({ deployment: fc.string(), fromConcept: fc.string({ minLength: 1, maxLength: 50 }), toConcept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('storageTopology'), input: fc.record({ deployment: fc.string() }) }),
              fc.record({ action: fc.constant('environmentDiff'), input: fc.record({ deploymentA: fc.string(), deploymentB: fc.string() }) }),
              fc.record({ action: fc.constant('validateAgainstSpecs'), input: fc.record({ deployment: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = deploymentEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(deploymentEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty name in deployments
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof deploymentEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(deploymentEntityHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof deploymentEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), manifest: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = deploymentEntityHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
