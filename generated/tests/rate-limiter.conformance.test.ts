// RateLimiter Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { rateLimiterHandler } from '../../handlers/ts/execution/rate-limiter.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('RateLimiter functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof rateLimiterHandler.configure !== 'function') return;
      const result = await interpret(rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "api_limiter" -> ok', async () => {
      if (typeof rateLimiterHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "duplicate_limiter" -> exists', async () => {
      if (typeof rateLimiterHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "50", refillRate: "5", refillIntervalMs: "2000" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('exists'));
    });

  });

  describe('acquire', () => {
    it('builds a valid StorageProgram', () => {
      const program = rateLimiterHandler.acquire({ endpoint: "openai-api", tokens: "5" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = rateLimiterHandler.acquire({ endpoint: "openai-api", tokens: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = rateLimiterHandler.acquire({ endpoint: "openai-api", tokens: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = rateLimiterHandler.acquire({ endpoint: "openai-api", tokens: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = rateLimiterHandler.acquire({ endpoint: "openai-api", tokens: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof rateLimiterHandler.acquire !== 'function') return;
      const result = await interpret(rateLimiterHandler.acquire({ endpoint: "openai-api", tokens: "5" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "acquire_tokens" -> ok', async () => {
      if (typeof rateLimiterHandler.acquire !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_api_limiter = await interpret(rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" }), storage);
      const _pool = Object.assign({}, (afterResult_api_limiter?.output ?? {}));
      const _fixtureInput = { endpoint: "openai-api", tokens: "5" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(rateLimiterHandler.acquire({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "acquire_exhausted" -> limited', async () => {
      if (typeof rateLimiterHandler.acquire !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(rateLimiterHandler.acquire({ endpoint: "openai-api", tokens: "999" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('limited'));
    });

    it('fixture "acquire_no_limiter" -> notFound', async () => {
      if (typeof rateLimiterHandler.acquire !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(rateLimiterHandler.acquire({ endpoint: "unknown-service", tokens: "1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('release', () => {
    it('builds a valid StorageProgram', () => {
      const program = rateLimiterHandler.release({ endpoint: "openai-api", tokens: "3" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = rateLimiterHandler.release({ endpoint: "openai-api", tokens: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = rateLimiterHandler.release({ endpoint: "openai-api", tokens: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = rateLimiterHandler.release({ endpoint: "openai-api", tokens: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = rateLimiterHandler.release({ endpoint: "openai-api", tokens: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof rateLimiterHandler.release !== 'function') return;
      const result = await interpret(rateLimiterHandler.release({ endpoint: "openai-api", tokens: "3" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "release_tokens" -> ok', async () => {
      if (typeof rateLimiterHandler.release !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_api_limiter = await interpret(rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" }), storage);
      const afterResult_acquire_tokens = await interpret(rateLimiterHandler.acquire({ endpoint: "openai-api", tokens: "5" }), storage);
      const _pool = Object.assign({}, (afterResult_api_limiter?.output ?? {}), (afterResult_acquire_tokens?.output ?? {}));
      const _fixtureInput = { endpoint: "openai-api", tokens: "3" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(rateLimiterHandler.release({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "release_no_limiter" -> notFound', async () => {
      if (typeof rateLimiterHandler.release !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(rateLimiterHandler.release({ endpoint: "unknown-service", tokens: "1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = rateLimiterHandler.get({ endpoint: "openai-api" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = rateLimiterHandler.get({ endpoint: "openai-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = rateLimiterHandler.get({ endpoint: "openai-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = rateLimiterHandler.get({ endpoint: "openai-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = rateLimiterHandler.get({ endpoint: "openai-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof rateLimiterHandler.get !== 'function') return;
      const result = await interpret(rateLimiterHandler.get({ endpoint: "openai-api" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_status" -> ok', async () => {
      if (typeof rateLimiterHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_api_limiter = await interpret(rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" }), storage);
      const _pool = Object.assign({}, (afterResult_api_limiter?.output ?? {}));
      const _fixtureInput = { endpoint: "openai-api" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(rateLimiterHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_no_limiter" -> notFound', async () => {
      if (typeof rateLimiterHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(rateLimiterHandler.get({ endpoint: "unknown-service" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('reset', () => {
    it('builds a valid StorageProgram', () => {
      const program = rateLimiterHandler.reset({ endpoint: "openai-api" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = rateLimiterHandler.reset({ endpoint: "openai-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = rateLimiterHandler.reset({ endpoint: "openai-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = rateLimiterHandler.reset({ endpoint: "openai-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = rateLimiterHandler.reset({ endpoint: "openai-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof rateLimiterHandler.reset !== 'function') return;
      const result = await interpret(rateLimiterHandler.reset({ endpoint: "openai-api" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "reset_limiter" -> ok', async () => {
      if (typeof rateLimiterHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_api_limiter = await interpret(rateLimiterHandler.configure({ endpoint: "openai-api", maxTokens: "100", refillRate: "10", refillIntervalMs: "1000" }), storage);
      const _pool = Object.assign({}, (afterResult_api_limiter?.output ?? {}));
      const _fixtureInput = { endpoint: "openai-api" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(rateLimiterHandler.reset({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reset_no_limiter" -> notFound', async () => {
      if (typeof rateLimiterHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(rateLimiterHandler.reset({ endpoint: "unknown-service" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof rateLimiterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = rateLimiterHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('RateLimiter');
    });
  });

  describe('invariant examples', () => {
    it("acquire succeeds after configure", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(rateLimiterHandler.configure({ endpoint: {"type":"literal","value":"openai-api"}, maxTokens: {"type":"literal","value":100}, refillRate: {"type":"literal","value":10}, refillIntervalMs: {"type":"literal","value":1000} }), storage);
      expect(configureResult0.variant).toBe("ok");
      let limiter = configureResult0.output["limiter"];
      const thenResult0 = await interpret(rateLimiterHandler.acquire({ endpoint: {"type":"literal","value":"openai-api"}, tokens: {"type":"literal","value":1} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("acquire limited when tokens exhausted", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(rateLimiterHandler.configure({ endpoint: {"type":"literal","value":"test-api"}, maxTokens: {"type":"literal","value":2}, refillRate: {"type":"literal","value":1}, refillIntervalMs: {"type":"literal","value":60000} }), storage);
      expect(configureResult0.variant).toBe("ok");
      let limiter = configureResult0.output["limiter"];
      const acquireResult1 = await interpret(rateLimiterHandler.acquire({ endpoint: {"type":"literal","value":"test-api"}, tokens: {"type":"literal","value":2} }), storage);
      expect(acquireResult1.variant).toBe("ok");
      limiter = acquireResult1.output["limiter"];
      let remaining = acquireResult1.output["remaining"];
      const thenResult0 = await interpret(rateLimiterHandler.acquire({ endpoint: {"type":"literal","value":"test-api"}, tokens: {"type":"literal","value":1} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: limiters have valid endpoint', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), maxTokens: fc.integer({ min: 1, max: 1000 }), refillRate: fc.integer({ min: 1, max: 1000 }), refillIntervalMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('acquire'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), tokens: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('release'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), tokens: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = rateLimiterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(rateLimiterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: negative token count', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), maxTokens: fc.integer({ min: 1, max: 1000 }), refillRate: fc.integer({ min: 1, max: 1000 }), refillIntervalMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('acquire'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), tokens: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('release'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), tokens: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = rateLimiterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(rateLimiterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: negative token count
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof rateLimiterHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(rateLimiterHandler.configure({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('configure ensures on ok: ', async () => {
      if (typeof rateLimiterHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), maxTokens: fc.integer({ min: 1, max: 1000 }), refillRate: fc.integer({ min: 1, max: 1000 }), refillIntervalMs: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = rateLimiterHandler.configure(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('acquire handles empty input: ', async () => {
      if (typeof rateLimiterHandler.acquire !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(rateLimiterHandler.acquire({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('acquire ensures on ok: ', async () => {
      if (typeof rateLimiterHandler.acquire !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), tokens: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = rateLimiterHandler.acquire(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
