// RateLimiter Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { rateLimiterHandler } from '../../handlers/ts/execution/rate-limiter.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('RateLimiter imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('executes without crashing', async () => {
      if (typeof rateLimiterHandler.configure !== 'function') return;
      try {
        const result = await rateLimiterHandler.configure({ endpoint: 'test-endpoint', maxTokens: 1, refillRate: 1, refillIntervalMs: 1 }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('acquire', () => {
    it('executes without crashing', async () => {
      if (typeof rateLimiterHandler.acquire !== 'function') return;
      try {
        const result = await rateLimiterHandler.acquire({ endpoint: 'test-endpoint', tokens: 1 }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('release', () => {
    it('executes without crashing', async () => {
      if (typeof rateLimiterHandler.release !== 'function') return;
      try {
        const result = await rateLimiterHandler.release({ endpoint: 'test-endpoint', tokens: 1 }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('get', () => {
    it('executes without crashing', async () => {
      if (typeof rateLimiterHandler.get !== 'function') return;
      try {
        const result = await rateLimiterHandler.get({ endpoint: 'test-endpoint' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('reset', () => {
    it('executes without crashing', async () => {
      if (typeof rateLimiterHandler.reset !== 'function') return;
      try {
        const result = await rateLimiterHandler.reset({ endpoint: 'test-endpoint' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("acquire succeeds after configure", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await rateLimiterHandler.configure({ endpoint: {"type":"literal","value":"openai-api"}, maxTokens: {"type":"literal","value":100}, refillRate: {"type":"literal","value":10}, refillIntervalMs: {"type":"literal","value":1000} }, storage);
      expect(configureResult0.variant).toBe("ok");
      const limiter = configureResult0.output["limiter"];
      const thenResult0 = await rateLimiterHandler.acquire({ endpoint: {"type":"literal","value":"openai-api"}, tokens: {"type":"literal","value":1} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("acquire limited when tokens exhausted", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await rateLimiterHandler.configure({ endpoint: {"type":"literal","value":"test-api"}, maxTokens: {"type":"literal","value":2}, refillRate: {"type":"literal","value":1}, refillIntervalMs: {"type":"literal","value":60000} }, storage);
      expect(configureResult0.variant).toBe("ok");
      const limiter = configureResult0.output["limiter"];
      const acquireResult1 = await rateLimiterHandler.acquire({ endpoint: {"type":"literal","value":"test-api"}, tokens: {"type":"literal","value":2} }, storage);
      expect(acquireResult1.variant).toBe("ok");
      const limiter = acquireResult1.output["limiter"];
      const remaining = acquireResult1.output["remaining"];
      const thenResult0 = await rateLimiterHandler.acquire({ endpoint: {"type":"literal","value":"test-api"}, tokens: {"type":"literal","value":1} }, storage);
      expect(thenResult0.variant).toBe("limited");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: limiters have valid endpoint', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }), maxTokens: fc.integer({ min: 0, max: 1000 }), refillRate: fc.integer({ min: 0, max: 1000 }), refillIntervalMs: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('acquire'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }), tokens: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('release'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }), tokens: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = rateLimiterHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(rateLimiterHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: negative token count', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }), maxTokens: fc.integer({ min: 0, max: 1000 }), refillRate: fc.integer({ min: 0, max: 1000 }), refillIntervalMs: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('acquire'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }), tokens: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('release'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }), tokens: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = rateLimiterHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(rateLimiterHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: negative token count
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof rateLimiterHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await rateLimiterHandler.configure({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('configure ensures on ok: ', async () => {
      if (typeof rateLimiterHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), maxTokens: fc.integer({ min: 1, max: 1000 }), refillRate: fc.integer({ min: 1, max: 1000 }), refillIntervalMs: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await rateLimiterHandler.configure(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('acquire handles empty input: ', async () => {
      if (typeof rateLimiterHandler.acquire !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await rateLimiterHandler.acquire({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('acquire ensures on ok: ', async () => {
      if (typeof rateLimiterHandler.acquire !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), tokens: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await rateLimiterHandler.acquire(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
