// PromptOptimizer Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { promptOptimizerHandler } from '../../handlers/ts/llm-prompt/prompt-optimizer.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('PromptOptimizer functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptOptimizerHandler.create({ target: 'test-target', metric: 'test-metric', training_set: 'test', strategy: 'test-strategy', max_llm_calls: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptOptimizerHandler.create({ target: 'test-target', metric: 'test-metric', training_set: 'test', strategy: 'test-strategy', max_llm_calls: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptOptimizerHandler.create({ target: 'test-target', metric: 'test-metric', training_set: 'test', strategy: 'test-strategy', max_llm_calls: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptOptimizerHandler.create({ target: 'test-target', metric: 'test-metric', training_set: 'test', strategy: 'test-strategy', max_llm_calls: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptOptimizerHandler.create({ target: 'test-target', metric: 'test-metric', training_set: 'test', strategy: 'test-strategy', max_llm_calls: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptOptimizerHandler.create !== 'function') return;
      const result = await interpret(promptOptimizerHandler.create({ target: 'test-target', metric: 'test-metric', training_set: 'test', strategy: 'test-strategy', max_llm_calls: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('optimize', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptOptimizerHandler.optimize({ optimizer: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptOptimizerHandler.optimize({ optimizer: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptOptimizerHandler.optimize({ optimizer: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptOptimizerHandler.optimize({ optimizer: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptOptimizerHandler.optimize({ optimizer: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptOptimizerHandler.optimize !== 'function') return;
      const result = await interpret(promptOptimizerHandler.optimize({ optimizer: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('evaluate', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptOptimizerHandler.evaluate({ optimizer: 'test', program: 'test-program', dataset: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptOptimizerHandler.evaluate({ optimizer: 'test', program: 'test-program', dataset: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptOptimizerHandler.evaluate({ optimizer: 'test', program: 'test-program', dataset: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptOptimizerHandler.evaluate({ optimizer: 'test', program: 'test-program', dataset: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptOptimizerHandler.evaluate({ optimizer: 'test', program: 'test-program', dataset: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptOptimizerHandler.evaluate !== 'function') return;
      const result = await interpret(promptOptimizerHandler.evaluate({ optimizer: 'test', program: 'test-program', dataset: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('compare', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptOptimizerHandler.compare({ optimizer: 'test', programs: 'test', dataset: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptOptimizerHandler.compare({ optimizer: 'test', programs: 'test', dataset: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptOptimizerHandler.compare({ optimizer: 'test', programs: 'test', dataset: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptOptimizerHandler.compare({ optimizer: 'test', programs: 'test', dataset: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptOptimizerHandler.compare({ optimizer: 'test', programs: 'test', dataset: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptOptimizerHandler.compare !== 'function') return;
      const result = await interpret(promptOptimizerHandler.compare({ optimizer: 'test', programs: 'test', dataset: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('rollback', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptOptimizerHandler.rollback({ optimizer: 'test', iteration: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptOptimizerHandler.rollback({ optimizer: 'test', iteration: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptOptimizerHandler.rollback({ optimizer: 'test', iteration: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptOptimizerHandler.rollback({ optimizer: 'test', iteration: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptOptimizerHandler.rollback({ optimizer: 'test', iteration: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptOptimizerHandler.rollback !== 'function') return;
      const result = await interpret(promptOptimizerHandler.rollback({ optimizer: 'test', iteration: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof promptOptimizerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = promptOptimizerHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('PromptOptimizer');
    });
  });

  describe('invariant examples', () => {
    it("create-then-optimize", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(promptOptimizerHandler.create({ target: "test-t", metric: "accuracy", training_set: "test-ts", strategy: "mipro_v2", max_llm_calls: 100 }), storage);
      expect(createResult0.variant).toBe("ok");
      let optimizer = createResult0.output["optimizer"];
      let o = optimizer;
      const thenResult0 = await interpret(promptOptimizerHandler.optimize({ optimizer: o }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-target_program', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), metric: fc.string({ minLength: 1, maxLength: 50 }), training_set: fc.string(), strategy: fc.string({ minLength: 1, maxLength: 50 }), max_llm_calls: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('optimize'), input: fc.record({ optimizer: fc.string() }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ optimizer: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }), dataset: fc.string() }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ optimizer: fc.string(), programs: fc.string(), dataset: fc.string() }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ optimizer: fc.string(), iteration: fc.integer({ min: 1, max: 1000 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = promptOptimizerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(promptOptimizerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-metric', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), metric: fc.string({ minLength: 1, maxLength: 50 }), training_set: fc.string(), strategy: fc.string({ minLength: 1, maxLength: 50 }), max_llm_calls: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('optimize'), input: fc.record({ optimizer: fc.string() }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ optimizer: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }), dataset: fc.string() }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ optimizer: fc.string(), programs: fc.string(), dataset: fc.string() }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ optimizer: fc.string(), iteration: fc.integer({ min: 1, max: 1000 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = promptOptimizerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(promptOptimizerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-metric
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof promptOptimizerHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(promptOptimizerHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof promptOptimizerHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), metric: fc.string({ minLength: 1, maxLength: 50 }), training_set: fc.string(), strategy: fc.string({ minLength: 1, maxLength: 50 }), max_llm_calls: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = promptOptimizerHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
