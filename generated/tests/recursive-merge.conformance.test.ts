// RecursiveMerge Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { recursiveMergeHandler } from '../../handlers/ts/recursive-merge.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('RecursiveMerge functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = recursiveMergeHandler.register({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = recursiveMergeHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = recursiveMergeHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = recursiveMergeHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = recursiveMergeHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof recursiveMergeHandler.register !== 'function') return;
      try {
        const result = await interpret(recursiveMergeHandler.register({  }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof recursiveMergeHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(recursiveMergeHandler.register({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('execute', () => {
    it('builds a valid StorageProgram', () => {
      const program = recursiveMergeHandler.execute({ base: "line1\nline2", ours: "line1\nline2", theirs: "line1\nline3" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = recursiveMergeHandler.execute({ base: "line1\nline2", ours: "line1\nline2", theirs: "line1\nline3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = recursiveMergeHandler.execute({ base: "line1\nline2", ours: "line1\nline2", theirs: "line1\nline3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = recursiveMergeHandler.execute({ base: "line1\nline2", ours: "line1\nline2", theirs: "line1\nline3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = recursiveMergeHandler.execute({ base: "line1\nline2", ours: "line1\nline2", theirs: "line1\nline3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof recursiveMergeHandler.execute !== 'function') return;
      try {
        const result = await interpret(recursiveMergeHandler.execute({ base: "line1\nline2", ours: "line1\nline2", theirs: "line1\nline3" }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "clean_recursive" -> ok', async () => {
      if (typeof recursiveMergeHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(recursiveMergeHandler.execute({ base: "line1\nline2", ours: "line1\nline2", theirs: "line1\nline3" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "conflict_recursive" -> ok', async () => {
      if (typeof recursiveMergeHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(recursiveMergeHandler.execute({ base: "line1", ours: "lineA", theirs: "lineB" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "non_string_content" -> error', async () => {
      if (typeof recursiveMergeHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(recursiveMergeHandler.execute({ base: "42", ours: "text", theirs: "text" }), storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof recursiveMergeHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      let result: any;
      try {
        const r = recursiveMergeHandler.register({}, storage);
        result = r instanceof Promise ? await r : r;
        // If StorageProgram, interpret it
        if (result?.instructions && !result.variant) {
          result = await interpret(result, storage);
        }
      } catch { return; }
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('RecursiveMerge');
    });
  });

  describe('invariant examples', () => {
    it("execute then execute", async () => {
      const storage = createInMemoryStorage();
      const executeResult0 = await interpret(recursiveMergeHandler.execute({ base: {"type":"variable","name":"b"}, ours: {"type":"variable","name":"b"}, theirs: {"type":"variable","name":"t"} }), storage);
      expect(executeResult0.variant).toBe("clean");
      const result = executeResult0.output["result"];
      const thenResult0 = await interpret(recursiveMergeHandler.execute({ base: {"type":"variable","name":"b"}, ours: {"type":"variable","name":"o"}, theirs: {"type":"variable","name":"b"} }), storage);
      expect(thenResult0.variant).toBe("clean");
    });

  });

});
