// RemoteQueryProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { remoteQueryProviderHandler } from '../../handlers/ts/view/remote-query-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('RemoteQueryProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof remoteQueryProviderHandler.register !== 'function') return;
      const result = await interpret(remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_devto" -> ok', async () => {
      if (typeof remoteQueryProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_name" -> invalid', async () => {
      if (typeof remoteQueryProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(remoteQueryProviderHandler.register({ name: "", source: "devto", manifestRef: "m", projectionRef: "p", pushdownOps: "[]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof remoteQueryProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_devto = await interpret(remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_devto?.output ?? {}));
      const _fixtureInput = { name: "devto-provider", source: "devto", manifestRef: "m", projectionRef: "p", pushdownOps: "[]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(remoteQueryProviderHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('execute', () => {
    it('builds a valid StorageProgram', () => {
      const program = remoteQueryProviderHandler.execute({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\",\"bindAs\":\"records\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"},\"bindAs\":\"filtered\"},{\"type\":\"limit\",\"count\":25,\"output\":\"page\"}]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = remoteQueryProviderHandler.execute({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\",\"bindAs\":\"records\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"},\"bindAs\":\"filtered\"},{\"type\":\"limit\",\"count\":25,\"output\":\"page\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = remoteQueryProviderHandler.execute({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\",\"bindAs\":\"records\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"},\"bindAs\":\"filtered\"},{\"type\":\"limit\",\"count\":25,\"output\":\"page\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = remoteQueryProviderHandler.execute({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\",\"bindAs\":\"records\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"},\"bindAs\":\"filtered\"},{\"type\":\"limit\",\"count\":25,\"output\":\"page\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = remoteQueryProviderHandler.execute({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\",\"bindAs\":\"records\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"},\"bindAs\":\"filtered\"},{\"type\":\"limit\",\"count\":25,\"output\":\"page\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof remoteQueryProviderHandler.execute !== 'function') return;
      const result = await interpret(remoteQueryProviderHandler.execute({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\",\"bindAs\":\"records\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"},\"bindAs\":\"filtered\"},{\"type\":\"limit\",\"count\":25,\"output\":\"page\"}]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "execute_list" -> ok', async () => {
      if (typeof remoteQueryProviderHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_devto = await interpret(remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" }), storage);
      const result = await interpret(remoteQueryProviderHandler.execute({ provider: afterResult_register_devto?.output?.["provider"], program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\",\"bindAs\":\"records\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"},\"bindAs\":\"filtered\"},{\"type\":\"limit\",\"count\":25,\"output\":\"page\"}]}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "execute_missing" -> notfound', async () => {
      if (typeof remoteQueryProviderHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(remoteQueryProviderHandler.execute({ provider: "nonexistent", program: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('planPushdown', () => {
    it('builds a valid StorageProgram', () => {
      const program = remoteQueryProviderHandler.planPushdown({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"}},{\"type\":\"sort\",\"keys\":[{\"field\":\"published_at\",\"direction\":\"desc\"}]},{\"type\":\"limit\",\"count\":25}]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = remoteQueryProviderHandler.planPushdown({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"}},{\"type\":\"sort\",\"keys\":[{\"field\":\"published_at\",\"direction\":\"desc\"}]},{\"type\":\"limit\",\"count\":25}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = remoteQueryProviderHandler.planPushdown({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"}},{\"type\":\"sort\",\"keys\":[{\"field\":\"published_at\",\"direction\":\"desc\"}]},{\"type\":\"limit\",\"count\":25}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = remoteQueryProviderHandler.planPushdown({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"}},{\"type\":\"sort\",\"keys\":[{\"field\":\"published_at\",\"direction\":\"desc\"}]},{\"type\":\"limit\",\"count\":25}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = remoteQueryProviderHandler.planPushdown({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"}},{\"type\":\"sort\",\"keys\":[{\"field\":\"published_at\",\"direction\":\"desc\"}]},{\"type\":\"limit\",\"count\":25}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof remoteQueryProviderHandler.planPushdown !== 'function') return;
      const result = await interpret(remoteQueryProviderHandler.planPushdown({ provider: {"type":"ref","fixture":"register_devto","field":"provider"}, program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"}},{\"type\":\"sort\",\"keys\":[{\"field\":\"published_at\",\"direction\":\"desc\"}]},{\"type\":\"limit\",\"count\":25}]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "plan_with_pushdown" -> ok', async () => {
      if (typeof remoteQueryProviderHandler.planPushdown !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_devto = await interpret(remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" }), storage);
      const result = await interpret(remoteQueryProviderHandler.planPushdown({ provider: afterResult_register_devto?.output?.["provider"], program: "{\"instructions\":[{\"type\":\"scan\",\"source\":\"articles\"},{\"type\":\"filter\",\"node\":{\"type\":\"eq\",\"field\":\"tag\",\"value\":\"javascript\"}},{\"type\":\"sort\",\"keys\":[{\"field\":\"published_at\",\"direction\":\"desc\"}]},{\"type\":\"limit\",\"count\":25}]}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "plan_missing_provider" -> notfound', async () => {
      if (typeof remoteQueryProviderHandler.planPushdown !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(remoteQueryProviderHandler.planPushdown({ provider: "nonexistent", program: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = remoteQueryProviderHandler.get({ provider: {"type":"ref","fixture":"register_devto","field":"provider"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = remoteQueryProviderHandler.get({ provider: {"type":"ref","fixture":"register_devto","field":"provider"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = remoteQueryProviderHandler.get({ provider: {"type":"ref","fixture":"register_devto","field":"provider"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = remoteQueryProviderHandler.get({ provider: {"type":"ref","fixture":"register_devto","field":"provider"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = remoteQueryProviderHandler.get({ provider: {"type":"ref","fixture":"register_devto","field":"provider"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof remoteQueryProviderHandler.get !== 'function') return;
      const result = await interpret(remoteQueryProviderHandler.get({ provider: {"type":"ref","fixture":"register_devto","field":"provider"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof remoteQueryProviderHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_devto = await interpret(remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" }), storage);
      const result = await interpret(remoteQueryProviderHandler.get({ provider: afterResult_register_devto?.output?.["provider"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof remoteQueryProviderHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(remoteQueryProviderHandler.get({ provider: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = remoteQueryProviderHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = remoteQueryProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = remoteQueryProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = remoteQueryProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = remoteQueryProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof remoteQueryProviderHandler.list !== 'function') return;
      const result = await interpret(remoteQueryProviderHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof remoteQueryProviderHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_devto = await interpret(remoteQueryProviderHandler.register({ name: "devto-provider", source: "devto", manifestRef: "app.external.yaml", projectionRef: "projection:devto:Article", pushdownOps: "[\"filter\", \"sort\", \"limit\"]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_devto?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(remoteQueryProviderHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then execute", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(remoteQueryProviderHandler.register({ name: "test-remote", source: "test-api", manifestRef: "manifest.yaml", projectionRef: "proj:test", pushdownOps: "[\"filter\"]" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let provider = registerResult0.output["provider"];
      let p = provider;
      const thenResult0 = await interpret(remoteQueryProviderHandler.get({ provider: p }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: providers have source', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), manifestRef: fc.string({ minLength: 1, maxLength: 50 }), projectionRef: fc.string({ minLength: 1, maxLength: 50 }), pushdownOps: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ provider: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('planPushdown'), input: fc.record({ provider: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ provider: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = remoteQueryProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(remoteQueryProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: provider without name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), manifestRef: fc.string({ minLength: 1, maxLength: 50 }), projectionRef: fc.string({ minLength: 1, maxLength: 50 }), pushdownOps: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ provider: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('planPushdown'), input: fc.record({ provider: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ provider: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = remoteQueryProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(remoteQueryProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: provider without name
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof remoteQueryProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(remoteQueryProviderHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof remoteQueryProviderHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), manifestRef: fc.string({ minLength: 1, maxLength: 50 }), projectionRef: fc.string({ minLength: 1, maxLength: 50 }), pushdownOps: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = remoteQueryProviderHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
