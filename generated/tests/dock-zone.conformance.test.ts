// DockZone Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { dockZoneHandler } from '../../handlers/ts/app/dock-zone.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('DockZone functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dockZoneHandler.register !== 'function') return;
      const result = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_left" -> ok', async () => {
      if (typeof dockZoneHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_right" -> ok', async () => {
      if (typeof dockZoneHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.register({ zone: "dz-2", name: "right-panel", edge: "right", label: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_center" -> ok', async () => {
      if (typeof dockZoneHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.register({ zone: "dz-3", name: "main-area", edge: "center", label: "Main" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_name" -> invalid', async () => {
      if (typeof dockZoneHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.register({ zone: "dz-bad", name: "", edge: "left", label: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "register_invalid_edge" -> invalid', async () => {
      if (typeof dockZoneHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.register({ zone: "dz-bad2", name: "x-panel", edge: "diagonal", label: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof dockZoneHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const _pool = Object.assign({}, (afterResult_register_left?.output ?? {}));
      const _fixtureInput = { zone: "dz-dup", name: "left-panel", edge: "right", label: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(dockZoneHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('dock', () => {
    it('builds a valid StorageProgram', () => {
      const program = dockZoneHandler.dock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dockZoneHandler.dock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dockZoneHandler.dock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dockZoneHandler.dock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dockZoneHandler.dock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dockZoneHandler.dock !== 'function') return;
      const result = await interpret(dockZoneHandler.dock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "dock_pane_ok" -> ok', async () => {
      if (typeof dockZoneHandler.dock !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const result = await interpret(dockZoneHandler.dock({ zone: afterResult_register_left?.output?.["zone"], paneId: "pane-explorer-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "dock_missing_zone" -> notfound', async () => {
      if (typeof dockZoneHandler.dock !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.dock({ zone: "dz-nonexistent", paneId: "pane-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "dock_empty_pane_id" -> invalid', async () => {
      if (typeof dockZoneHandler.dock !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const result = await interpret(dockZoneHandler.dock({ zone: afterResult_register_left?.output?.["zone"], paneId: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('undock', () => {
    it('builds a valid StorageProgram', () => {
      const program = dockZoneHandler.undock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dockZoneHandler.undock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dockZoneHandler.undock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dockZoneHandler.undock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dockZoneHandler.undock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dockZoneHandler.undock !== 'function') return;
      const result = await interpret(dockZoneHandler.undock({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, paneId: "pane-explorer-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "undock_ok" -> ok', async () => {
      if (typeof dockZoneHandler.undock !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const afterResult_dock_pane_ok = await interpret(dockZoneHandler.dock({ zone: afterResult_register_left?.output?.["zone"], paneId: "pane-explorer-1" }), storage);
      const result = await interpret(dockZoneHandler.undock({ zone: afterResult_register_left?.output?.["zone"], paneId: "pane-explorer-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "undock_missing_zone" -> notfound', async () => {
      if (typeof dockZoneHandler.undock !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.undock({ zone: "dz-nonexistent", paneId: "pane-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "undock_pane_not_docked" -> notfound', async () => {
      if (typeof dockZoneHandler.undock !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_right = await interpret(dockZoneHandler.register({ zone: "dz-2", name: "right-panel", edge: "right", label: null }), storage);
      const result = await interpret(dockZoneHandler.undock({ zone: afterResult_register_right?.output?.["zone"], paneId: "pane-not-there" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('moveTo', () => {
    it('builds a valid StorageProgram', () => {
      const program = dockZoneHandler.moveTo({ fromZone: {"type":"ref","fixture":"register_left","field":"zone"}, toZone: {"type":"ref","fixture":"register_right","field":"zone"}, paneId: "pane-explorer-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dockZoneHandler.moveTo({ fromZone: {"type":"ref","fixture":"register_left","field":"zone"}, toZone: {"type":"ref","fixture":"register_right","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dockZoneHandler.moveTo({ fromZone: {"type":"ref","fixture":"register_left","field":"zone"}, toZone: {"type":"ref","fixture":"register_right","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dockZoneHandler.moveTo({ fromZone: {"type":"ref","fixture":"register_left","field":"zone"}, toZone: {"type":"ref","fixture":"register_right","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dockZoneHandler.moveTo({ fromZone: {"type":"ref","fixture":"register_left","field":"zone"}, toZone: {"type":"ref","fixture":"register_right","field":"zone"}, paneId: "pane-explorer-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dockZoneHandler.moveTo !== 'function') return;
      const result = await interpret(dockZoneHandler.moveTo({ fromZone: {"type":"ref","fixture":"register_left","field":"zone"}, toZone: {"type":"ref","fixture":"register_right","field":"zone"}, paneId: "pane-explorer-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "move_pane_ok" -> ok', async () => {
      if (typeof dockZoneHandler.moveTo !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const afterResult_dock_pane_ok = await interpret(dockZoneHandler.dock({ zone: afterResult_register_left?.output?.["zone"], paneId: "pane-explorer-1" }), storage);
      const result = await interpret(dockZoneHandler.moveTo({ fromZone: afterResult_register_left?.output?.["zone"], toZone: undefined /* unresolved ref: $register_right.zone */, paneId: "pane-explorer-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "move_missing_source" -> notfound', async () => {
      if (typeof dockZoneHandler.moveTo !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_right = await interpret(dockZoneHandler.register({ zone: "dz-2", name: "right-panel", edge: "right", label: null }), storage);
      const result = await interpret(dockZoneHandler.moveTo({ fromZone: "dz-nonexistent", toZone: afterResult_register_right?.output?.["zone"], paneId: "pane-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "move_missing_dest" -> notfound', async () => {
      if (typeof dockZoneHandler.moveTo !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const afterResult_dock_pane_ok = await interpret(dockZoneHandler.dock({ zone: afterResult_register_left?.output?.["zone"], paneId: "pane-explorer-1" }), storage);
      const result = await interpret(dockZoneHandler.moveTo({ fromZone: afterResult_register_left?.output?.["zone"], toZone: "dz-nonexistent", paneId: "pane-explorer-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('setRules', () => {
    it('builds a valid StorageProgram', () => {
      const program = dockZoneHandler.setRules({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, allowedSchemas: null, maxTabs: "3" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dockZoneHandler.setRules({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, allowedSchemas: null, maxTabs: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dockZoneHandler.setRules({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, allowedSchemas: null, maxTabs: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dockZoneHandler.setRules({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, allowedSchemas: null, maxTabs: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dockZoneHandler.setRules({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, allowedSchemas: null, maxTabs: "3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dockZoneHandler.setRules !== 'function') return;
      const result = await interpret(dockZoneHandler.setRules({ zone: {"type":"ref","fixture":"register_left","field":"zone"}, allowedSchemas: null, maxTabs: "3" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_max_tabs" -> ok', async () => {
      if (typeof dockZoneHandler.setRules !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const result = await interpret(dockZoneHandler.setRules({ zone: afterResult_register_left?.output?.["zone"], allowedSchemas: null, maxTabs: "3" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_schemas" -> ok', async () => {
      if (typeof dockZoneHandler.setRules !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_right = await interpret(dockZoneHandler.register({ zone: "dz-2", name: "right-panel", edge: "right", label: null }), storage);
      const result = await interpret(dockZoneHandler.setRules({ zone: afterResult_register_right?.output?.["zone"], allowedSchemas: "[\"editor\", \"terminal\"]", maxTabs: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_rules_missing_zone" -> notfound', async () => {
      if (typeof dockZoneHandler.setRules !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.setRules({ zone: "dz-nonexistent", allowedSchemas: null, maxTabs: "2" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "set_rules_zero_max" -> invalid', async () => {
      if (typeof dockZoneHandler.setRules !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const result = await interpret(dockZoneHandler.setRules({ zone: afterResult_register_left?.output?.["zone"], allowedSchemas: null, maxTabs: "0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "set_rules_negative_max" -> invalid', async () => {
      if (typeof dockZoneHandler.setRules !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const result = await interpret(dockZoneHandler.setRules({ zone: afterResult_register_left?.output?.["zone"], allowedSchemas: null, maxTabs: "-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('toggleAutoHide', () => {
    it('builds a valid StorageProgram', () => {
      const program = dockZoneHandler.toggleAutoHide({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dockZoneHandler.toggleAutoHide({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dockZoneHandler.toggleAutoHide({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dockZoneHandler.toggleAutoHide({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dockZoneHandler.toggleAutoHide({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dockZoneHandler.toggleAutoHide !== 'function') return;
      const result = await interpret(dockZoneHandler.toggleAutoHide({ zone: {"type":"ref","fixture":"register_left","field":"zone"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "toggle_auto_hide_ok" -> ok', async () => {
      if (typeof dockZoneHandler.toggleAutoHide !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const result = await interpret(dockZoneHandler.toggleAutoHide({ zone: afterResult_register_left?.output?.["zone"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "toggle_auto_hide_missing" -> notfound', async () => {
      if (typeof dockZoneHandler.toggleAutoHide !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.toggleAutoHide({ zone: "dz-nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = dockZoneHandler.get({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dockZoneHandler.get({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dockZoneHandler.get({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dockZoneHandler.get({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dockZoneHandler.get({ zone: {"type":"ref","fixture":"register_left","field":"zone"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dockZoneHandler.get !== 'function') return;
      const result = await interpret(dockZoneHandler.get({ zone: {"type":"ref","fixture":"register_left","field":"zone"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof dockZoneHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const result = await interpret(dockZoneHandler.get({ zone: afterResult_register_left?.output?.["zone"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof dockZoneHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dockZoneHandler.get({ zone: "dz-nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = dockZoneHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dockZoneHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dockZoneHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dockZoneHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dockZoneHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dockZoneHandler.list !== 'function') return;
      const result = await interpret(dockZoneHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof dockZoneHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_left = await interpret(dockZoneHandler.register({ zone: "dz-1", name: "left-panel", edge: "left", label: "Left Panel" }), storage);
      const _pool = Object.assign({}, (afterResult_register_left?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(dockZoneHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then dock then get", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(dockZoneHandler.register({ zone: "test-z", name: "sidebar", edge: "left", label: "Sidebar" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let zone = registerResult0.output["zone"];
      let z = zone;
      const thenResult0 = await interpret(dockZoneHandler.dock({ zone: z, paneId: "pane-filetree" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(dockZoneHandler.get({ zone: z }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

    it("dock then undock clears content", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(dockZoneHandler.register({ zone: "test-z", name: "bottom", edge: "bottom", label: false }), storage);
      expect(registerResult0.variant).toBe("ok");
      let zone = registerResult0.output["zone"];
      let z = zone;
      const dockResult1 = await interpret(dockZoneHandler.dock({ zone: z, paneId: "pane-terminal" }), storage);
      expect(dockResult1.variant).toBe("ok");
      zone = dockResult1.output["zone"];
      const thenResult0 = await interpret(dockZoneHandler.undock({ zone: z, paneId: "pane-terminal" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(dockZoneHandler.get({ zone: z }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

    it("register then duplicate rejected", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(dockZoneHandler.register({ zone: "test-z", name: "left-panel", edge: "left", label: false }), storage);
      expect(registerResult0.variant).toBe("ok");
      let zone = registerResult0.output["zone"];
      let z = zone;
      const thenResult0 = await interpret(dockZoneHandler.register({ zone: "test-z2", name: "left-panel", edge: "right", label: false }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: zones have non-empty names', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ zone: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), edge: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string() }) }),
              fc.record({ action: fc.constant('dock'), input: fc.record({ zone: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('undock'), input: fc.record({ zone: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('moveTo'), input: fc.record({ fromZone: fc.string(), toZone: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setRules'), input: fc.record({ zone: fc.string(), allowedSchemas: fc.string(), maxTabs: fc.string() }) }),
              fc.record({ action: fc.constant('toggleAutoHide'), input: fc.record({ zone: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ zone: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = dockZoneHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(dockZoneHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: invalid edge stored', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ zone: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), edge: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string() }) }),
              fc.record({ action: fc.constant('dock'), input: fc.record({ zone: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('undock'), input: fc.record({ zone: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('moveTo'), input: fc.record({ fromZone: fc.string(), toZone: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setRules'), input: fc.record({ zone: fc.string(), allowedSchemas: fc.string(), maxTabs: fc.string() }) }),
              fc.record({ action: fc.constant('toggleAutoHide'), input: fc.record({ zone: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ zone: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = dockZoneHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(dockZoneHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: invalid edge stored
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof dockZoneHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(dockZoneHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof dockZoneHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ zone: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), edge: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = dockZoneHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('dock handles empty input: ', async () => {
      if (typeof dockZoneHandler.dock !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(dockZoneHandler.dock({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('dock ensures on ok: ', async () => {
      if (typeof dockZoneHandler.dock !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ zone: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = dockZoneHandler.dock(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('setRules handles empty input: ', async () => {
      if (typeof dockZoneHandler.setRules !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(dockZoneHandler.setRules({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('setRules ensures on ok: ', async () => {
      if (typeof dockZoneHandler.setRules !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ zone: fc.string(), allowedSchemas: fc.string(), maxTabs: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = dockZoneHandler.setRules(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
