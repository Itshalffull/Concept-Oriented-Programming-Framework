// CommutativityProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { commutativityProviderHandler } from '../../handlers/ts/monadic/providers/commutativity-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('CommutativityProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('check', () => {
    it('builds a valid StorageProgram', () => {
      const program = commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "put(orders, o1, data)", readWriteSetsA: "{\"r\":[],\"w\":[\"users\"]}", readWriteSetsB: "{\"r\":[],\"w\":[\"orders\"]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "put(orders, o1, data)", readWriteSetsA: "{\"r\":[],\"w\":[\"users\"]}", readWriteSetsB: "{\"r\":[],\"w\":[\"orders\"]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "put(orders, o1, data)", readWriteSetsA: "{\"r\":[],\"w\":[\"users\"]}", readWriteSetsB: "{\"r\":[],\"w\":[\"orders\"]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "put(orders, o1, data)", readWriteSetsA: "{\"r\":[],\"w\":[\"users\"]}", readWriteSetsB: "{\"r\":[],\"w\":[\"orders\"]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "put(orders, o1, data)", readWriteSetsA: "{\"r\":[],\"w\":[\"users\"]}", readWriteSetsB: "{\"r\":[],\"w\":[\"orders\"]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof commutativityProviderHandler.check !== 'function') return;
      const result = await interpret(commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "put(orders, o1, data)", readWriteSetsA: "{\"r\":[],\"w\":[\"users\"]}", readWriteSetsB: "{\"r\":[],\"w\":[\"orders\"]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "disjoint_sets" -> ok', async () => {
      if (typeof commutativityProviderHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "put(orders, o1, data)", readWriteSetsA: "{\"r\":[],\"w\":[\"users\"]}", readWriteSetsB: "{\"r\":[],\"w\":[\"orders\"]}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "overlapping_sets" -> ok', async () => {
      if (typeof commutativityProviderHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "get(users, u1)", readWriteSetsA: "{\"r\":[],\"w\":[\"users\"]}", readWriteSetsB: "{\"r\":[\"users\"],\"w\":[]}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_sets" -> error', async () => {
      if (typeof commutativityProviderHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(commutativityProviderHandler.check({ programA: "", programB: "", readWriteSetsA: "", readWriteSetsB: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof commutativityProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = commutativityProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('CommutativityProvider');
    });
  });

  describe('invariant examples', () => {
    it("disjoint write sets commute", async () => {
      const storage = createInMemoryStorage();
      const checkResult0 = await interpret(commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "put(orders, o1, data)", readWriteSetsA: "{r: [], w: [users]}", readWriteSetsB: "{r: [], w: [orders]}" }), storage);
      expect(checkResult0.variant).toBe("ok");
      let result = checkResult0.output["result"];
      let commutes = checkResult0.output["commutes"];
      let reason = checkResult0.output["reason"];
    });

    it("overlapping read-write sets do not commute", async () => {
      const storage = createInMemoryStorage();
      const checkResult0 = await interpret(commutativityProviderHandler.check({ programA: "put(users, u1, data)", programB: "get(users, u1)", readWriteSetsA: "{r: [], w: [users]}", readWriteSetsB: "{r: [users], w: []}" }), storage);
      expect(checkResult0.variant).toBe("ok");
      let result = checkResult0.output["result"];
      let commutes = checkResult0.output["commutes"];
      let reason = checkResult0.output["reason"];
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: results record both programs', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('check'), input: fc.record({ programA: fc.string({ minLength: 1, maxLength: 50 }), programB: fc.string({ minLength: 1, maxLength: 50 }), readWriteSetsA: fc.string({ minLength: 1, maxLength: 50 }), readWriteSetsB: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = commutativityProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(commutativityProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('check handles empty input: ', async () => {
      if (typeof commutativityProviderHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(commutativityProviderHandler.check({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('check ensures on ok: ', async () => {
      if (typeof commutativityProviderHandler.check !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ programA: fc.string({ minLength: 1, maxLength: 50 }), programB: fc.string({ minLength: 1, maxLength: 50 }), readWriteSetsA: fc.string({ minLength: 1, maxLength: 50 }), readWriteSetsB: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = commutativityProviderHandler.check(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
