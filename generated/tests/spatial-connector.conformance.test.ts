// SpatialConnector Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { spatialConnectorHandler } from '../../handlers/ts/spatial-connector.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SpatialConnector functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('draw', () => {
    it('builds a valid StorageProgram', () => {
      const program = spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof spatialConnectorHandler.draw !== 'function') return;
      const result = await interpret(spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_draw" -> ok', async () => {
      if (typeof spatialConnectorHandler.draw !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "semantic_draw" -> ok', async () => {
      if (typeof spatialConnectorHandler.draw !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-c", type: "semantic" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('promote', () => {
    it('builds a valid StorageProgram', () => {
      const program = spatialConnectorHandler.promote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = spatialConnectorHandler.promote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = spatialConnectorHandler.promote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = spatialConnectorHandler.promote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = spatialConnectorHandler.promote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof spatialConnectorHandler.promote !== 'function') return;
      const result = await interpret(spatialConnectorHandler.promote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_promote" -> ok', async () => {
      if (typeof spatialConnectorHandler.promote !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_draw = await interpret(spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" }), storage);
      const result = await interpret(spatialConnectorHandler.promote({ connector: afterResult_valid_draw?.output?.["connector"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "already_promoted" -> already_semantic', async () => {
      if (typeof spatialConnectorHandler.promote !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(spatialConnectorHandler.promote({ connector: "connector-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('already_semantic'));
    });

  });

  describe('demote', () => {
    it('builds a valid StorageProgram', () => {
      const program = spatialConnectorHandler.demote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = spatialConnectorHandler.demote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = spatialConnectorHandler.demote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = spatialConnectorHandler.demote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = spatialConnectorHandler.demote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof spatialConnectorHandler.demote !== 'function') return;
      const result = await interpret(spatialConnectorHandler.demote({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_demote" -> ok', async () => {
      if (typeof spatialConnectorHandler.demote !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_draw = await interpret(spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" }), storage);
      const result = await interpret(spatialConnectorHandler.demote({ connector: afterResult_valid_draw?.output?.["connector"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "demote_visual" -> not_semantic', async () => {
      if (typeof spatialConnectorHandler.demote !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(spatialConnectorHandler.demote({ connector: "connector-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_semantic'));
    });

  });

  describe('surface', () => {
    it('builds a valid StorageProgram', () => {
      const program = spatialConnectorHandler.surface({ canvas: "c1", source: "node-a", target: "node-b" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = spatialConnectorHandler.surface({ canvas: "c1", source: "node-a", target: "node-b" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = spatialConnectorHandler.surface({ canvas: "c1", source: "node-a", target: "node-b" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = spatialConnectorHandler.surface({ canvas: "c1", source: "node-a", target: "node-b" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = spatialConnectorHandler.surface({ canvas: "c1", source: "node-a", target: "node-b" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof spatialConnectorHandler.surface !== 'function') return;
      const result = await interpret(spatialConnectorHandler.surface({ canvas: "c1", source: "node-a", target: "node-b" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_surface" -> ok', async () => {
      if (typeof spatialConnectorHandler.surface !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_draw = await interpret(spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_draw?.output ?? {}));
      const _fixtureInput = { canvas: "c1", source: "node-a", target: "node-b" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(spatialConnectorHandler.surface({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "surface_no_ref" -> no_reference', async () => {
      if (typeof spatialConnectorHandler.surface !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(spatialConnectorHandler.surface({ canvas: "c1", source: "orphan-x", target: "orphan-y" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('no_reference'));
    });

  });

  describe('hide', () => {
    it('builds a valid StorageProgram', () => {
      const program = spatialConnectorHandler.hide({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = spatialConnectorHandler.hide({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = spatialConnectorHandler.hide({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = spatialConnectorHandler.hide({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = spatialConnectorHandler.hide({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof spatialConnectorHandler.hide !== 'function') return;
      const result = await interpret(spatialConnectorHandler.hide({ connector: {"type":"ref","fixture":"valid_draw","field":"connector"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_hide" -> ok', async () => {
      if (typeof spatialConnectorHandler.hide !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_draw = await interpret(spatialConnectorHandler.draw({ canvas: "c1", source: "node-a", target: "node-b", type: "visual" }), storage);
      const result = await interpret(spatialConnectorHandler.hide({ connector: afterResult_valid_draw?.output?.["connector"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "hide_missing" -> notFound', async () => {
      if (typeof spatialConnectorHandler.hide !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(spatialConnectorHandler.hide({ connector: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof spatialConnectorHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = spatialConnectorHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('SpatialConnector');
    });
  });

  describe('invariant examples', () => {
    it("draw-then-promote", async () => {
      const storage = createInMemoryStorage();
      const drawResult0 = await interpret(spatialConnectorHandler.draw({ canvas: "c1", source: "a", target: "b", type: "visual" }), storage);
      expect(drawResult0.variant).toBe("ok");
      let connector = drawResult0.output["connector"];
      let conn = connector;
      const thenResult0 = await interpret(spatialConnectorHandler.promote({ connector: conn }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-connector_canvas', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('draw'), input: fc.record({ canvas: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), type: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('promote'), input: fc.record({ connector: fc.string() }) }),
              fc.record({ action: fc.constant('demote'), input: fc.record({ connector: fc.string() }) }),
              fc.record({ action: fc.constant('surface'), input: fc.record({ canvas: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('hide'), input: fc.record({ connector: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = spatialConnectorHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(spatialConnectorHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-connector_source', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('draw'), input: fc.record({ canvas: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), type: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('promote'), input: fc.record({ connector: fc.string() }) }),
              fc.record({ action: fc.constant('demote'), input: fc.record({ connector: fc.string() }) }),
              fc.record({ action: fc.constant('surface'), input: fc.record({ canvas: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('hide'), input: fc.record({ connector: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = spatialConnectorHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(spatialConnectorHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-connector_source
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('draw handles empty input: ', async () => {
      if (typeof spatialConnectorHandler.draw !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(spatialConnectorHandler.draw({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('draw ensures on ok: ', async () => {
      if (typeof spatialConnectorHandler.draw !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ canvas: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), type: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = spatialConnectorHandler.draw(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
