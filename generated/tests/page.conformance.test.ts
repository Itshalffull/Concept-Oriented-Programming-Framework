// Page Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { pageHandler } from '../../handlers/ts/page.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Page functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.create !== 'function') return;
      const result = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_article_page" -> ok', async () => {
      if (typeof pageHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_home_page" -> ok', async () => {
      if (typeof pageHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.create({ page: "page-2", slug: "home", title: "Home", urlPattern: "/", renderMode: "static", description: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_dashboard_page" -> ok', async () => {
      if (typeof pageHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.create({ page: "page-3", slug: "dashboard", title: "Dashboard", urlPattern: "/dashboard", renderMode: "client", description: "Interactive user dashboard" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_slug" -> invalid', async () => {
      if (typeof pageHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.create({ page: "page-4", slug: "", title: "Bad Page", urlPattern: "/bad", renderMode: "server" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "bad_render_mode" -> invalid', async () => {
      if (typeof pageHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.create({ page: "page-5", slug: "test", title: "Test", urlPattern: "/test", renderMode: "hybrid" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "duplicate_slug" -> duplicate', async () => {
      if (typeof pageHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_page?.output ?? {}));
      const _fixtureInput = { page: "page-6", slug: "article-detail", title: "Another Article", urlPattern: "/articles-alt/:id", renderMode: "server" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(pageHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.get({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.get({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.get({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.get({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.get({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.get !== 'function') return;
      const result = await interpret(pageHandler.get({ page: {"type":"ref","fixture":"create_article_page","field":"page"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof pageHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.get({ page: afterResult_create_article_page?.output?.["page"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof pageHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.get({ page: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.list !== 'function') return;
      const result = await interpret(pageHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof pageHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const _pool = Object.assign({}, (afterResult_create_article_page?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(pageHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('publish', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.publish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.publish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.publish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.publish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.publish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.publish !== 'function') return;
      const result = await interpret(pageHandler.publish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "publish_draft" -> ok', async () => {
      if (typeof pageHandler.publish !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.publish({ page: afterResult_create_article_page?.output?.["page"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "publish_missing" -> notfound', async () => {
      if (typeof pageHandler.publish !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.publish({ page: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('unpublish', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.unpublish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.unpublish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.unpublish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.unpublish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.unpublish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.unpublish !== 'function') return;
      const result = await interpret(pageHandler.unpublish({ page: {"type":"ref","fixture":"create_article_page","field":"page"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "unpublish_published" -> ok', async () => {
      if (typeof pageHandler.unpublish !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.unpublish({ page: afterResult_create_article_page?.output?.["page"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unpublish_missing" -> notfound', async () => {
      if (typeof pageHandler.unpublish !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.unpublish({ page: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('updatePattern', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.updatePattern({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, urlPattern: "/articles/:id/comments/:commentId" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.updatePattern({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, urlPattern: "/articles/:id/comments/:commentId" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.updatePattern({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, urlPattern: "/articles/:id/comments/:commentId" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.updatePattern({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, urlPattern: "/articles/:id/comments/:commentId" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.updatePattern({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, urlPattern: "/articles/:id/comments/:commentId" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.updatePattern !== 'function') return;
      const result = await interpret(pageHandler.updatePattern({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, urlPattern: "/articles/:id/comments/:commentId" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "update_pattern" -> ok', async () => {
      if (typeof pageHandler.updatePattern !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.updatePattern({ page: afterResult_create_article_page?.output?.["page"], urlPattern: "/articles/:id/comments/:commentId" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "update_pattern_missing" -> notfound', async () => {
      if (typeof pageHandler.updatePattern !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.updatePattern({ page: "nonexistent", urlPattern: "/foo" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('setRenderMode', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.setRenderMode({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, renderMode: "client" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.setRenderMode({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, renderMode: "client" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.setRenderMode({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, renderMode: "client" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.setRenderMode({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, renderMode: "client" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.setRenderMode({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, renderMode: "client" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.setRenderMode !== 'function') return;
      const result = await interpret(pageHandler.setRenderMode({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, renderMode: "client" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_to_client" -> ok', async () => {
      if (typeof pageHandler.setRenderMode !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.setRenderMode({ page: afterResult_create_article_page?.output?.["page"], renderMode: "client" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_invalid_mode" -> invalid', async () => {
      if (typeof pageHandler.setRenderMode !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.setRenderMode({ page: afterResult_create_article_page?.output?.["page"], renderMode: "hybrid" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "set_mode_missing" -> notfound', async () => {
      if (typeof pageHandler.setRenderMode !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.setRenderMode({ page: "nonexistent", renderMode: "server" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('setLayout', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.setLayout({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, layoutRef: "layouts/two-column" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.setLayout({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, layoutRef: "layouts/two-column" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.setLayout({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, layoutRef: "layouts/two-column" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.setLayout({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, layoutRef: "layouts/two-column" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.setLayout({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, layoutRef: "layouts/two-column" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.setLayout !== 'function') return;
      const result = await interpret(pageHandler.setLayout({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, layoutRef: "layouts/two-column" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_layout" -> ok', async () => {
      if (typeof pageHandler.setLayout !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.setLayout({ page: afterResult_create_article_page?.output?.["page"], layoutRef: "layouts/two-column" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_layout_missing" -> notfound', async () => {
      if (typeof pageHandler.setLayout !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.setLayout({ page: "nonexistent", layoutRef: "layouts/default" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('setMeta', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.setMeta({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, meta: "{\"og:title\": \"Articles\", \"og:description\": \"Browse articles\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.setMeta({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, meta: "{\"og:title\": \"Articles\", \"og:description\": \"Browse articles\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.setMeta({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, meta: "{\"og:title\": \"Articles\", \"og:description\": \"Browse articles\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.setMeta({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, meta: "{\"og:title\": \"Articles\", \"og:description\": \"Browse articles\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.setMeta({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, meta: "{\"og:title\": \"Articles\", \"og:description\": \"Browse articles\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.setMeta !== 'function') return;
      const result = await interpret(pageHandler.setMeta({ page: {"type":"ref","fixture":"create_article_page","field":"page"}, meta: "{\"og:title\": \"Articles\", \"og:description\": \"Browse articles\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_meta_seo" -> ok', async () => {
      if (typeof pageHandler.setMeta !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.setMeta({ page: afterResult_create_article_page?.output?.["page"], meta: "{\"og:title\": \"Articles\", \"og:description\": \"Browse articles\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_meta_invalid_json" -> invalid', async () => {
      if (typeof pageHandler.setMeta !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.setMeta({ page: afterResult_create_article_page?.output?.["page"], meta: "not-valid-json" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "set_meta_missing" -> notfound', async () => {
      if (typeof pageHandler.setMeta !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.setMeta({ page: "nonexistent", meta: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('delete', () => {
    it('builds a valid StorageProgram', () => {
      const program = pageHandler.delete({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = pageHandler.delete({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = pageHandler.delete({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = pageHandler.delete({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = pageHandler.delete({ page: {"type":"ref","fixture":"create_article_page","field":"page"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof pageHandler.delete !== 'function') return;
      const result = await interpret(pageHandler.delete({ page: {"type":"ref","fixture":"create_article_page","field":"page"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "delete_draft" -> ok', async () => {
      if (typeof pageHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_article_page = await interpret(pageHandler.create({ page: "page-1", slug: "article-detail", title: "Article Detail", urlPattern: "/articles/:id", renderMode: "server", description: "Displays a single article by ID" }), storage);
      const result = await interpret(pageHandler.delete({ page: afterResult_create_article_page?.output?.["page"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "delete_missing" -> notfound', async () => {
      if (typeof pageHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(pageHandler.delete({ page: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof pageHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = pageHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Page');
    });
  });

  describe('invariant examples', () => {
    it("create then get", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(pageHandler.create({ page: "test-p", slug: "blog-post", title: "Blog Post", urlPattern: "/blog/:slug", renderMode: "server", description: "A blog post page" }), storage);
      expect(createResult0.variant).toBe("ok");
      let page = createResult0.output["page"];
      let p = page;
      const thenResult0 = await interpret(pageHandler.get({ page: p }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("create publish unpublish", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(pageHandler.create({ page: "test-p", slug: "about", title: "About", urlPattern: "/about", renderMode: "static", description: "test-_" }), storage);
      expect(createResult0.variant).toBe("ok");
      let page = createResult0.output["page"];
      let p = page;
      const thenResult0 = await interpret(pageHandler.publish({ page: p }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: pages have required identity fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ page: fc.string(), slug: fc.string({ minLength: 1, maxLength: 50 }), title: fc.string({ minLength: 1, maxLength: 50 }), urlPattern: fc.string({ minLength: 1, maxLength: 50 }), renderMode: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ page: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('publish'), input: fc.record({ page: fc.string() }) }),
              fc.record({ action: fc.constant('unpublish'), input: fc.record({ page: fc.string() }) }),
              fc.record({ action: fc.constant('updatePattern'), input: fc.record({ page: fc.string(), urlPattern: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setRenderMode'), input: fc.record({ page: fc.string(), renderMode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setLayout'), input: fc.record({ page: fc.string(), layoutRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setMeta'), input: fc.record({ page: fc.string(), meta: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ page: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = pageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(pageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: renderMode is valid', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ page: fc.string(), slug: fc.string({ minLength: 1, maxLength: 50 }), title: fc.string({ minLength: 1, maxLength: 50 }), urlPattern: fc.string({ minLength: 1, maxLength: 50 }), renderMode: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ page: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('publish'), input: fc.record({ page: fc.string() }) }),
              fc.record({ action: fc.constant('unpublish'), input: fc.record({ page: fc.string() }) }),
              fc.record({ action: fc.constant('updatePattern'), input: fc.record({ page: fc.string(), urlPattern: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setRenderMode'), input: fc.record({ page: fc.string(), renderMode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setLayout'), input: fc.record({ page: fc.string(), layoutRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setMeta'), input: fc.record({ page: fc.string(), meta: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ page: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = pageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(pageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: published page without URL pattern', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ page: fc.string(), slug: fc.string({ minLength: 1, maxLength: 50 }), title: fc.string({ minLength: 1, maxLength: 50 }), urlPattern: fc.string({ minLength: 1, maxLength: 50 }), renderMode: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ page: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('publish'), input: fc.record({ page: fc.string() }) }),
              fc.record({ action: fc.constant('unpublish'), input: fc.record({ page: fc.string() }) }),
              fc.record({ action: fc.constant('updatePattern'), input: fc.record({ page: fc.string(), urlPattern: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setRenderMode'), input: fc.record({ page: fc.string(), renderMode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setLayout'), input: fc.record({ page: fc.string(), layoutRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setMeta'), input: fc.record({ page: fc.string(), meta: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ page: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = pageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(pageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: published page without URL pattern
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof pageHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(pageHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof pageHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ page: fc.string(), slug: fc.string({ minLength: 1, maxLength: 50 }), title: fc.string({ minLength: 1, maxLength: 50 }), urlPattern: fc.string({ minLength: 1, maxLength: 50 }), renderMode: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = pageHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('get ensures on ok: ', async () => {
      if (typeof pageHandler.get !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ page: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = pageHandler.get(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('publish ensures on ok: ', async () => {
      if (typeof pageHandler.publish !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ page: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = pageHandler.publish(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
