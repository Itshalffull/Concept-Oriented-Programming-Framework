// ManifestReader Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { manifestReaderHandler } from '../../handlers/ts/app/manifest-reader.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ManifestReader functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      const result = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_ok" -> ok', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_editorconfig" -> ok', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(manifestReaderHandler.register({ reader: "editorconfig", formats: [".editorconfig"], priority: "5" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_clef_yaml" -> ok', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(manifestReaderHandler.register({ reader: "clef-yaml", formats: [".clef/providers.yaml",".clef/providers.yml"], priority: "100" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_tree_sitter" -> ok', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(manifestReaderHandler.register({ reader: "tree-sitter", formats: ["node_modules/tree-sitter-*/package.json"], priority: "20" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_reader" -> error', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(manifestReaderHandler.register({ reader: "", formats: [".prettierrc"], priority: "10" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_empty_formats" -> error', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(manifestReaderHandler.register({ reader: "bad-reader", formats: [], priority: "10" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { reader: "prettier-config", formats: [".prettierrc"], priority: "10" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(manifestReaderHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('read', () => {
    it('builds a valid StorageProgram', () => {
      const program = manifestReaderHandler.read({ reader: "prettier-config", path: ".prettierrc" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = manifestReaderHandler.read({ reader: "prettier-config", path: ".prettierrc" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = manifestReaderHandler.read({ reader: "prettier-config", path: ".prettierrc" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = manifestReaderHandler.read({ reader: "prettier-config", path: ".prettierrc" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = manifestReaderHandler.read({ reader: "prettier-config", path: ".prettierrc" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof manifestReaderHandler.read !== 'function') return;
      const result = await interpret(manifestReaderHandler.read({ reader: "prettier-config", path: ".prettierrc" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "read_ok" -> ok', async () => {
      if (typeof manifestReaderHandler.read !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { reader: "prettier-config", path: ".prettierrc" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(manifestReaderHandler.read({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "read_empty_path" -> error', async () => {
      if (typeof manifestReaderHandler.read !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(manifestReaderHandler.read({ reader: "prettier-config", path: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "read_no_reader" -> not_found', async () => {
      if (typeof manifestReaderHandler.read !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(manifestReaderHandler.read({ reader: "nonexistent-reader", path: ".prettierrc" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('listReaders', () => {
    it('builds a valid StorageProgram', () => {
      const program = manifestReaderHandler.listReaders({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = manifestReaderHandler.listReaders({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = manifestReaderHandler.listReaders({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = manifestReaderHandler.listReaders({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = manifestReaderHandler.listReaders({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof manifestReaderHandler.listReaders !== 'function') return;
      const result = await interpret(manifestReaderHandler.listReaders({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof manifestReaderHandler.listReaders !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(manifestReaderHandler.listReaders({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_after_register" -> ok', async () => {
      if (typeof manifestReaderHandler.listReaders !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc",".prettierrc.json",".prettierrc.yaml","prettier.config.js"], priority: "10" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(manifestReaderHandler.listReaders({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then listReaders includes reader id", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc"], priority: 10 }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let _ = id;
      const thenResult0 = await interpret(manifestReaderHandler.listReaders({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register then read dispatches to reader", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc"], priority: 10 }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let _ = id;
      const thenResult0 = await interpret(manifestReaderHandler.read({ reader: "prettier-config", path: ".prettierrc" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("read unregistered reader returns not_found", async () => {
      const storage = createInMemoryStorage();
    });

    it("duplicate reader id is rejected", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc"], priority: 10 }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let _ = id;
      const thenResult0 = await interpret(manifestReaderHandler.register({ reader: "prettier-config", formats: [".prettierrc.json"], priority: 5 }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every reader has a non-empty id', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ reader: fc.string({ minLength: 1, maxLength: 50 }), formats: fc.string(), priority: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('read'), input: fc.record({ reader: fc.string({ minLength: 1, maxLength: 50 }), path: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listReaders'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = manifestReaderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(manifestReaderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: every reader has at least one format pattern', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ reader: fc.string({ minLength: 1, maxLength: 50 }), formats: fc.string(), priority: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('read'), input: fc.record({ reader: fc.string({ minLength: 1, maxLength: 50 }), path: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listReaders'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = manifestReaderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(manifestReaderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: two readers share the same id', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ reader: fc.string({ minLength: 1, maxLength: 50 }), formats: fc.string(), priority: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('read'), input: fc.record({ reader: fc.string({ minLength: 1, maxLength: 50 }), path: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listReaders'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = manifestReaderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(manifestReaderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: two readers share the same id
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(manifestReaderHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof manifestReaderHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ reader: fc.string({ minLength: 1, maxLength: 50 }), formats: fc.string(), priority: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = manifestReaderHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
