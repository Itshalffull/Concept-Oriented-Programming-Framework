// StepRun Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { stepRunHandler } from '../../handlers/ts/process-foundation/step-run.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('StepRun functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('start', () => {
    it('builds a valid StorageProgram', () => {
      const program = stepRunHandler.start({ run_ref: 'test-run_ref', step_key: 'test-step_key', step_type: 'test-step_type', input: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stepRunHandler.start({ run_ref: 'test-run_ref', step_key: 'test-step_key', step_type: 'test-step_type', input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stepRunHandler.start({ run_ref: 'test-run_ref', step_key: 'test-step_key', step_type: 'test-step_type', input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stepRunHandler.start({ run_ref: 'test-run_ref', step_key: 'test-step_key', step_type: 'test-step_type', input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stepRunHandler.start({ run_ref: 'test-run_ref', step_key: 'test-step_key', step_type: 'test-step_type', input: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stepRunHandler.start !== 'function') return;
      const result = await interpret(stepRunHandler.start({ run_ref: 'test-run_ref', step_key: 'test-step_key', step_type: 'test-step_type', input: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('complete', () => {
    it('builds a valid StorageProgram', () => {
      const program = stepRunHandler.complete({ step: 'test', output: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stepRunHandler.complete({ step: 'test', output: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stepRunHandler.complete({ step: 'test', output: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stepRunHandler.complete({ step: 'test', output: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stepRunHandler.complete({ step: 'test', output: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stepRunHandler.complete !== 'function') return;
      const result = await interpret(stepRunHandler.complete({ step: 'test', output: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('fail', () => {
    it('builds a valid StorageProgram', () => {
      const program = stepRunHandler.fail({ step: 'test', error: 'test-error' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stepRunHandler.fail({ step: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stepRunHandler.fail({ step: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stepRunHandler.fail({ step: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stepRunHandler.fail({ step: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stepRunHandler.fail !== 'function') return;
      const result = await interpret(stepRunHandler.fail({ step: 'test', error: 'test-error' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('cancel', () => {
    it('builds a valid StorageProgram', () => {
      const program = stepRunHandler.cancel({ step: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stepRunHandler.cancel({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stepRunHandler.cancel({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stepRunHandler.cancel({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stepRunHandler.cancel({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stepRunHandler.cancel !== 'function') return;
      const result = await interpret(stepRunHandler.cancel({ step: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('skip', () => {
    it('builds a valid StorageProgram', () => {
      const program = stepRunHandler.skip({ step: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stepRunHandler.skip({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stepRunHandler.skip({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stepRunHandler.skip({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stepRunHandler.skip({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stepRunHandler.skip !== 'function') return;
      const result = await interpret(stepRunHandler.skip({ step: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = stepRunHandler.get({ step: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stepRunHandler.get({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stepRunHandler.get({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stepRunHandler.get({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stepRunHandler.get({ step: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stepRunHandler.get !== 'function') return;
      const result = await interpret(stepRunHandler.get({ step: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('seed', () => {
    it('builds a valid StorageProgram', () => {
      const program = stepRunHandler.seed({ run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stepRunHandler.seed({ run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stepRunHandler.seed({ run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stepRunHandler.seed({ run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stepRunHandler.seed({ run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stepRunHandler.seed !== 'function') return;
      const result = await interpret(stepRunHandler.seed({ run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "seed_ok" -> ok', async () => {
      if (typeof stepRunHandler.seed !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(stepRunHandler.seed({ run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "seed_duplicate" -> duplicate', async () => {
      if (typeof stepRunHandler.seed !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_seed_ok = await interpret(stepRunHandler.seed({ run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" }), storage);
      const _pool = Object.assign({}, (afterResult_seed_ok?.output ?? {}));
      const _fixtureInput = { run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(stepRunHandler.seed({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "seed_empty_run_ref" -> error', async () => {
      if (typeof stepRunHandler.seed !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(stepRunHandler.seed({ run_ref: "", step_key: "fetch-user", step_type: "automation", input: "e30=", output: "e30=" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('getIO', () => {
    it('builds a valid StorageProgram', () => {
      const program = stepRunHandler.getIO({ step: {"type":"ref","fixture":"seed_ok","field":"step"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stepRunHandler.getIO({ step: {"type":"ref","fixture":"seed_ok","field":"step"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stepRunHandler.getIO({ step: {"type":"ref","fixture":"seed_ok","field":"step"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stepRunHandler.getIO({ step: {"type":"ref","fixture":"seed_ok","field":"step"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stepRunHandler.getIO({ step: {"type":"ref","fixture":"seed_ok","field":"step"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stepRunHandler.getIO !== 'function') return;
      const result = await interpret(stepRunHandler.getIO({ step: {"type":"ref","fixture":"seed_ok","field":"step"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "getio_ok" -> ok', async () => {
      if (typeof stepRunHandler.getIO !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_seed_ok = await interpret(stepRunHandler.seed({ run_ref: "run-42", step_key: "fetch-user", step_type: "automation", input: "eyJpZCI6IjEifQ==", output: "eyJ1c2VyIjoiYWxpY2UifQ==" }), storage);
      const result = await interpret(stepRunHandler.getIO({ step: afterResult_seed_ok?.output?.["step"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "getio_missing" -> notfound', async () => {
      if (typeof stepRunHandler.getIO !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(stepRunHandler.getIO({ step: "nonexistent-step" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof stepRunHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = stepRunHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('StepRun');
    });
  });

  describe('invariant examples', () => {
    it("start then complete", async () => {
      const storage = createInMemoryStorage();
      const startResult0 = await interpret(stepRunHandler.start({ run_ref: "r1", step_key: "kyc", step_type: "automation", input: "test-d" }), storage);
      expect(startResult0.variant).toBe("ok");
      let step = startResult0.output["step"];
      let s = step;
      let run_ref = startResult0.output["run_ref"];
      let step_key = startResult0.output["step_key"];
      let step_type = startResult0.output["step_type"];
      const thenResult0 = await interpret(stepRunHandler.complete({ step: s, output: "test-o" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: steps have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ run_ref: fc.string({ minLength: 1, maxLength: 50 }), step_key: fc.string({ minLength: 1, maxLength: 50 }), step_type: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string() }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ step: fc.string(), output: fc.string() }) }),
              fc.record({ action: fc.constant('fail'), input: fc.record({ step: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('cancel'), input: fc.record({ step: fc.string() }) }),
              fc.record({ action: fc.constant('skip'), input: fc.record({ step: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ step: fc.string() }) }),
              fc.record({ action: fc.constant('seed'), input: fc.record({ run_ref: fc.string({ minLength: 1, maxLength: 50 }), step_key: fc.string({ minLength: 1, maxLength: 50 }), step_type: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string(), output: fc.string() }) }),
              fc.record({ action: fc.constant('getIO'), input: fc.record({ step: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = stepRunHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(stepRunHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in steps', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ run_ref: fc.string({ minLength: 1, maxLength: 50 }), step_key: fc.string({ minLength: 1, maxLength: 50 }), step_type: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string() }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ step: fc.string(), output: fc.string() }) }),
              fc.record({ action: fc.constant('fail'), input: fc.record({ step: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('cancel'), input: fc.record({ step: fc.string() }) }),
              fc.record({ action: fc.constant('skip'), input: fc.record({ step: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ step: fc.string() }) }),
              fc.record({ action: fc.constant('seed'), input: fc.record({ run_ref: fc.string({ minLength: 1, maxLength: 50 }), step_key: fc.string({ minLength: 1, maxLength: 50 }), step_type: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string(), output: fc.string() }) }),
              fc.record({ action: fc.constant('getIO'), input: fc.record({ step: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = stepRunHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(stepRunHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in steps
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('start handles empty input: ', async () => {
      if (typeof stepRunHandler.start !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(stepRunHandler.start({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('start ensures on ok: ', async () => {
      if (typeof stepRunHandler.start !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ run_ref: fc.string({ minLength: 1, maxLength: 50 }), step_key: fc.string({ minLength: 1, maxLength: 50 }), step_type: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = stepRunHandler.start(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('complete ensures on ok: ', async () => {
      if (typeof stepRunHandler.complete !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ step: fc.string(), output: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = stepRunHandler.complete(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('fail handles empty input: ', async () => {
      if (typeof stepRunHandler.fail !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(stepRunHandler.fail({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fail ensures on ok: ', async () => {
      if (typeof stepRunHandler.fail !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ step: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = stepRunHandler.fail(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
