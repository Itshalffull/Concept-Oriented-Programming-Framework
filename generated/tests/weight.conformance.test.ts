// Weight Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { weightHandler } from '../../handlers/ts/app/governance/weight.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('Weight functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('updateWeight', () => {
    it('builds a valid StorageProgram', () => {
      const program = weightHandler.updateWeight({ participant: 'test-participant', source: 'test-source', value: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = weightHandler.updateWeight({ participant: 'test-participant', source: 'test-source', value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = weightHandler.updateWeight({ participant: 'test-participant', source: 'test-source', value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('updated');
    });

    it('declares read and write sets', () => {
      const program = weightHandler.updateWeight({ participant: 'test-participant', source: 'test-source', value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = weightHandler.updateWeight({ participant: 'test-participant', source: 'test-source', value: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof weightHandler.updateWeight !== 'function') return;
      const result = await interpret(weightHandler.updateWeight({ participant: 'test-participant', source: 'test-source', value: 1 }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('snapshot', () => {
    it('builds a valid StorageProgram', () => {
      const program = weightHandler.snapshot({ time: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = weightHandler.snapshot({ time: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = weightHandler.snapshot({ time: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('snapshotted');
    });

    it('declares read and write sets', () => {
      const program = weightHandler.snapshot({ time: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = weightHandler.snapshot({ time: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof weightHandler.snapshot !== 'function') return;
      const result = await interpret(weightHandler.snapshot({ time: 'test' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('getWeight', () => {
    it('builds a valid StorageProgram', () => {
      const program = weightHandler.getWeight({ participant: 'test-participant', atTime: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = weightHandler.getWeight({ participant: 'test-participant', atTime: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = weightHandler.getWeight({ participant: 'test-participant', atTime: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('weight');
      expect(variants).toContain('not_found');
    });

    it('declares read and write sets', () => {
      const program = weightHandler.getWeight({ participant: 'test-participant', atTime: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = weightHandler.getWeight({ participant: 'test-participant', atTime: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof weightHandler.getWeight !== 'function') return;
      const result = await interpret(weightHandler.getWeight({ participant: 'test-participant', atTime: 'test' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('getWeightFromSnapshot', () => {
    it('builds a valid StorageProgram', () => {
      const program = weightHandler.getWeightFromSnapshot({ snapshotId: 'test', participant: 'test-participant' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = weightHandler.getWeightFromSnapshot({ snapshotId: 'test', participant: 'test-participant' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = weightHandler.getWeightFromSnapshot({ snapshotId: 'test', participant: 'test-participant' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('weight');
      expect(variants).toContain('snapshot_not_found');
    });

    it('declares read and write sets', () => {
      const program = weightHandler.getWeightFromSnapshot({ snapshotId: 'test', participant: 'test-participant' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = weightHandler.getWeightFromSnapshot({ snapshotId: 'test', participant: 'test-participant' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof weightHandler.getWeightFromSnapshot !== 'function') return;
      const result = await interpret(weightHandler.getWeightFromSnapshot({ snapshotId: 'test', participant: 'test-participant' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invariant examples', () => {
    it("updateWeight-then-getWeight", async () => {
      const storage = createInMemoryStorage();
      const updateWeightResult0 = await interpret(weightHandler.updateWeight({ participant: {"type":"variable","name":"p"}, source: {"type":"variable","name":"_"}, value: {"type":"variable","name":"v"} }), storage);
      expect(updateWeightResult0.variant).toBe("updated");
      const weight = updateWeightResult0.output["weight"];
      const thenResult0 = await interpret(weightHandler.getWeight({ participant: {"type":"variable","name":"p"}, atTime: {"type":"variable","name":"_"} }), storage);
      expect(thenResult0.variant).toBe("weight");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-participant', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('updateWeight'), input: fc.record({ participant: fc.string({ minLength: 1, maxLength: 20 }), source: fc.string({ minLength: 1, maxLength: 20 }), value: fc.string() }) }),
              fc.record({ action: fc.constant('snapshot'), input: fc.record({ time: fc.string() }) }),
              fc.record({ action: fc.constant('getWeight'), input: fc.record({ participant: fc.string({ minLength: 1, maxLength: 20 }), atTime: fc.string() }) }),
              fc.record({ action: fc.constant('getWeightFromSnapshot'), input: fc.record({ snapshotId: fc.string(), participant: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = weightHandler[step.action];
              if (typeof actionFn === 'function') {
                const program = actionFn.call(weightHandler, step.input as Record<string, unknown>);
                const result = await interpret(program, storage);
                expect(result.variant).toBeDefined();
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-source', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('updateWeight'), input: fc.record({ participant: fc.string({ minLength: 1, maxLength: 20 }), source: fc.string({ minLength: 1, maxLength: 20 }), value: fc.string() }) }),
              fc.record({ action: fc.constant('snapshot'), input: fc.record({ time: fc.string() }) }),
              fc.record({ action: fc.constant('getWeight'), input: fc.record({ participant: fc.string({ minLength: 1, maxLength: 20 }), atTime: fc.string() }) }),
              fc.record({ action: fc.constant('getWeightFromSnapshot'), input: fc.record({ snapshotId: fc.string(), participant: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = weightHandler[step.action];
              if (typeof actionFn === 'function') {
                const program = actionFn.call(weightHandler, step.input as Record<string, unknown>);
                const result = await interpret(program, storage);
                expect(result.variant).toBeDefined();
                // Never: orphaned-source
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('updateWeight requires: ', async () => {
      const storage = createInMemoryStorage();
      const result = await interpret(weightHandler.updateWeight({  }), storage);
      expect(['error', 'invalid', 'missing', 'notFound']).toContain(result.variant);
    });

    it('updateWeight ensures on updated: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ participant: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), value: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = weightHandler.updateWeight(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            fc.pre(result.variant === "updated");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
