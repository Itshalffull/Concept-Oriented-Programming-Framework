// Finding Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { findingHandler } from '../../handlers/ts/quality-measurement/finding.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Finding functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('report', () => {
    it('builds a valid StorageProgram', () => {
      const program = findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof findingHandler.report !== 'function') return;
      const result = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "report_new" -> ok', async () => {
      if (typeof findingHandler.report !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const _pool = Object.assign({}, (afterResult_query_all?.output ?? {}));
      const _fixtureInput = { ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(findingHandler.report({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "report_duplicate" -> existing', async () => {
      if (typeof findingHandler.report !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_minimal = await interpret(findingHandler.report({ ruleId: "no-unused-vars", target: "utils/helpers.ts", location: "42:5", message: "Variable x is unused", severity: "minor", category: "lint" }), storage);
      const _pool = Object.assign({}, (afterResult_query_all?.output ?? {}), (afterResult_report_minimal?.output ?? {}));
      const _fixtureInput = { ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(findingHandler.report({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('existing'));
    });

    it('fixture "report_minimal" -> ok', async () => {
      if (typeof findingHandler.report !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const _pool = Object.assign({}, (afterResult_query_all?.output ?? {}));
      const _fixtureInput = { ruleId: "no-unused-vars", target: "utils/helpers.ts", location: "42:5", message: "Variable x is unused", severity: "minor", category: "lint" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(findingHandler.report({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('acknowledge', () => {
    it('builds a valid StorageProgram', () => {
      const program = findingHandler.acknowledge({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = findingHandler.acknowledge({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = findingHandler.acknowledge({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = findingHandler.acknowledge({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = findingHandler.acknowledge({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof findingHandler.acknowledge !== 'function') return;
      const result = await interpret(findingHandler.acknowledge({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "ack_ok" -> ok', async () => {
      if (typeof findingHandler.acknowledge !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const result = await interpret(findingHandler.acknowledge({ finding: afterResult_report_new?.output?.["finding"], by: "alice" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "ack_not_open" -> notOpen', async () => {
      if (typeof findingHandler.acknowledge !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const result = await interpret(findingHandler.acknowledge({ finding: afterResult_report_new?.output?.["finding"], by: "bob" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notOpen'));
    });

    it('fixture "ack_missing" -> notfound', async () => {
      if (typeof findingHandler.acknowledge !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(findingHandler.acknowledge({ finding: "nonexistent", by: "alice" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('suppress', () => {
    it('builds a valid StorageProgram', () => {
      const program = findingHandler.suppress({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice", reason: "Accepted technical debt per team decision" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = findingHandler.suppress({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice", reason: "Accepted technical debt per team decision" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = findingHandler.suppress({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice", reason: "Accepted technical debt per team decision" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = findingHandler.suppress({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice", reason: "Accepted technical debt per team decision" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = findingHandler.suppress({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice", reason: "Accepted technical debt per team decision" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof findingHandler.suppress !== 'function') return;
      const result = await interpret(findingHandler.suppress({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, by: "alice", reason: "Accepted technical debt per team decision" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "suppress_ok" -> ok', async () => {
      if (typeof findingHandler.suppress !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const result = await interpret(findingHandler.suppress({ finding: afterResult_report_new?.output?.["finding"], by: "alice", reason: "Accepted technical debt per team decision" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "suppress_already" -> alreadySuppressed', async () => {
      if (typeof findingHandler.suppress !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const result = await interpret(findingHandler.suppress({ finding: afterResult_report_new?.output?.["finding"], by: "bob", reason: "duplicate" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('alreadySuppressed'));
    });

    it('fixture "suppress_missing" -> notfound', async () => {
      if (typeof findingHandler.suppress !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(findingHandler.suppress({ finding: "nonexistent", by: "alice", reason: "n/a" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = findingHandler.resolve({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, resolvedIn: "fix/auth-refactor" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = findingHandler.resolve({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, resolvedIn: "fix/auth-refactor" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = findingHandler.resolve({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, resolvedIn: "fix/auth-refactor" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = findingHandler.resolve({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, resolvedIn: "fix/auth-refactor" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = findingHandler.resolve({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, resolvedIn: "fix/auth-refactor" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof findingHandler.resolve !== 'function') return;
      const result = await interpret(findingHandler.resolve({ finding: {"type":"ref","fixture":"report_new","field":"finding"}, resolvedIn: "fix/auth-refactor" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_ok" -> ok', async () => {
      if (typeof findingHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const result = await interpret(findingHandler.resolve({ finding: afterResult_report_new?.output?.["finding"], resolvedIn: "fix/auth-refactor" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_already" -> alreadyResolved', async () => {
      if (typeof findingHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const result = await interpret(findingHandler.resolve({ finding: afterResult_report_new?.output?.["finding"] }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('alreadyResolved'));
    });

    it('fixture "resolve_missing" -> notfound', async () => {
      if (typeof findingHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(findingHandler.resolve({ finding: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('query', () => {
    it('builds a valid StorageProgram', () => {
      const program = findingHandler.query({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = findingHandler.query({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = findingHandler.query({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = findingHandler.query({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = findingHandler.query({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof findingHandler.query !== 'function') return;
      const result = await interpret(findingHandler.query({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "query_all" -> ok', async () => {
      if (typeof findingHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(findingHandler.query({  }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_by_severity" -> ok', async () => {
      if (typeof findingHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const _pool = Object.assign({}, (afterResult_query_all?.output ?? {}), (afterResult_report_new?.output ?? {}));
      const _fixtureInput = { severities: ["critical"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(findingHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_by_status" -> ok', async () => {
      if (typeof findingHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const _pool = Object.assign({}, (afterResult_query_all?.output ?? {}), (afterResult_report_new?.output ?? {}));
      const _fixtureInput = { statuses: ["open"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(findingHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_by_rule" -> ok', async () => {
      if (typeof findingHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const _pool = Object.assign({}, (afterResult_query_all?.output ?? {}), (afterResult_report_new?.output ?? {}));
      const _fixtureInput = { ruleIds: ["max-cognitive-complexity"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(findingHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_by_target" -> ok', async () => {
      if (typeof findingHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const _pool = Object.assign({}, (afterResult_query_all?.output ?? {}), (afterResult_report_new?.output ?? {}));
      const _fixtureInput = { targets: ["auth/login.ts"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(findingHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('summary', () => {
    it('builds a valid StorageProgram', () => {
      const program = findingHandler.summary({ groupBy: "severity" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = findingHandler.summary({ groupBy: "severity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = findingHandler.summary({ groupBy: "severity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = findingHandler.summary({ groupBy: "severity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = findingHandler.summary({ groupBy: "severity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof findingHandler.summary !== 'function') return;
      const result = await interpret(findingHandler.summary({ groupBy: "severity" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "summary_by_severity" -> ok', async () => {
      if (typeof findingHandler.summary !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const _pool = Object.assign({}, (afterResult_query_all?.output ?? {}), (afterResult_report_new?.output ?? {}));
      const _fixtureInput = { groupBy: "severity" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(findingHandler.summary({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "summary_by_category" -> ok', async () => {
      if (typeof findingHandler.summary !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_query_all = await interpret(findingHandler.query({  }), storage);
      const afterResult_report_new = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds threshold 15", severity: "critical", category: "complexity", effort: "medium", tags: ["maintainability"], source: "static" }), storage);
      const _pool = Object.assign({}, (afterResult_query_all?.output ?? {}), (afterResult_report_new?.output ?? {}));
      const _fixtureInput = { groupBy: "category" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(findingHandler.summary({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "summary_invalid" -> invalidGroupBy', async () => {
      if (typeof findingHandler.summary !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(findingHandler.summary({ groupBy: "nonexistent-dimension" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalidGroupBy'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof findingHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = findingHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Finding');
    });
  });

  describe('invariant examples', () => {
    it("report then acknowledge then resolve", async () => {
      const storage = createInMemoryStorage();
      const reportResult0 = await interpret(findingHandler.report({ ruleId: "max-cognitive-complexity", target: "auth/login.ts", location: "15:1", message: "Complexity 23 exceeds 15", severity: "critical", category: "complexity" }), storage);
      expect(reportResult0.variant).toBe("new");
      let finding = reportResult0.output["finding"];
      let f = finding;
      const acknowledgeResult1 = await interpret(findingHandler.acknowledge({ finding: f, by: "alice" }), storage);
      expect(acknowledgeResult1.variant).toBe("ok");
      finding = acknowledgeResult1.output["finding"];
      const resolveResult2 = await interpret(findingHandler.resolve({ finding: f }), storage);
      expect(resolveResult2.variant).toBe("ok");
      finding = resolveResult2.output["finding"];
      const thenResult0 = await interpret(findingHandler.query({ statuses: ["resolved"], ruleIds: ["max-cognitive-complexity"] }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("report duplicate is deduplicated", async () => {
      const storage = createInMemoryStorage();
      const reportResult0 = await interpret(findingHandler.report({ ruleId: "no-unused-vars", target: "utils/helpers.ts", location: "42:5", message: "Variable x is unused", severity: "minor", category: "lint" }), storage);
      expect(reportResult0.variant).toBe("new");
      let finding = reportResult0.output["finding"];
      let f = finding;
      const thenResult0 = await interpret(findingHandler.report({ ruleId: "no-unused-vars", target: "utils/helpers.ts", location: "42:5", message: "Variable x is unused", severity: "minor", category: "lint" }), storage);
      expect(thenResult0.variant).toBe("existing");
    });

    it("suppress excludes from open count", async () => {
      const storage = createInMemoryStorage();
      const reportResult0 = await interpret(findingHandler.report({ ruleId: "naming-convention", target: "legacy/old.ts", location: "1:1", message: "Non-standard name", severity: "minor", category: "style" }), storage);
      expect(reportResult0.variant).toBe("new");
      let finding = reportResult0.output["finding"];
      let f = finding;
      const suppressResult1 = await interpret(findingHandler.suppress({ finding: f, by: "alice", reason: "Legacy code, migration planned" }), storage);
      expect(suppressResult1.variant).toBe("ok");
      finding = suppressResult1.output["finding"];
      const thenResult0 = await interpret(findingHandler.query({ statuses: ["open"] }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: orphan findings', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('report'), input: fc.record({ ruleId: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), location: fc.string({ minLength: 1, maxLength: 50 }), message: fc.string({ minLength: 1, maxLength: 50 }), severity: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), effort: fc.string(), tags: fc.string(), source: fc.string() }) }),
              fc.record({ action: fc.constant('acknowledge'), input: fc.record({ finding: fc.string(), by: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('suppress'), input: fc.record({ finding: fc.string(), by: fc.string({ minLength: 1, maxLength: 50 }), reason: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ finding: fc.string(), resolvedIn: fc.string() }) }),
              fc.record({ action: fc.constant('query'), input: fc.record({ targets: fc.string(), ruleIds: fc.string(), severities: fc.string(), statuses: fc.string(), sources: fc.string() }) }),
              fc.record({ action: fc.constant('summary'), input: fc.record({ groupBy: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = findingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(findingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphan findings
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: valid status', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('report'), input: fc.record({ ruleId: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), location: fc.string({ minLength: 1, maxLength: 50 }), message: fc.string({ minLength: 1, maxLength: 50 }), severity: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), effort: fc.string(), tags: fc.string(), source: fc.string() }) }),
              fc.record({ action: fc.constant('acknowledge'), input: fc.record({ finding: fc.string(), by: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('suppress'), input: fc.record({ finding: fc.string(), by: fc.string({ minLength: 1, maxLength: 50 }), reason: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ finding: fc.string(), resolvedIn: fc.string() }) }),
              fc.record({ action: fc.constant('query'), input: fc.record({ targets: fc.string(), ruleIds: fc.string(), severities: fc.string(), statuses: fc.string(), sources: fc.string() }) }),
              fc.record({ action: fc.constant('summary'), input: fc.record({ groupBy: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = findingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(findingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
