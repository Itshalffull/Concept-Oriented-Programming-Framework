// InteractorEntity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { interactorEntityHandler } from '../../handlers/ts/score/interactor-entity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('InteractorEntity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interactorEntityHandler.register !== 'function') return;
      const result = await interpret(interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_single_choice" -> ok', async () => {
      if (typeof interactorEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_text_input" -> ok', async () => {
      if (typeof interactorEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interactorEntityHandler.register({ name: "text-input", category: "input", properties: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_name" -> error', async () => {
      if (typeof interactorEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interactorEntityHandler.register({ name: "", category: "selection", properties: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByCategory', () => {
    it('builds a valid StorageProgram', () => {
      const program = interactorEntityHandler.findByCategory({ category: "selection" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interactorEntityHandler.findByCategory({ category: "selection" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interactorEntityHandler.findByCategory({ category: "selection" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interactorEntityHandler.findByCategory({ category: "selection" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interactorEntityHandler.findByCategory({ category: "selection" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interactorEntityHandler.findByCategory !== 'function') return;
      const result = await interpret(interactorEntityHandler.findByCategory({ category: "selection" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_selection" -> ok', async () => {
      if (typeof interactorEntityHandler.findByCategory !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_single_choice = await interpret(interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_single_choice?.output ?? {}));
      const _fixtureInput = { category: "selection" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(interactorEntityHandler.findByCategory({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_empty_category" -> error', async () => {
      if (typeof interactorEntityHandler.findByCategory !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interactorEntityHandler.findByCategory({ category: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('matchingWidgets', () => {
    it('builds a valid StorageProgram', () => {
      const program = interactorEntityHandler.matchingWidgets({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"}, context: "desktop" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interactorEntityHandler.matchingWidgets({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"}, context: "desktop" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interactorEntityHandler.matchingWidgets({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"}, context: "desktop" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interactorEntityHandler.matchingWidgets({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"}, context: "desktop" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interactorEntityHandler.matchingWidgets({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"}, context: "desktop" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interactorEntityHandler.matchingWidgets !== 'function') return;
      const result = await interpret(interactorEntityHandler.matchingWidgets({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"}, context: "desktop" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "matching_desktop" -> ok', async () => {
      if (typeof interactorEntityHandler.matchingWidgets !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_single_choice = await interpret(interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" }), storage);
      const result = await interpret(interactorEntityHandler.matchingWidgets({ interactor: afterResult_register_single_choice?.output?.["entity"], context: "desktop" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "matching_missing" -> error', async () => {
      if (typeof interactorEntityHandler.matchingWidgets !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interactorEntityHandler.matchingWidgets({ interactor: "nonexistent-id", context: "mobile" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('classifiedFields', () => {
    it('builds a valid StorageProgram', () => {
      const program = interactorEntityHandler.classifiedFields({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interactorEntityHandler.classifiedFields({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interactorEntityHandler.classifiedFields({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interactorEntityHandler.classifiedFields({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interactorEntityHandler.classifiedFields({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interactorEntityHandler.classifiedFields !== 'function') return;
      const result = await interpret(interactorEntityHandler.classifiedFields({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "classified_valid" -> ok', async () => {
      if (typeof interactorEntityHandler.classifiedFields !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_single_choice = await interpret(interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" }), storage);
      const result = await interpret(interactorEntityHandler.classifiedFields({ interactor: afterResult_register_single_choice?.output?.["entity"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "classified_missing" -> error', async () => {
      if (typeof interactorEntityHandler.classifiedFields !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interactorEntityHandler.classifiedFields({ interactor: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('coverageReport', () => {
    it('builds a valid StorageProgram', () => {
      const program = interactorEntityHandler.coverageReport({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interactorEntityHandler.coverageReport({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interactorEntityHandler.coverageReport({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interactorEntityHandler.coverageReport({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interactorEntityHandler.coverageReport({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interactorEntityHandler.coverageReport !== 'function') return;
      const result = await interpret(interactorEntityHandler.coverageReport({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "coverage_report_valid" -> ok', async () => {
      if (typeof interactorEntityHandler.coverageReport !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_single_choice = await interpret(interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_single_choice?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(interactorEntityHandler.coverageReport({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = interactorEntityHandler.get({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interactorEntityHandler.get({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interactorEntityHandler.get({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interactorEntityHandler.get({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interactorEntityHandler.get({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interactorEntityHandler.get !== 'function') return;
      const result = await interpret(interactorEntityHandler.get({ interactor: {"type":"ref","fixture":"register_single_choice","field":"entity"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_interactor" -> ok', async () => {
      if (typeof interactorEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_single_choice = await interpret(interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{\"dataType\":\"enum\",\"cardinality\":\"scalar\"}" }), storage);
      const result = await interpret(interactorEntityHandler.get({ interactor: afterResult_register_single_choice?.output?.["entity"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> error', async () => {
      if (typeof interactorEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interactorEntityHandler.get({ interactor: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(interactorEntityHandler.register({ name: "single-choice", category: "selection", properties: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let entity = registerResult0.output["entity"];
      let i = entity;
      const thenResult0 = await interpret(interactorEntityHandler.get({ interactor: i }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every interactors entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByCategory'), input: fc.record({ category: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('matchingWidgets'), input: fc.record({ interactor: fc.string(), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('classifiedFields'), input: fc.record({ interactor: fc.string() }) }),
              fc.record({ action: fc.constant('coverageReport'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ interactor: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = interactorEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(interactorEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in interactors', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByCategory'), input: fc.record({ category: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('matchingWidgets'), input: fc.record({ interactor: fc.string(), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('classifiedFields'), input: fc.record({ interactor: fc.string() }) }),
              fc.record({ action: fc.constant('coverageReport'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ interactor: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = interactorEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(interactorEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty name in interactors
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof interactorEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(interactorEntityHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof interactorEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = interactorEntityHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
