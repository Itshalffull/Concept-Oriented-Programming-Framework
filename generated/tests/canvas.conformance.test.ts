// Canvas Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { canvasHandler } from '../../handlers/ts/app/canvas.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Canvas functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('addNode', () => {
    it('builds a valid StorageProgram', () => {
      const program = canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof canvasHandler.addNode !== 'function') return;
      const result = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_add" -> ok', async () => {
      if (typeof canvasHandler.addNode !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_to_new_canvas" -> ok', async () => {
      if (typeof canvasHandler.addNode !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(canvasHandler.addNode({ canvas: "board-2", node: "card-b", x: "0", y: "0" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('moveNode', () => {
    it('builds a valid StorageProgram', () => {
      const program = canvasHandler.moveNode({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", x: "300", y: "400" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = canvasHandler.moveNode({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", x: "300", y: "400" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = canvasHandler.moveNode({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", x: "300", y: "400" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = canvasHandler.moveNode({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", x: "300", y: "400" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = canvasHandler.moveNode({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", x: "300", y: "400" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof canvasHandler.moveNode !== 'function') return;
      const result = await interpret(canvasHandler.moveNode({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", x: "300", y: "400" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_move" -> ok', async () => {
      if (typeof canvasHandler.moveNode !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const result = await interpret(canvasHandler.moveNode({ canvas: afterResult_valid_add?.output?.["id"], node: "card-a", x: "300", y: "400" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "move_missing_canvas" -> notfound', async () => {
      if (typeof canvasHandler.moveNode !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(canvasHandler.moveNode({ canvas: "nonexistent", node: "card-a", x: "0", y: "0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('groupNodes', () => {
    it('builds a valid StorageProgram', () => {
      const program = canvasHandler.groupNodes({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, nodes: "[\"card-a\",\"card-b\"]", group: {"type":"ref","fixture":"valid_add","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = canvasHandler.groupNodes({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, nodes: "[\"card-a\",\"card-b\"]", group: {"type":"ref","fixture":"valid_add","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = canvasHandler.groupNodes({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, nodes: "[\"card-a\",\"card-b\"]", group: {"type":"ref","fixture":"valid_add","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = canvasHandler.groupNodes({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, nodes: "[\"card-a\",\"card-b\"]", group: {"type":"ref","fixture":"valid_add","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = canvasHandler.groupNodes({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, nodes: "[\"card-a\",\"card-b\"]", group: {"type":"ref","fixture":"valid_add","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof canvasHandler.groupNodes !== 'function') return;
      const result = await interpret(canvasHandler.groupNodes({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, nodes: "[\"card-a\",\"card-b\"]", group: {"type":"ref","fixture":"valid_add","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_group" -> ok', async () => {
      if (typeof canvasHandler.groupNodes !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const result = await interpret(canvasHandler.groupNodes({ canvas: afterResult_valid_add?.output?.["id"], nodes: "[\"card-a\",\"card-b\"]", group: afterResult_valid_add?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "group_missing_canvas" -> notfound', async () => {
      if (typeof canvasHandler.groupNodes !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(canvasHandler.groupNodes({ canvas: "nonexistent", nodes: "[\"card-a\"]", group: "g1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('removeItem', () => {
    it('builds a valid StorageProgram', () => {
      const program = canvasHandler.removeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = canvasHandler.removeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = canvasHandler.removeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = canvasHandler.removeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = canvasHandler.removeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof canvasHandler.removeItem !== 'function') return;
      const result = await interpret(canvasHandler.removeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_remove" -> ok', async () => {
      if (typeof canvasHandler.removeItem !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const result = await interpret(canvasHandler.removeItem({ canvas: afterResult_valid_add?.output?.["id"], node: "card-a" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_missing" -> notfound', async () => {
      if (typeof canvasHandler.removeItem !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(canvasHandler.removeItem({ canvas: "nonexistent", node: "card-a" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('resizeItem', () => {
    it('builds a valid StorageProgram', () => {
      const program = canvasHandler.resizeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", width: "200", height: "150" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = canvasHandler.resizeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", width: "200", height: "150" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = canvasHandler.resizeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", width: "200", height: "150" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = canvasHandler.resizeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", width: "200", height: "150" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = canvasHandler.resizeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", width: "200", height: "150" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof canvasHandler.resizeItem !== 'function') return;
      const result = await interpret(canvasHandler.resizeItem({ canvas: {"type":"ref","fixture":"valid_add","field":"id"}, node: "card-a", width: "200", height: "150" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_resize" -> ok', async () => {
      if (typeof canvasHandler.resizeItem !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const result = await interpret(canvasHandler.resizeItem({ canvas: afterResult_valid_add?.output?.["id"], node: "card-a", width: "200", height: "150" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resize_missing" -> notfound', async () => {
      if (typeof canvasHandler.resizeItem !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(canvasHandler.resizeItem({ canvas: "nonexistent", node: "card-a", width: "100", height: "100" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('createConnector', () => {
    it('builds a valid StorageProgram', () => {
      const program = canvasHandler.createConnector({ canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = canvasHandler.createConnector({ canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = canvasHandler.createConnector({ canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = canvasHandler.createConnector({ canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = canvasHandler.createConnector({ canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof canvasHandler.createConnector !== 'function') return;
      const result = await interpret(canvasHandler.createConnector({ canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_ok" -> ok', async () => {
      if (typeof canvasHandler.createConnector !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_add?.output ?? {}));
      const _fixtureInput = { canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(canvasHandler.createConnector({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_branch" -> ok', async () => {
      if (typeof canvasHandler.createConnector !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_add?.output ?? {}));
      const _fixtureInput = { canvas: "board-1", connector: "edge-2", fromNode: "card-a", toNode: "card-a", kind: "branch" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(canvasHandler.createConnector({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_duplicate" -> duplicate', async () => {
      if (typeof canvasHandler.createConnector !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const afterResult_create_ok = await interpret(canvasHandler.createConnector({ canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_add?.output ?? {}), (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(canvasHandler.createConnector({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "create_missing_node" -> missing_node', async () => {
      if (typeof canvasHandler.createConnector !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_add?.output ?? {}));
      const _fixtureInput = { canvas: "board-1", connector: "edge-3", fromNode: "card-a", toNode: "no-such-node", kind: "default" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(canvasHandler.createConnector({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('missing_node'));
    });

    it('fixture "create_invalid_kind" -> invalid_kind', async () => {
      if (typeof canvasHandler.createConnector !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_add?.output ?? {}));
      const _fixtureInput = { canvas: "board-1", connector: "edge-4", fromNode: "card-a", toNode: "card-a", kind: "teleport" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(canvasHandler.createConnector({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_kind'));
    });

    it('fixture "create_missing_canvas" -> error', async () => {
      if (typeof canvasHandler.createConnector !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(canvasHandler.createConnector({ canvas: "nonexistent", connector: "edge-5", fromNode: "card-a", toNode: "card-a", kind: "default" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('removeConnector', () => {
    it('builds a valid StorageProgram', () => {
      const program = canvasHandler.removeConnector({ canvas: "board-1", connector: "edge-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = canvasHandler.removeConnector({ canvas: "board-1", connector: "edge-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = canvasHandler.removeConnector({ canvas: "board-1", connector: "edge-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = canvasHandler.removeConnector({ canvas: "board-1", connector: "edge-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = canvasHandler.removeConnector({ canvas: "board-1", connector: "edge-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof canvasHandler.removeConnector !== 'function') return;
      const result = await interpret(canvasHandler.removeConnector({ canvas: "board-1", connector: "edge-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_ok" -> ok', async () => {
      if (typeof canvasHandler.removeConnector !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const afterResult_create_ok = await interpret(canvasHandler.createConnector({ canvas: "board-1", connector: "edge-1", fromNode: "card-a", toNode: "card-a", kind: "default" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_add?.output ?? {}), (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { canvas: "board-1", connector: "edge-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(canvasHandler.removeConnector({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_notfound" -> notfound', async () => {
      if (typeof canvasHandler.removeConnector !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_add = await interpret(canvasHandler.addNode({ canvas: "board-1", node: "card-a", x: "100", y: "200" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_add?.output ?? {}));
      const _fixtureInput = { canvas: "board-1", connector: "no-such-edge" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(canvasHandler.removeConnector({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "remove_missing_canvas" -> error', async () => {
      if (typeof canvasHandler.removeConnector !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(canvasHandler.removeConnector({ canvas: "nonexistent", connector: "edge-1" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof canvasHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = canvasHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Canvas');
    });
  });

  describe('invariant examples', () => {
    it("addNode-then-moveNode", async () => {
      const storage = createInMemoryStorage();
      const addNodeResult0 = await interpret(canvasHandler.addNode({ canvas: "test-v", node: "a", x: 0, y: 0 }), storage);
      expect(addNodeResult0.variant).toBe("ok");
      const thenResult0 = await interpret(canvasHandler.moveNode({ canvas: "test-v", node: "a", x: 100, y: 200 }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("createConnector-then-removeConnector", async () => {
      const storage = createInMemoryStorage();
    });

    it("createConnector-duplicate-rejected", async () => {
      const storage = createInMemoryStorage();
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-nodes', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('addNode'), input: fc.record({ canvas: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), x: fc.integer({ min: 1, max: 1000 }), y: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('moveNode'), input: fc.record({ canvas: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), x: fc.integer({ min: 1, max: 1000 }), y: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('groupNodes'), input: fc.record({ canvas: fc.string(), nodes: fc.string({ minLength: 1, maxLength: 50 }), group: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeItem'), input: fc.record({ canvas: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resizeItem'), input: fc.record({ canvas: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), width: fc.integer({ min: 1, max: 1000 }), height: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('createConnector'), input: fc.record({ canvas: fc.string(), connector: fc.string({ minLength: 1, maxLength: 50 }), fromNode: fc.string({ minLength: 1, maxLength: 50 }), toNode: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeConnector'), input: fc.record({ canvas: fc.string(), connector: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = canvasHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(canvasHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-positions', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('addNode'), input: fc.record({ canvas: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), x: fc.integer({ min: 1, max: 1000 }), y: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('moveNode'), input: fc.record({ canvas: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), x: fc.integer({ min: 1, max: 1000 }), y: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('groupNodes'), input: fc.record({ canvas: fc.string(), nodes: fc.string({ minLength: 1, maxLength: 50 }), group: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeItem'), input: fc.record({ canvas: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resizeItem'), input: fc.record({ canvas: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), width: fc.integer({ min: 1, max: 1000 }), height: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('createConnector'), input: fc.record({ canvas: fc.string(), connector: fc.string({ minLength: 1, maxLength: 50 }), fromNode: fc.string({ minLength: 1, maxLength: 50 }), toNode: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeConnector'), input: fc.record({ canvas: fc.string(), connector: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = canvasHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(canvasHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-positions
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('addNode handles empty input: ', async () => {
      if (typeof canvasHandler.addNode !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(canvasHandler.addNode({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('addNode ensures on ok: ', async () => {
      if (typeof canvasHandler.addNode !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ canvas: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), x: fc.integer({ min: 1, max: 1000 }), y: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = canvasHandler.addNode(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
