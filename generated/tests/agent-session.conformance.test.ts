// AgentSession Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { agentSessionHandler } from '../../handlers/ts/app/agent-session.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('AgentSession functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('spawn', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentSessionHandler.spawn !== 'function') return;
      const result = await interpret(agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "spawn_research_bot" -> ok', async () => {
      if (typeof agentSessionHandler.spawn !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "spawn_missing" -> notfound', async () => {
      if (typeof agentSessionHandler.spawn !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentSessionHandler.spawn({ personaPageId: "nonexistent", strategy: "react", tools: "[]", context: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "spawn_empty_persona" -> error', async () => {
      if (typeof agentSessionHandler.spawn !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentSessionHandler.spawn({ personaPageId: "", strategy: "react", tools: "[]", context: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('invoke', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentSessionHandler.invoke({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"}, goal: "Find recent papers on transformer architectures" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentSessionHandler.invoke({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"}, goal: "Find recent papers on transformer architectures" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentSessionHandler.invoke({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"}, goal: "Find recent papers on transformer architectures" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentSessionHandler.invoke({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"}, goal: "Find recent papers on transformer architectures" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentSessionHandler.invoke({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"}, goal: "Find recent papers on transformer architectures" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentSessionHandler.invoke !== 'function') return;
      const result = await interpret(agentSessionHandler.invoke({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"}, goal: "Find recent papers on transformer architectures" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "invoke_research" -> ok', async () => {
      if (typeof agentSessionHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_spawn_research_bot = await interpret(agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" }), storage);
      const result = await interpret(agentSessionHandler.invoke({ session: afterResult_spawn_research_bot?.output?.["session"], goal: "Find recent papers on transformer architectures" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invoke_missing" -> notfound', async () => {
      if (typeof agentSessionHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentSessionHandler.invoke({ session: "nonexistent", goal: "test" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('suspend', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentSessionHandler.suspend({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentSessionHandler.suspend({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentSessionHandler.suspend({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentSessionHandler.suspend({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentSessionHandler.suspend({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentSessionHandler.suspend !== 'function') return;
      const result = await interpret(agentSessionHandler.suspend({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "suspend_session" -> ok', async () => {
      if (typeof agentSessionHandler.suspend !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_spawn_research_bot = await interpret(agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" }), storage);
      const result = await interpret(agentSessionHandler.suspend({ session: afterResult_spawn_research_bot?.output?.["session"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "suspend_missing" -> notfound', async () => {
      if (typeof agentSessionHandler.suspend !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentSessionHandler.suspend({ session: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('resume', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentSessionHandler.resume({ session: {"type":"ref","fixture":"suspend_session","field":"session"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentSessionHandler.resume({ session: {"type":"ref","fixture":"suspend_session","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentSessionHandler.resume({ session: {"type":"ref","fixture":"suspend_session","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentSessionHandler.resume({ session: {"type":"ref","fixture":"suspend_session","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentSessionHandler.resume({ session: {"type":"ref","fixture":"suspend_session","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentSessionHandler.resume !== 'function') return;
      const result = await interpret(agentSessionHandler.resume({ session: {"type":"ref","fixture":"suspend_session","field":"session"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resume_session" -> ok', async () => {
      if (typeof agentSessionHandler.resume !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_spawn_research_bot = await interpret(agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" }), storage);
      const afterResult_suspend_session = await interpret(agentSessionHandler.suspend({ session: afterResult_spawn_research_bot?.output?.["session"] }), storage);
      const result = await interpret(agentSessionHandler.resume({ session: afterResult_suspend_session?.output?.["session"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resume_missing" -> notfound', async () => {
      if (typeof agentSessionHandler.resume !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentSessionHandler.resume({ session: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('terminate', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentSessionHandler.terminate({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentSessionHandler.terminate({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentSessionHandler.terminate({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentSessionHandler.terminate({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentSessionHandler.terminate({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentSessionHandler.terminate !== 'function') return;
      const result = await interpret(agentSessionHandler.terminate({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "terminate_session" -> ok', async () => {
      if (typeof agentSessionHandler.terminate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_spawn_research_bot = await interpret(agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" }), storage);
      const result = await interpret(agentSessionHandler.terminate({ session: afterResult_spawn_research_bot?.output?.["session"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "terminate_missing" -> notfound', async () => {
      if (typeof agentSessionHandler.terminate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentSessionHandler.terminate({ session: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('getStatus', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentSessionHandler.getStatus({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentSessionHandler.getStatus({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentSessionHandler.getStatus({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentSessionHandler.getStatus({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentSessionHandler.getStatus({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentSessionHandler.getStatus !== 'function') return;
      const result = await interpret(agentSessionHandler.getStatus({ session: {"type":"ref","fixture":"spawn_research_bot","field":"session"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_status" -> ok', async () => {
      if (typeof agentSessionHandler.getStatus !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_spawn_research_bot = await interpret(agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" }), storage);
      const result = await interpret(agentSessionHandler.getStatus({ session: afterResult_spawn_research_bot?.output?.["session"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_status_missing" -> notfound', async () => {
      if (typeof agentSessionHandler.getStatus !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentSessionHandler.getStatus({ session: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentSessionHandler.list({ personaPageId: "", status: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentSessionHandler.list({ personaPageId: "", status: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentSessionHandler.list({ personaPageId: "", status: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentSessionHandler.list({ personaPageId: "", status: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentSessionHandler.list({ personaPageId: "", status: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentSessionHandler.list !== 'function') return;
      const result = await interpret(agentSessionHandler.list({ personaPageId: "", status: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof agentSessionHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_spawn_research_bot = await interpret(agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" }), storage);
      const _pool = Object.assign({}, (afterResult_spawn_research_bot?.output ?? {}));
      const _fixtureInput = { personaPageId: "", status: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(agentSessionHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_persona" -> ok', async () => {
      if (typeof agentSessionHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_spawn_research_bot = await interpret(agentSessionHandler.spawn({ personaPageId: "persona-research-bot", strategy: "plan_and_execute", tools: "[\"web_search\",\"code_exec\"]", context: "Research ML papers" }), storage);
      const _pool = Object.assign({}, (afterResult_spawn_research_bot?.output ?? {}));
      const _fixtureInput = { personaPageId: "persona-research-bot", status: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(agentSessionHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof agentSessionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = agentSessionHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('AgentSession');
    });
  });

  describe('invariant examples', () => {
    it("spawn-then-invoke", async () => {
      const storage = createInMemoryStorage();
      const spawnResult0 = await interpret(agentSessionHandler.spawn({ personaPageId: "p1", strategy: "react", tools: "[\"search\"]", context: "" }), storage);
      expect(spawnResult0.variant).toBe("ok");
      let session = spawnResult0.output["session"];
      let s = session;
      let agentLoopId = spawnResult0.output["agentLoopId"];
      let _ = agentLoopId;
      let conversationId = spawnResult0.output["conversationId"];
      const thenResult0 = await interpret(agentSessionHandler.invoke({ session: s, goal: "Find X" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("spawn-then-suspend-then-resume", async () => {
      const storage = createInMemoryStorage();
      const spawnResult0 = await interpret(agentSessionHandler.spawn({ personaPageId: "p1", strategy: "react", tools: "[]", context: "" }), storage);
      expect(spawnResult0.variant).toBe("ok");
      let session = spawnResult0.output["session"];
      let s = session;
      let agentLoopId = spawnResult0.output["agentLoopId"];
      let _ = agentLoopId;
      let conversationId = spawnResult0.output["conversationId"];
      const suspendResult1 = await interpret(agentSessionHandler.suspend({ session: s }), storage);
      expect(suspendResult1.variant).toBe("ok");
      session = suspendResult1.output["session"];
      let snapshot = suspendResult1.output["snapshot"];
      const thenResult0 = await interpret(agentSessionHandler.resume({ session: s }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-status', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('spawn'), input: fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }), strategy: fc.string({ minLength: 1, maxLength: 50 }), tools: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ session: fc.string(), goal: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('suspend'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('resume'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('terminate'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('getStatus'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ personaPageId: fc.string(), status: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = agentSessionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(agentSessionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-personaPageId', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('spawn'), input: fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }), strategy: fc.string({ minLength: 1, maxLength: 50 }), tools: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ session: fc.string(), goal: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('suspend'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('resume'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('terminate'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('getStatus'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ personaPageId: fc.string(), status: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = agentSessionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(agentSessionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-personaPageId
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('spawn handles empty input: ', async () => {
      if (typeof agentSessionHandler.spawn !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentSessionHandler.spawn({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('spawn ensures on ok: ', async () => {
      if (typeof agentSessionHandler.spawn !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }), strategy: fc.string({ minLength: 1, maxLength: 50 }), tools: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = agentSessionHandler.spawn(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
