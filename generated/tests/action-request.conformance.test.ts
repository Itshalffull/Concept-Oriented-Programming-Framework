// ActionRequest Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { actionRequestHandler } from '../../handlers/ts/governance-identity/action-request.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ActionRequest functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('request', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionRequestHandler.request !== 'function') return;
      const result = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "request_ok" -> ok', async () => {
      if (typeof actionRequestHandler.request !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "request_empty_subject" -> invalid', async () => {
      if (typeof actionRequestHandler.request !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionRequestHandler.request({ subjectId: "", conceptName: "ContentNode", actionName: "delete", actionInput: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "request_empty_concept" -> invalid', async () => {
      if (typeof actionRequestHandler.request !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "", actionName: "delete", actionInput: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "request_bad_json" -> invalid', async () => {
      if (typeof actionRequestHandler.request !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "not-json" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('approve', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionRequestHandler.approve({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Looks good" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionRequestHandler.approve({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Looks good" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionRequestHandler.approve({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Looks good" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionRequestHandler.approve({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Looks good" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionRequestHandler.approve({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Looks good" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionRequestHandler.approve !== 'function') return;
      const result = await interpret(actionRequestHandler.approve({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Looks good" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "approve_ok" -> ok', async () => {
      if (typeof actionRequestHandler.approve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_request_ok = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" }), storage);
      const result = await interpret(actionRequestHandler.approve({ request: afterResult_request_ok?.output?.["request"], reviewerId: "subject-admin-001", reviewNote: "Looks good" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "approve_missing" -> notfound', async () => {
      if (typeof actionRequestHandler.approve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionRequestHandler.approve({ request: "nonexistent", reviewerId: "subject-admin-001", reviewNote: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('reject', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionRequestHandler.reject({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Denied — insufficient privilege" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionRequestHandler.reject({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Denied — insufficient privilege" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionRequestHandler.reject({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Denied — insufficient privilege" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionRequestHandler.reject({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Denied — insufficient privilege" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionRequestHandler.reject({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Denied — insufficient privilege" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionRequestHandler.reject !== 'function') return;
      const result = await interpret(actionRequestHandler.reject({ request: {"type":"ref","fixture":"request_ok","field":"request"}, reviewerId: "subject-admin-001", reviewNote: "Denied — insufficient privilege" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "reject_ok" -> ok', async () => {
      if (typeof actionRequestHandler.reject !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_request_ok = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" }), storage);
      const result = await interpret(actionRequestHandler.reject({ request: afterResult_request_ok?.output?.["request"], reviewerId: "subject-admin-001", reviewNote: "Denied — insufficient privilege" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reject_missing" -> notfound', async () => {
      if (typeof actionRequestHandler.reject !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionRequestHandler.reject({ request: "nonexistent", reviewerId: "subject-admin-001", reviewNote: "N/A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "reject_empty_note" -> invalid', async () => {
      if (typeof actionRequestHandler.reject !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_request_ok = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" }), storage);
      const result = await interpret(actionRequestHandler.reject({ request: afterResult_request_ok?.output?.["request"], reviewerId: "subject-admin-001", reviewNote: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('execute', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionRequestHandler.execute({ request: {"type":"ref","fixture":"approve_ok","field":"request"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionRequestHandler.execute({ request: {"type":"ref","fixture":"approve_ok","field":"request"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionRequestHandler.execute({ request: {"type":"ref","fixture":"approve_ok","field":"request"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionRequestHandler.execute({ request: {"type":"ref","fixture":"approve_ok","field":"request"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionRequestHandler.execute({ request: {"type":"ref","fixture":"approve_ok","field":"request"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionRequestHandler.execute !== 'function') return;
      const result = await interpret(actionRequestHandler.execute({ request: {"type":"ref","fixture":"approve_ok","field":"request"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "execute_ok" -> ok', async () => {
      if (typeof actionRequestHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_request_ok = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" }), storage);
      const afterResult_approve_ok = await interpret(actionRequestHandler.approve({ request: afterResult_request_ok?.output?.["request"], reviewerId: "subject-admin-001", reviewNote: "Looks good" }), storage);
      const result = await interpret(actionRequestHandler.execute({ request: afterResult_approve_ok?.output?.["request"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "execute_missing" -> notfound', async () => {
      if (typeof actionRequestHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionRequestHandler.execute({ request: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionRequestHandler.get({ request: {"type":"ref","fixture":"request_ok","field":"request"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionRequestHandler.get({ request: {"type":"ref","fixture":"request_ok","field":"request"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionRequestHandler.get({ request: {"type":"ref","fixture":"request_ok","field":"request"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionRequestHandler.get({ request: {"type":"ref","fixture":"request_ok","field":"request"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionRequestHandler.get({ request: {"type":"ref","fixture":"request_ok","field":"request"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionRequestHandler.get !== 'function') return;
      const result = await interpret(actionRequestHandler.get({ request: {"type":"ref","fixture":"request_ok","field":"request"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof actionRequestHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_request_ok = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"node-abc\"}" }), storage);
      const result = await interpret(actionRequestHandler.get({ request: afterResult_request_ok?.output?.["request"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof actionRequestHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionRequestHandler.get({ request: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof actionRequestHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = actionRequestHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ActionRequest');
    });
  });

  describe('invariant examples', () => {
    it("request-approve-execute", async () => {
      const storage = createInMemoryStorage();
      const requestResult0 = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"n1\"}" }), storage);
      expect(requestResult0.variant).toBe("ok");
      let request = requestResult0.output["request"];
      let r = request;
      const approveResult1 = await interpret(actionRequestHandler.approve({ request: r, reviewerId: "subject-admin-001", reviewNote: false }), storage);
      expect(approveResult1.variant).toBe("ok");
      request = approveResult1.output["request"];
      const thenResult0 = await interpret(actionRequestHandler.execute({ request: r }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("request-then-reject", async () => {
      const storage = createInMemoryStorage();
      const requestResult0 = await interpret(actionRequestHandler.request({ subjectId: "subject-agent-001", conceptName: "ContentNode", actionName: "delete", actionInput: "{\"node\":\"n2\"}" }), storage);
      expect(requestResult0.variant).toBe("ok");
      let request = requestResult0.output["request"];
      let r = request;
      const thenResult0 = await interpret(actionRequestHandler.reject({ request: r, reviewerId: "subject-admin-001", reviewNote: "Denied" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-status', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('request'), input: fc.record({ subjectId: fc.string({ minLength: 1, maxLength: 50 }), conceptName: fc.string({ minLength: 1, maxLength: 50 }), actionName: fc.string({ minLength: 1, maxLength: 50 }), actionInput: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('approve'), input: fc.record({ request: fc.string(), reviewerId: fc.string({ minLength: 1, maxLength: 50 }), reviewNote: fc.string() }) }),
              fc.record({ action: fc.constant('reject'), input: fc.record({ request: fc.string(), reviewerId: fc.string({ minLength: 1, maxLength: 50 }), reviewNote: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ request: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ request: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionRequestHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(actionRequestHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: executed-without-approval', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('request'), input: fc.record({ subjectId: fc.string({ minLength: 1, maxLength: 50 }), conceptName: fc.string({ minLength: 1, maxLength: 50 }), actionName: fc.string({ minLength: 1, maxLength: 50 }), actionInput: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('approve'), input: fc.record({ request: fc.string(), reviewerId: fc.string({ minLength: 1, maxLength: 50 }), reviewNote: fc.string() }) }),
              fc.record({ action: fc.constant('reject'), input: fc.record({ request: fc.string(), reviewerId: fc.string({ minLength: 1, maxLength: 50 }), reviewNote: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ request: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ request: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionRequestHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(actionRequestHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: executed-without-approval
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('request handles empty input: ', async () => {
      if (typeof actionRequestHandler.request !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionRequestHandler.request({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('request handles empty input: ', async () => {
      if (typeof actionRequestHandler.request !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionRequestHandler.request({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('request handles empty input: ', async () => {
      if (typeof actionRequestHandler.request !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionRequestHandler.request({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('request ensures on ok: ', async () => {
      if (typeof actionRequestHandler.request !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ subjectId: fc.string({ minLength: 1, maxLength: 50 }), conceptName: fc.string({ minLength: 1, maxLength: 50 }), actionName: fc.string({ minLength: 1, maxLength: 50 }), actionInput: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = actionRequestHandler.request(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('execute handles empty input: ', async () => {
      if (typeof actionRequestHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionRequestHandler.execute({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('execute ensures on ok: ', async () => {
      if (typeof actionRequestHandler.execute !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ request: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = actionRequestHandler.execute(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
