// VercelKVProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { vercelKVProviderHandler } from '../../handlers/ts/deploy/vercel-kv-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('VercelKVProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('provision', () => {
    it('builds a valid StorageProgram', () => {
      const program = vercelKVProviderHandler.provision({ storeName: "session-cache", config: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vercelKVProviderHandler.provision({ storeName: "session-cache", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vercelKVProviderHandler.provision({ storeName: "session-cache", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vercelKVProviderHandler.provision({ storeName: "session-cache", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vercelKVProviderHandler.provision({ storeName: "session-cache", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vercelKVProviderHandler.provision !== 'function') return;
      const result = await interpret(vercelKVProviderHandler.provision({ storeName: "session-cache", config: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "provision_session_cache" -> ok', async () => {
      if (typeof vercelKVProviderHandler.provision !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(vercelKVProviderHandler.provision({ storeName: "session-cache", config: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "provision_rate_limiter" -> ok', async () => {
      if (typeof vercelKVProviderHandler.provision !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(vercelKVProviderHandler.provision({ storeName: "rate-limiter", config: "{\"maxSize\": 1024}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "provision_empty_name" -> error', async () => {
      if (typeof vercelKVProviderHandler.provision !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(vercelKVProviderHandler.provision({ storeName: "", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('getCredentials', () => {
    it('builds a valid StorageProgram', () => {
      const program = vercelKVProviderHandler.getCredentials({ storeName: "session-cache" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vercelKVProviderHandler.getCredentials({ storeName: "session-cache" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vercelKVProviderHandler.getCredentials({ storeName: "session-cache" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vercelKVProviderHandler.getCredentials({ storeName: "session-cache" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vercelKVProviderHandler.getCredentials({ storeName: "session-cache" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vercelKVProviderHandler.getCredentials !== 'function') return;
      const result = await interpret(vercelKVProviderHandler.getCredentials({ storeName: "session-cache" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_session_cache" -> ok', async () => {
      if (typeof vercelKVProviderHandler.getCredentials !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(vercelKVProviderHandler.provision({ storeName: "session-cache", config: "{}" }), storage);
      const result = await interpret(vercelKVProviderHandler.getCredentials({ storeName: "session-cache" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_nonexistent" -> error', async () => {
      if (typeof vercelKVProviderHandler.getCredentials !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(vercelKVProviderHandler.getCredentials({ storeName: "no-such-store" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('destroy', () => {
    it('builds a valid StorageProgram', () => {
      const program = vercelKVProviderHandler.destroy({ storeName: "session-cache" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = vercelKVProviderHandler.destroy({ storeName: "session-cache" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = vercelKVProviderHandler.destroy({ storeName: "session-cache" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = vercelKVProviderHandler.destroy({ storeName: "session-cache" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = vercelKVProviderHandler.destroy({ storeName: "session-cache" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof vercelKVProviderHandler.destroy !== 'function') return;
      const result = await interpret(vercelKVProviderHandler.destroy({ storeName: "session-cache" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "destroy_session_cache" -> ok', async () => {
      if (typeof vercelKVProviderHandler.destroy !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(vercelKVProviderHandler.provision({ storeName: "session-cache", config: "{}" }), storage);
      const result = await interpret(vercelKVProviderHandler.destroy({ storeName: "session-cache" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "destroy_nonexistent" -> error', async () => {
      if (typeof vercelKVProviderHandler.destroy !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(vercelKVProviderHandler.destroy({ storeName: "no-such-store" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof vercelKVProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = vercelKVProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('VercelKVProvider');
    });
  });

  describe('invariant examples', () => {
    it("provision-then-getCredentials-2", async () => {
      const storage = createInMemoryStorage();
      const provisionResult0 = await interpret(vercelKVProviderHandler.provision({ storeName: {"type":"literal","value":"test-kv"}, config: {"type":"literal","value":"{}"} }), storage);
      expect(provisionResult0.variant).toBe("ok");
      let storeName = provisionResult0.output["storeName"];
      let storeId = provisionResult0.output["storeId"];
      let credentials = provisionResult0.output["credentials"];
      const thenResult0 = await interpret(vercelKVProviderHandler.getCredentials({ storeName: {"type":"literal","value":"test-kv"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("provision-then-getCredentials", async () => {
      const storage = createInMemoryStorage();
      const provisionResult0 = await interpret(vercelKVProviderHandler.provision({ storeName: {"type":"literal","value":"test-kv"}, config: {"type":"literal","value":"{}"} }), storage);
      expect(provisionResult0.variant).toBe("ok");
      let storeName = provisionResult0.output["storeName"];
      let storeId = provisionResult0.output["storeId"];
      let credentials = provisionResult0.output["credentials"];
      const thenResult0 = await interpret(vercelKVProviderHandler.destroy({ storeName: {"type":"literal","value":"test-kv"} }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(vercelKVProviderHandler.getCredentials({ storeName: {"type":"literal","value":"test-kv"} }), storage);
      expect(thenResult1.variant).toBe("notfound");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-storeName', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('provision'), input: fc.record({ storeName: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getCredentials'), input: fc.record({ storeName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('destroy'), input: fc.record({ storeName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = vercelKVProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(vercelKVProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-storeId', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('provision'), input: fc.record({ storeName: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getCredentials'), input: fc.record({ storeName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('destroy'), input: fc.record({ storeName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = vercelKVProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(vercelKVProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-storeId
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('provision handles empty input: ', async () => {
      if (typeof vercelKVProviderHandler.provision !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(vercelKVProviderHandler.provision({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('provision ensures on ok: ', async () => {
      if (typeof vercelKVProviderHandler.provision !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ storeName: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = vercelKVProviderHandler.provision(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
