// FilterSpec Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { filterSpecHandler } from '../../handlers/ts/view/filter-spec.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('FilterSpec functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterSpecHandler.create !== 'function') return;
      const result = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_toggle_filter" -> ok', async () => {
      if (typeof filterSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_identity" -> ok', async () => {
      if (typeof filterSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterSpecHandler.create({ name: "pass-all", tree: {"type":"true"}, sourceType: "system" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_duplicate" -> duplicate', async () => {
      if (typeof filterSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const _pool = Object.assign({}, (afterResult_create_toggle_filter?.output ?? {}));
      const _fixtureInput = { name: "schema-filter", tree: {"type":"true"}, sourceType: "system" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterSpecHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterSpecHandler.get({ name: {"type":"ref","fixture":"create_toggle_filter","field":"filter"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterSpecHandler.get({ name: {"type":"ref","fixture":"create_toggle_filter","field":"filter"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterSpecHandler.get({ name: {"type":"ref","fixture":"create_toggle_filter","field":"filter"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterSpecHandler.get({ name: {"type":"ref","fixture":"create_toggle_filter","field":"filter"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterSpecHandler.get({ name: {"type":"ref","fixture":"create_toggle_filter","field":"filter"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterSpecHandler.get !== 'function') return;
      const result = await interpret(filterSpecHandler.get({ name: {"type":"ref","fixture":"create_toggle_filter","field":"filter"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof filterSpecHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const result = await interpret(filterSpecHandler.get({ name: afterResult_create_toggle_filter?.output?.["filter"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof filterSpecHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterSpecHandler.get({ name: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('compose', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterSpecHandler.compose({ a: "schema-filter", b: "pass-all" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterSpecHandler.compose({ a: "schema-filter", b: "pass-all" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterSpecHandler.compose({ a: "schema-filter", b: "pass-all" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterSpecHandler.compose({ a: "schema-filter", b: "pass-all" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterSpecHandler.compose({ a: "schema-filter", b: "pass-all" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterSpecHandler.compose !== 'function') return;
      const result = await interpret(filterSpecHandler.compose({ a: "schema-filter", b: "pass-all" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compose_two" -> ok', async () => {
      if (typeof filterSpecHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const afterResult_create_identity = await interpret(filterSpecHandler.create({ name: "pass-all", tree: {"type":"true"}, sourceType: "system" }), storage);
      const _pool = Object.assign({}, (afterResult_create_toggle_filter?.output ?? {}), (afterResult_create_identity?.output ?? {}));
      const _fixtureInput = { a: "schema-filter", b: "pass-all" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterSpecHandler.compose({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compose_missing" -> notfound', async () => {
      if (typeof filterSpecHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_identity = await interpret(filterSpecHandler.create({ name: "pass-all", tree: {"type":"true"}, sourceType: "system" }), storage);
      const _pool = Object.assign({}, (afterResult_create_identity?.output ?? {}));
      const _fixtureInput = { a: "nonexistent", b: "pass-all" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterSpecHandler.compose({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('evaluate', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterSpecHandler.evaluate({ name: "schema-filter", rows: [{"schemas":["concept"]},{"schemas":["sync"]}] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterSpecHandler.evaluate({ name: "schema-filter", rows: [{"schemas":["concept"]},{"schemas":["sync"]}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterSpecHandler.evaluate({ name: "schema-filter", rows: [{"schemas":["concept"]},{"schemas":["sync"]}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterSpecHandler.evaluate({ name: "schema-filter", rows: [{"schemas":["concept"]},{"schemas":["sync"]}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterSpecHandler.evaluate({ name: "schema-filter", rows: [{"schemas":["concept"]},{"schemas":["sync"]}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterSpecHandler.evaluate !== 'function') return;
      const result = await interpret(filterSpecHandler.evaluate({ name: "schema-filter", rows: [{"schemas":["concept"]},{"schemas":["sync"]}] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "evaluate_filter" -> ok', async () => {
      if (typeof filterSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const _pool = Object.assign({}, (afterResult_create_toggle_filter?.output ?? {}));
      const _fixtureInput = { name: "schema-filter", rows: [{"schemas":["concept"]},{"schemas":["sync"]}] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterSpecHandler.evaluate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "evaluate_missing" -> notfound', async () => {
      if (typeof filterSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterSpecHandler.evaluate({ name: "nonexistent", rows: "[]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "evaluate_bad_rows" -> error', async () => {
      if (typeof filterSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const _pool = Object.assign({}, (afterResult_create_toggle_filter?.output ?? {}));
      const _fixtureInput = { name: "schema-filter", rows: "not-json" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterSpecHandler.evaluate({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('normalize', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterSpecHandler.normalize({ name: "schema-filter", form: "cnf" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterSpecHandler.normalize({ name: "schema-filter", form: "cnf" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterSpecHandler.normalize({ name: "schema-filter", form: "cnf" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterSpecHandler.normalize({ name: "schema-filter", form: "cnf" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterSpecHandler.normalize({ name: "schema-filter", form: "cnf" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterSpecHandler.normalize !== 'function') return;
      const result = await interpret(filterSpecHandler.normalize({ name: "schema-filter", form: "cnf" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "normalize_cnf" -> ok', async () => {
      if (typeof filterSpecHandler.normalize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const _pool = Object.assign({}, (afterResult_create_toggle_filter?.output ?? {}));
      const _fixtureInput = { name: "schema-filter", form: "cnf" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterSpecHandler.normalize({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "normalize_missing" -> notfound', async () => {
      if (typeof filterSpecHandler.normalize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterSpecHandler.normalize({ name: "nonexistent", form: "cnf" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('validate', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterSpecHandler.validate({ name: "schema-filter", schema: {"fields":["schemas","node"]} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterSpecHandler.validate({ name: "schema-filter", schema: {"fields":["schemas","node"]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterSpecHandler.validate({ name: "schema-filter", schema: {"fields":["schemas","node"]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterSpecHandler.validate({ name: "schema-filter", schema: {"fields":["schemas","node"]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterSpecHandler.validate({ name: "schema-filter", schema: {"fields":["schemas","node"]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterSpecHandler.validate !== 'function') return;
      const result = await interpret(filterSpecHandler.validate({ name: "schema-filter", schema: {"fields":["schemas","node"]} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "validate_valid" -> ok', async () => {
      if (typeof filterSpecHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const _pool = Object.assign({}, (afterResult_create_toggle_filter?.output ?? {}));
      const _fixtureInput = { name: "schema-filter", schema: {"fields":["schemas","node"]} } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterSpecHandler.validate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_invalid" -> invalid', async () => {
      if (typeof filterSpecHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const _pool = Object.assign({}, (afterResult_create_toggle_filter?.output ?? {}));
      const _fixtureInput = { name: "schema-filter", schema: {"fields":["node"]} } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterSpecHandler.validate({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "validate_missing" -> notfound', async () => {
      if (typeof filterSpecHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterSpecHandler.validate({ name: "nonexistent", schema: {} }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('bind', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterSpecHandler.bind({ name: "schema-filter", bindings: {"entityId":"abc123"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterSpecHandler.bind({ name: "schema-filter", bindings: {"entityId":"abc123"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterSpecHandler.bind({ name: "schema-filter", bindings: {"entityId":"abc123"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterSpecHandler.bind({ name: "schema-filter", bindings: {"entityId":"abc123"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterSpecHandler.bind({ name: "schema-filter", bindings: {"entityId":"abc123"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterSpecHandler.bind !== 'function') return;
      const result = await interpret(filterSpecHandler.bind({ name: "schema-filter", bindings: {"entityId":"abc123"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "bind_params" -> ok', async () => {
      if (typeof filterSpecHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const _pool = Object.assign({}, (afterResult_create_toggle_filter?.output ?? {}));
      const _fixtureInput = { name: "schema-filter", bindings: {"entityId":"abc123"} } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterSpecHandler.bind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "bind_missing" -> notfound', async () => {
      if (typeof filterSpecHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterSpecHandler.bind({ name: "nonexistent", bindings: {} }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterSpecHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterSpecHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterSpecHandler.list !== 'function') return;
      const result = await interpret(filterSpecHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof filterSpecHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle_filter = await interpret(filterSpecHandler.create({ name: "schema-filter", tree: {"type":"in","field":"schemas","values":["concept"]}, sourceType: "interactive" }), storage);
      const _pool = Object.assign({}, (afterResult_create_toggle_filter?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(filterSpecHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof filterSpecHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = filterSpecHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('FilterSpec');
    });
  });

  describe('invariant examples', () => {
    it("create then get returns filter", async () => {
      const storage = createInMemoryStorage();
    });

    it("compose with identity preserves filter", async () => {
      const storage = createInMemoryStorage();
    });

    it("evaluate filters rows correctly", async () => {
      const storage = createInMemoryStorage();
    });

    it("duplicate create is rejected", async () => {
      const storage = createInMemoryStorage();
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: filters have non-empty names', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), tree: fc.string({ minLength: 1, maxLength: 50 }), sourceType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ a: fc.string({ minLength: 1, maxLength: 50 }), b: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), rows: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('normalize'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), form: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('bind'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), bindings: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = filterSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(filterSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: filters have valid trees', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), tree: fc.string({ minLength: 1, maxLength: 50 }), sourceType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ a: fc.string({ minLength: 1, maxLength: 50 }), b: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), rows: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('normalize'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), form: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('bind'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), bindings: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = filterSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(filterSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: compose with identity changes tree', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), tree: fc.string({ minLength: 1, maxLength: 50 }), sourceType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ a: fc.string({ minLength: 1, maxLength: 50 }), b: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), rows: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('normalize'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), form: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('bind'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), bindings: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = filterSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(filterSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: compose with identity changes tree
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof filterSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(filterSpecHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof filterSpecHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), tree: fc.string({ minLength: 1, maxLength: 50 }), sourceType: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = filterSpecHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('evaluate handles empty input: ', async () => {
      if (typeof filterSpecHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(filterSpecHandler.evaluate({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('evaluate ensures on ok: ', async () => {
      if (typeof filterSpecHandler.evaluate !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), rows: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = filterSpecHandler.evaluate(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
