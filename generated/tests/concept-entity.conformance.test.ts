// ConceptEntity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { conceptEntityHandler } from '../../handlers/ts/score/concept-entity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ConceptEntity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = conceptEntityHandler.register({ name: "Article", source: "specs/article.concept", ast: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = conceptEntityHandler.register({ name: "Article", source: "specs/article.concept", ast: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = conceptEntityHandler.register({ name: "Article", source: "specs/article.concept", ast: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = conceptEntityHandler.register({ name: "Article", source: "specs/article.concept", ast: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = conceptEntityHandler.register({ name: "Article", source: "specs/article.concept", ast: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof conceptEntityHandler.register !== 'function') return;
      const result = await interpret(conceptEntityHandler.register({ name: "Article", source: "specs/article.concept", ast: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_article" -> ok', async () => {
      if (typeof conceptEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.register({ name: "Article", source: "specs/article.concept", ast: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_name" -> error', async () => {
      if (typeof conceptEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.register({ name: "", source: "specs/empty.concept", ast: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = conceptEntityHandler.get({ name: "Article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = conceptEntityHandler.get({ name: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = conceptEntityHandler.get({ name: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = conceptEntityHandler.get({ name: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = conceptEntityHandler.get({ name: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof conceptEntityHandler.get !== 'function') return;
      const result = await interpret(conceptEntityHandler.get({ name: "Article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_article" -> ok', async () => {
      if (typeof conceptEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.get({ name: "Article" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_nonexistent" -> error', async () => {
      if (typeof conceptEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.get({ name: "Nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByCapability', () => {
    it('builds a valid StorageProgram', () => {
      const program = conceptEntityHandler.findByCapability({ capability: "persistent-storage" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = conceptEntityHandler.findByCapability({ capability: "persistent-storage" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = conceptEntityHandler.findByCapability({ capability: "persistent-storage" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = conceptEntityHandler.findByCapability({ capability: "persistent-storage" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = conceptEntityHandler.findByCapability({ capability: "persistent-storage" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof conceptEntityHandler.findByCapability !== 'function') return;
      const result = await interpret(conceptEntityHandler.findByCapability({ capability: "persistent-storage" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_persistent_storage" -> ok', async () => {
      if (typeof conceptEntityHandler.findByCapability !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.findByCapability({ capability: "persistent-storage" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unknown_capability" -> error', async () => {
      if (typeof conceptEntityHandler.findByCapability !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.findByCapability({ capability: "quantum-compute" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findBySuite', () => {
    it('builds a valid StorageProgram', () => {
      const program = conceptEntityHandler.findBySuite({ suite: "governance" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = conceptEntityHandler.findBySuite({ suite: "governance" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = conceptEntityHandler.findBySuite({ suite: "governance" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = conceptEntityHandler.findBySuite({ suite: "governance" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = conceptEntityHandler.findBySuite({ suite: "governance" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof conceptEntityHandler.findBySuite !== 'function') return;
      const result = await interpret(conceptEntityHandler.findBySuite({ suite: "governance" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_governance_suite" -> ok', async () => {
      if (typeof conceptEntityHandler.findBySuite !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.findBySuite({ suite: "governance" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_empty_suite" -> error', async () => {
      if (typeof conceptEntityHandler.findBySuite !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.findBySuite({ suite: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('generatedArtifacts', () => {
    it('builds a valid StorageProgram', () => {
      const program = conceptEntityHandler.generatedArtifacts({ entity: "concept-entity-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = conceptEntityHandler.generatedArtifacts({ entity: "concept-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = conceptEntityHandler.generatedArtifacts({ entity: "concept-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = conceptEntityHandler.generatedArtifacts({ entity: "concept-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = conceptEntityHandler.generatedArtifacts({ entity: "concept-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof conceptEntityHandler.generatedArtifacts !== 'function') return;
      const result = await interpret(conceptEntityHandler.generatedArtifacts({ entity: "concept-entity-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "artifacts_for_article" -> ok', async () => {
      if (typeof conceptEntityHandler.generatedArtifacts !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.generatedArtifacts({ entity: "concept-entity-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "artifacts_nonexistent" -> error', async () => {
      if (typeof conceptEntityHandler.generatedArtifacts !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.generatedArtifacts({ entity: "nonexistent-entity" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('participatingSyncs', () => {
    it('builds a valid StorageProgram', () => {
      const program = conceptEntityHandler.participatingSyncs({ entity: "concept-entity-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = conceptEntityHandler.participatingSyncs({ entity: "concept-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = conceptEntityHandler.participatingSyncs({ entity: "concept-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = conceptEntityHandler.participatingSyncs({ entity: "concept-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = conceptEntityHandler.participatingSyncs({ entity: "concept-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof conceptEntityHandler.participatingSyncs !== 'function') return;
      const result = await interpret(conceptEntityHandler.participatingSyncs({ entity: "concept-entity-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "syncs_for_article" -> ok', async () => {
      if (typeof conceptEntityHandler.participatingSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.participatingSyncs({ entity: "concept-entity-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "syncs_nonexistent" -> error', async () => {
      if (typeof conceptEntityHandler.participatingSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.participatingSyncs({ entity: "nonexistent-entity" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('checkCompatibility', () => {
    it('builds a valid StorageProgram', () => {
      const program = conceptEntityHandler.checkCompatibility({ a: "concept-entity-1", b: "concept-entity-2" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = conceptEntityHandler.checkCompatibility({ a: "concept-entity-1", b: "concept-entity-2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = conceptEntityHandler.checkCompatibility({ a: "concept-entity-1", b: "concept-entity-2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = conceptEntityHandler.checkCompatibility({ a: "concept-entity-1", b: "concept-entity-2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = conceptEntityHandler.checkCompatibility({ a: "concept-entity-1", b: "concept-entity-2" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof conceptEntityHandler.checkCompatibility !== 'function') return;
      const result = await interpret(conceptEntityHandler.checkCompatibility({ a: "concept-entity-1", b: "concept-entity-2" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compatible_concepts" -> ok', async () => {
      if (typeof conceptEntityHandler.checkCompatibility !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.checkCompatibility({ a: "concept-entity-1", b: "concept-entity-2" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "incompatible_missing" -> error', async () => {
      if (typeof conceptEntityHandler.checkCompatibility !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(conceptEntityHandler.checkCompatibility({ a: "nonexistent-a", b: "nonexistent-b" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof conceptEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = conceptEntityHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('ConceptEntity');
    });
  });

  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(conceptEntityHandler.register({ name: {"type":"literal","value":"Article"}, source: {"type":"literal","value":"specs/article.concept"}, ast: {"type":"literal","value":"{}"} }), storage);
      expect(registerResult0.variant).toBe("ok");
      const entity = registerResult0.output["entity"];
      const thenResult0 = await interpret(conceptEntityHandler.get({ name: {"type":"literal","value":"Article"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate registration returns existing", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(conceptEntityHandler.register({ name: {"type":"literal","value":"Article"}, source: {"type":"literal","value":"specs/article.concept"}, ast: {"type":"literal","value":"{}"} }), storage);
      expect(registerResult0.variant).toBe("ok");
      const entity = registerResult0.output["entity"];
      const thenResult0 = await interpret(conceptEntityHandler.register({ name: {"type":"literal","value":"Article"}, source: {"type":"literal","value":"specs/article.concept"}, ast: {"type":"literal","value":"{}"} }), storage);
      expect(thenResult0.variant).toBe("alreadyRegistered");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every entities entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), ast: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByCapability'), input: fc.record({ capability: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBySuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('generatedArtifacts'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('participatingSyncs'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('checkCompatibility'), input: fc.record({ a: fc.string(), b: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = conceptEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(conceptEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in entities', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), ast: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByCapability'), input: fc.record({ capability: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBySuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('generatedArtifacts'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('participatingSyncs'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('checkCompatibility'), input: fc.record({ a: fc.string(), b: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = conceptEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(conceptEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty name in entities
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof conceptEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(conceptEntityHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof conceptEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), ast: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = conceptEntityHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
