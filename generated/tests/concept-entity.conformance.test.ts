// ConceptEntity Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { conceptEntityHandler } from '../../handlers/ts/score/concept-entity.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('ConceptEntity imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('executes without crashing', async () => {
      if (typeof conceptEntityHandler.register !== 'function') return;
      try {
        const result = await conceptEntityHandler.register({ name: 'test-name', source: 'test-source', ast: 'test-ast' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('get', () => {
    it('executes without crashing', async () => {
      if (typeof conceptEntityHandler.get !== 'function') return;
      try {
        const result = await conceptEntityHandler.get({ name: 'test-name' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('findByCapability', () => {
    it('executes without crashing', async () => {
      if (typeof conceptEntityHandler.findByCapability !== 'function') return;
      try {
        const result = await conceptEntityHandler.findByCapability({ capability: 'test-capability' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('findBySuite', () => {
    it('executes without crashing', async () => {
      if (typeof conceptEntityHandler.findBySuite !== 'function') return;
      try {
        const result = await conceptEntityHandler.findBySuite({ suite: 'test-suite' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('generatedArtifacts', () => {
    it('executes without crashing', async () => {
      if (typeof conceptEntityHandler.generatedArtifacts !== 'function') return;
      try {
        const result = await conceptEntityHandler.generatedArtifacts({ entity: 'test' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('participatingSyncs', () => {
    it('executes without crashing', async () => {
      if (typeof conceptEntityHandler.participatingSyncs !== 'function') return;
      try {
        const result = await conceptEntityHandler.participatingSyncs({ entity: 'test' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('checkCompatibility', () => {
    it('executes without crashing', async () => {
      if (typeof conceptEntityHandler.checkCompatibility !== 'function') return;
      try {
        const result = await conceptEntityHandler.checkCompatibility({ a: 'test', b: 'test' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await conceptEntityHandler.register({ name: {"type":"literal","value":"Article"}, source: {"type":"literal","value":"specs/article.concept"}, ast: {"type":"literal","value":"{}"} }, storage);
      expect(registerResult0.variant).toBe("ok");
      const entity = registerResult0.output["entity"];
      const thenResult0 = await conceptEntityHandler.get({ name: {"type":"literal","value":"Article"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate registration returns existing", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await conceptEntityHandler.register({ name: {"type":"literal","value":"Article"}, source: {"type":"literal","value":"specs/article.concept"}, ast: {"type":"literal","value":"{}"} }, storage);
      expect(registerResult0.variant).toBe("ok");
      const entity = registerResult0.output["entity"];
      const thenResult0 = await conceptEntityHandler.register({ name: {"type":"literal","value":"Article"}, source: {"type":"literal","value":"specs/article.concept"}, ast: {"type":"literal","value":"{}"} }, storage);
      expect(thenResult0.variant).toBe("alreadyRegistered");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every entities entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), source: fc.string({ minLength: 1, maxLength: 20 }), ast: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findByCapability'), input: fc.record({ capability: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findBySuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('generatedArtifacts'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('participatingSyncs'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('checkCompatibility'), input: fc.record({ a: fc.string(), b: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = conceptEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(conceptEntityHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in entities', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), source: fc.string({ minLength: 1, maxLength: 20 }), ast: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findByCapability'), input: fc.record({ capability: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('findBySuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('generatedArtifacts'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('participatingSyncs'), input: fc.record({ entity: fc.string() }) }),
              fc.record({ action: fc.constant('checkCompatibility'), input: fc.record({ a: fc.string(), b: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = conceptEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(conceptEntityHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: empty name in entities
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof conceptEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await conceptEntityHandler.register({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('register ensures on ok: ', async () => {
      if (typeof conceptEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), ast: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await conceptEntityHandler.register(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
