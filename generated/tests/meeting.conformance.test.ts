// Meeting Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { meetingHandler } from '../../handlers/ts/app/governance/meeting.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Meeting functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('schedule', () => {
    it('builds a valid StorageProgram', () => {
      const program = meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof meetingHandler.schedule !== 'function') return;
      const result = await interpret(meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "schedule_board_meeting" -> ok', async () => {
      if (typeof meetingHandler.schedule !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "schedule_empty_title" -> error', async () => {
      if (typeof meetingHandler.schedule !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(meetingHandler.schedule({ title: "", agenda: ["Item 1"] }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('callToOrder', () => {
    it('builds a valid StorageProgram', () => {
      const program = meetingHandler.callToOrder({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, chair: "alice" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = meetingHandler.callToOrder({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, chair: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = meetingHandler.callToOrder({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, chair: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = meetingHandler.callToOrder({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, chair: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = meetingHandler.callToOrder({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, chair: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof meetingHandler.callToOrder !== 'function') return;
      const result = await interpret(meetingHandler.callToOrder({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, chair: "alice" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "call_existing_meeting" -> ok', async () => {
      if (typeof meetingHandler.callToOrder !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_schedule_board_meeting = await interpret(meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] }), storage);
      const result = await interpret(meetingHandler.callToOrder({ meeting: afterResult_schedule_board_meeting?.output?.["id"], chair: "alice" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "call_missing_meeting" -> error', async () => {
      if (typeof meetingHandler.callToOrder !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(meetingHandler.callToOrder({ meeting: "meeting-nonexistent", chair: "alice" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('makeMotion', () => {
    it('builds a valid StorageProgram', () => {
      const program = meetingHandler.makeMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, mover: "bob", motionType: "main", text: "Approve the Q2 budget" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = meetingHandler.makeMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, mover: "bob", motionType: "main", text: "Approve the Q2 budget" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = meetingHandler.makeMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, mover: "bob", motionType: "main", text: "Approve the Q2 budget" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = meetingHandler.makeMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, mover: "bob", motionType: "main", text: "Approve the Q2 budget" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = meetingHandler.makeMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, mover: "bob", motionType: "main", text: "Approve the Q2 budget" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof meetingHandler.makeMotion !== 'function') return;
      const result = await interpret(meetingHandler.makeMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, mover: "bob", motionType: "main", text: "Approve the Q2 budget" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "make_main_motion" -> ok', async () => {
      if (typeof meetingHandler.makeMotion !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_schedule_board_meeting = await interpret(meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] }), storage);
      const result = await interpret(meetingHandler.makeMotion({ meeting: afterResult_schedule_board_meeting?.output?.["id"], mover: "bob", motionType: "main", text: "Approve the Q2 budget" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "make_motion_missing_meeting" -> error', async () => {
      if (typeof meetingHandler.makeMotion !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(meetingHandler.makeMotion({ meeting: "meeting-nonexistent", mover: "bob", motionType: "main", text: "Test" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('secondMotion', () => {
    it('builds a valid StorageProgram', () => {
      const program = meetingHandler.secondMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, seconder: "carol", motionIndex: "0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = meetingHandler.secondMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, seconder: "carol", motionIndex: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = meetingHandler.secondMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, seconder: "carol", motionIndex: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = meetingHandler.secondMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, seconder: "carol", motionIndex: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = meetingHandler.secondMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, seconder: "carol", motionIndex: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof meetingHandler.secondMotion !== 'function') return;
      const result = await interpret(meetingHandler.secondMotion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, seconder: "carol", motionIndex: "0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "second_existing_motion" -> ok', async () => {
      if (typeof meetingHandler.secondMotion !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_schedule_board_meeting = await interpret(meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] }), storage);
      const result = await interpret(meetingHandler.secondMotion({ meeting: afterResult_schedule_board_meeting?.output?.["id"], seconder: "carol", motionIndex: "0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "second_missing_motion" -> error', async () => {
      if (typeof meetingHandler.secondMotion !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(meetingHandler.secondMotion({ meeting: "meeting-001", seconder: "carol", motionIndex: "99" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('callQuestion', () => {
    it('builds a valid StorageProgram', () => {
      const program = meetingHandler.callQuestion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = meetingHandler.callQuestion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = meetingHandler.callQuestion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = meetingHandler.callQuestion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = meetingHandler.callQuestion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof meetingHandler.callQuestion !== 'function') return;
      const result = await interpret(meetingHandler.callQuestion({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "call_question_valid" -> ok', async () => {
      if (typeof meetingHandler.callQuestion !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_schedule_board_meeting = await interpret(meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] }), storage);
      const result = await interpret(meetingHandler.callQuestion({ meeting: afterResult_schedule_board_meeting?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "call_question_missing" -> error', async () => {
      if (typeof meetingHandler.callQuestion !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(meetingHandler.callQuestion({ meeting: "meeting-nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('recordMinute', () => {
    it('builds a valid StorageProgram', () => {
      const program = meetingHandler.recordMinute({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, record: "Motion to approve Q2 budget passed unanimously" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = meetingHandler.recordMinute({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, record: "Motion to approve Q2 budget passed unanimously" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = meetingHandler.recordMinute({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, record: "Motion to approve Q2 budget passed unanimously" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = meetingHandler.recordMinute({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, record: "Motion to approve Q2 budget passed unanimously" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = meetingHandler.recordMinute({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, record: "Motion to approve Q2 budget passed unanimously" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof meetingHandler.recordMinute !== 'function') return;
      const result = await interpret(meetingHandler.recordMinute({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"}, record: "Motion to approve Q2 budget passed unanimously" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "record_minute_entry" -> ok', async () => {
      if (typeof meetingHandler.recordMinute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_schedule_board_meeting = await interpret(meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] }), storage);
      const result = await interpret(meetingHandler.recordMinute({ meeting: afterResult_schedule_board_meeting?.output?.["id"], record: "Motion to approve Q2 budget passed unanimously" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "record_missing_meeting" -> error', async () => {
      if (typeof meetingHandler.recordMinute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(meetingHandler.recordMinute({ meeting: "meeting-nonexistent", record: "Test" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('adjourn', () => {
    it('builds a valid StorageProgram', () => {
      const program = meetingHandler.adjourn({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = meetingHandler.adjourn({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = meetingHandler.adjourn({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = meetingHandler.adjourn({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = meetingHandler.adjourn({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof meetingHandler.adjourn !== 'function') return;
      const result = await interpret(meetingHandler.adjourn({ meeting: {"type":"ref","fixture":"schedule_board_meeting","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "adjourn_valid" -> ok', async () => {
      if (typeof meetingHandler.adjourn !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_schedule_board_meeting = await interpret(meetingHandler.schedule({ title: "Q2 Board Meeting", agenda: ["Budget review","Hiring plan","Product roadmap"] }), storage);
      const result = await interpret(meetingHandler.adjourn({ meeting: afterResult_schedule_board_meeting?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "adjourn_missing" -> error', async () => {
      if (typeof meetingHandler.adjourn !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(meetingHandler.adjourn({ meeting: "meeting-nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof meetingHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = meetingHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Meeting');
    });
  });

  describe('invariant examples', () => {
    it("schedule-then-adjourn", async () => {
      const storage = createInMemoryStorage();
      const scheduleResult0 = await interpret(meetingHandler.schedule({ title: "test-_", agenda: "test-_" }), storage);
      expect(scheduleResult0.variant).toBe("ok");
      let meeting = scheduleResult0.output["meeting"];
      const thenResult0 = await interpret(meetingHandler.callToOrder({ meeting: "test-mt", chair: "test-_" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(meetingHandler.makeMotion({ meeting: "test-mt", mover: "test-_", motionType: "test-_", text: "test-_" }), storage);
      expect(thenResult1.variant).toBe("ok");
      const thenResult2 = await interpret(meetingHandler.adjourn({ meeting: "test-mt" }), storage);
      expect(thenResult2.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-title', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('schedule'), input: fc.record({ title: fc.string({ minLength: 1, maxLength: 50 }), agenda: fc.string() }) }),
              fc.record({ action: fc.constant('callToOrder'), input: fc.record({ meeting: fc.string(), chair: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('makeMotion'), input: fc.record({ meeting: fc.string(), mover: fc.string({ minLength: 1, maxLength: 50 }), motionType: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('secondMotion'), input: fc.record({ meeting: fc.string(), seconder: fc.string({ minLength: 1, maxLength: 50 }), motionIndex: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('callQuestion'), input: fc.record({ meeting: fc.string() }) }),
              fc.record({ action: fc.constant('recordMinute'), input: fc.record({ meeting: fc.string(), record: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('adjourn'), input: fc.record({ meeting: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = meetingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(meetingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-title', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('schedule'), input: fc.record({ title: fc.string({ minLength: 1, maxLength: 50 }), agenda: fc.string() }) }),
              fc.record({ action: fc.constant('callToOrder'), input: fc.record({ meeting: fc.string(), chair: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('makeMotion'), input: fc.record({ meeting: fc.string(), mover: fc.string({ minLength: 1, maxLength: 50 }), motionType: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('secondMotion'), input: fc.record({ meeting: fc.string(), seconder: fc.string({ minLength: 1, maxLength: 50 }), motionIndex: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('callQuestion'), input: fc.record({ meeting: fc.string() }) }),
              fc.record({ action: fc.constant('recordMinute'), input: fc.record({ meeting: fc.string(), record: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('adjourn'), input: fc.record({ meeting: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = meetingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(meetingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-title
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('schedule handles empty input: ', async () => {
      if (typeof meetingHandler.schedule !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(meetingHandler.schedule({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('schedule ensures on ok: ', async () => {
      if (typeof meetingHandler.schedule !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ title: fc.string({ minLength: 1, maxLength: 50 }), agenda: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = meetingHandler.schedule(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
