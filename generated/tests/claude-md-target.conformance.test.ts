// ClaudeMdTarget Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { claudeMdTargetHandler } from '../../handlers/ts/framework/providers/claude-md-target.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('ClaudeMdTarget functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('generate', () => {
    it('builds a valid StorageProgram', () => {
      const program = claudeMdTargetHandler.generate({ projection: "{\"conceptName\":\"ScoreApi\",\"conceptManifest\":\"{\\\"name\\\":\\\"ScoreApi\\\",\\\"purpose\\\":\\\"Score management\\\",\\\"actions\\\":[]}\"}", config: "{\"projectName\":\"MyProject\",\"outputPath\":\"CLAUDE.md\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = claudeMdTargetHandler.generate({ projection: "{\"conceptName\":\"ScoreApi\",\"conceptManifest\":\"{\\\"name\\\":\\\"ScoreApi\\\",\\\"purpose\\\":\\\"Score management\\\",\\\"actions\\\":[]}\"}", config: "{\"projectName\":\"MyProject\",\"outputPath\":\"CLAUDE.md\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = claudeMdTargetHandler.generate({ projection: "{\"conceptName\":\"ScoreApi\",\"conceptManifest\":\"{\\\"name\\\":\\\"ScoreApi\\\",\\\"purpose\\\":\\\"Score management\\\",\\\"actions\\\":[]}\"}", config: "{\"projectName\":\"MyProject\",\"outputPath\":\"CLAUDE.md\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = claudeMdTargetHandler.generate({ projection: "{\"conceptName\":\"ScoreApi\",\"conceptManifest\":\"{\\\"name\\\":\\\"ScoreApi\\\",\\\"purpose\\\":\\\"Score management\\\",\\\"actions\\\":[]}\"}", config: "{\"projectName\":\"MyProject\",\"outputPath\":\"CLAUDE.md\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = claudeMdTargetHandler.generate({ projection: "{\"conceptName\":\"ScoreApi\",\"conceptManifest\":\"{\\\"name\\\":\\\"ScoreApi\\\",\\\"purpose\\\":\\\"Score management\\\",\\\"actions\\\":[]}\"}", config: "{\"projectName\":\"MyProject\",\"outputPath\":\"CLAUDE.md\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof claudeMdTargetHandler.generate !== 'function') return;
      try {
        const result = await interpret(claudeMdTargetHandler.generate({ projection: "{\"conceptName\":\"ScoreApi\",\"conceptManifest\":\"{\\\"name\\\":\\\"ScoreApi\\\",\\\"purpose\\\":\\\"Score management\\\",\\\"actions\\\":[]}\"}", config: "{\"projectName\":\"MyProject\",\"outputPath\":\"CLAUDE.md\"}" }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "generate_project" -> ok', async () => {
      if (typeof claudeMdTargetHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(claudeMdTargetHandler.generate({ projection: "{\"conceptName\":\"ScoreApi\",\"conceptManifest\":\"{\\\"name\\\":\\\"ScoreApi\\\",\\\"purpose\\\":\\\"Score management\\\",\\\"actions\\\":[]}\"}", config: "{\"projectName\":\"MyProject\",\"outputPath\":\"CLAUDE.md\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "generate_with_conventions" -> ok', async () => {
      if (typeof claudeMdTargetHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(claudeMdTargetHandler.generate({ projection: "{\"conceptName\":\"Agent\",\"conceptManifest\":\"{\\\"name\\\":\\\"Agent\\\",\\\"purpose\\\":\\\"Agent management\\\",\\\"actions\\\":[]}\"}", config: "{\"projectName\":\"Test\",\"conventions\":[\"Use kebab-case\"]}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "generate_empty_projection" -> error', async () => {
      if (typeof claudeMdTargetHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(claudeMdTargetHandler.generate({ projection: "", config: "{}" }), storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('validate', () => {
    it('builds a valid StorageProgram', () => {
      const program = claudeMdTargetHandler.validate({ document: "claude-md-001" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = claudeMdTargetHandler.validate({ document: "claude-md-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = claudeMdTargetHandler.validate({ document: "claude-md-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = claudeMdTargetHandler.validate({ document: "claude-md-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = claudeMdTargetHandler.validate({ document: "claude-md-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof claudeMdTargetHandler.validate !== 'function') return;
      try {
        const result = await interpret(claudeMdTargetHandler.validate({ document: "claude-md-001" }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "validate_valid" -> ok', async () => {
      if (typeof claudeMdTargetHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(claudeMdTargetHandler.validate({ document: "claude-md-001" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_not_found" -> error', async () => {
      if (typeof claudeMdTargetHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(claudeMdTargetHandler.validate({ document: "claude-md-nonexistent" }), storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('preview', () => {
    it('builds a valid StorageProgram', () => {
      const program = claudeMdTargetHandler.preview({ config: "{\"projectName\":\"TestProject\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = claudeMdTargetHandler.preview({ config: "{\"projectName\":\"TestProject\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = claudeMdTargetHandler.preview({ config: "{\"projectName\":\"TestProject\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = claudeMdTargetHandler.preview({ config: "{\"projectName\":\"TestProject\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = claudeMdTargetHandler.preview({ config: "{\"projectName\":\"TestProject\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof claudeMdTargetHandler.preview !== 'function') return;
      try {
        const result = await interpret(claudeMdTargetHandler.preview({ config: "{\"projectName\":\"TestProject\"}" }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "preview_full" -> ok', async () => {
      if (typeof claudeMdTargetHandler.preview !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(claudeMdTargetHandler.preview({ config: "{\"projectName\":\"TestProject\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "preview_empty" -> error', async () => {
      if (typeof claudeMdTargetHandler.preview !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(claudeMdTargetHandler.preview({ config: "" }), storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof claudeMdTargetHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      let result: any;
      try {
        const r = claudeMdTargetHandler.register({}, storage);
        result = r instanceof Promise ? await r : r;
        // If StorageProgram, interpret it
        if (result?.instructions && !result.variant) {
          result = await interpret(result, storage);
        }
      } catch { return; }
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('ClaudeMdTarget');
    });
  });

  describe('invariant examples', () => {
    it("generate-then-validate", async () => {
      const storage = createInMemoryStorage();
      const generateResult0 = await interpret(claudeMdTargetHandler.generate({ projection: {"type":"literal","value":"all-projections"}, config: {"type":"literal","value":"{\"projectName\":\"Test\"}"} }), storage);
      expect(generateResult0.variant).toBe("ok");
      const document = generateResult0.output["document"];
      const files = generateResult0.output["files"];
      const thenResult0 = await interpret(claudeMdTargetHandler.validate({ document: {"type":"variable","name":"d"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-projectName', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('generate'), input: fc.record({ projection: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ document: fc.string() }) }),
              fc.record({ action: fc.constant('preview'), input: fc.record({ config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = claudeMdTargetHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(claudeMdTargetHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-outputPath', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('generate'), input: fc.record({ projection: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ document: fc.string() }) }),
              fc.record({ action: fc.constant('preview'), input: fc.record({ config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = claudeMdTargetHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(claudeMdTargetHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                  // Never: orphaned-outputPath
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('generate handles empty input: ', async () => {
      if (typeof claudeMdTargetHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(claudeMdTargetHandler.generate({  }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('generate ensures on ok: ', async () => {
      if (typeof claudeMdTargetHandler.generate !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ projection: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = claudeMdTargetHandler.generate(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
