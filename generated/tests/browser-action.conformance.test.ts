// BrowserAction Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { browserActionHandler } from '../../handlers/ts/extension/browser/browser-action.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('BrowserAction functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = browserActionHandler.configure({ extensionId: 'test-extensionId', icon: 'test-icon', popupUrl: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = browserActionHandler.configure({ extensionId: 'test-extensionId', icon: 'test-icon', popupUrl: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = browserActionHandler.configure({ extensionId: 'test-extensionId', icon: 'test-icon', popupUrl: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = browserActionHandler.configure({ extensionId: 'test-extensionId', icon: 'test-icon', popupUrl: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = browserActionHandler.configure({ extensionId: 'test-extensionId', icon: 'test-icon', popupUrl: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof browserActionHandler.configure !== 'function') return;
      const result = await interpret(browserActionHandler.configure({ extensionId: 'test-extensionId', icon: 'test-icon', popupUrl: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('setBadge', () => {
    it('builds a valid StorageProgram', () => {
      const program = browserActionHandler.setBadge({ action: 'test', tabId: 'test', text: 'test-text', color: 'test-color' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = browserActionHandler.setBadge({ action: 'test', tabId: 'test', text: 'test-text', color: 'test-color' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = browserActionHandler.setBadge({ action: 'test', tabId: 'test', text: 'test-text', color: 'test-color' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = browserActionHandler.setBadge({ action: 'test', tabId: 'test', text: 'test-text', color: 'test-color' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = browserActionHandler.setBadge({ action: 'test', tabId: 'test', text: 'test-text', color: 'test-color' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof browserActionHandler.setBadge !== 'function') return;
      const result = await interpret(browserActionHandler.setBadge({ action: 'test', tabId: 'test', text: 'test-text', color: 'test-color' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('setIcon', () => {
    it('builds a valid StorageProgram', () => {
      const program = browserActionHandler.setIcon({ action: 'test', tabId: 'test', icon: 'test-icon' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = browserActionHandler.setIcon({ action: 'test', tabId: 'test', icon: 'test-icon' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = browserActionHandler.setIcon({ action: 'test', tabId: 'test', icon: 'test-icon' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = browserActionHandler.setIcon({ action: 'test', tabId: 'test', icon: 'test-icon' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = browserActionHandler.setIcon({ action: 'test', tabId: 'test', icon: 'test-icon' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof browserActionHandler.setIcon !== 'function') return;
      const result = await interpret(browserActionHandler.setIcon({ action: 'test', tabId: 'test', icon: 'test-icon' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('setPopup', () => {
    it('builds a valid StorageProgram', () => {
      const program = browserActionHandler.setPopup({ action: 'test', popupUrl: 'test-popupUrl' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = browserActionHandler.setPopup({ action: 'test', popupUrl: 'test-popupUrl' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = browserActionHandler.setPopup({ action: 'test', popupUrl: 'test-popupUrl' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = browserActionHandler.setPopup({ action: 'test', popupUrl: 'test-popupUrl' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = browserActionHandler.setPopup({ action: 'test', popupUrl: 'test-popupUrl' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof browserActionHandler.setPopup !== 'function') return;
      const result = await interpret(browserActionHandler.setPopup({ action: 'test', popupUrl: 'test-popupUrl' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('setEnabled', () => {
    it('builds a valid StorageProgram', () => {
      const program = browserActionHandler.setEnabled({ action: 'test', tabId: 'test', enabled: true });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = browserActionHandler.setEnabled({ action: 'test', tabId: 'test', enabled: true });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = browserActionHandler.setEnabled({ action: 'test', tabId: 'test', enabled: true });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = browserActionHandler.setEnabled({ action: 'test', tabId: 'test', enabled: true });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = browserActionHandler.setEnabled({ action: 'test', tabId: 'test', enabled: true });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof browserActionHandler.setEnabled !== 'function') return;
      const result = await interpret(browserActionHandler.setEnabled({ action: 'test', tabId: 'test', enabled: true }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('addContextMenu', () => {
    it('builds a valid StorageProgram', () => {
      const program = browserActionHandler.addContextMenu({ action: 'test', id: 'test-id', title: 'test-title', contexts: 'test-contexts' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = browserActionHandler.addContextMenu({ action: 'test', id: 'test-id', title: 'test-title', contexts: 'test-contexts' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = browserActionHandler.addContextMenu({ action: 'test', id: 'test-id', title: 'test-title', contexts: 'test-contexts' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = browserActionHandler.addContextMenu({ action: 'test', id: 'test-id', title: 'test-title', contexts: 'test-contexts' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = browserActionHandler.addContextMenu({ action: 'test', id: 'test-id', title: 'test-title', contexts: 'test-contexts' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof browserActionHandler.addContextMenu !== 'function') return;
      const result = await interpret(browserActionHandler.addContextMenu({ action: 'test', id: 'test-id', title: 'test-title', contexts: 'test-contexts' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('removeContextMenu', () => {
    it('builds a valid StorageProgram', () => {
      const program = browserActionHandler.removeContextMenu({ action: 'test', id: 'test-id' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = browserActionHandler.removeContextMenu({ action: 'test', id: 'test-id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = browserActionHandler.removeContextMenu({ action: 'test', id: 'test-id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = browserActionHandler.removeContextMenu({ action: 'test', id: 'test-id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = browserActionHandler.removeContextMenu({ action: 'test', id: 'test-id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof browserActionHandler.removeContextMenu !== 'function') return;
      const result = await interpret(browserActionHandler.removeContextMenu({ action: 'test', id: 'test-id' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('onClicked', () => {
    it('builds a valid StorageProgram', () => {
      const program = browserActionHandler.onClicked({ action: 'test', tabId: 'test-tabId' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = browserActionHandler.onClicked({ action: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = browserActionHandler.onClicked({ action: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = browserActionHandler.onClicked({ action: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = browserActionHandler.onClicked({ action: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof browserActionHandler.onClicked !== 'function') return;
      const result = await interpret(browserActionHandler.onClicked({ action: 'test', tabId: 'test-tabId' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof browserActionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = browserActionHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('BrowserAction');
    });
  });

  describe('invariant examples', () => {
    it("configure-then-setEnabled", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(browserActionHandler.configure({ extensionId: "test-_", icon: "test-_", popupUrl: "test-_" }), storage);
      expect(configureResult0.variant).toBe("ok");
      let action = configureResult0.output["action"];
      let a = action;
      const thenResult0 = await interpret(browserActionHandler.setBadge({ action: a, tabId: "test-_", text: "test-_", color: "test-_" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(browserActionHandler.setEnabled({ action: a, tabId: "test-_", enabled: "test-_" }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-extensionId', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ extensionId: fc.string({ minLength: 1, maxLength: 50 }), icon: fc.string({ minLength: 1, maxLength: 50 }), popupUrl: fc.string() }) }),
              fc.record({ action: fc.constant('setBadge'), input: fc.record({ action: fc.string(), tabId: fc.string(), text: fc.string({ minLength: 1, maxLength: 50 }), color: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setIcon'), input: fc.record({ action: fc.string(), tabId: fc.string(), icon: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setPopup'), input: fc.record({ action: fc.string(), popupUrl: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setEnabled'), input: fc.record({ action: fc.string(), tabId: fc.string(), enabled: fc.boolean() }) }),
              fc.record({ action: fc.constant('addContextMenu'), input: fc.record({ action: fc.string(), id: fc.string({ minLength: 1, maxLength: 50 }), title: fc.string({ minLength: 1, maxLength: 50 }), contexts: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeContextMenu'), input: fc.record({ action: fc.string(), id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('onClicked'), input: fc.record({ action: fc.string(), tabId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = browserActionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(browserActionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-badgeText', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ extensionId: fc.string({ minLength: 1, maxLength: 50 }), icon: fc.string({ minLength: 1, maxLength: 50 }), popupUrl: fc.string() }) }),
              fc.record({ action: fc.constant('setBadge'), input: fc.record({ action: fc.string(), tabId: fc.string(), text: fc.string({ minLength: 1, maxLength: 50 }), color: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setIcon'), input: fc.record({ action: fc.string(), tabId: fc.string(), icon: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setPopup'), input: fc.record({ action: fc.string(), popupUrl: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setEnabled'), input: fc.record({ action: fc.string(), tabId: fc.string(), enabled: fc.boolean() }) }),
              fc.record({ action: fc.constant('addContextMenu'), input: fc.record({ action: fc.string(), id: fc.string({ minLength: 1, maxLength: 50 }), title: fc.string({ minLength: 1, maxLength: 50 }), contexts: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeContextMenu'), input: fc.record({ action: fc.string(), id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('onClicked'), input: fc.record({ action: fc.string(), tabId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = browserActionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(browserActionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-badgeText
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('addContextMenu handles empty input: ', async () => {
      if (typeof browserActionHandler.addContextMenu !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(browserActionHandler.addContextMenu({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('addContextMenu ensures on ok: ', async () => {
      if (typeof browserActionHandler.addContextMenu !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ action: fc.string(), id: fc.string({ minLength: 1, maxLength: 50 }), title: fc.string({ minLength: 1, maxLength: 50 }), contexts: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = browserActionHandler.addContextMenu(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
