// Property Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { propertyHandler } from '../../handlers/ts/app/property.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Property functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('set', () => {
    it('builds a valid StorageProgram', () => {
      const program = propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof propertyHandler.set !== 'function') return;
      const result = await interpret(propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_title" -> ok', async () => {
      if (typeof propertyHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "set_empty_entity" -> error', async () => {
      if (typeof propertyHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(propertyHandler.set({ entity: "", key: "title", value: "Hello" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = propertyHandler.get({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = propertyHandler.get({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = propertyHandler.get({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = propertyHandler.get({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = propertyHandler.get({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof propertyHandler.get !== 'function') return;
      const result = await interpret(propertyHandler.get({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof propertyHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_set_title = await interpret(propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" }), storage);
      const result = await interpret(propertyHandler.get({ entity: afterResult_set_title?.output?.["entity"], key: "title" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "get_missing" -> error', async () => {
      if (typeof propertyHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(propertyHandler.get({ entity: "nonexistent", key: "title" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('delete', () => {
    it('builds a valid StorageProgram', () => {
      const program = propertyHandler.delete({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = propertyHandler.delete({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = propertyHandler.delete({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = propertyHandler.delete({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = propertyHandler.delete({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof propertyHandler.delete !== 'function') return;
      const result = await interpret(propertyHandler.delete({ entity: {"type":"ref","fixture":"set_title","field":"entity"}, key: "title" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "delete_existing" -> ok', async () => {
      if (typeof propertyHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_set_title = await interpret(propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" }), storage);
      const result = await interpret(propertyHandler.delete({ entity: afterResult_set_title?.output?.["entity"], key: "title" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "delete_missing" -> error', async () => {
      if (typeof propertyHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(propertyHandler.delete({ entity: "nonexistent", key: "title" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listAll', () => {
    it('builds a valid StorageProgram', () => {
      const program = propertyHandler.listAll({ entity: {"type":"ref","fixture":"set_title","field":"entity"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = propertyHandler.listAll({ entity: {"type":"ref","fixture":"set_title","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = propertyHandler.listAll({ entity: {"type":"ref","fixture":"set_title","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = propertyHandler.listAll({ entity: {"type":"ref","fixture":"set_title","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = propertyHandler.listAll({ entity: {"type":"ref","fixture":"set_title","field":"entity"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof propertyHandler.listAll !== 'function') return;
      const result = await interpret(propertyHandler.listAll({ entity: {"type":"ref","fixture":"set_title","field":"entity"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all_props" -> ok', async () => {
      if (typeof propertyHandler.listAll !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_set_title = await interpret(propertyHandler.set({ entity: "page-1", key: "title", value: "Hello World" }), storage);
      const result = await interpret(propertyHandler.listAll({ entity: afterResult_set_title?.output?.["entity"] }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "list_empty_entity" -> error', async () => {
      if (typeof propertyHandler.listAll !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(propertyHandler.listAll({ entity: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof propertyHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = propertyHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Property');
    });
  });

  describe('invariant examples', () => {
    it("set-then-get-2", async () => {
      const storage = createInMemoryStorage();
      const setResult0 = await interpret(propertyHandler.set({ entity: {"type":"variable","name":"e"}, key: {"type":"literal","value":"title"}, value: {"type":"literal","value":"Hello World"} }), storage);
      const _isErr0 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr0(setResult0.variant), `step 0: expected success but got '${setResult0.variant}'`).toBe(false);
      let entity = setResult0.output["entity"];
      const thenResult0 = await interpret(propertyHandler.get({ entity: {"type":"variable","name":"e"}, key: {"type":"literal","value":"title"} }), storage);
      const _isErrA0 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErrA0(thenResult0.variant), `assertion 0: expected success but got '${thenResult0.variant}'`).toBe(false);
    });

    it("set-then-get", async () => {
      const storage = createInMemoryStorage();
      const setResult0 = await interpret(propertyHandler.set({ entity: {"type":"variable","name":"e"}, key: {"type":"literal","value":"title"}, value: {"type":"literal","value":"Hello"} }), storage);
      const _isErr0 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr0(setResult0.variant), `step 0: expected success but got '${setResult0.variant}'`).toBe(false);
      let entity = setResult0.output["entity"];
      const deleteResult1 = await interpret(propertyHandler.delete({ entity: {"type":"variable","name":"e"}, key: {"type":"literal","value":"title"} }), storage);
      const _isErr1 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr1(deleteResult1.variant), `step 1: expected success but got '${deleteResult1.variant}'`).toBe(false);
      entity = deleteResult1.output["entity"];
      const thenResult0 = await interpret(propertyHandler.get({ entity: {"type":"variable","name":"e"}, key: {"type":"literal","value":"title"} }), storage);
      expect(thenResult0.variant).toBe("notfound");
    });

  });

  describe('action contracts (PBT)', () => {
    it('set handles empty input: ', async () => {
      if (typeof propertyHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(propertyHandler.set({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('set ensures on ok: ', async () => {
      if (typeof propertyHandler.set !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ entity: fc.string(), key: fc.string({ minLength: 1, maxLength: 50 }), value: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = propertyHandler.set(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
