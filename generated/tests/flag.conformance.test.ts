// Flag Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { flagHandler } from '../../handlers/ts/app/flag.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Flag functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('flag', () => {
    it('builds a valid StorageProgram', () => {
      const program = flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof flagHandler.flag !== 'function') return;
      const result = await interpret(flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "flag_bookmark" -> ok', async () => {
      if (typeof flagHandler.flag !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "flag_empty_flagging" -> ok', async () => {
      if (typeof flagHandler.flag !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(flagHandler.flag({ flagging: "", flagType: "bookmark", entity: "article-42", user: "user-7" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('unflag', () => {
    it('builds a valid StorageProgram', () => {
      const program = flagHandler.unflag({ flagging: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = flagHandler.unflag({ flagging: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = flagHandler.unflag({ flagging: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = flagHandler.unflag({ flagging: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = flagHandler.unflag({ flagging: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof flagHandler.unflag !== 'function') return;
      const result = await interpret(flagHandler.unflag({ flagging: {"type":"ref","fixture":"flag_bookmark","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "unflag_existing" -> ok', async () => {
      if (typeof flagHandler.unflag !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_flag_bookmark = await interpret(flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" }), storage);
      const result = await interpret(flagHandler.unflag({ flagging: afterResult_flag_bookmark?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unflag_missing" -> error', async () => {
      if (typeof flagHandler.unflag !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(flagHandler.unflag({ flagging: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('isFlagged', () => {
    it('builds a valid StorageProgram', () => {
      const program = flagHandler.isFlagged({ flagType: "bookmark", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"}, user: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = flagHandler.isFlagged({ flagType: "bookmark", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"}, user: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = flagHandler.isFlagged({ flagType: "bookmark", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"}, user: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = flagHandler.isFlagged({ flagType: "bookmark", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"}, user: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = flagHandler.isFlagged({ flagType: "bookmark", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"}, user: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof flagHandler.isFlagged !== 'function') return;
      const result = await interpret(flagHandler.isFlagged({ flagType: "bookmark", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"}, user: {"type":"ref","fixture":"flag_bookmark","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "is_flagged_check" -> ok', async () => {
      if (typeof flagHandler.isFlagged !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_flag_bookmark = await interpret(flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" }), storage);
      const result = await interpret(flagHandler.isFlagged({ flagType: "bookmark", entity: afterResult_flag_bookmark?.output?.["id"], user: afterResult_flag_bookmark?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "is_flagged_empty_type" -> ok', async () => {
      if (typeof flagHandler.isFlagged !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_flag_bookmark = await interpret(flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" }), storage);
      const result = await interpret(flagHandler.isFlagged({ flagType: "", entity: afterResult_flag_bookmark?.output?.["id"], user: afterResult_flag_bookmark?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('getCount', () => {
    it('builds a valid StorageProgram', () => {
      const program = flagHandler.getCount({ flagType: "like", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = flagHandler.getCount({ flagType: "like", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = flagHandler.getCount({ flagType: "like", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = flagHandler.getCount({ flagType: "like", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = flagHandler.getCount({ flagType: "like", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof flagHandler.getCount !== 'function') return;
      const result = await interpret(flagHandler.getCount({ flagType: "like", entity: {"type":"ref","fixture":"flag_bookmark","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_count_likes" -> ok', async () => {
      if (typeof flagHandler.getCount !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_flag_bookmark = await interpret(flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" }), storage);
      const result = await interpret(flagHandler.getCount({ flagType: "like", entity: afterResult_flag_bookmark?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_count_empty" -> ok', async () => {
      if (typeof flagHandler.getCount !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_flag_bookmark = await interpret(flagHandler.flag({ flagging: "flag-1", flagType: "bookmark", entity: "article-42", user: "user-7" }), storage);
      const _pool = Object.assign({}, (afterResult_flag_bookmark?.output ?? {}));
      const _fixtureInput = { flagType: "", entity: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(flagHandler.getCount({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof flagHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = flagHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Flag');
    });
  });

  describe('invariant examples', () => {
    it("flag-then-isFlagged-2", async () => {
      const storage = createInMemoryStorage();
      const flagResult0 = await interpret(flagHandler.flag({ flagging: {"type":"variable","name":"f"}, flagType: {"type":"variable","name":"t"}, entity: {"type":"variable","name":"e"}, user: {"type":"variable","name":"u"} }), storage);
      expect(flagResult0.variant).toBe("ok");
      const thenResult0 = await interpret(flagHandler.isFlagged({ flagType: {"type":"variable","name":"t"}, entity: {"type":"variable","name":"e"}, user: {"type":"variable","name":"u"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("flag-then-isFlagged", async () => {
      const storage = createInMemoryStorage();
      const flagResult0 = await interpret(flagHandler.flag({ flagging: {"type":"variable","name":"f"}, flagType: {"type":"variable","name":"t"}, entity: {"type":"variable","name":"e"}, user: {"type":"variable","name":"u"} }), storage);
      expect(flagResult0.variant).toBe("ok");
      const thenResult0 = await interpret(flagHandler.unflag({ flagging: {"type":"variable","name":"f"} }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(flagHandler.isFlagged({ flagType: {"type":"variable","name":"t"}, entity: {"type":"variable","name":"e"}, user: {"type":"variable","name":"u"} }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-flaggings', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('flag'), input: fc.record({ flagging: fc.string(), flagType: fc.string({ minLength: 1, maxLength: 50 }), entity: fc.string({ minLength: 1, maxLength: 50 }), user: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unflag'), input: fc.record({ flagging: fc.string() }) }),
              fc.record({ action: fc.constant('isFlagged'), input: fc.record({ flagType: fc.string({ minLength: 1, maxLength: 50 }), entity: fc.string({ minLength: 1, maxLength: 50 }), user: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getCount'), input: fc.record({ flagType: fc.string({ minLength: 1, maxLength: 50 }), entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = flagHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(flagHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-counts', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('flag'), input: fc.record({ flagging: fc.string(), flagType: fc.string({ minLength: 1, maxLength: 50 }), entity: fc.string({ minLength: 1, maxLength: 50 }), user: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unflag'), input: fc.record({ flagging: fc.string() }) }),
              fc.record({ action: fc.constant('isFlagged'), input: fc.record({ flagType: fc.string({ minLength: 1, maxLength: 50 }), entity: fc.string({ minLength: 1, maxLength: 50 }), user: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getCount'), input: fc.record({ flagType: fc.string({ minLength: 1, maxLength: 50 }), entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = flagHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(flagHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-counts
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('flag handles empty input: ', async () => {
      if (typeof flagHandler.flag !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(flagHandler.flag({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('flag ensures on ok: ', async () => {
      if (typeof flagHandler.flag !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ flagging: fc.string(), flagType: fc.string({ minLength: 1, maxLength: 50 }), entity: fc.string({ minLength: 1, maxLength: 50 }), user: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = flagHandler.flag(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
