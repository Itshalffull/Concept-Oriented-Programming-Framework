// Patch Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { patchHandler } from '../../handlers/ts/patch.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Patch functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof patchHandler.create !== 'function') return;
      const result = await interpret(patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_patch" -> ok', async () => {
      if (typeof patchHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invalid_effect" -> error', async () => {
      if (typeof patchHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "not-json" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('apply', () => {
    it('builds a valid StorageProgram', () => {
      const program = patchHandler.apply({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"}, content: "hello\nworld" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = patchHandler.apply({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"}, content: "hello\nworld" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = patchHandler.apply({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"}, content: "hello\nworld" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = patchHandler.apply({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"}, content: "hello\nworld" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = patchHandler.apply({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"}, content: "hello\nworld" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof patchHandler.apply !== 'function') return;
      const result = await interpret(patchHandler.apply({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"}, content: "hello\nworld" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "apply_existing" -> ok', async () => {
      if (typeof patchHandler.apply !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_patch = await interpret(patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" }), storage);
      const result = await interpret(patchHandler.apply({ patchId: afterResult_create_patch?.output?.["patchId"], content: "hello\nworld" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "apply_missing" -> error', async () => {
      if (typeof patchHandler.apply !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(patchHandler.apply({ patchId: "patch-nonexistent", content: "hello" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('invert', () => {
    it('builds a valid StorageProgram', () => {
      const program = patchHandler.invert({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = patchHandler.invert({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = patchHandler.invert({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = patchHandler.invert({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = patchHandler.invert({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof patchHandler.invert !== 'function') return;
      const result = await interpret(patchHandler.invert({ patchId: {"type":"ref","fixture":"create_patch","field":"patchId"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "invert_existing" -> ok', async () => {
      if (typeof patchHandler.invert !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_patch = await interpret(patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" }), storage);
      const result = await interpret(patchHandler.invert({ patchId: afterResult_create_patch?.output?.["patchId"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invert_missing" -> error', async () => {
      if (typeof patchHandler.invert !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(patchHandler.invert({ patchId: "patch-nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('compose', () => {
    it('builds a valid StorageProgram', () => {
      const program = patchHandler.compose({ first: {"type":"ref","fixture":"create_patch","field":"patchId"}, second: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = patchHandler.compose({ first: {"type":"ref","fixture":"create_patch","field":"patchId"}, second: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = patchHandler.compose({ first: {"type":"ref","fixture":"create_patch","field":"patchId"}, second: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = patchHandler.compose({ first: {"type":"ref","fixture":"create_patch","field":"patchId"}, second: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = patchHandler.compose({ first: {"type":"ref","fixture":"create_patch","field":"patchId"}, second: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof patchHandler.compose !== 'function') return;
      const result = await interpret(patchHandler.compose({ first: {"type":"ref","fixture":"create_patch","field":"patchId"}, second: {"type":"ref","fixture":"create_patch","field":"patchId"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compose_sequential" -> ok', async () => {
      if (typeof patchHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_patch = await interpret(patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" }), storage);
      const result = await interpret(patchHandler.compose({ first: afterResult_create_patch?.output?.["patchId"], second: afterResult_create_patch?.output?.["patchId"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compose_missing" -> error', async () => {
      if (typeof patchHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(patchHandler.compose({ first: "patch-nonexistent", second: "patch-2" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('commute', () => {
    it('builds a valid StorageProgram', () => {
      const program = patchHandler.commute({ p1: {"type":"ref","fixture":"create_patch","field":"patchId"}, p2: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = patchHandler.commute({ p1: {"type":"ref","fixture":"create_patch","field":"patchId"}, p2: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = patchHandler.commute({ p1: {"type":"ref","fixture":"create_patch","field":"patchId"}, p2: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = patchHandler.commute({ p1: {"type":"ref","fixture":"create_patch","field":"patchId"}, p2: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = patchHandler.commute({ p1: {"type":"ref","fixture":"create_patch","field":"patchId"}, p2: {"type":"ref","fixture":"create_patch","field":"patchId"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof patchHandler.commute !== 'function') return;
      const result = await interpret(patchHandler.commute({ p1: {"type":"ref","fixture":"create_patch","field":"patchId"}, p2: {"type":"ref","fixture":"create_patch","field":"patchId"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "commute_independent" -> ok', async () => {
      if (typeof patchHandler.commute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_patch = await interpret(patchHandler.create({ base: "sha256:aaa111", target: "sha256:bbb222", effect: "[{\"type\":\"equal\",\"line\":0,\"content\":\"hello\"}]" }), storage);
      const result = await interpret(patchHandler.commute({ p1: afterResult_create_patch?.output?.["patchId"], p2: afterResult_create_patch?.output?.["patchId"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "commute_missing" -> error', async () => {
      if (typeof patchHandler.commute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(patchHandler.commute({ p1: "patch-nonexistent", p2: "patch-2" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof patchHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = patchHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Patch');
    });
  });

  describe('invariant examples', () => {
    it("create then apply", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(patchHandler.create({ base: {"type":"variable","name":"b"}, target: {"type":"variable","name":"t"}, effect: {"type":"variable","name":"e"} }), storage);
      expect(createResult0.variant).toBe("ok");
      let patchId = createResult0.output["patchId"];
      const thenResult0 = await interpret(patchHandler.apply({ patchId: {"type":"variable","name":"p"}, content: {"type":"variable","name":"b"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("invert then apply", async () => {
      const storage = createInMemoryStorage();
      const invertResult0 = await interpret(patchHandler.invert({ patchId: {"type":"variable","name":"p"} }), storage);
      expect(invertResult0.variant).toBe("ok");
      let inversePatchId = invertResult0.output["inversePatchId"];
      const thenResult0 = await interpret(patchHandler.apply({ patchId: {"type":"variable","name":"p"}, content: {"type":"variable","name":"b"} }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(patchHandler.apply({ patchId: {"type":"variable","name":"inv"}, content: {"type":"variable","name":"t"} }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: patches have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ base: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), effect: fc.string() }) }),
              fc.record({ action: fc.constant('apply'), input: fc.record({ patchId: fc.string(), content: fc.string() }) }),
              fc.record({ action: fc.constant('invert'), input: fc.record({ patchId: fc.string() }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string() }) }),
              fc.record({ action: fc.constant('commute'), input: fc.record({ p1: fc.string(), p2: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = patchHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(patchHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in patches', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ base: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), effect: fc.string() }) }),
              fc.record({ action: fc.constant('apply'), input: fc.record({ patchId: fc.string(), content: fc.string() }) }),
              fc.record({ action: fc.constant('invert'), input: fc.record({ patchId: fc.string() }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string() }) }),
              fc.record({ action: fc.constant('commute'), input: fc.record({ p1: fc.string(), p2: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = patchHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(patchHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in patches
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof patchHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(patchHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof patchHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ base: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), effect: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = patchHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('apply ensures on ok: ', async () => {
      if (typeof patchHandler.apply !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ patchId: fc.string(), content: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = patchHandler.apply(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('invert ensures on ok: ', async () => {
      if (typeof patchHandler.invert !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ patchId: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = patchHandler.invert(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
