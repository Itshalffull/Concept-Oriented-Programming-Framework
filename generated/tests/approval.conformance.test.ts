// Approval Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { approvalHandler } from '../../handlers/ts/process-human/approval.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Approval functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('request', () => {
    it('builds a valid StorageProgram', () => {
      const program = approvalHandler.request({ step_ref: 'test-step_ref', policy_kind: 'test-policy_kind', required_count: 1, roles: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = approvalHandler.request({ step_ref: 'test-step_ref', policy_kind: 'test-policy_kind', required_count: 1, roles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = approvalHandler.request({ step_ref: 'test-step_ref', policy_kind: 'test-policy_kind', required_count: 1, roles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = approvalHandler.request({ step_ref: 'test-step_ref', policy_kind: 'test-policy_kind', required_count: 1, roles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = approvalHandler.request({ step_ref: 'test-step_ref', policy_kind: 'test-policy_kind', required_count: 1, roles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof approvalHandler.request !== 'function') return;
      const result = await interpret(approvalHandler.request({ step_ref: 'test-step_ref', policy_kind: 'test-policy_kind', required_count: 1, roles: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('approve', () => {
    it('builds a valid StorageProgram', () => {
      const program = approvalHandler.approve({ approval: 'test', actor: 'test-actor', comment: 'test-comment' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = approvalHandler.approve({ approval: 'test', actor: 'test-actor', comment: 'test-comment' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = approvalHandler.approve({ approval: 'test', actor: 'test-actor', comment: 'test-comment' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = approvalHandler.approve({ approval: 'test', actor: 'test-actor', comment: 'test-comment' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = approvalHandler.approve({ approval: 'test', actor: 'test-actor', comment: 'test-comment' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof approvalHandler.approve !== 'function') return;
      const result = await interpret(approvalHandler.approve({ approval: 'test', actor: 'test-actor', comment: 'test-comment' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('deny', () => {
    it('builds a valid StorageProgram', () => {
      const program = approvalHandler.deny({ approval: 'test', actor: 'test-actor', reason: 'test-reason' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = approvalHandler.deny({ approval: 'test', actor: 'test-actor', reason: 'test-reason' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = approvalHandler.deny({ approval: 'test', actor: 'test-actor', reason: 'test-reason' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = approvalHandler.deny({ approval: 'test', actor: 'test-actor', reason: 'test-reason' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = approvalHandler.deny({ approval: 'test', actor: 'test-actor', reason: 'test-reason' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof approvalHandler.deny !== 'function') return;
      const result = await interpret(approvalHandler.deny({ approval: 'test', actor: 'test-actor', reason: 'test-reason' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('request_changes', () => {
    it('builds a valid StorageProgram', () => {
      const program = approvalHandler.request_changes({ approval: 'test', actor: 'test-actor', feedback: 'test-feedback' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = approvalHandler.request_changes({ approval: 'test', actor: 'test-actor', feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = approvalHandler.request_changes({ approval: 'test', actor: 'test-actor', feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = approvalHandler.request_changes({ approval: 'test', actor: 'test-actor', feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = approvalHandler.request_changes({ approval: 'test', actor: 'test-actor', feedback: 'test-feedback' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof approvalHandler.request_changes !== 'function') return;
      const result = await interpret(approvalHandler.request_changes({ approval: 'test', actor: 'test-actor', feedback: 'test-feedback' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('timeout', () => {
    it('builds a valid StorageProgram', () => {
      const program = approvalHandler.timeout({ approval: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = approvalHandler.timeout({ approval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = approvalHandler.timeout({ approval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = approvalHandler.timeout({ approval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = approvalHandler.timeout({ approval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof approvalHandler.timeout !== 'function') return;
      const result = await interpret(approvalHandler.timeout({ approval: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('get_status', () => {
    it('builds a valid StorageProgram', () => {
      const program = approvalHandler.get_status({ approval: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = approvalHandler.get_status({ approval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = approvalHandler.get_status({ approval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = approvalHandler.get_status({ approval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = approvalHandler.get_status({ approval: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof approvalHandler.get_status !== 'function') return;
      const result = await interpret(approvalHandler.get_status({ approval: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof approvalHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = approvalHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Approval');
    });
  });

  describe('invariant examples', () => {
    it("request then approve", async () => {
      const storage = createInMemoryStorage();
      const requestResult0 = await interpret(approvalHandler.request({ step_ref: "mgr_approve", policy_kind: "one_of", required_count: 1, roles: "test-r" }), storage);
      expect(requestResult0.variant).toBe("ok");
      let approval = requestResult0.output["approval"];
      let a = approval;
      let step_ref = requestResult0.output["step_ref"];
      const thenResult0 = await interpret(approvalHandler.approve({ approval: a, actor: "boss", comment: "looks good" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: approvals have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('request'), input: fc.record({ step_ref: fc.string({ minLength: 1, maxLength: 50 }), policy_kind: fc.string({ minLength: 1, maxLength: 50 }), required_count: fc.integer({ min: 1, max: 1000 }), roles: fc.string() }) }),
              fc.record({ action: fc.constant('approve'), input: fc.record({ approval: fc.string(), actor: fc.string({ minLength: 1, maxLength: 50 }), comment: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deny'), input: fc.record({ approval: fc.string(), actor: fc.string({ minLength: 1, maxLength: 50 }), reason: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('request_changes'), input: fc.record({ approval: fc.string(), actor: fc.string({ minLength: 1, maxLength: 50 }), feedback: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('timeout'), input: fc.record({ approval: fc.string() }) }),
              fc.record({ action: fc.constant('get_status'), input: fc.record({ approval: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = approvalHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(approvalHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in approvals', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('request'), input: fc.record({ step_ref: fc.string({ minLength: 1, maxLength: 50 }), policy_kind: fc.string({ minLength: 1, maxLength: 50 }), required_count: fc.integer({ min: 1, max: 1000 }), roles: fc.string() }) }),
              fc.record({ action: fc.constant('approve'), input: fc.record({ approval: fc.string(), actor: fc.string({ minLength: 1, maxLength: 50 }), comment: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deny'), input: fc.record({ approval: fc.string(), actor: fc.string({ minLength: 1, maxLength: 50 }), reason: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('request_changes'), input: fc.record({ approval: fc.string(), actor: fc.string({ minLength: 1, maxLength: 50 }), feedback: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('timeout'), input: fc.record({ approval: fc.string() }) }),
              fc.record({ action: fc.constant('get_status'), input: fc.record({ approval: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = approvalHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(approvalHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in approvals
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('request handles empty input: ', async () => {
      if (typeof approvalHandler.request !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(approvalHandler.request({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('request ensures on ok: ', async () => {
      if (typeof approvalHandler.request !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ step_ref: fc.string({ minLength: 1, maxLength: 50 }), policy_kind: fc.string({ minLength: 1, maxLength: 50 }), required_count: fc.integer({ min: 1, max: 1000 }), roles: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = approvalHandler.request(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('approve handles empty input: ', async () => {
      if (typeof approvalHandler.approve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(approvalHandler.approve({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('approve ensures on ok: ', async () => {
      if (typeof approvalHandler.approve !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ approval: fc.string(), actor: fc.string({ minLength: 1, maxLength: 50 }), comment: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = approvalHandler.approve(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('deny handles empty input: ', async () => {
      if (typeof approvalHandler.deny !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(approvalHandler.deny({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('deny ensures on ok: ', async () => {
      if (typeof approvalHandler.deny !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ approval: fc.string(), actor: fc.string({ minLength: 1, maxLength: 50 }), reason: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = approvalHandler.deny(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
