// OptimisticOracleFinality Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { optimisticOracleFinalityHandler } from '../../handlers/ts/app/governance/optimistic-oracle-finality.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('OptimisticOracleFinality functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('assertFinality', () => {
    it('builds a valid StorageProgram', () => {
      const program = optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof optimisticOracleFinalityHandler.assertFinality !== 'function') return;
      const result = await interpret(optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "assert_standard" -> ok', async () => {
      if (typeof optimisticOracleFinalityHandler.assertFinality !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "assert_short_window" -> ok', async () => {
      if (typeof optimisticOracleFinalityHandler.assertFinality !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-002", asserter: "validator-bob", bond: "50.0", challengeWindowHours: "2.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "assert_large_bond" -> ok', async () => {
      if (typeof optimisticOracleFinalityHandler.assertFinality !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-003", asserter: "validator-carol", bond: "10000.0", challengeWindowHours: "24.0" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('challenge', () => {
    it('builds a valid StorageProgram', () => {
      const program = optimisticOracleFinalityHandler.challenge({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, challenger: "watchdog-carol", bond: "200.0", evidence: "Invalid state transition detected" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = optimisticOracleFinalityHandler.challenge({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, challenger: "watchdog-carol", bond: "200.0", evidence: "Invalid state transition detected" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = optimisticOracleFinalityHandler.challenge({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, challenger: "watchdog-carol", bond: "200.0", evidence: "Invalid state transition detected" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = optimisticOracleFinalityHandler.challenge({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, challenger: "watchdog-carol", bond: "200.0", evidence: "Invalid state transition detected" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = optimisticOracleFinalityHandler.challenge({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, challenger: "watchdog-carol", bond: "200.0", evidence: "Invalid state transition detected" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof optimisticOracleFinalityHandler.challenge !== 'function') return;
      const result = await interpret(optimisticOracleFinalityHandler.challenge({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, challenger: "watchdog-carol", bond: "200.0", evidence: "Invalid state transition detected" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "challenge_pending" -> ok', async () => {
      if (typeof optimisticOracleFinalityHandler.challenge !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assert_standard = await interpret(optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" }), storage);
      const result = await interpret(optimisticOracleFinalityHandler.challenge({ assertion: afterResult_assert_standard?.output?.["id"], challenger: "watchdog-carol", bond: "200.0", evidence: "Invalid state transition detected" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "challenge_already_resolved" -> expired', async () => {
      if (typeof optimisticOracleFinalityHandler.challenge !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(optimisticOracleFinalityHandler.challenge({ assertion: "oo-finalized", challenger: "watchdog-carol", bond: "200.0", evidence: "N/A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('expired'));
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = optimisticOracleFinalityHandler.resolve({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, validAssertion: "true" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = optimisticOracleFinalityHandler.resolve({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, validAssertion: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = optimisticOracleFinalityHandler.resolve({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, validAssertion: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = optimisticOracleFinalityHandler.resolve({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, validAssertion: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = optimisticOracleFinalityHandler.resolve({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, validAssertion: "true" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof optimisticOracleFinalityHandler.resolve !== 'function') return;
      const result = await interpret(optimisticOracleFinalityHandler.resolve({ assertion: {"type":"ref","fixture":"assert_standard","field":"id"}, validAssertion: "true" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_valid" -> ok', async () => {
      if (typeof optimisticOracleFinalityHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assert_standard = await interpret(optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" }), storage);
      const result = await interpret(optimisticOracleFinalityHandler.resolve({ assertion: afterResult_assert_standard?.output?.["id"], validAssertion: "true" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_invalid" -> finalized', async () => {
      if (typeof optimisticOracleFinalityHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assert_standard = await interpret(optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" }), storage);
      const result = await interpret(optimisticOracleFinalityHandler.resolve({ assertion: afterResult_assert_standard?.output?.["id"], validAssertion: "false" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('finalized'));
    });

    it('fixture "resolve_rejected" -> rejected', async () => {
      if (typeof optimisticOracleFinalityHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(optimisticOracleFinalityHandler.resolve({ assertion: "oo-002", validAssertion: "false" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('rejected'));
    });

  });

  describe('checkExpiry', () => {
    it('builds a valid StorageProgram', () => {
      const program = optimisticOracleFinalityHandler.checkExpiry({ assertion: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = optimisticOracleFinalityHandler.checkExpiry({ assertion: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = optimisticOracleFinalityHandler.checkExpiry({ assertion: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = optimisticOracleFinalityHandler.checkExpiry({ assertion: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = optimisticOracleFinalityHandler.checkExpiry({ assertion: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof optimisticOracleFinalityHandler.checkExpiry !== 'function') return;
      const result = await interpret(optimisticOracleFinalityHandler.checkExpiry({ assertion: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "check_expired" -> finalized', async () => {
      if (typeof optimisticOracleFinalityHandler.checkExpiry !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assert_standard = await interpret(optimisticOracleFinalityHandler.assertFinality({ operationRef: "proposal-001", asserter: "validator-alice", bond: "100.0", challengeWindowHours: "48.0" }), storage);
      const result = await interpret(optimisticOracleFinalityHandler.checkExpiry({ assertion: afterResult_assert_standard?.output?.["id"] }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('finalized'));
    });

    it('fixture "check_still_pending" -> still_pending', async () => {
      if (typeof optimisticOracleFinalityHandler.checkExpiry !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(optimisticOracleFinalityHandler.checkExpiry({ assertion: "oo-recent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('still_pending'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof optimisticOracleFinalityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = optimisticOracleFinalityHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('OptimisticOracleFinality');
    });
  });

  describe('invariant examples', () => {
    it("assertFinality-then-checkExpiry", async () => {
      const storage = createInMemoryStorage();
      const assertFinalityResult0 = await interpret(optimisticOracleFinalityHandler.assertFinality({ operationRef: "test-_", asserter: "test-_", bond: "test-_", challengeWindowHours: 48 }), storage);
      expect(assertFinalityResult0.variant).toBe("ok");
      let assertion = assertFinalityResult0.output["assertion"];
      let oo = assertion;
      const thenResult0 = await interpret(optimisticOracleFinalityHandler.checkExpiry({ assertion: oo }), storage);
      expect(thenResult0.variant).toBe("finalized");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-operationRef', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('assertFinality'), input: fc.record({ operationRef: fc.string({ minLength: 1, maxLength: 50 }), asserter: fc.string({ minLength: 1, maxLength: 50 }), bond: fc.string(), challengeWindowHours: fc.string() }) }),
              fc.record({ action: fc.constant('challenge'), input: fc.record({ assertion: fc.string(), challenger: fc.string({ minLength: 1, maxLength: 50 }), bond: fc.string(), evidence: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ assertion: fc.string(), validAssertion: fc.boolean() }) }),
              fc.record({ action: fc.constant('checkExpiry'), input: fc.record({ assertion: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = optimisticOracleFinalityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(optimisticOracleFinalityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-asserter', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('assertFinality'), input: fc.record({ operationRef: fc.string({ minLength: 1, maxLength: 50 }), asserter: fc.string({ minLength: 1, maxLength: 50 }), bond: fc.string(), challengeWindowHours: fc.string() }) }),
              fc.record({ action: fc.constant('challenge'), input: fc.record({ assertion: fc.string(), challenger: fc.string({ minLength: 1, maxLength: 50 }), bond: fc.string(), evidence: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ assertion: fc.string(), validAssertion: fc.boolean() }) }),
              fc.record({ action: fc.constant('checkExpiry'), input: fc.record({ assertion: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = optimisticOracleFinalityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(optimisticOracleFinalityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-asserter
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('assertFinality handles empty input: ', async () => {
      if (typeof optimisticOracleFinalityHandler.assertFinality !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(optimisticOracleFinalityHandler.assertFinality({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('assertFinality ensures on ok: ', async () => {
      if (typeof optimisticOracleFinalityHandler.assertFinality !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ operationRef: fc.string({ minLength: 1, maxLength: 50 }), asserter: fc.string({ minLength: 1, maxLength: 50 }), bond: fc.string(), challengeWindowHours: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = optimisticOracleFinalityHandler.assertFinality(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
