// SupplyChainQuality Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { supplyChainQualityHandler } from '../../handlers/ts/quality-analysis/supply-chain-quality.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SupplyChainQuality functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('scan', () => {
    it('builds a valid StorageProgram', () => {
      const program = supplyChainQualityHandler.scan({ ecosystem: "npm", targets: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = supplyChainQualityHandler.scan({ ecosystem: "npm", targets: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = supplyChainQualityHandler.scan({ ecosystem: "npm", targets: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = supplyChainQualityHandler.scan({ ecosystem: "npm", targets: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = supplyChainQualityHandler.scan({ ecosystem: "npm", targets: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof supplyChainQualityHandler.scan !== 'function') return;
      const result = await interpret(supplyChainQualityHandler.scan({ ecosystem: "npm", targets: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "scan_npm" -> ok', async () => {
      if (typeof supplyChainQualityHandler.scan !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_lodash = await interpret(supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_lodash?.output ?? {}));
      const _fixtureInput = { ecosystem: "npm", targets: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(supplyChainQualityHandler.scan({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "scan_specific" -> ok', async () => {
      if (typeof supplyChainQualityHandler.scan !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_lodash = await interpret(supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_lodash?.output ?? {}));
      const _fixtureInput = { ecosystem: "npm", targets: ["lodash","express"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(supplyChainQualityHandler.scan({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "scan_bad_ecosystem" -> error', async () => {
      if (typeof supplyChainQualityHandler.scan !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(supplyChainQualityHandler.scan({ ecosystem: "", targets: null }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('assess', () => {
    it('builds a valid StorageProgram', () => {
      const program = supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof supplyChainQualityHandler.assess !== 'function') return;
      const result = await interpret(supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "assess_lodash" -> ok', async () => {
      if (typeof supplyChainQualityHandler.assess !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "assess_not_found" -> notFound', async () => {
      if (typeof supplyChainQualityHandler.assess !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(supplyChainQualityHandler.assess({ packageName: "nonexistent-package-xyz", packageVersion: "0.0.0", ecosystem: "npm" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('risks', () => {
    it('builds a valid StorageProgram', () => {
      const program = supplyChainQualityHandler.risks({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = supplyChainQualityHandler.risks({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = supplyChainQualityHandler.risks({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = supplyChainQualityHandler.risks({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = supplyChainQualityHandler.risks({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof supplyChainQualityHandler.risks !== 'function') return;
      const result = await interpret(supplyChainQualityHandler.risks({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "risks_default" -> ok', async () => {
      if (typeof supplyChainQualityHandler.risks !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_lodash = await interpret(supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" }), storage);
      const afterResult_scan_npm = await interpret(supplyChainQualityHandler.scan({ ecosystem: "npm", targets: null }), storage);
      const _pool = Object.assign({}, (afterResult_assess_lodash?.output ?? {}), (afterResult_scan_npm?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(supplyChainQualityHandler.risks({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "risks_high" -> ok', async () => {
      if (typeof supplyChainQualityHandler.risks !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_lodash = await interpret(supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" }), storage);
      const afterResult_scan_npm = await interpret(supplyChainQualityHandler.scan({ ecosystem: "npm", targets: null }), storage);
      const _pool = Object.assign({}, (afterResult_assess_lodash?.output ?? {}), (afterResult_scan_npm?.output ?? {}));
      const _fixtureInput = { minRiskLevel: "high" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(supplyChainQualityHandler.risks({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('summary', () => {
    it('builds a valid StorageProgram', () => {
      const program = supplyChainQualityHandler.summary({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = supplyChainQualityHandler.summary({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = supplyChainQualityHandler.summary({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = supplyChainQualityHandler.summary({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = supplyChainQualityHandler.summary({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof supplyChainQualityHandler.summary !== 'function') return;
      const result = await interpret(supplyChainQualityHandler.summary({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "summary_ok" -> ok', async () => {
      if (typeof supplyChainQualityHandler.summary !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_lodash = await interpret(supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" }), storage);
      const afterResult_scan_npm = await interpret(supplyChainQualityHandler.scan({ ecosystem: "npm", targets: null }), storage);
      const _pool = Object.assign({}, (afterResult_assess_lodash?.output ?? {}), (afterResult_scan_npm?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(supplyChainQualityHandler.summary({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('verify', () => {
    it('builds a valid StorageProgram', () => {
      const program = supplyChainQualityHandler.verify({ packageName: "lodash", packageVersion: "4.17.21" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = supplyChainQualityHandler.verify({ packageName: "lodash", packageVersion: "4.17.21" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = supplyChainQualityHandler.verify({ packageName: "lodash", packageVersion: "4.17.21" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = supplyChainQualityHandler.verify({ packageName: "lodash", packageVersion: "4.17.21" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = supplyChainQualityHandler.verify({ packageName: "lodash", packageVersion: "4.17.21" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof supplyChainQualityHandler.verify !== 'function') return;
      const result = await interpret(supplyChainQualityHandler.verify({ packageName: "lodash", packageVersion: "4.17.21" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "verify_known" -> ok', async () => {
      if (typeof supplyChainQualityHandler.verify !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_lodash = await interpret(supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_lodash?.output ?? {}));
      const _fixtureInput = { packageName: "lodash", packageVersion: "4.17.21" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(supplyChainQualityHandler.verify({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "verify_unknown" -> unknown', async () => {
      if (typeof supplyChainQualityHandler.verify !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(supplyChainQualityHandler.verify({ packageName: "never-assessed-pkg", packageVersion: "1.0.0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unknown'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof supplyChainQualityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = supplyChainQualityHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('SupplyChainQuality');
    });
  });

  describe('invariant examples', () => {
    it("scan then query risks", async () => {
      const storage = createInMemoryStorage();
      const scanResult0 = await interpret(supplyChainQualityHandler.scan({ ecosystem: "npm" }), storage);
      expect(scanResult0.variant).toBe("ok");
      let assessments = scanResult0.output["assessments"];
      let as = assessments;
      const thenResult0 = await interpret(supplyChainQualityHandler.risks({ minRiskLevel: "high" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("scan then summary", async () => {
      const storage = createInMemoryStorage();
      const scanResult0 = await interpret(supplyChainQualityHandler.scan({ ecosystem: "npm" }), storage);
      expect(scanResult0.variant).toBe("ok");
      let assessments = scanResult0.output["assessments"];
      let as = assessments;
      const thenResult0 = await interpret(supplyChainQualityHandler.summary({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("assess then verify", async () => {
      const storage = createInMemoryStorage();
      const assessResult0 = await interpret(supplyChainQualityHandler.assess({ packageName: "lodash", packageVersion: "4.17.21", ecosystem: "npm" }), storage);
      expect(assessResult0.variant).toBe("ok");
      let assessment = assessResult0.output["assessment"];
      let s = assessment;
      const thenResult0 = await interpret(supplyChainQualityHandler.verify({ packageName: "lodash", packageVersion: "4.17.21" }), storage);
      expect(thenResult0.variant).toBe("trusted");
    });

  });

  describe('forall properties (PBT)', () => {
    it('forall: unknown packages cannot be verified', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.constantFrom("ghost-pkg", "phantom-lib"),
          async (pkg) => {
            const storage = createInMemoryStorage();
            const result = await interpret(supplyChainQualityHandler.verify({ packageName: pkg, packageVersion: "1.0.0" }), storage);
            expect(result.variant).toBeDefined();
            expect(result.variant).toBe("unknown");
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
