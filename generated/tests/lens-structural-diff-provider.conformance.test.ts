// LensStructuralDiffProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { lensStructuralDiffProviderHandler } from '../../handlers/ts/monadic/providers/lens-structural-diff-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('LensStructuralDiffProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('analyze', () => {
    it('builds a valid StorageProgram', () => {
      const program = lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"email\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"emailAddress\",\"type\":\"String\"}]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"email\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"emailAddress\",\"type\":\"String\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"email\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"emailAddress\",\"type\":\"String\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"email\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"emailAddress\",\"type\":\"String\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"email\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"emailAddress\",\"type\":\"String\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof lensStructuralDiffProviderHandler.analyze !== 'function') return;
      const result = await interpret(lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"email\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"emailAddress\",\"type\":\"String\"}]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "renamed_field" -> ok', async () => {
      if (typeof lensStructuralDiffProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"email\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"emailAddress\",\"type\":\"String\"}]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "identical_schemas" -> ok', async () => {
      if (typeof lensStructuralDiffProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"email\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"email\",\"type\":\"String\"}]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "added_field" -> ok', async () => {
      if (typeof lensStructuralDiffProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"name\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"name\",\"type\":\"String\"},{\"name\":\"age\",\"type\":\"Int\"}]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invalid_schema" -> error', async () => {
      if (typeof lensStructuralDiffProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensStructuralDiffProviderHandler.analyze({ oldSchema: "not json", newSchema: "[{\"name\":\"email\",\"type\":\"String\"}]" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof lensStructuralDiffProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = lensStructuralDiffProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('LensStructuralDiffProvider');
    });
  });

  describe('invariant examples', () => {
    it("renamed field produces diff operations", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(lensStructuralDiffProviderHandler.analyze({ oldSchema: "[{\"name\":\"email\",\"type\":\"String\"}]", newSchema: "[{\"name\":\"emailAddress\",\"type\":\"String\"}]" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let result = analyzeResult0.output["result"];
      let r = result;
      let operations = analyzeResult0.output["operations"];
      let ops = operations;
      let editScript = analyzeResult0.output["editScript"];
      let es = editScript;
    });

    it("identical schemas return identical", async () => {
      const storage = createInMemoryStorage();
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: diff results contain edit script', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('analyze'), input: fc.record({ oldSchema: fc.string({ minLength: 1, maxLength: 50 }), newSchema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = lensStructuralDiffProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(lensStructuralDiffProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('analyze handles empty input: ', async () => {
      if (typeof lensStructuralDiffProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(lensStructuralDiffProviderHandler.analyze({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('analyze ensures on ok: ', async () => {
      if (typeof lensStructuralDiffProviderHandler.analyze !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ oldSchema: fc.string({ minLength: 1, maxLength: 50 }), newSchema: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = lensStructuralDiffProviderHandler.analyze(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
