// Password Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { passwordHandler } from '../../handlers/ts/app/password.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Password functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('set', () => {
    it('builds a valid StorageProgram', () => {
      const program = passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof passwordHandler.set !== 'function') return;
      const result = await interpret(passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_password" -> ok', async () => {
      if (typeof passwordHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "short_password" -> invalid', async () => {
      if (typeof passwordHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(passwordHandler.set({ user: "bob", password: "abc" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('check', () => {
    it('builds a valid StorageProgram', () => {
      const program = passwordHandler.check({ user: "alice", password: "Str0ngP@ssw0rd!" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = passwordHandler.check({ user: "alice", password: "Str0ngP@ssw0rd!" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = passwordHandler.check({ user: "alice", password: "Str0ngP@ssw0rd!" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = passwordHandler.check({ user: "alice", password: "Str0ngP@ssw0rd!" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = passwordHandler.check({ user: "alice", password: "Str0ngP@ssw0rd!" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof passwordHandler.check !== 'function') return;
      const result = await interpret(passwordHandler.check({ user: "alice", password: "Str0ngP@ssw0rd!" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "existing_user" -> ok', async () => {
      if (typeof passwordHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_password = await interpret(passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_password?.output ?? {}));
      const _fixtureInput = { user: "alice", password: "Str0ngP@ssw0rd!" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(passwordHandler.check({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unknown_user" -> notfound', async () => {
      if (typeof passwordHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(passwordHandler.check({ user: "nonexistent", password: "anypassword" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('validate', () => {
    it('builds a valid StorageProgram', () => {
      const program = passwordHandler.validate({ password: "MyS3cure!Pass" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = passwordHandler.validate({ password: "MyS3cure!Pass" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = passwordHandler.validate({ password: "MyS3cure!Pass" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = passwordHandler.validate({ password: "MyS3cure!Pass" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = passwordHandler.validate({ password: "MyS3cure!Pass" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof passwordHandler.validate !== 'function') return;
      const result = await interpret(passwordHandler.validate({ password: "MyS3cure!Pass" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "strong_password" -> ok', async () => {
      if (typeof passwordHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_password = await interpret(passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_password?.output ?? {}));
      const _fixtureInput = { password: "MyS3cure!Pass" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(passwordHandler.validate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "weak_password" -> ok', async () => {
      if (typeof passwordHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_password = await interpret(passwordHandler.set({ user: "alice", password: "Str0ngP@ssw0rd!" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_password?.output ?? {}));
      const _fixtureInput = { password: "short" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(passwordHandler.validate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof passwordHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = passwordHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Password');
    });
  });

  describe('invariant examples', () => {
    it("set then check correct password", async () => {
      const storage = createInMemoryStorage();
      const setResult0 = await interpret(passwordHandler.set({ user: "test-x", password: "secret123" }), storage);
      expect(setResult0.variant).toBe("ok");
      let user = setResult0.output["user"];
      let x = user;
      const thenResult0 = await interpret(passwordHandler.check({ user: x, password: "secret123" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(passwordHandler.check({ user: x, password: "wrongpass" }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

    it("check nonexistent user returns notfound", async () => {
      const storage = createInMemoryStorage();
      const checkResult0 = await interpret(passwordHandler.check({ user: "test-z", password: "any" }), storage);
      expect(checkResult0.variant).toBe("notfound");
      let message = checkResult0.output["message"];
    });

    it("weak password rejected", async () => {
      const storage = createInMemoryStorage();
      const setResult0 = await interpret(passwordHandler.set({ user: "test-x", password: "" }), storage);
      expect(setResult0.variant).toBe("invalid");
      let message = setResult0.output["message"];
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: stored credentials have hash and salt', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('set'), input: fc.record({ user: fc.string(), password: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ user: fc.string(), password: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ password: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = passwordHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(passwordHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('set handles empty input: ', async () => {
      if (typeof passwordHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(passwordHandler.set({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('set ensures on ok: ', async () => {
      if (typeof passwordHandler.set !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ user: fc.string(), password: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = passwordHandler.set(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('set ensures on invalid: ', async () => {
      if (typeof passwordHandler.set !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ user: fc.string(), password: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = passwordHandler.set(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "invalid") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('check handles empty input: ', async () => {
      if (typeof passwordHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(passwordHandler.check({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('check ensures on ok: ', async () => {
      if (typeof passwordHandler.check !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ user: fc.string(), password: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = passwordHandler.check(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('check ensures on notfound: ', async () => {
      if (typeof passwordHandler.check !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ user: fc.string(), password: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = passwordHandler.check(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "notfound") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
