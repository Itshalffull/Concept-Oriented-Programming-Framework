// GlickoRating Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { glickoRatingHandler } from '../../handlers/ts/app/governance/glicko-rating.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('GlickoRating functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof glickoRatingHandler.configure !== 'function') return;
      const result = await interpret(glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "configure_standard" -> ok', async () => {
      if (typeof glickoRatingHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "configure_zero_deviation" -> error', async () => {
      if (typeof glickoRatingHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "0.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('recordOutcome', () => {
    it('builds a valid StorageProgram', () => {
      const program = glickoRatingHandler.recordOutcome({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", opponent: "bob", outcome: "1.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = glickoRatingHandler.recordOutcome({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", opponent: "bob", outcome: "1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = glickoRatingHandler.recordOutcome({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", opponent: "bob", outcome: "1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = glickoRatingHandler.recordOutcome({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", opponent: "bob", outcome: "1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = glickoRatingHandler.recordOutcome({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", opponent: "bob", outcome: "1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof glickoRatingHandler.recordOutcome !== 'function') return;
      const result = await interpret(glickoRatingHandler.recordOutcome({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", opponent: "bob", outcome: "1.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "outcome_win" -> ok', async () => {
      if (typeof glickoRatingHandler.recordOutcome !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_standard = await interpret(glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" }), storage);
      const result = await interpret(glickoRatingHandler.recordOutcome({ config: afterResult_configure_standard?.output?.["id"], participant: "alice", opponent: "bob", outcome: "1.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "outcome_invalid" -> error', async () => {
      if (typeof glickoRatingHandler.recordOutcome !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(glickoRatingHandler.recordOutcome({ config: "glicko-001", participant: "", opponent: "", outcome: "-1.0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('applyInactivityDecay', () => {
    it('builds a valid StorageProgram', () => {
      const program = glickoRatingHandler.applyInactivityDecay({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", daysSinceActive: "30.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = glickoRatingHandler.applyInactivityDecay({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", daysSinceActive: "30.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = glickoRatingHandler.applyInactivityDecay({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", daysSinceActive: "30.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = glickoRatingHandler.applyInactivityDecay({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", daysSinceActive: "30.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = glickoRatingHandler.applyInactivityDecay({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", daysSinceActive: "30.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof glickoRatingHandler.applyInactivityDecay !== 'function') return;
      const result = await interpret(glickoRatingHandler.applyInactivityDecay({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice", daysSinceActive: "30.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "decay_30_days" -> ok', async () => {
      if (typeof glickoRatingHandler.applyInactivityDecay !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_standard = await interpret(glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" }), storage);
      const result = await interpret(glickoRatingHandler.applyInactivityDecay({ config: afterResult_configure_standard?.output?.["id"], participant: "alice", daysSinceActive: "30.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "decay_nonexistent" -> error', async () => {
      if (typeof glickoRatingHandler.applyInactivityDecay !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(glickoRatingHandler.applyInactivityDecay({ config: "glicko-001", participant: "unknown-player", daysSinceActive: "90.0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('getReliableWeight', () => {
    it('builds a valid StorageProgram', () => {
      const program = glickoRatingHandler.getReliableWeight({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = glickoRatingHandler.getReliableWeight({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = glickoRatingHandler.getReliableWeight({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = glickoRatingHandler.getReliableWeight({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = glickoRatingHandler.getReliableWeight({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof glickoRatingHandler.getReliableWeight !== 'function') return;
      const result = await interpret(glickoRatingHandler.getReliableWeight({ config: {"type":"ref","fixture":"configure_standard","field":"id"}, participant: "alice" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "weight_alice" -> ok', async () => {
      if (typeof glickoRatingHandler.getReliableWeight !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_standard = await interpret(glickoRatingHandler.configure({ initialRating: "1500.0", initialDeviation: "350.0", initialVolatility: "0.06", inactivityGrowthRate: "30.0" }), storage);
      const result = await interpret(glickoRatingHandler.getReliableWeight({ config: afterResult_configure_standard?.output?.["id"], participant: "alice" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "weight_unknown" -> error', async () => {
      if (typeof glickoRatingHandler.getReliableWeight !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(glickoRatingHandler.getReliableWeight({ config: "glicko-001", participant: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof glickoRatingHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = glickoRatingHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('GlickoRating');
    });
  });

  describe('invariant examples', () => {
    it("configure-then-recordOutcome", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(glickoRatingHandler.configure({ initialRating: {"type":"literal","value":1500}, initialDeviation: {"type":"literal","value":350}, initialVolatility: {"type":"literal","value":0.06}, inactivityGrowthRate: {"type":"variable","name":"_"} }), storage);
      expect(configureResult0.variant).toBe("ok");
      let config = configureResult0.output["config"];
      const thenResult0 = await interpret(glickoRatingHandler.recordOutcome({ config: {"type":"variable","name":"gl"}, participant: {"type":"variable","name":"p"}, opponent: {"type":"variable","name":"_"}, outcome: {"type":"variable","name":"_"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-participant', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ initialRating: fc.string(), initialDeviation: fc.string(), initialVolatility: fc.string(), inactivityGrowthRate: fc.string() }) }),
              fc.record({ action: fc.constant('recordOutcome'), input: fc.record({ config: fc.string(), participant: fc.string({ minLength: 1, maxLength: 50 }), opponent: fc.string({ minLength: 1, maxLength: 50 }), outcome: fc.string() }) }),
              fc.record({ action: fc.constant('applyInactivityDecay'), input: fc.record({ config: fc.string(), participant: fc.string({ minLength: 1, maxLength: 50 }), daysSinceActive: fc.string() }) }),
              fc.record({ action: fc.constant('getReliableWeight'), input: fc.record({ config: fc.string(), participant: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = glickoRatingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(glickoRatingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-rating', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ initialRating: fc.string(), initialDeviation: fc.string(), initialVolatility: fc.string(), inactivityGrowthRate: fc.string() }) }),
              fc.record({ action: fc.constant('recordOutcome'), input: fc.record({ config: fc.string(), participant: fc.string({ minLength: 1, maxLength: 50 }), opponent: fc.string({ minLength: 1, maxLength: 50 }), outcome: fc.string() }) }),
              fc.record({ action: fc.constant('applyInactivityDecay'), input: fc.record({ config: fc.string(), participant: fc.string({ minLength: 1, maxLength: 50 }), daysSinceActive: fc.string() }) }),
              fc.record({ action: fc.constant('getReliableWeight'), input: fc.record({ config: fc.string(), participant: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = glickoRatingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(glickoRatingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-rating
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof glickoRatingHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(glickoRatingHandler.configure({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('configure ensures on ok: ', async () => {
      if (typeof glickoRatingHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ initialRating: fc.string(), initialDeviation: fc.string(), initialVolatility: fc.string(), inactivityGrowthRate: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = glickoRatingHandler.configure(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
