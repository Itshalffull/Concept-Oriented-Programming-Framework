// PeerAllocation Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { peerAllocationHandler } from '../../handlers/ts/app/governance/peer-allocation.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('PeerAllocation functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('openRound', () => {
    it('builds a valid StorageProgram', () => {
      const program = peerAllocationHandler.openRound({ budget: "100", deadlineDays: "7.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = peerAllocationHandler.openRound({ budget: "100", deadlineDays: "7.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = peerAllocationHandler.openRound({ budget: "100", deadlineDays: "7.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = peerAllocationHandler.openRound({ budget: "100", deadlineDays: "7.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = peerAllocationHandler.openRound({ budget: "100", deadlineDays: "7.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof peerAllocationHandler.openRound !== 'function') return;
      const result = await interpret(peerAllocationHandler.openRound({ budget: "100", deadlineDays: "7.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "open_weekly_round" -> ok', async () => {
      if (typeof peerAllocationHandler.openRound !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(peerAllocationHandler.openRound({ budget: "100", deadlineDays: "7.0" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "open_zero_budget" -> error', async () => {
      if (typeof peerAllocationHandler.openRound !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(peerAllocationHandler.openRound({ budget: "0", deadlineDays: "7.0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('allocate', () => {
    it('builds a valid StorageProgram', () => {
      const program = peerAllocationHandler.allocate({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"}, allocator: "alice", recipient: "bob", amount: "30", note: "Great code review" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = peerAllocationHandler.allocate({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"}, allocator: "alice", recipient: "bob", amount: "30", note: "Great code review" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = peerAllocationHandler.allocate({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"}, allocator: "alice", recipient: "bob", amount: "30", note: "Great code review" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = peerAllocationHandler.allocate({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"}, allocator: "alice", recipient: "bob", amount: "30", note: "Great code review" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = peerAllocationHandler.allocate({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"}, allocator: "alice", recipient: "bob", amount: "30", note: "Great code review" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof peerAllocationHandler.allocate !== 'function') return;
      const result = await interpret(peerAllocationHandler.allocate({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"}, allocator: "alice", recipient: "bob", amount: "30", note: "Great code review" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "allocate_to_peer" -> ok', async () => {
      if (typeof peerAllocationHandler.allocate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_open_weekly_round = await interpret(peerAllocationHandler.openRound({ budget: "100", deadlineDays: "7.0" }), storage);
      const result = await interpret(peerAllocationHandler.allocate({ round: afterResult_open_weekly_round?.output?.["id"], allocator: "alice", recipient: "bob", amount: "30", note: "Great code review" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "allocate_to_self" -> error', async () => {
      if (typeof peerAllocationHandler.allocate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(peerAllocationHandler.allocate({ round: "peer-alloc-001", allocator: "alice", recipient: "alice", amount: "10" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('finalize', () => {
    it('builds a valid StorageProgram', () => {
      const program = peerAllocationHandler.finalize({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = peerAllocationHandler.finalize({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = peerAllocationHandler.finalize({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = peerAllocationHandler.finalize({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = peerAllocationHandler.finalize({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof peerAllocationHandler.finalize !== 'function') return;
      const result = await interpret(peerAllocationHandler.finalize({ round: {"type":"ref","fixture":"open_weekly_round","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "finalize_round" -> ok', async () => {
      if (typeof peerAllocationHandler.finalize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_open_weekly_round = await interpret(peerAllocationHandler.openRound({ budget: "100", deadlineDays: "7.0" }), storage);
      const result = await interpret(peerAllocationHandler.finalize({ round: afterResult_open_weekly_round?.output?.["id"] }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "finalize_nonexistent" -> error', async () => {
      if (typeof peerAllocationHandler.finalize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(peerAllocationHandler.finalize({ round: "peer-alloc-nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof peerAllocationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = peerAllocationHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('PeerAllocation');
    });
  });

  describe('invariant examples', () => {
    it("openRound-then-finalize", async () => {
      const storage = createInMemoryStorage();
      const openRoundResult0 = await interpret(peerAllocationHandler.openRound({ budget: {"type":"literal","value":100}, deadlineDays: {"type":"literal","value":7} }), storage);
      expect(openRoundResult0.variant).toBe("opened");
      let round = openRoundResult0.output["round"];
      const thenResult0 = await interpret(peerAllocationHandler.allocate({ round: {"type":"variable","name":"pa"}, allocator: {"type":"variable","name":"a"}, recipient: {"type":"variable","name":"b"}, amount: {"type":"literal","value":30}, note: {"type":"variable","name":"_"} }), storage);
      expect(thenResult0.variant).toBe("allocated");
      const thenResult1 = await interpret(peerAllocationHandler.finalize({ round: {"type":"variable","name":"pa"} }), storage);
      expect(thenResult1.variant).toBe("finalized");
    });

  });

  describe('action contracts (PBT)', () => {
    it('openRound handles empty input: ', async () => {
      if (typeof peerAllocationHandler.openRound !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(peerAllocationHandler.openRound({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('openRound ensures on opened: ', async () => {
      if (typeof peerAllocationHandler.openRound !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ budget: fc.integer({ min: 1, max: 1000 }), deadlineDays: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = peerAllocationHandler.openRound(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "opened") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
