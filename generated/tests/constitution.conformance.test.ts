// Constitution Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { constitutionHandler } from '../../handlers/ts/llm-agent/constitution.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Constitution functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = constitutionHandler.create({ name: 'test-name', principles: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = constitutionHandler.create({ name: 'test-name', principles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = constitutionHandler.create({ name: 'test-name', principles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = constitutionHandler.create({ name: 'test-name', principles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = constitutionHandler.create({ name: 'test-name', principles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof constitutionHandler.create !== 'function') return;
      const result = await interpret(constitutionHandler.create({ name: 'test-name', principles: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('critique', () => {
    it('builds a valid StorageProgram', () => {
      const program = constitutionHandler.critique({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = constitutionHandler.critique({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = constitutionHandler.critique({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = constitutionHandler.critique({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = constitutionHandler.critique({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof constitutionHandler.critique !== 'function') return;
      const result = await interpret(constitutionHandler.critique({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('revise', () => {
    it('builds a valid StorageProgram', () => {
      const program = constitutionHandler.revise({ constitution: 'test', response: 'test-response', critique: 'test-critique' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = constitutionHandler.revise({ constitution: 'test', response: 'test-response', critique: 'test-critique' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = constitutionHandler.revise({ constitution: 'test', response: 'test-response', critique: 'test-critique' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = constitutionHandler.revise({ constitution: 'test', response: 'test-response', critique: 'test-critique' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = constitutionHandler.revise({ constitution: 'test', response: 'test-response', critique: 'test-critique' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof constitutionHandler.revise !== 'function') return;
      const result = await interpret(constitutionHandler.revise({ constitution: 'test', response: 'test-response', critique: 'test-critique' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('critiqueAndRevise', () => {
    it('builds a valid StorageProgram', () => {
      const program = constitutionHandler.critiqueAndRevise({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = constitutionHandler.critiqueAndRevise({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = constitutionHandler.critiqueAndRevise({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = constitutionHandler.critiqueAndRevise({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = constitutionHandler.critiqueAndRevise({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof constitutionHandler.critiqueAndRevise !== 'function') return;
      const result = await interpret(constitutionHandler.critiqueAndRevise({ constitution: 'test', response: 'test-response', prompt: 'test-prompt' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('addPrinciple', () => {
    it('builds a valid StorageProgram', () => {
      const program = constitutionHandler.addPrinciple({ constitution: 'test', text: 'test-text', category: 'test-category', priority: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = constitutionHandler.addPrinciple({ constitution: 'test', text: 'test-text', category: 'test-category', priority: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = constitutionHandler.addPrinciple({ constitution: 'test', text: 'test-text', category: 'test-category', priority: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = constitutionHandler.addPrinciple({ constitution: 'test', text: 'test-text', category: 'test-category', priority: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = constitutionHandler.addPrinciple({ constitution: 'test', text: 'test-text', category: 'test-category', priority: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof constitutionHandler.addPrinciple !== 'function') return;
      const result = await interpret(constitutionHandler.addPrinciple({ constitution: 'test', text: 'test-text', category: 'test-category', priority: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('removePrinciple', () => {
    it('builds a valid StorageProgram', () => {
      const program = constitutionHandler.removePrinciple({ constitution: 'test', principle_id: 'test-principle_id' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = constitutionHandler.removePrinciple({ constitution: 'test', principle_id: 'test-principle_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = constitutionHandler.removePrinciple({ constitution: 'test', principle_id: 'test-principle_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = constitutionHandler.removePrinciple({ constitution: 'test', principle_id: 'test-principle_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = constitutionHandler.removePrinciple({ constitution: 'test', principle_id: 'test-principle_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof constitutionHandler.removePrinciple !== 'function') return;
      const result = await interpret(constitutionHandler.removePrinciple({ constitution: 'test', principle_id: 'test-principle_id' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof constitutionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = constitutionHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Constitution');
    });
  });

  describe('invariant examples', () => {
    it("create-then-critique", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(constitutionHandler.create({ name: "safety", principles: [{"text":"Never generate harmful content","category":"safety","priority":1}] }), storage);
      expect(createResult0.variant).toBe("ok");
      let constitution = createResult0.output["constitution"];
      let w = constitution;
      const thenResult0 = await interpret(constitutionHandler.critique({ constitution: w, response: "safe response", prompt: "test" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), principles: fc.string() }) }),
              fc.record({ action: fc.constant('critique'), input: fc.record({ constitution: fc.string(), response: fc.string({ minLength: 1, maxLength: 50 }), prompt: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('revise'), input: fc.record({ constitution: fc.string(), response: fc.string({ minLength: 1, maxLength: 50 }), critique: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('critiqueAndRevise'), input: fc.record({ constitution: fc.string(), response: fc.string({ minLength: 1, maxLength: 50 }), prompt: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('addPrinciple'), input: fc.record({ constitution: fc.string(), text: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), priority: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('removePrinciple'), input: fc.record({ constitution: fc.string(), principle_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = constitutionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(constitutionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), principles: fc.string() }) }),
              fc.record({ action: fc.constant('critique'), input: fc.record({ constitution: fc.string(), response: fc.string({ minLength: 1, maxLength: 50 }), prompt: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('revise'), input: fc.record({ constitution: fc.string(), response: fc.string({ minLength: 1, maxLength: 50 }), critique: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('critiqueAndRevise'), input: fc.record({ constitution: fc.string(), response: fc.string({ minLength: 1, maxLength: 50 }), prompt: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('addPrinciple'), input: fc.record({ constitution: fc.string(), text: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), priority: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('removePrinciple'), input: fc.record({ constitution: fc.string(), principle_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = constitutionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(constitutionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-name
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof constitutionHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(constitutionHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof constitutionHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), principles: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = constitutionHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
