// ActionType Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { actionTypeHandler } from '../../handlers/ts/app/action-type.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ActionType functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('define', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionTypeHandler.define !== 'function') return;
      const result = await interpret(actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "define_invoke" -> ok', async () => {
      if (typeof actionTypeHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "define_commit" -> ok', async () => {
      if (typeof actionTypeHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionTypeHandler.define({ actionType: "commit", name: "commit", properties: "{\"semantics\": \"finalize\", \"reversibility\": \"irreversible\", \"async\": false}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "define_trigger" -> ok', async () => {
      if (typeof actionTypeHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionTypeHandler.define({ actionType: "trigger", name: "trigger", properties: "{\"semantics\": \"fire-and-forget\", \"reversibility\": \"irreversible\", \"async\": true}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "define_duplicate" -> duplicate', async () => {
      if (typeof actionTypeHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_invoke = await interpret(actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" }), storage);
      const _pool = Object.assign({}, (afterResult_define_invoke?.output ?? {}));
      const _fixtureInput = { actionType: "invoke", name: "invoke", properties: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(actionTypeHandler.define({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('classify', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionTypeHandler.classify({ target: "Task/complete", variants: "[\"ok\",\"error\"]", context: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionTypeHandler.classify({ target: "Task/complete", variants: "[\"ok\",\"error\"]", context: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionTypeHandler.classify({ target: "Task/complete", variants: "[\"ok\",\"error\"]", context: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionTypeHandler.classify({ target: "Task/complete", variants: "[\"ok\",\"error\"]", context: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionTypeHandler.classify({ target: "Task/complete", variants: "[\"ok\",\"error\"]", context: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionTypeHandler.classify !== 'function') return;
      const result = await interpret(actionTypeHandler.classify({ target: "Task/complete", variants: "[\"ok\",\"error\"]", context: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "classify_button" -> ok', async () => {
      if (typeof actionTypeHandler.classify !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_invoke = await interpret(actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" }), storage);
      const _pool = Object.assign({}, (afterResult_define_invoke?.output ?? {}));
      const _fixtureInput = { target: "Task/complete", variants: "[\"ok\",\"error\"]", context: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(actionTypeHandler.classify({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "classify_form_submit" -> ok', async () => {
      if (typeof actionTypeHandler.classify !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_commit = await interpret(actionTypeHandler.define({ actionType: "commit", name: "commit", properties: "{\"semantics\": \"finalize\", \"reversibility\": \"irreversible\", \"async\": false}" }), storage);
      const _pool = Object.assign({}, (afterResult_define_commit?.output ?? {}));
      const _fixtureInput = { target: "Invoice/submit", variants: "[\"ok\",\"error\",\"invalid\"]", context: "{\"formFlow\": true}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(actionTypeHandler.classify({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "classify_background" -> ok', async () => {
      if (typeof actionTypeHandler.classify !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_trigger = await interpret(actionTypeHandler.define({ actionType: "trigger", name: "trigger", properties: "{\"semantics\": \"fire-and-forget\", \"reversibility\": \"irreversible\", \"async\": true}" }), storage);
      const _pool = Object.assign({}, (afterResult_define_trigger?.output ?? {}));
      const _fixtureInput = { target: "Report/generate", variants: "[\"ok\",\"error\"]", context: "{\"async\": true}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(actionTypeHandler.classify({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionTypeHandler.get({ actionType: {"type":"ref","fixture":"define_invoke","field":"actionType"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionTypeHandler.get({ actionType: {"type":"ref","fixture":"define_invoke","field":"actionType"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionTypeHandler.get({ actionType: {"type":"ref","fixture":"define_invoke","field":"actionType"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionTypeHandler.get({ actionType: {"type":"ref","fixture":"define_invoke","field":"actionType"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionTypeHandler.get({ actionType: {"type":"ref","fixture":"define_invoke","field":"actionType"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionTypeHandler.get !== 'function') return;
      const result = await interpret(actionTypeHandler.get({ actionType: {"type":"ref","fixture":"define_invoke","field":"actionType"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof actionTypeHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_invoke = await interpret(actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" }), storage);
      const result = await interpret(actionTypeHandler.get({ actionType: afterResult_define_invoke?.output?.["actionType"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof actionTypeHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionTypeHandler.get({ actionType: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionTypeHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionTypeHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionTypeHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionTypeHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionTypeHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionTypeHandler.list !== 'function') return;
      const result = await interpret(actionTypeHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof actionTypeHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_invoke = await interpret(actionTypeHandler.define({ actionType: "invoke", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"reversibility\": \"irreversible\", \"async\": false}" }), storage);
      const _pool = Object.assign({}, (afterResult_define_invoke?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(actionTypeHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof actionTypeHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = actionTypeHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ActionType');
    });
  });

  describe('invariant examples', () => {
    it("define then get", async () => {
      const storage = createInMemoryStorage();
      const defineResult0 = await interpret(actionTypeHandler.define({ actionType: "test-t", name: "invoke", properties: "{\"semantics\": \"one-shot\", \"async\": false}" }), storage);
      expect(defineResult0.variant).toBe("ok");
      let actionType = defineResult0.output["actionType"];
      let t = actionType;
      const thenResult0 = await interpret(actionTypeHandler.get({ actionType: t }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("define duplicate rejected", async () => {
      const storage = createInMemoryStorage();
      const defineResult0 = await interpret(actionTypeHandler.define({ actionType: "test-t", name: "commit", properties: "{\"semantics\": \"finalize\"}" }), storage);
      expect(defineResult0.variant).toBe("ok");
      let actionType = defineResult0.output["actionType"];
      let t = actionType;
      const thenResult0 = await interpret(actionTypeHandler.define({ actionType: t, name: "commit", properties: "{}" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: registered types have names', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('define'), input: fc.record({ actionType: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('classify'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), variants: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ actionType: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionTypeHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(actionTypeHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned type properties', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('define'), input: fc.record({ actionType: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('classify'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), variants: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ actionType: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionTypeHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(actionTypeHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned type properties
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('define handles empty input: ', async () => {
      if (typeof actionTypeHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionTypeHandler.define({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('define ensures on ok: ', async () => {
      if (typeof actionTypeHandler.define !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ actionType: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = actionTypeHandler.define(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('get handles empty input: ', async () => {
      if (typeof actionTypeHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionTypeHandler.get({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

});
