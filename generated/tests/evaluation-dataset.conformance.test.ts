// EvaluationDataset Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { evaluationDatasetHandler } from '../../handlers/ts/repertoire/llm-training/evaluation-dataset.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('EvaluationDataset functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationDatasetHandler.create({ name: 'test-name', examples: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationDatasetHandler.create({ name: 'test-name', examples: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationDatasetHandler.create({ name: 'test-name', examples: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationDatasetHandler.create({ name: 'test-name', examples: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationDatasetHandler.create({ name: 'test-name', examples: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationDatasetHandler.create !== 'function') return;
      const result = await interpret(evaluationDatasetHandler.create({ name: 'test-name', examples: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('addExample', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationDatasetHandler.addExample({ dataset: 'test', input: 'test-input', expected: 'test-expected', rubric: 'test', tags: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationDatasetHandler.addExample({ dataset: 'test', input: 'test-input', expected: 'test-expected', rubric: 'test', tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationDatasetHandler.addExample({ dataset: 'test', input: 'test-input', expected: 'test-expected', rubric: 'test', tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationDatasetHandler.addExample({ dataset: 'test', input: 'test-input', expected: 'test-expected', rubric: 'test', tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationDatasetHandler.addExample({ dataset: 'test', input: 'test-input', expected: 'test-expected', rubric: 'test', tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationDatasetHandler.addExample !== 'function') return;
      const result = await interpret(evaluationDatasetHandler.addExample({ dataset: 'test', input: 'test-input', expected: 'test-expected', rubric: 'test', tags: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('evaluate', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationDatasetHandler.evaluate({ dataset: 'test', program: 'test-program', metrics: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationDatasetHandler.evaluate({ dataset: 'test', program: 'test-program', metrics: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationDatasetHandler.evaluate({ dataset: 'test', program: 'test-program', metrics: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationDatasetHandler.evaluate({ dataset: 'test', program: 'test-program', metrics: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationDatasetHandler.evaluate({ dataset: 'test', program: 'test-program', metrics: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationDatasetHandler.evaluate !== 'function') return;
      const result = await interpret(evaluationDatasetHandler.evaluate({ dataset: 'test', program: 'test-program', metrics: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('detectDrift', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationDatasetHandler.detectDrift({ dataset: 'test', current_program: 'test-current_program' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationDatasetHandler.detectDrift({ dataset: 'test', current_program: 'test-current_program' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationDatasetHandler.detectDrift({ dataset: 'test', current_program: 'test-current_program' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationDatasetHandler.detectDrift({ dataset: 'test', current_program: 'test-current_program' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationDatasetHandler.detectDrift({ dataset: 'test', current_program: 'test-current_program' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationDatasetHandler.detectDrift !== 'function') return;
      const result = await interpret(evaluationDatasetHandler.detectDrift({ dataset: 'test', current_program: 'test-current_program' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('setBaseline', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationDatasetHandler.setBaseline({ dataset: 'test', program: 'test-program' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationDatasetHandler.setBaseline({ dataset: 'test', program: 'test-program' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationDatasetHandler.setBaseline({ dataset: 'test', program: 'test-program' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationDatasetHandler.setBaseline({ dataset: 'test', program: 'test-program' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationDatasetHandler.setBaseline({ dataset: 'test', program: 'test-program' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationDatasetHandler.setBaseline !== 'function') return;
      const result = await interpret(evaluationDatasetHandler.setBaseline({ dataset: 'test', program: 'test-program' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('compare', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationDatasetHandler.compare({ dataset: 'test', program_a: 'test-program_a', program_b: 'test-program_b' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationDatasetHandler.compare({ dataset: 'test', program_a: 'test-program_a', program_b: 'test-program_b' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationDatasetHandler.compare({ dataset: 'test', program_a: 'test-program_a', program_b: 'test-program_b' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationDatasetHandler.compare({ dataset: 'test', program_a: 'test-program_a', program_b: 'test-program_b' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationDatasetHandler.compare({ dataset: 'test', program_a: 'test-program_a', program_b: 'test-program_b' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationDatasetHandler.compare !== 'function') return;
      const result = await interpret(evaluationDatasetHandler.compare({ dataset: 'test', program_a: 'test-program_a', program_b: 'test-program_b' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('curate', () => {
    it('builds a valid StorageProgram', () => {
      const program = evaluationDatasetHandler.curate({ dataset: 'test', filter_tags: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evaluationDatasetHandler.curate({ dataset: 'test', filter_tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evaluationDatasetHandler.curate({ dataset: 'test', filter_tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evaluationDatasetHandler.curate({ dataset: 'test', filter_tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evaluationDatasetHandler.curate({ dataset: 'test', filter_tags: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evaluationDatasetHandler.curate !== 'function') return;
      const result = await interpret(evaluationDatasetHandler.curate({ dataset: 'test', filter_tags: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof evaluationDatasetHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = evaluationDatasetHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('EvaluationDataset');
    });
  });

  describe('invariant examples', () => {
    it("create-then-evaluate", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(evaluationDatasetHandler.create({ name: "qa_golden", examples: [{"input":"What is 2+2?","expected":"4","rubric":"test-_","tags":["math"]}] }), storage);
      expect(createResult0.variant).toBe("ok");
      let dataset = createResult0.output["dataset"];
      let v = dataset;
      const thenResult0 = await interpret(evaluationDatasetHandler.evaluate({ dataset: v, program: "gpt-4o + default prompt", metrics: ["accuracy"] }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), examples: fc.string() }) }),
              fc.record({ action: fc.constant('addExample'), input: fc.record({ dataset: fc.string(), input: fc.string({ minLength: 1, maxLength: 50 }), expected: fc.string({ minLength: 1, maxLength: 50 }), rubric: fc.string(), tags: fc.string() }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ dataset: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }), metrics: fc.string() }) }),
              fc.record({ action: fc.constant('detectDrift'), input: fc.record({ dataset: fc.string(), current_program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setBaseline'), input: fc.record({ dataset: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ dataset: fc.string(), program_a: fc.string({ minLength: 1, maxLength: 50 }), program_b: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('curate'), input: fc.record({ dataset: fc.string(), filter_tags: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = evaluationDatasetHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(evaluationDatasetHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), examples: fc.string() }) }),
              fc.record({ action: fc.constant('addExample'), input: fc.record({ dataset: fc.string(), input: fc.string({ minLength: 1, maxLength: 50 }), expected: fc.string({ minLength: 1, maxLength: 50 }), rubric: fc.string(), tags: fc.string() }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ dataset: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }), metrics: fc.string() }) }),
              fc.record({ action: fc.constant('detectDrift'), input: fc.record({ dataset: fc.string(), current_program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setBaseline'), input: fc.record({ dataset: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ dataset: fc.string(), program_a: fc.string({ minLength: 1, maxLength: 50 }), program_b: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('curate'), input: fc.record({ dataset: fc.string(), filter_tags: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = evaluationDatasetHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(evaluationDatasetHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-name
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof evaluationDatasetHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(evaluationDatasetHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof evaluationDatasetHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), examples: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = evaluationDatasetHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
