// ContentSerializer Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { contentSerializerHandler } from '../../handlers/ts/content/content-serializer.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ContentSerializer functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentSerializerHandler.register !== 'function') return;
      const result = await interpret(contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_markdown" -> ok', async () => {
      if (typeof contentSerializerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_html" -> ok', async () => {
      if (typeof contentSerializerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentSerializerHandler.register({ provider: "html-serialize", target: "html", config: "{\"selfClosing\":true}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_json" -> ok', async () => {
      if (typeof contentSerializerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentSerializerHandler.register({ provider: "json-serialize", target: "json", config: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_pdf" -> ok', async () => {
      if (typeof contentSerializerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentSerializerHandler.register({ provider: "pdf-serialize", target: "pdf", config: "{\"pageSize\":\"A4\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_provider" -> error', async () => {
      if (typeof contentSerializerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentSerializerHandler.register({ provider: "", target: "markdown", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "empty_target" -> error', async () => {
      if (typeof contentSerializerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentSerializerHandler.register({ provider: "micromark-serialize", target: "", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "duplicate_provider" -> duplicate', async () => {
      if (typeof contentSerializerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_markdown = await interpret(contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_markdown?.output ?? {}));
      const _fixtureInput = { provider: "micromark-serialize", target: "markdown", config: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(contentSerializerHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('serialize', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentSerializerHandler.serialize({ target: "markdown", rootNodeId: {"type":"ref","fixture":"register_markdown","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentSerializerHandler.serialize({ target: "markdown", rootNodeId: {"type":"ref","fixture":"register_markdown","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentSerializerHandler.serialize({ target: "markdown", rootNodeId: {"type":"ref","fixture":"register_markdown","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentSerializerHandler.serialize({ target: "markdown", rootNodeId: {"type":"ref","fixture":"register_markdown","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentSerializerHandler.serialize({ target: "markdown", rootNodeId: {"type":"ref","fixture":"register_markdown","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentSerializerHandler.serialize !== 'function') return;
      const result = await interpret(contentSerializerHandler.serialize({ target: "markdown", rootNodeId: {"type":"ref","fixture":"register_markdown","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "serialize_markdown" -> ok', async () => {
      if (typeof contentSerializerHandler.serialize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_markdown = await interpret(contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" }), storage);
      const result = await interpret(contentSerializerHandler.serialize({ target: "markdown", rootNodeId: afterResult_register_markdown?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "serialize_html" -> ok', async () => {
      if (typeof contentSerializerHandler.serialize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_html = await interpret(contentSerializerHandler.register({ provider: "html-serialize", target: "html", config: "{\"selfClosing\":true}" }), storage);
      const result = await interpret(contentSerializerHandler.serialize({ target: "html", rootNodeId: afterResult_register_html?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "no_provider_target" -> no_provider', async () => {
      if (typeof contentSerializerHandler.serialize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentSerializerHandler.serialize({ target: "latex", rootNodeId: "node-abc" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('no_provider'));
    });

    it('fixture "missing_root" -> error', async () => {
      if (typeof contentSerializerHandler.serialize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentSerializerHandler.serialize({ target: "markdown", rootNodeId: "nonexistent-node-xyz" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listTargets', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentSerializerHandler.listTargets({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentSerializerHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentSerializerHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentSerializerHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentSerializerHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentSerializerHandler.listTargets !== 'function') return;
      const result = await interpret(contentSerializerHandler.listTargets({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_after_register" -> ok', async () => {
      if (typeof contentSerializerHandler.listTargets !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_markdown = await interpret(contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_markdown?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(contentSerializerHandler.listTargets({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof contentSerializerHandler.listTargets !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_markdown = await interpret(contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_markdown?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(contentSerializerHandler.listTargets({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then serialize", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(contentSerializerHandler.register({ provider: "micromark-serialize", target: "markdown", config: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let p = id;
      const thenResult0 = await interpret(contentSerializerHandler.serialize({ target: "markdown", rootNodeId: "page-root-1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register then list", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(contentSerializerHandler.register({ provider: "html-serialize", target: "html", config: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let p = id;
      const thenResult0 = await interpret(contentSerializerHandler.listTargets({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate rejected", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(contentSerializerHandler.register({ provider: "json-serialize", target: "json", config: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let p = id;
      const thenResult0 = await interpret(contentSerializerHandler.register({ provider: "json-serialize", target: "json", config: "{}" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

    it("no_provider for unregistered target", async () => {
      const storage = createInMemoryStorage();
      const listTargetsResult0 = await interpret(contentSerializerHandler.listTargets({  }), storage);
      expect(listTargetsResult0.variant).toBe("ok");
      let targets = listTargetsResult0.output["targets"];
      let ts = targets;
      const thenResult0 = await interpret(contentSerializerHandler.serialize({ target: "docx", rootNodeId: "page-root-1" }), storage);
      expect(thenResult0.variant).toBe("no_provider");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: byTarget index consistent', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('serialize'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), rootNodeId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listTargets'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = contentSerializerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(contentSerializerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphan index entry', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('serialize'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), rootNodeId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listTargets'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = contentSerializerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(contentSerializerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphan index entry
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
