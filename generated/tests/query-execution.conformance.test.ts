// QueryExecution Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { queryExecutionHandler } from '../../handlers/ts/view/query-execution.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('QueryExecution functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('registerKind', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryExecutionHandler.registerKind({ kind: "kernel" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryExecutionHandler.registerKind({ kind: "kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryExecutionHandler.registerKind({ kind: "kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryExecutionHandler.registerKind({ kind: "kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryExecutionHandler.registerKind({ kind: "kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryExecutionHandler.registerKind !== 'function') return;
      const result = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_kernel" -> ok', async () => {
      if (typeof queryExecutionHandler.registerKind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_inmemory" -> ok', async () => {
      if (typeof queryExecutionHandler.registerKind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryExecutionHandler.registerKind({ kind: "in-memory" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty" -> error', async () => {
      if (typeof queryExecutionHandler.registerKind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryExecutionHandler.registerKind({ kind: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof queryExecutionHandler.registerKind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}));
      const _fixtureInput = { kind: "kernel" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryExecutionHandler.registerKind({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryExecutionHandler.register !== 'function') return;
      const result = await interpret(queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_default_kernel" -> ok', async () => {
      if (typeof queryExecutionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}));
      const _fixtureInput = { name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryExecutionHandler.register({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate_name" -> duplicate', async () => {
      if (typeof queryExecutionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const afterResult_register_default_kernel = await interpret(queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}), (afterResult_register_default_kernel?.output ?? {}));
      const _fixtureInput = { name: "default-kernel", kind: "kernel", capabilities: [] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryExecutionHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "register_empty_name" -> error', async () => {
      if (typeof queryExecutionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryExecutionHandler.register({ name: "", kind: "kernel", capabilities: [] }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('execute', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryExecutionHandler.execute({ program: {"instructions":[]}, kind: "kernel" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryExecutionHandler.execute({ program: {"instructions":[]}, kind: "kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryExecutionHandler.execute({ program: {"instructions":[]}, kind: "kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryExecutionHandler.execute({ program: {"instructions":[]}, kind: "kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryExecutionHandler.execute({ program: {"instructions":[]}, kind: "kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryExecutionHandler.execute !== 'function') return;
      const result = await interpret(queryExecutionHandler.execute({ program: {"instructions":[]}, kind: "kernel" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "execute_kernel" -> ok', async () => {
      if (typeof queryExecutionHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}));
      const _fixtureInput = { program: {"instructions":[]}, kind: "kernel" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryExecutionHandler.execute({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "execute_invoke_pending" -> ok', async () => {
      if (typeof queryExecutionHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}));
      const _fixtureInput = { program: {"instructions":[{"type":"invoke","concept":"Task","action":"complete","input":"{}"}]}, kind: "kernel" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryExecutionHandler.execute({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "execute_unknown" -> error', async () => {
      if (typeof queryExecutionHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryExecutionHandler.execute({ program: {}, kind: "unknown" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('resumeAfterInvoke', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryExecutionHandler.resumeAfterInvoke({ continuation: {"remainingInstructions":[],"bindings":{},"programId":"p1","instructionIndex":"1"}, variant: "ok", output: {"rows":[]} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryExecutionHandler.resumeAfterInvoke({ continuation: {"remainingInstructions":[],"bindings":{},"programId":"p1","instructionIndex":"1"}, variant: "ok", output: {"rows":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryExecutionHandler.resumeAfterInvoke({ continuation: {"remainingInstructions":[],"bindings":{},"programId":"p1","instructionIndex":"1"}, variant: "ok", output: {"rows":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryExecutionHandler.resumeAfterInvoke({ continuation: {"remainingInstructions":[],"bindings":{},"programId":"p1","instructionIndex":"1"}, variant: "ok", output: {"rows":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryExecutionHandler.resumeAfterInvoke({ continuation: {"remainingInstructions":[],"bindings":{},"programId":"p1","instructionIndex":"1"}, variant: "ok", output: {"rows":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryExecutionHandler.resumeAfterInvoke !== 'function') return;
      const result = await interpret(queryExecutionHandler.resumeAfterInvoke({ continuation: {"remainingInstructions":[],"bindings":{},"programId":"p1","instructionIndex":"1"}, variant: "ok", output: {"rows":[]} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resume_ok" -> ok', async () => {
      if (typeof queryExecutionHandler.resumeAfterInvoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}));
      const _fixtureInput = { continuation: {"remainingInstructions":[],"bindings":{},"programId":"p1","instructionIndex":"1"}, variant: "ok", output: {"rows":[]} } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryExecutionHandler.resumeAfterInvoke({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resume_chain" -> ok', async () => {
      if (typeof queryExecutionHandler.resumeAfterInvoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}));
      const _fixtureInput = { continuation: {"remainingInstructions":[{"type":"invoke","concept":"Task","action":"archive","input":"{}"}],"bindings":{},"programId":"p2","instructionIndex":"2"}, variant: "ok", output: {} } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryExecutionHandler.resumeAfterInvoke({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resume_invalid" -> error', async () => {
      if (typeof queryExecutionHandler.resumeAfterInvoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryExecutionHandler.resumeAfterInvoke({ continuation: "not-json", variant: "ok", output: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('planPushdown', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryExecutionHandler.planPushdown({ program: {"instructions":[{"type":"scan"},{"type":"filter"},{"type":"sort"}]}, provider: "default-kernel" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryExecutionHandler.planPushdown({ program: {"instructions":[{"type":"scan"},{"type":"filter"},{"type":"sort"}]}, provider: "default-kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryExecutionHandler.planPushdown({ program: {"instructions":[{"type":"scan"},{"type":"filter"},{"type":"sort"}]}, provider: "default-kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryExecutionHandler.planPushdown({ program: {"instructions":[{"type":"scan"},{"type":"filter"},{"type":"sort"}]}, provider: "default-kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryExecutionHandler.planPushdown({ program: {"instructions":[{"type":"scan"},{"type":"filter"},{"type":"sort"}]}, provider: "default-kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryExecutionHandler.planPushdown !== 'function') return;
      const result = await interpret(queryExecutionHandler.planPushdown({ program: {"instructions":[{"type":"scan"},{"type":"filter"},{"type":"sort"}]}, provider: "default-kernel" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "plan_kernel" -> ok', async () => {
      if (typeof queryExecutionHandler.planPushdown !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const afterResult_register_default_kernel = await interpret(queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}), (afterResult_register_default_kernel?.output ?? {}));
      const _fixtureInput = { program: {"instructions":[{"type":"scan"},{"type":"filter"},{"type":"sort"}]}, provider: "default-kernel" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryExecutionHandler.planPushdown({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "plan_missing" -> error', async () => {
      if (typeof queryExecutionHandler.planPushdown !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryExecutionHandler.planPushdown({ program: {}, provider: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryExecutionHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryExecutionHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryExecutionHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryExecutionHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryExecutionHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryExecutionHandler.list !== 'function') return;
      const result = await interpret(queryExecutionHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof queryExecutionHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(queryExecutionHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryExecutionHandler.get({ name: "default-kernel" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryExecutionHandler.get({ name: "default-kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryExecutionHandler.get({ name: "default-kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryExecutionHandler.get({ name: "default-kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryExecutionHandler.get({ name: "default-kernel" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryExecutionHandler.get !== 'function') return;
      const result = await interpret(queryExecutionHandler.get({ name: "default-kernel" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof queryExecutionHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_kernel = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      const afterResult_register_default_kernel = await interpret(queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort","limit"] }), storage);
      const _pool = Object.assign({}, (afterResult_register_kernel?.output ?? {}), (afterResult_register_default_kernel?.output ?? {}));
      const _fixtureInput = { name: "default-kernel" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryExecutionHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof queryExecutionHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryExecutionHandler.get({ name: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });


  describe('invariant examples', () => {
    it("registerKind then register provider succeeds", async () => {
      const storage = createInMemoryStorage();
      const registerKindResult0 = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      expect(registerKindResult0.variant).toBe("ok");
      let kind = registerKindResult0.output["kind"];
      let k = kind;
      const thenResult0 = await interpret(queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter","sort"] }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate registerKind returns duplicate", async () => {
      const storage = createInMemoryStorage();
      const registerKindResult0 = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      expect(registerKindResult0.variant).toBe("ok");
      let kind = registerKindResult0.output["kind"];
      let k = kind;
      const thenResult0 = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

    it("register then get returns provider", async () => {
      const storage = createInMemoryStorage();
      const registerKindResult0 = await interpret(queryExecutionHandler.registerKind({ kind: "kernel" }), storage);
      expect(registerKindResult0.variant).toBe("ok");
      let kind = registerKindResult0.output["kind"];
      let k = kind;
      const registerResult1 = await interpret(queryExecutionHandler.register({ name: "default-kernel", kind: "kernel", capabilities: ["filter"] }), storage);
      expect(registerResult1.variant).toBe("ok");
      let provider = registerResult1.output["provider"];
      let p = provider;
      const thenResult0 = await interpret(queryExecutionHandler.get({ name: "default-kernel" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("execute with unknown kind returns error", async () => {
      const storage = createInMemoryStorage();
    });

    it("planPushdown with missing provider returns error", async () => {
      const storage = createInMemoryStorage();
    });

    it("list returns registered providers", async () => {
      const storage = createInMemoryStorage();
      const registerKindResult0 = await interpret(queryExecutionHandler.registerKind({ kind: "in-memory" }), storage);
      expect(registerKindResult0.variant).toBe("ok");
      let kind = registerKindResult0.output["kind"];
      let k = kind;
      const registerResult1 = await interpret(queryExecutionHandler.register({ name: "mem-provider", kind: "in-memory", capabilities: [] }), storage);
      expect(registerResult1.variant).toBe("ok");
      let provider = registerResult1.output["provider"];
      let p = provider;
      const thenResult0 = await interpret(queryExecutionHandler.list({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("get unknown provider returns notfound", async () => {
      const storage = createInMemoryStorage();
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: providers have non-empty names', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerKind'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), capabilities: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resumeAfterInvoke'), input: fc.record({ continuation: fc.string({ minLength: 1, maxLength: 50 }), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('planPushdown'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), provider: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryExecutionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryExecutionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: provider kinds are registered', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerKind'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), capabilities: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resumeAfterInvoke'), input: fc.record({ continuation: fc.string({ minLength: 1, maxLength: 50 }), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('planPushdown'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), provider: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryExecutionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryExecutionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
