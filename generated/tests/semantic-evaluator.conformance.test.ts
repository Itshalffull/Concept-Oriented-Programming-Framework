// SemanticEvaluator Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { semanticEvaluatorHandler } from '../../handlers/ts/quality-review/semantic-evaluator.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SemanticEvaluator functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('assess', () => {
    it('builds a valid StorageProgram', () => {
      const program = semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof semanticEvaluatorHandler.assess !== 'function') return;
      const result = await interpret(semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "assess_ok" -> ok', async () => {
      if (typeof semanticEvaluatorHandler.assess !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_ok = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" }), storage);
      const _pool = Object.assign({}, (afterResult_configure_ok?.output ?? {}));
      const _fixtureInput = { target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(semanticEvaluatorHandler.assess({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "assess_minimal" -> ok', async () => {
      if (typeof semanticEvaluatorHandler.assess !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_ok = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" }), storage);
      const _pool = Object.assign({}, (afterResult_configure_ok?.output ?? {}));
      const _fixtureInput = { target: "utils/helpers.ts", intentDescription: null, persona: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(semanticEvaluatorHandler.assess({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "assess_not_found" -> targetNotFound', async () => {
      if (typeof semanticEvaluatorHandler.assess !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(semanticEvaluatorHandler.assess({ target: "nonexistent/file.ts", intentDescription: null, persona: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('targetNotFound'));
    });

  });

  describe('assessBatch', () => {
    it('builds a valid StorageProgram', () => {
      const program = semanticEvaluatorHandler.assessBatch({ targets: [{"target":"auth/login.ts","intentDescription":"Auth flow"},{"target":"api/users.ts","intentDescription":null}], persona: "senior" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = semanticEvaluatorHandler.assessBatch({ targets: [{"target":"auth/login.ts","intentDescription":"Auth flow"},{"target":"api/users.ts","intentDescription":null}], persona: "senior" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = semanticEvaluatorHandler.assessBatch({ targets: [{"target":"auth/login.ts","intentDescription":"Auth flow"},{"target":"api/users.ts","intentDescription":null}], persona: "senior" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = semanticEvaluatorHandler.assessBatch({ targets: [{"target":"auth/login.ts","intentDescription":"Auth flow"},{"target":"api/users.ts","intentDescription":null}], persona: "senior" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = semanticEvaluatorHandler.assessBatch({ targets: [{"target":"auth/login.ts","intentDescription":"Auth flow"},{"target":"api/users.ts","intentDescription":null}], persona: "senior" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof semanticEvaluatorHandler.assessBatch !== 'function') return;
      const result = await interpret(semanticEvaluatorHandler.assessBatch({ targets: [{"target":"auth/login.ts","intentDescription":"Auth flow"},{"target":"api/users.ts","intentDescription":null}], persona: "senior" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "batch_ok" -> ok', async () => {
      if (typeof semanticEvaluatorHandler.assessBatch !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_ok = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" }), storage);
      const _pool = Object.assign({}, (afterResult_configure_ok?.output ?? {}));
      const _fixtureInput = { targets: [{"target":"auth/login.ts","intentDescription":"Auth flow"},{"target":"api/users.ts","intentDescription":null}], persona: "senior" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(semanticEvaluatorHandler.assessBatch({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "batch_single" -> ok', async () => {
      if (typeof semanticEvaluatorHandler.assessBatch !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_ok = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" }), storage);
      const _pool = Object.assign({}, (afterResult_configure_ok?.output ?? {}));
      const _fixtureInput = { targets: [{"target":"utils/helpers.ts","intentDescription":null}], persona: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(semanticEvaluatorHandler.assessBatch({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof semanticEvaluatorHandler.configure !== 'function') return;
      const result = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "configure_ok" -> ok', async () => {
      if (typeof semanticEvaluatorHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "configure_minimal" -> ok', async () => {
      if (typeof semanticEvaluatorHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: null, promptTemplate: null, confidenceThreshold: null }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('compare', () => {
    it('builds a valid StorageProgram', () => {
      const program = semanticEvaluatorHandler.compare({ target: "auth/login.ts", beforeVersion: "abc123", afterVersion: "def456" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = semanticEvaluatorHandler.compare({ target: "auth/login.ts", beforeVersion: "abc123", afterVersion: "def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = semanticEvaluatorHandler.compare({ target: "auth/login.ts", beforeVersion: "abc123", afterVersion: "def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = semanticEvaluatorHandler.compare({ target: "auth/login.ts", beforeVersion: "abc123", afterVersion: "def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = semanticEvaluatorHandler.compare({ target: "auth/login.ts", beforeVersion: "abc123", afterVersion: "def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof semanticEvaluatorHandler.compare !== 'function') return;
      const result = await interpret(semanticEvaluatorHandler.compare({ target: "auth/login.ts", beforeVersion: "abc123", afterVersion: "def456" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compare_ok" -> ok', async () => {
      if (typeof semanticEvaluatorHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_ok = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" }), storage);
      const afterResult_assess_ok = await interpret(semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" }), storage);
      const _pool = Object.assign({}, (afterResult_configure_ok?.output ?? {}), (afterResult_assess_ok?.output ?? {}));
      const _fixtureInput = { target: "auth/login.ts", beforeVersion: "abc123", afterVersion: "def456" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(semanticEvaluatorHandler.compare({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compare_not_found" -> targetNotFound', async () => {
      if (typeof semanticEvaluatorHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(semanticEvaluatorHandler.compare({ target: "nonexistent/file.ts", beforeVersion: "a", afterVersion: "b" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('targetNotFound'));
    });

  });

  describe('query', () => {
    it('builds a valid StorageProgram', () => {
      const program = semanticEvaluatorHandler.query({ targets: null, minRating: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = semanticEvaluatorHandler.query({ targets: null, minRating: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = semanticEvaluatorHandler.query({ targets: null, minRating: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = semanticEvaluatorHandler.query({ targets: null, minRating: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = semanticEvaluatorHandler.query({ targets: null, minRating: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof semanticEvaluatorHandler.query !== 'function') return;
      const result = await interpret(semanticEvaluatorHandler.query({ targets: null, minRating: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "query_all" -> ok', async () => {
      if (typeof semanticEvaluatorHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_ok = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" }), storage);
      const afterResult_assess_ok = await interpret(semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" }), storage);
      const _pool = Object.assign({}, (afterResult_configure_ok?.output ?? {}), (afterResult_assess_ok?.output ?? {}));
      const _fixtureInput = { targets: null, minRating: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(semanticEvaluatorHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_filtered" -> ok', async () => {
      if (typeof semanticEvaluatorHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_ok = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "senior", promptTemplate: null, confidenceThreshold: "0.8" }), storage);
      const afterResult_assess_ok = await interpret(semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" }), storage);
      const _pool = Object.assign({}, (afterResult_configure_ok?.output ?? {}), (afterResult_assess_ok?.output ?? {}));
      const _fixtureInput = { targets: ["auth/login.ts"], minRating: "5.0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(semanticEvaluatorHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof semanticEvaluatorHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = semanticEvaluatorHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('SemanticEvaluator');
    });
  });

  describe('invariant examples', () => {
    it("assess then query", async () => {
      const storage = createInMemoryStorage();
      const assessResult0 = await interpret(semanticEvaluatorHandler.assess({ target: "auth/login.ts", intentDescription: "Authenticate user with email and password, returning JWT on success", persona: "security" }), storage);
      expect(assessResult0.variant).toBe("ok");
      let evaluation = assessResult0.output["evaluation"];
      let e = evaluation;
      let overallRating = assessResult0.output["overallRating"];
      let r = overallRating;
      let issues = assessResult0.output["issues"];
      let _ = issues;
      const thenResult0 = await interpret(semanticEvaluatorHandler.query({ targets: ["auth/login.ts"], minRating: false }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("configure then assess uses defaults", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(semanticEvaluatorHandler.configure({ defaultPersona: "architect", promptTemplate: false, confidenceThreshold: 0.9 }), storage);
      expect(configureResult0.variant).toBe("ok");
      const thenResult0 = await interpret(semanticEvaluatorHandler.assess({ target: "api/routes.ts", intentDescription: false, persona: false }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: evaluation without target', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('assess'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), intentDescription: fc.string(), persona: fc.string() }) }),
              fc.record({ action: fc.constant('assessBatch'), input: fc.record({ targets: fc.string(), persona: fc.string() }) }),
              fc.record({ action: fc.constant('configure'), input: fc.record({ defaultPersona: fc.string(), promptTemplate: fc.string(), confidenceThreshold: fc.string() }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), beforeVersion: fc.string({ minLength: 1, maxLength: 50 }), afterVersion: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('query'), input: fc.record({ targets: fc.string(), minRating: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = semanticEvaluatorHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(semanticEvaluatorHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: evaluation without target
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
