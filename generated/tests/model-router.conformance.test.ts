// ModelRouter Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { modelRouterHandler } from '../../handlers/ts/llm-core/model-router.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ModelRouter functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('addRoute', () => {
    it('builds a valid StorageProgram', () => {
      const program = modelRouterHandler.addRoute({ name: 'test-name', model_id: 'test-model_id', conditions: 'test', priority: 1, weight: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = modelRouterHandler.addRoute({ name: 'test-name', model_id: 'test-model_id', conditions: 'test', priority: 1, weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = modelRouterHandler.addRoute({ name: 'test-name', model_id: 'test-model_id', conditions: 'test', priority: 1, weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = modelRouterHandler.addRoute({ name: 'test-name', model_id: 'test-model_id', conditions: 'test', priority: 1, weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = modelRouterHandler.addRoute({ name: 'test-name', model_id: 'test-model_id', conditions: 'test', priority: 1, weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof modelRouterHandler.addRoute !== 'function') return;
      const result = await interpret(modelRouterHandler.addRoute({ name: 'test-name', model_id: 'test-model_id', conditions: 'test', priority: 1, weight: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('route', () => {
    it('builds a valid StorageProgram', () => {
      const program = modelRouterHandler.route({ task_type: 'test-task_type', complexity: 'test', cost_limit: 'test', latency_limit: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = modelRouterHandler.route({ task_type: 'test-task_type', complexity: 'test', cost_limit: 'test', latency_limit: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = modelRouterHandler.route({ task_type: 'test-task_type', complexity: 'test', cost_limit: 'test', latency_limit: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = modelRouterHandler.route({ task_type: 'test-task_type', complexity: 'test', cost_limit: 'test', latency_limit: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = modelRouterHandler.route({ task_type: 'test-task_type', complexity: 'test', cost_limit: 'test', latency_limit: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof modelRouterHandler.route !== 'function') return;
      const result = await interpret(modelRouterHandler.route({ task_type: 'test-task_type', complexity: 'test', cost_limit: 'test', latency_limit: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('fallback', () => {
    it('builds a valid StorageProgram', () => {
      const program = modelRouterHandler.fallback({ failed_model_id: 'test-failed_model_id', error_type: 'test-error_type' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = modelRouterHandler.fallback({ failed_model_id: 'test-failed_model_id', error_type: 'test-error_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = modelRouterHandler.fallback({ failed_model_id: 'test-failed_model_id', error_type: 'test-error_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = modelRouterHandler.fallback({ failed_model_id: 'test-failed_model_id', error_type: 'test-error_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = modelRouterHandler.fallback({ failed_model_id: 'test-failed_model_id', error_type: 'test-error_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof modelRouterHandler.fallback !== 'function') return;
      const result = await interpret(modelRouterHandler.fallback({ failed_model_id: 'test-failed_model_id', error_type: 'test-error_type' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('recordOutcome', () => {
    it('builds a valid StorageProgram', () => {
      const program = modelRouterHandler.recordOutcome({ route: 'test', success: true, latency_ms: 1, tokens: 1, cost: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = modelRouterHandler.recordOutcome({ route: 'test', success: true, latency_ms: 1, tokens: 1, cost: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = modelRouterHandler.recordOutcome({ route: 'test', success: true, latency_ms: 1, tokens: 1, cost: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = modelRouterHandler.recordOutcome({ route: 'test', success: true, latency_ms: 1, tokens: 1, cost: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = modelRouterHandler.recordOutcome({ route: 'test', success: true, latency_ms: 1, tokens: 1, cost: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof modelRouterHandler.recordOutcome !== 'function') return;
      const result = await interpret(modelRouterHandler.recordOutcome({ route: 'test', success: true, latency_ms: 1, tokens: 1, cost: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('getHealth', () => {
    it('builds a valid StorageProgram', () => {
      const program = modelRouterHandler.getHealth({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = modelRouterHandler.getHealth({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = modelRouterHandler.getHealth({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = modelRouterHandler.getHealth({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = modelRouterHandler.getHealth({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof modelRouterHandler.getHealth !== 'function') return;
      const result = await interpret(modelRouterHandler.getHealth({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof modelRouterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = modelRouterHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ModelRouter');
    });
  });

  describe('invariant examples', () => {
    it("addRoute-then-route", async () => {
      const storage = createInMemoryStorage();
      const addRouteResult0 = await interpret(modelRouterHandler.addRoute({ name: "fast", model_id: "gpt-4o-mini", conditions: "test-c", priority: 1, weight: 1 }), storage);
      expect(addRouteResult0.variant).toBe("ok");
      let route = addRouteResult0.output["route"];
      let r = route;
      const thenResult0 = await interpret(modelRouterHandler.route({ task_type: "chat", complexity: "test-_", cost_limit: "test-_", latency_limit: "test-_" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-route_name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('addRoute'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), model_id: fc.string({ minLength: 1, maxLength: 50 }), conditions: fc.string(), priority: fc.integer({ min: 1, max: 1000 }), weight: fc.string() }) }),
              fc.record({ action: fc.constant('route'), input: fc.record({ task_type: fc.string({ minLength: 1, maxLength: 50 }), complexity: fc.string(), cost_limit: fc.string(), latency_limit: fc.string() }) }),
              fc.record({ action: fc.constant('fallback'), input: fc.record({ failed_model_id: fc.string({ minLength: 1, maxLength: 50 }), error_type: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordOutcome'), input: fc.record({ route: fc.string(), success: fc.boolean(), latency_ms: fc.integer({ min: 1, max: 1000 }), tokens: fc.integer({ min: 1, max: 1000 }), cost: fc.string() }) }),
              fc.record({ action: fc.constant('getHealth'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = modelRouterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(modelRouterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-model_id', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('addRoute'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), model_id: fc.string({ minLength: 1, maxLength: 50 }), conditions: fc.string(), priority: fc.integer({ min: 1, max: 1000 }), weight: fc.string() }) }),
              fc.record({ action: fc.constant('route'), input: fc.record({ task_type: fc.string({ minLength: 1, maxLength: 50 }), complexity: fc.string(), cost_limit: fc.string(), latency_limit: fc.string() }) }),
              fc.record({ action: fc.constant('fallback'), input: fc.record({ failed_model_id: fc.string({ minLength: 1, maxLength: 50 }), error_type: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordOutcome'), input: fc.record({ route: fc.string(), success: fc.boolean(), latency_ms: fc.integer({ min: 1, max: 1000 }), tokens: fc.integer({ min: 1, max: 1000 }), cost: fc.string() }) }),
              fc.record({ action: fc.constant('getHealth'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = modelRouterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(modelRouterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-model_id
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('addRoute handles empty input: ', async () => {
      if (typeof modelRouterHandler.addRoute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(modelRouterHandler.addRoute({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('addRoute ensures on ok: ', async () => {
      if (typeof modelRouterHandler.addRoute !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), model_id: fc.string({ minLength: 1, maxLength: 50 }), conditions: fc.string(), priority: fc.integer({ min: 1, max: 1000 }), weight: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = modelRouterHandler.addRoute(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
