// ContentReconciler Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { contentReconcilerHandler } from '../../handlers/ts/app/content-reconciler.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ContentReconciler functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentReconcilerHandler.register !== 'function') return;
      const result = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_program" -> ok', async () => {
      if (typeof contentReconcilerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_proposal" -> ok', async () => {
      if (typeof contentReconcilerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "proposal-001.md", targetConcept: "Proposal", mapping: "{\"projection\":\"page-to-proposal\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_source" -> error', async () => {
      if (typeof contentReconcilerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "", targetConcept: "Constitution", mapping: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('reconcile', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentReconcilerHandler.reconcile({ sourceKind: "page", sourceId: "program.md" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentReconcilerHandler.reconcile({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentReconcilerHandler.reconcile({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentReconcilerHandler.reconcile({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentReconcilerHandler.reconcile({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentReconcilerHandler.reconcile !== 'function') return;
      const result = await interpret(contentReconcilerHandler.reconcile({ sourceKind: "page", sourceId: "program.md" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "reconcile_program" -> ok', async () => {
      if (typeof contentReconcilerHandler.reconcile !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_program = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_program?.output ?? {}));
      const _fixtureInput = { sourceKind: "page", sourceId: "program.md" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(contentReconcilerHandler.reconcile({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reconcile_missing" -> notfound', async () => {
      if (typeof contentReconcilerHandler.reconcile !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentReconcilerHandler.reconcile({ sourceKind: "page", sourceId: "nonexistent.md" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "reconcile_empty" -> error', async () => {
      if (typeof contentReconcilerHandler.reconcile !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentReconcilerHandler.reconcile({ sourceKind: "page", sourceId: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('plan', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentReconcilerHandler.plan({ sourceKind: "page", sourceId: "program.md" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentReconcilerHandler.plan({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentReconcilerHandler.plan({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentReconcilerHandler.plan({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentReconcilerHandler.plan({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentReconcilerHandler.plan !== 'function') return;
      const result = await interpret(contentReconcilerHandler.plan({ sourceKind: "page", sourceId: "program.md" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "plan_program" -> ok', async () => {
      if (typeof contentReconcilerHandler.plan !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_program = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_program?.output ?? {}));
      const _fixtureInput = { sourceKind: "page", sourceId: "program.md" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(contentReconcilerHandler.plan({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "plan_missing" -> notfound', async () => {
      if (typeof contentReconcilerHandler.plan !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentReconcilerHandler.plan({ sourceKind: "page", sourceId: "nonexistent.md" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('status', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentReconcilerHandler.status({ sourceKind: "page", sourceId: "program.md" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentReconcilerHandler.status({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentReconcilerHandler.status({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentReconcilerHandler.status({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentReconcilerHandler.status({ sourceKind: "page", sourceId: "program.md" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentReconcilerHandler.status !== 'function') return;
      const result = await interpret(contentReconcilerHandler.status({ sourceKind: "page", sourceId: "program.md" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "status_program" -> ok', async () => {
      if (typeof contentReconcilerHandler.status !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_program = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_program?.output ?? {}));
      const _fixtureInput = { sourceKind: "page", sourceId: "program.md" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(contentReconcilerHandler.status({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "status_missing" -> notfound', async () => {
      if (typeof contentReconcilerHandler.status !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(contentReconcilerHandler.status({ sourceKind: "page", sourceId: "nonexistent.md" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentReconcilerHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentReconcilerHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentReconcilerHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentReconcilerHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentReconcilerHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentReconcilerHandler.list !== 'function') return;
      const result = await interpret(contentReconcilerHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_after_two" -> ok', async () => {
      if (typeof contentReconcilerHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_program = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{\"projection\":\"program-to-constitution\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_program?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(contentReconcilerHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register-then-reconcile", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let registration = registerResult0.output["registration"];
      let r = registration;
      const thenResult0 = await interpret(contentReconcilerHandler.reconcile({ sourceKind: "page", sourceId: "program.md" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register-then-status", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(contentReconcilerHandler.register({ sourceKind: "page", sourceId: "program.md", targetConcept: "Constitution", mapping: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let registration = registerResult0.output["registration"];
      let r = registration;
      const thenResult0 = await interpret(contentReconcilerHandler.status({ sourceKind: "page", sourceId: "program.md" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-drift', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceId: fc.string({ minLength: 1, maxLength: 50 }), targetConcept: fc.string({ minLength: 1, maxLength: 50 }), mapping: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reconcile'), input: fc.record({ sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('plan'), input: fc.record({ sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('status'), input: fc.record({ sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = contentReconcilerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(contentReconcilerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-source', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceId: fc.string({ minLength: 1, maxLength: 50 }), targetConcept: fc.string({ minLength: 1, maxLength: 50 }), mapping: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reconcile'), input: fc.record({ sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('plan'), input: fc.record({ sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('status'), input: fc.record({ sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = contentReconcilerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(contentReconcilerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-source
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('reconcile handles empty input: ', async () => {
      if (typeof contentReconcilerHandler.reconcile !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(contentReconcilerHandler.reconcile({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('plan handles empty input: ', async () => {
      if (typeof contentReconcilerHandler.plan !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(contentReconcilerHandler.plan({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

});
