// GenerationProvenance Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { generationProvenanceHandler } from '../../handlers/ts/score/generation-provenance.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('GenerationProvenance functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('record', () => {
    it('builds a valid StorageProgram', () => {
      const program = generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof generationProvenanceHandler.record !== 'function') return;
      const result = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "record_handler" -> ok', async () => {
      if (typeof generationProvenanceHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "record_empty_output" -> error', async () => {
      if (typeof generationProvenanceHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(generationProvenanceHandler.record({ outputFile: "", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('getByFile', () => {
    it('builds a valid StorageProgram', () => {
      const program = generationProvenanceHandler.getByFile({ outputFile: "handlers/ts/article.handler.ts" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = generationProvenanceHandler.getByFile({ outputFile: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = generationProvenanceHandler.getByFile({ outputFile: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = generationProvenanceHandler.getByFile({ outputFile: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = generationProvenanceHandler.getByFile({ outputFile: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof generationProvenanceHandler.getByFile !== 'function') return;
      const result = await interpret(generationProvenanceHandler.getByFile({ outputFile: "handlers/ts/article.handler.ts" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_known_file" -> ok', async () => {
      if (typeof generationProvenanceHandler.getByFile !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { outputFile: "handlers/ts/article.handler.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(generationProvenanceHandler.getByFile({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_handwritten_file" -> error', async () => {
      if (typeof generationProvenanceHandler.getByFile !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(generationProvenanceHandler.getByFile({ outputFile: "src/manual-code.ts" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByGenerator', () => {
    it('builds a valid StorageProgram', () => {
      const program = generationProvenanceHandler.findByGenerator({ generator: "HandlerScaffoldGen" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = generationProvenanceHandler.findByGenerator({ generator: "HandlerScaffoldGen" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = generationProvenanceHandler.findByGenerator({ generator: "HandlerScaffoldGen" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = generationProvenanceHandler.findByGenerator({ generator: "HandlerScaffoldGen" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = generationProvenanceHandler.findByGenerator({ generator: "HandlerScaffoldGen" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof generationProvenanceHandler.findByGenerator !== 'function') return;
      const result = await interpret(generationProvenanceHandler.findByGenerator({ generator: "HandlerScaffoldGen" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_scaffold_gen_files" -> ok', async () => {
      if (typeof generationProvenanceHandler.findByGenerator !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { generator: "HandlerScaffoldGen" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(generationProvenanceHandler.findByGenerator({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unknown_gen" -> error', async () => {
      if (typeof generationProvenanceHandler.findByGenerator !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { generator: "NonexistentGen" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(generationProvenanceHandler.findByGenerator({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findBySource', () => {
    it('builds a valid StorageProgram', () => {
      const program = generationProvenanceHandler.findBySource({ sourceSpec: "specs/app/article.concept" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = generationProvenanceHandler.findBySource({ sourceSpec: "specs/app/article.concept" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = generationProvenanceHandler.findBySource({ sourceSpec: "specs/app/article.concept" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = generationProvenanceHandler.findBySource({ sourceSpec: "specs/app/article.concept" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = generationProvenanceHandler.findBySource({ sourceSpec: "specs/app/article.concept" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof generationProvenanceHandler.findBySource !== 'function') return;
      const result = await interpret(generationProvenanceHandler.findBySource({ sourceSpec: "specs/app/article.concept" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_article_outputs" -> ok', async () => {
      if (typeof generationProvenanceHandler.findBySource !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { sourceSpec: "specs/app/article.concept" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(generationProvenanceHandler.findBySource({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_no_outputs" -> error', async () => {
      if (typeof generationProvenanceHandler.findBySource !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { sourceSpec: "specs/nonexistent.concept" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(generationProvenanceHandler.findBySource({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('generationChain', () => {
    it('builds a valid StorageProgram', () => {
      const program = generationProvenanceHandler.generationChain({ outputFile: "handlers/ts/article.handler.ts" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = generationProvenanceHandler.generationChain({ outputFile: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = generationProvenanceHandler.generationChain({ outputFile: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = generationProvenanceHandler.generationChain({ outputFile: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = generationProvenanceHandler.generationChain({ outputFile: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof generationProvenanceHandler.generationChain !== 'function') return;
      const result = await interpret(generationProvenanceHandler.generationChain({ outputFile: "handlers/ts/article.handler.ts" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "chain_handler" -> ok', async () => {
      if (typeof generationProvenanceHandler.generationChain !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { outputFile: "handlers/ts/article.handler.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(generationProvenanceHandler.generationChain({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "chain_handwritten" -> error', async () => {
      if (typeof generationProvenanceHandler.generationChain !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(generationProvenanceHandler.generationChain({ outputFile: "src/manual-code.ts" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('staleFiles', () => {
    it('builds a valid StorageProgram', () => {
      const program = generationProvenanceHandler.staleFiles({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = generationProvenanceHandler.staleFiles({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = generationProvenanceHandler.staleFiles({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = generationProvenanceHandler.staleFiles({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = generationProvenanceHandler.staleFiles({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof generationProvenanceHandler.staleFiles !== 'function') return;
      const result = await interpret(generationProvenanceHandler.staleFiles({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof generationProvenanceHandler.staleFiles !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(generationProvenanceHandler.staleFiles({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "stale_check_failure" -> error', async () => {
      if (typeof generationProvenanceHandler.staleFiles !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(generationProvenanceHandler.staleFiles({  }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('impactOfGeneratorChange', () => {
    it('builds a valid StorageProgram', () => {
      const program = generationProvenanceHandler.impactOfGeneratorChange({ generator: "HandlerScaffoldGen" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = generationProvenanceHandler.impactOfGeneratorChange({ generator: "HandlerScaffoldGen" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = generationProvenanceHandler.impactOfGeneratorChange({ generator: "HandlerScaffoldGen" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = generationProvenanceHandler.impactOfGeneratorChange({ generator: "HandlerScaffoldGen" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = generationProvenanceHandler.impactOfGeneratorChange({ generator: "HandlerScaffoldGen" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof generationProvenanceHandler.impactOfGeneratorChange !== 'function') return;
      const result = await interpret(generationProvenanceHandler.impactOfGeneratorChange({ generator: "HandlerScaffoldGen" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "impact_scaffold_change" -> ok', async () => {
      if (typeof generationProvenanceHandler.impactOfGeneratorChange !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { generator: "HandlerScaffoldGen" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(generationProvenanceHandler.impactOfGeneratorChange({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "impact_unknown_gen" -> error', async () => {
      if (typeof generationProvenanceHandler.impactOfGeneratorChange !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { generator: "NonexistentGen" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(generationProvenanceHandler.impactOfGeneratorChange({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('isGenerated', () => {
    it('builds a valid StorageProgram', () => {
      const program = generationProvenanceHandler.isGenerated({ file: "handlers/ts/article.handler.ts" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = generationProvenanceHandler.isGenerated({ file: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = generationProvenanceHandler.isGenerated({ file: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = generationProvenanceHandler.isGenerated({ file: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = generationProvenanceHandler.isGenerated({ file: "handlers/ts/article.handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof generationProvenanceHandler.isGenerated !== 'function') return;
      const result = await interpret(generationProvenanceHandler.isGenerated({ file: "handlers/ts/article.handler.ts" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "check_generated_file" -> ok', async () => {
      if (typeof generationProvenanceHandler.isGenerated !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_handler = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_handler?.output ?? {}));
      const _fixtureInput = { file: "handlers/ts/article.handler.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(generationProvenanceHandler.isGenerated({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_handwritten_file" -> error', async () => {
      if (typeof generationProvenanceHandler.isGenerated !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(generationProvenanceHandler.isGenerated({ file: "src/manual-code.ts" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof generationProvenanceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = generationProvenanceHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('GenerationProvenance');
    });
  });

  describe('invariant examples', () => {
    it("recorded entry is retrievable", async () => {
      const storage = createInMemoryStorage();
      const recordResult0 = await interpret(generationProvenanceHandler.record({ outputFile: "handlers/ts/article.handler.ts", generator: "HandlerScaffoldGen", sourceSpec: "specs/app/article.concept", sourceSpecKind: "concept", config: "{}" }), storage);
      expect(recordResult0.variant).toBe("ok");
      let provenance = recordResult0.output["provenance"];
      let g = provenance;
      const thenResult0 = await interpret(generationProvenanceHandler.getByFile({ outputFile: "handlers/ts/article.handler.ts" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every records entry has outputFile', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('record'), input: fc.record({ outputFile: fc.string({ minLength: 1, maxLength: 50 }), generator: fc.string({ minLength: 1, maxLength: 50 }), sourceSpec: fc.string({ minLength: 1, maxLength: 50 }), sourceSpecKind: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getByFile'), input: fc.record({ outputFile: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByGenerator'), input: fc.record({ generator: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBySource'), input: fc.record({ sourceSpec: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('generationChain'), input: fc.record({ outputFile: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('staleFiles'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('impactOfGeneratorChange'), input: fc.record({ generator: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('isGenerated'), input: fc.record({ file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = generationProvenanceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(generationProvenanceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty outputFile in records', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('record'), input: fc.record({ outputFile: fc.string({ minLength: 1, maxLength: 50 }), generator: fc.string({ minLength: 1, maxLength: 50 }), sourceSpec: fc.string({ minLength: 1, maxLength: 50 }), sourceSpecKind: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getByFile'), input: fc.record({ outputFile: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByGenerator'), input: fc.record({ generator: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBySource'), input: fc.record({ sourceSpec: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('generationChain'), input: fc.record({ outputFile: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('staleFiles'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('impactOfGeneratorChange'), input: fc.record({ generator: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('isGenerated'), input: fc.record({ file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = generationProvenanceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(generationProvenanceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty outputFile in records
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('record handles empty input: ', async () => {
      if (typeof generationProvenanceHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(generationProvenanceHandler.record({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('record ensures on ok: ', async () => {
      if (typeof generationProvenanceHandler.record !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ outputFile: fc.string({ minLength: 1, maxLength: 50 }), generator: fc.string({ minLength: 1, maxLength: 50 }), sourceSpec: fc.string({ minLength: 1, maxLength: 50 }), sourceSpecKind: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = generationProvenanceHandler.record(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
