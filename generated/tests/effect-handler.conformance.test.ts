// EffectHandler Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { effectHandlerHandler } from '../../handlers/ts/monadic/effect-handler.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('EffectHandler functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = effectHandlerHandler.register({ protocol: "http", operation: "GET" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = effectHandlerHandler.register({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = effectHandlerHandler.register({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = effectHandlerHandler.register({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = effectHandlerHandler.register({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof effectHandlerHandler.register !== 'function') return;
      const result = await interpret(effectHandlerHandler.register({ protocol: "http", operation: "GET" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_http_get" -> ok', async () => {
      if (typeof effectHandlerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(effectHandlerHandler.register({ protocol: "http", operation: "GET" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_protocol" -> error', async () => {
      if (typeof effectHandlerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(effectHandlerHandler.register({ protocol: "", operation: "GET" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = effectHandlerHandler.resolve({ protocol: "http", operation: "GET" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = effectHandlerHandler.resolve({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = effectHandlerHandler.resolve({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = effectHandlerHandler.resolve({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = effectHandlerHandler.resolve({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof effectHandlerHandler.resolve !== 'function') return;
      const result = await interpret(effectHandlerHandler.resolve({ protocol: "http", operation: "GET" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_http_get" -> ok', async () => {
      if (typeof effectHandlerHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_http_get = await interpret(effectHandlerHandler.register({ protocol: "http", operation: "GET" }), storage);
      const _pool = Object.assign({}, (afterResult_register_http_get?.output ?? {}));
      const _fixtureInput = { protocol: "http", operation: "GET" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(effectHandlerHandler.resolve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_unknown" -> error', async () => {
      if (typeof effectHandlerHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(effectHandlerHandler.resolve({ protocol: "grpc", operation: "invoke" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listByProtocol', () => {
    it('builds a valid StorageProgram', () => {
      const program = effectHandlerHandler.listByProtocol({ protocol: "http" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = effectHandlerHandler.listByProtocol({ protocol: "http" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = effectHandlerHandler.listByProtocol({ protocol: "http" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = effectHandlerHandler.listByProtocol({ protocol: "http" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = effectHandlerHandler.listByProtocol({ protocol: "http" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof effectHandlerHandler.listByProtocol !== 'function') return;
      const result = await interpret(effectHandlerHandler.listByProtocol({ protocol: "http" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_http" -> ok', async () => {
      if (typeof effectHandlerHandler.listByProtocol !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_http_get = await interpret(effectHandlerHandler.register({ protocol: "http", operation: "GET" }), storage);
      const _pool = Object.assign({}, (afterResult_register_http_get?.output ?? {}));
      const _fixtureInput = { protocol: "http" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(effectHandlerHandler.listByProtocol({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_empty" -> error', async () => {
      if (typeof effectHandlerHandler.listByProtocol !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(effectHandlerHandler.listByProtocol({ protocol: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('deregister', () => {
    it('builds a valid StorageProgram', () => {
      const program = effectHandlerHandler.deregister({ protocol: "http", operation: "GET" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = effectHandlerHandler.deregister({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = effectHandlerHandler.deregister({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = effectHandlerHandler.deregister({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = effectHandlerHandler.deregister({ protocol: "http", operation: "GET" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof effectHandlerHandler.deregister !== 'function') return;
      const result = await interpret(effectHandlerHandler.deregister({ protocol: "http", operation: "GET" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "deregister_http_get" -> ok', async () => {
      if (typeof effectHandlerHandler.deregister !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_http_get = await interpret(effectHandlerHandler.register({ protocol: "http", operation: "GET" }), storage);
      const _pool = Object.assign({}, (afterResult_register_http_get?.output ?? {}));
      const _fixtureInput = { protocol: "http", operation: "GET" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(effectHandlerHandler.deregister({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "deregister_empty" -> error', async () => {
      if (typeof effectHandlerHandler.deregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(effectHandlerHandler.deregister({ protocol: "", operation: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then resolve returns handler", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(effectHandlerHandler.register({ protocol: "http", operation: "GET" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let handler = registerResult0.output["handler"];
      let h = handler;
      const thenResult0 = await interpret(effectHandlerHandler.resolve({ protocol: "http", operation: "GET" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("resolve unregistered pair returns unresolved", async () => {
      const storage = createInMemoryStorage();
      const resolveResult0 = await interpret(effectHandlerHandler.resolve({ protocol: "grpc", operation: "invoke" }), storage);
      expect(resolveResult0.variant).toBe("ok");
      let protocol = resolveResult0.output["protocol"];
      let p = protocol;
      let operation = resolveResult0.output["operation"];
      let o = operation;
    });

    it("deregister removes handler", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(effectHandlerHandler.register({ protocol: "http", operation: "GET" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let handler = registerResult0.output["handler"];
      let h = handler;
      const deregisterResult1 = await interpret(effectHandlerHandler.deregister({ protocol: "http", operation: "GET" }), storage);
      expect(deregisterResult1.variant).toBe("ok");
      handler = deregisterResult1.output["handler"];
      const thenResult0 = await interpret(effectHandlerHandler.resolve({ protocol: "http", operation: "GET" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate register returns existing handler", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(effectHandlerHandler.register({ protocol: "http", operation: "GET" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let handler = registerResult0.output["handler"];
      let h = handler;
      const thenResult0 = await interpret(effectHandlerHandler.register({ protocol: "http", operation: "GET" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: registered handlers have protocol and operation', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listByProtocol'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deregister'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = effectHandlerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(effectHandlerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: resolve succeeds without registration', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listByProtocol'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deregister'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = effectHandlerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(effectHandlerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: resolve succeeds without registration
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof effectHandlerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(effectHandlerHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof effectHandlerHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = effectHandlerHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('resolve handles empty input: ', async () => {
      if (typeof effectHandlerHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(effectHandlerHandler.resolve({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('resolve ensures on ok: ', async () => {
      if (typeof effectHandlerHandler.resolve !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = effectHandlerHandler.resolve(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
