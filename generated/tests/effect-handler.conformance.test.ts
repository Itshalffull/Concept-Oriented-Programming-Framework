// EffectHandler Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { effectHandlerHandler } from '../../handlers/ts/monadic/effect-handler.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('EffectHandler imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('executes without crashing', async () => {
      if (typeof effectHandlerHandler.register !== 'function') return;
      try {
        const result = await effectHandlerHandler.register({ protocol: 'test-protocol', operation: 'test-operation' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('resolve', () => {
    it('executes without crashing', async () => {
      if (typeof effectHandlerHandler.resolve !== 'function') return;
      try {
        const result = await effectHandlerHandler.resolve({ protocol: 'test-protocol', operation: 'test-operation' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('listByProtocol', () => {
    it('executes without crashing', async () => {
      if (typeof effectHandlerHandler.listByProtocol !== 'function') return;
      try {
        const result = await effectHandlerHandler.listByProtocol({ protocol: 'test-protocol' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('deregister', () => {
    it('executes without crashing', async () => {
      if (typeof effectHandlerHandler.deregister !== 'function') return;
      try {
        const result = await effectHandlerHandler.deregister({ protocol: 'test-protocol', operation: 'test-operation' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("register then resolve returns handler", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await effectHandlerHandler.register({ protocol: {"type":"literal","value":"http"}, operation: {"type":"literal","value":"GET"} }, storage);
      expect(registerResult0.variant).toBe("ok");
      const handler = registerResult0.output["handler"];
      const thenResult0 = await effectHandlerHandler.resolve({ protocol: {"type":"literal","value":"http"}, operation: {"type":"literal","value":"GET"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("resolve unregistered pair returns unresolved", async () => {
      const storage = createInMemoryStorage();
      const resolveResult0 = await effectHandlerHandler.resolve({ protocol: {"type":"literal","value":"grpc"}, operation: {"type":"literal","value":"invoke"} }, storage);
      expect(resolveResult0.variant).toBe("unresolved");
      const protocol = resolveResult0.output["protocol"];
      const operation = resolveResult0.output["operation"];
    });

    it("deregister removes handler", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await effectHandlerHandler.register({ protocol: {"type":"literal","value":"http"}, operation: {"type":"literal","value":"GET"} }, storage);
      expect(registerResult0.variant).toBe("ok");
      const handler = registerResult0.output["handler"];
      const deregisterResult1 = await effectHandlerHandler.deregister({ protocol: {"type":"literal","value":"http"}, operation: {"type":"literal","value":"GET"} }, storage);
      expect(deregisterResult1.variant).toBe("ok");
      const handler = deregisterResult1.output["handler"];
      const thenResult0 = await effectHandlerHandler.resolve({ protocol: {"type":"literal","value":"http"}, operation: {"type":"literal","value":"GET"} }, storage);
      expect(thenResult0.variant).toBe("unresolved");
    });

    it("duplicate register returns existing handler", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await effectHandlerHandler.register({ protocol: {"type":"literal","value":"http"}, operation: {"type":"literal","value":"GET"} }, storage);
      expect(registerResult0.variant).toBe("ok");
      const handler = registerResult0.output["handler"];
      const thenResult0 = await effectHandlerHandler.register({ protocol: {"type":"literal","value":"http"}, operation: {"type":"literal","value":"GET"} }, storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: registered handlers have protocol and operation', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 20 }), operation: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 20 }), operation: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('listByProtocol'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('deregister'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 20 }), operation: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = effectHandlerHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(effectHandlerHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: resolve succeeds without registration', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 20 }), operation: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 20 }), operation: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('listByProtocol'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('deregister'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 20 }), operation: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = effectHandlerHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(effectHandlerHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: resolve succeeds without registration
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof effectHandlerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await effectHandlerHandler.register({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('register ensures on ok: ', async () => {
      if (typeof effectHandlerHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await effectHandlerHandler.register(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('resolve handles empty input: ', async () => {
      if (typeof effectHandlerHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await effectHandlerHandler.resolve({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('resolve ensures on ok: ', async () => {
      if (typeof effectHandlerHandler.resolve !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await effectHandlerHandler.resolve(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
