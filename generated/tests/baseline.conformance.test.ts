// Baseline Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { baselineHandler } from '../../handlers/ts/quality-measurement/baseline.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Baseline functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('capture', () => {
    it('builds a valid StorageProgram', () => {
      const program = baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof baselineHandler.capture !== 'function') return;
      const result = await interpret(baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "capture_version" -> ok', async () => {
      if (typeof baselineHandler.capture !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(baselineHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}));
      const _fixtureInput = { name: "v2.1-release", strategy: "version", reference: "v2.1.0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(baselineHandler.capture({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "capture_branch" -> ok', async () => {
      if (typeof baselineHandler.capture !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(baselineHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}));
      const _fixtureInput = { name: "sprint-42", strategy: "branch", reference: "main" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(baselineHandler.capture({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "capture_manual" -> ok', async () => {
      if (typeof baselineHandler.capture !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(baselineHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}));
      const _fixtureInput = { name: "manual-check", strategy: "manual", reference: "ad-hoc" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(baselineHandler.capture({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "capture_duplicate" -> duplicate', async () => {
      if (typeof baselineHandler.capture !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(baselineHandler.list({  }), storage);
      const afterResult_capture_version = await interpret(baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}), (afterResult_capture_version?.output ?? {}));
      const _fixtureInput = { name: "v2.1-release", strategy: "version", reference: "v2.1.0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(baselineHandler.capture({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "capture_bad_strategy" -> error', async () => {
      if (typeof baselineHandler.capture !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(baselineHandler.capture({ name: "bad", strategy: "unknown", reference: "x" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('compare', () => {
    it('builds a valid StorageProgram', () => {
      const program = baselineHandler.compare({ baseline: {"type":"ref","fixture":"capture_version","field":"baseline"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = baselineHandler.compare({ baseline: {"type":"ref","fixture":"capture_version","field":"baseline"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = baselineHandler.compare({ baseline: {"type":"ref","fixture":"capture_version","field":"baseline"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = baselineHandler.compare({ baseline: {"type":"ref","fixture":"capture_version","field":"baseline"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = baselineHandler.compare({ baseline: {"type":"ref","fixture":"capture_version","field":"baseline"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof baselineHandler.compare !== 'function') return;
      const result = await interpret(baselineHandler.compare({ baseline: {"type":"ref","fixture":"capture_version","field":"baseline"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compare_ok" -> ok', async () => {
      if (typeof baselineHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(baselineHandler.list({  }), storage);
      const afterResult_capture_version = await interpret(baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" }), storage);
      const result = await interpret(baselineHandler.compare({ baseline: afterResult_capture_version?.output?.["baseline"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compare_missing" -> notFound', async () => {
      if (typeof baselineHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(baselineHandler.compare({ baseline: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('advance', () => {
    it('builds a valid StorageProgram', () => {
      const program = baselineHandler.advance({ name: "v2.1-release" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = baselineHandler.advance({ name: "v2.1-release" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = baselineHandler.advance({ name: "v2.1-release" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = baselineHandler.advance({ name: "v2.1-release" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = baselineHandler.advance({ name: "v2.1-release" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof baselineHandler.advance !== 'function') return;
      const result = await interpret(baselineHandler.advance({ name: "v2.1-release" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "advance_ok" -> ok', async () => {
      if (typeof baselineHandler.advance !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(baselineHandler.list({  }), storage);
      const afterResult_capture_version = await interpret(baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}), (afterResult_capture_version?.output ?? {}));
      const _fixtureInput = { name: "v2.1-release" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(baselineHandler.advance({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "advance_missing" -> notFound', async () => {
      if (typeof baselineHandler.advance !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(baselineHandler.advance({ name: "nonexistent-baseline" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = baselineHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = baselineHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = baselineHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = baselineHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = baselineHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof baselineHandler.list !== 'function') return;
      const result = await interpret(baselineHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof baselineHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(baselineHandler.list({  }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_after_capture" -> ok', async () => {
      if (typeof baselineHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(baselineHandler.list({  }), storage);
      const afterResult_capture_version = await interpret(baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}), (afterResult_capture_version?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(baselineHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('delete', () => {
    it('builds a valid StorageProgram', () => {
      const program = baselineHandler.delete({ name: "v2.1-release" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = baselineHandler.delete({ name: "v2.1-release" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = baselineHandler.delete({ name: "v2.1-release" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = baselineHandler.delete({ name: "v2.1-release" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = baselineHandler.delete({ name: "v2.1-release" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof baselineHandler.delete !== 'function') return;
      const result = await interpret(baselineHandler.delete({ name: "v2.1-release" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "delete_ok" -> ok', async () => {
      if (typeof baselineHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(baselineHandler.list({  }), storage);
      const afterResult_capture_version = await interpret(baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}), (afterResult_capture_version?.output ?? {}));
      const _fixtureInput = { name: "v2.1-release" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(baselineHandler.delete({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "delete_missing" -> notFound', async () => {
      if (typeof baselineHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(baselineHandler.delete({ name: "nonexistent-baseline" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof baselineHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = baselineHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Baseline');
    });
  });

  describe('invariant examples', () => {
    it("capture then compare", async () => {
      const storage = createInMemoryStorage();
      const captureResult0 = await interpret(baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" }), storage);
      expect(captureResult0.variant).toBe("ok");
      let baseline = captureResult0.output["baseline"];
      let b = baseline;
      let metricsCount = captureResult0.output["metricsCount"];
      let _ = metricsCount;
      let findingsCount = captureResult0.output["findingsCount"];
      const thenResult0 = await interpret(baselineHandler.compare({ baseline: b }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("capture then list", async () => {
      const storage = createInMemoryStorage();
      const captureResult0 = await interpret(baselineHandler.capture({ name: "sprint-42", strategy: "branch", reference: "main" }), storage);
      expect(captureResult0.variant).toBe("ok");
      let baseline = captureResult0.output["baseline"];
      let b = baseline;
      let metricsCount = captureResult0.output["metricsCount"];
      let _ = metricsCount;
      let findingsCount = captureResult0.output["findingsCount"];
      const thenResult0 = await interpret(baselineHandler.list({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("capture then advance", async () => {
      const storage = createInMemoryStorage();
      const captureResult0 = await interpret(baselineHandler.capture({ name: "v2.1-release", strategy: "version", reference: "v2.1.0" }), storage);
      expect(captureResult0.variant).toBe("ok");
      let baseline = captureResult0.output["baseline"];
      let b = baseline;
      let metricsCount = captureResult0.output["metricsCount"];
      let _ = metricsCount;
      let findingsCount = captureResult0.output["findingsCount"];
      const thenResult0 = await interpret(baselineHandler.advance({ name: "v2.1-release" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("capture then delete then list empty", async () => {
      const storage = createInMemoryStorage();
      const captureResult0 = await interpret(baselineHandler.capture({ name: "temp-baseline", strategy: "manual", reference: "ad-hoc" }), storage);
      expect(captureResult0.variant).toBe("ok");
      let baseline = captureResult0.output["baseline"];
      let b = baseline;
      let metricsCount = captureResult0.output["metricsCount"];
      let _ = metricsCount;
      let findingsCount = captureResult0.output["findingsCount"];
      const deleteResult1 = await interpret(baselineHandler.delete({ name: "temp-baseline" }), storage);
      expect(deleteResult1.variant).toBe("ok");
      let name = deleteResult1.output["name"];
      const thenResult0 = await interpret(baselineHandler.list({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('forall properties (PBT)', () => {
    it('forall: unique baseline names', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.constantFrom("alpha-release", "beta-release", "rc-1"),
          async (n) => {
            const storage = createInMemoryStorage();
            const result = await interpret(baselineHandler.capture({ name: {"type":"variable","name":"n"}, strategy: {"type":"literal","value":"version"}, reference: {"type":"literal","value":"v1.0.0"} }), storage);
            expect(result.variant).toBeDefined();
            expect(result.variant).toBe("ok");
          },
        ),
        { numRuns: 100 },
      );
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: orphan snapshots', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('capture'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), strategy: fc.string({ minLength: 1, maxLength: 50 }), reference: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ baseline: fc.string() }) }),
              fc.record({ action: fc.constant('advance'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = baselineHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(baselineHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphan snapshots
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
