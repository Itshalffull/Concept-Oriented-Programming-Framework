// SymbolRelationship Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { symbolRelationshipHandler } from '../../handlers/ts/symbol-relationship.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SymbolRelationship functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('add', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolRelationshipHandler.add !== 'function') return;
      const result = await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_add" -> ok', async () => {
      if (typeof symbolRelationshipHandler.add !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_extends" -> ok', async () => {
      if (typeof symbolRelationshipHandler.add !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(symbolRelationshipHandler.add({ source: "ts/class/AdminHandler", target: "ts/class/UserHandler", kind: "extends" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_duplicate" -> ok', async () => {
      if (typeof symbolRelationshipHandler.add !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      const result = await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('findFrom', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolRelationshipHandler.findFrom({ source: "ts/class/UserHandler", kind: "implements" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolRelationshipHandler.findFrom({ source: "ts/class/UserHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolRelationshipHandler.findFrom({ source: "ts/class/UserHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolRelationshipHandler.findFrom({ source: "ts/class/UserHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolRelationshipHandler.findFrom({ source: "ts/class/UserHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolRelationshipHandler.findFrom !== 'function') return;
      const result = await interpret(symbolRelationshipHandler.findFrom({ source: "ts/class/UserHandler", kind: "implements" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_from_source" -> ok', async () => {
      if (typeof symbolRelationshipHandler.findFrom !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      const result = await interpret(symbolRelationshipHandler.findFrom({ source: "ts/class/UserHandler", kind: "implements" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_from_all_kinds" -> ok', async () => {
      if (typeof symbolRelationshipHandler.findFrom !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      const result = await interpret(symbolRelationshipHandler.findFrom({ source: "ts/class/UserHandler", kind: "" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('findTo', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolRelationshipHandler.findTo({ target: "ts/interface/IHandler", kind: "implements" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolRelationshipHandler.findTo({ target: "ts/interface/IHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolRelationshipHandler.findTo({ target: "ts/interface/IHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolRelationshipHandler.findTo({ target: "ts/interface/IHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolRelationshipHandler.findTo({ target: "ts/interface/IHandler", kind: "implements" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolRelationshipHandler.findTo !== 'function') return;
      const result = await interpret(symbolRelationshipHandler.findTo({ target: "ts/interface/IHandler", kind: "implements" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_to_target" -> ok', async () => {
      if (typeof symbolRelationshipHandler.findTo !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      const result = await interpret(symbolRelationshipHandler.findTo({ target: "ts/interface/IHandler", kind: "implements" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_to_all_kinds" -> ok', async () => {
      if (typeof symbolRelationshipHandler.findTo !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      const result = await interpret(symbolRelationshipHandler.findTo({ target: "ts/interface/IHandler", kind: "" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('transitiveClosure', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolRelationshipHandler.transitiveClosure({ start: "ts/class/BaseHandler", kind: "extends", direction: "forward" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolRelationshipHandler.transitiveClosure({ start: "ts/class/BaseHandler", kind: "extends", direction: "forward" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolRelationshipHandler.transitiveClosure({ start: "ts/class/BaseHandler", kind: "extends", direction: "forward" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolRelationshipHandler.transitiveClosure({ start: "ts/class/BaseHandler", kind: "extends", direction: "forward" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolRelationshipHandler.transitiveClosure({ start: "ts/class/BaseHandler", kind: "extends", direction: "forward" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolRelationshipHandler.transitiveClosure !== 'function') return;
      const result = await interpret(symbolRelationshipHandler.transitiveClosure({ start: "ts/class/BaseHandler", kind: "extends", direction: "forward" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "forward_closure" -> ok', async () => {
      if (typeof symbolRelationshipHandler.transitiveClosure !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      const result = await interpret(symbolRelationshipHandler.transitiveClosure({ start: "ts/class/BaseHandler", kind: "extends", direction: "forward" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "backward_closure" -> ok', async () => {
      if (typeof symbolRelationshipHandler.transitiveClosure !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      const result = await interpret(symbolRelationshipHandler.transitiveClosure({ start: "ts/class/AdminHandler", kind: "extends", direction: "backward" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolRelationshipHandler.get({ relationship: "symbol-relationship-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolRelationshipHandler.get({ relationship: "symbol-relationship-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolRelationshipHandler.get({ relationship: "symbol-relationship-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolRelationshipHandler.get({ relationship: "symbol-relationship-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolRelationshipHandler.get({ relationship: "symbol-relationship-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolRelationshipHandler.get !== 'function') return;
      const result = await interpret(symbolRelationshipHandler.get({ relationship: "symbol-relationship-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_get" -> ok', async () => {
      if (typeof symbolRelationshipHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(symbolRelationshipHandler.add({ source: "ts/class/UserHandler", target: "ts/interface/IHandler", kind: "implements" }), storage);
      const result = await interpret(symbolRelationshipHandler.get({ relationship: "symbol-relationship-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_nonexistent" -> error', async () => {
      if (typeof symbolRelationshipHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(symbolRelationshipHandler.get({ relationship: "symbol-relationship-999" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof symbolRelationshipHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = symbolRelationshipHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('SymbolRelationship');
    });
  });

  describe('invariant examples', () => {
    it("add-then-findFrom", async () => {
      const storage = createInMemoryStorage();
      const addResult0 = await interpret(symbolRelationshipHandler.add({ source: {"type":"literal","value":"ts/class/Handler"}, target: {"type":"literal","value":"ts/interface/IHandler"}, kind: {"type":"literal","value":"implements"} }), storage);
      expect(addResult0.variant).toBe("ok");
      const relationship = addResult0.output["relationship"];
      const thenResult0 = await interpret(symbolRelationshipHandler.findFrom({ source: {"type":"literal","value":"ts/class/Handler"}, kind: {"type":"literal","value":"implements"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("add-then-add", async () => {
      const storage = createInMemoryStorage();
      const addResult0 = await interpret(symbolRelationshipHandler.add({ source: {"type":"literal","value":"ts/class/Handler"}, target: {"type":"literal","value":"ts/interface/IHandler"}, kind: {"type":"literal","value":"implements"} }), storage);
      expect(addResult0.variant).toBe("ok");
      const relationship = addResult0.output["relationship"];
      const thenResult0 = await interpret(symbolRelationshipHandler.add({ source: {"type":"literal","value":"ts/class/Handler"}, target: {"type":"literal","value":"ts/interface/IHandler"}, kind: {"type":"literal","value":"implements"} }), storage);
      expect(thenResult0.variant).toBe("alreadyExists");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-source', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('add'), input: fc.record({ source: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findFrom'), input: fc.record({ source: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findTo'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('transitiveClosure'), input: fc.record({ start: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), direction: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ relationship: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = symbolRelationshipHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(symbolRelationshipHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-target', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('add'), input: fc.record({ source: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findFrom'), input: fc.record({ source: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findTo'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('transitiveClosure'), input: fc.record({ start: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), direction: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ relationship: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = symbolRelationshipHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(symbolRelationshipHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-target
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('add handles empty input: ', async () => {
      if (typeof symbolRelationshipHandler.add !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(symbolRelationshipHandler.add({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('add ensures on ok: ', async () => {
      if (typeof symbolRelationshipHandler.add !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ source: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = symbolRelationshipHandler.add(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
