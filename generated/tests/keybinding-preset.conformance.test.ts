// KeybindingPreset Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { keybindingPresetHandler } from '../../handlers/ts/app/keybinding-preset.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('KeybindingPreset functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof keybindingPresetHandler.register !== 'function') return;
      const result = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_vim" -> ok', async () => {
      if (typeof keybindingPresetHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_vscode" -> ok', async () => {
      if (typeof keybindingPresetHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(keybindingPresetHandler.register({ preset: "vscode", name: "VS Code", description: "VS Code compatible shortcuts." }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof keybindingPresetHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}));
      const _fixtureInput = { preset: "vim", name: "Vim again", description: "Duplicate." } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('addBinding', () => {
    it('builds a valid StorageProgram', () => {
      const program = keybindingPresetHandler.addBinding({ preset: "vim", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = keybindingPresetHandler.addBinding({ preset: "vim", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = keybindingPresetHandler.addBinding({ preset: "vim", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = keybindingPresetHandler.addBinding({ preset: "vim", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = keybindingPresetHandler.addBinding({ preset: "vim", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof keybindingPresetHandler.addBinding !== 'function') return;
      const result = await interpret(keybindingPresetHandler.addBinding({ preset: "vim", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "add_bold_override" -> ok', async () => {
      if (typeof keybindingPresetHandler.addBinding !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}));
      const _fixtureInput = { preset: "vim", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.addBinding({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_save_override" -> ok', async () => {
      if (typeof keybindingPresetHandler.addBinding !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}));
      const _fixtureInput = { preset: "vim", binding: "save", chord: [{"mod":["mod"],"key":"s","code":"KeyS"}] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.addBinding({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_to_missing_preset" -> preset_not_found', async () => {
      if (typeof keybindingPresetHandler.addBinding !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(keybindingPresetHandler.addBinding({ preset: "does-not-exist", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('preset_not_found'));
    });

    it('fixture "add_chord_empty" -> duplicate_binding', async () => {
      if (typeof keybindingPresetHandler.addBinding !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}));
      const _fixtureInput = { preset: "vim", binding: "bold", chord: [] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.addBinding({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate_binding'));
    });

  });

  describe('removeBinding', () => {
    it('builds a valid StorageProgram', () => {
      const program = keybindingPresetHandler.removeBinding({ preset: "vim", binding: "bold" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = keybindingPresetHandler.removeBinding({ preset: "vim", binding: "bold" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = keybindingPresetHandler.removeBinding({ preset: "vim", binding: "bold" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = keybindingPresetHandler.removeBinding({ preset: "vim", binding: "bold" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = keybindingPresetHandler.removeBinding({ preset: "vim", binding: "bold" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof keybindingPresetHandler.removeBinding !== 'function') return;
      const result = await interpret(keybindingPresetHandler.removeBinding({ preset: "vim", binding: "bold" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_ok" -> ok', async () => {
      if (typeof keybindingPresetHandler.removeBinding !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const afterResult_add_bold_override = await interpret(keybindingPresetHandler.addBinding({ preset: "vim", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}), (afterResult_add_bold_override?.output ?? {}));
      const _fixtureInput = { preset: "vim", binding: "bold" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.removeBinding({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_from_missing_preset" -> preset_not_found', async () => {
      if (typeof keybindingPresetHandler.removeBinding !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(keybindingPresetHandler.removeBinding({ preset: "does-not-exist", binding: "bold" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('preset_not_found'));
    });

    it('fixture "remove_missing_binding" -> binding_not_in_preset', async () => {
      if (typeof keybindingPresetHandler.removeBinding !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}));
      const _fixtureInput = { preset: "vim", binding: "nonexistent-binding" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.removeBinding({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('binding_not_in_preset'));
    });

  });

  describe('listBindings', () => {
    it('builds a valid StorageProgram', () => {
      const program = keybindingPresetHandler.listBindings({ preset: "vim" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = keybindingPresetHandler.listBindings({ preset: "vim" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = keybindingPresetHandler.listBindings({ preset: "vim" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = keybindingPresetHandler.listBindings({ preset: "vim" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = keybindingPresetHandler.listBindings({ preset: "vim" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof keybindingPresetHandler.listBindings !== 'function') return;
      const result = await interpret(keybindingPresetHandler.listBindings({ preset: "vim" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_ok" -> ok', async () => {
      if (typeof keybindingPresetHandler.listBindings !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const afterResult_add_bold_override = await interpret(keybindingPresetHandler.addBinding({ preset: "vim", binding: "bold", chord: [{"mod":[],"key":"b","code":"KeyB"}] }), storage);
      const afterResult_add_save_override = await interpret(keybindingPresetHandler.addBinding({ preset: "vim", binding: "save", chord: [{"mod":["mod"],"key":"s","code":"KeyS"}] }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}), (afterResult_add_bold_override?.output ?? {}), (afterResult_add_save_override?.output ?? {}));
      const _fixtureInput = { preset: "vim" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.listBindings({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_missing" -> preset_not_found', async () => {
      if (typeof keybindingPresetHandler.listBindings !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(keybindingPresetHandler.listBindings({ preset: "does-not-exist" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('preset_not_found'));
    });

  });

  describe('activate', () => {
    it('builds a valid StorageProgram', () => {
      const program = keybindingPresetHandler.activate({ user: "user-1", preset: "vim" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = keybindingPresetHandler.activate({ user: "user-1", preset: "vim" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = keybindingPresetHandler.activate({ user: "user-1", preset: "vim" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = keybindingPresetHandler.activate({ user: "user-1", preset: "vim" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = keybindingPresetHandler.activate({ user: "user-1", preset: "vim" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof keybindingPresetHandler.activate !== 'function') return;
      const result = await interpret(keybindingPresetHandler.activate({ user: "user-1", preset: "vim" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "activate_vim" -> ok', async () => {
      if (typeof keybindingPresetHandler.activate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}));
      const _fixtureInput = { user: "user-1", preset: "vim" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.activate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "activate_vscode" -> ok', async () => {
      if (typeof keybindingPresetHandler.activate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vscode = await interpret(keybindingPresetHandler.register({ preset: "vscode", name: "VS Code", description: "VS Code compatible shortcuts." }), storage);
      const _pool = Object.assign({}, (afterResult_register_vscode?.output ?? {}));
      const _fixtureInput = { user: "user-1", preset: "vscode" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.activate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "activate_missing" -> preset_not_found', async () => {
      if (typeof keybindingPresetHandler.activate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(keybindingPresetHandler.activate({ user: "user-1", preset: "does-not-exist" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('preset_not_found'));
    });

  });

  describe('deactivate', () => {
    it('builds a valid StorageProgram', () => {
      const program = keybindingPresetHandler.deactivate({ user: "user-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = keybindingPresetHandler.deactivate({ user: "user-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = keybindingPresetHandler.deactivate({ user: "user-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = keybindingPresetHandler.deactivate({ user: "user-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = keybindingPresetHandler.deactivate({ user: "user-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof keybindingPresetHandler.deactivate !== 'function') return;
      const result = await interpret(keybindingPresetHandler.deactivate({ user: "user-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "deactivate_ok" -> ok', async () => {
      if (typeof keybindingPresetHandler.deactivate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const afterResult_activate_vim = await interpret(keybindingPresetHandler.activate({ user: "user-1", preset: "vim" }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}), (afterResult_activate_vim?.output ?? {}));
      const _fixtureInput = { user: "user-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.deactivate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "deactivate_not_active" -> not_active', async () => {
      if (typeof keybindingPresetHandler.deactivate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(keybindingPresetHandler.deactivate({ user: "user-2" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_active'));
    });

  });

  describe('getActive', () => {
    it('builds a valid StorageProgram', () => {
      const program = keybindingPresetHandler.getActive({ user: "user-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = keybindingPresetHandler.getActive({ user: "user-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = keybindingPresetHandler.getActive({ user: "user-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = keybindingPresetHandler.getActive({ user: "user-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = keybindingPresetHandler.getActive({ user: "user-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof keybindingPresetHandler.getActive !== 'function') return;
      const result = await interpret(keybindingPresetHandler.getActive({ user: "user-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_active_set" -> ok', async () => {
      if (typeof keybindingPresetHandler.getActive !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const afterResult_activate_vim = await interpret(keybindingPresetHandler.activate({ user: "user-1", preset: "vim" }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}), (afterResult_activate_vim?.output ?? {}));
      const _fixtureInput = { user: "user-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.getActive({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_active_none" -> ok', async () => {
      if (typeof keybindingPresetHandler.getActive !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_vim = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim-style motion and editing bindings." }), storage);
      const _pool = Object.assign({}, (afterResult_register_vim?.output ?? {}));
      const _fixtureInput = { user: "user-2" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(keybindingPresetHandler.getActive({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register-then-activate", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(keybindingPresetHandler.register({ preset: "vim", name: "Vim", description: "Vim preset." }), storage);
      expect(registerResult0.variant).toBe("ok");
      let preset = registerResult0.output["preset"];
      let p = preset;
      const activateResult1 = await interpret(keybindingPresetHandler.activate({ user: "u1", preset: p }), storage);
      expect(activateResult1.variant).toBe("ok");
      const thenResult0 = await interpret(keybindingPresetHandler.getActive({ user: "u1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("addBinding-then-listBindings", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(keybindingPresetHandler.register({ preset: "vscode", name: "VS Code", description: "VS Code preset." }), storage);
      expect(registerResult0.variant).toBe("ok");
      let preset = registerResult0.output["preset"];
      let q = preset;
      const addBindingResult1 = await interpret(keybindingPresetHandler.addBinding({ preset: q, binding: "bold", chord: [{"mod":["mod"],"key":"b","code":"KeyB"}] }), storage);
      expect(addBindingResult1.variant).toBe("ok");
      const addBindingResult2 = await interpret(keybindingPresetHandler.addBinding({ preset: q, binding: "save", chord: [{"mod":["mod"],"key":"s","code":"KeyS"}] }), storage);
      expect(addBindingResult2.variant).toBe("ok");
      const thenResult0 = await interpret(keybindingPresetHandler.listBindings({ preset: q }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: two presets active for same user', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ preset: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('addBinding'), input: fc.record({ preset: fc.string(), binding: fc.string(), chord: fc.string() }) }),
              fc.record({ action: fc.constant('removeBinding'), input: fc.record({ preset: fc.string(), binding: fc.string() }) }),
              fc.record({ action: fc.constant('listBindings'), input: fc.record({ preset: fc.string() }) }),
              fc.record({ action: fc.constant('activate'), input: fc.record({ user: fc.string(), preset: fc.string() }) }),
              fc.record({ action: fc.constant('deactivate'), input: fc.record({ user: fc.string() }) }),
              fc.record({ action: fc.constant('getActive'), input: fc.record({ user: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = keybindingPresetHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(keybindingPresetHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: two presets active for same user
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: chord is non-empty in any binding entry', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ preset: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('addBinding'), input: fc.record({ preset: fc.string(), binding: fc.string(), chord: fc.string() }) }),
              fc.record({ action: fc.constant('removeBinding'), input: fc.record({ preset: fc.string(), binding: fc.string() }) }),
              fc.record({ action: fc.constant('listBindings'), input: fc.record({ preset: fc.string() }) }),
              fc.record({ action: fc.constant('activate'), input: fc.record({ user: fc.string(), preset: fc.string() }) }),
              fc.record({ action: fc.constant('deactivate'), input: fc.record({ user: fc.string() }) }),
              fc.record({ action: fc.constant('getActive'), input: fc.record({ user: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = keybindingPresetHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(keybindingPresetHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
