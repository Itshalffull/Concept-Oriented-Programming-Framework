// VoyageCodeEmbeddingProvider Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { voyageCodeEmbeddingProviderHandler } from '../../handlers/ts/voyage-code-embedding-provider.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('VoyageCodeEmbeddingProvider imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('initialize', () => {
    it('executes without crashing', async () => {
      if (typeof voyageCodeEmbeddingProviderHandler.initialize !== 'function') return;
      try {
        const result = await voyageCodeEmbeddingProviderHandler.initialize({ apiKey: 'test-apiKey', apiModel: 'test-apiModel', dimensions: 1 }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('embed', () => {
    it('executes without crashing', async () => {
      if (typeof voyageCodeEmbeddingProviderHandler.embed !== 'function') return;
      try {
        const result = await voyageCodeEmbeddingProviderHandler.embed({ text: 'test-text' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("initialize-then-embed", async () => {
      const storage = createInMemoryStorage();
      const initializeResult0 = await voyageCodeEmbeddingProviderHandler.initialize({ apiKey: {"type":"literal","value":"pa-test"}, apiModel: {"type":"literal","value":"voyage-code-3"}, dimensions: {"type":"literal","value":1024} }, storage);
      expect(initializeResult0.variant).toBe("ok");
      const instance = initializeResult0.output["instance"];
      const thenResult0 = await voyageCodeEmbeddingProviderHandler.embed({ text: {"type":"literal","value":"function add(a, b) { return a + b; }"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-providerRef', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('initialize'), input: fc.record({ apiKey: fc.string({ minLength: 1, maxLength: 20 }), apiModel: fc.string({ minLength: 1, maxLength: 20 }), dimensions: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('embed'), input: fc.record({ text: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = voyageCodeEmbeddingProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(voyageCodeEmbeddingProviderHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-modelName', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('initialize'), input: fc.record({ apiKey: fc.string({ minLength: 1, maxLength: 20 }), apiModel: fc.string({ minLength: 1, maxLength: 20 }), dimensions: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('embed'), input: fc.record({ text: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = voyageCodeEmbeddingProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(voyageCodeEmbeddingProviderHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: orphaned-modelName
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('initialize handles empty input: ', async () => {
      if (typeof voyageCodeEmbeddingProviderHandler.initialize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await voyageCodeEmbeddingProviderHandler.initialize({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('initialize ensures on ok: ', async () => {
      if (typeof voyageCodeEmbeddingProviderHandler.initialize !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ apiKey: fc.string({ minLength: 1, maxLength: 50 }), apiModel: fc.string({ minLength: 1, maxLength: 50 }), dimensions: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await voyageCodeEmbeddingProviderHandler.initialize(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
