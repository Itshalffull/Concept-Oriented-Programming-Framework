// RelationResolver Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { relationResolverHandler } from '../../handlers/ts/app/relation-resolver.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('RelationResolver functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = relationResolverHandler.resolve({ entity: "article-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = relationResolverHandler.resolve({ entity: "article-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = relationResolverHandler.resolve({ entity: "article-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = relationResolverHandler.resolve({ entity: "article-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = relationResolverHandler.resolve({ entity: "article-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof relationResolverHandler.resolve !== 'function') return;
      const result = await interpret(relationResolverHandler.resolve({ entity: "article-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_ok" -> ok', async () => {
      if (typeof relationResolverHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_get_refs_empty = await interpret(relationResolverHandler.getReferences({ entity: "isolated-entity-1" }), storage);
      const afterResult_backfill_ok = await interpret(relationResolverHandler.backfill({ schema: "Article", field: "author" }), storage);
      const afterResult_propagate_ok = await interpret(relationResolverHandler.propagate({ changedEntity: "person-42" }), storage);
      const _pool = Object.assign({}, (afterResult_get_refs_empty?.output ?? {}), (afterResult_backfill_ok?.output ?? {}), (afterResult_propagate_ok?.output ?? {}));
      const _fixtureInput = { entity: "article-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(relationResolverHandler.resolve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_no_relations" -> no_relations', async () => {
      if (typeof relationResolverHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(relationResolverHandler.resolve({ entity: "bare-node-1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('no_relations'));
    });

  });

  describe('propagate', () => {
    it('builds a valid StorageProgram', () => {
      const program = relationResolverHandler.propagate({ changedEntity: "person-42" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = relationResolverHandler.propagate({ changedEntity: "person-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = relationResolverHandler.propagate({ changedEntity: "person-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = relationResolverHandler.propagate({ changedEntity: "person-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = relationResolverHandler.propagate({ changedEntity: "person-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof relationResolverHandler.propagate !== 'function') return;
      const result = await interpret(relationResolverHandler.propagate({ changedEntity: "person-42" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "propagate_ok" -> ok', async () => {
      if (typeof relationResolverHandler.propagate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_get_refs_empty = await interpret(relationResolverHandler.getReferences({ entity: "isolated-entity-1" }), storage);
      const afterResult_backfill_ok = await interpret(relationResolverHandler.backfill({ schema: "Article", field: "author" }), storage);
      const _pool = Object.assign({}, (afterResult_get_refs_empty?.output ?? {}), (afterResult_backfill_ok?.output ?? {}));
      const _fixtureInput = { changedEntity: "person-42" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(relationResolverHandler.propagate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "propagate_no_refs" -> no_references', async () => {
      if (typeof relationResolverHandler.propagate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(relationResolverHandler.propagate({ changedEntity: "isolated-node-99" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('no_references'));
    });

  });

  describe('backfill', () => {
    it('builds a valid StorageProgram', () => {
      const program = relationResolverHandler.backfill({ schema: "Article", field: "author" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = relationResolverHandler.backfill({ schema: "Article", field: "author" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = relationResolverHandler.backfill({ schema: "Article", field: "author" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = relationResolverHandler.backfill({ schema: "Article", field: "author" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = relationResolverHandler.backfill({ schema: "Article", field: "author" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof relationResolverHandler.backfill !== 'function') return;
      const result = await interpret(relationResolverHandler.backfill({ schema: "Article", field: "author" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "backfill_ok" -> ok', async () => {
      if (typeof relationResolverHandler.backfill !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_get_refs_empty = await interpret(relationResolverHandler.getReferences({ entity: "isolated-entity-1" }), storage);
      const _pool = Object.assign({}, (afterResult_get_refs_empty?.output ?? {}));
      const _fixtureInput = { schema: "Article", field: "author" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(relationResolverHandler.backfill({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "backfill_missing_field" -> not_found', async () => {
      if (typeof relationResolverHandler.backfill !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(relationResolverHandler.backfill({ schema: "Article", field: "nonexistent-field" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

    it('fixture "backfill_missing_schema" -> not_found', async () => {
      if (typeof relationResolverHandler.backfill !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(relationResolverHandler.backfill({ schema: "NonexistentSchema", field: "author" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('getReferences', () => {
    it('builds a valid StorageProgram', () => {
      const program = relationResolverHandler.getReferences({ entity: "person-42" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = relationResolverHandler.getReferences({ entity: "person-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = relationResolverHandler.getReferences({ entity: "person-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = relationResolverHandler.getReferences({ entity: "person-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = relationResolverHandler.getReferences({ entity: "person-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof relationResolverHandler.getReferences !== 'function') return;
      const result = await interpret(relationResolverHandler.getReferences({ entity: "person-42" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_refs_ok" -> ok', async () => {
      if (typeof relationResolverHandler.getReferences !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_get_refs_empty = await interpret(relationResolverHandler.getReferences({ entity: "isolated-entity-1" }), storage);
      const afterResult_backfill_ok = await interpret(relationResolverHandler.backfill({ schema: "Article", field: "author" }), storage);
      const afterResult_propagate_ok = await interpret(relationResolverHandler.propagate({ changedEntity: "person-42" }), storage);
      const _pool = Object.assign({}, (afterResult_get_refs_empty?.output ?? {}), (afterResult_backfill_ok?.output ?? {}), (afterResult_propagate_ok?.output ?? {}));
      const _fixtureInput = { entity: "person-42" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(relationResolverHandler.getReferences({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_refs_empty" -> ok', async () => {
      if (typeof relationResolverHandler.getReferences !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(relationResolverHandler.getReferences({ entity: "isolated-entity-1" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('clearDenormalized', () => {
    it('builds a valid StorageProgram', () => {
      const program = relationResolverHandler.clearDenormalized({ schema: "Article", field: "author" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = relationResolverHandler.clearDenormalized({ schema: "Article", field: "author" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = relationResolverHandler.clearDenormalized({ schema: "Article", field: "author" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = relationResolverHandler.clearDenormalized({ schema: "Article", field: "author" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = relationResolverHandler.clearDenormalized({ schema: "Article", field: "author" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof relationResolverHandler.clearDenormalized !== 'function') return;
      const result = await interpret(relationResolverHandler.clearDenormalized({ schema: "Article", field: "author" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "clear_ok" -> ok', async () => {
      if (typeof relationResolverHandler.clearDenormalized !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_get_refs_empty = await interpret(relationResolverHandler.getReferences({ entity: "isolated-entity-1" }), storage);
      const afterResult_backfill_ok = await interpret(relationResolverHandler.backfill({ schema: "Article", field: "author" }), storage);
      const _pool = Object.assign({}, (afterResult_get_refs_empty?.output ?? {}), (afterResult_backfill_ok?.output ?? {}));
      const _fixtureInput = { schema: "Article", field: "author" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(relationResolverHandler.clearDenormalized({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "clear_missing" -> not_found', async () => {
      if (typeof relationResolverHandler.clearDenormalized !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(relationResolverHandler.clearDenormalized({ schema: "Article", field: "nonexistent-field" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof relationResolverHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = relationResolverHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('RelationResolver');
    });
  });

  describe('invariant examples', () => {
    it("resolve then get references", async () => {
      const storage = createInMemoryStorage();
      const resolveResult0 = await interpret(relationResolverHandler.resolve({ entity: "article-1" }), storage);
      expect(resolveResult0.variant).toBe("ok");
      let resolvedFields = resolveResult0.output["resolvedFields"];
      let fields = resolvedFields;
      const thenResult0 = await interpret(relationResolverHandler.getReferences({ entity: "person-42" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("backfill then clear removes denormalized data", async () => {
      const storage = createInMemoryStorage();
      const backfillResult0 = await interpret(relationResolverHandler.backfill({ schema: "Article", field: "author" }), storage);
      expect(backfillResult0.variant).toBe("ok");
      let processedCount = backfillResult0.output["processedCount"];
      let n = processedCount;
      const thenResult0 = await interpret(relationResolverHandler.clearDenormalized({ schema: "Article", field: "author" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("propagate with no references returns no_references", async () => {
      const storage = createInMemoryStorage();
      const propagateResult0 = await interpret(relationResolverHandler.propagate({ changedEntity: "isolated-node-99" }), storage);
      expect(propagateResult0.variant).toBe("no_references");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: depth is positive', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('resolve'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('propagate'), input: fc.record({ changedEntity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('backfill'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getReferences'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clearDenormalized'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = relationResolverHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(relationResolverHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: referrer and target are non-empty', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('resolve'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('propagate'), input: fc.record({ changedEntity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('backfill'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getReferences'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clearDenormalized'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = relationResolverHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(relationResolverHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphan reverse index entry', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('resolve'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('propagate'), input: fc.record({ changedEntity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('backfill'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getReferences'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clearDenormalized'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = relationResolverHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(relationResolverHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphan reverse index entry
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: zero-depth entry', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('resolve'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('propagate'), input: fc.record({ changedEntity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('backfill'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getReferences'), input: fc.record({ entity: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clearDenormalized'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = relationResolverHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(relationResolverHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: zero-depth entry
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
