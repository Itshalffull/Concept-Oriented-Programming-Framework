// Health Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { healthHandler } from '../../handlers/ts/deploy/health.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('Health functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('checkConcept', () => {
    it('builds a valid StorageProgram', () => {
      const program = healthHandler.checkConcept({ concept: 'test-concept', runtime: 'test-runtime' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = healthHandler.checkConcept({ concept: 'test-concept', runtime: 'test-runtime' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = healthHandler.checkConcept({ concept: 'test-concept', runtime: 'test-runtime' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
      expect(variants).toContain('unreachable');
      expect(variants).toContain('storageFailed');
      expect(variants).toContain('degraded');
    });

    it('declares read and write sets', () => {
      const program = healthHandler.checkConcept({ concept: 'test-concept', runtime: 'test-runtime' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = healthHandler.checkConcept({ concept: 'test-concept', runtime: 'test-runtime' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof healthHandler.checkConcept !== 'function') return;
      const result = await interpret(healthHandler.checkConcept({ concept: 'test-concept', runtime: 'test-runtime' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('checkSync', () => {
    it('builds a valid StorageProgram', () => {
      const program = healthHandler.checkSync({ sync: 'test-sync', concepts: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = healthHandler.checkSync({ sync: 'test-sync', concepts: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = healthHandler.checkSync({ sync: 'test-sync', concepts: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
      expect(variants).toContain('partialFailure');
      expect(variants).toContain('timeout');
    });

    it('declares read and write sets', () => {
      const program = healthHandler.checkSync({ sync: 'test-sync', concepts: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = healthHandler.checkSync({ sync: 'test-sync', concepts: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof healthHandler.checkSync !== 'function') return;
      const result = await interpret(healthHandler.checkSync({ sync: 'test-sync', concepts: 'test' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('checkSuite', () => {
    it('builds a valid StorageProgram', () => {
      const program = healthHandler.checkSuite({ suite: 'test-suite', environment: 'test-environment' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = healthHandler.checkSuite({ suite: 'test-suite', environment: 'test-environment' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = healthHandler.checkSuite({ suite: 'test-suite', environment: 'test-environment' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
      expect(variants).toContain('degraded');
      expect(variants).toContain('failed');
    });

    it('declares read and write sets', () => {
      const program = healthHandler.checkSuite({ suite: 'test-suite', environment: 'test-environment' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = healthHandler.checkSuite({ suite: 'test-suite', environment: 'test-environment' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof healthHandler.checkSuite !== 'function') return;
      const result = await interpret(healthHandler.checkSuite({ suite: 'test-suite', environment: 'test-environment' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('checkInvariant', () => {
    it('builds a valid StorageProgram', () => {
      const program = healthHandler.checkInvariant({ concept: 'test-concept', invariant: 'test-invariant' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = healthHandler.checkInvariant({ concept: 'test-concept', invariant: 'test-invariant' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = healthHandler.checkInvariant({ concept: 'test-concept', invariant: 'test-invariant' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
      expect(variants).toContain('violated');
    });

    it('declares read and write sets', () => {
      const program = healthHandler.checkInvariant({ concept: 'test-concept', invariant: 'test-invariant' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = healthHandler.checkInvariant({ concept: 'test-concept', invariant: 'test-invariant' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof healthHandler.checkInvariant !== 'function') return;
      const result = await interpret(healthHandler.checkInvariant({ concept: 'test-concept', invariant: 'test-invariant' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invariant examples', () => {
    it("checkConcept-then-checkSuite", async () => {
      const storage = createInMemoryStorage();
      const checkConceptResult0 = await interpret(healthHandler.checkConcept({ concept: {"type":"literal","value":"User"}, runtime: {"type":"literal","value":"server"} }), storage);
      expect(checkConceptResult0.variant).toBe("ok");
      const check = checkConceptResult0.output["check"];
      const latencyMs = checkConceptResult0.output["latencyMs"];
      const thenResult0 = await interpret(healthHandler.checkSuite({ suite: {"type":"literal","value":"auth"}, environment: {"type":"literal","value":"staging"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-target', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('checkConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 20 }), runtime: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('checkSync'), input: fc.record({ sync: fc.string({ minLength: 1, maxLength: 20 }), concepts: fc.string() }) }),
              fc.record({ action: fc.constant('checkSuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 20 }), environment: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('checkInvariant'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 20 }), invariant: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = healthHandler[step.action];
              if (typeof actionFn === 'function') {
                const program = actionFn.call(healthHandler, step.input as Record<string, unknown>);
                const result = await interpret(program, storage);
                expect(result.variant).toBeDefined();
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-kind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('checkConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 20 }), runtime: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('checkSync'), input: fc.record({ sync: fc.string({ minLength: 1, maxLength: 20 }), concepts: fc.string() }) }),
              fc.record({ action: fc.constant('checkSuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 20 }), environment: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('checkInvariant'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 20 }), invariant: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = healthHandler[step.action];
              if (typeof actionFn === 'function') {
                const program = actionFn.call(healthHandler, step.input as Record<string, unknown>);
                const result = await interpret(program, storage);
                expect(result.variant).toBeDefined();
                // Never: orphaned-kind
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('checkConcept requires: ', async () => {
      const storage = createInMemoryStorage();
      const result = await interpret(healthHandler.checkConcept({  }), storage);
      expect(['error', 'invalid', 'missing', 'notFound']).toContain(result.variant);
    });

    it('checkConcept ensures on ok: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), runtime: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = healthHandler.checkConcept(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            fc.pre(result.variant === "ok");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
