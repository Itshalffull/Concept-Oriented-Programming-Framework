// Health Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { healthHandler } from '../../handlers/ts/deploy/health.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Health functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('checkConcept', () => {
    it('builds a valid StorageProgram', () => {
      const program = healthHandler.checkConcept({ concept: "User", runtime: "server" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = healthHandler.checkConcept({ concept: "User", runtime: "server" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = healthHandler.checkConcept({ concept: "User", runtime: "server" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = healthHandler.checkConcept({ concept: "User", runtime: "server" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = healthHandler.checkConcept({ concept: "User", runtime: "server" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof healthHandler.checkConcept !== 'function') return;
      const result = await interpret(healthHandler.checkConcept({ concept: "User", runtime: "server" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "check_user" -> ok', async () => {
      if (typeof healthHandler.checkConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(healthHandler.checkConcept({ concept: "User", runtime: "server" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_payment" -> ok', async () => {
      if (typeof healthHandler.checkConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(healthHandler.checkConcept({ concept: "Payment", runtime: "worker" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_empty_concept" -> ok', async () => {
      if (typeof healthHandler.checkConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(healthHandler.checkConcept({ concept: "", runtime: "server" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('checkSync', () => {
    it('builds a valid StorageProgram', () => {
      const program = healthHandler.checkSync({ sync: "auth-session-sync", concepts: ["User","Session"] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = healthHandler.checkSync({ sync: "auth-session-sync", concepts: ["User","Session"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = healthHandler.checkSync({ sync: "auth-session-sync", concepts: ["User","Session"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = healthHandler.checkSync({ sync: "auth-session-sync", concepts: ["User","Session"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = healthHandler.checkSync({ sync: "auth-session-sync", concepts: ["User","Session"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof healthHandler.checkSync !== 'function') return;
      const result = await interpret(healthHandler.checkSync({ sync: "auth-session-sync", concepts: ["User","Session"] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "check_auth_sync" -> ok', async () => {
      if (typeof healthHandler.checkSync !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(healthHandler.checkConcept({ concept: "User", runtime: "server" }), storage);
      const result = await interpret(healthHandler.checkSync({ sync: "auth-session-sync", concepts: ["User","Session"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_sync_empty" -> ok', async () => {
      if (typeof healthHandler.checkSync !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(healthHandler.checkConcept({ concept: "User", runtime: "server" }), storage);
      const result = await interpret(healthHandler.checkSync({ sync: "", concepts: [] }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('checkSuite', () => {
    it('builds a valid StorageProgram', () => {
      const program = healthHandler.checkSuite({ suite: "auth-suite", environment: "staging" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = healthHandler.checkSuite({ suite: "auth-suite", environment: "staging" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = healthHandler.checkSuite({ suite: "auth-suite", environment: "staging" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = healthHandler.checkSuite({ suite: "auth-suite", environment: "staging" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = healthHandler.checkSuite({ suite: "auth-suite", environment: "staging" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof healthHandler.checkSuite !== 'function') return;
      const result = await interpret(healthHandler.checkSuite({ suite: "auth-suite", environment: "staging" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "check_auth_suite" -> ok', async () => {
      if (typeof healthHandler.checkSuite !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(healthHandler.checkConcept({ concept: "User", runtime: "server" }), storage);
      const result = await interpret(healthHandler.checkSuite({ suite: "auth-suite", environment: "staging" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_suite_prod" -> ok', async () => {
      if (typeof healthHandler.checkSuite !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(healthHandler.checkConcept({ concept: "User", runtime: "server" }), storage);
      const result = await interpret(healthHandler.checkSuite({ suite: "payments", environment: "production" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_suite_empty" -> ok', async () => {
      if (typeof healthHandler.checkSuite !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(healthHandler.checkConcept({ concept: "User", runtime: "server" }), storage);
      const result = await interpret(healthHandler.checkSuite({ suite: "", environment: "staging" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('checkInvariant', () => {
    it('builds a valid StorageProgram', () => {
      const program = healthHandler.checkInvariant({ concept: "User", invariant: "valid-email" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = healthHandler.checkInvariant({ concept: "User", invariant: "valid-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = healthHandler.checkInvariant({ concept: "User", invariant: "valid-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = healthHandler.checkInvariant({ concept: "User", invariant: "valid-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = healthHandler.checkInvariant({ concept: "User", invariant: "valid-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof healthHandler.checkInvariant !== 'function') return;
      const result = await interpret(healthHandler.checkInvariant({ concept: "User", invariant: "valid-email" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "check_invariant" -> ok', async () => {
      if (typeof healthHandler.checkInvariant !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(healthHandler.checkConcept({ concept: "User", runtime: "server" }), storage);
      const result = await interpret(healthHandler.checkInvariant({ concept: "User", invariant: "valid-email" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_invariant_empty" -> ok', async () => {
      if (typeof healthHandler.checkInvariant !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(healthHandler.checkConcept({ concept: "User", runtime: "server" }), storage);
      const result = await interpret(healthHandler.checkInvariant({ concept: "", invariant: "valid-email" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof healthHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = healthHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('Health');
    });
  });

  describe('invariant examples', () => {
    it("checkConcept-then-checkSuite", async () => {
      const storage = createInMemoryStorage();
      const checkConceptResult0 = await interpret(healthHandler.checkConcept({ concept: {"type":"literal","value":"User"}, runtime: {"type":"literal","value":"server"} }), storage);
      expect(checkConceptResult0.variant).toBe("ok");
      const check = checkConceptResult0.output["check"];
      const latencyMs = checkConceptResult0.output["latencyMs"];
      const thenResult0 = await interpret(healthHandler.checkSuite({ suite: {"type":"literal","value":"auth"}, environment: {"type":"literal","value":"staging"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-target', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('checkConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), runtime: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('checkSync'), input: fc.record({ sync: fc.string({ minLength: 1, maxLength: 50 }), concepts: fc.string() }) }),
              fc.record({ action: fc.constant('checkSuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 50 }), environment: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('checkInvariant'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), invariant: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = healthHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(healthHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-kind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('checkConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), runtime: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('checkSync'), input: fc.record({ sync: fc.string({ minLength: 1, maxLength: 50 }), concepts: fc.string() }) }),
              fc.record({ action: fc.constant('checkSuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 50 }), environment: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('checkInvariant'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), invariant: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = healthHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(healthHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-kind
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('checkConcept handles empty input: ', async () => {
      if (typeof healthHandler.checkConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(healthHandler.checkConcept({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('checkConcept ensures on ok: ', async () => {
      if (typeof healthHandler.checkConcept !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), runtime: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = healthHandler.checkConcept(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
