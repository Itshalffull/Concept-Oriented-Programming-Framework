// VerificationRun Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { verificationRunHandler } from '../../handlers/ts/suites/formal-verification/verification-run.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('VerificationRun functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('start', () => {
    it('builds a valid StorageProgram', () => {
      const program = verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof verificationRunHandler.start !== 'function') return;
      const result = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_start" -> ok', async () => {
      if (typeof verificationRunHandler.start !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "auto_solver" -> ok', async () => {
      if (typeof verificationRunHandler.start !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Token", properties: ["invariant-1"], solver: "auto", timeout_ms: "60000" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_properties" -> invalid', async () => {
      if (typeof verificationRunHandler.start !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: [], solver: "z3", timeout_ms: "10000" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('complete', () => {
    it('builds a valid StorageProgram', () => {
      const program = verificationRunHandler.complete({ results: "{\"prop-1\":\"proved\",\"prop-2\":\"refuted\"}", resource_usage: "{\"total_time_ms\":1200}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = verificationRunHandler.complete({ results: "{\"prop-1\":\"proved\",\"prop-2\":\"refuted\"}", resource_usage: "{\"total_time_ms\":1200}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = verificationRunHandler.complete({ results: "{\"prop-1\":\"proved\",\"prop-2\":\"refuted\"}", resource_usage: "{\"total_time_ms\":1200}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = verificationRunHandler.complete({ results: "{\"prop-1\":\"proved\",\"prop-2\":\"refuted\"}", resource_usage: "{\"total_time_ms\":1200}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = verificationRunHandler.complete({ results: "{\"prop-1\":\"proved\",\"prop-2\":\"refuted\"}", resource_usage: "{\"total_time_ms\":1200}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof verificationRunHandler.complete !== 'function') return;
      const result = await interpret(verificationRunHandler.complete({ results: "{\"prop-1\":\"proved\",\"prop-2\":\"refuted\"}", resource_usage: "{\"total_time_ms\":1200}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_complete" -> ok', async () => {
      if (typeof verificationRunHandler.complete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_start = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_start?.output ?? {}));
      const _fixtureInput = { results: "{\"prop-1\":\"proved\",\"prop-2\":\"refuted\"}", resource_usage: "{\"total_time_ms\":1200}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(verificationRunHandler.complete({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_run" -> notfound', async () => {
      if (typeof verificationRunHandler.complete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_start = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_start?.output ?? {}));
      const _fixtureInput = { id: "vr-nonexistent", results: "{}", resource_usage: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(verificationRunHandler.complete({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('timeout', () => {
    it('builds a valid StorageProgram', () => {
      const program = verificationRunHandler.timeout({ partial_results: "{\"prop-1\":\"proved\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = verificationRunHandler.timeout({ partial_results: "{\"prop-1\":\"proved\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = verificationRunHandler.timeout({ partial_results: "{\"prop-1\":\"proved\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = verificationRunHandler.timeout({ partial_results: "{\"prop-1\":\"proved\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = verificationRunHandler.timeout({ partial_results: "{\"prop-1\":\"proved\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof verificationRunHandler.timeout !== 'function') return;
      const result = await interpret(verificationRunHandler.timeout({ partial_results: "{\"prop-1\":\"proved\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_timeout" -> ok', async () => {
      if (typeof verificationRunHandler.timeout !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_start = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_start?.output ?? {}));
      const _fixtureInput = { partial_results: "{\"prop-1\":\"proved\"}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(verificationRunHandler.timeout({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_partial" -> ok', async () => {
      if (typeof verificationRunHandler.timeout !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_start = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_start?.output ?? {}));
      const _fixtureInput = { partial_results: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(verificationRunHandler.timeout({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('cancel', () => {
    it('builds a valid StorageProgram', () => {
      const program = verificationRunHandler.cancel({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = verificationRunHandler.cancel({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = verificationRunHandler.cancel({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = verificationRunHandler.cancel({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = verificationRunHandler.cancel({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof verificationRunHandler.cancel !== 'function') return;
      const result = await interpret(verificationRunHandler.cancel({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_cancel" -> ok', async () => {
      if (typeof verificationRunHandler.cancel !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_start = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_start?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(verificationRunHandler.cancel({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_cancel" -> notfound', async () => {
      if (typeof verificationRunHandler.cancel !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(verificationRunHandler.cancel({ id: "vr-nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get_status', () => {
    it('builds a valid StorageProgram', () => {
      const program = verificationRunHandler.get_status({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = verificationRunHandler.get_status({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = verificationRunHandler.get_status({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = verificationRunHandler.get_status({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = verificationRunHandler.get_status({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof verificationRunHandler.get_status !== 'function') return;
      const result = await interpret(verificationRunHandler.get_status({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_status" -> ok', async () => {
      if (typeof verificationRunHandler.get_status !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_start = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_start?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(verificationRunHandler.get_status({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_status" -> notfound', async () => {
      if (typeof verificationRunHandler.get_status !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(verificationRunHandler.get_status({ id: "vr-nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('compare', () => {
    it('builds a valid StorageProgram', () => {
      const program = verificationRunHandler.compare({ run_id_a: "vr-aaa", run_id_b: "vr-bbb" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = verificationRunHandler.compare({ run_id_a: "vr-aaa", run_id_b: "vr-bbb" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = verificationRunHandler.compare({ run_id_a: "vr-aaa", run_id_b: "vr-bbb" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = verificationRunHandler.compare({ run_id_a: "vr-aaa", run_id_b: "vr-bbb" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = verificationRunHandler.compare({ run_id_a: "vr-aaa", run_id_b: "vr-bbb" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof verificationRunHandler.compare !== 'function') return;
      const result = await interpret(verificationRunHandler.compare({ run_id_a: "vr-aaa", run_id_b: "vr-bbb" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_compare" -> ok', async () => {
      if (typeof verificationRunHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_start = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: ["prop-1","prop-2"], solver: "z3", timeout_ms: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_start?.output ?? {}));
      const _fixtureInput = { run_id_a: "vr-aaa", run_id_b: "vr-bbb" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(verificationRunHandler.compare({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_run_a" -> notfound', async () => {
      if (typeof verificationRunHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(verificationRunHandler.compare({ run_id_a: "vr-nonexistent", run_id_b: "vr-bbb" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof verificationRunHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = verificationRunHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('VerificationRun');
    });
  });

  describe('invariant examples', () => {
    it("start-then-complete", async () => {
      const storage = createInMemoryStorage();
      const startResult0 = await interpret(verificationRunHandler.start({ target_symbol: "clef/concept/Password", properties: {"type":"list","items":[{"type":"literal","value":"p1"},{"type":"literal","value":"p2"}]}, solver: "z3", timeout_ms: 10000 }), storage);
      expect(startResult0.variant).toBe("ok");
      let run = startResult0.output["run"];
      let r = run;
      const thenResult0 = await interpret(verificationRunHandler.complete({ run: r, results: "test-res", resource_usage: "test-usage" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-target_symbol', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ target_symbol: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string(), solver: fc.string({ minLength: 1, maxLength: 50 }), timeout_ms: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ run: fc.string(), results: fc.string(), resource_usage: fc.string() }) }),
              fc.record({ action: fc.constant('timeout'), input: fc.record({ run: fc.string(), partial_results: fc.string() }) }),
              fc.record({ action: fc.constant('cancel'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('get_status'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ run1: fc.string(), run2: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = verificationRunHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(verificationRunHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-properties_checked', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ target_symbol: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string(), solver: fc.string({ minLength: 1, maxLength: 50 }), timeout_ms: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ run: fc.string(), results: fc.string(), resource_usage: fc.string() }) }),
              fc.record({ action: fc.constant('timeout'), input: fc.record({ run: fc.string(), partial_results: fc.string() }) }),
              fc.record({ action: fc.constant('cancel'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('get_status'), input: fc.record({ run: fc.string() }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ run1: fc.string(), run2: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = verificationRunHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(verificationRunHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-properties_checked
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('start handles empty input: ', async () => {
      if (typeof verificationRunHandler.start !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(verificationRunHandler.start({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('start ensures on ok: ', async () => {
      if (typeof verificationRunHandler.start !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ target_symbol: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string(), solver: fc.string({ minLength: 1, maxLength: 50 }), timeout_ms: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = verificationRunHandler.start(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
