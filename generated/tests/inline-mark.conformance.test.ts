// InlineMark Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { inlineMarkHandler } from '../../handlers/ts/app/inline-mark.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('InlineMark functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('toggleMark', () => {
    it('builds a valid StorageProgram', () => {
      const program = inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof inlineMarkHandler.toggleMark !== 'function') return;
      const result = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "toggle_create" -> ok', async () => {
      if (typeof inlineMarkHandler.toggleMark !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_block_empty = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      const _pool = Object.assign({}, (afterResult_list_block_empty?.output ?? {}));
      const _fixtureInput = { block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(inlineMarkHandler.toggleMark({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "toggle_remove" -> removed', async () => {
      if (typeof inlineMarkHandler.toggleMark !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_block_empty = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      const afterResult_toggle_create = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" }), storage);
      const _pool = Object.assign({}, (afterResult_list_block_empty?.output ?? {}), (afterResult_toggle_create?.output ?? {}));
      const _fixtureInput = { block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(inlineMarkHandler.toggleMark({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('removed'));
    });

    it('fixture "toggle_invalid_kind" -> invalid_kind', async () => {
      if (typeof inlineMarkHandler.toggleMark !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "underline", attributes: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_kind'));
    });

    it('fixture "toggle_invalid_range_negative" -> invalid_range', async () => {
      if (typeof inlineMarkHandler.toggleMark !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "-1", rangeEnd: "5", kind: "italic", attributes: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_range'));
    });

    it('fixture "toggle_invalid_range_reversed" -> invalid_range', async () => {
      if (typeof inlineMarkHandler.toggleMark !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "5", rangeEnd: "2", kind: "italic", attributes: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_range'));
    });

    it('fixture "toggle_invalid_empty_block" -> invalid_range', async () => {
      if (typeof inlineMarkHandler.toggleMark !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(inlineMarkHandler.toggleMark({ block: "", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_range'));
    });

    it('fixture "toggle_link" -> ok', async () => {
      if (typeof inlineMarkHandler.toggleMark !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_block_empty = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      const _pool = Object.assign({}, (afterResult_list_block_empty?.output ?? {}));
      const _fixtureInput = { block: "block-2", rangeStart: "3", rangeEnd: "10", kind: "link", attributes: "{\"href\":\"https://example.com\"}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(inlineMarkHandler.toggleMark({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "toggle_wikilink" -> ok', async () => {
      if (typeof inlineMarkHandler.toggleMark !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_block_empty = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      const _pool = Object.assign({}, (afterResult_list_block_empty?.output ?? {}));
      const _fixtureInput = { block: "block-3", rangeStart: "0", rangeEnd: "8", kind: "wikilink", attributes: "{\"entity-ref\":\"page-abc\"}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(inlineMarkHandler.toggleMark({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('openEditor', () => {
    it('builds a valid StorageProgram', () => {
      const program = inlineMarkHandler.openEditor({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = inlineMarkHandler.openEditor({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = inlineMarkHandler.openEditor({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = inlineMarkHandler.openEditor({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = inlineMarkHandler.openEditor({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof inlineMarkHandler.openEditor !== 'function') return;
      const result = await interpret(inlineMarkHandler.openEditor({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "openEditor_ok" -> ok', async () => {
      if (typeof inlineMarkHandler.openEditor !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_block_empty = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      const afterResult_toggle_create = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" }), storage);
      const result = await interpret(inlineMarkHandler.openEditor({ mark: afterResult_toggle_create?.output?.["mark"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "openEditor_link" -> ok', async () => {
      if (typeof inlineMarkHandler.openEditor !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_block_empty = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      const afterResult_toggle_link = await interpret(inlineMarkHandler.toggleMark({ block: "block-2", rangeStart: "3", rangeEnd: "10", kind: "link", attributes: "{\"href\":\"https://example.com\"}" }), storage);
      const result = await interpret(inlineMarkHandler.openEditor({ mark: afterResult_toggle_link?.output?.["mark"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "openEditor_missing" -> notfound', async () => {
      if (typeof inlineMarkHandler.openEditor !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(inlineMarkHandler.openEditor({ mark: "nonexistent-mark" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('listForBlock', () => {
    it('builds a valid StorageProgram', () => {
      const program = inlineMarkHandler.listForBlock({ block: "block-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = inlineMarkHandler.listForBlock({ block: "block-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = inlineMarkHandler.listForBlock({ block: "block-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = inlineMarkHandler.listForBlock({ block: "block-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = inlineMarkHandler.listForBlock({ block: "block-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof inlineMarkHandler.listForBlock !== 'function') return;
      const result = await interpret(inlineMarkHandler.listForBlock({ block: "block-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_block_ok" -> ok', async () => {
      if (typeof inlineMarkHandler.listForBlock !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_block_empty = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      const afterResult_toggle_create = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" }), storage);
      const _pool = Object.assign({}, (afterResult_list_block_empty?.output ?? {}), (afterResult_toggle_create?.output ?? {}));
      const _fixtureInput = { block: "block-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(inlineMarkHandler.listForBlock({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_block_empty" -> ok', async () => {
      if (typeof inlineMarkHandler.listForBlock !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('removeMark', () => {
    it('builds a valid StorageProgram', () => {
      const program = inlineMarkHandler.removeMark({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = inlineMarkHandler.removeMark({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = inlineMarkHandler.removeMark({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = inlineMarkHandler.removeMark({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = inlineMarkHandler.removeMark({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof inlineMarkHandler.removeMark !== 'function') return;
      const result = await interpret(inlineMarkHandler.removeMark({ mark: {"type":"ref","fixture":"toggle_create","field":"mark"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "removeMark_ok" -> ok', async () => {
      if (typeof inlineMarkHandler.removeMark !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_block_empty = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      const afterResult_toggle_create = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: "0", rangeEnd: "5", kind: "bold", attributes: "" }), storage);
      const result = await interpret(inlineMarkHandler.removeMark({ mark: afterResult_toggle_create?.output?.["mark"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "removeMark_notfound" -> notfound', async () => {
      if (typeof inlineMarkHandler.removeMark !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(inlineMarkHandler.removeMark({ mark: "nonexistent-mark" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('updateAttributes', () => {
    it('builds a valid StorageProgram', () => {
      const program = inlineMarkHandler.updateAttributes({ mark: {"type":"ref","fixture":"toggle_link","field":"mark"}, attributes: "{\"href\":\"https://updated.example.com\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = inlineMarkHandler.updateAttributes({ mark: {"type":"ref","fixture":"toggle_link","field":"mark"}, attributes: "{\"href\":\"https://updated.example.com\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = inlineMarkHandler.updateAttributes({ mark: {"type":"ref","fixture":"toggle_link","field":"mark"}, attributes: "{\"href\":\"https://updated.example.com\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = inlineMarkHandler.updateAttributes({ mark: {"type":"ref","fixture":"toggle_link","field":"mark"}, attributes: "{\"href\":\"https://updated.example.com\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = inlineMarkHandler.updateAttributes({ mark: {"type":"ref","fixture":"toggle_link","field":"mark"}, attributes: "{\"href\":\"https://updated.example.com\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof inlineMarkHandler.updateAttributes !== 'function') return;
      const result = await interpret(inlineMarkHandler.updateAttributes({ mark: {"type":"ref","fixture":"toggle_link","field":"mark"}, attributes: "{\"href\":\"https://updated.example.com\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "updateAttributes_ok" -> ok', async () => {
      if (typeof inlineMarkHandler.updateAttributes !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_block_empty = await interpret(inlineMarkHandler.listForBlock({ block: "block-999" }), storage);
      const afterResult_toggle_link = await interpret(inlineMarkHandler.toggleMark({ block: "block-2", rangeStart: "3", rangeEnd: "10", kind: "link", attributes: "{\"href\":\"https://example.com\"}" }), storage);
      const result = await interpret(inlineMarkHandler.updateAttributes({ mark: afterResult_toggle_link?.output?.["mark"], attributes: "{\"href\":\"https://updated.example.com\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "updateAttributes_notfound" -> notfound', async () => {
      if (typeof inlineMarkHandler.updateAttributes !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(inlineMarkHandler.updateAttributes({ mark: "nonexistent-mark", attributes: "{\"href\":\"https://example.com\"}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof inlineMarkHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = inlineMarkHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('InlineMark');
    });
  });

  describe('invariant examples', () => {
    it("toggle-create then toggle-remove returns to original state", async () => {
      const storage = createInMemoryStorage();
      const toggleMarkResult0 = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: 0, rangeEnd: 4, kind: "bold", attributes: "" }), storage);
      expect(toggleMarkResult0.variant).toBe("ok");
      let mark = toggleMarkResult0.output["mark"];
      let m = mark;
      const thenResult0 = await interpret(inlineMarkHandler.toggleMark({ block: "block-1", rangeStart: 0, rangeEnd: 4, kind: "bold", attributes: "" }), storage);
      expect(thenResult0.variant).toBe("removed");
    });

    it("toggle-create then listForBlock", async () => {
      const storage = createInMemoryStorage();
      const toggleMarkResult0 = await interpret(inlineMarkHandler.toggleMark({ block: "doc-block-1", rangeStart: 2, rangeEnd: 9, kind: "italic", attributes: "" }), storage);
      expect(toggleMarkResult0.variant).toBe("ok");
      let mark = toggleMarkResult0.output["mark"];
      let m = mark;
      const thenResult0 = await interpret(inlineMarkHandler.listForBlock({ block: "doc-block-1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("toggle-create then updateAttributes", async () => {
      const storage = createInMemoryStorage();
      const toggleMarkResult0 = await interpret(inlineMarkHandler.toggleMark({ block: "block-link", rangeStart: 0, rangeEnd: 6, kind: "link", attributes: "{\"href\":\"https://a.com\"}" }), storage);
      expect(toggleMarkResult0.variant).toBe("ok");
      let mark = toggleMarkResult0.output["mark"];
      let m = mark;
      const thenResult0 = await interpret(inlineMarkHandler.updateAttributes({ mark: m, attributes: "{\"href\":\"https://b.com\"}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: range invariant', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('toggleMark'), input: fc.record({ block: fc.string({ minLength: 1, maxLength: 50 }), rangeStart: fc.integer({ min: 1, max: 1000 }), rangeEnd: fc.integer({ min: 1, max: 1000 }), kind: fc.string({ minLength: 1, maxLength: 50 }), attributes: fc.string() }) }),
              fc.record({ action: fc.constant('openEditor'), input: fc.record({ mark: fc.string() }) }),
              fc.record({ action: fc.constant('listForBlock'), input: fc.record({ block: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeMark'), input: fc.record({ mark: fc.string() }) }),
              fc.record({ action: fc.constant('updateAttributes'), input: fc.record({ mark: fc.string(), attributes: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = inlineMarkHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(inlineMarkHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: range non-negative', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('toggleMark'), input: fc.record({ block: fc.string({ minLength: 1, maxLength: 50 }), rangeStart: fc.integer({ min: 1, max: 1000 }), rangeEnd: fc.integer({ min: 1, max: 1000 }), kind: fc.string({ minLength: 1, maxLength: 50 }), attributes: fc.string() }) }),
              fc.record({ action: fc.constant('openEditor'), input: fc.record({ mark: fc.string() }) }),
              fc.record({ action: fc.constant('listForBlock'), input: fc.record({ block: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeMark'), input: fc.record({ mark: fc.string() }) }),
              fc.record({ action: fc.constant('updateAttributes'), input: fc.record({ mark: fc.string(), attributes: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = inlineMarkHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(inlineMarkHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: kind allowlist', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('toggleMark'), input: fc.record({ block: fc.string({ minLength: 1, maxLength: 50 }), rangeStart: fc.integer({ min: 1, max: 1000 }), rangeEnd: fc.integer({ min: 1, max: 1000 }), kind: fc.string({ minLength: 1, maxLength: 50 }), attributes: fc.string() }) }),
              fc.record({ action: fc.constant('openEditor'), input: fc.record({ mark: fc.string() }) }),
              fc.record({ action: fc.constant('listForBlock'), input: fc.record({ block: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeMark'), input: fc.record({ mark: fc.string() }) }),
              fc.record({ action: fc.constant('updateAttributes'), input: fc.record({ mark: fc.string(), attributes: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = inlineMarkHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(inlineMarkHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: duplicate mark on same block-range-kind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('toggleMark'), input: fc.record({ block: fc.string({ minLength: 1, maxLength: 50 }), rangeStart: fc.integer({ min: 1, max: 1000 }), rangeEnd: fc.integer({ min: 1, max: 1000 }), kind: fc.string({ minLength: 1, maxLength: 50 }), attributes: fc.string() }) }),
              fc.record({ action: fc.constant('openEditor'), input: fc.record({ mark: fc.string() }) }),
              fc.record({ action: fc.constant('listForBlock'), input: fc.record({ block: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeMark'), input: fc.record({ mark: fc.string() }) }),
              fc.record({ action: fc.constant('updateAttributes'), input: fc.record({ mark: fc.string(), attributes: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = inlineMarkHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(inlineMarkHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: duplicate mark on same block-range-kind
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
