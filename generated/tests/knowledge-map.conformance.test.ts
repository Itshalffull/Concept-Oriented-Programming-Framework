// KnowledgeMap Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { knowledgeMapHandler } from '../../handlers/ts/quality-analysis/knowledge-map.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('KnowledgeMap functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('analyze', () => {
    it('builds a valid StorageProgram', () => {
      const program = knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof knowledgeMapHandler.analyze !== 'function') return;
      const result = await interpret(knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "analyze_all" -> ok', async () => {
      if (typeof knowledgeMapHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "analyze_targets" -> ok', async () => {
      if (typeof knowledgeMapHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(knowledgeMapHandler.analyze({ targets: ["payments/stripe.ts","auth/login.ts"], period: "12m" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "analyze_defaults" -> ok', async () => {
      if (typeof knowledgeMapHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(knowledgeMapHandler.analyze({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('experts', () => {
    it('builds a valid StorageProgram', () => {
      const program = knowledgeMapHandler.experts({ target: "payments/stripe.ts" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = knowledgeMapHandler.experts({ target: "payments/stripe.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = knowledgeMapHandler.experts({ target: "payments/stripe.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = knowledgeMapHandler.experts({ target: "payments/stripe.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = knowledgeMapHandler.experts({ target: "payments/stripe.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof knowledgeMapHandler.experts !== 'function') return;
      const result = await interpret(knowledgeMapHandler.experts({ target: "payments/stripe.ts" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "experts_existing" -> ok', async () => {
      if (typeof knowledgeMapHandler.experts !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_targets = await interpret(knowledgeMapHandler.analyze({ targets: ["payments/stripe.ts","auth/login.ts"], period: "12m" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_targets?.output ?? {}));
      const _fixtureInput = { target: "payments/stripe.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(knowledgeMapHandler.experts({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "experts_missing" -> notTracked', async () => {
      if (typeof knowledgeMapHandler.experts !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(knowledgeMapHandler.experts({ target: "nonexistent/file.ts" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notTracked'));
    });

  });

  describe('risks', () => {
    it('builds a valid StorageProgram', () => {
      const program = knowledgeMapHandler.risks({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = knowledgeMapHandler.risks({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = knowledgeMapHandler.risks({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = knowledgeMapHandler.risks({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = knowledgeMapHandler.risks({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof knowledgeMapHandler.risks !== 'function') return;
      const result = await interpret(knowledgeMapHandler.risks({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "risks_default" -> ok', async () => {
      if (typeof knowledgeMapHandler.risks !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_all = await interpret(knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_all?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(knowledgeMapHandler.risks({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "risks_threshold" -> ok', async () => {
      if (typeof knowledgeMapHandler.risks !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_all = await interpret(knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_all?.output ?? {}));
      const _fixtureInput = { minBusFactor: "2" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(knowledgeMapHandler.risks({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('teamOverview', () => {
    it('builds a valid StorageProgram', () => {
      const program = knowledgeMapHandler.teamOverview({ team: ["alice","bob","carol"] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = knowledgeMapHandler.teamOverview({ team: ["alice","bob","carol"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = knowledgeMapHandler.teamOverview({ team: ["alice","bob","carol"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = knowledgeMapHandler.teamOverview({ team: ["alice","bob","carol"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = knowledgeMapHandler.teamOverview({ team: ["alice","bob","carol"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof knowledgeMapHandler.teamOverview !== 'function') return;
      const result = await interpret(knowledgeMapHandler.teamOverview({ team: ["alice","bob","carol"] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "team_overview" -> ok', async () => {
      if (typeof knowledgeMapHandler.teamOverview !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_all = await interpret(knowledgeMapHandler.analyze({ period: "12m", activeThreshold: "6m" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_all?.output ?? {}));
      const _fixtureInput = { team: ["alice","bob","carol"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(knowledgeMapHandler.teamOverview({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof knowledgeMapHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = knowledgeMapHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('KnowledgeMap');
    });
  });

  describe('invariant examples', () => {
    it("analyze then experts", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(knowledgeMapHandler.analyze({ targets: ["payments/stripe.ts"], period: "12m" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let entries = analyzeResult0.output["entries"];
      const thenResult0 = await interpret(knowledgeMapHandler.experts({ target: "payments/stripe.ts" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("analyze then risks", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(knowledgeMapHandler.analyze({ period: "12m" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let entries = analyzeResult0.output["entries"];
      let _ = entries;
      const thenResult0 = await interpret(knowledgeMapHandler.risks({ minBusFactor: 1 }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("analyze then team overview", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(knowledgeMapHandler.analyze({ period: "12m" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let entries = analyzeResult0.output["entries"];
      let _ = entries;
      const thenResult0 = await interpret(knowledgeMapHandler.teamOverview({ team: ["alice","bob"] }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

});
