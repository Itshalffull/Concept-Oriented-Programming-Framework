// ControlState Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { controlStateHandler } from '../../handlers/ts/app/control-state.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ControlState functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof controlStateHandler.create !== 'function') return;
      const result = await interpret(controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_slider" -> ok', async () => {
      if (typeof controlStateHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_select" -> ok', async () => {
      if (typeof controlStateHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(controlStateHandler.create({ control: "theme-select", controlType: "select", defaultValue: "light", options: "[\"light\",\"dark\",\"system\"]", personal: "true" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_toggle" -> ok', async () => {
      if (typeof controlStateHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(controlStateHandler.create({ control: "show-grid", controlType: "toggle", defaultValue: "false", options: null, personal: "false" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_date" -> ok', async () => {
      if (typeof controlStateHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(controlStateHandler.create({ control: "range-start", controlType: "date", defaultValue: "2024-01-01", options: null, personal: "false" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_duplicate" -> duplicate', async () => {
      if (typeof controlStateHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_slider = await interpret(controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" }), storage);
      const _pool = Object.assign({}, (afterResult_create_slider?.output ?? {}));
      const _fixtureInput = { control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(controlStateHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('set', () => {
    it('builds a valid StorageProgram', () => {
      const program = controlStateHandler.set({ control: {"type":"ref","fixture":"create_slider","field":"control"}, value: "75" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = controlStateHandler.set({ control: {"type":"ref","fixture":"create_slider","field":"control"}, value: "75" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = controlStateHandler.set({ control: {"type":"ref","fixture":"create_slider","field":"control"}, value: "75" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = controlStateHandler.set({ control: {"type":"ref","fixture":"create_slider","field":"control"}, value: "75" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = controlStateHandler.set({ control: {"type":"ref","fixture":"create_slider","field":"control"}, value: "75" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof controlStateHandler.set !== 'function') return;
      const result = await interpret(controlStateHandler.set({ control: {"type":"ref","fixture":"create_slider","field":"control"}, value: "75" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_ok" -> ok', async () => {
      if (typeof controlStateHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_slider = await interpret(controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" }), storage);
      const result = await interpret(controlStateHandler.set({ control: afterResult_create_slider?.output?.["control"], value: "75" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_select_ok" -> ok', async () => {
      if (typeof controlStateHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_select = await interpret(controlStateHandler.create({ control: "theme-select", controlType: "select", defaultValue: "light", options: "[\"light\",\"dark\",\"system\"]", personal: "true" }), storage);
      const result = await interpret(controlStateHandler.set({ control: afterResult_create_select?.output?.["control"], value: "dark" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_notfound" -> notfound', async () => {
      if (typeof controlStateHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(controlStateHandler.set({ control: "nonexistent-ctrl", value: "42" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "set_empty_value" -> invalid', async () => {
      if (typeof controlStateHandler.set !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_toggle = await interpret(controlStateHandler.create({ control: "show-grid", controlType: "toggle", defaultValue: "false", options: null, personal: "false" }), storage);
      const result = await interpret(controlStateHandler.set({ control: afterResult_create_toggle?.output?.["control"], value: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('reset', () => {
    it('builds a valid StorageProgram', () => {
      const program = controlStateHandler.reset({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = controlStateHandler.reset({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = controlStateHandler.reset({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = controlStateHandler.reset({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = controlStateHandler.reset({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof controlStateHandler.reset !== 'function') return;
      const result = await interpret(controlStateHandler.reset({ control: {"type":"ref","fixture":"create_slider","field":"control"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "reset_ok" -> ok', async () => {
      if (typeof controlStateHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_slider = await interpret(controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" }), storage);
      const result = await interpret(controlStateHandler.reset({ control: afterResult_create_slider?.output?.["control"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reset_notfound" -> notfound', async () => {
      if (typeof controlStateHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(controlStateHandler.reset({ control: "nonexistent-ctrl" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = controlStateHandler.get({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = controlStateHandler.get({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = controlStateHandler.get({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = controlStateHandler.get({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = controlStateHandler.get({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof controlStateHandler.get !== 'function') return;
      const result = await interpret(controlStateHandler.get({ control: {"type":"ref","fixture":"create_slider","field":"control"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof controlStateHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_slider = await interpret(controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" }), storage);
      const result = await interpret(controlStateHandler.get({ control: afterResult_create_slider?.output?.["control"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_after_set" -> ok', async () => {
      if (typeof controlStateHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_select = await interpret(controlStateHandler.create({ control: "theme-select", controlType: "select", defaultValue: "light", options: "[\"light\",\"dark\",\"system\"]", personal: "true" }), storage);
      const afterResult_set_select_ok = await interpret(controlStateHandler.set({ control: afterResult_create_select?.output?.["control"], value: "dark" }), storage);
      const result = await interpret(controlStateHandler.get({ control: afterResult_create_select?.output?.["control"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_notfound" -> notfound', async () => {
      if (typeof controlStateHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(controlStateHandler.get({ control: "nonexistent-ctrl" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = controlStateHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = controlStateHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = controlStateHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = controlStateHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = controlStateHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof controlStateHandler.list !== 'function') return;
      const result = await interpret(controlStateHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_ok" -> ok', async () => {
      if (typeof controlStateHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_slider = await interpret(controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" }), storage);
      const _pool = Object.assign({}, (afterResult_create_slider?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(controlStateHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_after_create" -> ok', async () => {
      if (typeof controlStateHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_slider = await interpret(controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" }), storage);
      const afterResult_create_select = await interpret(controlStateHandler.create({ control: "theme-select", controlType: "select", defaultValue: "light", options: "[\"light\",\"dark\",\"system\"]", personal: "true" }), storage);
      const _pool = Object.assign({}, (afterResult_create_slider?.output ?? {}), (afterResult_create_select?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(controlStateHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('remove', () => {
    it('builds a valid StorageProgram', () => {
      const program = controlStateHandler.remove({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = controlStateHandler.remove({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = controlStateHandler.remove({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = controlStateHandler.remove({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = controlStateHandler.remove({ control: {"type":"ref","fixture":"create_slider","field":"control"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof controlStateHandler.remove !== 'function') return;
      const result = await interpret(controlStateHandler.remove({ control: {"type":"ref","fixture":"create_slider","field":"control"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_ok" -> ok', async () => {
      if (typeof controlStateHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_slider = await interpret(controlStateHandler.create({ control: "vol-slider", controlType: "slider", defaultValue: "50", options: null, personal: "false" }), storage);
      const result = await interpret(controlStateHandler.remove({ control: afterResult_create_slider?.output?.["control"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_notfound" -> notfound', async () => {
      if (typeof controlStateHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(controlStateHandler.remove({ control: "nonexistent-ctrl" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof controlStateHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = controlStateHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ControlState');
    });
  });

  describe('invariant examples', () => {
    it("set then get returns new value", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(controlStateHandler.create({ control: "test-k", controlType: "slider", defaultValue: "0", options: false, personal: false }), storage);
      expect(createResult0.variant).toBe("ok");
      let control = createResult0.output["control"];
      let k = control;
      const setResult1 = await interpret(controlStateHandler.set({ control: k, value: "80" }), storage);
      expect(setResult1.variant).toBe("ok");
      let previous = setResult1.output["previous"];
      let current = setResult1.output["current"];
      const thenResult0 = await interpret(controlStateHandler.get({ control: k }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("reset restores default", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(controlStateHandler.create({ control: "test-k", controlType: "text", defaultValue: "hello", options: false, personal: false }), storage);
      expect(createResult0.variant).toBe("ok");
      let control = createResult0.output["control"];
      let k = control;
      const setResult1 = await interpret(controlStateHandler.set({ control: k, value: "world" }), storage);
      expect(setResult1.variant).toBe("ok");
      let previous = setResult1.output["previous"];
      let current = setResult1.output["current"];
      const resetResult2 = await interpret(controlStateHandler.reset({ control: k }), storage);
      expect(resetResult2.variant).toBe("ok");
      let value = resetResult2.output["value"];
      const thenResult0 = await interpret(controlStateHandler.get({ control: k }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate create is rejected", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(controlStateHandler.create({ control: "test-k", controlType: "toggle", defaultValue: "false", options: false, personal: false }), storage);
      expect(createResult0.variant).toBe("ok");
      let control = createResult0.output["control"];
      let k = control;
      const thenResult0 = await interpret(controlStateHandler.create({ control: k, controlType: "toggle", defaultValue: "true", options: false, personal: true }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: orphaned value', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ control: fc.string(), controlType: fc.string({ minLength: 1, maxLength: 50 }), defaultValue: fc.string({ minLength: 1, maxLength: 50 }), options: fc.string(), personal: fc.boolean() }) }),
              fc.record({ action: fc.constant('set'), input: fc.record({ control: fc.string(), value: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ control: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ control: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ control: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = controlStateHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(controlStateHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned value
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned type', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ control: fc.string(), controlType: fc.string({ minLength: 1, maxLength: 50 }), defaultValue: fc.string({ minLength: 1, maxLength: 50 }), options: fc.string(), personal: fc.boolean() }) }),
              fc.record({ action: fc.constant('set'), input: fc.record({ control: fc.string(), value: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ control: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ control: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ control: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = controlStateHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(controlStateHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned type
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
