// GraphqlProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { graphqlProviderHandler } from '../../handlers/ts/execution/providers/graphql-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('GraphqlProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = graphqlProviderHandler.register({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = graphqlProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = graphqlProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = graphqlProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = graphqlProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof graphqlProviderHandler.register !== 'function') return;
      const result = await interpret(graphqlProviderHandler.register({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof graphqlProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(graphqlProviderHandler.register({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = graphqlProviderHandler.configure({ name: "github-api", url: "https://api.github.com/graphql", headers: "{\"Authorization\":\"Bearer ghp_token\"}", schemaRef: "github-schema" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = graphqlProviderHandler.configure({ name: "github-api", url: "https://api.github.com/graphql", headers: "{\"Authorization\":\"Bearer ghp_token\"}", schemaRef: "github-schema" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = graphqlProviderHandler.configure({ name: "github-api", url: "https://api.github.com/graphql", headers: "{\"Authorization\":\"Bearer ghp_token\"}", schemaRef: "github-schema" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = graphqlProviderHandler.configure({ name: "github-api", url: "https://api.github.com/graphql", headers: "{\"Authorization\":\"Bearer ghp_token\"}", schemaRef: "github-schema" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = graphqlProviderHandler.configure({ name: "github-api", url: "https://api.github.com/graphql", headers: "{\"Authorization\":\"Bearer ghp_token\"}", schemaRef: "github-schema" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof graphqlProviderHandler.configure !== 'function') return;
      const result = await interpret(graphqlProviderHandler.configure({ name: "github-api", url: "https://api.github.com/graphql", headers: "{\"Authorization\":\"Bearer ghp_token\"}", schemaRef: "github-schema" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "configure_github" -> ok', async () => {
      if (typeof graphqlProviderHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(graphqlProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { name: "github-api", url: "https://api.github.com/graphql", headers: "{\"Authorization\":\"Bearer ghp_token\"}", schemaRef: "github-schema" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(graphqlProviderHandler.configure({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "configure_local" -> ok', async () => {
      if (typeof graphqlProviderHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(graphqlProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { name: "local-gql", url: "http://localhost:4000/graphql", headers: "{}", schemaRef: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(graphqlProviderHandler.configure({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('execute', () => {
    it('builds a valid StorageProgram', () => {
      const program = graphqlProviderHandler.execute({ endpoint: "github-api", query: "{ viewer { login } }", variables: "{}", operationType: "query" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = graphqlProviderHandler.execute({ endpoint: "github-api", query: "{ viewer { login } }", variables: "{}", operationType: "query" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = graphqlProviderHandler.execute({ endpoint: "github-api", query: "{ viewer { login } }", variables: "{}", operationType: "query" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = graphqlProviderHandler.execute({ endpoint: "github-api", query: "{ viewer { login } }", variables: "{}", operationType: "query" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = graphqlProviderHandler.execute({ endpoint: "github-api", query: "{ viewer { login } }", variables: "{}", operationType: "query" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof graphqlProviderHandler.execute !== 'function') return;
      const result = await interpret(graphqlProviderHandler.execute({ endpoint: "github-api", query: "{ viewer { login } }", variables: "{}", operationType: "query" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "query_viewer" -> ok', async () => {
      if (typeof graphqlProviderHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(graphqlProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { endpoint: "github-api", query: "{ viewer { login } }", variables: "{}", operationType: "query" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(graphqlProviderHandler.execute({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "mutation_create" -> ok', async () => {
      if (typeof graphqlProviderHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(graphqlProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { endpoint: "github-api", query: "mutation { createIssue(input: {}) { id } }", variables: "{}", operationType: "mutation" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(graphqlProviderHandler.execute({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "execute_unknown_endpoint" -> notFound', async () => {
      if (typeof graphqlProviderHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(graphqlProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { endpoint: "nonexistent", query: "{ test }", variables: "{}", operationType: "query" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(graphqlProviderHandler.execute({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = graphqlProviderHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = graphqlProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = graphqlProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = graphqlProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = graphqlProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof graphqlProviderHandler.list !== 'function') return;
      const result = await interpret(graphqlProviderHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof graphqlProviderHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(graphqlProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(graphqlProviderHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof graphqlProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = graphqlProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('GraphqlProvider');
    });
  });

  describe('invariant examples', () => {
    it("execute fails for unknown endpoint", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(graphqlProviderHandler.configure({ name: {"type":"literal","value":"github-api"}, url: {"type":"literal","value":"https://api.github.com/graphql"}, headers: {"type":"literal","value":"{}"}, schemaRef: {"type":"literal","value":""} }), storage);
      expect(configureResult0.variant).toBe("ok");
      let endpoint = configureResult0.output["endpoint"];
      const thenResult0 = await interpret(graphqlProviderHandler.execute({ endpoint: {"type":"literal","value":"unknown-api"}, query: {"type":"literal","value":"{ viewer { login } }"}, variables: {"type":"literal","value":"{}"}, operationType: {"type":"literal","value":"query"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: endpoints have valid name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('configure'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), url: fc.string({ minLength: 1, maxLength: 50 }), headers: fc.string({ minLength: 1, maxLength: 50 }), schemaRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), query: fc.string({ minLength: 1, maxLength: 50 }), variables: fc.string({ minLength: 1, maxLength: 50 }), operationType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = graphqlProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(graphqlProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: endpoint without URL', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('configure'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), url: fc.string({ minLength: 1, maxLength: 50 }), headers: fc.string({ minLength: 1, maxLength: 50 }), schemaRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), query: fc.string({ minLength: 1, maxLength: 50 }), variables: fc.string({ minLength: 1, maxLength: 50 }), operationType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = graphqlProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(graphqlProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: endpoint without URL
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof graphqlProviderHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(graphqlProviderHandler.configure({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('configure ensures on ok: ', async () => {
      if (typeof graphqlProviderHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), url: fc.string({ minLength: 1, maxLength: 50 }), headers: fc.string({ minLength: 1, maxLength: 50 }), schemaRef: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = graphqlProviderHandler.configure(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
