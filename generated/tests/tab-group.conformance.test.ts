// TabGroup Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { tabGroupHandler } from '../../handlers/ts/app/tab-group.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('TabGroup functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.create({ group: "G-1", name: "main" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.create({ group: "G-1", name: "main" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.create({ group: "G-1", name: "main" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.create({ group: "G-1", name: "main" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.create({ group: "G-1", name: "main" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.create !== 'function') return;
      const result = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_ok" -> ok', async () => {
      if (typeof tabGroupHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_second" -> ok', async () => {
      if (typeof tabGroupHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.create({ group: "G-2", name: "secondary" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "duplicate_name" -> duplicate', async () => {
      if (typeof tabGroupHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { group: "G-3", name: "main" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(tabGroupHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "empty_name" -> invalid', async () => {
      if (typeof tabGroupHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.create({ group: "G-4", name: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('addTab', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.addTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", position: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.addTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.addTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.addTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.addTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.addTab !== 'function') return;
      const result = await interpret(tabGroupHandler.addTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", position: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "add_first_tab" -> ok', async () => {
      if (typeof tabGroupHandler.addTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_at_position" -> ok', async () => {
      if (typeof tabGroupHandler.addTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-B", position: "0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_duplicate" -> duplicate', async () => {
      if (typeof tabGroupHandler.addTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "add_missing_group" -> notfound', async () => {
      if (typeof tabGroupHandler.addTab !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.addTab({ group: "G-nonexistent", paneId: "pane-X", position: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "add_empty_pane" -> invalid', async () => {
      if (typeof tabGroupHandler.addTab !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.addTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "", position: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('removeTab', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.removeTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.removeTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.removeTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.removeTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.removeTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.removeTab !== 'function') return;
      const result = await interpret(tabGroupHandler.removeTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_ok" -> ok', async () => {
      if (typeof tabGroupHandler.removeTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.removeTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_missing_pane" -> notfound', async () => {
      if (typeof tabGroupHandler.removeTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.removeTab({ group: afterResult_create_ok?.output?.["group"], paneId: "no-such-pane" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "remove_missing_group" -> notfound', async () => {
      if (typeof tabGroupHandler.removeTab !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.removeTab({ group: "G-nonexistent", paneId: "pane-A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "remove_pinned" -> invalid', async () => {
      if (typeof tabGroupHandler.removeTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.removeTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('activateTab', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.activateTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.activateTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.activateTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.activateTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.activateTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.activateTab !== 'function') return;
      const result = await interpret(tabGroupHandler.activateTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "activate_ok" -> ok', async () => {
      if (typeof tabGroupHandler.activateTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.activateTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "activate_missing_pane" -> notfound', async () => {
      if (typeof tabGroupHandler.activateTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.activateTab({ group: afterResult_create_ok?.output?.["group"], paneId: "no-such-pane" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "activate_missing_group" -> notfound', async () => {
      if (typeof tabGroupHandler.activateTab !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.activateTab({ group: "G-nonexistent", paneId: "pane-A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('moveTab', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.moveTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", newPosition: "0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.moveTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", newPosition: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.moveTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", newPosition: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.moveTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", newPosition: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.moveTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", newPosition: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.moveTab !== 'function') return;
      const result = await interpret(tabGroupHandler.moveTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A", newPosition: "0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "move_ok" -> ok', async () => {
      if (typeof tabGroupHandler.moveTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.moveTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", newPosition: "0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "move_out_of_range" -> invalid', async () => {
      if (typeof tabGroupHandler.moveTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.moveTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", newPosition: "99" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "move_missing_pane" -> notfound', async () => {
      if (typeof tabGroupHandler.moveTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.moveTab({ group: afterResult_create_ok?.output?.["group"], paneId: "no-such", newPosition: "0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "move_missing_group" -> notfound', async () => {
      if (typeof tabGroupHandler.moveTab !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.moveTab({ group: "G-nonexistent", paneId: "pane-A", newPosition: "0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('pinTab', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.pinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.pinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.pinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.pinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.pinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.pinTab !== 'function') return;
      const result = await interpret(tabGroupHandler.pinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "pin_ok" -> ok', async () => {
      if (typeof tabGroupHandler.pinTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.pinTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "pin_again" -> duplicate', async () => {
      if (typeof tabGroupHandler.pinTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const afterResult_pin_ok = await interpret(tabGroupHandler.pinTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      const result = await interpret(tabGroupHandler.pinTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "pin_missing_pane" -> notfound', async () => {
      if (typeof tabGroupHandler.pinTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.pinTab({ group: afterResult_create_ok?.output?.["group"], paneId: "no-such" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "pin_missing_group" -> notfound', async () => {
      if (typeof tabGroupHandler.pinTab !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.pinTab({ group: "G-nonexistent", paneId: "pane-A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('unpinTab', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.unpinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.unpinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.unpinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.unpinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.unpinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.unpinTab !== 'function') return;
      const result = await interpret(tabGroupHandler.unpinTab({ group: {"type":"ref","fixture":"create_ok","field":"group"}, paneId: "pane-A" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "unpin_ok" -> ok', async () => {
      if (typeof tabGroupHandler.unpinTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const afterResult_pin_ok = await interpret(tabGroupHandler.pinTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      const result = await interpret(tabGroupHandler.unpinTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unpin_not_pinned" -> notfound', async () => {
      if (typeof tabGroupHandler.unpinTab !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.unpinTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "unpin_missing_group" -> notfound', async () => {
      if (typeof tabGroupHandler.unpinTab !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.unpinTab({ group: "G-nonexistent", paneId: "pane-A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('activatePrevious', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.activatePrevious({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.activatePrevious({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.activatePrevious({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.activatePrevious({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.activatePrevious({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.activatePrevious !== 'function') return;
      const result = await interpret(tabGroupHandler.activatePrevious({ group: {"type":"ref","fixture":"create_ok","field":"group"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "activate_prev_ok" -> ok', async () => {
      if (typeof tabGroupHandler.activatePrevious !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const afterResult_activate_ok = await interpret(tabGroupHandler.activateTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      const result = await interpret(tabGroupHandler.activatePrevious({ group: afterResult_create_ok?.output?.["group"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "activate_prev_empty_history" -> empty', async () => {
      if (typeof tabGroupHandler.activatePrevious !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.activatePrevious({ group: afterResult_create_ok?.output?.["group"] }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('empty'));
    });

    it('fixture "activate_prev_missing" -> notfound', async () => {
      if (typeof tabGroupHandler.activatePrevious !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.activatePrevious({ group: "G-nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('closeOthers', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.closeOthers({ group: {"type":"ref","fixture":"create_ok","field":"group"}, keepPaneId: "pane-A" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.closeOthers({ group: {"type":"ref","fixture":"create_ok","field":"group"}, keepPaneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.closeOthers({ group: {"type":"ref","fixture":"create_ok","field":"group"}, keepPaneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.closeOthers({ group: {"type":"ref","fixture":"create_ok","field":"group"}, keepPaneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.closeOthers({ group: {"type":"ref","fixture":"create_ok","field":"group"}, keepPaneId: "pane-A" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.closeOthers !== 'function') return;
      const result = await interpret(tabGroupHandler.closeOthers({ group: {"type":"ref","fixture":"create_ok","field":"group"}, keepPaneId: "pane-A" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "close_others_ok" -> ok', async () => {
      if (typeof tabGroupHandler.closeOthers !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.closeOthers({ group: afterResult_create_ok?.output?.["group"], keepPaneId: "pane-A" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "close_others_keep_pinned" -> invalid', async () => {
      if (typeof tabGroupHandler.closeOthers !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const afterResult_pin_ok = await interpret(tabGroupHandler.pinTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A" }), storage);
      const result = await interpret(tabGroupHandler.closeOthers({ group: afterResult_create_ok?.output?.["group"], keepPaneId: "pane-A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "close_others_missing_pane" -> notfound', async () => {
      if (typeof tabGroupHandler.closeOthers !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.closeOthers({ group: afterResult_create_ok?.output?.["group"], keepPaneId: "no-such" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "close_others_missing_group" -> notfound', async () => {
      if (typeof tabGroupHandler.closeOthers !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.closeOthers({ group: "G-nonexistent", keepPaneId: "pane-A" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('closeAll', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.closeAll({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.closeAll({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.closeAll({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.closeAll({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.closeAll({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.closeAll !== 'function') return;
      const result = await interpret(tabGroupHandler.closeAll({ group: {"type":"ref","fixture":"create_ok","field":"group"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "close_all_ok" -> ok', async () => {
      if (typeof tabGroupHandler.closeAll !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.closeAll({ group: afterResult_create_ok?.output?.["group"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "close_all_empty" -> ok', async () => {
      if (typeof tabGroupHandler.closeAll !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.closeAll({ group: afterResult_create_ok?.output?.["group"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "close_all_missing" -> notfound', async () => {
      if (typeof tabGroupHandler.closeAll !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.closeAll({ group: "G-nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.get({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.get({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.get({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.get({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.get({ group: {"type":"ref","fixture":"create_ok","field":"group"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.get !== 'function') return;
      const result = await interpret(tabGroupHandler.get({ group: {"type":"ref","fixture":"create_ok","field":"group"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof tabGroupHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const result = await interpret(tabGroupHandler.get({ group: afterResult_create_ok?.output?.["group"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_with_tabs" -> ok', async () => {
      if (typeof tabGroupHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const afterResult_add_first_tab = await interpret(tabGroupHandler.addTab({ group: afterResult_create_ok?.output?.["group"], paneId: "pane-A", position: null }), storage);
      const result = await interpret(tabGroupHandler.get({ group: afterResult_create_ok?.output?.["group"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof tabGroupHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(tabGroupHandler.get({ group: "G-nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = tabGroupHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = tabGroupHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = tabGroupHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = tabGroupHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = tabGroupHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof tabGroupHandler.list !== 'function') return;
      const result = await interpret(tabGroupHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof tabGroupHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(tabGroupHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_after_create" -> ok', async () => {
      if (typeof tabGroupHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(tabGroupHandler.create({ group: "G-1", name: "main" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(tabGroupHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof tabGroupHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = tabGroupHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('TabGroup');
    });
  });

  describe('invariant examples', () => {
    it("create and get", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(tabGroupHandler.create({ group: "test-g", name: "editors" }), storage);
      expect(createResult0.variant).toBe("ok");
      let group = createResult0.output["group"];
      let g = group;
      const thenResult0 = await interpret(tabGroupHandler.get({ group: g }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("add tab activates first", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(tabGroupHandler.create({ group: "test-g", name: "editors" }), storage);
      expect(createResult0.variant).toBe("ok");
      let group = createResult0.output["group"];
      let g = group;
      const addTabResult1 = await interpret(tabGroupHandler.addTab({ group: g, paneId: "p1", position: false }), storage);
      expect(addTabResult1.variant).toBe("ok");
      group = addTabResult1.output["group"];
      const thenResult0 = await interpret(tabGroupHandler.get({ group: g }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("pin then unpin", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(tabGroupHandler.create({ group: "test-g", name: "editors" }), storage);
      expect(createResult0.variant).toBe("ok");
      let group = createResult0.output["group"];
      let g = group;
      const addTabResult1 = await interpret(tabGroupHandler.addTab({ group: g, paneId: "p1", position: false }), storage);
      expect(addTabResult1.variant).toBe("ok");
      group = addTabResult1.output["group"];
      const pinTabResult2 = await interpret(tabGroupHandler.pinTab({ group: g, paneId: "p1" }), storage);
      expect(pinTabResult2.variant).toBe("ok");
      group = pinTabResult2.output["group"];
      const unpinTabResult3 = await interpret(tabGroupHandler.unpinTab({ group: g, paneId: "p1" }), storage);
      expect(unpinTabResult3.variant).toBe("ok");
      group = unpinTabResult3.output["group"];
      let pinnedTabs = unpinTabResult3.output["pinnedTabs"];
      let pts = pinnedTabs;
    });

    it("activate records history", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(tabGroupHandler.create({ group: "test-g", name: "editors" }), storage);
      expect(createResult0.variant).toBe("ok");
      let group = createResult0.output["group"];
      let g = group;
      const addTabResult1 = await interpret(tabGroupHandler.addTab({ group: g, paneId: "p1", position: false }), storage);
      expect(addTabResult1.variant).toBe("ok");
      group = addTabResult1.output["group"];
      const addTabResult2 = await interpret(tabGroupHandler.addTab({ group: g, paneId: "p2", position: false }), storage);
      expect(addTabResult2.variant).toBe("ok");
      group = addTabResult2.output["group"];
      const activateTabResult3 = await interpret(tabGroupHandler.activateTab({ group: g, paneId: "p1" }), storage);
      expect(activateTabResult3.variant).toBe("ok");
      group = activateTabResult3.output["group"];
      let activeTab = activateTabResult3.output["activeTab"];
      const activateTabResult4 = await interpret(tabGroupHandler.activateTab({ group: g, paneId: "p2" }), storage);
      expect(activateTabResult4.variant).toBe("ok");
      group = activateTabResult4.output["group"];
      activeTab = activateTabResult4.output["activeTab"];
      const thenResult0 = await interpret(tabGroupHandler.activatePrevious({ group: g }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("closeAll clears tabs", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(tabGroupHandler.create({ group: "test-g", name: "editors" }), storage);
      expect(createResult0.variant).toBe("ok");
      let group = createResult0.output["group"];
      let g = group;
      const addTabResult1 = await interpret(tabGroupHandler.addTab({ group: g, paneId: "p1", position: false }), storage);
      expect(addTabResult1.variant).toBe("ok");
      group = addTabResult1.output["group"];
      const closeAllResult2 = await interpret(tabGroupHandler.closeAll({ group: g }), storage);
      expect(closeAllResult2.variant).toBe("ok");
      group = closeAllResult2.output["group"];
      const thenResult0 = await interpret(tabGroupHandler.get({ group: g }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate name rejected", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(tabGroupHandler.create({ group: "test-g1", name: "editors" }), storage);
      expect(createResult0.variant).toBe("ok");
      let group = createResult0.output["group"];
      let g1 = group;
      const thenResult0 = await interpret(tabGroupHandler.create({ group: "test-g2", name: "editors" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: active tab is member of tabs', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ group: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('addTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }), position: fc.string() }) }),
              fc.record({ action: fc.constant('removeTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('activateTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('moveTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }), newPosition: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('pinTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unpinTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('activatePrevious'), input: fc.record({ group: fc.string() }) }),
              fc.record({ action: fc.constant('closeOthers'), input: fc.record({ group: fc.string(), keepPaneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('closeAll'), input: fc.record({ group: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ group: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = tabGroupHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(tabGroupHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: pinned tab outside group', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ group: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('addTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }), position: fc.string() }) }),
              fc.record({ action: fc.constant('removeTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('activateTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('moveTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }), newPosition: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('pinTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unpinTab'), input: fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('activatePrevious'), input: fc.record({ group: fc.string() }) }),
              fc.record({ action: fc.constant('closeOthers'), input: fc.record({ group: fc.string(), keepPaneId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('closeAll'), input: fc.record({ group: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ group: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = tabGroupHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(tabGroupHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: pinned tab outside group
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof tabGroupHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(tabGroupHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof tabGroupHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ group: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = tabGroupHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('addTab handles empty input: ', async () => {
      if (typeof tabGroupHandler.addTab !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(tabGroupHandler.addTab({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('addTab ensures on ok: ', async () => {
      if (typeof tabGroupHandler.addTab !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }), position: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = tabGroupHandler.addTab(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('activateTab ensures on ok: ', async () => {
      if (typeof tabGroupHandler.activateTab !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ group: fc.string(), paneId: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = tabGroupHandler.activateTab(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
