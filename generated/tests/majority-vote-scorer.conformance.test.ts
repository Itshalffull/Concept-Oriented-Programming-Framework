// MajorityVoteScorer Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { majorityVoteScorerHandler } from '../../handlers/ts/quality-measurement/providers/majority-vote-scorer.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('MajorityVoteScorer functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = majorityVoteScorerHandler.register({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = majorityVoteScorerHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = majorityVoteScorerHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = majorityVoteScorerHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = majorityVoteScorerHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof majorityVoteScorerHandler.register !== 'function') return;
      const result = await interpret(majorityVoteScorerHandler.register({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('score', () => {
    it('builds a valid StorageProgram', () => {
      const program = majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":true},{\"passed\":false}]", config: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":true},{\"passed\":false}]", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":true},{\"passed\":false}]", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":true},{\"passed\":false}]", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":true},{\"passed\":false}]", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof majorityVoteScorerHandler.score !== 'function') return;
      const result = await interpret(majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":true},{\"passed\":false}]", config: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "score_majority" -> ok', async () => {
      if (typeof majorityVoteScorerHandler.score !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":true},{\"passed\":false}]", config: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "score_min_pass" -> ok', async () => {
      if (typeof majorityVoteScorerHandler.score !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":false},{\"passed\":false}]", config: "{\"min_pass\":1}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "score_threshold" -> ok', async () => {
      if (typeof majorityVoteScorerHandler.score !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":true},{\"passed\":false},{\"passed\":false}]", config: "{\"threshold\":0.6}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "score_empty" -> error', async () => {
      if (typeof majorityVoteScorerHandler.score !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(majorityVoteScorerHandler.score({ results: "", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "score_bad_results" -> error', async () => {
      if (typeof majorityVoteScorerHandler.score !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(majorityVoteScorerHandler.score({ results: "not-json", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "score_bad_config" -> error', async () => {
      if (typeof majorityVoteScorerHandler.score !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(majorityVoteScorerHandler.score({ results: "[{\"passed\":true}]", config: "bad" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof majorityVoteScorerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = majorityVoteScorerHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('MajorityVoteScorer');
    });
  });

  describe('invariant examples', () => {
    it("register then score majority", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(majorityVoteScorerHandler.register({  }), storage);
      expect(registerResult0.variant).toBe("ok");
      let name = registerResult0.output["name"];
      const thenResult0 = await interpret(majorityVoteScorerHandler.score({ results: "[{\"passed\":true},{\"passed\":true},{\"passed\":false}]", config: "{}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register then score fails minority", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(majorityVoteScorerHandler.register({  }), storage);
      expect(registerResult0.variant).toBe("ok");
      let name = registerResult0.output["name"];
      const thenResult0 = await interpret(majorityVoteScorerHandler.score({ results: "[{\"passed\":false},{\"passed\":false},{\"passed\":true}]", config: "{}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: empty registration name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('score'), input: fc.record({ results: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = majorityVoteScorerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(majorityVoteScorerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty registration name
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
