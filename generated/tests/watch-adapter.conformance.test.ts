// WatchAdapter Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { watchAdapterHandler } from '../../handlers/ts/app/watch-adapter.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('WatchAdapter functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('normalize', () => {
    it('builds a valid StorageProgram', () => {
      const program = watchAdapterHandler.normalize({ props: "{\"type\":\"navigation\",\"destination\":\"heartRate\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = watchAdapterHandler.normalize({ props: "{\"type\":\"navigation\",\"destination\":\"heartRate\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = watchAdapterHandler.normalize({ props: "{\"type\":\"navigation\",\"destination\":\"heartRate\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = watchAdapterHandler.normalize({ props: "{\"type\":\"navigation\",\"destination\":\"heartRate\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = watchAdapterHandler.normalize({ props: "{\"type\":\"navigation\",\"destination\":\"heartRate\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof watchAdapterHandler.normalize !== 'function') return;
      const result = await interpret(watchAdapterHandler.normalize({ props: "{\"type\":\"navigation\",\"destination\":\"heartRate\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_navigation" -> ok', async () => {
      if (typeof watchAdapterHandler.normalize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(watchAdapterHandler.normalize({ props: "{\"type\":\"navigation\",\"destination\":\"heartRate\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "with_aria" -> ok', async () => {
      if (typeof watchAdapterHandler.normalize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(watchAdapterHandler.normalize({ props: "{\"aria-label\":\"Close\",\"onclick\":\"handleClose\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "with_events" -> ok', async () => {
      if (typeof watchAdapterHandler.normalize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(watchAdapterHandler.normalize({ props: "{\"onclick\":\"tap\",\"onscroll\":\"scroll\",\"class\":\"primary\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_props" -> error', async () => {
      if (typeof watchAdapterHandler.normalize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(watchAdapterHandler.normalize({ props: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "bad_json" -> error', async () => {
      if (typeof watchAdapterHandler.normalize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(watchAdapterHandler.normalize({ props: "not-json" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof watchAdapterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = watchAdapterHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('WatchAdapter');
    });
  });

  describe('invariant examples', () => {
    it("normalize then normalize", async () => {
      const storage = createInMemoryStorage();
      const normalizeResult0 = await interpret(watchAdapterHandler.normalize({ adapter: {"type":"variable","name":"a"}, props: {"type":"literal","value":"{ \"type\": \"navigation\", \"destination\": \"heartRate\" }"} }), storage);
      expect(normalizeResult0.variant).toBe("ok");
      const adapter = normalizeResult0.output["adapter"];
      const normalized = normalizeResult0.output["normalized"];
      const thenResult0 = await interpret(watchAdapterHandler.normalize({ adapter: {"type":"variable","name":"a"}, props: {"type":"literal","value":""} }), storage);
      expect(thenResult0.variant).toBe("error");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: successful normalize produces valid platform output', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('normalize'), input: fc.record({ adapter: fc.string(), props: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = watchAdapterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(watchAdapterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty props yield ok', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('normalize'), input: fc.record({ adapter: fc.string(), props: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = watchAdapterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(watchAdapterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty props yield ok
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('normalize handles empty input: ', async () => {
      if (typeof watchAdapterHandler.normalize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(watchAdapterHandler.normalize({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('normalize ensures on ok: ', async () => {
      if (typeof watchAdapterHandler.normalize !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ adapter: fc.string(), props: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = watchAdapterHandler.normalize(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
