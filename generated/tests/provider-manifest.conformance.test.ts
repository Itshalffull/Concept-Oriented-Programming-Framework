// ProviderManifest Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { providerManifestHandler } from '../../handlers/ts/app/provider-manifest.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ProviderManifest functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('load', () => {
    it('builds a valid StorageProgram', () => {
      const program = providerManifestHandler.load({ sources: [".prettierrc",".clef/providers.yaml"] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = providerManifestHandler.load({ sources: [".prettierrc",".clef/providers.yaml"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = providerManifestHandler.load({ sources: [".prettierrc",".clef/providers.yaml"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = providerManifestHandler.load({ sources: [".prettierrc",".clef/providers.yaml"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = providerManifestHandler.load({ sources: [".prettierrc",".clef/providers.yaml"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof providerManifestHandler.load !== 'function') return;
      const result = await interpret(providerManifestHandler.load({ sources: [".prettierrc",".clef/providers.yaml"] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "load_ok" -> ok', async () => {
      if (typeof providerManifestHandler.load !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(providerManifestHandler.listEntries({  }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}));
      const _fixtureInput = { sources: [".prettierrc",".clef/providers.yaml"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(providerManifestHandler.load({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "load_single" -> ok', async () => {
      if (typeof providerManifestHandler.load !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(providerManifestHandler.listEntries({  }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}));
      const _fixtureInput = { sources: [".clef/providers.yaml"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(providerManifestHandler.load({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "load_empty_sources" -> error', async () => {
      if (typeof providerManifestHandler.load !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(providerManifestHandler.load({ sources: [] }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('reload', () => {
    it('builds a valid StorageProgram', () => {
      const program = providerManifestHandler.reload({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = providerManifestHandler.reload({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = providerManifestHandler.reload({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = providerManifestHandler.reload({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = providerManifestHandler.reload({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof providerManifestHandler.reload !== 'function') return;
      const result = await interpret(providerManifestHandler.reload({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "reload_ok" -> ok', async () => {
      if (typeof providerManifestHandler.reload !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(providerManifestHandler.listEntries({  }), storage);
      const afterResult_load_ok = await interpret(providerManifestHandler.load({ sources: [".prettierrc",".clef/providers.yaml"] }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}), (afterResult_load_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(providerManifestHandler.reload({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reload_no_prior_load" -> error', async () => {
      if (typeof providerManifestHandler.reload !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(providerManifestHandler.reload({  }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listEntries', () => {
    it('builds a valid StorageProgram', () => {
      const program = providerManifestHandler.listEntries({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = providerManifestHandler.listEntries({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = providerManifestHandler.listEntries({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = providerManifestHandler.listEntries({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = providerManifestHandler.listEntries({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof providerManifestHandler.listEntries !== 'function') return;
      const result = await interpret(providerManifestHandler.listEntries({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof providerManifestHandler.listEntries !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(providerManifestHandler.listEntries({  }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_after_load" -> ok', async () => {
      if (typeof providerManifestHandler.listEntries !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(providerManifestHandler.listEntries({  }), storage);
      const afterResult_load_ok = await interpret(providerManifestHandler.load({ sources: [".prettierrc",".clef/providers.yaml"] }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}), (afterResult_load_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(providerManifestHandler.listEntries({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof providerManifestHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = providerManifestHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ProviderManifest');
    });
  });

  describe('invariant examples', () => {
    it("load then listEntries returns non-empty bytes", async () => {
      const storage = createInMemoryStorage();
      const loadResult0 = await interpret(providerManifestHandler.load({ sources: [".clef/providers.yaml"] }), storage);
      expect(loadResult0.variant).toBe("ok");
      let count = loadResult0.output["count"];
      let _ = count;
      const thenResult0 = await interpret(providerManifestHandler.listEntries({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("reload without prior load returns error", async () => {
      const storage = createInMemoryStorage();
    });

    it("load with empty sources returns error", async () => {
      const storage = createInMemoryStorage();
    });

    it("reload after load returns diff counts", async () => {
      const storage = createInMemoryStorage();
      const loadResult0 = await interpret(providerManifestHandler.load({ sources: [".clef/providers.yaml"] }), storage);
      expect(loadResult0.variant).toBe("ok");
      let count = loadResult0.output["count"];
      let _ = count;
      const thenResult0 = await interpret(providerManifestHandler.reload({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every entry has a non-empty slot', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('load'), input: fc.record({ sources: fc.string() }) }),
              fc.record({ action: fc.constant('reload'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('listEntries'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = providerManifestHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(providerManifestHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: every entry has a non-empty provider', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('load'), input: fc.record({ sources: fc.string() }) }),
              fc.record({ action: fc.constant('reload'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('listEntries'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = providerManifestHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(providerManifestHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: every entry kind is a recognized value', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('load'), input: fc.record({ sources: fc.string() }) }),
              fc.record({ action: fc.constant('reload'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('listEntries'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = providerManifestHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(providerManifestHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: two entries share the same kind and slot', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('load'), input: fc.record({ sources: fc.string() }) }),
              fc.record({ action: fc.constant('reload'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('listEntries'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = providerManifestHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(providerManifestHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: two entries share the same kind and slot
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('load handles empty input: ', async () => {
      if (typeof providerManifestHandler.load !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(providerManifestHandler.load({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('load ensures on ok: ', async () => {
      if (typeof providerManifestHandler.load !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ sources: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = providerManifestHandler.load(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
