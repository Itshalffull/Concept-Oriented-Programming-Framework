// ProgramCache Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { programCacheHandler } from '../../handlers/ts/monadic/program-cache.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ProgramCache functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('lookup', () => {
    it('builds a valid StorageProgram', () => {
      const program = programCacheHandler.lookup({ programHash: 'test-programHash', stateHash: 'test-stateHash' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = programCacheHandler.lookup({ programHash: 'test-programHash', stateHash: 'test-stateHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = programCacheHandler.lookup({ programHash: 'test-programHash', stateHash: 'test-stateHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = programCacheHandler.lookup({ programHash: 'test-programHash', stateHash: 'test-stateHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = programCacheHandler.lookup({ programHash: 'test-programHash', stateHash: 'test-stateHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof programCacheHandler.lookup !== 'function') return;
      const result = await interpret(programCacheHandler.lookup({ programHash: 'test-programHash', stateHash: 'test-stateHash' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "lookup_existing" -> hit', async () => {
      if (typeof programCacheHandler.lookup !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_store_new_entry = await interpret(programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "{\"variant\":\"ok\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_store_new_entry?.output ?? {}));
      const _fixtureInput = { programHash: "sha256_abc123", stateHash: "sha256_def456" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(programCacheHandler.lookup({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('hit'));
    });

    it('fixture "lookup_empty_hash" -> error', async () => {
      if (typeof programCacheHandler.lookup !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(programCacheHandler.lookup({ programHash: "", stateHash: "sha256_def456" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('store', () => {
    it('builds a valid StorageProgram', () => {
      const program = programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "{\"variant\":\"ok\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "{\"variant\":\"ok\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "{\"variant\":\"ok\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "{\"variant\":\"ok\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "{\"variant\":\"ok\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof programCacheHandler.store !== 'function') return;
      const result = await interpret(programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "{\"variant\":\"ok\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "store_new_entry" -> ok', async () => {
      if (typeof programCacheHandler.store !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "{\"variant\":\"ok\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "store_empty_result" -> error', async () => {
      if (typeof programCacheHandler.store !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('invalidateByState', () => {
    it('builds a valid StorageProgram', () => {
      const program = programCacheHandler.invalidateByState({ stateHash: 'test-stateHash' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = programCacheHandler.invalidateByState({ stateHash: 'test-stateHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = programCacheHandler.invalidateByState({ stateHash: 'test-stateHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = programCacheHandler.invalidateByState({ stateHash: 'test-stateHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = programCacheHandler.invalidateByState({ stateHash: 'test-stateHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof programCacheHandler.invalidateByState !== 'function') return;
      const result = await interpret(programCacheHandler.invalidateByState({ stateHash: 'test-stateHash' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "invalidate_state" -> error', async () => {
      if (typeof programCacheHandler.invalidateByState !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(programCacheHandler.invalidateByState({ stateHash: "sha256_def456" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "invalidate_empty_state" -> error', async () => {
      if (typeof programCacheHandler.invalidateByState !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(programCacheHandler.invalidateByState({ stateHash: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('invalidateByProgram', () => {
    it('builds a valid StorageProgram', () => {
      const program = programCacheHandler.invalidateByProgram({ programHash: 'test-programHash' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = programCacheHandler.invalidateByProgram({ programHash: 'test-programHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = programCacheHandler.invalidateByProgram({ programHash: 'test-programHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = programCacheHandler.invalidateByProgram({ programHash: 'test-programHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = programCacheHandler.invalidateByProgram({ programHash: 'test-programHash' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof programCacheHandler.invalidateByProgram !== 'function') return;
      const result = await interpret(programCacheHandler.invalidateByProgram({ programHash: 'test-programHash' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "invalidate_program" -> error', async () => {
      if (typeof programCacheHandler.invalidateByProgram !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(programCacheHandler.invalidateByProgram({ programHash: "sha256_abc123" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "invalidate_empty_program" -> error', async () => {
      if (typeof programCacheHandler.invalidateByProgram !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(programCacheHandler.invalidateByProgram({ programHash: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('stats', () => {
    it('builds a valid StorageProgram', () => {
      const program = programCacheHandler.stats({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = programCacheHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = programCacheHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = programCacheHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = programCacheHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof programCacheHandler.stats !== 'function') return;
      const result = await interpret(programCacheHandler.stats({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof programCacheHandler.stats !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_store_new_entry = await interpret(programCacheHandler.store({ programHash: "sha256_abc123", stateHash: "sha256_def456", result: "{\"variant\":\"ok\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_store_new_entry?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(programCacheHandler.stats({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof programCacheHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = programCacheHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ProgramCache');
    });
  });

  describe('invariant examples', () => {
    it("store then lookup returns hit", async () => {
      const storage = createInMemoryStorage();
      const storeResult0 = await interpret(programCacheHandler.store({ programHash: "abc", stateHash: "def", result: "ok" }), storage);
      expect(storeResult0.variant).toBe("ok");
      let entry = storeResult0.output["entry"];
      let c = entry;
      const thenResult0 = await interpret(programCacheHandler.lookup({ programHash: "abc", stateHash: "def" }), storage);
      expect(thenResult0.variant).toBe("hit");
    });

    it("invalidateByState evicts matching entries", async () => {
      const storage = createInMemoryStorage();
      const storeResult0 = await interpret(programCacheHandler.store({ programHash: "abc", stateHash: "def", result: "ok" }), storage);
      expect(storeResult0.variant).toBe("ok");
      let entry = storeResult0.output["entry"];
      let c = entry;
      const invalidateByStateResult1 = await interpret(programCacheHandler.invalidateByState({ stateHash: "def" }), storage);
      expect(invalidateByStateResult1.variant).toBe("ok");
      let evicted = invalidateByStateResult1.output["evicted"];
      let n = evicted;
      const thenResult0 = await interpret(programCacheHandler.lookup({ programHash: "abc", stateHash: "def" }), storage);
      expect(thenResult0.variant).toBe("miss");
    });

    it("invalidateByProgram evicts matching entries", async () => {
      const storage = createInMemoryStorage();
      const storeResult0 = await interpret(programCacheHandler.store({ programHash: "abc", stateHash: "def", result: "ok" }), storage);
      expect(storeResult0.variant).toBe("ok");
      let entry = storeResult0.output["entry"];
      let c = entry;
      const invalidateByProgramResult1 = await interpret(programCacheHandler.invalidateByProgram({ programHash: "abc" }), storage);
      expect(invalidateByProgramResult1.variant).toBe("ok");
      let evicted = invalidateByProgramResult1.output["evicted"];
      let n = evicted;
      const thenResult0 = await interpret(programCacheHandler.lookup({ programHash: "abc", stateHash: "def" }), storage);
      expect(thenResult0.variant).toBe("miss");
    });

    it("duplicate store returns exists", async () => {
      const storage = createInMemoryStorage();
      const storeResult0 = await interpret(programCacheHandler.store({ programHash: "abc", stateHash: "def", result: "ok" }), storage);
      expect(storeResult0.variant).toBe("ok");
      let entry = storeResult0.output["entry"];
      let c = entry;
      const thenResult0 = await interpret(programCacheHandler.store({ programHash: "abc", stateHash: "def", result: "ok" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: cached entries have valid hashes', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('lookup'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }), stateHash: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('store'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }), stateHash: fc.string({ minLength: 1, maxLength: 50 }), result: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invalidateByState'), input: fc.record({ stateHash: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invalidateByProgram'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('stats'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = programCacheHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(programCacheHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: hit without prior store', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('lookup'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }), stateHash: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('store'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }), stateHash: fc.string({ minLength: 1, maxLength: 50 }), result: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invalidateByState'), input: fc.record({ stateHash: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invalidateByProgram'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('stats'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = programCacheHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(programCacheHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: hit without prior store
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('store handles empty input: ', async () => {
      if (typeof programCacheHandler.store !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(programCacheHandler.store({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('store ensures on ok: ', async () => {
      if (typeof programCacheHandler.store !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }), stateHash: fc.string({ minLength: 1, maxLength: 50 }), result: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = programCacheHandler.store(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('lookup handles empty input: ', async () => {
      if (typeof programCacheHandler.lookup !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(programCacheHandler.lookup({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('lookup ensures on hit: ', async () => {
      if (typeof programCacheHandler.lookup !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }), stateHash: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = programCacheHandler.lookup(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "hit") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
