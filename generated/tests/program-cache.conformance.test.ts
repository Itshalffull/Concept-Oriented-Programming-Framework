// ProgramCache Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { programCacheHandler } from '../../handlers/ts/monadic/program-cache.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('ProgramCache imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('lookup', () => {
    it('executes successfully', async () => {
      if (typeof programCacheHandler.lookup !== 'function') return;
      const result = await programCacheHandler.lookup({ programHash: 'test-programHash', stateHash: 'test-stateHash' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('store', () => {
    it('executes successfully', async () => {
      if (typeof programCacheHandler.store !== 'function') return;
      const result = await programCacheHandler.store({ programHash: 'test-programHash', stateHash: 'test-stateHash', result: 'test-result' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invalidateByState', () => {
    it('executes successfully', async () => {
      if (typeof programCacheHandler.invalidateByState !== 'function') return;
      const result = await programCacheHandler.invalidateByState({ stateHash: 'test-stateHash' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invalidateByProgram', () => {
    it('executes successfully', async () => {
      if (typeof programCacheHandler.invalidateByProgram !== 'function') return;
      const result = await programCacheHandler.invalidateByProgram({ programHash: 'test-programHash' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('stats', () => {
    it('executes successfully', async () => {
      if (typeof programCacheHandler.stats !== 'function') return;
      const result = await programCacheHandler.stats({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invariant examples', () => {
    it("store then lookup returns hit", async () => {
      const storage = createInMemoryStorage();
      const storeResult0 = await programCacheHandler.store({ programHash: {"type":"literal","value":"abc"}, stateHash: {"type":"literal","value":"def"}, result: {"type":"literal","value":"ok"} }, storage);
      expect(storeResult0.variant).toBe("ok");
      const entry = storeResult0.output["entry"];
      const thenResult0 = await programCacheHandler.lookup({ programHash: {"type":"literal","value":"abc"}, stateHash: {"type":"literal","value":"def"} }, storage);
      expect(thenResult0.variant).toBe("hit");
    });

    it("invalidateByState evicts matching entries", async () => {
      const storage = createInMemoryStorage();
      const storeResult0 = await programCacheHandler.store({ programHash: {"type":"literal","value":"abc"}, stateHash: {"type":"literal","value":"def"}, result: {"type":"literal","value":"ok"} }, storage);
      expect(storeResult0.variant).toBe("ok");
      const entry = storeResult0.output["entry"];
      const invalidateByStateResult1 = await programCacheHandler.invalidateByState({ stateHash: {"type":"literal","value":"def"} }, storage);
      expect(invalidateByStateResult1.variant).toBe("ok");
      const evicted = invalidateByStateResult1.output["evicted"];
      const thenResult0 = await programCacheHandler.lookup({ programHash: {"type":"literal","value":"abc"}, stateHash: {"type":"literal","value":"def"} }, storage);
      expect(thenResult0.variant).toBe("miss");
    });

    it("invalidateByProgram evicts matching entries", async () => {
      const storage = createInMemoryStorage();
      const storeResult0 = await programCacheHandler.store({ programHash: {"type":"literal","value":"abc"}, stateHash: {"type":"literal","value":"def"}, result: {"type":"literal","value":"ok"} }, storage);
      expect(storeResult0.variant).toBe("ok");
      const entry = storeResult0.output["entry"];
      const invalidateByProgramResult1 = await programCacheHandler.invalidateByProgram({ programHash: {"type":"literal","value":"abc"} }, storage);
      expect(invalidateByProgramResult1.variant).toBe("ok");
      const evicted = invalidateByProgramResult1.output["evicted"];
      const thenResult0 = await programCacheHandler.lookup({ programHash: {"type":"literal","value":"abc"}, stateHash: {"type":"literal","value":"def"} }, storage);
      expect(thenResult0.variant).toBe("miss");
    });

    it("duplicate store returns exists", async () => {
      const storage = createInMemoryStorage();
      const storeResult0 = await programCacheHandler.store({ programHash: {"type":"literal","value":"abc"}, stateHash: {"type":"literal","value":"def"}, result: {"type":"literal","value":"ok"} }, storage);
      expect(storeResult0.variant).toBe("ok");
      const entry = storeResult0.output["entry"];
      const thenResult0 = await programCacheHandler.store({ programHash: {"type":"literal","value":"abc"}, stateHash: {"type":"literal","value":"def"}, result: {"type":"literal","value":"ok"} }, storage);
      expect(thenResult0.variant).toBe("exists");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: cached entries have valid hashes', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('lookup'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 20 }), stateHash: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('store'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 20 }), stateHash: fc.string({ minLength: 1, maxLength: 20 }), result: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('invalidateByState'), input: fc.record({ stateHash: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('invalidateByProgram'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('stats'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = programCacheHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await actionFn.call(programCacheHandler, step.input as Record<string, unknown>, storage);
                expect(result.variant).toBeDefined();
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: hit without prior store', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('lookup'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 20 }), stateHash: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('store'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 20 }), stateHash: fc.string({ minLength: 1, maxLength: 20 }), result: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('invalidateByState'), input: fc.record({ stateHash: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('invalidateByProgram'), input: fc.record({ programHash: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('stats'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = programCacheHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await actionFn.call(programCacheHandler, step.input as Record<string, unknown>, storage);
                expect(result.variant).toBeDefined();
                // Never: hit without prior store
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('store requires: ', async () => {
      const storage = createInMemoryStorage();
      const result = await programCacheHandler.store({  }, storage);
      expect(['error', 'invalid', 'missing', 'notFound']).toContain(result.variant);
    });

    it('store ensures on ok: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }), stateHash: fc.string({ minLength: 1, maxLength: 50 }), result: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await programCacheHandler.store(input as Record<string, unknown>, storage);
            fc.pre(result.variant === "ok");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

    it('lookup requires: ', async () => {
      const storage = createInMemoryStorage();
      const result = await programCacheHandler.lookup({  }, storage);
      expect(['error', 'invalid', 'missing', 'notFound']).toContain(result.variant);
    });

    it('lookup ensures on hit: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ programHash: fc.string({ minLength: 1, maxLength: 50 }), stateHash: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await programCacheHandler.lookup(input as Record<string, unknown>, storage);
            fc.pre(result.variant === "hit");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
