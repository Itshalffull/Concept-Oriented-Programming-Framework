// RetryPolicy Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { retryPolicyHandler } from '../../handlers/ts/repertoire/process-reliability/retry-policy.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('RetryPolicy functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = retryPolicyHandler.create({ step_ref: 'test-step_ref', run_ref: 'test-run_ref', max_attempts: 1, initial_interval_ms: 1, backoff_coefficient: 1, max_interval_ms: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retryPolicyHandler.create({ step_ref: 'test-step_ref', run_ref: 'test-run_ref', max_attempts: 1, initial_interval_ms: 1, backoff_coefficient: 1, max_interval_ms: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retryPolicyHandler.create({ step_ref: 'test-step_ref', run_ref: 'test-run_ref', max_attempts: 1, initial_interval_ms: 1, backoff_coefficient: 1, max_interval_ms: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retryPolicyHandler.create({ step_ref: 'test-step_ref', run_ref: 'test-run_ref', max_attempts: 1, initial_interval_ms: 1, backoff_coefficient: 1, max_interval_ms: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retryPolicyHandler.create({ step_ref: 'test-step_ref', run_ref: 'test-run_ref', max_attempts: 1, initial_interval_ms: 1, backoff_coefficient: 1, max_interval_ms: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retryPolicyHandler.create !== 'function') return;
      const result = await interpret(retryPolicyHandler.create({ step_ref: 'test-step_ref', run_ref: 'test-run_ref', max_attempts: 1, initial_interval_ms: 1, backoff_coefficient: 1, max_interval_ms: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('should_retry', () => {
    it('builds a valid StorageProgram', () => {
      const program = retryPolicyHandler.should_retry({ policy: 'test', error: 'test-error' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retryPolicyHandler.should_retry({ policy: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retryPolicyHandler.should_retry({ policy: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retryPolicyHandler.should_retry({ policy: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retryPolicyHandler.should_retry({ policy: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retryPolicyHandler.should_retry !== 'function') return;
      const result = await interpret(retryPolicyHandler.should_retry({ policy: 'test', error: 'test-error' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('record_attempt', () => {
    it('builds a valid StorageProgram', () => {
      const program = retryPolicyHandler.record_attempt({ policy: 'test', error: 'test-error' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retryPolicyHandler.record_attempt({ policy: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retryPolicyHandler.record_attempt({ policy: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retryPolicyHandler.record_attempt({ policy: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retryPolicyHandler.record_attempt({ policy: 'test', error: 'test-error' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retryPolicyHandler.record_attempt !== 'function') return;
      const result = await interpret(retryPolicyHandler.record_attempt({ policy: 'test', error: 'test-error' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('mark_succeeded', () => {
    it('builds a valid StorageProgram', () => {
      const program = retryPolicyHandler.mark_succeeded({ policy: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = retryPolicyHandler.mark_succeeded({ policy: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = retryPolicyHandler.mark_succeeded({ policy: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = retryPolicyHandler.mark_succeeded({ policy: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = retryPolicyHandler.mark_succeeded({ policy: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof retryPolicyHandler.mark_succeeded !== 'function') return;
      const result = await interpret(retryPolicyHandler.mark_succeeded({ policy: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof retryPolicyHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = retryPolicyHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('RetryPolicy');
    });
  });

  describe('state invariants (stateful PBT)', () => {
    it('always: policies have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ step_ref: fc.string({ minLength: 1, maxLength: 50 }), run_ref: fc.string({ minLength: 1, maxLength: 50 }), max_attempts: fc.integer({ min: 1, max: 1000 }), initial_interval_ms: fc.integer({ min: 1, max: 1000 }), backoff_coefficient: fc.string(), max_interval_ms: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('should_retry'), input: fc.record({ policy: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('record_attempt'), input: fc.record({ policy: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('mark_succeeded'), input: fc.record({ policy: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = retryPolicyHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(retryPolicyHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in policies', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ step_ref: fc.string({ minLength: 1, maxLength: 50 }), run_ref: fc.string({ minLength: 1, maxLength: 50 }), max_attempts: fc.integer({ min: 1, max: 1000 }), initial_interval_ms: fc.integer({ min: 1, max: 1000 }), backoff_coefficient: fc.string(), max_interval_ms: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('should_retry'), input: fc.record({ policy: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('record_attempt'), input: fc.record({ policy: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('mark_succeeded'), input: fc.record({ policy: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = retryPolicyHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(retryPolicyHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in policies
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof retryPolicyHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(retryPolicyHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof retryPolicyHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ step_ref: fc.string({ minLength: 1, maxLength: 50 }), run_ref: fc.string({ minLength: 1, maxLength: 50 }), max_attempts: fc.integer({ min: 1, max: 1000 }), initial_interval_ms: fc.integer({ min: 1, max: 1000 }), backoff_coefficient: fc.string(), max_interval_ms: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = retryPolicyHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('should_retry handles empty input: ', async () => {
      if (typeof retryPolicyHandler.should_retry !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(retryPolicyHandler.should_retry({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('should_retry ensures on ok: ', async () => {
      if (typeof retryPolicyHandler.should_retry !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ policy: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = retryPolicyHandler.should_retry(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('record_attempt handles empty input: ', async () => {
      if (typeof retryPolicyHandler.record_attempt !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(retryPolicyHandler.record_attempt({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('record_attempt ensures on ok: ', async () => {
      if (typeof retryPolicyHandler.record_attempt !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ policy: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = retryPolicyHandler.record_attempt(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
