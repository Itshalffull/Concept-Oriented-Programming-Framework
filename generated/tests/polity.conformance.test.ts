// Polity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { polityHandler } from '../../handlers/ts/governance-structure/polity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Polity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('establish', () => {
    it('builds a valid StorageProgram', () => {
      const program = polityHandler.establish({ name: "Acme Co-op", purpose: "Democratic governance for Acme", values: ["transparency","equity"], scope: ["budgets","hiring"] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = polityHandler.establish({ name: "Acme Co-op", purpose: "Democratic governance for Acme", values: ["transparency","equity"], scope: ["budgets","hiring"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = polityHandler.establish({ name: "Acme Co-op", purpose: "Democratic governance for Acme", values: ["transparency","equity"], scope: ["budgets","hiring"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = polityHandler.establish({ name: "Acme Co-op", purpose: "Democratic governance for Acme", values: ["transparency","equity"], scope: ["budgets","hiring"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = polityHandler.establish({ name: "Acme Co-op", purpose: "Democratic governance for Acme", values: ["transparency","equity"], scope: ["budgets","hiring"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof polityHandler.establish !== 'function') return;
      const result = await interpret(polityHandler.establish({ name: "Acme Co-op", purpose: "Democratic governance for Acme", values: ["transparency","equity"], scope: ["budgets","hiring"] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_establish" -> ok', async () => {
      if (typeof polityHandler.establish !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(polityHandler.establish({ name: "Acme Co-op", purpose: "Democratic governance for Acme", values: ["transparency","equity"], scope: ["budgets","hiring"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "minimal_establish" -> ok', async () => {
      if (typeof polityHandler.establish !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(polityHandler.establish({ name: "Small Circle", purpose: "Team coordination", values: [], scope: ["sprint-planning"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_name" -> error', async () => {
      if (typeof polityHandler.establish !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(polityHandler.establish({ name: "", purpose: "No name provided", values: [], scope: [] }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('amend', () => {
    it('builds a valid StorageProgram', () => {
      const program = polityHandler.amend({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, field: "purpose", newValue: "Updated governance mission" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = polityHandler.amend({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, field: "purpose", newValue: "Updated governance mission" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = polityHandler.amend({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, field: "purpose", newValue: "Updated governance mission" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = polityHandler.amend({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, field: "purpose", newValue: "Updated governance mission" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = polityHandler.amend({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, field: "purpose", newValue: "Updated governance mission" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof polityHandler.amend !== 'function') return;
      const result = await interpret(polityHandler.amend({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, field: "purpose", newValue: "Updated governance mission" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_amend" -> ok', async () => {
      if (typeof polityHandler.amend !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_establish = await interpret(polityHandler.establish({ name: "Acme Co-op", purpose: "Democratic governance for Acme", values: ["transparency","equity"], scope: ["budgets","hiring"] }), storage);
      const result = await interpret(polityHandler.amend({ polity: afterResult_valid_establish?.output?.["id"], field: "purpose", newValue: "Updated governance mission" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "amend_nonexistent" -> error', async () => {
      if (typeof polityHandler.amend !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(polityHandler.amend({ polity: "polity-999", field: "purpose", newValue: "No such polity" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('dissolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = polityHandler.dissolve({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, reason: "Merger with partner org" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = polityHandler.dissolve({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, reason: "Merger with partner org" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = polityHandler.dissolve({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, reason: "Merger with partner org" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = polityHandler.dissolve({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, reason: "Merger with partner org" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = polityHandler.dissolve({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, reason: "Merger with partner org" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof polityHandler.dissolve !== 'function') return;
      const result = await interpret(polityHandler.dissolve({ polity: {"type":"ref","fixture":"valid_establish","field":"id"}, reason: "Merger with partner org" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_dissolve" -> ok', async () => {
      if (typeof polityHandler.dissolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_establish = await interpret(polityHandler.establish({ name: "Acme Co-op", purpose: "Democratic governance for Acme", values: ["transparency","equity"], scope: ["budgets","hiring"] }), storage);
      const result = await interpret(polityHandler.dissolve({ polity: afterResult_valid_establish?.output?.["id"], reason: "Merger with partner org" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "dissolve_nonexistent" -> error', async () => {
      if (typeof polityHandler.dissolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(polityHandler.dissolve({ polity: "polity-999", reason: "Does not exist" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof polityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = polityHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Polity');
    });
  });

  describe('invariant examples', () => {
    it("establish-then-amend", async () => {
      const storage = createInMemoryStorage();
      const establishResult0 = await interpret(polityHandler.establish({ name: "test-_", purpose: "test-_", values: "test-_", scope: "test-_" }), storage);
      expect(establishResult0.variant).toBe("ok");
      let polity = establishResult0.output["polity"];
      let g = polity;
      const thenResult0 = await interpret(polityHandler.amend({ polity: g, field: "purpose", newValue: "test-_" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('establish'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), purpose: fc.string({ minLength: 1, maxLength: 50 }), values: fc.string(), scope: fc.string() }) }),
              fc.record({ action: fc.constant('amend'), input: fc.record({ polity: fc.string(), field: fc.string({ minLength: 1, maxLength: 50 }), newValue: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dissolve'), input: fc.record({ polity: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = polityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(polityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-purpose', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('establish'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), purpose: fc.string({ minLength: 1, maxLength: 50 }), values: fc.string(), scope: fc.string() }) }),
              fc.record({ action: fc.constant('amend'), input: fc.record({ polity: fc.string(), field: fc.string({ minLength: 1, maxLength: 50 }), newValue: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dissolve'), input: fc.record({ polity: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = polityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(polityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-purpose
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('establish handles empty input: ', async () => {
      if (typeof polityHandler.establish !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(polityHandler.establish({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('establish ensures on established: ', async () => {
      if (typeof polityHandler.establish !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), purpose: fc.string({ minLength: 1, maxLength: 50 }), values: fc.string(), scope: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = polityHandler.establish(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "established") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
