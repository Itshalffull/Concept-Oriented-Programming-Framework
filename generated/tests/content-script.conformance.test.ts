// ContentScript Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { contentScriptHandler } from '../../handlers/ts/extension/browser/content-script.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ContentScript functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentScriptHandler.register({ extensionId: 'test-extensionId', urlPatterns: 'test-urlPatterns', runAt: 'test-runAt', world: 'test-world', jsFiles: 'test-jsFiles', cssFiles: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentScriptHandler.register({ extensionId: 'test-extensionId', urlPatterns: 'test-urlPatterns', runAt: 'test-runAt', world: 'test-world', jsFiles: 'test-jsFiles', cssFiles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentScriptHandler.register({ extensionId: 'test-extensionId', urlPatterns: 'test-urlPatterns', runAt: 'test-runAt', world: 'test-world', jsFiles: 'test-jsFiles', cssFiles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentScriptHandler.register({ extensionId: 'test-extensionId', urlPatterns: 'test-urlPatterns', runAt: 'test-runAt', world: 'test-world', jsFiles: 'test-jsFiles', cssFiles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentScriptHandler.register({ extensionId: 'test-extensionId', urlPatterns: 'test-urlPatterns', runAt: 'test-runAt', world: 'test-world', jsFiles: 'test-jsFiles', cssFiles: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentScriptHandler.register !== 'function') return;
      const result = await interpret(contentScriptHandler.register({ extensionId: 'test-extensionId', urlPatterns: 'test-urlPatterns', runAt: 'test-runAt', world: 'test-world', jsFiles: 'test-jsFiles', cssFiles: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('inject', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentScriptHandler.inject({ script: 'test', tabId: 'test-tabId' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentScriptHandler.inject({ script: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentScriptHandler.inject({ script: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentScriptHandler.inject({ script: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentScriptHandler.inject({ script: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentScriptHandler.inject !== 'function') return;
      const result = await interpret(contentScriptHandler.inject({ script: 'test', tabId: 'test-tabId' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('remove', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentScriptHandler.remove({ script: 'test', tabId: 'test-tabId' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentScriptHandler.remove({ script: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentScriptHandler.remove({ script: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentScriptHandler.remove({ script: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentScriptHandler.remove({ script: 'test', tabId: 'test-tabId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentScriptHandler.remove !== 'function') return;
      const result = await interpret(contentScriptHandler.remove({ script: 'test', tabId: 'test-tabId' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('sendMessage', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentScriptHandler.sendMessage({ script: 'test', tabId: 'test-tabId', message: 'test-message' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentScriptHandler.sendMessage({ script: 'test', tabId: 'test-tabId', message: 'test-message' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentScriptHandler.sendMessage({ script: 'test', tabId: 'test-tabId', message: 'test-message' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentScriptHandler.sendMessage({ script: 'test', tabId: 'test-tabId', message: 'test-message' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentScriptHandler.sendMessage({ script: 'test', tabId: 'test-tabId', message: 'test-message' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentScriptHandler.sendMessage !== 'function') return;
      const result = await interpret(contentScriptHandler.sendMessage({ script: 'test', tabId: 'test-tabId', message: 'test-message' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('listInjected', () => {
    it('builds a valid StorageProgram', () => {
      const program = contentScriptHandler.listInjected({ extensionId: 'test-extensionId' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = contentScriptHandler.listInjected({ extensionId: 'test-extensionId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = contentScriptHandler.listInjected({ extensionId: 'test-extensionId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = contentScriptHandler.listInjected({ extensionId: 'test-extensionId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = contentScriptHandler.listInjected({ extensionId: 'test-extensionId' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof contentScriptHandler.listInjected !== 'function') return;
      const result = await interpret(contentScriptHandler.listInjected({ extensionId: 'test-extensionId' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });


  describe('invariant examples', () => {
    it("register-then-listInjected", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(contentScriptHandler.register({ extensionId: "test-_", urlPatterns: "test-_", runAt: "test-_", world: "test-_", jsFiles: "test-_", cssFiles: "test-_" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let script = registerResult0.output["script"];
      let s = script;
      const thenResult0 = await interpret(contentScriptHandler.inject({ script: s, tabId: "test-_" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(contentScriptHandler.listInjected({ extensionId: "test-_" }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-urlPatterns', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ extensionId: fc.string({ minLength: 1, maxLength: 50 }), urlPatterns: fc.string({ minLength: 1, maxLength: 50 }), runAt: fc.string({ minLength: 1, maxLength: 50 }), world: fc.string({ minLength: 1, maxLength: 50 }), jsFiles: fc.string({ minLength: 1, maxLength: 50 }), cssFiles: fc.string() }) }),
              fc.record({ action: fc.constant('inject'), input: fc.record({ script: fc.string(), tabId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ script: fc.string(), tabId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sendMessage'), input: fc.record({ script: fc.string(), tabId: fc.string({ minLength: 1, maxLength: 50 }), message: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listInjected'), input: fc.record({ extensionId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = contentScriptHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(contentScriptHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-runAt', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ extensionId: fc.string({ minLength: 1, maxLength: 50 }), urlPatterns: fc.string({ minLength: 1, maxLength: 50 }), runAt: fc.string({ minLength: 1, maxLength: 50 }), world: fc.string({ minLength: 1, maxLength: 50 }), jsFiles: fc.string({ minLength: 1, maxLength: 50 }), cssFiles: fc.string() }) }),
              fc.record({ action: fc.constant('inject'), input: fc.record({ script: fc.string(), tabId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ script: fc.string(), tabId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sendMessage'), input: fc.record({ script: fc.string(), tabId: fc.string({ minLength: 1, maxLength: 50 }), message: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listInjected'), input: fc.record({ extensionId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = contentScriptHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(contentScriptHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-runAt
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof contentScriptHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(contentScriptHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof contentScriptHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ extensionId: fc.string({ minLength: 1, maxLength: 50 }), urlPatterns: fc.string({ minLength: 1, maxLength: 50 }), runAt: fc.string({ minLength: 1, maxLength: 50 }), world: fc.string({ minLength: 1, maxLength: 50 }), jsFiles: fc.string({ minLength: 1, maxLength: 50 }), cssFiles: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = contentScriptHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
