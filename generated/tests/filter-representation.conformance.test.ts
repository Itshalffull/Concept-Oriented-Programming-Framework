// FilterRepresentation Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { filterRepresentationHandler } from '../../handlers/ts/view/filter-representation.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('FilterRepresentation functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('registerKind', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterRepresentationHandler.registerKind({ kind: "toggle-group" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterRepresentationHandler.registerKind({ kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterRepresentationHandler.registerKind({ kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterRepresentationHandler.registerKind({ kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterRepresentationHandler.registerKind({ kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterRepresentationHandler.registerKind !== 'function') return;
      const result = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_toggle_group" -> ok', async () => {
      if (typeof filterRepresentationHandler.registerKind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_url_params" -> ok', async () => {
      if (typeof filterRepresentationHandler.registerKind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterRepresentationHandler.registerKind({ kind: "url-params" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty" -> error', async () => {
      if (typeof filterRepresentationHandler.registerKind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterRepresentationHandler.registerKind({ kind: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof filterRepresentationHandler.registerKind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_toggle_group = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      const _pool = Object.assign({}, (afterResult_register_toggle_group?.output ?? {}));
      const _fixtureInput = { kind: "toggle-group" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterRepresentationHandler.registerKind({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterRepresentationHandler.register({ name: "default-toggle", kind: "toggle-group" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterRepresentationHandler.register({ name: "default-toggle", kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterRepresentationHandler.register({ name: "default-toggle", kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterRepresentationHandler.register({ name: "default-toggle", kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterRepresentationHandler.register({ name: "default-toggle", kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterRepresentationHandler.register !== 'function') return;
      const result = await interpret(filterRepresentationHandler.register({ name: "default-toggle", kind: "toggle-group" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_default_toggle" -> ok', async () => {
      if (typeof filterRepresentationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_toggle_group = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      const _pool = Object.assign({}, (afterResult_register_toggle_group?.output ?? {}));
      const _fixtureInput = { name: "default-toggle", kind: "toggle-group" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterRepresentationHandler.register({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate_name" -> duplicate', async () => {
      if (typeof filterRepresentationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_toggle_group = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      const afterResult_register_default_toggle = await interpret(filterRepresentationHandler.register({ name: "default-toggle", kind: "toggle-group" }), storage);
      const _pool = Object.assign({}, (afterResult_register_toggle_group?.output ?? {}), (afterResult_register_default_toggle?.output ?? {}));
      const _fixtureInput = { name: "default-toggle", kind: "toggle-group" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterRepresentationHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "register_empty_name" -> error', async () => {
      if (typeof filterRepresentationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterRepresentationHandler.register({ name: "", kind: "toggle-group" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('parse', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterRepresentationHandler.parse({ repr: {"schemas":["Concept","Sync"]}, kind: "toggle-group" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterRepresentationHandler.parse({ repr: {"schemas":["Concept","Sync"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterRepresentationHandler.parse({ repr: {"schemas":["Concept","Sync"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterRepresentationHandler.parse({ repr: {"schemas":["Concept","Sync"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterRepresentationHandler.parse({ repr: {"schemas":["Concept","Sync"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterRepresentationHandler.parse !== 'function') return;
      const result = await interpret(filterRepresentationHandler.parse({ repr: {"schemas":["Concept","Sync"]}, kind: "toggle-group" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "parse_toggle" -> ok', async () => {
      if (typeof filterRepresentationHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_toggle_group = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      const _pool = Object.assign({}, (afterResult_register_toggle_group?.output ?? {}));
      const _fixtureInput = { repr: {"schemas":["Concept","Sync"]}, kind: "toggle-group" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterRepresentationHandler.parse({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "parse_unknown_kind" -> error', async () => {
      if (typeof filterRepresentationHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterRepresentationHandler.parse({ repr: {}, kind: "unknown" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('print', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterRepresentationHandler.print({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterRepresentationHandler.print({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterRepresentationHandler.print({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterRepresentationHandler.print({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterRepresentationHandler.print({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterRepresentationHandler.print !== 'function') return;
      const result = await interpret(filterRepresentationHandler.print({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "print_toggle" -> ok', async () => {
      if (typeof filterRepresentationHandler.print !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_toggle_group = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      const _pool = Object.assign({}, (afterResult_register_toggle_group?.output ?? {}));
      const _fixtureInput = { tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterRepresentationHandler.print({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "print_unknown_kind" -> error', async () => {
      if (typeof filterRepresentationHandler.print !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(filterRepresentationHandler.print({ tree: {}, kind: "unknown" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('canPrint', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterRepresentationHandler.canPrint({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterRepresentationHandler.canPrint({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterRepresentationHandler.canPrint({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterRepresentationHandler.canPrint({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterRepresentationHandler.canPrint({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterRepresentationHandler.canPrint !== 'function') return;
      const result = await interpret(filterRepresentationHandler.canPrint({ tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "can_print_simple" -> ok', async () => {
      if (typeof filterRepresentationHandler.canPrint !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_toggle_group = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      const _pool = Object.assign({}, (afterResult_register_toggle_group?.output ?? {}));
      const _fixtureInput = { tree: {"type":"in","field":"schemas","values":["Concept"]}, kind: "toggle-group" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterRepresentationHandler.canPrint({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "can_print_complex" -> ok', async () => {
      if (typeof filterRepresentationHandler.canPrint !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_toggle_group = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      const _pool = Object.assign({}, (afterResult_register_toggle_group?.output ?? {}));
      const _fixtureInput = { tree: {"type":"and","conditions":[{"type":"or","conditions":[]}]}, kind: "toggle-group" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(filterRepresentationHandler.canPrint({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = filterRepresentationHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = filterRepresentationHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = filterRepresentationHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = filterRepresentationHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = filterRepresentationHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof filterRepresentationHandler.list !== 'function') return;
      const result = await interpret(filterRepresentationHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof filterRepresentationHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_toggle_group = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      const _pool = Object.assign({}, (afterResult_register_toggle_group?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(filterRepresentationHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("registerKind then register provider succeeds", async () => {
      const storage = createInMemoryStorage();
      const registerKindResult0 = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      expect(registerKindResult0.variant).toBe("ok");
      let kind = registerKindResult0.output["kind"];
      let k = kind;
      const thenResult0 = await interpret(filterRepresentationHandler.register({ name: "default-toggle", kind: "toggle-group" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("parse then print round-trips", async () => {
      const storage = createInMemoryStorage();
    });

    it("duplicate registerKind returns duplicate", async () => {
      const storage = createInMemoryStorage();
      const registerKindResult0 = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      expect(registerKindResult0.variant).toBe("ok");
      let kind = registerKindResult0.output["kind"];
      let k = kind;
      const thenResult0 = await interpret(filterRepresentationHandler.registerKind({ kind: "toggle-group" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

    it("parse with unknown kind returns error", async () => {
      const storage = createInMemoryStorage();
    });

    it("list returns registered representations", async () => {
      const storage = createInMemoryStorage();
      const registerKindResult0 = await interpret(filterRepresentationHandler.registerKind({ kind: "url-params" }), storage);
      expect(registerKindResult0.variant).toBe("ok");
      let kind = registerKindResult0.output["kind"];
      let k = kind;
      const registerResult1 = await interpret(filterRepresentationHandler.register({ name: "url-repr", kind: "url-params" }), storage);
      expect(registerResult1.variant).toBe("ok");
      let representation = registerResult1.output["representation"];
      let r = representation;
      const thenResult0 = await interpret(filterRepresentationHandler.list({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: representations have non-empty names', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerKind'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('parse'), input: fc.record({ repr: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('print'), input: fc.record({ tree: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('canPrint'), input: fc.record({ tree: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = filterRepresentationHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(filterRepresentationHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: representation kinds are registered', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerKind'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('parse'), input: fc.record({ repr: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('print'), input: fc.record({ tree: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('canPrint'), input: fc.record({ tree: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = filterRepresentationHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(filterRepresentationHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
