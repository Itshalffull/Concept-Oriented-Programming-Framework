// TechnicalDebt Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { technicalDebtHandler } from '../../handlers/ts/quality-policy/technical-debt.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('TechnicalDebt functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('assess', () => {
    it('builds a valid StorageProgram', () => {
      const program = technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof technicalDebtHandler.assess !== 'function') return;
      const result = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "assess_auth_login" -> ok', async () => {
      if (typeof technicalDebtHandler.assess !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "assess_data_layer" -> ok', async () => {
      if (typeof technicalDebtHandler.assess !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(technicalDebtHandler.assess({ findingRef: "f-002", target: "data/query.ts", principal: "16.0", principalUnit: "hours", interest: "2.0", interestPeriod: "weekly" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "assess_trivial" -> ok', async () => {
      if (typeof technicalDebtHandler.assess !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(technicalDebtHandler.assess({ findingRef: "f-003", target: "utils/format.ts", principal: "0.25", principalUnit: "hours", interest: "0.1", interestPeriod: "monthly" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "assess_duplicate" -> duplicate', async () => {
      if (typeof technicalDebtHandler.assess !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_auth_login = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_auth_login?.output ?? {}));
      const _fixtureInput = { findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(technicalDebtHandler.assess({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('prioritize', () => {
    it('builds a valid StorageProgram', () => {
      const program = technicalDebtHandler.prioritize({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = technicalDebtHandler.prioritize({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = technicalDebtHandler.prioritize({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = technicalDebtHandler.prioritize({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = technicalDebtHandler.prioritize({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof technicalDebtHandler.prioritize !== 'function') return;
      const result = await interpret(technicalDebtHandler.prioritize({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "prioritize_all" -> ok', async () => {
      if (typeof technicalDebtHandler.prioritize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_auth_login = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_auth_login?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(technicalDebtHandler.prioritize({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "prioritize_filtered" -> ok', async () => {
      if (typeof technicalDebtHandler.prioritize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_auth_login = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_auth_login?.output ?? {}));
      const _fixtureInput = { targets: ["auth/login.ts"], limit: "5" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(technicalDebtHandler.prioritize({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('summary', () => {
    it('builds a valid StorageProgram', () => {
      const program = technicalDebtHandler.summary({ groupBy: "target" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = technicalDebtHandler.summary({ groupBy: "target" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = technicalDebtHandler.summary({ groupBy: "target" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = technicalDebtHandler.summary({ groupBy: "target" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = technicalDebtHandler.summary({ groupBy: "target" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof technicalDebtHandler.summary !== 'function') return;
      const result = await interpret(technicalDebtHandler.summary({ groupBy: "target" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "summary_by_target" -> ok', async () => {
      if (typeof technicalDebtHandler.summary !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_auth_login = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_auth_login?.output ?? {}));
      const _fixtureInput = { groupBy: "target" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(technicalDebtHandler.summary({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "summary_default" -> ok', async () => {
      if (typeof technicalDebtHandler.summary !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_auth_login = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_auth_login?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(technicalDebtHandler.summary({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('retire', () => {
    it('builds a valid StorageProgram', () => {
      const program = technicalDebtHandler.retire({ findingRef: "f-001" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = technicalDebtHandler.retire({ findingRef: "f-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = technicalDebtHandler.retire({ findingRef: "f-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = technicalDebtHandler.retire({ findingRef: "f-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = technicalDebtHandler.retire({ findingRef: "f-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof technicalDebtHandler.retire !== 'function') return;
      const result = await interpret(technicalDebtHandler.retire({ findingRef: "f-001" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "retire_existing" -> ok', async () => {
      if (typeof technicalDebtHandler.retire !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_auth_login = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_auth_login?.output ?? {}));
      const _fixtureInput = { findingRef: "f-001" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(technicalDebtHandler.retire({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "retire_missing" -> notFound', async () => {
      if (typeof technicalDebtHandler.retire !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(technicalDebtHandler.retire({ findingRef: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('recalculateInterest', () => {
    it('builds a valid StorageProgram', () => {
      const program = technicalDebtHandler.recalculateInterest({ findingRef: "f-001", changeFrequency: "3.5" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = technicalDebtHandler.recalculateInterest({ findingRef: "f-001", changeFrequency: "3.5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = technicalDebtHandler.recalculateInterest({ findingRef: "f-001", changeFrequency: "3.5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = technicalDebtHandler.recalculateInterest({ findingRef: "f-001", changeFrequency: "3.5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = technicalDebtHandler.recalculateInterest({ findingRef: "f-001", changeFrequency: "3.5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof technicalDebtHandler.recalculateInterest !== 'function') return;
      const result = await interpret(technicalDebtHandler.recalculateInterest({ findingRef: "f-001", changeFrequency: "3.5" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "recalculate_existing" -> ok', async () => {
      if (typeof technicalDebtHandler.recalculateInterest !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_assess_auth_login = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: "4.0", principalUnit: "hours", interest: "0.5", interestPeriod: "weekly" }), storage);
      const _pool = Object.assign({}, (afterResult_assess_auth_login?.output ?? {}));
      const _fixtureInput = { findingRef: "f-001", changeFrequency: "3.5" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(technicalDebtHandler.recalculateInterest({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "recalculate_missing" -> notFound', async () => {
      if (typeof technicalDebtHandler.recalculateInterest !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(technicalDebtHandler.recalculateInterest({ findingRef: "nonexistent", changeFrequency: "1.0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof technicalDebtHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = technicalDebtHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('TechnicalDebt');
    });
  });

  describe('invariant examples', () => {
    it("assess then retire removes from totals", async () => {
      const storage = createInMemoryStorage();
      const assessResult0 = await interpret(technicalDebtHandler.assess({ findingRef: "f-001", target: "auth/login.ts", principal: 4, principalUnit: "hours", interest: 0.5, interestPeriod: "weekly" }), storage);
      expect(assessResult0.variant).toBe("ok");
      let debt = assessResult0.output["debt"];
      let d = debt;
      let breakEvenDays = assessResult0.output["breakEvenDays"];
      const retireResult1 = await interpret(technicalDebtHandler.retire({ findingRef: "f-001" }), storage);
      expect(retireResult1.variant).toBe("ok");
      debt = retireResult1.output["debt"];
      let principalSaved = retireResult1.output["principalSaved"];
      const thenResult0 = await interpret(technicalDebtHandler.summary({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("prioritize ranks by ROI", async () => {
      const storage = createInMemoryStorage();
      const assessResult0 = await interpret(technicalDebtHandler.assess({ findingRef: "f-low-roi", target: "low.ts", principal: 16, principalUnit: "hours", interest: 0.5, interestPeriod: "weekly" }), storage);
      expect(assessResult0.variant).toBe("ok");
      let debt = assessResult0.output["debt"];
      let d1 = debt;
      const assessResult1 = await interpret(technicalDebtHandler.assess({ findingRef: "f-high-roi", target: "high.ts", principal: 1, principalUnit: "hours", interest: 2, interestPeriod: "weekly" }), storage);
      expect(assessResult1.variant).toBe("ok");
      debt = assessResult1.output["debt"];
      let d2 = debt;
      const thenResult0 = await interpret(technicalDebtHandler.prioritize({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('forall properties (PBT)', () => {
    it('forall: unique finding refs', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.constantFrom("f-001", "f-002", "f-003"),
          async (f) => {
            const storage = createInMemoryStorage();
            const result = await interpret(technicalDebtHandler.assess({ findingRef: f, target: "x.ts", principal: 1, principalUnit: "hours", interest: 0.1, interestPeriod: "weekly" }), storage);
            expect(result.variant).toBeDefined();
            expect(result.variant).toBe("ok");
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
