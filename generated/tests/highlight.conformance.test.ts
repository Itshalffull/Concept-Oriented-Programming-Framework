// Highlight Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { highlightHandler } from '../../handlers/ts/app/highlight.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Highlight functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof highlightHandler.register !== 'function') return;
      const result = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_shiki" -> ok', async () => {
      if (typeof highlightHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_katex" -> ok', async () => {
      if (typeof highlightHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(highlightHandler.register({ provider: "katex", language: "latex", config: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof highlightHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_shiki = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_shiki?.output ?? {}));
      const _fixtureInput = { provider: "shiki", language: "typescript", config: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(highlightHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "register_empty_provider" -> error', async () => {
      if (typeof highlightHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(highlightHandler.register({ provider: "", language: "typescript", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_empty_language" -> error', async () => {
      if (typeof highlightHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(highlightHandler.register({ provider: "shiki", language: "", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('highlight', () => {
    it('builds a valid StorageProgram', () => {
      const program = highlightHandler.highlight({ language: "typescript", text: "const x = 1;" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = highlightHandler.highlight({ language: "typescript", text: "const x = 1;" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = highlightHandler.highlight({ language: "typescript", text: "const x = 1;" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = highlightHandler.highlight({ language: "typescript", text: "const x = 1;" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = highlightHandler.highlight({ language: "typescript", text: "const x = 1;" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof highlightHandler.highlight !== 'function') return;
      const result = await interpret(highlightHandler.highlight({ language: "typescript", text: "const x = 1;" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "highlight_ts" -> ok', async () => {
      if (typeof highlightHandler.highlight !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_shiki = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_shiki?.output ?? {}));
      const _fixtureInput = { language: "typescript", text: "const x = 1;" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(highlightHandler.highlight({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "highlight_latex" -> ok', async () => {
      if (typeof highlightHandler.highlight !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_katex = await interpret(highlightHandler.register({ provider: "katex", language: "latex", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_katex?.output ?? {}));
      const _fixtureInput = { language: "latex", text: "E = mc^2" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(highlightHandler.highlight({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "highlight_unknown" -> no_provider', async () => {
      if (typeof highlightHandler.highlight !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(highlightHandler.highlight({ language: "cobol", text: "IDENTIFICATION DIVISION." }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('no_provider'));
    });

    it('fixture "highlight_empty_language" -> error', async () => {
      if (typeof highlightHandler.highlight !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(highlightHandler.highlight({ language: "", text: "hello" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('deregister', () => {
    it('builds a valid StorageProgram', () => {
      const program = highlightHandler.deregister({ provider: "shiki", language: "typescript" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = highlightHandler.deregister({ provider: "shiki", language: "typescript" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = highlightHandler.deregister({ provider: "shiki", language: "typescript" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = highlightHandler.deregister({ provider: "shiki", language: "typescript" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = highlightHandler.deregister({ provider: "shiki", language: "typescript" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof highlightHandler.deregister !== 'function') return;
      const result = await interpret(highlightHandler.deregister({ provider: "shiki", language: "typescript" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "deregister_ok" -> ok', async () => {
      if (typeof highlightHandler.deregister !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_shiki = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_shiki?.output ?? {}));
      const _fixtureInput = { provider: "shiki", language: "typescript" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(highlightHandler.deregister({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "deregister_not_found" -> not_found', async () => {
      if (typeof highlightHandler.deregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(highlightHandler.deregister({ provider: "shiki", language: "cobol" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

    it('fixture "deregister_empty_language" -> error', async () => {
      if (typeof highlightHandler.deregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(highlightHandler.deregister({ provider: "shiki", language: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listLanguages', () => {
    it('builds a valid StorageProgram', () => {
      const program = highlightHandler.listLanguages({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = highlightHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = highlightHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = highlightHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = highlightHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof highlightHandler.listLanguages !== 'function') return;
      const result = await interpret(highlightHandler.listLanguages({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_none" -> ok', async () => {
      if (typeof highlightHandler.listLanguages !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_shiki = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_shiki?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(highlightHandler.listLanguages({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_after_register" -> ok', async () => {
      if (typeof highlightHandler.listLanguages !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_shiki = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "{}" }), storage);
      const afterResult_register_katex = await interpret(highlightHandler.register({ provider: "katex", language: "latex", config: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_shiki?.output ?? {}), (afterResult_register_katex?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(highlightHandler.listLanguages({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then highlight", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "test-b" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let h = id;
      const thenResult0 = await interpret(highlightHandler.highlight({ language: "typescript", text: "const x = 1;" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate registration rejected", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "test-b" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let h = id;
      const thenResult0 = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "test-b" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

    it("unknown language returns no_provider", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "test-b" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let h = id;
      const thenResult0 = await interpret(highlightHandler.highlight({ language: "python", text: "x = 1" }), storage);
      expect(thenResult0.variant).toBe("no_provider");
    });

    it("listLanguages reflects registrations", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(highlightHandler.register({ provider: "shiki", language: "typescript", config: "test-b1" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let h1 = id;
      const registerResult1 = await interpret(highlightHandler.register({ provider: "katex", language: "latex", config: "test-b2" }), storage);
      expect(registerResult1.variant).toBe("ok");
      id = registerResult1.output["id"];
      let h2 = id;
      const thenResult0 = await interpret(highlightHandler.listLanguages({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: orphaned provider record', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('highlight'), input: fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deregister'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listLanguages'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = highlightHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(highlightHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned provider record
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: provider name absent', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('highlight'), input: fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deregister'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listLanguages'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = highlightHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(highlightHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: provider name absent
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof highlightHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(highlightHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof highlightHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = highlightHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('highlight handles empty input: ', async () => {
      if (typeof highlightHandler.highlight !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(highlightHandler.highlight({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('highlight ensures on ok: ', async () => {
      if (typeof highlightHandler.highlight !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = highlightHandler.highlight(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
