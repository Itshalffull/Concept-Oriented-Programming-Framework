// TreeSitterComplexityProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { treeSitterComplexityProviderHandler } from '../../handlers/ts/quality-measurement/providers/tree-sitter-complexity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('TreeSitterComplexityProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = treeSitterComplexityProviderHandler.register({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = treeSitterComplexityProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = treeSitterComplexityProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = treeSitterComplexityProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = treeSitterComplexityProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof treeSitterComplexityProviderHandler.register !== 'function') return;
      const result = await interpret(treeSitterComplexityProviderHandler.register({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('compute', () => {
    it('builds a valid StorageProgram', () => {
      const program = treeSitterComplexityProviderHandler.compute({ source: "function f() { if (x) return 1; return 0; }", language: "typescript", target: "f.ts:f" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = treeSitterComplexityProviderHandler.compute({ source: "function f() { if (x) return 1; return 0; }", language: "typescript", target: "f.ts:f" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = treeSitterComplexityProviderHandler.compute({ source: "function f() { if (x) return 1; return 0; }", language: "typescript", target: "f.ts:f" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = treeSitterComplexityProviderHandler.compute({ source: "function f() { if (x) return 1; return 0; }", language: "typescript", target: "f.ts:f" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = treeSitterComplexityProviderHandler.compute({ source: "function f() { if (x) return 1; return 0; }", language: "typescript", target: "f.ts:f" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof treeSitterComplexityProviderHandler.compute !== 'function') return;
      const result = await interpret(treeSitterComplexityProviderHandler.compute({ source: "function f() { if (x) return 1; return 0; }", language: "typescript", target: "f.ts:f" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compute_simple" -> ok', async () => {
      if (typeof treeSitterComplexityProviderHandler.compute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(treeSitterComplexityProviderHandler.compute({ source: "function f() { if (x) return 1; return 0; }", language: "typescript", target: "f.ts:f" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compute_empty" -> error', async () => {
      if (typeof treeSitterComplexityProviderHandler.compute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(treeSitterComplexityProviderHandler.compute({ source: "", language: "typescript", target: "f.ts:f" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "compute_unsupported" -> error', async () => {
      if (typeof treeSitterComplexityProviderHandler.compute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(treeSitterComplexityProviderHandler.compute({ source: "def f(): pass", language: "cobol", target: "f.cobol:f" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof treeSitterComplexityProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = treeSitterComplexityProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('TreeSitterComplexityProvider');
    });
  });

  describe('invariant examples', () => {
    it("register then compute", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(treeSitterComplexityProviderHandler.register({  }), storage);
      expect(registerResult0.variant).toBe("ok");
      let name = registerResult0.output["name"];
      let metricName = registerResult0.output["metricName"];
      let language = registerResult0.output["language"];
      let _ = language;
      const thenResult0 = await interpret(treeSitterComplexityProviderHandler.compute({ source: "function handleLogin(user, pass) { if (!user) return; if (!pass) return; return true; }", language: "typescript", target: "auth.ts:handleLogin" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: empty registration name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('compute'), input: fc.record({ source: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = treeSitterComplexityProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(treeSitterComplexityProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty registration name
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
