// TestGen Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { testGenHandler } from '../../handlers/ts/framework/test/test-gen.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('TestGen functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('generate', () => {
    it('builds a valid StorageProgram', () => {
      const program = testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof testGenHandler.generate !== 'function') return;
      const result = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "generate_typescript" -> ok', async () => {
      if (typeof testGenHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "generate_rust" -> ok', async () => {
      if (typeof testGenHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Auth", language: "rust", invariant_version: "v2" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invalid_language" -> invalid', async () => {
      if (typeof testGenHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "python", invariant_version: "v1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "missing_ref" -> invalid', async () => {
      if (typeof testGenHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(testGenHandler.generate({ concept_ref: "", language: "typescript", invariant_version: "v1" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('buildTestPlan', () => {
    it('builds a valid StorageProgram', () => {
      const program = testGenHandler.buildTestPlan({ concept_ref: "clef/concept/User", concept_data: "{\"name\":\"User\",\"actions\":[],\"invariants\":[]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = testGenHandler.buildTestPlan({ concept_ref: "clef/concept/User", concept_data: "{\"name\":\"User\",\"actions\":[],\"invariants\":[]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = testGenHandler.buildTestPlan({ concept_ref: "clef/concept/User", concept_data: "{\"name\":\"User\",\"actions\":[],\"invariants\":[]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = testGenHandler.buildTestPlan({ concept_ref: "clef/concept/User", concept_data: "{\"name\":\"User\",\"actions\":[],\"invariants\":[]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = testGenHandler.buildTestPlan({ concept_ref: "clef/concept/User", concept_data: "{\"name\":\"User\",\"actions\":[],\"invariants\":[]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof testGenHandler.buildTestPlan !== 'function') return;
      const result = await interpret(testGenHandler.buildTestPlan({ concept_ref: "clef/concept/User", concept_data: "{\"name\":\"User\",\"actions\":[],\"invariants\":[]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "build_plan_basic" -> ok', async () => {
      if (typeof testGenHandler.buildTestPlan !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { concept_ref: "clef/concept/User", concept_data: "{\"name\":\"User\",\"actions\":[],\"invariants\":[]}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenHandler.buildTestPlan({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "build_plan_missing_ref" -> invalid', async () => {
      if (typeof testGenHandler.buildTestPlan !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { concept_ref: "", concept_data: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenHandler.buildTestPlan({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('regenerate', () => {
    it('builds a valid StorageProgram', () => {
      const program = testGenHandler.regenerate({ generation: "tg-abc123" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = testGenHandler.regenerate({ generation: "tg-abc123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = testGenHandler.regenerate({ generation: "tg-abc123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = testGenHandler.regenerate({ generation: "tg-abc123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = testGenHandler.regenerate({ generation: "tg-abc123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof testGenHandler.regenerate !== 'function') return;
      const result = await interpret(testGenHandler.regenerate({ generation: "tg-abc123" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "regenerate_existing" -> ok', async () => {
      if (typeof testGenHandler.regenerate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { generation: "tg-abc123" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenHandler.regenerate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "regenerate_missing" -> notfound', async () => {
      if (typeof testGenHandler.regenerate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { generation: "tg-nonexistent" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenHandler.regenerate({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = testGenHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = testGenHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = testGenHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = testGenHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = testGenHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof testGenHandler.list !== 'function') return;
      const result = await interpret(testGenHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof testGenHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(testGenHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_concept" -> ok', async () => {
      if (typeof testGenHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { concept_ref: "clef/concept/Password" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = testGenHandler.configure({ generation: "tg-abc123", num_runs: "50000" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = testGenHandler.configure({ generation: "tg-abc123", num_runs: "50000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = testGenHandler.configure({ generation: "tg-abc123", num_runs: "50000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = testGenHandler.configure({ generation: "tg-abc123", num_runs: "50000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = testGenHandler.configure({ generation: "tg-abc123", num_runs: "50000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof testGenHandler.configure !== 'function') return;
      const result = await interpret(testGenHandler.configure({ generation: "tg-abc123", num_runs: "50000" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "configure_runs" -> ok', async () => {
      if (typeof testGenHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { generation: "tg-abc123", num_runs: "50000" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenHandler.configure({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "configure_fuzz" -> ok', async () => {
      if (typeof testGenHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { generation: "tg-abc123", fuzz_duration_s: "120", shrink_enabled: "true" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenHandler.configure({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('coverage', () => {
    it('builds a valid StorageProgram', () => {
      const program = testGenHandler.coverage({ concept_ref: "clef/concept/Password" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = testGenHandler.coverage({ concept_ref: "clef/concept/Password" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = testGenHandler.coverage({ concept_ref: "clef/concept/Password" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = testGenHandler.coverage({ concept_ref: "clef/concept/Password" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = testGenHandler.coverage({ concept_ref: "clef/concept/Password" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof testGenHandler.coverage !== 'function') return;
      const result = await interpret(testGenHandler.coverage({ concept_ref: "clef/concept/Password" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "coverage_password" -> ok', async () => {
      if (typeof testGenHandler.coverage !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { concept_ref: "clef/concept/Password" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenHandler.coverage({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "coverage_missing" -> error', async () => {
      if (typeof testGenHandler.coverage !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_generate_typescript = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      const _pool = Object.assign({}, (afterResult_generate_typescript?.output ?? {}));
      const _fixtureInput = { concept_ref: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(testGenHandler.coverage({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof testGenHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = testGenHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('TestGen');
    });
  });

  describe('invariant examples', () => {
    it("generate produces strategies for all constructs", async () => {
      const storage = createInMemoryStorage();
      const generateResult0 = await interpret(testGenHandler.generate({ concept_ref: "clef/concept/Password", language: "typescript", invariant_version: "v1" }), storage);
      expect(generateResult0.variant).toBe("ok");
      let generation = generateResult0.output["generation"];
      let generated_files = generateResult0.output["generated_files"];
      let provider_used = generateResult0.output["provider_used"];
      let strategies = generateResult0.output["strategies"];
      const thenResult0 = await interpret(testGenHandler.list({ concept_ref: "clef/concept/Password", language: "typescript" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("buildTestPlan returns structured plan", async () => {
      const storage = createInMemoryStorage();
      const buildTestPlanResult0 = await interpret(testGenHandler.buildTestPlan({ concept_ref: "clef/concept/User", concept_data: "{}" }), storage);
      expect(buildTestPlanResult0.variant).toBe("ok");
      let test_plan = buildTestPlanResult0.output["test_plan"];
      let provider = buildTestPlanResult0.output["provider"];
      let language = buildTestPlanResult0.output["language"];
    });

  });

  describe('forall properties (PBT)', () => {
    it('forall: valid languages accepted', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.constantFrom("typescript", "rust", "swift", "solidity"),
          async (lang) => {
            const storage = createInMemoryStorage();
            const result = await interpret(testGenHandler.generate({ concept_ref: {"type":"literal","value":"clef/concept/Test"}, language: {"type":"variable","name":"lang"}, invariant_version: {"type":"literal","value":"v1"} }), storage);
            expect(result.variant).toBeDefined();
            expect(result.variant).toBe("ok");
          },
        ),
        { numRuns: 100 },
      );
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: invalid language accepted', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('generate'), input: fc.record({ concept_ref: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), invariant_version: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('buildTestPlan'), input: fc.record({ concept_ref: fc.string({ minLength: 1, maxLength: 50 }), concept_data: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('regenerate'), input: fc.record({ generation: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ concept_ref: fc.string(), language: fc.string() }) }),
              fc.record({ action: fc.constant('configure'), input: fc.record({ generation: fc.string(), num_runs: fc.string(), fuzz_duration_s: fc.string(), shrink_enabled: fc.string(), stateful_test_depth: fc.string() }) }),
              fc.record({ action: fc.constant('coverage'), input: fc.record({ concept_ref: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = testGenHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(testGenHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: invalid language accepted
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
