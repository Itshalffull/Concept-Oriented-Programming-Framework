// PersonaCompiler Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { personaCompilerHandler } from '../../handlers/ts/app/persona-compiler.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('PersonaCompiler functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('compile', () => {
    it('builds a valid StorageProgram', () => {
      const program = personaCompilerHandler.compile({ personaPageId: "persona-research-bot" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = personaCompilerHandler.compile({ personaPageId: "persona-research-bot" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = personaCompilerHandler.compile({ personaPageId: "persona-research-bot" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = personaCompilerHandler.compile({ personaPageId: "persona-research-bot" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = personaCompilerHandler.compile({ personaPageId: "persona-research-bot" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof personaCompilerHandler.compile !== 'function') return;
      const result = await interpret(personaCompilerHandler.compile({ personaPageId: "persona-research-bot" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compile_persona" -> ok', async () => {
      if (typeof personaCompilerHandler.compile !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(personaCompilerHandler.compile({ personaPageId: "persona-research-bot" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compile_missing" -> notfound', async () => {
      if (typeof personaCompilerHandler.compile !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(personaCompilerHandler.compile({ personaPageId: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "compile_empty" -> invalid', async () => {
      if (typeof personaCompilerHandler.compile !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(personaCompilerHandler.compile({ personaPageId: "empty-persona" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('recompile', () => {
    it('builds a valid StorageProgram', () => {
      const program = personaCompilerHandler.recompile({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = personaCompilerHandler.recompile({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = personaCompilerHandler.recompile({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = personaCompilerHandler.recompile({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = personaCompilerHandler.recompile({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof personaCompilerHandler.recompile !== 'function') return;
      const result = await interpret(personaCompilerHandler.recompile({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "recompile_existing" -> ok', async () => {
      if (typeof personaCompilerHandler.recompile !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_compile_persona = await interpret(personaCompilerHandler.compile({ personaPageId: "persona-research-bot" }), storage);
      const result = await interpret(personaCompilerHandler.recompile({ compilation: afterResult_compile_persona?.output?.["compilation"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "recompile_missing" -> notfound', async () => {
      if (typeof personaCompilerHandler.recompile !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(personaCompilerHandler.recompile({ compilation: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('markStale', () => {
    it('builds a valid StorageProgram', () => {
      const program = personaCompilerHandler.markStale({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = personaCompilerHandler.markStale({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = personaCompilerHandler.markStale({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = personaCompilerHandler.markStale({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = personaCompilerHandler.markStale({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof personaCompilerHandler.markStale !== 'function') return;
      const result = await interpret(personaCompilerHandler.markStale({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "mark_stale" -> ok', async () => {
      if (typeof personaCompilerHandler.markStale !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_compile_persona = await interpret(personaCompilerHandler.compile({ personaPageId: "persona-research-bot" }), storage);
      const result = await interpret(personaCompilerHandler.markStale({ compilation: afterResult_compile_persona?.output?.["compilation"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "mark_stale_missing" -> notfound', async () => {
      if (typeof personaCompilerHandler.markStale !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(personaCompilerHandler.markStale({ compilation: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('getAssembly', () => {
    it('builds a valid StorageProgram', () => {
      const program = personaCompilerHandler.getAssembly({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = personaCompilerHandler.getAssembly({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = personaCompilerHandler.getAssembly({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = personaCompilerHandler.getAssembly({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = personaCompilerHandler.getAssembly({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof personaCompilerHandler.getAssembly !== 'function') return;
      const result = await interpret(personaCompilerHandler.getAssembly({ compilation: {"type":"ref","fixture":"compile_persona","field":"compilation"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_assembly" -> ok', async () => {
      if (typeof personaCompilerHandler.getAssembly !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_compile_persona = await interpret(personaCompilerHandler.compile({ personaPageId: "persona-research-bot" }), storage);
      const result = await interpret(personaCompilerHandler.getAssembly({ compilation: afterResult_compile_persona?.output?.["compilation"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_assembly_missing" -> notfound', async () => {
      if (typeof personaCompilerHandler.getAssembly !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(personaCompilerHandler.getAssembly({ compilation: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('listByPersona', () => {
    it('builds a valid StorageProgram', () => {
      const program = personaCompilerHandler.listByPersona({ personaPageId: "persona-research-bot" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = personaCompilerHandler.listByPersona({ personaPageId: "persona-research-bot" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = personaCompilerHandler.listByPersona({ personaPageId: "persona-research-bot" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = personaCompilerHandler.listByPersona({ personaPageId: "persona-research-bot" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = personaCompilerHandler.listByPersona({ personaPageId: "persona-research-bot" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof personaCompilerHandler.listByPersona !== 'function') return;
      const result = await interpret(personaCompilerHandler.listByPersona({ personaPageId: "persona-research-bot" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_compilations" -> ok', async () => {
      if (typeof personaCompilerHandler.listByPersona !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_compile_persona = await interpret(personaCompilerHandler.compile({ personaPageId: "persona-research-bot" }), storage);
      const _pool = Object.assign({}, (afterResult_compile_persona?.output ?? {}));
      const _fixtureInput = { personaPageId: "persona-research-bot" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(personaCompilerHandler.listByPersona({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_missing" -> notfound', async () => {
      if (typeof personaCompilerHandler.listByPersona !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(personaCompilerHandler.listByPersona({ personaPageId: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof personaCompilerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = personaCompilerHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('PersonaCompiler');
    });
  });

  describe('invariant examples', () => {
    it("compile-then-get", async () => {
      const storage = createInMemoryStorage();
      const compileResult0 = await interpret(personaCompilerHandler.compile({ personaPageId: "page-1" }), storage);
      expect(compileResult0.variant).toBe("ok");
      let compilation = compileResult0.output["compilation"];
      let c = compilation;
      let assemblyId = compileResult0.output["assemblyId"];
      let a = assemblyId;
      let sections = compileResult0.output["sections"];
      let _ = sections;
      let totalTokens = compileResult0.output["totalTokens"];
      const thenResult0 = await interpret(personaCompilerHandler.getAssembly({ compilation: c }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("compile-then-mark-stale-then-recompile", async () => {
      const storage = createInMemoryStorage();
      const compileResult0 = await interpret(personaCompilerHandler.compile({ personaPageId: "page-1" }), storage);
      expect(compileResult0.variant).toBe("ok");
      let compilation = compileResult0.output["compilation"];
      let c = compilation;
      let assemblyId = compileResult0.output["assemblyId"];
      let _ = assemblyId;
      let sections = compileResult0.output["sections"];
      let totalTokens = compileResult0.output["totalTokens"];
      const markStaleResult1 = await interpret(personaCompilerHandler.markStale({ compilation: c }), storage);
      expect(markStaleResult1.variant).toBe("ok");
      compilation = markStaleResult1.output["compilation"];
      const thenResult0 = await interpret(personaCompilerHandler.recompile({ compilation: c }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-status', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('compile'), input: fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recompile'), input: fc.record({ compilation: fc.string() }) }),
              fc.record({ action: fc.constant('markStale'), input: fc.record({ compilation: fc.string() }) }),
              fc.record({ action: fc.constant('getAssembly'), input: fc.record({ compilation: fc.string() }) }),
              fc.record({ action: fc.constant('listByPersona'), input: fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = personaCompilerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(personaCompilerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-personaPageId', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('compile'), input: fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recompile'), input: fc.record({ compilation: fc.string() }) }),
              fc.record({ action: fc.constant('markStale'), input: fc.record({ compilation: fc.string() }) }),
              fc.record({ action: fc.constant('getAssembly'), input: fc.record({ compilation: fc.string() }) }),
              fc.record({ action: fc.constant('listByPersona'), input: fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = personaCompilerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(personaCompilerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-personaPageId
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
