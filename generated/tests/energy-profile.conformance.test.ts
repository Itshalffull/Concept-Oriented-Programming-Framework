// EnergyProfile Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { energyProfileHandler } from '../../handlers/ts/quality-analysis/energy-profile.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('EnergyProfile functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof energyProfileHandler.configure !== 'function') return;
      const result = await interpret(energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof energyProfileHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "another_target" -> ok', async () => {
      if (typeof energyProfileHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(energyProfileHandler.configure({ target: "api/users", functionalUnit: "per-transaction", gridCarbonIntensity: "250.0", embodiedCarbonPerUnit: "0.005" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "duplicate_target" -> duplicate', async () => {
      if (typeof energyProfileHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(energyProfileHandler.configure({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "invalid_unit" -> error', async () => {
      if (typeof energyProfileHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(energyProfileHandler.configure({ target: "api/bad", functionalUnit: "", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('measure', () => {
    it('builds a valid StorageProgram', () => {
      const program = energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof energyProfileHandler.measure !== 'function') return;
      const result = await interpret(energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "measure_ok" -> ok', async () => {
      if (typeof energyProfileHandler.measure !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { target: "api/orders", energyJoules: "0.5", requestCount: "100.0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(energyProfileHandler.measure({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "measure_not_configured" -> notConfigured', async () => {
      if (typeof energyProfileHandler.measure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(energyProfileHandler.measure({ target: "api/unknown", energyJoules: "1.0", requestCount: "50.0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notConfigured'));
    });

  });

  describe('query', () => {
    it('builds a valid StorageProgram', () => {
      const program = energyProfileHandler.query({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = energyProfileHandler.query({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = energyProfileHandler.query({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = energyProfileHandler.query({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = energyProfileHandler.query({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof energyProfileHandler.query !== 'function') return;
      const result = await interpret(energyProfileHandler.query({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "query_all" -> ok', async () => {
      if (typeof energyProfileHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" }), storage);
      const afterResult_measure_ok = await interpret(energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}), (afterResult_measure_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(energyProfileHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_specific" -> ok', async () => {
      if (typeof energyProfileHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" }), storage);
      const afterResult_measure_ok = await interpret(energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}), (afterResult_measure_ok?.output ?? {}));
      const _fixtureInput = { targets: ["api/orders"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(energyProfileHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('trend', () => {
    it('builds a valid StorageProgram', () => {
      const program = energyProfileHandler.trend({ target: "api/orders", count: "10" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = energyProfileHandler.trend({ target: "api/orders", count: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = energyProfileHandler.trend({ target: "api/orders", count: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = energyProfileHandler.trend({ target: "api/orders", count: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = energyProfileHandler.trend({ target: "api/orders", count: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof energyProfileHandler.trend !== 'function') return;
      const result = await interpret(energyProfileHandler.trend({ target: "api/orders", count: "10" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "trend_ok" -> ok', async () => {
      if (typeof energyProfileHandler.trend !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" }), storage);
      const afterResult_measure_ok = await interpret(energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}), (afterResult_measure_ok?.output ?? {}));
      const _fixtureInput = { target: "api/orders", count: "10" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(energyProfileHandler.trend({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "trend_not_tracked" -> notTracked', async () => {
      if (typeof energyProfileHandler.trend !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(energyProfileHandler.trend({ target: "api/unknown" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notTracked'));
    });

  });

  describe('summary', () => {
    it('builds a valid StorageProgram', () => {
      const program = energyProfileHandler.summary({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = energyProfileHandler.summary({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = energyProfileHandler.summary({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = energyProfileHandler.summary({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = energyProfileHandler.summary({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof energyProfileHandler.summary !== 'function') return;
      const result = await interpret(energyProfileHandler.summary({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "summary_ok" -> ok', async () => {
      if (typeof energyProfileHandler.summary !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: "400.0", embodiedCarbonPerUnit: "0.01" }), storage);
      const afterResult_measure_ok = await interpret(energyProfileHandler.measure({ target: "api/orders", energyJoules: "0.5", requestCount: "100.0" }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}), (afterResult_measure_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(energyProfileHandler.summary({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof energyProfileHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = energyProfileHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('EnergyProfile');
    });
  });

  describe('invariant examples', () => {
    it("configure then measure then query", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(energyProfileHandler.configure({ target: "api/orders", functionalUnit: "per-request", gridCarbonIntensity: 400, embodiedCarbonPerUnit: 0.01 }), storage);
      expect(configureResult0.variant).toBe("ok");
      let profile = configureResult0.output["profile"];
      let e = profile;
      const thenResult0 = await interpret(energyProfileHandler.measure({ target: "api/orders", energyJoules: 0.5, requestCount: 100 }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(energyProfileHandler.query({ targets: ["api/orders"] }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

    it("measure requires configuration", async () => {
      const storage = createInMemoryStorage();
      const measureResult0 = await interpret(energyProfileHandler.measure({ target: "api/unconfigured", energyJoules: 1, requestCount: 50 }), storage);
      expect(measureResult0.variant).toBe("notConfigured");
      let target = measureResult0.output["target"];
    });

  });

  describe('forall properties (PBT)', () => {
    it('forall: duplicate configuration rejected', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.constantFrom("api/orders", "api/users"),
          async (t) => {
            const storage = createInMemoryStorage();
            const result = await interpret(energyProfileHandler.configure({ target: {"type":"variable","name":"t"}, functionalUnit: {"type":"literal","value":"per-request"}, gridCarbonIntensity: {"type":"literal","value":400}, embodiedCarbonPerUnit: {"type":"literal","value":0.01} }), storage);
            expect(result.variant).toBeDefined();
            expect(result.variant).toBe("ok");
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
