// Evidence Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { evidenceHandler } from '../../handlers/ts/suites/formal-verification/evidence.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Evidence functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('record', () => {
    it('builds a valid StorageProgram', () => {
      const program = evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evidenceHandler.record !== 'function') return;
      const result = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_record" -> ok', async () => {
      if (typeof evidenceHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invalid_type" -> invalid', async () => {
      if (typeof evidenceHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "bogus", content: "data" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "empty_content" -> invalid', async () => {
      if (typeof evidenceHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('validate', () => {
    it('builds a valid StorageProgram', () => {
      const program = evidenceHandler.validate({ id: "ev-001" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evidenceHandler.validate({ id: "ev-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evidenceHandler.validate({ id: "ev-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evidenceHandler.validate({ id: "ev-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evidenceHandler.validate({ id: "ev-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evidenceHandler.validate !== 'function') return;
      const result = await interpret(evidenceHandler.validate({ id: "ev-001" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_validate" -> ok', async () => {
      if (typeof evidenceHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_record = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_record?.output ?? {}));
      const _fixtureInput = { id: "ev-001" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(evidenceHandler.validate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_missing" -> notfound', async () => {
      if (typeof evidenceHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(evidenceHandler.validate({ id: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('retrieve', () => {
    it('builds a valid StorageProgram', () => {
      const program = evidenceHandler.retrieve({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evidenceHandler.retrieve({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evidenceHandler.retrieve({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evidenceHandler.retrieve({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evidenceHandler.retrieve({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evidenceHandler.retrieve !== 'function') return;
      const result = await interpret(evidenceHandler.retrieve({ id: {"type":"ref","fixture":"valid_record","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_retrieve" -> ok', async () => {
      if (typeof evidenceHandler.retrieve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_record = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" }), storage);
      const result = await interpret(evidenceHandler.retrieve({ id: afterResult_valid_record?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "retrieve_missing" -> notfound', async () => {
      if (typeof evidenceHandler.retrieve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(evidenceHandler.retrieve({ id: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('compare', () => {
    it('builds a valid StorageProgram', () => {
      const program = evidenceHandler.compare({ id_a: "ev-001", id_b: "ev-002" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evidenceHandler.compare({ id_a: "ev-001", id_b: "ev-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evidenceHandler.compare({ id_a: "ev-001", id_b: "ev-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evidenceHandler.compare({ id_a: "ev-001", id_b: "ev-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evidenceHandler.compare({ id_a: "ev-001", id_b: "ev-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evidenceHandler.compare !== 'function') return;
      const result = await interpret(evidenceHandler.compare({ id_a: "ev-001", id_b: "ev-002" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_compare" -> ok', async () => {
      if (typeof evidenceHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_record = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_record?.output ?? {}));
      const _fixtureInput = { id_a: "ev-001", id_b: "ev-002" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(evidenceHandler.compare({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compare_missing" -> notfound', async () => {
      if (typeof evidenceHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(evidenceHandler.compare({ id_a: "ev-001", id_b: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('minimize', () => {
    it('builds a valid StorageProgram', () => {
      const program = evidenceHandler.minimize({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evidenceHandler.minimize({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evidenceHandler.minimize({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evidenceHandler.minimize({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evidenceHandler.minimize({ id: {"type":"ref","fixture":"valid_record","field":"id"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evidenceHandler.minimize !== 'function') return;
      const result = await interpret(evidenceHandler.minimize({ id: {"type":"ref","fixture":"valid_record","field":"id"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_minimize" -> ok', async () => {
      if (typeof evidenceHandler.minimize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_record = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" }), storage);
      const result = await interpret(evidenceHandler.minimize({ id: afterResult_valid_record?.output?.["id"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "minimize_non_counter" -> not_applicable', async () => {
      if (typeof evidenceHandler.minimize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(evidenceHandler.minimize({ id: "ev-proof-001" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_applicable'));
    });

    it('fixture "minimize_missing" -> notfound', async () => {
      if (typeof evidenceHandler.minimize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(evidenceHandler.minimize({ id: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = evidenceHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = evidenceHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = evidenceHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = evidenceHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = evidenceHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof evidenceHandler.list !== 'function') return;
      const result = await interpret(evidenceHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof evidenceHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_record = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_record?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(evidenceHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_type" -> ok', async () => {
      if (typeof evidenceHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_record = await interpret(evidenceHandler.record({ property_ref: "prop-1", artifact_type: "proof_certificate", content: "(proof-body QED)", solver: "z3" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_record?.output ?? {}));
      const _fixtureInput = { artifact_type: "proof_certificate" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(evidenceHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof evidenceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = evidenceHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Evidence');
    });
  });

  describe('invariant examples', () => {
    it("record-then-validate", async () => {
      const storage = createInMemoryStorage();
      const recordResult0 = await interpret(evidenceHandler.record({ artifact_type: "proof_certificate", content: "test-c", solver_metadata: "test-m", property_ref: "prop-1", confidence_score: 1 }), storage);
      expect(recordResult0.variant).toBe("ok");
      let evidence = recordResult0.output["evidence"];
      let e = evidence;
      let content_hash = recordResult0.output["content_hash"];
      let h = content_hash;
      const thenResult0 = await interpret(evidenceHandler.validate({ evidence: e }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-artifact_type', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('record'), input: fc.record({ property_ref: fc.string({ minLength: 1, maxLength: 50 }), artifact_type: fc.string({ minLength: 1, maxLength: 50 }), content: fc.string({ minLength: 1, maxLength: 50 }), solver: fc.string(), run_ref: fc.string() }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('retrieve'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ id_a: fc.string({ minLength: 1, maxLength: 50 }), id_b: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('minimize'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ property_ref: fc.string(), artifact_type: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = evidenceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(evidenceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-content_path', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('record'), input: fc.record({ property_ref: fc.string({ minLength: 1, maxLength: 50 }), artifact_type: fc.string({ minLength: 1, maxLength: 50 }), content: fc.string({ minLength: 1, maxLength: 50 }), solver: fc.string(), run_ref: fc.string() }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('retrieve'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ id_a: fc.string({ minLength: 1, maxLength: 50 }), id_b: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('minimize'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ property_ref: fc.string(), artifact_type: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = evidenceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(evidenceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-content_path
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('record handles empty input: ', async () => {
      if (typeof evidenceHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(evidenceHandler.record({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('record ensures on ok: ', async () => {
      if (typeof evidenceHandler.record !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ property_ref: fc.string({ minLength: 1, maxLength: 50 }), artifact_type: fc.string({ minLength: 1, maxLength: 50 }), content: fc.string({ minLength: 1, maxLength: 50 }), solver: fc.string(), run_ref: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = evidenceHandler.record(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
