// LocalRuntime Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { localRuntimeHandler } from '../../handlers/ts/deploy/local-runtime.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('LocalRuntime functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('provision', () => {
    it('builds a valid StorageProgram', () => {
      const program = localRuntimeHandler.provision({ concept: 'test-concept', command: 'test-command', port: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = localRuntimeHandler.provision({ concept: 'test-concept', command: 'test-command', port: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = localRuntimeHandler.provision({ concept: 'test-concept', command: 'test-command', port: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = localRuntimeHandler.provision({ concept: 'test-concept', command: 'test-command', port: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = localRuntimeHandler.provision({ concept: 'test-concept', command: 'test-command', port: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof localRuntimeHandler.provision !== 'function') return;
      try {
        const result = await interpret(localRuntimeHandler.provision({ concept: 'test-concept', command: 'test-command', port: 1 }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('deploy', () => {
    it('builds a valid StorageProgram', () => {
      const program = localRuntimeHandler.deploy({ process: 'test', command: 'test-command' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = localRuntimeHandler.deploy({ process: 'test', command: 'test-command' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = localRuntimeHandler.deploy({ process: 'test', command: 'test-command' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = localRuntimeHandler.deploy({ process: 'test', command: 'test-command' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = localRuntimeHandler.deploy({ process: 'test', command: 'test-command' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof localRuntimeHandler.deploy !== 'function') return;
      try {
        const result = await interpret(localRuntimeHandler.deploy({ process: 'test', command: 'test-command' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('setTrafficWeight', () => {
    it('builds a valid StorageProgram', () => {
      const program = localRuntimeHandler.setTrafficWeight({ process: 'test', weight: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = localRuntimeHandler.setTrafficWeight({ process: 'test', weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = localRuntimeHandler.setTrafficWeight({ process: 'test', weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = localRuntimeHandler.setTrafficWeight({ process: 'test', weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = localRuntimeHandler.setTrafficWeight({ process: 'test', weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof localRuntimeHandler.setTrafficWeight !== 'function') return;
      try {
        const result = await interpret(localRuntimeHandler.setTrafficWeight({ process: 'test', weight: 1 }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('rollback', () => {
    it('builds a valid StorageProgram', () => {
      const program = localRuntimeHandler.rollback({ process: 'test', previousCommand: 'test-previousCommand' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = localRuntimeHandler.rollback({ process: 'test', previousCommand: 'test-previousCommand' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = localRuntimeHandler.rollback({ process: 'test', previousCommand: 'test-previousCommand' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = localRuntimeHandler.rollback({ process: 'test', previousCommand: 'test-previousCommand' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = localRuntimeHandler.rollback({ process: 'test', previousCommand: 'test-previousCommand' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof localRuntimeHandler.rollback !== 'function') return;
      try {
        const result = await interpret(localRuntimeHandler.rollback({ process: 'test', previousCommand: 'test-previousCommand' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('destroy', () => {
    it('builds a valid StorageProgram', () => {
      const program = localRuntimeHandler.destroy({ process: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = localRuntimeHandler.destroy({ process: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = localRuntimeHandler.destroy({ process: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = localRuntimeHandler.destroy({ process: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = localRuntimeHandler.destroy({ process: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof localRuntimeHandler.destroy !== 'function') return;
      try {
        const result = await interpret(localRuntimeHandler.destroy({ process: 'test' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("provision-then-deploy", async () => {
      const storage = createInMemoryStorage();
      const provisionResult0 = await interpret(localRuntimeHandler.provision({ concept: {"type":"literal","value":"User"}, command: {"type":"literal","value":"node server.js"}, port: {"type":"literal","value":3000} }), storage);
      expect(provisionResult0.variant).toBe("ok");
      const process = provisionResult0.output["process"];
      const pid = provisionResult0.output["pid"];
      const endpoint = provisionResult0.output["endpoint"];
      const thenResult0 = await interpret(localRuntimeHandler.deploy({ process: {"type":"variable","name":"p"}, command: {"type":"literal","value":"node server.js"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-command', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('provision'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 20 }), command: fc.string({ minLength: 1, maxLength: 20 }), port: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('deploy'), input: fc.record({ process: fc.string(), command: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('setTrafficWeight'), input: fc.record({ process: fc.string(), weight: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ process: fc.string(), previousCommand: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('destroy'), input: fc.record({ process: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = localRuntimeHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(localRuntimeHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-workingDirectory', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('provision'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 20 }), command: fc.string({ minLength: 1, maxLength: 20 }), port: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('deploy'), input: fc.record({ process: fc.string(), command: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('setTrafficWeight'), input: fc.record({ process: fc.string(), weight: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ process: fc.string(), previousCommand: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('destroy'), input: fc.record({ process: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = localRuntimeHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(localRuntimeHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                  // Never: orphaned-workingDirectory
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('provision handles empty input: ', async () => {
      if (typeof localRuntimeHandler.provision !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(localRuntimeHandler.provision({  }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('provision ensures on ok: ', async () => {
      if (typeof localRuntimeHandler.provision !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), command: fc.string({ minLength: 1, maxLength: 50 }), port: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = localRuntimeHandler.provision(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
