// WebhookEndpoint Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { webhookEndpointHandler } from '../../handlers/ts/execution/instances/webhook-endpoint.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('WebhookEndpoint imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('executes successfully', async () => {
      if (typeof webhookEndpointHandler.register !== 'function') return;
      const result = await webhookEndpointHandler.register({ name: 'test-name', url: 'test-url', headers: 'test-headers' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('resolve', () => {
    it('executes successfully', async () => {
      if (typeof webhookEndpointHandler.resolve !== 'function') return;
      const result = await webhookEndpointHandler.resolve({ name: 'test-name' }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('list', () => {
    it('executes successfully', async () => {
      if (typeof webhookEndpointHandler.list !== 'function') return;
      const result = await webhookEndpointHandler.list({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invariant examples', () => {
    it("resolve after register", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await webhookEndpointHandler.register({ name: {"type":"literal","value":"deploy-webhook"}, url: {"type":"literal","value":"https://hooks.example.com/deploy"}, headers: {"type":"literal","value":"{}"} }, storage);
      expect(registerResult0.variant).toBe("ok");
      const endpoint = registerResult0.output["endpoint"];
      const thenResult0 = await webhookEndpointHandler.resolve({ name: {"type":"literal","value":"deploy-webhook"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: endpoints have valid name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), url: fc.string({ minLength: 1, maxLength: 20 }), headers: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = webhookEndpointHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await actionFn.call(webhookEndpointHandler, step.input as Record<string, unknown>, storage);
                expect(result.variant).toBeDefined();
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: endpoint without URL', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), url: fc.string({ minLength: 1, maxLength: 20 }), headers: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = webhookEndpointHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await actionFn.call(webhookEndpointHandler, step.input as Record<string, unknown>, storage);
                expect(result.variant).toBeDefined();
                // Never: endpoint without URL
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register requires: ', async () => {
      const storage = createInMemoryStorage();
      const result = await webhookEndpointHandler.register({  }, storage);
      expect(['error', 'invalid', 'missing', 'notFound']).toContain(result.variant);
    });

    it('register ensures on ok: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), url: fc.string({ minLength: 1, maxLength: 50 }), headers: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await webhookEndpointHandler.register(input as Record<string, unknown>, storage);
            fc.pre(result.variant === "ok");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
