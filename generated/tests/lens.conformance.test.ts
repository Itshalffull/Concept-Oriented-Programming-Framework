// Lens Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { lensHandler } from '../../handlers/ts/monadic/lens.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('Lens imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('executes without crashing', async () => {
      if (typeof lensHandler.create !== 'function') return;
      try {
        const result = await lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "create_field_lens" -> ok', async () => {
      if (typeof lensHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_relation_lens" -> ok', async () => {
      if (typeof lensHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.create({ lens: "lens-users", relation: "users", key: "", field: "" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_empty_relation" -> error', async () => {
      if (typeof lensHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.create({ lens: "lens-bad", relation: "", key: "", field: "" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('fromRelation', () => {
    it('executes without crashing', async () => {
      if (typeof lensHandler.fromRelation !== 'function') return;
      try {
        const result = await lensHandler.fromRelation({ lens: "lens-users-rel", relation: "users" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "from_users_relation" -> ok', async () => {
      if (typeof lensHandler.fromRelation !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.fromRelation({ lens: "lens-users-rel", relation: "users" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "from_empty_relation" -> error', async () => {
      if (typeof lensHandler.fromRelation !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.fromRelation({ lens: "lens-bad", relation: "" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('compose', () => {
    it('executes without crashing', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      try {
        const result = await lensHandler.compose({ outer: "lens-users-rel", inner: "lens-user-email" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "compose_valid" -> ok', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.compose({ outer: "lens-users-rel", inner: "lens-user-email" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compose_missing" -> error', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.compose({ outer: "nonexistent", inner: "lens-user-email" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('get', () => {
    it('executes without crashing', async () => {
      if (typeof lensHandler.get !== 'function') return;
      try {
        const result = await lensHandler.get({ lens: "lens-user-email" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof lensHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.get({ lens: "lens-user-email" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> error', async () => {
      if (typeof lensHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.get({ lens: "nonexistent" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('decompose', () => {
    it('executes without crashing', async () => {
      if (typeof lensHandler.decompose !== 'function') return;
      try {
        const result = await lensHandler.decompose({ lens: "lens-user-email" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "decompose_existing" -> ok', async () => {
      if (typeof lensHandler.decompose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.decompose({ lens: "lens-user-email" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "decompose_missing" -> error', async () => {
      if (typeof lensHandler.decompose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.decompose({ lens: "nonexistent" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('validate', () => {
    it('executes without crashing', async () => {
      if (typeof lensHandler.validate !== 'function') return;
      try {
        const result = await lensHandler.validate({ lens: "lens-user-email", conceptSpec: "{\"state\":{\"users\":\"set U\",\"email\":\"U -> String\"}}" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "validate_valid" -> ok', async () => {
      if (typeof lensHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.validate({ lens: "lens-user-email", conceptSpec: "{\"state\":{\"users\":\"set U\",\"email\":\"U -> String\"}}" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_missing_lens" -> error', async () => {
      if (typeof lensHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.validate({ lens: "nonexistent", conceptSpec: "{\"state\":{\"users\":\"set U\"}}" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('list', () => {
    it('executes without crashing', async () => {
      if (typeof lensHandler.list !== 'function') return;
      try {
        const result = await lensHandler.list({  }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof lensHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.list({  }, storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof lensHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      let result: any;
      try {
        const r = lensHandler.register({}, storage);
        result = r instanceof Promise ? await r : r;
        // If StorageProgram, interpret it
        if (result?.instructions && !result.variant) {
        }
      } catch { return; }
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('Lens');
    });
  });

  describe('invariant examples', () => {
    it("create field lens then get returns field kind", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await lensHandler.create({ lens: {"type":"variable","name":"l"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, field: {"type":"literal","value":"email"} }, storage);
      expect(createResult0.variant).toBe("ok");
      const lens = createResult0.output["lens"];
      const thenResult0 = await lensHandler.get({ lens: {"type":"variable","name":"l"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("fromRelation then decompose returns segments", async () => {
      const storage = createInMemoryStorage();
      const fromRelationResult0 = await lensHandler.fromRelation({ lens: {"type":"variable","name":"r"}, relation: {"type":"literal","value":"users"} }, storage);
      expect(fromRelationResult0.variant).toBe("ok");
      const lens = fromRelationResult0.output["lens"];
      const thenResult0 = await lensHandler.decompose({ lens: {"type":"variable","name":"r"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("relation-only vs field lens have different kinds", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await lensHandler.create({ lens: {"type":"variable","name":"a"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":""}, field: {"type":"literal","value":""} }, storage);
      expect(createResult0.variant).toBe("ok");
      const lens = createResult0.output["lens"];
      const createResult1 = await lensHandler.create({ lens: {"type":"variable","name":"b"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, field: {"type":"literal","value":"email"} }, storage);
      expect(createResult1.variant).toBe("ok");
      const lens = createResult1.output["lens"];
      const thenResult0 = await lensHandler.get({ lens: {"type":"variable","name":"a"} }, storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await lensHandler.get({ lens: {"type":"variable","name":"b"} }, storage);
      expect(thenResult1.variant).toBe("ok");
    });

    it("compose two lenses narrows focus", async () => {
      const storage = createInMemoryStorage();
      const fromRelationResult0 = await lensHandler.fromRelation({ lens: {"type":"variable","name":"outer"}, relation: {"type":"literal","value":"users"} }, storage);
      expect(fromRelationResult0.variant).toBe("ok");
      const lens = fromRelationResult0.output["lens"];
      const createResult1 = await lensHandler.create({ lens: {"type":"variable","name":"inner"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, field: {"type":"literal","value":"email"} }, storage);
      expect(createResult1.variant).toBe("ok");
      const lens = createResult1.output["lens"];
      const thenResult0 = await lensHandler.compose({ outer: {"type":"variable","name":"outer"}, inner: {"type":"variable","name":"inner"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("validate against matching spec returns valid", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await lensHandler.create({ lens: {"type":"variable","name":"l"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, field: {"type":"literal","value":"email"} }, storage);
      expect(createResult0.variant).toBe("ok");
      const lens = createResult0.output["lens"];
      const thenResult0 = await lensHandler.validate({ lens: {"type":"variable","name":"l"}, conceptSpec: {"type":"literal","value":"User { state { users: set U; email: U -> String } }"} }, storage);
      expect(thenResult0.variant).toBe("valid");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: lenses have at least one segment', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('fromRelation'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ outer: fc.string(), inner: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('decompose'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ lens: fc.string(), conceptSpec: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = lensHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(lensHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: lens kind matches segment count', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('fromRelation'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ outer: fc.string(), inner: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('decompose'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ lens: fc.string(), conceptSpec: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = lensHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(lensHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: compose succeeds with incompatible types', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('fromRelation'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ outer: fc.string(), inner: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('decompose'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ lens: fc.string(), conceptSpec: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = lensHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(lensHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: compose succeeds with incompatible types
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof lensHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.create({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('create ensures on ok: ', async () => {
      if (typeof lensHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await lensHandler.create(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('compose handles empty input: ', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await lensHandler.compose({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('compose ensures on ok: ', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ outer: fc.string(), inner: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await lensHandler.compose(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
