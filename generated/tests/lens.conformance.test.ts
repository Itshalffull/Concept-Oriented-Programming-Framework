// Lens Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { lensHandler } from '../../handlers/ts/monadic/lens.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Lens functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof lensHandler.create !== 'function') return;
      const result = await interpret(lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_field_lens" -> ok', async () => {
      if (typeof lensHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_relation_lens" -> ok', async () => {
      if (typeof lensHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensHandler.create({ lens: "lens-users", relation: "users", key: "", field: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_empty_relation" -> error', async () => {
      if (typeof lensHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensHandler.create({ lens: "lens-bad", relation: "", key: "", field: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('fromRelation', () => {
    it('builds a valid StorageProgram', () => {
      const program = lensHandler.fromRelation({ lens: "lens-users-rel", relation: "users" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lensHandler.fromRelation({ lens: "lens-users-rel", relation: "users" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lensHandler.fromRelation({ lens: "lens-users-rel", relation: "users" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lensHandler.fromRelation({ lens: "lens-users-rel", relation: "users" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lensHandler.fromRelation({ lens: "lens-users-rel", relation: "users" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof lensHandler.fromRelation !== 'function') return;
      const result = await interpret(lensHandler.fromRelation({ lens: "lens-users-rel", relation: "users" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "from_users_relation" -> ok', async () => {
      if (typeof lensHandler.fromRelation !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }), storage));
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-users", relation: "users", key: "", field: "" }), storage));
      const result = await interpret(lensHandler.fromRelation({ lens: "lens-users-rel", relation: "users" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "from_empty_relation" -> error', async () => {
      if (typeof lensHandler.fromRelation !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensHandler.fromRelation({ lens: "lens-bad", relation: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('compose', () => {
    it('builds a valid StorageProgram', () => {
      const program = lensHandler.compose({ outer: "lens-users-rel", inner: "lens-user-email" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lensHandler.compose({ outer: "lens-users-rel", inner: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lensHandler.compose({ outer: "lens-users-rel", inner: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lensHandler.compose({ outer: "lens-users-rel", inner: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lensHandler.compose({ outer: "lens-users-rel", inner: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      const result = await interpret(lensHandler.compose({ outer: "lens-users-rel", inner: "lens-user-email" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compose_valid" -> ok', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }), storage));
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-users", relation: "users", key: "", field: "" }), storage));
      const result = await interpret(lensHandler.compose({ outer: "lens-users-rel", inner: "lens-user-email" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compose_missing" -> error', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensHandler.compose({ outer: "nonexistent", inner: "lens-user-email" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = lensHandler.get({ lens: "lens-user-email" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lensHandler.get({ lens: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lensHandler.get({ lens: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lensHandler.get({ lens: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lensHandler.get({ lens: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof lensHandler.get !== 'function') return;
      const result = await interpret(lensHandler.get({ lens: "lens-user-email" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof lensHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }), storage));
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-users", relation: "users", key: "", field: "" }), storage));
      const result = await interpret(lensHandler.get({ lens: "lens-user-email" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> error', async () => {
      if (typeof lensHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensHandler.get({ lens: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('decompose', () => {
    it('builds a valid StorageProgram', () => {
      const program = lensHandler.decompose({ lens: "lens-user-email" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lensHandler.decompose({ lens: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lensHandler.decompose({ lens: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lensHandler.decompose({ lens: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lensHandler.decompose({ lens: "lens-user-email" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof lensHandler.decompose !== 'function') return;
      const result = await interpret(lensHandler.decompose({ lens: "lens-user-email" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "decompose_existing" -> ok', async () => {
      if (typeof lensHandler.decompose !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }), storage));
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-users", relation: "users", key: "", field: "" }), storage));
      const result = await interpret(lensHandler.decompose({ lens: "lens-user-email" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "decompose_missing" -> error', async () => {
      if (typeof lensHandler.decompose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensHandler.decompose({ lens: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('validate', () => {
    it('builds a valid StorageProgram', () => {
      const program = lensHandler.validate({ lens: "lens-user-email", conceptSpec: "{\"state\":{\"users\":\"set U\",\"email\":\"U -> String\"}}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lensHandler.validate({ lens: "lens-user-email", conceptSpec: "{\"state\":{\"users\":\"set U\",\"email\":\"U -> String\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lensHandler.validate({ lens: "lens-user-email", conceptSpec: "{\"state\":{\"users\":\"set U\",\"email\":\"U -> String\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lensHandler.validate({ lens: "lens-user-email", conceptSpec: "{\"state\":{\"users\":\"set U\",\"email\":\"U -> String\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lensHandler.validate({ lens: "lens-user-email", conceptSpec: "{\"state\":{\"users\":\"set U\",\"email\":\"U -> String\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof lensHandler.validate !== 'function') return;
      const result = await interpret(lensHandler.validate({ lens: "lens-user-email", conceptSpec: "{\"state\":{\"users\":\"set U\",\"email\":\"U -> String\"}}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "validate_valid" -> ok', async () => {
      if (typeof lensHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }), storage));
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-users", relation: "users", key: "", field: "" }), storage));
      const result = await interpret(lensHandler.validate({ lens: "lens-user-email", conceptSpec: "{\"state\":{\"users\":\"set U\",\"email\":\"U -> String\"}}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_missing_lens" -> error', async () => {
      if (typeof lensHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lensHandler.validate({ lens: "nonexistent", conceptSpec: "{\"state\":{\"users\":\"set U\"}}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = lensHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lensHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lensHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lensHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lensHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof lensHandler.list !== 'function') return;
      const result = await interpret(lensHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof lensHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-user-email", relation: "users", key: "u1", field: "email" }), storage));
      await safeInvoke(async () => await interpret(lensHandler.create({ lens: "lens-users", relation: "users", key: "", field: "" }), storage));
      const result = await interpret(lensHandler.list({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof lensHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = lensHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('Lens');
    });
  });

  describe('invariant examples', () => {
    it("create field lens then get returns field kind", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(lensHandler.create({ lens: {"type":"variable","name":"l"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, field: {"type":"literal","value":"email"} }), storage);
      expect(createResult0.variant).toBe("ok");
      const lens = createResult0.output["lens"];
      const thenResult0 = await interpret(lensHandler.get({ lens: {"type":"variable","name":"l"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("fromRelation then decompose returns segments", async () => {
      const storage = createInMemoryStorage();
      const fromRelationResult0 = await interpret(lensHandler.fromRelation({ lens: {"type":"variable","name":"r"}, relation: {"type":"literal","value":"users"} }), storage);
      expect(fromRelationResult0.variant).toBe("ok");
      const lens = fromRelationResult0.output["lens"];
      const thenResult0 = await interpret(lensHandler.decompose({ lens: {"type":"variable","name":"r"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("relation-only vs field lens have different kinds", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(lensHandler.create({ lens: {"type":"variable","name":"a"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":""}, field: {"type":"literal","value":""} }), storage);
      expect(createResult0.variant).toBe("ok");
      const lens = createResult0.output["lens"];
      const createResult1 = await interpret(lensHandler.create({ lens: {"type":"variable","name":"b"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, field: {"type":"literal","value":"email"} }), storage);
      expect(createResult1.variant).toBe("ok");
      const lens = createResult1.output["lens"];
      const thenResult0 = await interpret(lensHandler.get({ lens: {"type":"variable","name":"a"} }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(lensHandler.get({ lens: {"type":"variable","name":"b"} }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

    it("compose two lenses narrows focus", async () => {
      const storage = createInMemoryStorage();
      const fromRelationResult0 = await interpret(lensHandler.fromRelation({ lens: {"type":"variable","name":"outer"}, relation: {"type":"literal","value":"users"} }), storage);
      expect(fromRelationResult0.variant).toBe("ok");
      const lens = fromRelationResult0.output["lens"];
      const createResult1 = await interpret(lensHandler.create({ lens: {"type":"variable","name":"inner"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, field: {"type":"literal","value":"email"} }), storage);
      expect(createResult1.variant).toBe("ok");
      const lens = createResult1.output["lens"];
      const thenResult0 = await interpret(lensHandler.compose({ outer: {"type":"variable","name":"outer"}, inner: {"type":"variable","name":"inner"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("validate against matching spec returns valid", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(lensHandler.create({ lens: {"type":"variable","name":"l"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, field: {"type":"literal","value":"email"} }), storage);
      expect(createResult0.variant).toBe("ok");
      const lens = createResult0.output["lens"];
      const thenResult0 = await interpret(lensHandler.validate({ lens: {"type":"variable","name":"l"}, conceptSpec: {"type":"literal","value":"User { state { users: set U; email: U -> String } }"} }), storage);
      expect(thenResult0.variant).toBe("valid");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: lenses have at least one segment', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('fromRelation'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ outer: fc.string(), inner: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('decompose'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ lens: fc.string(), conceptSpec: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = lensHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(lensHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: lens kind matches segment count', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('fromRelation'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ outer: fc.string(), inner: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('decompose'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ lens: fc.string(), conceptSpec: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = lensHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(lensHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: compose succeeds with incompatible types', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('fromRelation'), input: fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ outer: fc.string(), inner: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('decompose'), input: fc.record({ lens: fc.string() }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ lens: fc.string(), conceptSpec: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = lensHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(lensHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: compose succeeds with incompatible types
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof lensHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(lensHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof lensHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ lens: fc.string(), relation: fc.string({ minLength: 1, maxLength: 50 }), key: fc.string({ minLength: 1, maxLength: 50 }), field: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = lensHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('compose handles empty input: ', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(lensHandler.compose({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('compose ensures on ok: ', async () => {
      if (typeof lensHandler.compose !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ outer: fc.string(), inner: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = lensHandler.compose(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
