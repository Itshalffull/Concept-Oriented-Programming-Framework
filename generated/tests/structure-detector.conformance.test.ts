// StructureDetector Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { structureDetectorHandler } from '../../handlers/ts/app/structure-detector.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('StructureDetector functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('detect', () => {
    it('builds a valid StorageProgram', () => {
      const program = structureDetectorHandler.detect({ content: "author: Alice\ndate: 2024-01-15", detectors: [] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = structureDetectorHandler.detect({ content: "author: Alice\ndate: 2024-01-15", detectors: [] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = structureDetectorHandler.detect({ content: "author: Alice\ndate: 2024-01-15", detectors: [] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = structureDetectorHandler.detect({ content: "author: Alice\ndate: 2024-01-15", detectors: [] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = structureDetectorHandler.detect({ content: "author: Alice\ndate: 2024-01-15", detectors: [] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof structureDetectorHandler.detect !== 'function') return;
      const result = await interpret(structureDetectorHandler.detect({ content: "author: Alice\ndate: 2024-01-15", detectors: [] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "detect_kv" -> ok', async () => {
      if (typeof structureDetectorHandler.detect !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      const afterResult_list_detectors_ok = await interpret(structureDetectorHandler.listDetectors({  }), storage);
      const afterResult_propose_ok = await interpret(structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}), (afterResult_list_detectors_ok?.output ?? {}), (afterResult_propose_ok?.output ?? {}));
      const _fixtureInput = { content: "author: Alice\ndate: 2024-01-15", detectors: [] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(structureDetectorHandler.detect({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "detect_specific" -> ok', async () => {
      if (typeof structureDetectorHandler.detect !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      const afterResult_list_detectors_ok = await interpret(structureDetectorHandler.listDetectors({  }), storage);
      const afterResult_propose_ok = await interpret(structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}), (afterResult_list_detectors_ok?.output ?? {}), (afterResult_propose_ok?.output ?? {}));
      const _fixtureInput = { content: "Follow #clef on https://example.com", detectors: ["tag_detector","url_detector"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(structureDetectorHandler.detect({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "detect_empty_content" -> error', async () => {
      if (typeof structureDetectorHandler.detect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.detect({ content: "", detectors: [] }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "detect_unknown_detector" -> error', async () => {
      if (typeof structureDetectorHandler.detect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.detect({ content: "some text", detectors: ["nonexistent_detector"] }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('propose', () => {
    it('builds a valid StorageProgram', () => {
      const program = structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof structureDetectorHandler.propose !== 'function') return;
      const result = await interpret(structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "propose_ok" -> ok', async () => {
      if (typeof structureDetectorHandler.propose !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      const afterResult_list_detectors_ok = await interpret(structureDetectorHandler.listDetectors({  }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}), (afterResult_list_detectors_ok?.output ?? {}));
      const _fixtureInput = { entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(structureDetectorHandler.propose({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "propose_empty_entity" -> error', async () => {
      if (typeof structureDetectorHandler.propose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.propose({ entityId: "", detections: "[]" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "propose_bad_json" -> error', async () => {
      if (typeof structureDetectorHandler.propose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.propose({ entityId: "doc-1", detections: "not-json" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "propose_empty_detections" -> error', async () => {
      if (typeof structureDetectorHandler.propose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.propose({ entityId: "doc-1", detections: "[]" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('apply', () => {
    it('builds a valid StorageProgram', () => {
      const program = structureDetectorHandler.apply({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = structureDetectorHandler.apply({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = structureDetectorHandler.apply({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = structureDetectorHandler.apply({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = structureDetectorHandler.apply({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof structureDetectorHandler.apply !== 'function') return;
      const result = await interpret(structureDetectorHandler.apply({ entityId: "doc-42", promotionId: "promo-doc-42-0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "apply_ok" -> ok', async () => {
      if (typeof structureDetectorHandler.apply !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      const afterResult_list_detectors_ok = await interpret(structureDetectorHandler.listDetectors({  }), storage);
      const afterResult_propose_ok = await interpret(structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}), (afterResult_list_detectors_ok?.output ?? {}), (afterResult_propose_ok?.output ?? {}));
      const _fixtureInput = { entityId: "doc-42", promotionId: "promo-doc-42-0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(structureDetectorHandler.apply({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "apply_notfound" -> notfound', async () => {
      if (typeof structureDetectorHandler.apply !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.apply({ entityId: "doc-42", promotionId: "promo-missing" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('dismiss', () => {
    it('builds a valid StorageProgram', () => {
      const program = structureDetectorHandler.dismiss({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = structureDetectorHandler.dismiss({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = structureDetectorHandler.dismiss({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = structureDetectorHandler.dismiss({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = structureDetectorHandler.dismiss({ entityId: "doc-42", promotionId: "promo-doc-42-0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof structureDetectorHandler.dismiss !== 'function') return;
      const result = await interpret(structureDetectorHandler.dismiss({ entityId: "doc-42", promotionId: "promo-doc-42-0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "dismiss_ok" -> ok', async () => {
      if (typeof structureDetectorHandler.dismiss !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      const afterResult_list_detectors_ok = await interpret(structureDetectorHandler.listDetectors({  }), storage);
      const afterResult_propose_ok = await interpret(structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}), (afterResult_list_detectors_ok?.output ?? {}), (afterResult_propose_ok?.output ?? {}));
      const _fixtureInput = { entityId: "doc-42", promotionId: "promo-doc-42-0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(structureDetectorHandler.dismiss({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "dismiss_notfound" -> notfound', async () => {
      if (typeof structureDetectorHandler.dismiss !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.dismiss({ entityId: "doc-42", promotionId: "promo-missing" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('listProposals', () => {
    it('builds a valid StorageProgram', () => {
      const program = structureDetectorHandler.listProposals({ entityId: "doc-42" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = structureDetectorHandler.listProposals({ entityId: "doc-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = structureDetectorHandler.listProposals({ entityId: "doc-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = structureDetectorHandler.listProposals({ entityId: "doc-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = structureDetectorHandler.listProposals({ entityId: "doc-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof structureDetectorHandler.listProposals !== 'function') return;
      const result = await interpret(structureDetectorHandler.listProposals({ entityId: "doc-42" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_ok" -> ok', async () => {
      if (typeof structureDetectorHandler.listProposals !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      const afterResult_list_detectors_ok = await interpret(structureDetectorHandler.listDetectors({  }), storage);
      const afterResult_propose_ok = await interpret(structureDetectorHandler.propose({ entityId: "doc-42", detections: "[{\"field\":\"author\",\"rawValue\":\"Alice\",\"normalizedValue\":\"Alice\",\"confidence\":0.95,\"detectorName\":\"kv_detector\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}), (afterResult_list_detectors_ok?.output ?? {}), (afterResult_propose_ok?.output ?? {}));
      const _fixtureInput = { entityId: "doc-42" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(structureDetectorHandler.listProposals({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_empty_entity" -> error', async () => {
      if (typeof structureDetectorHandler.listProposals !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.listProposals({ entityId: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listDetectors', () => {
    it('builds a valid StorageProgram', () => {
      const program = structureDetectorHandler.listDetectors({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = structureDetectorHandler.listDetectors({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = structureDetectorHandler.listDetectors({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = structureDetectorHandler.listDetectors({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = structureDetectorHandler.listDetectors({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof structureDetectorHandler.listDetectors !== 'function') return;
      const result = await interpret(structureDetectorHandler.listDetectors({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_detectors_ok" -> ok', async () => {
      if (typeof structureDetectorHandler.listDetectors !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(structureDetectorHandler.listDetectors({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('registerDetector', () => {
    it('builds a valid StorageProgram', () => {
      const program = structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof structureDetectorHandler.registerDetector !== 'function') return;
      const result = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_ok" -> ok', async () => {
      if (typeof structureDetectorHandler.registerDetector !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_name" -> error', async () => {
      if (typeof structureDetectorHandler.registerDetector !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.registerDetector({ name: "", pattern: ".*", fieldType: "String" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_empty_type" -> error', async () => {
      if (typeof structureDetectorHandler.registerDetector !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(structureDetectorHandler.registerDetector({ name: "my_detector", pattern: ".*", fieldType: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof structureDetectorHandler.registerDetector !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(structureDetectorHandler.registerDetector({ name: "phone_detector", pattern: "\\b\\d{3}[-.\\s]?\\d{3}[-.\\s]?\\d{4}\\b", fieldType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { name: "phone_detector", pattern: ".*", fieldType: "String" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(structureDetectorHandler.registerDetector({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof structureDetectorHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = structureDetectorHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('StructureDetector');
    });
  });

  describe('invariant examples', () => {
    it("detect-propose-apply", async () => {
      const storage = createInMemoryStorage();
      const detectResult0 = await interpret(structureDetectorHandler.detect({ content: "author: Alice\nstatus: active", detectors: [] }), storage);
      expect(detectResult0.variant).toBe("ok");
      let detections = detectResult0.output["detections"];
      let d = detections;
      const thenResult0 = await interpret(structureDetectorHandler.propose({ entityId: "doc-1", detections: d }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register-then-detect", async () => {
      const storage = createInMemoryStorage();
      const registerDetectorResult0 = await interpret(structureDetectorHandler.registerDetector({ name: "score_detector", pattern: "score:\\s*(\\d+)", fieldType: "Int" }), storage);
      expect(registerDetectorResult0.variant).toBe("ok");
      let id = registerDetectorResult0.output["id"];
      let did = id;
      const thenResult0 = await interpret(structureDetectorHandler.detect({ content: "score: 42", detectors: ["score_detector"] }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: promotion-status-valid', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('detect'), input: fc.record({ content: fc.string({ minLength: 1, maxLength: 50 }), detectors: fc.string() }) }),
              fc.record({ action: fc.constant('propose'), input: fc.record({ entityId: fc.string({ minLength: 1, maxLength: 50 }), detections: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('apply'), input: fc.record({ entityId: fc.string({ minLength: 1, maxLength: 50 }), promotionId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dismiss'), input: fc.record({ entityId: fc.string({ minLength: 1, maxLength: 50 }), promotionId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listProposals'), input: fc.record({ entityId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listDetectors'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('registerDetector'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), pattern: fc.string({ minLength: 1, maxLength: 50 }), fieldType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = structureDetectorHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(structureDetectorHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: applied-without-entity', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('detect'), input: fc.record({ content: fc.string({ minLength: 1, maxLength: 50 }), detectors: fc.string() }) }),
              fc.record({ action: fc.constant('propose'), input: fc.record({ entityId: fc.string({ minLength: 1, maxLength: 50 }), detections: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('apply'), input: fc.record({ entityId: fc.string({ minLength: 1, maxLength: 50 }), promotionId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dismiss'), input: fc.record({ entityId: fc.string({ minLength: 1, maxLength: 50 }), promotionId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listProposals'), input: fc.record({ entityId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listDetectors'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('registerDetector'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), pattern: fc.string({ minLength: 1, maxLength: 50 }), fieldType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = structureDetectorHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(structureDetectorHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: applied-without-entity
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('detect handles empty input: ', async () => {
      if (typeof structureDetectorHandler.detect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(structureDetectorHandler.detect({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('propose handles empty input: ', async () => {
      if (typeof structureDetectorHandler.propose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(structureDetectorHandler.propose({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

});
