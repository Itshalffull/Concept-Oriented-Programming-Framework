// ScoreKernel Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { scoreKernelHandler } from '../../handlers/ts/score/score-kernel.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ScoreKernel functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('boot', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreKernelHandler.boot !== 'function') return;
      const result = await interpret(scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "boot_project" -> ok', async () => {
      if (typeof scoreKernelHandler.boot !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "boot_empty" -> error', async () => {
      if (typeof scoreKernelHandler.boot !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreKernelHandler.boot({ projectRoot: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('discover', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreKernelHandler.discover({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, basePaths: "src,specs" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreKernelHandler.discover({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, basePaths: "src,specs" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreKernelHandler.discover({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, basePaths: "src,specs" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreKernelHandler.discover({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, basePaths: "src,specs" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreKernelHandler.discover({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, basePaths: "src,specs" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreKernelHandler.discover !== 'function') return;
      const result = await interpret(scoreKernelHandler.discover({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, basePaths: "src,specs" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "discover_src" -> ok', async () => {
      if (typeof scoreKernelHandler.discover !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_boot_project = await interpret(scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" }), storage);
      const result = await interpret(scoreKernelHandler.discover({ kernel: afterResult_boot_project?.output?.["kernel"], basePaths: "src,specs" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "discover_not_booted" -> error', async () => {
      if (typeof scoreKernelHandler.discover !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreKernelHandler.discover({ kernel: "nonexistent", basePaths: "src" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('status', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreKernelHandler.status({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreKernelHandler.status({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreKernelHandler.status({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreKernelHandler.status({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreKernelHandler.status({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreKernelHandler.status !== 'function') return;
      const result = await interpret(scoreKernelHandler.status({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "status_valid" -> ok', async () => {
      if (typeof scoreKernelHandler.status !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_boot_project = await interpret(scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" }), storage);
      const result = await interpret(scoreKernelHandler.status({ kernel: afterResult_boot_project?.output?.["kernel"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "status_missing" -> error', async () => {
      if (typeof scoreKernelHandler.status !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreKernelHandler.status({ kernel: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('connectRuntime', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreKernelHandler.connectRuntime({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, endpoint: "ws://localhost:8080/changes" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreKernelHandler.connectRuntime({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, endpoint: "ws://localhost:8080/changes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreKernelHandler.connectRuntime({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, endpoint: "ws://localhost:8080/changes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreKernelHandler.connectRuntime({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, endpoint: "ws://localhost:8080/changes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreKernelHandler.connectRuntime({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, endpoint: "ws://localhost:8080/changes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreKernelHandler.connectRuntime !== 'function') return;
      const result = await interpret(scoreKernelHandler.connectRuntime({ kernel: {"type":"ref","fixture":"boot_project","field":"kernel"}, endpoint: "ws://localhost:8080/changes" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "connect_local" -> ok', async () => {
      if (typeof scoreKernelHandler.connectRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_boot_project = await interpret(scoreKernelHandler.boot({ projectRoot: "/home/user/my-project" }), storage);
      const result = await interpret(scoreKernelHandler.connectRuntime({ kernel: afterResult_boot_project?.output?.["kernel"], endpoint: "ws://localhost:8080/changes" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "connect_not_booted" -> error', async () => {
      if (typeof scoreKernelHandler.connectRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreKernelHandler.connectRuntime({ kernel: "nonexistent", endpoint: "ws://localhost:8080" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof scoreKernelHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = scoreKernelHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ScoreKernel');
    });
  });

  describe('invariant examples', () => {
    it("booted kernel reports status", async () => {
      const storage = createInMemoryStorage();
      const bootResult0 = await interpret(scoreKernelHandler.boot({ projectRoot: "/tmp/test-project" }), storage);
      expect(bootResult0.variant).toBe("ok");
      let kernel = bootResult0.output["kernel"];
      let k = kernel;
      let conceptCount = bootResult0.output["conceptCount"];
      let _ = conceptCount;
      let syncCount = bootResult0.output["syncCount"];
      const thenResult0 = await interpret(scoreKernelHandler.status({ kernel: k }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate registration returns existing", async () => {
      const storage = createInMemoryStorage();
      const bootResult0 = await interpret(scoreKernelHandler.boot({ projectRoot: "/tmp/test-project" }), storage);
      expect(bootResult0.variant).toBe("ok");
      let kernel = bootResult0.output["kernel"];
      let k = kernel;
      let conceptCount = bootResult0.output["conceptCount"];
      let _ = conceptCount;
      let syncCount = bootResult0.output["syncCount"];
      const thenResult0 = await interpret(scoreKernelHandler.boot({ projectRoot: "/tmp/test-project" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every kernels entry has projectRoot', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('boot'), input: fc.record({ projectRoot: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('discover'), input: fc.record({ kernel: fc.string(), basePaths: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('status'), input: fc.record({ kernel: fc.string() }) }),
              fc.record({ action: fc.constant('connectRuntime'), input: fc.record({ kernel: fc.string(), endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = scoreKernelHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(scoreKernelHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty projectRoot in kernels', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('boot'), input: fc.record({ projectRoot: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('discover'), input: fc.record({ kernel: fc.string(), basePaths: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('status'), input: fc.record({ kernel: fc.string() }) }),
              fc.record({ action: fc.constant('connectRuntime'), input: fc.record({ kernel: fc.string(), endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = scoreKernelHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(scoreKernelHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty projectRoot in kernels
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('boot handles empty input: ', async () => {
      if (typeof scoreKernelHandler.boot !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(scoreKernelHandler.boot({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('boot ensures on ok: ', async () => {
      if (typeof scoreKernelHandler.boot !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ projectRoot: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = scoreKernelHandler.boot(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
