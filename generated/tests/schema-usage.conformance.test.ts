// SchemaUsage Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { schemaUsageHandler } from '../../handlers/ts/app/schema-usage.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SchemaUsage functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaUsageHandler.register !== 'function') return;
      const result = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_ok" -> ok', async () => {
      if (typeof schemaUsageHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_sort" -> ok', async () => {
      if (typeof schemaUsageHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "sort", usageRef: "content-sort-spec", usageLabel: "Sort by title" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof schemaUsageHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Duplicate registration" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaUsageHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('unregister', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaUsageHandler.unregister({ field: "article:title", usageRef: "content-list-view" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaUsageHandler.unregister({ field: "article:title", usageRef: "content-list-view" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaUsageHandler.unregister({ field: "article:title", usageRef: "content-list-view" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaUsageHandler.unregister({ field: "article:title", usageRef: "content-list-view" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaUsageHandler.unregister({ field: "article:title", usageRef: "content-list-view" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaUsageHandler.unregister !== 'function') return;
      const result = await interpret(schemaUsageHandler.unregister({ field: "article:title", usageRef: "content-list-view" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "unregister_ok" -> ok', async () => {
      if (typeof schemaUsageHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { field: "article:title", usageRef: "content-list-view" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaUsageHandler.unregister({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unregister_missing" -> not_found', async () => {
      if (typeof schemaUsageHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaUsageHandler.unregister({ field: "article:title", usageRef: "nonexistent-ref" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('scan', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaUsageHandler.scan({ field: "article:title" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaUsageHandler.scan({ field: "article:title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaUsageHandler.scan({ field: "article:title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaUsageHandler.scan({ field: "article:title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaUsageHandler.scan({ field: "article:title" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaUsageHandler.scan !== 'function') return;
      const result = await interpret(schemaUsageHandler.scan({ field: "article:title" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "scan_ok" -> ok', async () => {
      if (typeof schemaUsageHandler.scan !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { field: "article:title" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaUsageHandler.scan({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "scan_empty" -> ok', async () => {
      if (typeof schemaUsageHandler.scan !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { field: "article:body" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaUsageHandler.scan({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('scanSchema', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaUsageHandler.scanSchema({ schema: "article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaUsageHandler.scanSchema({ schema: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaUsageHandler.scanSchema({ schema: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaUsageHandler.scanSchema({ schema: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaUsageHandler.scanSchema({ schema: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaUsageHandler.scanSchema !== 'function') return;
      const result = await interpret(schemaUsageHandler.scanSchema({ schema: "article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "scan_schema_ok" -> ok', async () => {
      if (typeof schemaUsageHandler.scanSchema !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { schema: "article" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaUsageHandler.scanSchema({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "scan_schema_empty" -> ok', async () => {
      if (typeof schemaUsageHandler.scanSchema !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { schema: "nonexistent-schema" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaUsageHandler.scanSchema({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('remove', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaUsageHandler.remove({ field: "article:title", usageRef: "content-list-view" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaUsageHandler.remove({ field: "article:title", usageRef: "content-list-view" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaUsageHandler.remove({ field: "article:title", usageRef: "content-list-view" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaUsageHandler.remove({ field: "article:title", usageRef: "content-list-view" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaUsageHandler.remove({ field: "article:title", usageRef: "content-list-view" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaUsageHandler.remove !== 'function') return;
      const result = await interpret(schemaUsageHandler.remove({ field: "article:title", usageRef: "content-list-view" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_ok" -> ok', async () => {
      if (typeof schemaUsageHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaUsageHandler.register({ field: "article:title", usageType: "view", usageRef: "content-list-view", usageLabel: "Content list — column 1" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { field: "article:title", usageRef: "content-list-view" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaUsageHandler.remove({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_missing" -> not_found', async () => {
      if (typeof schemaUsageHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaUsageHandler.remove({ field: "article:title", usageRef: "nonexistent-ref" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });


  describe('invariant examples', () => {
    it("register then scan returns usage", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(schemaUsageHandler.register({ field: "tasks:status", usageType: "filter", usageRef: "task-filter-spec", usageLabel: "Task board filter by status" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let u = id;
      const thenResult0 = await interpret(schemaUsageHandler.scan({ field: "tasks:status" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register then unregister clears scan", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(schemaUsageHandler.register({ field: "tasks:priority", usageType: "sort", usageRef: "task-sort-spec", usageLabel: "Sort tasks by priority" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let u = id;
      const thenResult0 = await interpret(schemaUsageHandler.unregister({ field: "tasks:priority", usageRef: "task-sort-spec" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate registration rejected", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(schemaUsageHandler.register({ field: "events:startDate", usageType: "view", usageRef: "events-view", usageLabel: "Events list — start date column" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let u = id;
      const thenResult0 = await interpret(schemaUsageHandler.register({ field: "events:startDate", usageType: "view", usageRef: "events-view", usageLabel: "Duplicate" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: field key is non-empty', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }), usageType: fc.string({ minLength: 1, maxLength: 50 }), usageRef: fc.string({ minLength: 1, maxLength: 50 }), usageLabel: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }), usageRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('scanSchema'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }), usageRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = schemaUsageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(schemaUsageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: usageRef is non-empty', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }), usageType: fc.string({ minLength: 1, maxLength: 50 }), usageRef: fc.string({ minLength: 1, maxLength: 50 }), usageLabel: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }), usageRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('scanSchema'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }), usageRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = schemaUsageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(schemaUsageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: usage without type', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }), usageType: fc.string({ minLength: 1, maxLength: 50 }), usageRef: fc.string({ minLength: 1, maxLength: 50 }), usageLabel: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }), usageRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('scanSchema'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ field: fc.string({ minLength: 1, maxLength: 50 }), usageRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = schemaUsageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(schemaUsageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: usage without type
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
