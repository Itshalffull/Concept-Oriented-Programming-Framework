// Invocation Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { invocationHandler } from '../../handlers/ts/app/invocation.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Invocation functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('start', () => {
    it('builds a valid StorageProgram', () => {
      const program = invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof invocationHandler.start !== 'function') return;
      const result = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "start_ok" -> ok', async () => {
      if (typeof invocationHandler.start !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "start_duplicate" -> duplicate', async () => {
      if (typeof invocationHandler.start !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const _pool = Object.assign({}, (afterResult_start_ok?.output ?? {}));
      const _fixtureInput = { invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:01Z" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(invocationHandler.start({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "start_empty_binding" -> error', async () => {
      if (typeof invocationHandler.start !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(invocationHandler.start({ invocation: "inv-002", connection: "conn-001", binding: "", params: "e30=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('complete', () => {
    it('builds a valid StorageProgram', () => {
      const program = invocationHandler.complete({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:05Z" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = invocationHandler.complete({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:05Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = invocationHandler.complete({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:05Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = invocationHandler.complete({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:05Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = invocationHandler.complete({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:05Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof invocationHandler.complete !== 'function') return;
      const result = await interpret(invocationHandler.complete({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:05Z" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "complete_ok" -> ok', async () => {
      if (typeof invocationHandler.complete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const result = await interpret(invocationHandler.complete({ invocation: afterResult_start_ok?.output?.["invocation"], result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:05Z" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "complete_not_found" -> not_found', async () => {
      if (typeof invocationHandler.complete !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(invocationHandler.complete({ invocation: "inv-missing", result: "e30=", completedAt: "2026-04-13T10:00:05Z" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

    it('fixture "complete_already_completed" -> already_completed', async () => {
      if (typeof invocationHandler.complete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const afterResult_complete_ok = await interpret(invocationHandler.complete({ invocation: afterResult_start_ok?.output?.["invocation"], result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:05Z" }), storage);
      const result = await interpret(invocationHandler.complete({ invocation: afterResult_start_ok?.output?.["invocation"], result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:06Z" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('already_completed'));
    });

  });

  describe('fail', () => {
    it('builds a valid StorageProgram', () => {
      const program = invocationHandler.fail({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, error: "Kernel returned error: Not found", completedAt: "2026-04-13T10:00:05Z" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = invocationHandler.fail({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, error: "Kernel returned error: Not found", completedAt: "2026-04-13T10:00:05Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = invocationHandler.fail({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, error: "Kernel returned error: Not found", completedAt: "2026-04-13T10:00:05Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = invocationHandler.fail({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, error: "Kernel returned error: Not found", completedAt: "2026-04-13T10:00:05Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = invocationHandler.fail({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, error: "Kernel returned error: Not found", completedAt: "2026-04-13T10:00:05Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof invocationHandler.fail !== 'function') return;
      const result = await interpret(invocationHandler.fail({ invocation: {"type":"ref","fixture":"start_ok","field":"invocation"}, error: "Kernel returned error: Not found", completedAt: "2026-04-13T10:00:05Z" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "fail_ok" -> ok', async () => {
      if (typeof invocationHandler.fail !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const result = await interpret(invocationHandler.fail({ invocation: afterResult_start_ok?.output?.["invocation"], error: "Kernel returned error: Not found", completedAt: "2026-04-13T10:00:05Z" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "fail_not_found" -> not_found', async () => {
      if (typeof invocationHandler.fail !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(invocationHandler.fail({ invocation: "inv-missing", error: "timeout", completedAt: "2026-04-13T10:00:05Z" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

    it('fixture "fail_already_completed" -> already_completed', async () => {
      if (typeof invocationHandler.fail !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const afterResult_fail_ok = await interpret(invocationHandler.fail({ invocation: afterResult_start_ok?.output?.["invocation"], error: "Kernel returned error: Not found", completedAt: "2026-04-13T10:00:05Z" }), storage);
      const result = await interpret(invocationHandler.fail({ invocation: afterResult_start_ok?.output?.["invocation"], error: "second error", completedAt: "2026-04-13T10:00:06Z" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('already_completed'));
    });

  });

  describe('retry', () => {
    it('builds a valid StorageProgram', () => {
      const program = invocationHandler.retry({ invocation: {"type":"ref","fixture":"fail_ok","field":"invocation"}, newInvocation: "inv-retry-001", startedAt: "2026-04-13T10:00:10Z" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = invocationHandler.retry({ invocation: {"type":"ref","fixture":"fail_ok","field":"invocation"}, newInvocation: "inv-retry-001", startedAt: "2026-04-13T10:00:10Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = invocationHandler.retry({ invocation: {"type":"ref","fixture":"fail_ok","field":"invocation"}, newInvocation: "inv-retry-001", startedAt: "2026-04-13T10:00:10Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = invocationHandler.retry({ invocation: {"type":"ref","fixture":"fail_ok","field":"invocation"}, newInvocation: "inv-retry-001", startedAt: "2026-04-13T10:00:10Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = invocationHandler.retry({ invocation: {"type":"ref","fixture":"fail_ok","field":"invocation"}, newInvocation: "inv-retry-001", startedAt: "2026-04-13T10:00:10Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof invocationHandler.retry !== 'function') return;
      const result = await interpret(invocationHandler.retry({ invocation: {"type":"ref","fixture":"fail_ok","field":"invocation"}, newInvocation: "inv-retry-001", startedAt: "2026-04-13T10:00:10Z" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "retry_ok" -> ok', async () => {
      if (typeof invocationHandler.retry !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const afterResult_fail_ok = await interpret(invocationHandler.fail({ invocation: afterResult_start_ok?.output?.["invocation"], error: "Kernel returned error: Not found", completedAt: "2026-04-13T10:00:05Z" }), storage);
      const result = await interpret(invocationHandler.retry({ invocation: afterResult_fail_ok?.output?.["invocation"], newInvocation: "inv-retry-001", startedAt: "2026-04-13T10:00:10Z" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "retry_not_found" -> not_found', async () => {
      if (typeof invocationHandler.retry !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(invocationHandler.retry({ invocation: "inv-missing", newInvocation: "inv-retry-002", startedAt: "2026-04-13T10:00:10Z" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

    it('fixture "retry_not_failed" -> not_failed', async () => {
      if (typeof invocationHandler.retry !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const result = await interpret(invocationHandler.retry({ invocation: afterResult_start_ok?.output?.["invocation"], newInvocation: "inv-retry-003", startedAt: "2026-04-13T10:00:10Z" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_failed'));
    });

  });

  describe('dismiss', () => {
    it('builds a valid StorageProgram', () => {
      const program = invocationHandler.dismiss({ invocation: {"type":"ref","fixture":"complete_ok","field":"invocation"}, dismissedAt: "2026-04-13T10:01:00Z" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = invocationHandler.dismiss({ invocation: {"type":"ref","fixture":"complete_ok","field":"invocation"}, dismissedAt: "2026-04-13T10:01:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = invocationHandler.dismiss({ invocation: {"type":"ref","fixture":"complete_ok","field":"invocation"}, dismissedAt: "2026-04-13T10:01:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = invocationHandler.dismiss({ invocation: {"type":"ref","fixture":"complete_ok","field":"invocation"}, dismissedAt: "2026-04-13T10:01:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = invocationHandler.dismiss({ invocation: {"type":"ref","fixture":"complete_ok","field":"invocation"}, dismissedAt: "2026-04-13T10:01:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof invocationHandler.dismiss !== 'function') return;
      const result = await interpret(invocationHandler.dismiss({ invocation: {"type":"ref","fixture":"complete_ok","field":"invocation"}, dismissedAt: "2026-04-13T10:01:00Z" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "dismiss_ok" -> ok', async () => {
      if (typeof invocationHandler.dismiss !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const afterResult_complete_ok = await interpret(invocationHandler.complete({ invocation: afterResult_start_ok?.output?.["invocation"], result: "eyJzdGF0dXMiOiJkb25lIn0=", completedAt: "2026-04-13T10:00:05Z" }), storage);
      const result = await interpret(invocationHandler.dismiss({ invocation: afterResult_complete_ok?.output?.["invocation"], dismissedAt: "2026-04-13T10:01:00Z" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "dismiss_not_found" -> not_found', async () => {
      if (typeof invocationHandler.dismiss !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(invocationHandler.dismiss({ invocation: "inv-missing", dismissedAt: "2026-04-13T10:01:00Z" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('query', () => {
    it('builds a valid StorageProgram', () => {
      const program = invocationHandler.query({ connection: "conn-001", binding: null, since: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = invocationHandler.query({ connection: "conn-001", binding: null, since: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = invocationHandler.query({ connection: "conn-001", binding: null, since: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = invocationHandler.query({ connection: "conn-001", binding: null, since: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = invocationHandler.query({ connection: "conn-001", binding: null, since: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof invocationHandler.query !== 'function') return;
      const result = await interpret(invocationHandler.query({ connection: "conn-001", binding: null, since: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "query_all" -> ok', async () => {
      if (typeof invocationHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const _pool = Object.assign({}, (afterResult_start_ok?.output ?? {}));
      const _fixtureInput = { connection: "conn-001", binding: null, since: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(invocationHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_by_binding" -> ok', async () => {
      if (typeof invocationHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const _pool = Object.assign({}, (afterResult_start_ok?.output ?? {}));
      const _fixtureInput = { connection: "conn-001", binding: "binding-abc", since: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(invocationHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_since" -> ok', async () => {
      if (typeof invocationHandler.query !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_start_ok = await interpret(invocationHandler.start({ invocation: "inv-001", connection: "conn-001", binding: "binding-abc", params: "eyJpZCI6InQxIn0=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      const _pool = Object.assign({}, (afterResult_start_ok?.output ?? {}));
      const _fixtureInput = { connection: "conn-001", binding: null, since: "2026-04-13T10:00:00Z" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(invocationHandler.query({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof invocationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = invocationHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Invocation');
    });
  });

  describe('invariant examples', () => {
    it("start-then-complete", async () => {
      const storage = createInMemoryStorage();
      const startResult0 = await interpret(invocationHandler.start({ invocation: "i1", connection: "c1", binding: "b1", params: "e30=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      expect(startResult0.variant).toBe("ok");
      const thenResult0 = await interpret(invocationHandler.complete({ invocation: "i1", result: "eyJvayI6dHJ1ZX0=", completedAt: "2026-04-13T10:00:05Z" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(invocationHandler.query({ connection: "c1", binding: false, since: false }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

    it("start-then-fail-then-retry", async () => {
      const storage = createInMemoryStorage();
      const startResult0 = await interpret(invocationHandler.start({ invocation: "i2", connection: "c1", binding: "b1", params: "e30=", startedAt: "2026-04-13T10:00:00Z" }), storage);
      expect(startResult0.variant).toBe("ok");
      const thenResult0 = await interpret(invocationHandler.fail({ invocation: "i2", error: "timeout", completedAt: "2026-04-13T10:00:03Z" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(invocationHandler.retry({ invocation: "i2", newInvocation: "i3", startedAt: "2026-04-13T10:00:04Z" }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: completed-has-result-or-error', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ invocation: fc.string(), connection: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string(), startedAt: fc.string() }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ invocation: fc.string(), result: fc.string(), completedAt: fc.string() }) }),
              fc.record({ action: fc.constant('fail'), input: fc.record({ invocation: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }), completedAt: fc.string() }) }),
              fc.record({ action: fc.constant('retry'), input: fc.record({ invocation: fc.string(), newInvocation: fc.string(), startedAt: fc.string() }) }),
              fc.record({ action: fc.constant('dismiss'), input: fc.record({ invocation: fc.string(), dismissedAt: fc.string() }) }),
              fc.record({ action: fc.constant('query'), input: fc.record({ connection: fc.string(), binding: fc.string(), since: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = invocationHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(invocationHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: retried-from-non-failed', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ invocation: fc.string(), connection: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string(), startedAt: fc.string() }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ invocation: fc.string(), result: fc.string(), completedAt: fc.string() }) }),
              fc.record({ action: fc.constant('fail'), input: fc.record({ invocation: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }), completedAt: fc.string() }) }),
              fc.record({ action: fc.constant('retry'), input: fc.record({ invocation: fc.string(), newInvocation: fc.string(), startedAt: fc.string() }) }),
              fc.record({ action: fc.constant('dismiss'), input: fc.record({ invocation: fc.string(), dismissedAt: fc.string() }) }),
              fc.record({ action: fc.constant('query'), input: fc.record({ connection: fc.string(), binding: fc.string(), since: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = invocationHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(invocationHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: retried-from-non-failed
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: complete-without-start', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ invocation: fc.string(), connection: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string(), startedAt: fc.string() }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ invocation: fc.string(), result: fc.string(), completedAt: fc.string() }) }),
              fc.record({ action: fc.constant('fail'), input: fc.record({ invocation: fc.string(), error: fc.string({ minLength: 1, maxLength: 50 }), completedAt: fc.string() }) }),
              fc.record({ action: fc.constant('retry'), input: fc.record({ invocation: fc.string(), newInvocation: fc.string(), startedAt: fc.string() }) }),
              fc.record({ action: fc.constant('dismiss'), input: fc.record({ invocation: fc.string(), dismissedAt: fc.string() }) }),
              fc.record({ action: fc.constant('query'), input: fc.record({ connection: fc.string(), binding: fc.string(), since: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = invocationHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(invocationHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: complete-without-start
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
