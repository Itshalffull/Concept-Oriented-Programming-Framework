// InfrastructureEntity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { infrastructureEntityHandler } from '../../handlers/ts/score/infrastructure-entity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('InfrastructureEntity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof infrastructureEntityHandler.register !== 'function') return;
      const result = await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_app_storage" -> ok', async () => {
      if (typeof infrastructureEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_name" -> error', async () => {
      if (typeof infrastructureEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(infrastructureEntityHandler.register({ name: "", kind: "storage", sourceFile: "adapters/empty.ts", backend: "postgresql", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = infrastructureEntityHandler.get({ name: "AppStorage", kind: "storage" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = infrastructureEntityHandler.get({ name: "AppStorage", kind: "storage" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = infrastructureEntityHandler.get({ name: "AppStorage", kind: "storage" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = infrastructureEntityHandler.get({ name: "AppStorage", kind: "storage" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = infrastructureEntityHandler.get({ name: "AppStorage", kind: "storage" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof infrastructureEntityHandler.get !== 'function') return;
      const result = await interpret(infrastructureEntityHandler.get({ name: "AppStorage", kind: "storage" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_app_storage" -> ok', async () => {
      if (typeof infrastructureEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.get({ name: "AppStorage", kind: "storage" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_nonexistent" -> error', async () => {
      if (typeof infrastructureEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(infrastructureEntityHandler.get({ name: "Nonexistent", kind: "storage" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByBackend', () => {
    it('builds a valid StorageProgram', () => {
      const program = infrastructureEntityHandler.findByBackend({ backend: "postgresql" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = infrastructureEntityHandler.findByBackend({ backend: "postgresql" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = infrastructureEntityHandler.findByBackend({ backend: "postgresql" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = infrastructureEntityHandler.findByBackend({ backend: "postgresql" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = infrastructureEntityHandler.findByBackend({ backend: "postgresql" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof infrastructureEntityHandler.findByBackend !== 'function') return;
      const result = await interpret(infrastructureEntityHandler.findByBackend({ backend: "postgresql" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_postgresql_adapters" -> ok', async () => {
      if (typeof infrastructureEntityHandler.findByBackend !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.findByBackend({ backend: "postgresql" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unknown_backend" -> ok', async () => {
      if (typeof infrastructureEntityHandler.findByBackend !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.findByBackend({ backend: "unknown-db" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('findByConcept', () => {
    it('builds a valid StorageProgram', () => {
      const program = infrastructureEntityHandler.findByConcept({ concept: "Article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = infrastructureEntityHandler.findByConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = infrastructureEntityHandler.findByConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = infrastructureEntityHandler.findByConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = infrastructureEntityHandler.findByConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof infrastructureEntityHandler.findByConcept !== 'function') return;
      const result = await interpret(infrastructureEntityHandler.findByConcept({ concept: "Article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_article_adapters" -> ok', async () => {
      if (typeof infrastructureEntityHandler.findByConcept !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.findByConcept({ concept: "Article" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unbound_concept" -> ok', async () => {
      if (typeof infrastructureEntityHandler.findByConcept !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.findByConcept({ concept: "Nonexistent" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('findByRuntime', () => {
    it('builds a valid StorageProgram', () => {
      const program = infrastructureEntityHandler.findByRuntime({ runtime: "api-server" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = infrastructureEntityHandler.findByRuntime({ runtime: "api-server" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = infrastructureEntityHandler.findByRuntime({ runtime: "api-server" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = infrastructureEntityHandler.findByRuntime({ runtime: "api-server" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = infrastructureEntityHandler.findByRuntime({ runtime: "api-server" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof infrastructureEntityHandler.findByRuntime !== 'function') return;
      const result = await interpret(infrastructureEntityHandler.findByRuntime({ runtime: "api-server" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_api_runtime" -> ok', async () => {
      if (typeof infrastructureEntityHandler.findByRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.findByRuntime({ runtime: "api-server" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_unknown_runtime" -> ok', async () => {
      if (typeof infrastructureEntityHandler.findByRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.findByRuntime({ runtime: "nonexistent-runtime" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('sharedBackends', () => {
    it('builds a valid StorageProgram', () => {
      const program = infrastructureEntityHandler.sharedBackends({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = infrastructureEntityHandler.sharedBackends({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = infrastructureEntityHandler.sharedBackends({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = infrastructureEntityHandler.sharedBackends({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = infrastructureEntityHandler.sharedBackends({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof infrastructureEntityHandler.sharedBackends !== 'function') return;
      const result = await interpret(infrastructureEntityHandler.sharedBackends({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof infrastructureEntityHandler.sharedBackends !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.sharedBackends({  }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "shared_backend_failure" -> ok', async () => {
      if (typeof infrastructureEntityHandler.sharedBackends !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.sharedBackends({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('networkTopology', () => {
    it('builds a valid StorageProgram', () => {
      const program = infrastructureEntityHandler.networkTopology({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = infrastructureEntityHandler.networkTopology({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = infrastructureEntityHandler.networkTopology({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = infrastructureEntityHandler.networkTopology({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = infrastructureEntityHandler.networkTopology({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof infrastructureEntityHandler.networkTopology !== 'function') return;
      const result = await interpret(infrastructureEntityHandler.networkTopology({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof infrastructureEntityHandler.networkTopology !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.networkTopology({  }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "topology_failure" -> ok', async () => {
      if (typeof infrastructureEntityHandler.networkTopology !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({ name: "AppStorage", kind: "storage", sourceFile: "adapters/app-storage.ts", backend: "postgresql", config: "{}" }), storage));
      const result = await interpret(infrastructureEntityHandler.networkTopology({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof infrastructureEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = infrastructureEntityHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('InfrastructureEntity');
    });
  });

  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(infrastructureEntityHandler.register({ name: {"type":"literal","value":"AppStorage"}, kind: {"type":"literal","value":"storage"}, sourceFile: {"type":"literal","value":"adapters/app-storage.ts"}, backend: {"type":"literal","value":"postgresql"}, config: {"type":"literal","value":"{}"} }), storage);
      expect(registerResult0.variant).toBe("ok");
      const adapter = registerResult0.output["adapter"];
      const thenResult0 = await interpret(infrastructureEntityHandler.get({ name: {"type":"literal","value":"AppStorage"}, kind: {"type":"literal","value":"storage"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every adapters entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), sourceFile: fc.string({ minLength: 1, maxLength: 50 }), backend: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByBackend'), input: fc.record({ backend: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByRuntime'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sharedBackends'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('networkTopology'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = infrastructureEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(infrastructureEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in adapters', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), sourceFile: fc.string({ minLength: 1, maxLength: 50 }), backend: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByBackend'), input: fc.record({ backend: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByRuntime'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sharedBackends'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('networkTopology'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = infrastructureEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(infrastructureEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty name in adapters
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof infrastructureEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(infrastructureEntityHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof infrastructureEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), sourceFile: fc.string({ minLength: 1, maxLength: 50 }), backend: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = infrastructureEntityHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
