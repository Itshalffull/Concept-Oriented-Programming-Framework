// QueryProgram Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { queryProgramHandler } from '../../handlers/ts/view/query-program.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('QueryProgram functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.create({ program: "qprog-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.create({ program: "qprog-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.create({ program: "qprog-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.create({ program: "qprog-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.create({ program: "qprog-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.create !== 'function') return;
      const result = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_new" -> ok', async () => {
      if (typeof queryProgramHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_prog_a" -> ok', async () => {
      if (typeof queryProgramHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.create({ program: "qprog-a" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_prog_b" -> ok', async () => {
      if (typeof queryProgramHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.create({ program: "qprog-b" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_duplicate" -> exists', async () => {
      if (typeof queryProgramHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('exists'));
    });

  });

  describe('scan', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.scan({ program: "qprog-1", source: "contentNodes", bindAs: "nodes" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.scan({ program: "qprog-1", source: "contentNodes", bindAs: "nodes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.scan({ program: "qprog-1", source: "contentNodes", bindAs: "nodes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.scan({ program: "qprog-1", source: "contentNodes", bindAs: "nodes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.scan({ program: "qprog-1", source: "contentNodes", bindAs: "nodes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.scan !== 'function') return;
      const result = await interpret(queryProgramHandler.scan({ program: "qprog-1", source: "contentNodes", bindAs: "nodes" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "scan_source" -> ok', async () => {
      if (typeof queryProgramHandler.scan !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", source: "contentNodes", bindAs: "nodes" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.scan({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "scan_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.scan !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.scan({ program: "nonexistent", source: "contentNodes", bindAs: "nodes" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('filter', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.filter({ program: "qprog-1", node: "{\"type\":\"eq\",\"field\":\"kind\",\"value\":\"concept\"}", bindAs: "filtered" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.filter({ program: "qprog-1", node: "{\"type\":\"eq\",\"field\":\"kind\",\"value\":\"concept\"}", bindAs: "filtered" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.filter({ program: "qprog-1", node: "{\"type\":\"eq\",\"field\":\"kind\",\"value\":\"concept\"}", bindAs: "filtered" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.filter({ program: "qprog-1", node: "{\"type\":\"eq\",\"field\":\"kind\",\"value\":\"concept\"}", bindAs: "filtered" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.filter({ program: "qprog-1", node: "{\"type\":\"eq\",\"field\":\"kind\",\"value\":\"concept\"}", bindAs: "filtered" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.filter !== 'function') return;
      const result = await interpret(queryProgramHandler.filter({ program: "qprog-1", node: "{\"type\":\"eq\",\"field\":\"kind\",\"value\":\"concept\"}", bindAs: "filtered" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "filter_by_type" -> ok', async () => {
      if (typeof queryProgramHandler.filter !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", node: "{\"type\":\"eq\",\"field\":\"kind\",\"value\":\"concept\"}", bindAs: "filtered" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.filter({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "filter_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.filter !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.filter({ program: "nonexistent", node: "{\"type\":\"true\"}", bindAs: "filtered" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('sort', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.sort({ program: "qprog-1", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]", bindAs: "sorted" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.sort({ program: "qprog-1", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]", bindAs: "sorted" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.sort({ program: "qprog-1", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]", bindAs: "sorted" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.sort({ program: "qprog-1", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]", bindAs: "sorted" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.sort({ program: "qprog-1", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]", bindAs: "sorted" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.sort !== 'function') return;
      const result = await interpret(queryProgramHandler.sort({ program: "qprog-1", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]", bindAs: "sorted" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "sort_by_name" -> ok', async () => {
      if (typeof queryProgramHandler.sort !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", keys: "[{\"field\":\"node\",\"direction\":\"asc\"}]", bindAs: "sorted" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.sort({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "sort_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.sort !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.sort({ program: "nonexistent", keys: "[]", bindAs: "sorted" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('group', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.group({ program: "qprog-1", keys: "[\"kind\"]", config: "{\"aggregate\":\"count\"}", bindAs: "grouped" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.group({ program: "qprog-1", keys: "[\"kind\"]", config: "{\"aggregate\":\"count\"}", bindAs: "grouped" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.group({ program: "qprog-1", keys: "[\"kind\"]", config: "{\"aggregate\":\"count\"}", bindAs: "grouped" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.group({ program: "qprog-1", keys: "[\"kind\"]", config: "{\"aggregate\":\"count\"}", bindAs: "grouped" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.group({ program: "qprog-1", keys: "[\"kind\"]", config: "{\"aggregate\":\"count\"}", bindAs: "grouped" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.group !== 'function') return;
      const result = await interpret(queryProgramHandler.group({ program: "qprog-1", keys: "[\"kind\"]", config: "{\"aggregate\":\"count\"}", bindAs: "grouped" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "group_by_kind" -> ok', async () => {
      if (typeof queryProgramHandler.group !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", keys: "[\"kind\"]", config: "{\"aggregate\":\"count\"}", bindAs: "grouped" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.group({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "group_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.group !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.group({ program: "nonexistent", keys: "[\"kind\"]", config: "{}", bindAs: "grouped" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('project', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.project({ program: "qprog-1", fields: "[\"id\",\"node\",\"kind\"]", bindAs: "projected" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.project({ program: "qprog-1", fields: "[\"id\",\"node\",\"kind\"]", bindAs: "projected" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.project({ program: "qprog-1", fields: "[\"id\",\"node\",\"kind\"]", bindAs: "projected" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.project({ program: "qprog-1", fields: "[\"id\",\"node\",\"kind\"]", bindAs: "projected" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.project({ program: "qprog-1", fields: "[\"id\",\"node\",\"kind\"]", bindAs: "projected" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.project !== 'function') return;
      const result = await interpret(queryProgramHandler.project({ program: "qprog-1", fields: "[\"id\",\"node\",\"kind\"]", bindAs: "projected" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "project_fields" -> ok', async () => {
      if (typeof queryProgramHandler.project !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", fields: "[\"id\",\"node\",\"kind\"]", bindAs: "projected" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.project({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "project_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.project !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.project({ program: "nonexistent", fields: "[\"id\"]", bindAs: "projected" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('limit', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.limit({ program: "qprog-1", count: "25", output: "page" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.limit({ program: "qprog-1", count: "25", output: "page" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.limit({ program: "qprog-1", count: "25", output: "page" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.limit({ program: "qprog-1", count: "25", output: "page" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.limit({ program: "qprog-1", count: "25", output: "page" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.limit !== 'function') return;
      const result = await interpret(queryProgramHandler.limit({ program: "qprog-1", count: "25", output: "page" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "limit_records" -> ok', async () => {
      if (typeof queryProgramHandler.limit !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", count: "25", output: "page" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.limit({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "limit_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.limit !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.limit({ program: "nonexistent", count: "10", output: "page" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('pure', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.pure({ program: "qprog-1", variant: "ok", output: "nodes" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.pure({ program: "qprog-1", variant: "ok", output: "nodes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.pure({ program: "qprog-1", variant: "ok", output: "nodes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.pure({ program: "qprog-1", variant: "ok", output: "nodes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.pure({ program: "qprog-1", variant: "ok", output: "nodes" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.pure !== 'function') return;
      const result = await interpret(queryProgramHandler.pure({ program: "qprog-1", variant: "ok", output: "nodes" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "pure_ok" -> ok', async () => {
      if (typeof queryProgramHandler.pure !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", variant: "ok", output: "nodes" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.pure({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "pure_prog_a" -> ok', async () => {
      if (typeof queryProgramHandler.pure !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_prog_a = await interpret(queryProgramHandler.create({ program: "qprog-a" }), storage);
      const _pool = Object.assign({}, (afterResult_create_prog_a?.output ?? {}));
      const _fixtureInput = { program: "qprog-a", variant: "ok", output: "nodes" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.pure({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "pure_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.pure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.pure({ program: "nonexistent", variant: "ok", output: "nodes" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('join', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.join({ program: "qprog-1", source: "membership", localField: "node", foreignField: "entity_id", bindAs: "schemas" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.join({ program: "qprog-1", source: "membership", localField: "node", foreignField: "entity_id", bindAs: "schemas" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.join({ program: "qprog-1", source: "membership", localField: "node", foreignField: "entity_id", bindAs: "schemas" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.join({ program: "qprog-1", source: "membership", localField: "node", foreignField: "entity_id", bindAs: "schemas" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.join({ program: "qprog-1", source: "membership", localField: "node", foreignField: "entity_id", bindAs: "schemas" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.join !== 'function') return;
      const result = await interpret(queryProgramHandler.join({ program: "qprog-1", source: "membership", localField: "node", foreignField: "entity_id", bindAs: "schemas" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "join_membership" -> ok', async () => {
      if (typeof queryProgramHandler.join !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", source: "membership", localField: "node", foreignField: "entity_id", bindAs: "schemas" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.join({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "join_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.join !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.join({ program: "nonexistent", source: "membership", localField: "node", foreignField: "entity_id", bindAs: "schemas" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('compose', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.compose({ first: "qprog-a", second: "qprog-b", bindAs: "result" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.compose({ first: "qprog-a", second: "qprog-b", bindAs: "result" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.compose({ first: "qprog-a", second: "qprog-b", bindAs: "result" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.compose({ first: "qprog-a", second: "qprog-b", bindAs: "result" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.compose({ first: "qprog-a", second: "qprog-b", bindAs: "result" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.compose !== 'function') return;
      const result = await interpret(queryProgramHandler.compose({ first: "qprog-a", second: "qprog-b", bindAs: "result" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compose_programs" -> ok', async () => {
      if (typeof queryProgramHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_prog_a = await interpret(queryProgramHandler.create({ program: "qprog-a" }), storage);
      const afterResult_create_prog_b = await interpret(queryProgramHandler.create({ program: "qprog-b" }), storage);
      const _pool = Object.assign({}, (afterResult_create_prog_a?.output ?? {}), (afterResult_create_prog_b?.output ?? {}));
      const _fixtureInput = { first: "qprog-a", second: "qprog-b", bindAs: "result" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.compose({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compose_missing" -> notfound', async () => {
      if (typeof queryProgramHandler.compose !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.compose({ first: "nonexistent", second: "qprog-b", bindAs: "result" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('invoke', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.invoke({ program: "qprog-1", concept: "ContentNode", action: "create", input: "{\"node\":\"new-article\",\"kind\":\"concept\"}", bindAs: "createResult" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.invoke({ program: "qprog-1", concept: "ContentNode", action: "create", input: "{\"node\":\"new-article\",\"kind\":\"concept\"}", bindAs: "createResult" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.invoke({ program: "qprog-1", concept: "ContentNode", action: "create", input: "{\"node\":\"new-article\",\"kind\":\"concept\"}", bindAs: "createResult" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.invoke({ program: "qprog-1", concept: "ContentNode", action: "create", input: "{\"node\":\"new-article\",\"kind\":\"concept\"}", bindAs: "createResult" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.invoke({ program: "qprog-1", concept: "ContentNode", action: "create", input: "{\"node\":\"new-article\",\"kind\":\"concept\"}", bindAs: "createResult" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.invoke !== 'function') return;
      const result = await interpret(queryProgramHandler.invoke({ program: "qprog-1", concept: "ContentNode", action: "create", input: "{\"node\":\"new-article\",\"kind\":\"concept\"}", bindAs: "createResult" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "invoke_ok" -> ok', async () => {
      if (typeof queryProgramHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", concept: "ContentNode", action: "create", input: "{\"node\":\"new-article\",\"kind\":\"concept\"}", bindAs: "createResult" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.invoke({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invoke_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.invoke({ program: "nonexistent", concept: "ContentNode", action: "create", input: "{}", bindAs: "createResult" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "invoke_sealed_program" -> sealed', async () => {
      if (typeof queryProgramHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const afterResult_pure_ok = await interpret(queryProgramHandler.pure({ program: "qprog-1", variant: "ok", output: "nodes" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}), (afterResult_pure_ok?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", concept: "ContentNode", action: "create", input: "{}", bindAs: "createResult" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.invoke({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('sealed'));
    });

  });

  describe('match', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.match({ program: "qprog-1", binding: "createResult", cases: "{\"ok\":\"qprog-a\",\"*\":\"qprog-b\"}", bindAs: "final" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.match({ program: "qprog-1", binding: "createResult", cases: "{\"ok\":\"qprog-a\",\"*\":\"qprog-b\"}", bindAs: "final" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.match({ program: "qprog-1", binding: "createResult", cases: "{\"ok\":\"qprog-a\",\"*\":\"qprog-b\"}", bindAs: "final" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.match({ program: "qprog-1", binding: "createResult", cases: "{\"ok\":\"qprog-a\",\"*\":\"qprog-b\"}", bindAs: "final" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.match({ program: "qprog-1", binding: "createResult", cases: "{\"ok\":\"qprog-a\",\"*\":\"qprog-b\"}", bindAs: "final" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.match !== 'function') return;
      const result = await interpret(queryProgramHandler.match({ program: "qprog-1", binding: "createResult", cases: "{\"ok\":\"qprog-a\",\"*\":\"qprog-b\"}", bindAs: "final" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "match_ok" -> ok', async () => {
      if (typeof queryProgramHandler.match !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", binding: "createResult", cases: "{\"ok\":\"qprog-a\",\"*\":\"qprog-b\"}", bindAs: "final" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.match({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "match_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.match !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.match({ program: "nonexistent", binding: "createResult", cases: "{\"ok\":\"qprog-a\",\"*\":\"qprog-b\"}", bindAs: "final" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "match_invalid_cases" -> invalid_cases', async () => {
      if (typeof queryProgramHandler.match !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", binding: "createResult", cases: "not-valid-json", bindAs: "final" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.match({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_cases'));
    });

  });

  describe('traverseInvoke', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.traverseInvoke({ program: "qprog-1", sourceBinding: "overdue", itemBinding: "_task", concept: "Task", action: "escalate", inputTemplate: "{\"taskId\":\"$_task.id\"}", bindAs: "results" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.traverseInvoke({ program: "qprog-1", sourceBinding: "overdue", itemBinding: "_task", concept: "Task", action: "escalate", inputTemplate: "{\"taskId\":\"$_task.id\"}", bindAs: "results" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.traverseInvoke({ program: "qprog-1", sourceBinding: "overdue", itemBinding: "_task", concept: "Task", action: "escalate", inputTemplate: "{\"taskId\":\"$_task.id\"}", bindAs: "results" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.traverseInvoke({ program: "qprog-1", sourceBinding: "overdue", itemBinding: "_task", concept: "Task", action: "escalate", inputTemplate: "{\"taskId\":\"$_task.id\"}", bindAs: "results" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.traverseInvoke({ program: "qprog-1", sourceBinding: "overdue", itemBinding: "_task", concept: "Task", action: "escalate", inputTemplate: "{\"taskId\":\"$_task.id\"}", bindAs: "results" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.traverseInvoke !== 'function') return;
      const result = await interpret(queryProgramHandler.traverseInvoke({ program: "qprog-1", sourceBinding: "overdue", itemBinding: "_task", concept: "Task", action: "escalate", inputTemplate: "{\"taskId\":\"$_task.id\"}", bindAs: "results" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "traverse_invoke_ok" -> ok', async () => {
      if (typeof queryProgramHandler.traverseInvoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", sourceBinding: "overdue", itemBinding: "_task", concept: "Task", action: "escalate", inputTemplate: "{\"taskId\":\"$_task.id\"}", bindAs: "results" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.traverseInvoke({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "traverse_invoke_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.traverseInvoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.traverseInvoke({ program: "nonexistent", sourceBinding: "overdue", itemBinding: "_task", concept: "Task", action: "escalate", inputTemplate: "{\"taskId\":\"$_task.id\"}", bindAs: "results" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('traverse', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryProgramHandler.traverse({ program: "qprog-1", sourceBinding: "completed", itemBinding: "_task", bodyProgram: "qprog-a", bindAs: "outcomes", declaredEffects: "{\"invokedActions\":[\"Task/archive\"],\"completionVariants\":[\"ok\",\"error\"]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryProgramHandler.traverse({ program: "qprog-1", sourceBinding: "completed", itemBinding: "_task", bodyProgram: "qprog-a", bindAs: "outcomes", declaredEffects: "{\"invokedActions\":[\"Task/archive\"],\"completionVariants\":[\"ok\",\"error\"]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryProgramHandler.traverse({ program: "qprog-1", sourceBinding: "completed", itemBinding: "_task", bodyProgram: "qprog-a", bindAs: "outcomes", declaredEffects: "{\"invokedActions\":[\"Task/archive\"],\"completionVariants\":[\"ok\",\"error\"]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryProgramHandler.traverse({ program: "qprog-1", sourceBinding: "completed", itemBinding: "_task", bodyProgram: "qprog-a", bindAs: "outcomes", declaredEffects: "{\"invokedActions\":[\"Task/archive\"],\"completionVariants\":[\"ok\",\"error\"]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryProgramHandler.traverse({ program: "qprog-1", sourceBinding: "completed", itemBinding: "_task", bodyProgram: "qprog-a", bindAs: "outcomes", declaredEffects: "{\"invokedActions\":[\"Task/archive\"],\"completionVariants\":[\"ok\",\"error\"]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryProgramHandler.traverse !== 'function') return;
      const result = await interpret(queryProgramHandler.traverse({ program: "qprog-1", sourceBinding: "completed", itemBinding: "_task", bodyProgram: "qprog-a", bindAs: "outcomes", declaredEffects: "{\"invokedActions\":[\"Task/archive\"],\"completionVariants\":[\"ok\",\"error\"]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "traverse_ok" -> ok', async () => {
      if (typeof queryProgramHandler.traverse !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const afterResult_create_prog_a = await interpret(queryProgramHandler.create({ program: "qprog-a" }), storage);
      const afterResult_pure_prog_a = await interpret(queryProgramHandler.pure({ program: "qprog-a", variant: "ok", output: "nodes" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}), (afterResult_create_prog_a?.output ?? {}), (afterResult_pure_prog_a?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", sourceBinding: "completed", itemBinding: "_task", bodyProgram: "qprog-a", bindAs: "outcomes", declaredEffects: "{\"invokedActions\":[\"Task/archive\"],\"completionVariants\":[\"ok\",\"error\"]}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.traverse({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "traverse_missing_program" -> notfound', async () => {
      if (typeof queryProgramHandler.traverse !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryProgramHandler.traverse({ program: "nonexistent", sourceBinding: "completed", itemBinding: "_task", bodyProgram: "qprog-a", bindAs: "outcomes", declaredEffects: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "traverse_unsealed_body" -> not_sealed', async () => {
      if (typeof queryProgramHandler.traverse !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_new = await interpret(queryProgramHandler.create({ program: "qprog-1" }), storage);
      const afterResult_create_prog_a = await interpret(queryProgramHandler.create({ program: "qprog-a" }), storage);
      const _pool = Object.assign({}, (afterResult_create_new?.output ?? {}), (afterResult_create_prog_a?.output ?? {}));
      const _fixtureInput = { program: "qprog-1", sourceBinding: "completed", itemBinding: "_task", bodyProgram: "qprog-a", bindAs: "outcomes", declaredEffects: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(queryProgramHandler.traverse({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_sealed'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof queryProgramHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = queryProgramHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('QueryProgram');
    });
  });

  describe('invariant examples', () => {
    it("create, scan, filter, pure terminates program", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(queryProgramHandler.create({ program: "test-q" }), storage);
      expect(createResult0.variant).toBe("ok");
      const scanResult1 = await interpret(queryProgramHandler.scan({ program: "test-q", source: "contentNodes", bindAs: "nodes" }), storage);
      expect(scanResult1.variant).toBe("ok");
      let program = scanResult1.output["program"];
      let q = program;
      const filterResult2 = await interpret(queryProgramHandler.filter({ program: q, node: "{\"type\":\"true\"}", bindAs: "filtered" }), storage);
      expect(filterResult2.variant).toBe("ok");
      program = filterResult2.output["program"];
      const pureResult3 = await interpret(queryProgramHandler.pure({ program: q, variant: "ok", output: "filtered" }), storage);
      expect(pureResult3.variant).toBe("ok");
      program = pureResult3.output["program"];
      expect(pureResult3.output["terminated"]).toBe(true);
    });

    it("sealed program rejects new instructions", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(queryProgramHandler.create({ program: "test-q" }), storage);
      expect(createResult0.variant).toBe("ok");
      const pureResult1 = await interpret(queryProgramHandler.pure({ program: "test-q", variant: "ok", output: "nodes" }), storage);
      expect(pureResult1.variant).toBe("ok");
      let program = pureResult1.output["program"];
      let q = program;
      const thenResult0 = await interpret(queryProgramHandler.scan({ program: q, source: "contentNodes", bindAs: "nodes" }), storage);
      expect(thenResult0.variant).toBe("sealed");
    });

    it("compose chains two programs", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(queryProgramHandler.create({ program: "test-a" }), storage);
      expect(createResult0.variant).toBe("ok");
      const createResult1 = await interpret(queryProgramHandler.create({ program: "test-b" }), storage);
      expect(createResult1.variant).toBe("ok");
      const thenResult0 = await interpret(queryProgramHandler.compose({ first: "test-a", second: "test-b", bindAs: "result" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("create duplicate is rejected", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(queryProgramHandler.create({ program: "test-q" }), storage);
      expect(createResult0.variant).toBe("ok");
      const thenResult0 = await interpret(queryProgramHandler.create({ program: "test-q" }), storage);
      expect(thenResult0.variant).toBe("exists");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: terminated programs have instructions', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: programs have non-empty readFields when scanned', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: instruction appended after termination', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: instruction appended after termination
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: invoke instructions track their target', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: traverseInvoke instructions track their target', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: programs with invokes are read-write', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: read-only program contains invoke', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: read-only program contains invoke
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: invoke bindings are available downstream', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: match case sub-programs are sealed', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: traverse body sub-programs are sealed', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: traverse inherits body purity', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('scan'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('filter'), input: fc.record({ program: fc.string(), node: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('sort'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('group'), input: fc.record({ program: fc.string(), keys: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('project'), input: fc.record({ program: fc.string(), fields: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('limit'), input: fc.record({ program: fc.string(), count: fc.integer({ min: 1, max: 1000 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('join'), input: fc.record({ program: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), localField: fc.string({ minLength: 1, maxLength: 50 }), foreignField: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverseInvoke'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traverse'), input: fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryProgramHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof queryProgramHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(queryProgramHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof queryProgramHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = queryProgramHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('pure handles empty input: ', async () => {
      if (typeof queryProgramHandler.pure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(queryProgramHandler.pure({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('pure ensures on ok: ', async () => {
      if (typeof queryProgramHandler.pure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = queryProgramHandler.pure(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('invoke handles empty input: ', async () => {
      if (typeof queryProgramHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(queryProgramHandler.invoke({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('invoke ensures on ok: ', async () => {
      if (typeof queryProgramHandler.invoke !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = queryProgramHandler.invoke(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('match handles empty input: ', async () => {
      if (typeof queryProgramHandler.match !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(queryProgramHandler.match({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('match ensures on ok: ', async () => {
      if (typeof queryProgramHandler.match !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string(), binding: fc.string({ minLength: 1, maxLength: 50 }), cases: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = queryProgramHandler.match(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('traverseInvoke handles empty input: ', async () => {
      if (typeof queryProgramHandler.traverseInvoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(queryProgramHandler.traverseInvoke({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('traverseInvoke ensures on ok: ', async () => {
      if (typeof queryProgramHandler.traverseInvoke !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), inputTemplate: fc.string({ minLength: 1, maxLength: 50 }), bindAs: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = queryProgramHandler.traverseInvoke(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('traverse handles empty input: ', async () => {
      if (typeof queryProgramHandler.traverse !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(queryProgramHandler.traverse({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('traverse ensures on ok: ', async () => {
      if (typeof queryProgramHandler.traverse !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string(), sourceBinding: fc.string({ minLength: 1, maxLength: 50 }), itemBinding: fc.string({ minLength: 1, maxLength: 50 }), bodyProgram: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 50 }), declaredEffects: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = queryProgramHandler.traverse(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
