// InterfaceEntity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { interfaceEntityHandler } from '../../handlers/ts/score/interface-entity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('InterfaceEntity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.register !== 'function') return;
      const result = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_conduit_api" -> ok', async () => {
      if (typeof interfaceEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_minimal" -> ok', async () => {
      if (typeof interfaceEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.register({ name: "admin-cli", source: "cli.interface.yaml", manifest: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_name" -> error', async () => {
      if (typeof interfaceEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.register({ name: "", source: "empty.yaml", manifest: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.get({ name: "conduit-api" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.get({ name: "conduit-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.get({ name: "conduit-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.get({ name: "conduit-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.get({ name: "conduit-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.get !== 'function') return;
      const result = await interpret(interfaceEntityHandler.get({ name: "conduit-api" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_conduit" -> ok', async () => {
      if (typeof interfaceEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_conduit_api?.output ?? {}));
      const _fixtureInput = { name: "conduit-api" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(interfaceEntityHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> error', async () => {
      if (typeof interfaceEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.get({ name: "nonexistent-interface" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "get_api" -> ok', async () => {
      if (typeof interfaceEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_conduit_api?.output ?? {}));
      const _fixtureInput = { name: "conduit-api" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(interfaceEntityHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('listEndpoints', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.listEndpoints({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"}, target: "rest" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.listEndpoints({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"}, target: "rest" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.listEndpoints({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"}, target: "rest" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.listEndpoints({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"}, target: "rest" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.listEndpoints({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"}, target: "rest" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.listEndpoints !== 'function') return;
      const result = await interpret(interfaceEntityHandler.listEndpoints({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"}, target: "rest" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "endpoints_rest" -> ok', async () => {
      if (typeof interfaceEntityHandler.listEndpoints !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const result = await interpret(interfaceEntityHandler.listEndpoints({ interface: afterResult_register_conduit_api?.output?.["interface"], target: "rest" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "endpoints_empty_target" -> error', async () => {
      if (typeof interfaceEntityHandler.listEndpoints !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.listEndpoints({ interface: "iface-uuid-1", target: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listCommands', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.listCommands({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.listCommands({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.listCommands({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.listCommands({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.listCommands({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.listCommands !== 'function') return;
      const result = await interpret(interfaceEntityHandler.listCommands({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "commands_valid" -> ok', async () => {
      if (typeof interfaceEntityHandler.listCommands !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const result = await interpret(interfaceEntityHandler.listCommands({ interface: afterResult_register_conduit_api?.output?.["interface"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "commands_missing" -> error', async () => {
      if (typeof interfaceEntityHandler.listCommands !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.listCommands({ interface: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listTools', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.listTools({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.listTools({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.listTools({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.listTools({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.listTools({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.listTools !== 'function') return;
      const result = await interpret(interfaceEntityHandler.listTools({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "tools_valid" -> ok', async () => {
      if (typeof interfaceEntityHandler.listTools !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const result = await interpret(interfaceEntityHandler.listTools({ interface: afterResult_register_conduit_api?.output?.["interface"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "tools_missing" -> error', async () => {
      if (typeof interfaceEntityHandler.listTools !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.listTools({ interface: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listSkills', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.listSkills({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.listSkills({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.listSkills({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.listSkills({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.listSkills({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.listSkills !== 'function') return;
      const result = await interpret(interfaceEntityHandler.listSkills({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "skills_valid" -> ok', async () => {
      if (typeof interfaceEntityHandler.listSkills !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const result = await interpret(interfaceEntityHandler.listSkills({ interface: afterResult_register_conduit_api?.output?.["interface"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "skills_missing" -> error', async () => {
      if (typeof interfaceEntityHandler.listSkills !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.listSkills({ interface: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByConcept', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.findByConcept({ concept: "User" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.findByConcept({ concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.findByConcept({ concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.findByConcept({ concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.findByConcept({ concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.findByConcept !== 'function') return;
      const result = await interpret(interfaceEntityHandler.findByConcept({ concept: "User" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_user_concept" -> ok', async () => {
      if (typeof interfaceEntityHandler.findByConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_conduit_api?.output ?? {}));
      const _fixtureInput = { concept: "User" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(interfaceEntityHandler.findByConcept({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_empty_concept" -> error', async () => {
      if (typeof interfaceEntityHandler.findByConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.findByConcept({ concept: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByAction', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.findByAction({ concept: "User", action: "create" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.findByAction({ concept: "User", action: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.findByAction({ concept: "User", action: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.findByAction({ concept: "User", action: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.findByAction({ concept: "User", action: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.findByAction !== 'function') return;
      const result = await interpret(interfaceEntityHandler.findByAction({ concept: "User", action: "create" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_user_create" -> ok', async () => {
      if (typeof interfaceEntityHandler.findByAction !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_conduit_api?.output ?? {}));
      const _fixtureInput = { concept: "User", action: "create" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(interfaceEntityHandler.findByAction({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_empty_action" -> error', async () => {
      if (typeof interfaceEntityHandler.findByAction !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.findByAction({ concept: "User", action: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('traceEndpointToAction', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.traceEndpointToAction({ target: "rest", path: "/api/users", method: "POST" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.traceEndpointToAction({ target: "rest", path: "/api/users", method: "POST" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.traceEndpointToAction({ target: "rest", path: "/api/users", method: "POST" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.traceEndpointToAction({ target: "rest", path: "/api/users", method: "POST" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.traceEndpointToAction({ target: "rest", path: "/api/users", method: "POST" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.traceEndpointToAction !== 'function') return;
      const result = await interpret(interfaceEntityHandler.traceEndpointToAction({ target: "rest", path: "/api/users", method: "POST" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "trace_post_users" -> ok', async () => {
      if (typeof interfaceEntityHandler.traceEndpointToAction !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_conduit_api?.output ?? {}));
      const _fixtureInput = { target: "rest", path: "/api/users", method: "POST" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(interfaceEntityHandler.traceEndpointToAction({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "trace_missing_endpoint" -> error', async () => {
      if (typeof interfaceEntityHandler.traceEndpointToAction !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.traceEndpointToAction({ target: "rest", path: "/api/nonexistent", method: "GET" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "trace_rest" -> ok', async () => {
      if (typeof interfaceEntityHandler.traceEndpointToAction !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_conduit_api?.output ?? {}));
      const _fixtureInput = { target: "rest", path: "/api/users", method: "POST" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(interfaceEntityHandler.traceEndpointToAction({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "trace_missing" -> error', async () => {
      if (typeof interfaceEntityHandler.traceEndpointToAction !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.traceEndpointToAction({ target: "rest", path: "/nonexistent", method: "GET" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('traceToolToAction', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.traceToolToAction({ toolName: "user-create" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.traceToolToAction({ toolName: "user-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.traceToolToAction({ toolName: "user-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.traceToolToAction({ toolName: "user-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.traceToolToAction({ toolName: "user-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.traceToolToAction !== 'function') return;
      const result = await interpret(interfaceEntityHandler.traceToolToAction({ toolName: "user-create" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "trace_tool" -> ok', async () => {
      if (typeof interfaceEntityHandler.traceToolToAction !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_conduit_api?.output ?? {}));
      const _fixtureInput = { toolName: "user-create" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(interfaceEntityHandler.traceToolToAction({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "trace_missing_tool" -> error', async () => {
      if (typeof interfaceEntityHandler.traceToolToAction !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.traceToolToAction({ toolName: "nonexistent-tool" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "trace_tool_missing" -> error', async () => {
      if (typeof interfaceEntityHandler.traceToolToAction !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.traceToolToAction({ toolName: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('generatedSchemas', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.generatedSchemas({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.generatedSchemas({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.generatedSchemas({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.generatedSchemas({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.generatedSchemas({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.generatedSchemas !== 'function') return;
      const result = await interpret(interfaceEntityHandler.generatedSchemas({ interface: {"type":"ref","fixture":"register_conduit_api","field":"interface"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "schemas_valid" -> ok', async () => {
      if (typeof interfaceEntityHandler.generatedSchemas !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const result = await interpret(interfaceEntityHandler.generatedSchemas({ interface: afterResult_register_conduit_api?.output?.["interface"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "schemas_missing" -> error', async () => {
      if (typeof interfaceEntityHandler.generatedSchemas !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.generatedSchemas({ interface: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('validateAgainstSpecs', () => {
    it('builds a valid StorageProgram', () => {
      const program = interfaceEntityHandler.validateAgainstSpecs({ interface: "iface-uuid-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = interfaceEntityHandler.validateAgainstSpecs({ interface: "iface-uuid-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = interfaceEntityHandler.validateAgainstSpecs({ interface: "iface-uuid-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = interfaceEntityHandler.validateAgainstSpecs({ interface: "iface-uuid-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = interfaceEntityHandler.validateAgainstSpecs({ interface: "iface-uuid-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof interfaceEntityHandler.validateAgainstSpecs !== 'function') return;
      const result = await interpret(interfaceEntityHandler.validateAgainstSpecs({ interface: "iface-uuid-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "validate_valid" -> ok', async () => {
      if (typeof interfaceEntityHandler.validateAgainstSpecs !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_conduit_api = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{\"targets\":[\"rest\",\"mcp\"],\"generatedEndpoints\":[{\"method\":\"POST\",\"path\":\"/api/users\",\"concept\":\"User\",\"action\":\"create\",\"target\":\"rest\"}]}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_conduit_api?.output ?? {}));
      const _fixtureInput = { interface: "iface-uuid-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(interfaceEntityHandler.validateAgainstSpecs({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_missing" -> error', async () => {
      if (typeof interfaceEntityHandler.validateAgainstSpecs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(interfaceEntityHandler.validateAgainstSpecs({ interface: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(interfaceEntityHandler.register({ name: "conduit-api", source: "examples/conduit/app.interface.yaml", manifest: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let interface = registerResult0.output["interface"];
      let i = interface;
      const thenResult0 = await interpret(interfaceEntityHandler.get({ name: "conduit-api" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every interfaces entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), manifest: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listEndpoints'), input: fc.record({ interface: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listCommands'), input: fc.record({ interface: fc.string() }) }),
              fc.record({ action: fc.constant('listTools'), input: fc.record({ interface: fc.string() }) }),
              fc.record({ action: fc.constant('listSkills'), input: fc.record({ interface: fc.string() }) }),
              fc.record({ action: fc.constant('findByConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByAction'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traceEndpointToAction'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), path: fc.string({ minLength: 1, maxLength: 50 }), method: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traceToolToAction'), input: fc.record({ toolName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('generatedSchemas'), input: fc.record({ interface: fc.string() }) }),
              fc.record({ action: fc.constant('validateAgainstSpecs'), input: fc.record({ interface: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = interfaceEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(interfaceEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in interfaces', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), manifest: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listEndpoints'), input: fc.record({ interface: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listCommands'), input: fc.record({ interface: fc.string() }) }),
              fc.record({ action: fc.constant('listTools'), input: fc.record({ interface: fc.string() }) }),
              fc.record({ action: fc.constant('listSkills'), input: fc.record({ interface: fc.string() }) }),
              fc.record({ action: fc.constant('findByConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByAction'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traceEndpointToAction'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), path: fc.string({ minLength: 1, maxLength: 50 }), method: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('traceToolToAction'), input: fc.record({ toolName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('generatedSchemas'), input: fc.record({ interface: fc.string() }) }),
              fc.record({ action: fc.constant('validateAgainstSpecs'), input: fc.record({ interface: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = interfaceEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(interfaceEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty name in interfaces
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof interfaceEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(interfaceEntityHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof interfaceEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), source: fc.string({ minLength: 1, maxLength: 50 }), manifest: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = interfaceEntityHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
