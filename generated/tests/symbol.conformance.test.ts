// Symbol Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { symbolHandler } from '../../handlers/ts/symbol.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Symbol functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolHandler.register !== 'function') return;
      const result = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_register" -> ok', async () => {
      if (typeof symbolHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_concept" -> ok', async () => {
      if (typeof symbolHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(symbolHandler.register({ symbolString: "clef/concept/Order", kind: "concept", displayName: "Order", definingFile: "specs/order.concept" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate" -> ok', async () => {
      if (typeof symbolHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(symbolHandler.register({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolHandler.resolve({ symbolString: "clef/concept/Order" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolHandler.resolve({ symbolString: "clef/concept/Order" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolHandler.resolve({ symbolString: "clef/concept/Order" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolHandler.resolve({ symbolString: "clef/concept/Order" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolHandler.resolve({ symbolString: "clef/concept/Order" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolHandler.resolve !== 'function') return;
      const result = await interpret(symbolHandler.resolve({ symbolString: "clef/concept/Order" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_resolve" -> ok', async () => {
      if (typeof symbolHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { symbolString: "clef/concept/Order" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(symbolHandler.resolve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_nonexistent" -> error', async () => {
      if (typeof symbolHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(symbolHandler.resolve({ symbolString: "clef/concept/NonExistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByKind', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolHandler.findByKind({ kind: "function", namespace: "ts/function/src/handlers" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolHandler.findByKind({ kind: "function", namespace: "ts/function/src/handlers" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolHandler.findByKind({ kind: "function", namespace: "ts/function/src/handlers" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolHandler.findByKind({ kind: "function", namespace: "ts/function/src/handlers" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolHandler.findByKind({ kind: "function", namespace: "ts/function/src/handlers" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolHandler.findByKind !== 'function') return;
      const result = await interpret(symbolHandler.findByKind({ kind: "function", namespace: "ts/function/src/handlers" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_functions" -> ok', async () => {
      if (typeof symbolHandler.findByKind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { kind: "function", namespace: "ts/function/src/handlers" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(symbolHandler.findByKind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_all_concepts" -> ok', async () => {
      if (typeof symbolHandler.findByKind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { kind: "concept", namespace: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(symbolHandler.findByKind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('findByFile', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolHandler.findByFile({ file: "src/handlers/user.ts" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolHandler.findByFile({ file: "src/handlers/user.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolHandler.findByFile({ file: "src/handlers/user.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolHandler.findByFile({ file: "src/handlers/user.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolHandler.findByFile({ file: "src/handlers/user.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolHandler.findByFile !== 'function') return;
      const result = await interpret(symbolHandler.findByFile({ file: "src/handlers/user.ts" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_in_file" -> ok', async () => {
      if (typeof symbolHandler.findByFile !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { file: "src/handlers/user.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(symbolHandler.findByFile({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_in_empty_file" -> error', async () => {
      if (typeof symbolHandler.findByFile !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { file: "src/empty.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(symbolHandler.findByFile({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('rename', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolHandler.rename({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"}, newName: "updateUser" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolHandler.rename({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"}, newName: "updateUser" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolHandler.rename({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"}, newName: "updateUser" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolHandler.rename({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"}, newName: "updateUser" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolHandler.rename({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"}, newName: "updateUser" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolHandler.rename !== 'function') return;
      const result = await interpret(symbolHandler.rename({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"}, newName: "updateUser" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_rename" -> ok', async () => {
      if (typeof symbolHandler.rename !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      const result = await interpret(symbolHandler.rename({ symbol: afterResult_valid_register?.output?.["symbol"], newName: "updateUser" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "rename_nonexistent" -> error', async () => {
      if (typeof symbolHandler.rename !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(symbolHandler.rename({ symbol: "symbol-999", newName: "foo" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = symbolHandler.get({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = symbolHandler.get({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = symbolHandler.get({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = symbolHandler.get({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = symbolHandler.get({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof symbolHandler.get !== 'function') return;
      const result = await interpret(symbolHandler.get({ symbol: {"type":"ref","fixture":"valid_register","field":"symbol"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_get" -> ok', async () => {
      if (typeof symbolHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(symbolHandler.register({ symbolString: "ts/function/src/handlers/user.ts/createUser", kind: "function", displayName: "createUser", definingFile: "src/handlers/user.ts" }), storage);
      const result = await interpret(symbolHandler.get({ symbol: afterResult_valid_register?.output?.["symbol"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_nonexistent" -> error', async () => {
      if (typeof symbolHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(symbolHandler.get({ symbol: "symbol-999" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register-then-get", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(symbolHandler.register({ symbolString: "clef/concept/Article", kind: "concept", displayName: "Article", definingFile: "specs/article.concept" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let symbol = registerResult0.output["symbol"];
      let s = symbol;
      const thenResult0 = await interpret(symbolHandler.get({ symbol: s }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register-then-resolve", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(symbolHandler.register({ symbolString: "clef/concept/Article", kind: "concept", displayName: "Article", definingFile: "specs/article.concept" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let symbol = registerResult0.output["symbol"];
      let s = symbol;
      const thenResult0 = await interpret(symbolHandler.resolve({ symbolString: "clef/concept/Article" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register-then-register", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(symbolHandler.register({ symbolString: "clef/concept/Article", kind: "concept", displayName: "Article", definingFile: "specs/article.concept" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let symbol = registerResult0.output["symbol"];
      let s = symbol;
      const thenResult0 = await interpret(symbolHandler.register({ symbolString: "clef/concept/Article", kind: "concept", displayName: "Article", definingFile: "specs/article.concept" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-symbolString', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ symbolString: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), displayName: fc.string({ minLength: 1, maxLength: 50 }), definingFile: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ symbolString: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByKind'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }), namespace: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByFile'), input: fc.record({ file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rename'), input: fc.record({ symbol: fc.string(), newName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ symbol: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = symbolHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(symbolHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-kind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ symbolString: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), displayName: fc.string({ minLength: 1, maxLength: 50 }), definingFile: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ symbolString: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByKind'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }), namespace: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByFile'), input: fc.record({ file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rename'), input: fc.record({ symbol: fc.string(), newName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ symbol: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = symbolHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(symbolHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-kind
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof symbolHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(symbolHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof symbolHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ symbolString: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), displayName: fc.string({ minLength: 1, maxLength: 50 }), definingFile: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = symbolHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
