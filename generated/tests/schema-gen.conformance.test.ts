// SchemaGen Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { schemaGenHandler } from '../../handlers/ts/framework/schema-gen.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SchemaGen functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('generate', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaGenHandler.generate({ spec: "s1", ast: {"name":"Invoice","typeParams":["T"],"purpose":"Manage invoices.","state":[],"actions":[{"name":"create","params":[],"variants":[{"name":"ok","params":[],"description":"Created."}]}],"invariants":[],"capabilities":[]} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaGenHandler.generate({ spec: "s1", ast: {"name":"Invoice","typeParams":["T"],"purpose":"Manage invoices.","state":[],"actions":[{"name":"create","params":[],"variants":[{"name":"ok","params":[],"description":"Created."}]}],"invariants":[],"capabilities":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaGenHandler.generate({ spec: "s1", ast: {"name":"Invoice","typeParams":["T"],"purpose":"Manage invoices.","state":[],"actions":[{"name":"create","params":[],"variants":[{"name":"ok","params":[],"description":"Created."}]}],"invariants":[],"capabilities":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaGenHandler.generate({ spec: "s1", ast: {"name":"Invoice","typeParams":["T"],"purpose":"Manage invoices.","state":[],"actions":[{"name":"create","params":[],"variants":[{"name":"ok","params":[],"description":"Created."}]}],"invariants":[],"capabilities":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaGenHandler.generate({ spec: "s1", ast: {"name":"Invoice","typeParams":["T"],"purpose":"Manage invoices.","state":[],"actions":[{"name":"create","params":[],"variants":[{"name":"ok","params":[],"description":"Created."}]}],"invariants":[],"capabilities":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaGenHandler.generate !== 'function') return;
      const result = await interpret(schemaGenHandler.generate({ spec: "s1", ast: {"name":"Invoice","typeParams":["T"],"purpose":"Manage invoices.","state":[],"actions":[{"name":"create","params":[],"variants":[{"name":"ok","params":[],"description":"Created."}]}],"invariants":[],"capabilities":[]} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_ast" -> ok', async () => {
      if (typeof schemaGenHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaGenHandler.generate({ spec: "s1", ast: {"name":"Invoice","typeParams":["T"],"purpose":"Manage invoices.","state":[],"actions":[{"name":"create","params":[],"variants":[{"name":"ok","params":[],"description":"Created."}]}],"invariants":[],"capabilities":[]} }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_name" -> error', async () => {
      if (typeof schemaGenHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaGenHandler.generate({ spec: "s2", ast: {"name":""} }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaGenHandler.register({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaGenHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaGenHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaGenHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaGenHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaGenHandler.register !== 'function') return;
      const result = await interpret(schemaGenHandler.register({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof schemaGenHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_ast = await interpret(schemaGenHandler.generate({ spec: "s1", ast: {"name":"Invoice","typeParams":["T"],"purpose":"Manage invoices.","state":[],"actions":[{"name":"create","params":[],"variants":[{"name":"ok","params":[],"description":"Created."}]}],"invariants":[],"capabilities":[]} }), storage);
      const _pool = Object.assign({}, (afterResult_valid_ast?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(schemaGenHandler.register({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof schemaGenHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = schemaGenHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('SchemaGen');
    });
  });

  describe('invariant examples', () => {
    it("generate valid AST then invalid fails", async () => {
      const storage = createInMemoryStorage();
      const generateResult0 = await interpret(schemaGenHandler.generate({ spec: "s1", ast: {"type":"record","fields":[{"name":"name","value":{"type":"literal","value":"Ping"}},{"name":"typeParams","value":{"type":"list","items":[{"type":"literal","value":"T"}]}},{"name":"purpose","value":{"type":"literal","value":"A test."}},{"name":"state","value":{"type":"list","items":[]}},{"name":"actions","value":{"type":"list","items":[{"type":"record","fields":[{"name":"name","value":{"type":"literal","value":"ping"}},{"name":"params","value":{"type":"list","items":[]}},{"name":"variants","value":{"type":"list","items":[{"type":"record","fields":[{"name":"name","value":{"type":"literal","value":"ok"}},{"name":"params","value":{"type":"list","items":[]}},{"name":"description","value":{"type":"literal","value":"Pong."}}]}]}}]}]}},{"name":"invariants","value":{"type":"list","items":[]}},{"name":"capabilities","value":{"type":"list","items":[]}}]} }), storage);
      expect(generateResult0.variant).toBe("ok");
      let manifest = generateResult0.output["manifest"];
      let m = manifest;
      const thenResult0 = await interpret(schemaGenHandler.generate({ spec: "s2", ast: {"type":"record","fields":[{"name":"name","value":{"type":"literal","value":""}}]} }), storage);
      expect(thenResult0.variant).toBe("error");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: manifests are fully resolved', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('generate'), input: fc.record({ spec: fc.string(), ast: fc.string() }) }),
              fc.record({ action: fc.constant('register'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = schemaGenHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(schemaGenHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('generate handles empty input: ', async () => {
      if (typeof schemaGenHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(schemaGenHandler.generate({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('generate ensures on ok: ', async () => {
      if (typeof schemaGenHandler.generate !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ spec: fc.string(), ast: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = schemaGenHandler.generate(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
