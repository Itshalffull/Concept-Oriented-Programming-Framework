// AgentTrigger Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { agentTriggerHandler } from '../../handlers/ts/app/agent-trigger.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('AgentTrigger functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentTriggerHandler.create !== 'function') return;
      const result = await interpret(agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_schedule_trigger" -> ok', async () => {
      if (typeof agentTriggerHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_content_trigger" -> ok', async () => {
      if (typeof agentTriggerHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentTriggerHandler.create({ personaPageId: "persona-reviewer", triggerType: "content_event", triggerConfig: "{\"content_type\":\"pull-request\",\"action\":\"create\"}", strategy: "react", tools: "[\"code_review\"]", cooldownMs: "30000" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_invalid" -> error', async () => {
      if (typeof agentTriggerHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentTriggerHandler.create({ personaPageId: "", triggerType: "invalid", triggerConfig: "{}", strategy: "react", tools: "[]", cooldownMs: "0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('fire', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentTriggerHandler.fire({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, eventPayload: "{\"time\":\"2024-01-15T09:00:00Z\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentTriggerHandler.fire({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, eventPayload: "{\"time\":\"2024-01-15T09:00:00Z\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentTriggerHandler.fire({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, eventPayload: "{\"time\":\"2024-01-15T09:00:00Z\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentTriggerHandler.fire({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, eventPayload: "{\"time\":\"2024-01-15T09:00:00Z\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentTriggerHandler.fire({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, eventPayload: "{\"time\":\"2024-01-15T09:00:00Z\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentTriggerHandler.fire !== 'function') return;
      const result = await interpret(agentTriggerHandler.fire({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, eventPayload: "{\"time\":\"2024-01-15T09:00:00Z\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "fire_trigger" -> ok', async () => {
      if (typeof agentTriggerHandler.fire !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_schedule_trigger = await interpret(agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" }), storage);
      const result = await interpret(agentTriggerHandler.fire({ trigger: afterResult_create_schedule_trigger?.output?.["trigger"], eventPayload: "{\"time\":\"2024-01-15T09:00:00Z\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "fire_missing" -> notfound', async () => {
      if (typeof agentTriggerHandler.fire !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentTriggerHandler.fire({ trigger: "nonexistent", eventPayload: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('enable', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentTriggerHandler.enable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentTriggerHandler.enable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentTriggerHandler.enable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentTriggerHandler.enable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentTriggerHandler.enable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentTriggerHandler.enable !== 'function') return;
      const result = await interpret(agentTriggerHandler.enable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "enable_trigger" -> ok', async () => {
      if (typeof agentTriggerHandler.enable !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_schedule_trigger = await interpret(agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" }), storage);
      const result = await interpret(agentTriggerHandler.enable({ trigger: afterResult_create_schedule_trigger?.output?.["trigger"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "enable_missing" -> notfound', async () => {
      if (typeof agentTriggerHandler.enable !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentTriggerHandler.enable({ trigger: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('disable', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentTriggerHandler.disable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentTriggerHandler.disable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentTriggerHandler.disable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentTriggerHandler.disable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentTriggerHandler.disable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentTriggerHandler.disable !== 'function') return;
      const result = await interpret(agentTriggerHandler.disable({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "disable_trigger" -> ok', async () => {
      if (typeof agentTriggerHandler.disable !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_schedule_trigger = await interpret(agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" }), storage);
      const result = await interpret(agentTriggerHandler.disable({ trigger: afterResult_create_schedule_trigger?.output?.["trigger"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "disable_missing" -> notfound', async () => {
      if (typeof agentTriggerHandler.disable !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentTriggerHandler.disable({ trigger: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentTriggerHandler.list({ personaPageId: "", triggerType: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentTriggerHandler.list({ personaPageId: "", triggerType: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentTriggerHandler.list({ personaPageId: "", triggerType: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentTriggerHandler.list({ personaPageId: "", triggerType: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentTriggerHandler.list({ personaPageId: "", triggerType: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentTriggerHandler.list !== 'function') return;
      const result = await interpret(agentTriggerHandler.list({ personaPageId: "", triggerType: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof agentTriggerHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_schedule_trigger = await interpret(agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" }), storage);
      const _pool = Object.assign({}, (afterResult_create_schedule_trigger?.output ?? {}));
      const _fixtureInput = { personaPageId: "", triggerType: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(agentTriggerHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_persona" -> ok', async () => {
      if (typeof agentTriggerHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_schedule_trigger = await interpret(agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" }), storage);
      const _pool = Object.assign({}, (afterResult_create_schedule_trigger?.output ?? {}));
      const _fixtureInput = { personaPageId: "persona-research-bot", triggerType: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(agentTriggerHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('getHistory', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentTriggerHandler.getHistory({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, limit: "10" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentTriggerHandler.getHistory({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, limit: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentTriggerHandler.getHistory({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, limit: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentTriggerHandler.getHistory({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, limit: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentTriggerHandler.getHistory({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, limit: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentTriggerHandler.getHistory !== 'function') return;
      const result = await interpret(agentTriggerHandler.getHistory({ trigger: {"type":"ref","fixture":"create_schedule_trigger","field":"trigger"}, limit: "10" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_history" -> ok', async () => {
      if (typeof agentTriggerHandler.getHistory !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_schedule_trigger = await interpret(agentTriggerHandler.create({ personaPageId: "persona-research-bot", triggerType: "schedule", triggerConfig: "{\"cron\":\"0 9 * * 1\"}", strategy: "plan_and_execute", tools: "[\"web_search\"]", cooldownMs: "60000" }), storage);
      const result = await interpret(agentTriggerHandler.getHistory({ trigger: afterResult_create_schedule_trigger?.output?.["trigger"], limit: "10" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_history_missing" -> notfound', async () => {
      if (typeof agentTriggerHandler.getHistory !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentTriggerHandler.getHistory({ trigger: "nonexistent", limit: "10" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof agentTriggerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = agentTriggerHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('AgentTrigger');
    });
  });

  describe('invariant examples', () => {
    it("create-then-fire", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(agentTriggerHandler.create({ personaPageId: "p1", triggerType: "manual", triggerConfig: "{}", strategy: "react", tools: "[]", cooldownMs: 0 }), storage);
      expect(createResult0.variant).toBe("ok");
      let trigger = createResult0.output["trigger"];
      let t = trigger;
      const thenResult0 = await interpret(agentTriggerHandler.fire({ trigger: t, eventPayload: "{}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("disable-prevents-fire", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(agentTriggerHandler.create({ personaPageId: "p1", triggerType: "manual", triggerConfig: "{}", strategy: "react", tools: "[]", cooldownMs: 0 }), storage);
      expect(createResult0.variant).toBe("ok");
      let trigger = createResult0.output["trigger"];
      let t = trigger;
      const thenResult0 = await interpret(agentTriggerHandler.disable({ trigger: t }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(agentTriggerHandler.fire({ trigger: t, eventPayload: "{}" }), storage);
      expect(thenResult1.variant).toBe("disabled");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-triggerType', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }), triggerType: fc.string({ minLength: 1, maxLength: 50 }), triggerConfig: fc.string({ minLength: 1, maxLength: 50 }), strategy: fc.string({ minLength: 1, maxLength: 50 }), tools: fc.string({ minLength: 1, maxLength: 50 }), cooldownMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('fire'), input: fc.record({ trigger: fc.string(), eventPayload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('enable'), input: fc.record({ trigger: fc.string() }) }),
              fc.record({ action: fc.constant('disable'), input: fc.record({ trigger: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ personaPageId: fc.string(), triggerType: fc.string() }) }),
              fc.record({ action: fc.constant('getHistory'), input: fc.record({ trigger: fc.string(), limit: fc.integer({ min: 1, max: 1000 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = agentTriggerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(agentTriggerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-personaPageId', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }), triggerType: fc.string({ minLength: 1, maxLength: 50 }), triggerConfig: fc.string({ minLength: 1, maxLength: 50 }), strategy: fc.string({ minLength: 1, maxLength: 50 }), tools: fc.string({ minLength: 1, maxLength: 50 }), cooldownMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('fire'), input: fc.record({ trigger: fc.string(), eventPayload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('enable'), input: fc.record({ trigger: fc.string() }) }),
              fc.record({ action: fc.constant('disable'), input: fc.record({ trigger: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ personaPageId: fc.string(), triggerType: fc.string() }) }),
              fc.record({ action: fc.constant('getHistory'), input: fc.record({ trigger: fc.string(), limit: fc.integer({ min: 1, max: 1000 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = agentTriggerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(agentTriggerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-personaPageId
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof agentTriggerHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentTriggerHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof agentTriggerHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ personaPageId: fc.string({ minLength: 1, maxLength: 50 }), triggerType: fc.string({ minLength: 1, maxLength: 50 }), triggerConfig: fc.string({ minLength: 1, maxLength: 50 }), strategy: fc.string({ minLength: 1, maxLength: 50 }), tools: fc.string({ minLength: 1, maxLength: 50 }), cooldownMs: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = agentTriggerHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
