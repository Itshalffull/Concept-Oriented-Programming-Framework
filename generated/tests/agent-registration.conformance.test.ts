// AgentRegistration Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { agentRegistrationHandler } from '../../handlers/ts/app/agent-registration.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('AgentRegistration functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRegistrationHandler.register !== 'function') return;
      const result = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_research_agent" -> ok', async () => {
      if (typeof agentRegistrationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_minimal_agent" -> ok', async () => {
      if (typeof agentRegistrationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.register({ agent: "agent-minimal-001", displayName: "Minimal Agent", ownerSubject: "subject-admin-001", agentClass: "generic", personaRef: null, metadata: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate_agent" -> duplicate', async () => {
      if (typeof agentRegistrationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const result = await interpret(agentRegistrationHandler.register({ agent: afterResult_register_research_agent?.output?.["agent"], displayName: "Research Assistant Clone", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: null, metadata: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "register_empty_name" -> invalid', async () => {
      if (typeof agentRegistrationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.register({ agent: "agent-bad-001", displayName: "", ownerSubject: "subject-admin-001", agentClass: "generic", personaRef: null, metadata: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('setPersona', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRegistrationHandler.setPersona({ agent: {"type":"ref","fixture":"register_minimal_agent","field":"agent"}, personaRef: "persona-note-writer" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRegistrationHandler.setPersona({ agent: {"type":"ref","fixture":"register_minimal_agent","field":"agent"}, personaRef: "persona-note-writer" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRegistrationHandler.setPersona({ agent: {"type":"ref","fixture":"register_minimal_agent","field":"agent"}, personaRef: "persona-note-writer" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRegistrationHandler.setPersona({ agent: {"type":"ref","fixture":"register_minimal_agent","field":"agent"}, personaRef: "persona-note-writer" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRegistrationHandler.setPersona({ agent: {"type":"ref","fixture":"register_minimal_agent","field":"agent"}, personaRef: "persona-note-writer" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRegistrationHandler.setPersona !== 'function') return;
      const result = await interpret(agentRegistrationHandler.setPersona({ agent: {"type":"ref","fixture":"register_minimal_agent","field":"agent"}, personaRef: "persona-note-writer" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "set_persona" -> ok', async () => {
      if (typeof agentRegistrationHandler.setPersona !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_minimal_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-minimal-001", displayName: "Minimal Agent", ownerSubject: "subject-admin-001", agentClass: "generic", personaRef: null, metadata: "{}" }), storage);
      const result = await interpret(agentRegistrationHandler.setPersona({ agent: afterResult_register_minimal_agent?.output?.["agent"], personaRef: "persona-note-writer" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "clear_persona" -> ok', async () => {
      if (typeof agentRegistrationHandler.setPersona !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const result = await interpret(agentRegistrationHandler.setPersona({ agent: afterResult_register_research_agent?.output?.["agent"], personaRef: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_persona_missing" -> notfound', async () => {
      if (typeof agentRegistrationHandler.setPersona !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.setPersona({ agent: "agent-missing", personaRef: "persona-note-writer" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "set_persona_invalid" -> invalid', async () => {
      if (typeof agentRegistrationHandler.setPersona !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_minimal_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-minimal-001", displayName: "Minimal Agent", ownerSubject: "subject-admin-001", agentClass: "generic", personaRef: null, metadata: "{}" }), storage);
      const result = await interpret(agentRegistrationHandler.setPersona({ agent: afterResult_register_minimal_agent?.output?.["agent"], personaRef: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('disable', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRegistrationHandler.disable({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRegistrationHandler.disable({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRegistrationHandler.disable({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRegistrationHandler.disable({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRegistrationHandler.disable({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRegistrationHandler.disable !== 'function') return;
      const result = await interpret(agentRegistrationHandler.disable({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "disable_registered" -> ok', async () => {
      if (typeof agentRegistrationHandler.disable !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const result = await interpret(agentRegistrationHandler.disable({ agent: afterResult_register_research_agent?.output?.["agent"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "disable_missing" -> notfound', async () => {
      if (typeof agentRegistrationHandler.disable !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.disable({ agent: "agent-missing" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('revoke', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRegistrationHandler.revoke({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRegistrationHandler.revoke({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRegistrationHandler.revoke({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRegistrationHandler.revoke({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRegistrationHandler.revoke({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRegistrationHandler.revoke !== 'function') return;
      const result = await interpret(agentRegistrationHandler.revoke({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "revoke_registered" -> ok', async () => {
      if (typeof agentRegistrationHandler.revoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const result = await interpret(agentRegistrationHandler.revoke({ agent: afterResult_register_research_agent?.output?.["agent"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "revoke_missing" -> notfound', async () => {
      if (typeof agentRegistrationHandler.revoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.revoke({ agent: "agent-missing" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRegistrationHandler.get({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRegistrationHandler.get({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRegistrationHandler.get({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRegistrationHandler.get({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRegistrationHandler.get({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRegistrationHandler.get !== 'function') return;
      const result = await interpret(agentRegistrationHandler.get({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_registered" -> ok', async () => {
      if (typeof agentRegistrationHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const result = await interpret(agentRegistrationHandler.get({ agent: afterResult_register_research_agent?.output?.["agent"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof agentRegistrationHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.get({ agent: "agent-missing" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('listByOwner', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRegistrationHandler.listByOwner({ ownerSubject: "subject-admin-001" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRegistrationHandler.listByOwner({ ownerSubject: "subject-admin-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRegistrationHandler.listByOwner({ ownerSubject: "subject-admin-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRegistrationHandler.listByOwner({ ownerSubject: "subject-admin-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRegistrationHandler.listByOwner({ ownerSubject: "subject-admin-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRegistrationHandler.listByOwner !== 'function') return;
      const result = await interpret(agentRegistrationHandler.listByOwner({ ownerSubject: "subject-admin-001" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_owned_agents" -> ok', async () => {
      if (typeof agentRegistrationHandler.listByOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_research_agent?.output ?? {}));
      const _fixtureInput = { ownerSubject: "subject-admin-001" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(agentRegistrationHandler.listByOwner({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_unowned" -> notfound', async () => {
      if (typeof agentRegistrationHandler.listByOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.listByOwner({ ownerSubject: "subject-nobody" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "list_empty_owner" -> invalid', async () => {
      if (typeof agentRegistrationHandler.listByOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.listByOwner({ ownerSubject: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRegistrationHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRegistrationHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRegistrationHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRegistrationHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRegistrationHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRegistrationHandler.list !== 'function') return;
      const result = await interpret(agentRegistrationHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof agentRegistrationHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_research_agent?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(agentRegistrationHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_all_empty" -> ok', async () => {
      if (typeof agentRegistrationHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_research_agent?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(agentRegistrationHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('setOwner', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRegistrationHandler.setOwner({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"}, owner: "subject-admin-002" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRegistrationHandler.setOwner({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"}, owner: "subject-admin-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRegistrationHandler.setOwner({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"}, owner: "subject-admin-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRegistrationHandler.setOwner({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"}, owner: "subject-admin-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRegistrationHandler.setOwner({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"}, owner: "subject-admin-002" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRegistrationHandler.setOwner !== 'function') return;
      const result = await interpret(agentRegistrationHandler.setOwner({ agent: {"type":"ref","fixture":"register_research_agent","field":"agent"}, owner: "subject-admin-002" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "transfer_ownership" -> ok', async () => {
      if (typeof agentRegistrationHandler.setOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const result = await interpret(agentRegistrationHandler.setOwner({ agent: afterResult_register_research_agent?.output?.["agent"], owner: "subject-admin-002" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "set_owner_missing" -> notfound', async () => {
      if (typeof agentRegistrationHandler.setOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(agentRegistrationHandler.setOwner({ agent: "agent-missing", owner: "subject-admin-002" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "set_owner_invalid" -> invalid', async () => {
      if (typeof agentRegistrationHandler.setOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_research_agent = await interpret(agentRegistrationHandler.register({ agent: "agent-research-001", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{\"tier\":\"standard\"}" }), storage);
      const result = await interpret(agentRegistrationHandler.setOwner({ agent: afterResult_register_research_agent?.output?.["agent"], owner: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });


  describe('invariant examples', () => {
    it("register-then-get", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(agentRegistrationHandler.register({ agent: "test-a", displayName: "Research Assistant", ownerSubject: "subject-admin-001", agentClass: "knowledge-worker", personaRef: "persona-research-assistant", metadata: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let agent = registerResult0.output["agent"];
      let a = agent;
      const thenResult0 = await interpret(agentRegistrationHandler.get({ agent: a }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register-then-disable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(agentRegistrationHandler.register({ agent: "test-a", displayName: "Ops Agent", ownerSubject: "subject-admin-001", agentClass: "operations", personaRef: false, metadata: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let agent = registerResult0.output["agent"];
      let a = agent;
      const disableResult1 = await interpret(agentRegistrationHandler.disable({ agent: a }), storage);
      expect(disableResult1.variant).toBe("ok");
      agent = disableResult1.output["agent"];
      const thenResult0 = await interpret(agentRegistrationHandler.get({ agent: a }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register-then-revoke", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(agentRegistrationHandler.register({ agent: "test-a", displayName: "Ops Agent", ownerSubject: "subject-admin-001", agentClass: "operations", personaRef: false, metadata: "{}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let agent = registerResult0.output["agent"];
      let a = agent;
      const revokeResult1 = await interpret(agentRegistrationHandler.revoke({ agent: a }), storage);
      expect(revokeResult1.variant).toBe("ok");
      agent = revokeResult1.output["agent"];
      const thenResult0 = await interpret(agentRegistrationHandler.get({ agent: a }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: registered-agents-have-owner', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ agent: fc.string(), displayName: fc.string({ minLength: 1, maxLength: 50 }), ownerSubject: fc.string({ minLength: 1, maxLength: 50 }), agentClass: fc.string({ minLength: 1, maxLength: 50 }), personaRef: fc.string(), metadata: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setPersona'), input: fc.record({ agent: fc.string(), personaRef: fc.string() }) }),
              fc.record({ action: fc.constant('disable'), input: fc.record({ agent: fc.string() }) }),
              fc.record({ action: fc.constant('revoke'), input: fc.record({ agent: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ agent: fc.string() }) }),
              fc.record({ action: fc.constant('listByOwner'), input: fc.record({ ownerSubject: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('setOwner'), input: fc.record({ agent: fc.string(), owner: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = agentRegistrationHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(agentRegistrationHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: registered-agents-have-empty-status', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ agent: fc.string(), displayName: fc.string({ minLength: 1, maxLength: 50 }), ownerSubject: fc.string({ minLength: 1, maxLength: 50 }), agentClass: fc.string({ minLength: 1, maxLength: 50 }), personaRef: fc.string(), metadata: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setPersona'), input: fc.record({ agent: fc.string(), personaRef: fc.string() }) }),
              fc.record({ action: fc.constant('disable'), input: fc.record({ agent: fc.string() }) }),
              fc.record({ action: fc.constant('revoke'), input: fc.record({ agent: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ agent: fc.string() }) }),
              fc.record({ action: fc.constant('listByOwner'), input: fc.record({ ownerSubject: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('setOwner'), input: fc.record({ agent: fc.string(), owner: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = agentRegistrationHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(agentRegistrationHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: registered-agents-have-empty-status
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof agentRegistrationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentRegistrationHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register handles empty input: ', async () => {
      if (typeof agentRegistrationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentRegistrationHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register handles empty input: ', async () => {
      if (typeof agentRegistrationHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentRegistrationHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof agentRegistrationHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ agent: fc.string(), displayName: fc.string({ minLength: 1, maxLength: 50 }), ownerSubject: fc.string({ minLength: 1, maxLength: 50 }), agentClass: fc.string({ minLength: 1, maxLength: 50 }), personaRef: fc.string(), metadata: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = agentRegistrationHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('setPersona handles empty input: ', async () => {
      if (typeof agentRegistrationHandler.setPersona !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentRegistrationHandler.setPersona({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('disable handles empty input: ', async () => {
      if (typeof agentRegistrationHandler.disable !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentRegistrationHandler.disable({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('disable ensures on ok: ', async () => {
      if (typeof agentRegistrationHandler.disable !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ agent: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = agentRegistrationHandler.disable(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('revoke handles empty input: ', async () => {
      if (typeof agentRegistrationHandler.revoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentRegistrationHandler.revoke({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('revoke ensures on ok: ', async () => {
      if (typeof agentRegistrationHandler.revoke !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ agent: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = agentRegistrationHandler.revoke(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('setOwner handles empty input: ', async () => {
      if (typeof agentRegistrationHandler.setOwner !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentRegistrationHandler.setOwner({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('setOwner ensures on ok: ', async () => {
      if (typeof agentRegistrationHandler.setOwner !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ agent: fc.string(), owner: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = agentRegistrationHandler.setOwner(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
