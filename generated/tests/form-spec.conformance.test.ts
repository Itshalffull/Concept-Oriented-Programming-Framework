// FormSpec Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { formSpecHandler } from '../../handlers/ts/view/form-spec.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('FormSpec functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      const result = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_ok" -> ok', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_edit" -> ok', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.create({ form: "article-full-edit", schema: "article", name: "full-edit", mode: "edit" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_both" -> ok', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.create({ form: "task-form", schema: "task", name: "task-form", mode: "both" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_duplicate" -> duplicate', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create-dup", schema: "article", name: "quick-create", mode: "create" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "create_empty_schema" -> error', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.create({ form: "bad-form", schema: "", name: "my-form", mode: "create" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "create_empty_name" -> error', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.create({ form: "bad-form2", schema: "article", name: "", mode: "create" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "create_invalid_mode" -> error', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.create({ form: "bad-form3", schema: "article", name: "some-form", mode: "view" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('update', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.update({ form: "article-quick-create", layout: {"groups":[{"id":"basic","label":"Basic Info","fieldIds":["title","body"]}]}, steps: null, conditions: null, defaults: {"status":"draft"}, overrides: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.update({ form: "article-quick-create", layout: {"groups":[{"id":"basic","label":"Basic Info","fieldIds":["title","body"]}]}, steps: null, conditions: null, defaults: {"status":"draft"}, overrides: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.update({ form: "article-quick-create", layout: {"groups":[{"id":"basic","label":"Basic Info","fieldIds":["title","body"]}]}, steps: null, conditions: null, defaults: {"status":"draft"}, overrides: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.update({ form: "article-quick-create", layout: {"groups":[{"id":"basic","label":"Basic Info","fieldIds":["title","body"]}]}, steps: null, conditions: null, defaults: {"status":"draft"}, overrides: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.update({ form: "article-quick-create", layout: {"groups":[{"id":"basic","label":"Basic Info","fieldIds":["title","body"]}]}, steps: null, conditions: null, defaults: {"status":"draft"}, overrides: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.update !== 'function') return;
      const result = await interpret(formSpecHandler.update({ form: "article-quick-create", layout: {"groups":[{"id":"basic","label":"Basic Info","fieldIds":["title","body"]}]}, steps: null, conditions: null, defaults: {"status":"draft"}, overrides: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "update_ok" -> ok', async () => {
      if (typeof formSpecHandler.update !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", layout: {"groups":[{"id":"basic","label":"Basic Info","fieldIds":["title","body"]}]}, steps: null, conditions: null, defaults: {"status":"draft"}, overrides: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.update({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "update_missing" -> notfound', async () => {
      if (typeof formSpecHandler.update !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.update({ form: "nonexistent-form", layout: null, steps: null, conditions: null, defaults: null, overrides: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('addField', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.addField({ form: "article-quick-create", fieldId: "tags", group: "basic", position: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.addField({ form: "article-quick-create", fieldId: "tags", group: "basic", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.addField({ form: "article-quick-create", fieldId: "tags", group: "basic", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.addField({ form: "article-quick-create", fieldId: "tags", group: "basic", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.addField({ form: "article-quick-create", fieldId: "tags", group: "basic", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.addField !== 'function') return;
      const result = await interpret(formSpecHandler.addField({ form: "article-quick-create", fieldId: "tags", group: "basic", position: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "add_field_ok" -> ok', async () => {
      if (typeof formSpecHandler.addField !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", fieldId: "tags", group: "basic", position: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.addField({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_field_positioned" -> ok', async () => {
      if (typeof formSpecHandler.addField !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", fieldId: "author", group: null, position: "0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.addField({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_field_missing" -> notfound', async () => {
      if (typeof formSpecHandler.addField !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.addField({ form: "nonexistent-form", fieldId: "title", group: null, position: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('removeField', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.removeField({ form: "article-quick-create", fieldId: "tags" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.removeField({ form: "article-quick-create", fieldId: "tags" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.removeField({ form: "article-quick-create", fieldId: "tags" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.removeField({ form: "article-quick-create", fieldId: "tags" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.removeField({ form: "article-quick-create", fieldId: "tags" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.removeField !== 'function') return;
      const result = await interpret(formSpecHandler.removeField({ form: "article-quick-create", fieldId: "tags" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_field_ok" -> ok', async () => {
      if (typeof formSpecHandler.removeField !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const afterResult_add_field_ok = await interpret(formSpecHandler.addField({ form: "article-quick-create", fieldId: "tags", group: "basic", position: null }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}), (afterResult_add_field_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", fieldId: "tags" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.removeField({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_field_missing" -> notfound', async () => {
      if (typeof formSpecHandler.removeField !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.removeField({ form: "nonexistent-form", fieldId: "title" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('reorderField', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.reorderField({ form: "article-quick-create", fieldId: "body", position: "0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.reorderField({ form: "article-quick-create", fieldId: "body", position: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.reorderField({ form: "article-quick-create", fieldId: "body", position: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.reorderField({ form: "article-quick-create", fieldId: "body", position: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.reorderField({ form: "article-quick-create", fieldId: "body", position: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.reorderField !== 'function') return;
      const result = await interpret(formSpecHandler.reorderField({ form: "article-quick-create", fieldId: "body", position: "0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "reorder_ok" -> ok', async () => {
      if (typeof formSpecHandler.reorderField !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", fieldId: "body", position: "0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.reorderField({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reorder_missing" -> notfound', async () => {
      if (typeof formSpecHandler.reorderField !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.reorderField({ form: "nonexistent-form", fieldId: "title", position: "0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('addCondition', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.addCondition({ form: "article-quick-create", fieldId: "publishedAt", condition: {"when":{"field":"status","eq":"published"},"show":"true"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.addCondition({ form: "article-quick-create", fieldId: "publishedAt", condition: {"when":{"field":"status","eq":"published"},"show":"true"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.addCondition({ form: "article-quick-create", fieldId: "publishedAt", condition: {"when":{"field":"status","eq":"published"},"show":"true"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.addCondition({ form: "article-quick-create", fieldId: "publishedAt", condition: {"when":{"field":"status","eq":"published"},"show":"true"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.addCondition({ form: "article-quick-create", fieldId: "publishedAt", condition: {"when":{"field":"status","eq":"published"},"show":"true"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.addCondition !== 'function') return;
      const result = await interpret(formSpecHandler.addCondition({ form: "article-quick-create", fieldId: "publishedAt", condition: {"when":{"field":"status","eq":"published"},"show":"true"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "add_condition_ok" -> ok', async () => {
      if (typeof formSpecHandler.addCondition !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", fieldId: "publishedAt", condition: {"when":{"field":"status","eq":"published"},"show":"true"} } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.addCondition({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_condition_missing" -> notfound', async () => {
      if (typeof formSpecHandler.addCondition !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.addCondition({ form: "nonexistent-form", fieldId: "title", condition: {"when":{"field":"x","eq":"y"},"show":"true"} }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('removeCondition', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.removeCondition({ form: "article-quick-create", fieldId: "publishedAt" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.removeCondition({ form: "article-quick-create", fieldId: "publishedAt" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.removeCondition({ form: "article-quick-create", fieldId: "publishedAt" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.removeCondition({ form: "article-quick-create", fieldId: "publishedAt" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.removeCondition({ form: "article-quick-create", fieldId: "publishedAt" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.removeCondition !== 'function') return;
      const result = await interpret(formSpecHandler.removeCondition({ form: "article-quick-create", fieldId: "publishedAt" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_condition_ok" -> ok', async () => {
      if (typeof formSpecHandler.removeCondition !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const afterResult_add_condition_ok = await interpret(formSpecHandler.addCondition({ form: "article-quick-create", fieldId: "publishedAt", condition: {"when":{"field":"status","eq":"published"},"show":"true"} }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}), (afterResult_add_condition_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", fieldId: "publishedAt" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.removeCondition({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_condition_missing" -> notfound', async () => {
      if (typeof formSpecHandler.removeCondition !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.removeCondition({ form: "nonexistent-form", fieldId: "title" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('addStep', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.addStep({ form: "article-quick-create", title: "Details", position: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.addStep({ form: "article-quick-create", title: "Details", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.addStep({ form: "article-quick-create", title: "Details", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.addStep({ form: "article-quick-create", title: "Details", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.addStep({ form: "article-quick-create", title: "Details", position: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.addStep !== 'function') return;
      const result = await interpret(formSpecHandler.addStep({ form: "article-quick-create", title: "Details", position: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "add_step_ok" -> ok', async () => {
      if (typeof formSpecHandler.addStep !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", title: "Details", position: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.addStep({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_step_positioned" -> ok', async () => {
      if (typeof formSpecHandler.addStep !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", title: "Review", position: "0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.addStep({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "add_step_missing" -> notfound', async () => {
      if (typeof formSpecHandler.addStep !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.addStep({ form: "nonexistent-form", title: "Basic", position: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('removeStep', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.removeStep({ form: "article-quick-create", stepIndex: "0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.removeStep({ form: "article-quick-create", stepIndex: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.removeStep({ form: "article-quick-create", stepIndex: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.removeStep({ form: "article-quick-create", stepIndex: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.removeStep({ form: "article-quick-create", stepIndex: "0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.removeStep !== 'function') return;
      const result = await interpret(formSpecHandler.removeStep({ form: "article-quick-create", stepIndex: "0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_step_ok" -> ok', async () => {
      if (typeof formSpecHandler.removeStep !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const afterResult_add_step_ok = await interpret(formSpecHandler.addStep({ form: "article-quick-create", title: "Details", position: null }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}), (afterResult_add_step_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create", stepIndex: "0" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.removeStep({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_step_missing" -> notfound', async () => {
      if (typeof formSpecHandler.removeStep !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.removeStep({ form: "nonexistent-form", stepIndex: "0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.get({ form: "article-quick-create" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.get({ form: "article-quick-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.get({ form: "article-quick-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.get({ form: "article-quick-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.get({ form: "article-quick-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.get !== 'function') return;
      const result = await interpret(formSpecHandler.get({ form: "article-quick-create" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof formSpecHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof formSpecHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.get({ form: "nonexistent-form" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.list({ schema: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.list({ schema: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.list({ schema: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.list({ schema: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.list({ schema: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.list !== 'function') return;
      const result = await interpret(formSpecHandler.list({ schema: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof formSpecHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_schema" -> ok', async () => {
      if (typeof formSpecHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.resolve({ schema: "article", mode: "create" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.resolve({ schema: "article", mode: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.resolve({ schema: "article", mode: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.resolve({ schema: "article", mode: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.resolve({ schema: "article", mode: "create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.resolve !== 'function') return;
      const result = await interpret(formSpecHandler.resolve({ schema: "article", mode: "create" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_ok" -> ok', async () => {
      if (typeof formSpecHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", mode: "create" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.resolve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_missing" -> notfound', async () => {
      if (typeof formSpecHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.resolve({ schema: "nonexistent-schema", mode: "create" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('remove', () => {
    it('builds a valid StorageProgram', () => {
      const program = formSpecHandler.remove({ form: "article-quick-create" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formSpecHandler.remove({ form: "article-quick-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formSpecHandler.remove({ form: "article-quick-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formSpecHandler.remove({ form: "article-quick-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formSpecHandler.remove({ form: "article-quick-create" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formSpecHandler.remove !== 'function') return;
      const result = await interpret(formSpecHandler.remove({ form: "article-quick-create" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_ok" -> ok', async () => {
      if (typeof formSpecHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(formSpecHandler.create({ form: "article-quick-create", schema: "article", name: "quick-create", mode: "create" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { form: "article-quick-create" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formSpecHandler.remove({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_missing" -> notfound', async () => {
      if (typeof formSpecHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formSpecHandler.remove({ form: "nonexistent-form" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof formSpecHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = formSpecHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('FormSpec');
    });
  });

  describe('invariant examples', () => {
    it("create then get returns full config", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(formSpecHandler.create({ form: "article-qc", schema: "article", name: "quick-create", mode: "create" }), storage);
      expect(createResult0.variant).toBe("ok");
      let form = createResult0.output["form"];
      let f = form;
      const thenResult0 = await interpret(formSpecHandler.get({ form: f }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate create is rejected", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(formSpecHandler.create({ form: "task-form", schema: "task", name: "task-form", mode: "both" }), storage);
      expect(createResult0.variant).toBe("ok");
      let form = createResult0.output["form"];
      let f = form;
      const thenResult0 = await interpret(formSpecHandler.create({ form: "task-form-2", schema: "task", name: "task-form", mode: "both" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

    it("resolve finds form by schema and mode", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(formSpecHandler.create({ form: "article-create-form", schema: "article", name: "create-form", mode: "create" }), storage);
      expect(createResult0.variant).toBe("ok");
      let form = createResult0.output["form"];
      let f = form;
      const thenResult0 = await interpret(formSpecHandler.resolve({ schema: "article", mode: "create" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("update persists layout changes", async () => {
      const storage = createInMemoryStorage();
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: forms have non-empty schema and name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ form: fc.string(), schema: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('update'), input: fc.record({ form: fc.string(), layout: fc.string(), steps: fc.string(), conditions: fc.string(), defaults: fc.string(), overrides: fc.string() }) }),
              fc.record({ action: fc.constant('addField'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }), group: fc.string(), position: fc.string() }) }),
              fc.record({ action: fc.constant('removeField'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reorderField'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }), position: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('addCondition'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }), condition: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeCondition'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('addStep'), input: fc.record({ form: fc.string(), title: fc.string({ minLength: 1, maxLength: 50 }), position: fc.string() }) }),
              fc.record({ action: fc.constant('removeStep'), input: fc.record({ form: fc.string(), stepIndex: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ form: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ schema: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ form: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = formSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(formSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: mode is always a valid value', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ form: fc.string(), schema: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('update'), input: fc.record({ form: fc.string(), layout: fc.string(), steps: fc.string(), conditions: fc.string(), defaults: fc.string(), overrides: fc.string() }) }),
              fc.record({ action: fc.constant('addField'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }), group: fc.string(), position: fc.string() }) }),
              fc.record({ action: fc.constant('removeField'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reorderField'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }), position: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('addCondition'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }), condition: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeCondition'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('addStep'), input: fc.record({ form: fc.string(), title: fc.string({ minLength: 1, maxLength: 50 }), position: fc.string() }) }),
              fc.record({ action: fc.constant('removeStep'), input: fc.record({ form: fc.string(), stepIndex: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ form: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ schema: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ form: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = formSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(formSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned form state', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ form: fc.string(), schema: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('update'), input: fc.record({ form: fc.string(), layout: fc.string(), steps: fc.string(), conditions: fc.string(), defaults: fc.string(), overrides: fc.string() }) }),
              fc.record({ action: fc.constant('addField'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }), group: fc.string(), position: fc.string() }) }),
              fc.record({ action: fc.constant('removeField'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reorderField'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }), position: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('addCondition'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }), condition: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeCondition'), input: fc.record({ form: fc.string(), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('addStep'), input: fc.record({ form: fc.string(), title: fc.string({ minLength: 1, maxLength: 50 }), position: fc.string() }) }),
              fc.record({ action: fc.constant('removeStep'), input: fc.record({ form: fc.string(), stepIndex: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ form: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ schema: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ form: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = formSpecHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(formSpecHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned form state
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(formSpecHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof formSpecHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ form: fc.string(), schema: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), mode: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = formSpecHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
