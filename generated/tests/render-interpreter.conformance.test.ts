// RenderInterpreter Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { renderInterpreterHandler } from '../../handlers/ts/surface/render-interpreter.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('RenderInterpreter functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof renderInterpreterHandler.register !== 'function') return;
      const result = await interpret(renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "react_interpreter" -> ok', async () => {
      if (typeof renderInterpreterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "svelte_interpreter" -> ok', async () => {
      if (typeof renderInterpreterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(renderInterpreterHandler.register({ interpreter: "interp-svelte", target: "svelte", template: "tmpl" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('execute', () => {
    it('builds a valid StorageProgram', () => {
      const program = renderInterpreterHandler.execute({ interpreter: "interp-react", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}", snapshot: "current" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = renderInterpreterHandler.execute({ interpreter: "interp-react", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}", snapshot: "current" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = renderInterpreterHandler.execute({ interpreter: "interp-react", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}", snapshot: "current" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = renderInterpreterHandler.execute({ interpreter: "interp-react", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}", snapshot: "current" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = renderInterpreterHandler.execute({ interpreter: "interp-react", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}", snapshot: "current" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof renderInterpreterHandler.execute !== 'function') return;
      const result = await interpret(renderInterpreterHandler.execute({ interpreter: "interp-react", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}", snapshot: "current" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "execute_react" -> ok', async () => {
      if (typeof renderInterpreterHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_react_interpreter = await interpret(renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" }), storage);
      const _pool = Object.assign({}, (afterResult_react_interpreter?.output ?? {}));
      const _fixtureInput = { interpreter: "interp-react", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}", snapshot: "current" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(renderInterpreterHandler.execute({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "execute_unknown" -> notfound', async () => {
      if (typeof renderInterpreterHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_react_interpreter = await interpret(renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" }), storage);
      const _pool = Object.assign({}, (afterResult_react_interpreter?.output ?? {}));
      const _fixtureInput = { interpreter: "nonexistent", program: "{}", snapshot: "current" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(renderInterpreterHandler.execute({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('dryRun', () => {
    it('builds a valid StorageProgram', () => {
      const program = renderInterpreterHandler.dryRun({ interpreter: "interp-svelte", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = renderInterpreterHandler.dryRun({ interpreter: "interp-svelte", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = renderInterpreterHandler.dryRun({ interpreter: "interp-svelte", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = renderInterpreterHandler.dryRun({ interpreter: "interp-svelte", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = renderInterpreterHandler.dryRun({ interpreter: "interp-svelte", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof renderInterpreterHandler.dryRun !== 'function') return;
      const result = await interpret(renderInterpreterHandler.dryRun({ interpreter: "interp-svelte", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "dry_run_svelte" -> ok', async () => {
      if (typeof renderInterpreterHandler.dryRun !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_react_interpreter = await interpret(renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" }), storage);
      const _pool = Object.assign({}, (afterResult_react_interpreter?.output ?? {}));
      const _fixtureInput = { interpreter: "interp-svelte", program: "{\"instructions\":[{\"tag\":\"element\",\"part\":\"root\"}]}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(renderInterpreterHandler.dryRun({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "dry_run_unknown" -> notfound', async () => {
      if (typeof renderInterpreterHandler.dryRun !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_react_interpreter = await interpret(renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" }), storage);
      const _pool = Object.assign({}, (afterResult_react_interpreter?.output ?? {}));
      const _fixtureInput = { interpreter: "nonexistent", program: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(renderInterpreterHandler.dryRun({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('listTargets', () => {
    it('builds a valid StorageProgram', () => {
      const program = renderInterpreterHandler.listTargets({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = renderInterpreterHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = renderInterpreterHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = renderInterpreterHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = renderInterpreterHandler.listTargets({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof renderInterpreterHandler.listTargets !== 'function') return;
      const result = await interpret(renderInterpreterHandler.listTargets({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof renderInterpreterHandler.listTargets !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_react_interpreter = await interpret(renderInterpreterHandler.register({ interpreter: "interp-react", target: "react", template: "jsx" }), storage);
      const _pool = Object.assign({}, (afterResult_react_interpreter?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(renderInterpreterHandler.listTargets({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then execute succeeds", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(renderInterpreterHandler.register({ interpreter: "test-i", target: "react", template: "jsx" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let interpreter = registerResult0.output["interpreter"];
      let i = interpreter;
      const thenResult0 = await interpret(renderInterpreterHandler.execute({ interpreter: i, program: "p1", snapshot: "current" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate register returns exists", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(renderInterpreterHandler.register({ interpreter: "test-i", target: "react", template: "jsx" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let interpreter = registerResult0.output["interpreter"];
      let i = interpreter;
      const thenResult0 = await interpret(renderInterpreterHandler.register({ interpreter: i, target: "svelte", template: "tmpl" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("dryRun after register succeeds", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(renderInterpreterHandler.register({ interpreter: "test-i", target: "svelte", template: "tmpl" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let interpreter = registerResult0.output["interpreter"];
      let i = interpreter;
      const thenResult0 = await interpret(renderInterpreterHandler.dryRun({ interpreter: i, program: "p1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("listTargets includes registered target", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(renderInterpreterHandler.register({ interpreter: "test-i", target: "react", template: "jsx" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let interpreter = registerResult0.output["interpreter"];
      let i = interpreter;
      const thenResult0 = await interpret(renderInterpreterHandler.listTargets({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("execute without register returns notfound", async () => {
      const storage = createInMemoryStorage();
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: registered interpreters have targets', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ interpreter: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }), template: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ interpreter: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }), snapshot: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dryRun'), input: fc.record({ interpreter: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listTargets'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = renderInterpreterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(renderInterpreterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: registered interpreters have templates', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ interpreter: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }), template: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ interpreter: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }), snapshot: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dryRun'), input: fc.record({ interpreter: fc.string(), program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listTargets'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = renderInterpreterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(renderInterpreterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
