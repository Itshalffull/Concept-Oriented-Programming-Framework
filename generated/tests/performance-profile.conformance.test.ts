// PerformanceProfile Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { performanceProfileHandler } from '../../handlers/ts/score/performance-profile.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('PerformanceProfile functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('aggregate', () => {
    it('builds a valid StorageProgram', () => {
      const program = performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof performanceProfileHandler.aggregate !== 'function') return;
      const result = await interpret(performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "aggregate_article_create" -> ok', async () => {
      if (typeof performanceProfileHandler.aggregate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "aggregate_empty_symbol" -> error', async () => {
      if (typeof performanceProfileHandler.aggregate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(performanceProfileHandler.aggregate({ symbol: "", window: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('hotspots', () => {
    it('builds a valid StorageProgram', () => {
      const program = performanceProfileHandler.hotspots({ kind: "action", metric: "p90", topN: "10" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = performanceProfileHandler.hotspots({ kind: "action", metric: "p90", topN: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = performanceProfileHandler.hotspots({ kind: "action", metric: "p90", topN: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = performanceProfileHandler.hotspots({ kind: "action", metric: "p90", topN: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = performanceProfileHandler.hotspots({ kind: "action", metric: "p90", topN: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof performanceProfileHandler.hotspots !== 'function') return;
      const result = await interpret(performanceProfileHandler.hotspots({ kind: "action", metric: "p90", topN: "10" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "hotspots_actions_p90" -> ok', async () => {
      if (typeof performanceProfileHandler.hotspots !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_aggregate_article_create = await interpret(performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_aggregate_article_create?.output ?? {}));
      const _fixtureInput = { kind: "action", metric: "p90", topN: "10" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(performanceProfileHandler.hotspots({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "hotspots_invalid_metric" -> error', async () => {
      if (typeof performanceProfileHandler.hotspots !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(performanceProfileHandler.hotspots({ kind: "action", metric: "", topN: "0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('slowChains', () => {
    it('builds a valid StorageProgram', () => {
      const program = performanceProfileHandler.slowChains({ thresholdMs: "500" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = performanceProfileHandler.slowChains({ thresholdMs: "500" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = performanceProfileHandler.slowChains({ thresholdMs: "500" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = performanceProfileHandler.slowChains({ thresholdMs: "500" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = performanceProfileHandler.slowChains({ thresholdMs: "500" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof performanceProfileHandler.slowChains !== 'function') return;
      const result = await interpret(performanceProfileHandler.slowChains({ thresholdMs: "500" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "slow_chains_500ms" -> ok', async () => {
      if (typeof performanceProfileHandler.slowChains !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_aggregate_article_create = await interpret(performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_aggregate_article_create?.output ?? {}));
      const _fixtureInput = { thresholdMs: "500" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(performanceProfileHandler.slowChains({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "slow_chains_zero" -> error', async () => {
      if (typeof performanceProfileHandler.slowChains !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(performanceProfileHandler.slowChains({ thresholdMs: "0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('compareWindows', () => {
    it('builds a valid StorageProgram', () => {
      const program = performanceProfileHandler.compareWindows({ symbol: "clef/action/Article/create", windowA: "{\"start\":\"2026-03-01\",\"end\":\"2026-03-07\"}", windowB: "{\"start\":\"2026-03-08\",\"end\":\"2026-03-14\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = performanceProfileHandler.compareWindows({ symbol: "clef/action/Article/create", windowA: "{\"start\":\"2026-03-01\",\"end\":\"2026-03-07\"}", windowB: "{\"start\":\"2026-03-08\",\"end\":\"2026-03-14\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = performanceProfileHandler.compareWindows({ symbol: "clef/action/Article/create", windowA: "{\"start\":\"2026-03-01\",\"end\":\"2026-03-07\"}", windowB: "{\"start\":\"2026-03-08\",\"end\":\"2026-03-14\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = performanceProfileHandler.compareWindows({ symbol: "clef/action/Article/create", windowA: "{\"start\":\"2026-03-01\",\"end\":\"2026-03-07\"}", windowB: "{\"start\":\"2026-03-08\",\"end\":\"2026-03-14\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = performanceProfileHandler.compareWindows({ symbol: "clef/action/Article/create", windowA: "{\"start\":\"2026-03-01\",\"end\":\"2026-03-07\"}", windowB: "{\"start\":\"2026-03-08\",\"end\":\"2026-03-14\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof performanceProfileHandler.compareWindows !== 'function') return;
      const result = await interpret(performanceProfileHandler.compareWindows({ symbol: "clef/action/Article/create", windowA: "{\"start\":\"2026-03-01\",\"end\":\"2026-03-07\"}", windowB: "{\"start\":\"2026-03-08\",\"end\":\"2026-03-14\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compare_weekly" -> ok', async () => {
      if (typeof performanceProfileHandler.compareWindows !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(performanceProfileHandler.compareWindows({ symbol: "clef/action/Article/create", windowA: "{\"start\":\"2026-03-01\",\"end\":\"2026-03-07\"}", windowB: "{\"start\":\"2026-03-08\",\"end\":\"2026-03-14\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compare_empty_symbol" -> error', async () => {
      if (typeof performanceProfileHandler.compareWindows !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(performanceProfileHandler.compareWindows({ symbol: "", windowA: "{}", windowB: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = performanceProfileHandler.get({ profile: "performance-profile-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = performanceProfileHandler.get({ profile: "performance-profile-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = performanceProfileHandler.get({ profile: "performance-profile-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = performanceProfileHandler.get({ profile: "performance-profile-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = performanceProfileHandler.get({ profile: "performance-profile-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof performanceProfileHandler.get !== 'function') return;
      const result = await interpret(performanceProfileHandler.get({ profile: "performance-profile-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing_profile" -> ok', async () => {
      if (typeof performanceProfileHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_aggregate_article_create = await interpret(performanceProfileHandler.aggregate({ symbol: "clef/action/Article/create", window: "{}" }), storage);
      const _pool = Object.assign({}, (afterResult_aggregate_article_create?.output ?? {}));
      const _fixtureInput = { profile: "performance-profile-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(performanceProfileHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_nonexistent" -> error', async () => {
      if (typeof performanceProfileHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(performanceProfileHandler.get({ profile: "nonexistent-profile" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof performanceProfileHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = performanceProfileHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('PerformanceProfile');
    });
  });

  describe('invariant examples', () => {
    it("aggregated profile is retrievable", async () => {
      const storage = createInMemoryStorage();
      const aggregateResult0 = await interpret(performanceProfileHandler.aggregate({ symbol: {"type":"literal","value":"clef/action/Article/create"}, window: {"type":"literal","value":"{}"} }), storage);
      expect(aggregateResult0.variant).toBe("ok");
      let profile = aggregateResult0.output["profile"];
      const thenResult0 = await interpret(performanceProfileHandler.get({ profile: {"type":"variable","name":"p"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every profiles entry has entitySymbol', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('aggregate'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 50 }), window: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('hotspots'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }), metric: fc.string({ minLength: 1, maxLength: 50 }), topN: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('slowChains'), input: fc.record({ thresholdMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('compareWindows'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 50 }), windowA: fc.string({ minLength: 1, maxLength: 50 }), windowB: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ profile: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = performanceProfileHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(performanceProfileHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty entitySymbol in profiles', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('aggregate'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 50 }), window: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('hotspots'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }), metric: fc.string({ minLength: 1, maxLength: 50 }), topN: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('slowChains'), input: fc.record({ thresholdMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('compareWindows'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 50 }), windowA: fc.string({ minLength: 1, maxLength: 50 }), windowB: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ profile: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = performanceProfileHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(performanceProfileHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty entitySymbol in profiles
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('aggregate handles empty input: ', async () => {
      if (typeof performanceProfileHandler.aggregate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(performanceProfileHandler.aggregate({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('aggregate ensures on ok: ', async () => {
      if (typeof performanceProfileHandler.aggregate !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ symbol: fc.string({ minLength: 1, maxLength: 50 }), window: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = performanceProfileHandler.aggregate(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
