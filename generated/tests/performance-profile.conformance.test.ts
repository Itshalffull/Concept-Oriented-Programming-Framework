// PerformanceProfile Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { performanceProfileHandler } from '../../handlers/ts/score/performance-profile.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('PerformanceProfile imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('aggregate', () => {
    it('executes without crashing', async () => {
      if (typeof performanceProfileHandler.aggregate !== 'function') return;
      try {
        const result = await performanceProfileHandler.aggregate({ symbol: 'test-symbol', window: 'test-window' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('hotspots', () => {
    it('executes without crashing', async () => {
      if (typeof performanceProfileHandler.hotspots !== 'function') return;
      try {
        const result = await performanceProfileHandler.hotspots({ kind: 'test-kind', metric: 'test-metric', topN: 1 }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('slowChains', () => {
    it('executes without crashing', async () => {
      if (typeof performanceProfileHandler.slowChains !== 'function') return;
      try {
        const result = await performanceProfileHandler.slowChains({ thresholdMs: 1 }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('compareWindows', () => {
    it('executes without crashing', async () => {
      if (typeof performanceProfileHandler.compareWindows !== 'function') return;
      try {
        const result = await performanceProfileHandler.compareWindows({ symbol: 'test-symbol', windowA: 'test-windowA', windowB: 'test-windowB' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('get', () => {
    it('executes without crashing', async () => {
      if (typeof performanceProfileHandler.get !== 'function') return;
      try {
        const result = await performanceProfileHandler.get({ profile: 'test' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("aggregated profile is retrievable", async () => {
      const storage = createInMemoryStorage();
      const aggregateResult0 = await performanceProfileHandler.aggregate({ symbol: {"type":"literal","value":"clef/action/Article/create"}, window: {"type":"literal","value":"{}"} }, storage);
      expect(aggregateResult0.variant).toBe("ok");
      const profile = aggregateResult0.output["profile"];
      const thenResult0 = await performanceProfileHandler.get({ profile: {"type":"variable","name":"p"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every profiles entry has entitySymbol', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('aggregate'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 20 }), window: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('hotspots'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 20 }), metric: fc.string({ minLength: 1, maxLength: 20 }), topN: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('slowChains'), input: fc.record({ thresholdMs: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('compareWindows'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 20 }), windowA: fc.string({ minLength: 1, maxLength: 20 }), windowB: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ profile: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = performanceProfileHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(performanceProfileHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty entitySymbol in profiles', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('aggregate'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 20 }), window: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('hotspots'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 20 }), metric: fc.string({ minLength: 1, maxLength: 20 }), topN: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('slowChains'), input: fc.record({ thresholdMs: fc.integer({ min: 0, max: 1000 }) }) }),
              fc.record({ action: fc.constant('compareWindows'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 20 }), windowA: fc.string({ minLength: 1, maxLength: 20 }), windowB: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ profile: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = performanceProfileHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(performanceProfileHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: empty entitySymbol in profiles
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('aggregate handles empty input: ', async () => {
      if (typeof performanceProfileHandler.aggregate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await performanceProfileHandler.aggregate({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('aggregate ensures on ok: ', async () => {
      if (typeof performanceProfileHandler.aggregate !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ symbol: fc.string({ minLength: 1, maxLength: 50 }), window: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await performanceProfileHandler.aggregate(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
