// FormalProperty Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { formalPropertyHandler } from '../../handlers/ts/suites/formal-verification/formal-property.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('FormalProperty functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('define', () => {
    it('builds a valid StorageProgram', () => {
      const program = formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formalPropertyHandler.define !== 'function') return;
      const result = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_define" -> ok', async () => {
      if (typeof formalPropertyHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "invalid_kind" -> invalid', async () => {
      if (typeof formalPropertyHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formalPropertyHandler.define({ name: "bad-prop", target_symbol: "clef/concept/User", kind: "bogus", expression: "true", formal_language: "smtlib", scope: "local", priority: "optional" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('prove', () => {
    it('builds a valid StorageProgram', () => {
      const program = formalPropertyHandler.prove({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-abc" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formalPropertyHandler.prove({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-abc" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formalPropertyHandler.prove({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-abc" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formalPropertyHandler.prove({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-abc" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formalPropertyHandler.prove({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-abc" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formalPropertyHandler.prove !== 'function') return;
      const result = await interpret(formalPropertyHandler.prove({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-abc" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_prove" -> ok', async () => {
      if (typeof formalPropertyHandler.prove !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const result = await interpret(formalPropertyHandler.prove({ id: afterResult_valid_define?.output?.["property"], evidence_ref: "ev-abc" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "prove_missing" -> notfound', async () => {
      if (typeof formalPropertyHandler.prove !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formalPropertyHandler.prove({ id: "nonexistent", evidence_ref: "ev-abc" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('refute', () => {
    it('builds a valid StorageProgram', () => {
      const program = formalPropertyHandler.refute({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-counter" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formalPropertyHandler.refute({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-counter" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formalPropertyHandler.refute({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-counter" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formalPropertyHandler.refute({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-counter" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formalPropertyHandler.refute({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-counter" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formalPropertyHandler.refute !== 'function') return;
      const result = await interpret(formalPropertyHandler.refute({ id: {"type":"ref","fixture":"valid_define","field":"property"}, evidence_ref: "ev-counter" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_refute" -> ok', async () => {
      if (typeof formalPropertyHandler.refute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const result = await interpret(formalPropertyHandler.refute({ id: afterResult_valid_define?.output?.["property"], evidence_ref: "ev-counter" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "refute_missing" -> notfound', async () => {
      if (typeof formalPropertyHandler.refute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formalPropertyHandler.refute({ id: "nonexistent", evidence_ref: "ev-counter" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('check', () => {
    it('builds a valid StorageProgram', () => {
      const program = formalPropertyHandler.check({ id: {"type":"ref","fixture":"valid_define","field":"property"}, solver: "z3" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formalPropertyHandler.check({ id: {"type":"ref","fixture":"valid_define","field":"property"}, solver: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formalPropertyHandler.check({ id: {"type":"ref","fixture":"valid_define","field":"property"}, solver: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formalPropertyHandler.check({ id: {"type":"ref","fixture":"valid_define","field":"property"}, solver: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formalPropertyHandler.check({ id: {"type":"ref","fixture":"valid_define","field":"property"}, solver: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formalPropertyHandler.check !== 'function') return;
      const result = await interpret(formalPropertyHandler.check({ id: {"type":"ref","fixture":"valid_define","field":"property"}, solver: "z3" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_check" -> ok', async () => {
      if (typeof formalPropertyHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const result = await interpret(formalPropertyHandler.check({ id: afterResult_valid_define?.output?.["property"], solver: "z3" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "check_missing" -> notfound', async () => {
      if (typeof formalPropertyHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formalPropertyHandler.check({ id: "nonexistent", solver: "z3" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('synthesize', () => {
    it('builds a valid StorageProgram', () => {
      const program = formalPropertyHandler.synthesize({ target_symbol: "clef/concept/Password", intent_ref: {"type":"ref","fixture":"valid_define","field":"property"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formalPropertyHandler.synthesize({ target_symbol: "clef/concept/Password", intent_ref: {"type":"ref","fixture":"valid_define","field":"property"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formalPropertyHandler.synthesize({ target_symbol: "clef/concept/Password", intent_ref: {"type":"ref","fixture":"valid_define","field":"property"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formalPropertyHandler.synthesize({ target_symbol: "clef/concept/Password", intent_ref: {"type":"ref","fixture":"valid_define","field":"property"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formalPropertyHandler.synthesize({ target_symbol: "clef/concept/Password", intent_ref: {"type":"ref","fixture":"valid_define","field":"property"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formalPropertyHandler.synthesize !== 'function') return;
      const result = await interpret(formalPropertyHandler.synthesize({ target_symbol: "clef/concept/Password", intent_ref: {"type":"ref","fixture":"valid_define","field":"property"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_synthesize" -> ok', async () => {
      if (typeof formalPropertyHandler.synthesize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const result = await interpret(formalPropertyHandler.synthesize({ target_symbol: "clef/concept/Password", intent_ref: afterResult_valid_define?.output?.["property"] }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "synthesize_with_lang" -> ok', async () => {
      if (typeof formalPropertyHandler.synthesize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const result = await interpret(formalPropertyHandler.synthesize({ target_symbol: "clef/concept/User", intent_ref: afterResult_valid_define?.output?.["property"], formal_language: "lean" }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

  });

  describe('coverage', () => {
    it('builds a valid StorageProgram', () => {
      const program = formalPropertyHandler.coverage({ target_symbol: "clef/concept/Password" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formalPropertyHandler.coverage({ target_symbol: "clef/concept/Password" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formalPropertyHandler.coverage({ target_symbol: "clef/concept/Password" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formalPropertyHandler.coverage({ target_symbol: "clef/concept/Password" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formalPropertyHandler.coverage({ target_symbol: "clef/concept/Password" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formalPropertyHandler.coverage !== 'function') return;
      const result = await interpret(formalPropertyHandler.coverage({ target_symbol: "clef/concept/Password" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_coverage" -> ok', async () => {
      if (typeof formalPropertyHandler.coverage !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { target_symbol: "clef/concept/Password" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(formalPropertyHandler.coverage({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "coverage_unknown" -> ok', async () => {
      if (typeof formalPropertyHandler.coverage !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { target_symbol: "clef/concept/Unknown" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(formalPropertyHandler.coverage({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = formalPropertyHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formalPropertyHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formalPropertyHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formalPropertyHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formalPropertyHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formalPropertyHandler.list !== 'function') return;
      const result = await interpret(formalPropertyHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof formalPropertyHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(formalPropertyHandler.list({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "list_by_kind" -> ok', async () => {
      if (typeof formalPropertyHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { kind: "invariant" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(formalPropertyHandler.list({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

  });

  describe('invalidate', () => {
    it('builds a valid StorageProgram', () => {
      const program = formalPropertyHandler.invalidate({ id: {"type":"ref","fixture":"valid_define","field":"property"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formalPropertyHandler.invalidate({ id: {"type":"ref","fixture":"valid_define","field":"property"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formalPropertyHandler.invalidate({ id: {"type":"ref","fixture":"valid_define","field":"property"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formalPropertyHandler.invalidate({ id: {"type":"ref","fixture":"valid_define","field":"property"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formalPropertyHandler.invalidate({ id: {"type":"ref","fixture":"valid_define","field":"property"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formalPropertyHandler.invalidate !== 'function') return;
      const result = await interpret(formalPropertyHandler.invalidate({ id: {"type":"ref","fixture":"valid_define","field":"property"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_invalidate" -> ok', async () => {
      if (typeof formalPropertyHandler.invalidate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(formalPropertyHandler.define({ name: "hash-nonzero", target_symbol: "clef/concept/Password", kind: "invariant", expression: "forall p: Password | len(p.hash) > 0", formal_language: "smtlib", scope: "local", priority: "required" }), storage);
      const result = await interpret(formalPropertyHandler.invalidate({ id: afterResult_valid_define?.output?.["property"] }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "invalidate_missing" -> notfound', async () => {
      if (typeof formalPropertyHandler.invalidate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formalPropertyHandler.invalidate({ id: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof formalPropertyHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = formalPropertyHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('FormalProperty');
    });
  });

  describe('invariant examples', () => {
    it("define-then-coverage", async () => {
      const storage = createInMemoryStorage();
      const defineResult0 = await interpret(formalPropertyHandler.define({ target_symbol: {"type":"literal","value":"clef/concept/Password"}, kind: {"type":"literal","value":"invariant"}, property_text: {"type":"literal","value":"forall p: Password | len(p.hash) > 0"}, formal_language: {"type":"literal","value":"smtlib"}, scope: {"type":"literal","value":"local"}, priority: {"type":"literal","value":"required"} }), storage);
      const _isErr0 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr0(defineResult0.variant), `step 0: expected success but got '${defineResult0.variant}'`).toBe(false);
      let property = defineResult0.output["property"];
      const thenResult0 = await interpret(formalPropertyHandler.check({ property: {"type":"variable","name":"p"}, solver: {"type":"literal","value":"z3"}, timeout_ms: {"type":"literal","value":5000} }), storage);
      const _isErrA0 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErrA0(thenResult0.variant), `assertion 0: expected success but got '${thenResult0.variant}'`).toBe(false);
      const thenResult1 = await interpret(formalPropertyHandler.coverage({ target_symbol: {"type":"literal","value":"clef/concept/Password"} }), storage);
      const _isErrA1 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErrA1(thenResult1.variant), `assertion 1: expected success but got '${thenResult1.variant}'`).toBe(false);
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-target_symbol', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('define'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), target_symbol: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), expression: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string({ minLength: 1, maxLength: 50 }), scope: fc.string({ minLength: 1, maxLength: 50 }), priority: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('prove'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }), evidence_ref: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('refute'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }), evidence_ref: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }), solver: fc.string() }) }),
              fc.record({ action: fc.constant('synthesize'), input: fc.record({ target_symbol: fc.string({ minLength: 1, maxLength: 50 }), intent_ref: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string() }) }),
              fc.record({ action: fc.constant('coverage'), input: fc.record({ target_symbol: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ target_symbol: fc.string(), kind: fc.string(), status: fc.string() }) }),
              fc.record({ action: fc.constant('invalidate'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = formalPropertyHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(formalPropertyHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-kind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('define'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), target_symbol: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), expression: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string({ minLength: 1, maxLength: 50 }), scope: fc.string({ minLength: 1, maxLength: 50 }), priority: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('prove'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }), evidence_ref: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('refute'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }), evidence_ref: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }), solver: fc.string() }) }),
              fc.record({ action: fc.constant('synthesize'), input: fc.record({ target_symbol: fc.string({ minLength: 1, maxLength: 50 }), intent_ref: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string() }) }),
              fc.record({ action: fc.constant('coverage'), input: fc.record({ target_symbol: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ target_symbol: fc.string(), kind: fc.string(), status: fc.string() }) }),
              fc.record({ action: fc.constant('invalidate'), input: fc.record({ id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = formalPropertyHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(formalPropertyHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-kind
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('define handles empty input: ', async () => {
      if (typeof formalPropertyHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(formalPropertyHandler.define({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('define ensures on ok: ', async () => {
      if (typeof formalPropertyHandler.define !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), target_symbol: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), expression: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string({ minLength: 1, maxLength: 50 }), scope: fc.string({ minLength: 1, maxLength: 50 }), priority: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = formalPropertyHandler.define(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
