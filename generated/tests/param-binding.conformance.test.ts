// ParamBinding Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { paramBindingHandler } from '../../handlers/ts/param-binding.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ParamBinding functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const result = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "row_click_to_detail_page" -> ok', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "url_param_to_view_filter" -> ok', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.create({ binding: "bind-2", name: "filterBySchema", scopeRef: "view:content-list", sourceKind: "url-param", sourceExpression: "schema", targetKind: "view-filter", targetExpression: "schemas", triggerEvent: "load", transformExpression: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "relation_traversal_navigate" -> ok', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.create({ binding: "bind-3", name: "goToAuthor", scopeRef: "view:article-detail", sourceKind: "relation", sourceExpression: "author.profile.slug", targetKind: "navigate", targetExpression: "/profiles/{value}", triggerEvent: "click", transformExpression: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "literal_action_input" -> ok', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.create({ binding: "bind-4", name: "publishAction", scopeRef: "view:article-detail", sourceKind: "field", sourceExpression: "node", targetKind: "action-input", targetExpression: "ContentNode/update.node", triggerEvent: "click", transformExpression: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "control_to_filter" -> ok', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.create({ binding: "bind-5", name: "searchFilter", scopeRef: "view:content-list", sourceKind: "control", sourceExpression: "searchInput.value", targetKind: "view-filter", targetExpression: "title", triggerEvent: "change", transformExpression: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_name" -> invalid', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.create({ binding: "bind-6", name: "", scopeRef: "view:x", sourceKind: "field", sourceExpression: "id", targetKind: "navigate", targetExpression: "/x", triggerEvent: "click" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "bad_source_kind" -> invalid', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.create({ binding: "bind-7", name: "bad", scopeRef: "view:x", sourceKind: "magic", sourceExpression: "id", targetKind: "navigate", targetExpression: "/x", triggerEvent: "click" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "bad_trigger" -> invalid', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.create({ binding: "bind-8", name: "bad", scopeRef: "view:x", sourceKind: "field", sourceExpression: "id", targetKind: "navigate", targetExpression: "/x", triggerEvent: "hover" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "duplicate_name" -> duplicate', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_row_click_to_detail_page = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      const _pool = Object.assign({}, (afterResult_row_click_to_detail_page?.output ?? {}));
      const _fixtureInput = { binding: "bind-9", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "id", targetKind: "navigate", targetExpression: "/x", triggerEvent: "click" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramBindingHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramBindingHandler.resolve({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, data: "{\"node\": \"article-42\", \"title\": \"Hello\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramBindingHandler.resolve({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, data: "{\"node\": \"article-42\", \"title\": \"Hello\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramBindingHandler.resolve({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, data: "{\"node\": \"article-42\", \"title\": \"Hello\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramBindingHandler.resolve({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, data: "{\"node\": \"article-42\", \"title\": \"Hello\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramBindingHandler.resolve({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, data: "{\"node\": \"article-42\", \"title\": \"Hello\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramBindingHandler.resolve !== 'function') return;
      const result = await interpret(paramBindingHandler.resolve({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, data: "{\"node\": \"article-42\", \"title\": \"Hello\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_field" -> ok', async () => {
      if (typeof paramBindingHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_row_click_to_detail_page = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      const result = await interpret(paramBindingHandler.resolve({ binding: afterResult_row_click_to_detail_page?.output?.["binding"], data: "{\"node\": \"article-42\", \"title\": \"Hello\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_relation" -> ok', async () => {
      if (typeof paramBindingHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_relation_traversal_navigate = await interpret(paramBindingHandler.create({ binding: "bind-3", name: "goToAuthor", scopeRef: "view:article-detail", sourceKind: "relation", sourceExpression: "author.profile.slug", targetKind: "navigate", targetExpression: "/profiles/{value}", triggerEvent: "click", transformExpression: "" }), storage);
      const result = await interpret(paramBindingHandler.resolve({ binding: afterResult_relation_traversal_navigate?.output?.["binding"], data: "{\"author\": {\"profile\": {\"slug\": \"jane-doe\"}}}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_missing_binding" -> notfound', async () => {
      if (typeof paramBindingHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.resolve({ binding: "nonexistent", data: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "resolve_bad_expression" -> invalid', async () => {
      if (typeof paramBindingHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_row_click_to_detail_page = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      const result = await interpret(paramBindingHandler.resolve({ binding: afterResult_row_click_to_detail_page?.output?.["binding"], data: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('buildTarget', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramBindingHandler.buildTarget({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, resolvedValue: "article-42" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramBindingHandler.buildTarget({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, resolvedValue: "article-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramBindingHandler.buildTarget({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, resolvedValue: "article-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramBindingHandler.buildTarget({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, resolvedValue: "article-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramBindingHandler.buildTarget({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, resolvedValue: "article-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramBindingHandler.buildTarget !== 'function') return;
      const result = await interpret(paramBindingHandler.buildTarget({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"}, resolvedValue: "article-42" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "build_navigate_target" -> ok', async () => {
      if (typeof paramBindingHandler.buildTarget !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_row_click_to_detail_page = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      const result = await interpret(paramBindingHandler.buildTarget({ binding: afterResult_row_click_to_detail_page?.output?.["binding"], resolvedValue: "article-42" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "build_filter_target" -> ok', async () => {
      if (typeof paramBindingHandler.buildTarget !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_url_param_to_view_filter = await interpret(paramBindingHandler.create({ binding: "bind-2", name: "filterBySchema", scopeRef: "view:content-list", sourceKind: "url-param", sourceExpression: "schema", targetKind: "view-filter", targetExpression: "schemas", triggerEvent: "load", transformExpression: "" }), storage);
      const result = await interpret(paramBindingHandler.buildTarget({ binding: afterResult_url_param_to_view_filter?.output?.["binding"], resolvedValue: "Article" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "build_target_missing" -> notfound', async () => {
      if (typeof paramBindingHandler.buildTarget !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.buildTarget({ binding: "nonexistent", resolvedValue: "x" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramBindingHandler.get({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramBindingHandler.get({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramBindingHandler.get({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramBindingHandler.get({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramBindingHandler.get({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramBindingHandler.get !== 'function') return;
      const result = await interpret(paramBindingHandler.get({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof paramBindingHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_row_click_to_detail_page = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      const result = await interpret(paramBindingHandler.get({ binding: afterResult_row_click_to_detail_page?.output?.["binding"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof paramBindingHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.get({ binding: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('listForScope', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramBindingHandler.listForScope({ scopeRef: "view:content-list" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramBindingHandler.listForScope({ scopeRef: "view:content-list" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramBindingHandler.listForScope({ scopeRef: "view:content-list" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramBindingHandler.listForScope({ scopeRef: "view:content-list" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramBindingHandler.listForScope({ scopeRef: "view:content-list" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramBindingHandler.listForScope !== 'function') return;
      const result = await interpret(paramBindingHandler.listForScope({ scopeRef: "view:content-list" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_content_list_bindings" -> ok', async () => {
      if (typeof paramBindingHandler.listForScope !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_row_click_to_detail_page = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      const _pool = Object.assign({}, (afterResult_row_click_to_detail_page?.output ?? {}));
      const _fixtureInput = { scopeRef: "view:content-list" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramBindingHandler.listForScope({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_empty_scope" -> ok', async () => {
      if (typeof paramBindingHandler.listForScope !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_row_click_to_detail_page = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      const _pool = Object.assign({}, (afterResult_row_click_to_detail_page?.output ?? {}));
      const _fixtureInput = { scopeRef: "view:nonexistent" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramBindingHandler.listForScope({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('delete', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramBindingHandler.delete({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramBindingHandler.delete({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramBindingHandler.delete({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramBindingHandler.delete({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramBindingHandler.delete({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramBindingHandler.delete !== 'function') return;
      const result = await interpret(paramBindingHandler.delete({ binding: {"type":"ref","fixture":"row_click_to_detail_page","field":"binding"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "delete_existing" -> ok', async () => {
      if (typeof paramBindingHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_row_click_to_detail_page = await interpret(paramBindingHandler.create({ binding: "bind-1", name: "viewArticle", scopeRef: "view:content-list", sourceKind: "field", sourceExpression: "node", targetKind: "navigate", targetExpression: "/content/{value}", triggerEvent: "click", transformExpression: "encodeURIComponent" }), storage);
      const result = await interpret(paramBindingHandler.delete({ binding: afterResult_row_click_to_detail_page?.output?.["binding"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "delete_missing" -> notfound', async () => {
      if (typeof paramBindingHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramBindingHandler.delete({ binding: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof paramBindingHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = paramBindingHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ParamBinding');
    });
  });

  describe('invariant examples', () => {
    it("create then resolve then buildTarget", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(paramBindingHandler.create({ binding: "test-b", name: "goToItem", scopeRef: "view:items", sourceKind: "field", sourceExpression: "id", targetKind: "navigate", targetExpression: "/items/{value}", triggerEvent: "click", transformExpression: "test-_" }), storage);
      expect(createResult0.variant).toBe("ok");
      let binding = createResult0.output["binding"];
      let b = binding;
      const thenResult0 = await interpret(paramBindingHandler.resolve({ binding: b, data: "{\"id\": \"item-99\"}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("create then get", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(paramBindingHandler.create({ binding: "test-b", name: "filterByType", scopeRef: "view:list", sourceKind: "url-param", sourceExpression: "type", targetKind: "view-filter", targetExpression: "schemas", triggerEvent: "load", transformExpression: "test-_" }), storage);
      expect(createResult0.variant).toBe("ok");
      let binding = createResult0.output["binding"];
      let b = binding;
      const thenResult0 = await interpret(paramBindingHandler.get({ binding: b }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: bindings have valid sourceKind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ binding: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), scopeRef: fc.string({ minLength: 1, maxLength: 50 }), sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceExpression: fc.string({ minLength: 1, maxLength: 50 }), targetKind: fc.string({ minLength: 1, maxLength: 50 }), targetExpression: fc.string({ minLength: 1, maxLength: 50 }), triggerEvent: fc.string({ minLength: 1, maxLength: 50 }), transformExpression: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ binding: fc.string(), data: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('buildTarget'), input: fc.record({ binding: fc.string(), resolvedValue: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('listForScope'), input: fc.record({ scopeRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ binding: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paramBindingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paramBindingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: bindings have valid targetKind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ binding: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), scopeRef: fc.string({ minLength: 1, maxLength: 50 }), sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceExpression: fc.string({ minLength: 1, maxLength: 50 }), targetKind: fc.string({ minLength: 1, maxLength: 50 }), targetExpression: fc.string({ minLength: 1, maxLength: 50 }), triggerEvent: fc.string({ minLength: 1, maxLength: 50 }), transformExpression: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ binding: fc.string(), data: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('buildTarget'), input: fc.record({ binding: fc.string(), resolvedValue: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('listForScope'), input: fc.record({ scopeRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ binding: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paramBindingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paramBindingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: bindings have valid triggerEvent', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ binding: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), scopeRef: fc.string({ minLength: 1, maxLength: 50 }), sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceExpression: fc.string({ minLength: 1, maxLength: 50 }), targetKind: fc.string({ minLength: 1, maxLength: 50 }), targetExpression: fc.string({ minLength: 1, maxLength: 50 }), triggerEvent: fc.string({ minLength: 1, maxLength: 50 }), transformExpression: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ binding: fc.string(), data: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('buildTarget'), input: fc.record({ binding: fc.string(), resolvedValue: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('listForScope'), input: fc.record({ scopeRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ binding: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paramBindingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paramBindingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: binding without expressions', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ binding: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), scopeRef: fc.string({ minLength: 1, maxLength: 50 }), sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceExpression: fc.string({ minLength: 1, maxLength: 50 }), targetKind: fc.string({ minLength: 1, maxLength: 50 }), targetExpression: fc.string({ minLength: 1, maxLength: 50 }), triggerEvent: fc.string({ minLength: 1, maxLength: 50 }), transformExpression: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ binding: fc.string(), data: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('buildTarget'), input: fc.record({ binding: fc.string(), resolvedValue: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('listForScope'), input: fc.record({ scopeRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ binding: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paramBindingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paramBindingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: binding without expressions
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(paramBindingHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof paramBindingHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ binding: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), scopeRef: fc.string({ minLength: 1, maxLength: 50 }), sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceExpression: fc.string({ minLength: 1, maxLength: 50 }), targetKind: fc.string({ minLength: 1, maxLength: 50 }), targetExpression: fc.string({ minLength: 1, maxLength: 50 }), triggerEvent: fc.string({ minLength: 1, maxLength: 50 }), transformExpression: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = paramBindingHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('resolve handles empty input: ', async () => {
      if (typeof paramBindingHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(paramBindingHandler.resolve({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

});
