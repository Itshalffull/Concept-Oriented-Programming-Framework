// WebhookInbox Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { webhookInboxHandler } from '../../handlers/ts/process-automation/webhook-inbox.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('WebhookInbox functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = webhookInboxHandler.register({ run_ref: 'test-run_ref', step_ref: 'test-step_ref', event_type: 'test-event_type', correlation_key: 'test-correlation_key' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = webhookInboxHandler.register({ run_ref: 'test-run_ref', step_ref: 'test-step_ref', event_type: 'test-event_type', correlation_key: 'test-correlation_key' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = webhookInboxHandler.register({ run_ref: 'test-run_ref', step_ref: 'test-step_ref', event_type: 'test-event_type', correlation_key: 'test-correlation_key' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = webhookInboxHandler.register({ run_ref: 'test-run_ref', step_ref: 'test-step_ref', event_type: 'test-event_type', correlation_key: 'test-correlation_key' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = webhookInboxHandler.register({ run_ref: 'test-run_ref', step_ref: 'test-step_ref', event_type: 'test-event_type', correlation_key: 'test-correlation_key' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof webhookInboxHandler.register !== 'function') return;
      const result = await interpret(webhookInboxHandler.register({ run_ref: 'test-run_ref', step_ref: 'test-step_ref', event_type: 'test-event_type', correlation_key: 'test-correlation_key' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('receive', () => {
    it('builds a valid StorageProgram', () => {
      const program = webhookInboxHandler.receive({ correlation_key: 'test-correlation_key', event_type: 'test-event_type', payload: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = webhookInboxHandler.receive({ correlation_key: 'test-correlation_key', event_type: 'test-event_type', payload: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = webhookInboxHandler.receive({ correlation_key: 'test-correlation_key', event_type: 'test-event_type', payload: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = webhookInboxHandler.receive({ correlation_key: 'test-correlation_key', event_type: 'test-event_type', payload: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = webhookInboxHandler.receive({ correlation_key: 'test-correlation_key', event_type: 'test-event_type', payload: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof webhookInboxHandler.receive !== 'function') return;
      const result = await interpret(webhookInboxHandler.receive({ correlation_key: 'test-correlation_key', event_type: 'test-event_type', payload: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('expire', () => {
    it('builds a valid StorageProgram', () => {
      const program = webhookInboxHandler.expire({ hook: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = webhookInboxHandler.expire({ hook: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = webhookInboxHandler.expire({ hook: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = webhookInboxHandler.expire({ hook: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = webhookInboxHandler.expire({ hook: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof webhookInboxHandler.expire !== 'function') return;
      const result = await interpret(webhookInboxHandler.expire({ hook: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('ack', () => {
    it('builds a valid StorageProgram', () => {
      const program = webhookInboxHandler.ack({ hook: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = webhookInboxHandler.ack({ hook: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = webhookInboxHandler.ack({ hook: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = webhookInboxHandler.ack({ hook: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = webhookInboxHandler.ack({ hook: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof webhookInboxHandler.ack !== 'function') return;
      const result = await interpret(webhookInboxHandler.ack({ hook: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });


  describe('state invariants (stateful PBT)', () => {
    it('always: hooks have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ run_ref: fc.string({ minLength: 1, maxLength: 50 }), step_ref: fc.string({ minLength: 1, maxLength: 50 }), event_type: fc.string({ minLength: 1, maxLength: 50 }), correlation_key: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('receive'), input: fc.record({ correlation_key: fc.string({ minLength: 1, maxLength: 50 }), event_type: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string() }) }),
              fc.record({ action: fc.constant('expire'), input: fc.record({ hook: fc.string() }) }),
              fc.record({ action: fc.constant('ack'), input: fc.record({ hook: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = webhookInboxHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(webhookInboxHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in hooks', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ run_ref: fc.string({ minLength: 1, maxLength: 50 }), step_ref: fc.string({ minLength: 1, maxLength: 50 }), event_type: fc.string({ minLength: 1, maxLength: 50 }), correlation_key: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('receive'), input: fc.record({ correlation_key: fc.string({ minLength: 1, maxLength: 50 }), event_type: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string() }) }),
              fc.record({ action: fc.constant('expire'), input: fc.record({ hook: fc.string() }) }),
              fc.record({ action: fc.constant('ack'), input: fc.record({ hook: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = webhookInboxHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(webhookInboxHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in hooks
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof webhookInboxHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(webhookInboxHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof webhookInboxHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ run_ref: fc.string({ minLength: 1, maxLength: 50 }), step_ref: fc.string({ minLength: 1, maxLength: 50 }), event_type: fc.string({ minLength: 1, maxLength: 50 }), correlation_key: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = webhookInboxHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('receive handles empty input: ', async () => {
      if (typeof webhookInboxHandler.receive !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(webhookInboxHandler.receive({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('receive ensures on ok: ', async () => {
      if (typeof webhookInboxHandler.receive !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ correlation_key: fc.string({ minLength: 1, maxLength: 50 }), event_type: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = webhookInboxHandler.receive(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('expire ensures on ok: ', async () => {
      if (typeof webhookInboxHandler.expire !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ hook: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = webhookInboxHandler.expire(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
