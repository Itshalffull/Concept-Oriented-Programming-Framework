// PromptAssembly Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { promptAssemblyHandler } from '../../handlers/ts/llm-prompt/prompt-assembly.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('PromptAssembly functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptAssemblyHandler.create({ strategy: 'test-strategy', format: 'test-format', tokenizer_id: 'test-tokenizer_id', context_window: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptAssemblyHandler.create({ strategy: 'test-strategy', format: 'test-format', tokenizer_id: 'test-tokenizer_id', context_window: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptAssemblyHandler.create({ strategy: 'test-strategy', format: 'test-format', tokenizer_id: 'test-tokenizer_id', context_window: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptAssemblyHandler.create({ strategy: 'test-strategy', format: 'test-format', tokenizer_id: 'test-tokenizer_id', context_window: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptAssemblyHandler.create({ strategy: 'test-strategy', format: 'test-format', tokenizer_id: 'test-tokenizer_id', context_window: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptAssemblyHandler.create !== 'function') return;
      const result = await interpret(promptAssemblyHandler.create({ strategy: 'test-strategy', format: 'test-format', tokenizer_id: 'test-tokenizer_id', context_window: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('addSection', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptAssemblyHandler.addSection({ assembly: 'test', name: 'test-name', role: 'test-role', priority: 1, max_tokens: 1, required: true, content: 'test', template_ref: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptAssemblyHandler.addSection({ assembly: 'test', name: 'test-name', role: 'test-role', priority: 1, max_tokens: 1, required: true, content: 'test', template_ref: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptAssemblyHandler.addSection({ assembly: 'test', name: 'test-name', role: 'test-role', priority: 1, max_tokens: 1, required: true, content: 'test', template_ref: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptAssemblyHandler.addSection({ assembly: 'test', name: 'test-name', role: 'test-role', priority: 1, max_tokens: 1, required: true, content: 'test', template_ref: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptAssemblyHandler.addSection({ assembly: 'test', name: 'test-name', role: 'test-role', priority: 1, max_tokens: 1, required: true, content: 'test', template_ref: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptAssemblyHandler.addSection !== 'function') return;
      const result = await interpret(promptAssemblyHandler.addSection({ assembly: 'test', name: 'test-name', role: 'test-role', priority: 1, max_tokens: 1, required: true, content: 'test', template_ref: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('setVariable', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptAssemblyHandler.setVariable({ assembly: 'test', name: 'test-name', value: 'test-value' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptAssemblyHandler.setVariable({ assembly: 'test', name: 'test-name', value: 'test-value' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptAssemblyHandler.setVariable({ assembly: 'test', name: 'test-name', value: 'test-value' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptAssemblyHandler.setVariable({ assembly: 'test', name: 'test-name', value: 'test-value' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptAssemblyHandler.setVariable({ assembly: 'test', name: 'test-name', value: 'test-value' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptAssemblyHandler.setVariable !== 'function') return;
      const result = await interpret(promptAssemblyHandler.setVariable({ assembly: 'test', name: 'test-name', value: 'test-value' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('assemble', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptAssemblyHandler.assemble({ assembly: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptAssemblyHandler.assemble({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptAssemblyHandler.assemble({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptAssemblyHandler.assemble({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptAssemblyHandler.assemble({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptAssemblyHandler.assemble !== 'function') return;
      const result = await interpret(promptAssemblyHandler.assemble({ assembly: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('toMessages', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptAssemblyHandler.toMessages({ assembly: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptAssemblyHandler.toMessages({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptAssemblyHandler.toMessages({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptAssemblyHandler.toMessages({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptAssemblyHandler.toMessages({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptAssemblyHandler.toMessages !== 'function') return;
      const result = await interpret(promptAssemblyHandler.toMessages({ assembly: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('estimateTokens', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptAssemblyHandler.estimateTokens({ assembly: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptAssemblyHandler.estimateTokens({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptAssemblyHandler.estimateTokens({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptAssemblyHandler.estimateTokens({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptAssemblyHandler.estimateTokens({ assembly: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptAssemblyHandler.estimateTokens !== 'function') return;
      const result = await interpret(promptAssemblyHandler.estimateTokens({ assembly: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('removeSection', () => {
    it('builds a valid StorageProgram', () => {
      const program = promptAssemblyHandler.removeSection({ assembly: 'test', name: 'test-name' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = promptAssemblyHandler.removeSection({ assembly: 'test', name: 'test-name' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = promptAssemblyHandler.removeSection({ assembly: 'test', name: 'test-name' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = promptAssemblyHandler.removeSection({ assembly: 'test', name: 'test-name' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = promptAssemblyHandler.removeSection({ assembly: 'test', name: 'test-name' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof promptAssemblyHandler.removeSection !== 'function') return;
      const result = await interpret(promptAssemblyHandler.removeSection({ assembly: 'test', name: 'test-name' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof promptAssemblyHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = promptAssemblyHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('PromptAssembly');
    });
  });

  describe('invariant examples', () => {
    it("create-then-assemble", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(promptAssemblyHandler.create({ strategy: "priority_weighted", format: "chat_messages", tokenizer_id: "cl100k_base", context_window: 128000 }), storage);
      expect(createResult0.variant).toBe("ok");
      let assembly = createResult0.output["assembly"];
      let p = assembly;
      const addSectionResult1 = await interpret(promptAssemblyHandler.addSection({ assembly: p, name: "system", role: "system", priority: 100, max_tokens: 500, required: true, content: "You are helpful.", template_ref: "test-_" }), storage);
      expect(addSectionResult1.variant).toBe("ok");
      assembly = addSectionResult1.output["assembly"];
      const thenResult0 = await interpret(promptAssemblyHandler.assemble({ assembly: p }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-template_ref', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ strategy: fc.string({ minLength: 1, maxLength: 50 }), format: fc.string({ minLength: 1, maxLength: 50 }), tokenizer_id: fc.string({ minLength: 1, maxLength: 50 }), context_window: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('addSection'), input: fc.record({ assembly: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), role: fc.string({ minLength: 1, maxLength: 50 }), priority: fc.integer({ min: 1, max: 1000 }), max_tokens: fc.integer({ min: 1, max: 1000 }), required: fc.boolean(), content: fc.string(), template_ref: fc.string() }) }),
              fc.record({ action: fc.constant('setVariable'), input: fc.record({ assembly: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }), value: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('assemble'), input: fc.record({ assembly: fc.string() }) }),
              fc.record({ action: fc.constant('toMessages'), input: fc.record({ assembly: fc.string() }) }),
              fc.record({ action: fc.constant('estimateTokens'), input: fc.record({ assembly: fc.string() }) }),
              fc.record({ action: fc.constant('removeSection'), input: fc.record({ assembly: fc.string(), name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = promptAssemblyHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(promptAssemblyHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof promptAssemblyHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(promptAssemblyHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof promptAssemblyHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ strategy: fc.string({ minLength: 1, maxLength: 50 }), format: fc.string({ minLength: 1, maxLength: 50 }), tokenizer_id: fc.string({ minLength: 1, maxLength: 50 }), context_window: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = promptAssemblyHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
