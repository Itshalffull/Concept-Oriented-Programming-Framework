// FieldDefinition Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { fieldDefinitionHandler } from '../../handlers/ts/app/field-definition.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('FieldDefinition functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldDefinitionHandler.create !== 'function') return;
      const result = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_ok" -> ok', async () => {
      if (typeof fieldDefinitionHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_text_ok" -> ok', async () => {
      if (typeof fieldDefinitionHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "summary", label: "Summary", fieldType: "text", cardinality: "single", typeConfig: "{}", required: "true", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "2" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invalid_field_type" -> invalid_type', async () => {
      if (typeof fieldDefinitionHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "bad_field", label: "Bad", fieldType: "unknown-type", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "10" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_type'));
    });

    it('fixture "duplicate_field" -> duplicate', async () => {
      if (typeof fieldDefinitionHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", fieldId: "due_date", label: "Due Date Again", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "99" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.create({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('update', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldDefinitionHandler.update({ schema: "article", fieldId: "due_date", label: "Deadline", description: "When this article is due", required: "true", unique: "false", validations: "[]", defaultValue: "", widget: "date-picker", formatter: "relative-date", sortOrder: "1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldDefinitionHandler.update({ schema: "article", fieldId: "due_date", label: "Deadline", description: "When this article is due", required: "true", unique: "false", validations: "[]", defaultValue: "", widget: "date-picker", formatter: "relative-date", sortOrder: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldDefinitionHandler.update({ schema: "article", fieldId: "due_date", label: "Deadline", description: "When this article is due", required: "true", unique: "false", validations: "[]", defaultValue: "", widget: "date-picker", formatter: "relative-date", sortOrder: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldDefinitionHandler.update({ schema: "article", fieldId: "due_date", label: "Deadline", description: "When this article is due", required: "true", unique: "false", validations: "[]", defaultValue: "", widget: "date-picker", formatter: "relative-date", sortOrder: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldDefinitionHandler.update({ schema: "article", fieldId: "due_date", label: "Deadline", description: "When this article is due", required: "true", unique: "false", validations: "[]", defaultValue: "", widget: "date-picker", formatter: "relative-date", sortOrder: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldDefinitionHandler.update !== 'function') return;
      const result = await interpret(fieldDefinitionHandler.update({ schema: "article", fieldId: "due_date", label: "Deadline", description: "When this article is due", required: "true", unique: "false", validations: "[]", defaultValue: "", widget: "date-picker", formatter: "relative-date", sortOrder: "1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "update_ok" -> ok', async () => {
      if (typeof fieldDefinitionHandler.update !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", fieldId: "due_date", label: "Deadline", description: "When this article is due", required: "true", unique: "false", validations: "[]", defaultValue: "", widget: "date-picker", formatter: "relative-date", sortOrder: "1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.update({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "update_missing" -> not_found', async () => {
      if (typeof fieldDefinitionHandler.update !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.update({ schema: "article", fieldId: "nonexistent", label: "X", description: "", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('changeType', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldDefinitionHandler.changeType({ schema: 'test-schema', fieldId: 'test-fieldId', newType: 'test-newType', newTypeConfig: 'test-newTypeConfig', migrationStrategy: 'test-migrationStrategy' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldDefinitionHandler.changeType({ schema: 'test-schema', fieldId: 'test-fieldId', newType: 'test-newType', newTypeConfig: 'test-newTypeConfig', migrationStrategy: 'test-migrationStrategy' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldDefinitionHandler.changeType({ schema: 'test-schema', fieldId: 'test-fieldId', newType: 'test-newType', newTypeConfig: 'test-newTypeConfig', migrationStrategy: 'test-migrationStrategy' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldDefinitionHandler.changeType({ schema: 'test-schema', fieldId: 'test-fieldId', newType: 'test-newType', newTypeConfig: 'test-newTypeConfig', migrationStrategy: 'test-migrationStrategy' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldDefinitionHandler.changeType({ schema: 'test-schema', fieldId: 'test-fieldId', newType: 'test-newType', newTypeConfig: 'test-newTypeConfig', migrationStrategy: 'test-migrationStrategy' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldDefinitionHandler.changeType !== 'function') return;
      const result = await interpret(fieldDefinitionHandler.changeType({ schema: 'test-schema', fieldId: 'test-fieldId', newType: 'test-newType', newTypeConfig: 'test-newTypeConfig', migrationStrategy: 'test-migrationStrategy' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "change_type_ok" -> data_loss_warning', async () => {
      if (typeof fieldDefinitionHandler.changeType !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", fieldId: "due_date", newType: "text", newTypeConfig: "{}", migrationStrategy: "coerce" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.changeType({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('data_loss_warning'));
    });

    it('fixture "change_type_missing" -> not_found', async () => {
      if (typeof fieldDefinitionHandler.changeType !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.changeType({ schema: "article", fieldId: "nonexistent", newType: "text", newTypeConfig: "{}", migrationStrategy: "coerce" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

    it('fixture "change_type_invalid" -> invalid_type', async () => {
      if (typeof fieldDefinitionHandler.changeType !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", fieldId: "due_date", newType: "unknown-type", newTypeConfig: "{}", migrationStrategy: "coerce" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.changeType({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_type'));
    });

  });

  describe('rename', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldDefinitionHandler.rename({ schema: "article", fieldId: "due_date", newLabel: "Publish Date" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldDefinitionHandler.rename({ schema: "article", fieldId: "due_date", newLabel: "Publish Date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldDefinitionHandler.rename({ schema: "article", fieldId: "due_date", newLabel: "Publish Date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldDefinitionHandler.rename({ schema: "article", fieldId: "due_date", newLabel: "Publish Date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldDefinitionHandler.rename({ schema: "article", fieldId: "due_date", newLabel: "Publish Date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldDefinitionHandler.rename !== 'function') return;
      const result = await interpret(fieldDefinitionHandler.rename({ schema: "article", fieldId: "due_date", newLabel: "Publish Date" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "rename_ok" -> ok', async () => {
      if (typeof fieldDefinitionHandler.rename !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", fieldId: "due_date", newLabel: "Publish Date" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.rename({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "rename_missing" -> not_found', async () => {
      if (typeof fieldDefinitionHandler.rename !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.rename({ schema: "article", fieldId: "nonexistent", newLabel: "X" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('remove', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldDefinitionHandler.remove({ schema: "article", fieldId: "due_date" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldDefinitionHandler.remove({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldDefinitionHandler.remove({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldDefinitionHandler.remove({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldDefinitionHandler.remove({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldDefinitionHandler.remove !== 'function') return;
      const result = await interpret(fieldDefinitionHandler.remove({ schema: "article", fieldId: "due_date" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_ok" -> ok', async () => {
      if (typeof fieldDefinitionHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", fieldId: "due_date" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.remove({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_missing" -> not_found', async () => {
      if (typeof fieldDefinitionHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.remove({ schema: "article", fieldId: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('reorder', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldDefinitionHandler.reorder({ schema: "article", fieldId: "due_date", newSortOrder: "5" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldDefinitionHandler.reorder({ schema: "article", fieldId: "due_date", newSortOrder: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldDefinitionHandler.reorder({ schema: "article", fieldId: "due_date", newSortOrder: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldDefinitionHandler.reorder({ schema: "article", fieldId: "due_date", newSortOrder: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldDefinitionHandler.reorder({ schema: "article", fieldId: "due_date", newSortOrder: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldDefinitionHandler.reorder !== 'function') return;
      const result = await interpret(fieldDefinitionHandler.reorder({ schema: "article", fieldId: "due_date", newSortOrder: "5" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "reorder_ok" -> ok', async () => {
      if (typeof fieldDefinitionHandler.reorder !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", fieldId: "due_date", newSortOrder: "5" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.reorder({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reorder_missing" -> not_found', async () => {
      if (typeof fieldDefinitionHandler.reorder !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.reorder({ schema: "article", fieldId: "nonexistent", newSortOrder: "5" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldDefinitionHandler.get({ schema: "article", fieldId: "due_date" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldDefinitionHandler.get({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldDefinitionHandler.get({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldDefinitionHandler.get({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldDefinitionHandler.get({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldDefinitionHandler.get !== 'function') return;
      const result = await interpret(fieldDefinitionHandler.get({ schema: "article", fieldId: "due_date" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof fieldDefinitionHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", fieldId: "due_date" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> not_found', async () => {
      if (typeof fieldDefinitionHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.get({ schema: "article", fieldId: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldDefinitionHandler.list({ schema: "article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldDefinitionHandler.list({ schema: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldDefinitionHandler.list({ schema: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldDefinitionHandler.list({ schema: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldDefinitionHandler.list({ schema: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldDefinitionHandler.list !== 'function') return;
      const result = await interpret(fieldDefinitionHandler.list({ schema: "article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_ok" -> ok', async () => {
      if (typeof fieldDefinitionHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof fieldDefinitionHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "no-fields-schema" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('promote', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldDefinitionHandler.promote({ schema: "article", fieldId: "due_date" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldDefinitionHandler.promote({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldDefinitionHandler.promote({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldDefinitionHandler.promote({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldDefinitionHandler.promote({ schema: "article", fieldId: "due_date" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldDefinitionHandler.promote !== 'function') return;
      const result = await interpret(fieldDefinitionHandler.promote({ schema: "article", fieldId: "due_date" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "promote_ok" -> ok', async () => {
      if (typeof fieldDefinitionHandler.promote !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "due_date", label: "Due Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: "false", unique: "false", validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { schema: "article", fieldId: "due_date" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldDefinitionHandler.promote({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "promote_missing" -> not_found', async () => {
      if (typeof fieldDefinitionHandler.promote !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldDefinitionHandler.promote({ schema: "article", fieldId: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof fieldDefinitionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = fieldDefinitionHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('FieldDefinition');
    });
  });

  describe('invariant examples', () => {
    it("create then get", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "title", label: "Title", fieldType: "text", cardinality: "single", typeConfig: "{}", required: true, unique: false, validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: 1 }), storage);
      expect(createResult0.variant).toBe("ok");
      let id = createResult0.output["id"];
      let f = id;
      const thenResult0 = await interpret(fieldDefinitionHandler.get({ schema: "article", fieldId: "title" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("create then rename", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "pub_date", label: "Publish Date", fieldType: "date", cardinality: "single", typeConfig: "{}", required: false, unique: false, validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: 2 }), storage);
      expect(createResult0.variant).toBe("ok");
      let id = createResult0.output["id"];
      let f = id;
      const thenResult0 = await interpret(fieldDefinitionHandler.rename({ schema: "article", fieldId: "pub_date", newLabel: "Release Date" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(fieldDefinitionHandler.get({ schema: "article", fieldId: "pub_date" }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

    it("create then remove", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(fieldDefinitionHandler.create({ schema: "article", fieldId: "temp_field", label: "Temp", fieldType: "text", cardinality: "single", typeConfig: "{}", required: false, unique: false, validations: "[]", defaultValue: "", widget: "", formatter: "", sortOrder: 99 }), storage);
      expect(createResult0.variant).toBe("ok");
      let id = createResult0.output["id"];
      let f = id;
      const thenResult0 = await interpret(fieldDefinitionHandler.remove({ schema: "article", fieldId: "temp_field" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(fieldDefinitionHandler.get({ schema: "article", fieldId: "temp_field" }), storage);
      expect(thenResult1.variant).toBe("not_found");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: fieldId is immutable after creation', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), fieldType: fc.string({ minLength: 1, maxLength: 50 }), cardinality: fc.string({ minLength: 1, maxLength: 50 }), typeConfig: fc.string({ minLength: 1, maxLength: 50 }), required: fc.boolean(), unique: fc.boolean(), validations: fc.string({ minLength: 1, maxLength: 50 }), defaultValue: fc.string({ minLength: 1, maxLength: 50 }), widget: fc.string({ minLength: 1, maxLength: 50 }), formatter: fc.string({ minLength: 1, maxLength: 50 }), sortOrder: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('update'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }), required: fc.boolean(), unique: fc.boolean(), validations: fc.string({ minLength: 1, maxLength: 50 }), defaultValue: fc.string({ minLength: 1, maxLength: 50 }), widget: fc.string({ minLength: 1, maxLength: 50 }), formatter: fc.string({ minLength: 1, maxLength: 50 }), sortOrder: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('changeType'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), newType: fc.string({ minLength: 1, maxLength: 50 }), newTypeConfig: fc.string({ minLength: 1, maxLength: 50 }), migrationStrategy: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rename'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), newLabel: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reorder'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), newSortOrder: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('promote'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = fieldDefinitionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(fieldDefinitionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: fieldType is in valid type set', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), fieldType: fc.string({ minLength: 1, maxLength: 50 }), cardinality: fc.string({ minLength: 1, maxLength: 50 }), typeConfig: fc.string({ minLength: 1, maxLength: 50 }), required: fc.boolean(), unique: fc.boolean(), validations: fc.string({ minLength: 1, maxLength: 50 }), defaultValue: fc.string({ minLength: 1, maxLength: 50 }), widget: fc.string({ minLength: 1, maxLength: 50 }), formatter: fc.string({ minLength: 1, maxLength: 50 }), sortOrder: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('update'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }), required: fc.boolean(), unique: fc.boolean(), validations: fc.string({ minLength: 1, maxLength: 50 }), defaultValue: fc.string({ minLength: 1, maxLength: 50 }), widget: fc.string({ minLength: 1, maxLength: 50 }), formatter: fc.string({ minLength: 1, maxLength: 50 }), sortOrder: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('changeType'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), newType: fc.string({ minLength: 1, maxLength: 50 }), newTypeConfig: fc.string({ minLength: 1, maxLength: 50 }), migrationStrategy: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rename'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), newLabel: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reorder'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), newSortOrder: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('promote'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = fieldDefinitionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(fieldDefinitionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: missing schema reference', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), fieldType: fc.string({ minLength: 1, maxLength: 50 }), cardinality: fc.string({ minLength: 1, maxLength: 50 }), typeConfig: fc.string({ minLength: 1, maxLength: 50 }), required: fc.boolean(), unique: fc.boolean(), validations: fc.string({ minLength: 1, maxLength: 50 }), defaultValue: fc.string({ minLength: 1, maxLength: 50 }), widget: fc.string({ minLength: 1, maxLength: 50 }), formatter: fc.string({ minLength: 1, maxLength: 50 }), sortOrder: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('update'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }), required: fc.boolean(), unique: fc.boolean(), validations: fc.string({ minLength: 1, maxLength: 50 }), defaultValue: fc.string({ minLength: 1, maxLength: 50 }), widget: fc.string({ minLength: 1, maxLength: 50 }), formatter: fc.string({ minLength: 1, maxLength: 50 }), sortOrder: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('changeType'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), newType: fc.string({ minLength: 1, maxLength: 50 }), newTypeConfig: fc.string({ minLength: 1, maxLength: 50 }), migrationStrategy: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('rename'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), newLabel: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reorder'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }), newSortOrder: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('promote'), input: fc.record({ schema: fc.string({ minLength: 1, maxLength: 50 }), fieldId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = fieldDefinitionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(fieldDefinitionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: missing schema reference
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
