// SurfaceComponentScaffoldGen Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { surfaceComponentScaffoldGenHandler } from '../../handlers/ts/framework/surface-component-scaffold-gen.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('SurfaceComponentScaffoldGen functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('generate', () => {
    it('builds a valid StorageProgram', () => {
      const program = surfaceComponentScaffoldGenHandler.generate({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = surfaceComponentScaffoldGenHandler.generate({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = surfaceComponentScaffoldGenHandler.generate({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = surfaceComponentScaffoldGenHandler.generate({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = surfaceComponentScaffoldGenHandler.generate({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof surfaceComponentScaffoldGenHandler.generate !== 'function') return;
      try {
        const result = await interpret(surfaceComponentScaffoldGenHandler.generate({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('preview', () => {
    it('builds a valid StorageProgram', () => {
      const program = surfaceComponentScaffoldGenHandler.preview({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = surfaceComponentScaffoldGenHandler.preview({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = surfaceComponentScaffoldGenHandler.preview({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = surfaceComponentScaffoldGenHandler.preview({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = surfaceComponentScaffoldGenHandler.preview({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof surfaceComponentScaffoldGenHandler.preview !== 'function') return;
      try {
        const result = await interpret(surfaceComponentScaffoldGenHandler.preview({ name: 'test-name', parts: 'test', states: 'test', events: 'test', role: 'test', requires: 'test', affordance: 'test', props: 'test', compose: 'test' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = surfaceComponentScaffoldGenHandler.register({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = surfaceComponentScaffoldGenHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = surfaceComponentScaffoldGenHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = surfaceComponentScaffoldGenHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = surfaceComponentScaffoldGenHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof surfaceComponentScaffoldGenHandler.register !== 'function') return;
      try {
        const result = await interpret(surfaceComponentScaffoldGenHandler.register({  }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("generate produces widget scaffold", async () => {
      const storage = createInMemoryStorage();
      const generateResult0 = await interpret(surfaceComponentScaffoldGenHandler.generate({ name: {"type":"literal","value":"Button"}, parts: {"type":"list","items":[{"type":"literal","value":"root"},{"type":"literal","value":"label"}]}, states: {"type":"list","items":[{"type":"literal","value":"idle"},{"type":"literal","value":"pressed"}]}, events: {"type":"list","items":[{"type":"literal","value":"click"}]}, role: {"type":"literal","value":"button"}, requires: {"type":"literal","value":false}, affordance: {"type":"literal","value":false}, props: {"type":"list","items":[]}, compose: {"type":"list","items":[]} }), storage);
      expect(generateResult0.variant).toBe("ok");
      const files = generateResult0.output["files"];
      const filesGenerated = generateResult0.output["filesGenerated"];
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: components have names', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('generate'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), parts: fc.string(), states: fc.string(), events: fc.string(), role: fc.string(), requires: fc.string(), affordance: fc.string(), props: fc.string(), compose: fc.string() }) }),
              fc.record({ action: fc.constant('preview'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), parts: fc.string(), states: fc.string(), events: fc.string(), role: fc.string(), requires: fc.string(), affordance: fc.string(), props: fc.string(), compose: fc.string() }) }),
              fc.record({ action: fc.constant('register'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = surfaceComponentScaffoldGenHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(surfaceComponentScaffoldGenHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('generate handles empty input: ', async () => {
      if (typeof surfaceComponentScaffoldGenHandler.generate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(surfaceComponentScaffoldGenHandler.generate({  }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('generate ensures on ok: ', async () => {
      if (typeof surfaceComponentScaffoldGenHandler.generate !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), parts: fc.string(), states: fc.string(), events: fc.string(), role: fc.string(), requires: fc.string(), affordance: fc.string(), props: fc.string(), compose: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = surfaceComponentScaffoldGenHandler.generate(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
