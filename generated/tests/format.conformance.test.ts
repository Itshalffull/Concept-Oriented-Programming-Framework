// Format Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { formatHandler } from '../../handlers/ts/app/format.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Format functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = formatHandler.register({ provider: "prettier", language: "typescript", config: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formatHandler.register({ provider: "prettier", language: "typescript", config: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formatHandler.register({ provider: "prettier", language: "typescript", config: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formatHandler.register({ provider: "prettier", language: "typescript", config: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formatHandler.register({ provider: "prettier", language: "typescript", config: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formatHandler.register !== 'function') return;
      const result = await interpret(formatHandler.register({ provider: "prettier", language: "typescript", config: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_ok" -> ok', async () => {
      if (typeof formatHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formatHandler.register({ provider: "prettier", language: "typescript", config: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_markdown" -> ok', async () => {
      if (typeof formatHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formatHandler.register({ provider: "micromark-format", language: "markdown", config: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_json" -> ok', async () => {
      if (typeof formatHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formatHandler.register({ provider: "prettier", language: "json", config: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_provider" -> error', async () => {
      if (typeof formatHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formatHandler.register({ provider: "", language: "typescript", config: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_empty_language" -> error', async () => {
      if (typeof formatHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formatHandler.register({ provider: "prettier", language: "", config: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof formatHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(formatHandler.register({ provider: "prettier", language: "typescript", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { provider: "prettier-v3", language: "typescript", config: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formatHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('format', () => {
    it('builds a valid StorageProgram', () => {
      const program = formatHandler.format({ language: "typescript", text: "const x=1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formatHandler.format({ language: "typescript", text: "const x=1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formatHandler.format({ language: "typescript", text: "const x=1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formatHandler.format({ language: "typescript", text: "const x=1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formatHandler.format({ language: "typescript", text: "const x=1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formatHandler.format !== 'function') return;
      const result = await interpret(formatHandler.format({ language: "typescript", text: "const x=1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "format_ok" -> ok', async () => {
      if (typeof formatHandler.format !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(formatHandler.register({ provider: "prettier", language: "typescript", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { language: "typescript", text: "const x=1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formatHandler.format({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "format_markdown" -> ok', async () => {
      if (typeof formatHandler.format !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_markdown = await interpret(formatHandler.register({ provider: "micromark-format", language: "markdown", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_markdown?.output ?? {}));
      const _fixtureInput = { language: "markdown", text: "# Hello\n\nParagraph." } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(formatHandler.format({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "format_empty_language" -> error', async () => {
      if (typeof formatHandler.format !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formatHandler.format({ language: "", text: "const x=1" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "format_no_provider" -> no_provider', async () => {
      if (typeof formatHandler.format !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(formatHandler.format({ language: "rust", text: "fn main(){}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('no_provider'));
    });

  });

  describe('listLanguages', () => {
    it('builds a valid StorageProgram', () => {
      const program = formatHandler.listLanguages({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = formatHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = formatHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = formatHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = formatHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof formatHandler.listLanguages !== 'function') return;
      const result = await interpret(formatHandler.listLanguages({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof formatHandler.listLanguages !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(formatHandler.register({ provider: "prettier", language: "typescript", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(formatHandler.listLanguages({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_after_register" -> ok', async () => {
      if (typeof formatHandler.listLanguages !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(formatHandler.register({ provider: "prettier", language: "typescript", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(formatHandler.listLanguages({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then format returns patch", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(formatHandler.register({ provider: "prettier", language: "typescript", config: "" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let _ = id;
      const thenResult0 = await interpret(formatHandler.format({ language: "typescript", text: "const x=1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("format unknown language returns no_provider", async () => {
      const storage = createInMemoryStorage();
    });

    it("duplicate registration is rejected", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(formatHandler.register({ provider: "prettier", language: "typescript", config: "" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let _ = id;
      const thenResult0 = await interpret(formatHandler.register({ provider: "prettier-v3", language: "typescript", config: "" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

    it("register then listLanguages includes language", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(formatHandler.register({ provider: "prettier", language: "typescript", config: "" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let _ = id;
      const thenResult0 = await interpret(formatHandler.listLanguages({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every provider has a non-empty language', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('format'), input: fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listLanguages'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = formatHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(formatHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: every provider has a non-empty provider identifier', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('format'), input: fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listLanguages'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = formatHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(formatHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: two providers for the same language', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('format'), input: fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listLanguages'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = formatHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(formatHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: two providers for the same language
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof formatHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(formatHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof formatHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = formatHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('format handles empty input: ', async () => {
      if (typeof formatHandler.format !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(formatHandler.format({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('format ensures on ok: ', async () => {
      if (typeof formatHandler.format !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = formatHandler.format(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
