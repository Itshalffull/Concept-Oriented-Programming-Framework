// RuntimeCoverage Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { runtimeCoverageHandler } from '../../handlers/ts/score/runtime-coverage.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('RuntimeCoverage functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('record', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeCoverageHandler.record !== 'function') return;
      const result = await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "record_action" -> ok', async () => {
      if (typeof runtimeCoverageHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "record_variant" -> ok', async () => {
      if (typeof runtimeCoverageHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeCoverageHandler.record({ symbol: "clef/variant/Article/create/ok", kind: "variant", flowId: "flow-002" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "record_empty_symbol" -> error', async () => {
      if (typeof runtimeCoverageHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeCoverageHandler.record({ symbol: "", kind: "action", flowId: "flow-003" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('coverageReport', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeCoverageHandler.coverageReport({ kind: "action", since: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeCoverageHandler.coverageReport({ kind: "action", since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeCoverageHandler.coverageReport({ kind: "action", since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeCoverageHandler.coverageReport({ kind: "action", since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeCoverageHandler.coverageReport({ kind: "action", since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeCoverageHandler.coverageReport !== 'function') return;
      const result = await interpret(runtimeCoverageHandler.coverageReport({ kind: "action", since: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "coverage_actions" -> ok', async () => {
      if (typeof runtimeCoverageHandler.coverageReport !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.coverageReport({ kind: "action", since: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "coverage_variants_since" -> ok', async () => {
      if (typeof runtimeCoverageHandler.coverageReport !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.coverageReport({ kind: "variant", since: "2026-01-01T00:00:00Z" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "coverage_empty_kind" -> error', async () => {
      if (typeof runtimeCoverageHandler.coverageReport !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeCoverageHandler.coverageReport({ kind: "", since: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('variantCoverage', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeCoverageHandler.variantCoverage({ concept: "Article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeCoverageHandler.variantCoverage({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeCoverageHandler.variantCoverage({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeCoverageHandler.variantCoverage({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeCoverageHandler.variantCoverage({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeCoverageHandler.variantCoverage !== 'function') return;
      const result = await interpret(runtimeCoverageHandler.variantCoverage({ concept: "Article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "variant_coverage_article" -> ok', async () => {
      if (typeof runtimeCoverageHandler.variantCoverage !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.variantCoverage({ concept: "Article" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "variant_coverage_empty" -> error', async () => {
      if (typeof runtimeCoverageHandler.variantCoverage !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeCoverageHandler.variantCoverage({ concept: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('syncCoverage', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeCoverageHandler.syncCoverage({ since: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeCoverageHandler.syncCoverage({ since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeCoverageHandler.syncCoverage({ since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeCoverageHandler.syncCoverage({ since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeCoverageHandler.syncCoverage({ since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeCoverageHandler.syncCoverage !== 'function') return;
      const result = await interpret(runtimeCoverageHandler.syncCoverage({ since: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "sync_coverage_all" -> ok', async () => {
      if (typeof runtimeCoverageHandler.syncCoverage !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.syncCoverage({ since: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "sync_coverage_recent" -> ok', async () => {
      if (typeof runtimeCoverageHandler.syncCoverage !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.syncCoverage({ since: "2026-03-01T00:00:00Z" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('widgetStateCoverage', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeCoverageHandler.widgetStateCoverage({ widget: "dialog" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeCoverageHandler.widgetStateCoverage({ widget: "dialog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeCoverageHandler.widgetStateCoverage({ widget: "dialog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeCoverageHandler.widgetStateCoverage({ widget: "dialog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeCoverageHandler.widgetStateCoverage({ widget: "dialog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeCoverageHandler.widgetStateCoverage !== 'function') return;
      const result = await interpret(runtimeCoverageHandler.widgetStateCoverage({ widget: "dialog" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "widget_state_coverage_dialog" -> ok', async () => {
      if (typeof runtimeCoverageHandler.widgetStateCoverage !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.widgetStateCoverage({ widget: "dialog" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "widget_state_coverage_empty" -> error', async () => {
      if (typeof runtimeCoverageHandler.widgetStateCoverage !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeCoverageHandler.widgetStateCoverage({ widget: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('widgetLifecycleReport', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeCoverageHandler.widgetLifecycleReport({ widget: "dialog", since: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeCoverageHandler.widgetLifecycleReport({ widget: "dialog", since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeCoverageHandler.widgetLifecycleReport({ widget: "dialog", since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeCoverageHandler.widgetLifecycleReport({ widget: "dialog", since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeCoverageHandler.widgetLifecycleReport({ widget: "dialog", since: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeCoverageHandler.widgetLifecycleReport !== 'function') return;
      const result = await interpret(runtimeCoverageHandler.widgetLifecycleReport({ widget: "dialog", since: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "lifecycle_dialog" -> ok', async () => {
      if (typeof runtimeCoverageHandler.widgetLifecycleReport !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.widgetLifecycleReport({ widget: "dialog", since: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "lifecycle_since" -> ok', async () => {
      if (typeof runtimeCoverageHandler.widgetLifecycleReport !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.widgetLifecycleReport({ widget: "button", since: "2026-01-15T00:00:00Z" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "lifecycle_empty_widget" -> error', async () => {
      if (typeof runtimeCoverageHandler.widgetLifecycleReport !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeCoverageHandler.widgetLifecycleReport({ widget: "", since: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('widgetRenderTrace', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeCoverageHandler.widgetRenderTrace({ widgetInstance: "dialog-inst-001" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeCoverageHandler.widgetRenderTrace({ widgetInstance: "dialog-inst-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeCoverageHandler.widgetRenderTrace({ widgetInstance: "dialog-inst-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeCoverageHandler.widgetRenderTrace({ widgetInstance: "dialog-inst-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeCoverageHandler.widgetRenderTrace({ widgetInstance: "dialog-inst-001" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeCoverageHandler.widgetRenderTrace !== 'function') return;
      const result = await interpret(runtimeCoverageHandler.widgetRenderTrace({ widgetInstance: "dialog-inst-001" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "render_trace_valid" -> ok', async () => {
      if (typeof runtimeCoverageHandler.widgetRenderTrace !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.widgetRenderTrace({ widgetInstance: "dialog-inst-001" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "render_trace_empty" -> error', async () => {
      if (typeof runtimeCoverageHandler.widgetRenderTrace !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeCoverageHandler.widgetRenderTrace({ widgetInstance: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('widgetComparison', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeCoverageHandler.widgetComparison({ since: "", topN: "10" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeCoverageHandler.widgetComparison({ since: "", topN: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeCoverageHandler.widgetComparison({ since: "", topN: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeCoverageHandler.widgetComparison({ since: "", topN: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeCoverageHandler.widgetComparison({ since: "", topN: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeCoverageHandler.widgetComparison !== 'function') return;
      const result = await interpret(runtimeCoverageHandler.widgetComparison({ since: "", topN: "10" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "comparison_top10" -> ok', async () => {
      if (typeof runtimeCoverageHandler.widgetComparison !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.widgetComparison({ since: "", topN: "10" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "comparison_recent" -> ok', async () => {
      if (typeof runtimeCoverageHandler.widgetComparison !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.widgetComparison({ since: "2026-02-01T00:00:00Z", topN: "5" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('deadAtRuntime', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeCoverageHandler.deadAtRuntime({ kind: "variant" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeCoverageHandler.deadAtRuntime({ kind: "variant" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeCoverageHandler.deadAtRuntime({ kind: "variant" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeCoverageHandler.deadAtRuntime({ kind: "variant" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeCoverageHandler.deadAtRuntime({ kind: "variant" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeCoverageHandler.deadAtRuntime !== 'function') return;
      const result = await interpret(runtimeCoverageHandler.deadAtRuntime({ kind: "variant" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "dead_variants" -> ok', async () => {
      if (typeof runtimeCoverageHandler.deadAtRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.deadAtRuntime({ kind: "variant" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "dead_syncs" -> ok', async () => {
      if (typeof runtimeCoverageHandler.deadAtRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(runtimeCoverageHandler.record({ symbol: "clef/action/Article/create", kind: "action", flowId: "flow-001" }), storage);
      const result = await interpret(runtimeCoverageHandler.deadAtRuntime({ kind: "sync" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "dead_empty_kind" -> error', async () => {
      if (typeof runtimeCoverageHandler.deadAtRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeCoverageHandler.deadAtRuntime({ kind: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof runtimeCoverageHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = runtimeCoverageHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('RuntimeCoverage');
    });
  });

  describe('invariant examples', () => {
    it("recorded coverage is queryable", async () => {
      const storage = createInMemoryStorage();
      const recordResult0 = await interpret(runtimeCoverageHandler.record({ symbol: {"type":"literal","value":"clef/action/Article/create"}, kind: {"type":"literal","value":"action"}, flowId: {"type":"literal","value":"f-123"} }), storage);
      expect(recordResult0.variant).toBe("ok");
      let entry = recordResult0.output["entry"];
      const thenResult0 = await interpret(runtimeCoverageHandler.coverageReport({ kind: {"type":"literal","value":"action"}, since: {"type":"literal","value":""} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every entries entry has entitySymbol', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('record'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), flowId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('coverageReport'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('variantCoverage'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('syncCoverage'), input: fc.record({ since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('widgetStateCoverage'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('widgetLifecycleReport'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('widgetRenderTrace'), input: fc.record({ widgetInstance: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('widgetComparison'), input: fc.record({ since: fc.string({ minLength: 1, maxLength: 50 }), topN: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('deadAtRuntime'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = runtimeCoverageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(runtimeCoverageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty entitySymbol in entries', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('record'), input: fc.record({ symbol: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), flowId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('coverageReport'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('variantCoverage'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('syncCoverage'), input: fc.record({ since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('widgetStateCoverage'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('widgetLifecycleReport'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('widgetRenderTrace'), input: fc.record({ widgetInstance: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('widgetComparison'), input: fc.record({ since: fc.string({ minLength: 1, maxLength: 50 }), topN: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('deadAtRuntime'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = runtimeCoverageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(runtimeCoverageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty entitySymbol in entries
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('record handles empty input: ', async () => {
      if (typeof runtimeCoverageHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(runtimeCoverageHandler.record({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('record ensures on ok: ', async () => {
      if (typeof runtimeCoverageHandler.record !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ symbol: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), flowId: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = runtimeCoverageHandler.record(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
