// FieldFormatter Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { fieldFormatterHandler } from '../../handlers/ts/app/field-formatter.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('FieldFormatter functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldFormatterHandler.register !== 'function') return;
      const result = await interpret(fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_currency" -> ok', async () => {
      if (typeof fieldFormatterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_relative_date" -> ok', async () => {
      if (typeof fieldFormatterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldFormatterHandler.register({ formatterId: "relative-date", label: "Relative Date", description: "Render a date value as a human-readable relative string such as 'two days ago'.", acceptsTypes: ["DateTime","String"], optionsSchema: "{\"locale\":\"en\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_id" -> error', async () => {
      if (typeof fieldFormatterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldFormatterHandler.register({ formatterId: "", label: "Bad", description: ".", acceptsTypes: ["String"] }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_empty_types" -> error', async () => {
      if (typeof fieldFormatterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldFormatterHandler.register({ formatterId: "bad-formatter", label: "Bad", description: ".", acceptsTypes: [] }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldFormatterHandler.get({ formatterId: "plain" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldFormatterHandler.get({ formatterId: "plain" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldFormatterHandler.get({ formatterId: "plain" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldFormatterHandler.get({ formatterId: "plain" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldFormatterHandler.get({ formatterId: "plain" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldFormatterHandler.get !== 'function') return;
      const result = await interpret(fieldFormatterHandler.get({ formatterId: "plain" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_plain" -> ok', async () => {
      if (typeof fieldFormatterHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_currency = await interpret(fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_currency?.output ?? {}));
      const _fixtureInput = { formatterId: "plain" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldFormatterHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_currency" -> ok', async () => {
      if (typeof fieldFormatterHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_currency = await interpret(fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_currency?.output ?? {}));
      const _fixtureInput = { formatterId: "currency" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldFormatterHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof fieldFormatterHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldFormatterHandler.get({ formatterId: "nonexistent-formatter" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldFormatterHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldFormatterHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldFormatterHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldFormatterHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldFormatterHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldFormatterHandler.list !== 'function') return;
      const result = await interpret(fieldFormatterHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof fieldFormatterHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_currency = await interpret(fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_currency?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(fieldFormatterHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('listForType', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldFormatterHandler.listForType({ fieldType: "Number" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldFormatterHandler.listForType({ fieldType: "Number" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldFormatterHandler.listForType({ fieldType: "Number" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldFormatterHandler.listForType({ fieldType: "Number" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldFormatterHandler.listForType({ fieldType: "Number" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldFormatterHandler.listForType !== 'function') return;
      const result = await interpret(fieldFormatterHandler.listForType({ fieldType: "Number" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_for_number" -> ok', async () => {
      if (typeof fieldFormatterHandler.listForType !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_currency = await interpret(fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_currency?.output ?? {}));
      const _fixtureInput = { fieldType: "Number" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldFormatterHandler.listForType({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_for_empty" -> error', async () => {
      if (typeof fieldFormatterHandler.listForType !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldFormatterHandler.listForType({ fieldType: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('updateOptions', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldFormatterHandler.updateOptions({ formatterId: "currency", optionsSchema: "{\"currency\":\"EUR\",\"locale\":\"de-DE\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldFormatterHandler.updateOptions({ formatterId: "currency", optionsSchema: "{\"currency\":\"EUR\",\"locale\":\"de-DE\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldFormatterHandler.updateOptions({ formatterId: "currency", optionsSchema: "{\"currency\":\"EUR\",\"locale\":\"de-DE\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldFormatterHandler.updateOptions({ formatterId: "currency", optionsSchema: "{\"currency\":\"EUR\",\"locale\":\"de-DE\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldFormatterHandler.updateOptions({ formatterId: "currency", optionsSchema: "{\"currency\":\"EUR\",\"locale\":\"de-DE\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldFormatterHandler.updateOptions !== 'function') return;
      const result = await interpret(fieldFormatterHandler.updateOptions({ formatterId: "currency", optionsSchema: "{\"currency\":\"EUR\",\"locale\":\"de-DE\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "update_currency_options" -> ok', async () => {
      if (typeof fieldFormatterHandler.updateOptions !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_currency = await interpret(fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_currency?.output ?? {}));
      const _fixtureInput = { formatterId: "currency", optionsSchema: "{\"currency\":\"EUR\",\"locale\":\"de-DE\"}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldFormatterHandler.updateOptions({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "update_missing" -> notfound', async () => {
      if (typeof fieldFormatterHandler.updateOptions !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldFormatterHandler.updateOptions({ formatterId: "nonexistent", optionsSchema: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "update_bad_json" -> error', async () => {
      if (typeof fieldFormatterHandler.updateOptions !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_currency = await interpret(fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_currency?.output ?? {}));
      const _fixtureInput = { formatterId: "currency", optionsSchema: "not-json" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldFormatterHandler.updateOptions({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('unregister', () => {
    it('builds a valid StorageProgram', () => {
      const program = fieldFormatterHandler.unregister({ formatterId: "currency" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fieldFormatterHandler.unregister({ formatterId: "currency" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fieldFormatterHandler.unregister({ formatterId: "currency" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fieldFormatterHandler.unregister({ formatterId: "currency" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fieldFormatterHandler.unregister({ formatterId: "currency" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fieldFormatterHandler.unregister !== 'function') return;
      const result = await interpret(fieldFormatterHandler.unregister({ formatterId: "currency" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "unregister_custom" -> ok', async () => {
      if (typeof fieldFormatterHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_currency = await interpret(fieldFormatterHandler.register({ formatterId: "currency", label: "Currency", description: "Format a number as a currency string with locale-aware symbol.", acceptsTypes: ["Number","Float"], optionsSchema: "{\"currency\":\"USD\",\"locale\":\"en-US\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_register_currency?.output ?? {}));
      const _fixtureInput = { formatterId: "currency" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(fieldFormatterHandler.unregister({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unregister_missing" -> notfound', async () => {
      if (typeof fieldFormatterHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldFormatterHandler.unregister({ formatterId: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "unregister_builtin" -> invalid', async () => {
      if (typeof fieldFormatterHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(fieldFormatterHandler.unregister({ formatterId: "plain" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });


  describe('invariant examples', () => {
    it("register then list for type", async () => {
      const storage = createInMemoryStorage();
    });

    it("register then get then unregister", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(fieldFormatterHandler.register({ formatterId: "badge", label: "Badge", description: "Render a short string value inside a colored badge chip.", acceptsTypes: ["String"], optionsSchema: "{\"color\": \"blue\"}" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let formatter = registerResult0.output["formatter"];
      let f = formatter;
      const getResult1 = await interpret(fieldFormatterHandler.get({ formatterId: "badge" }), storage);
      expect(getResult1.variant).toBe("ok");
      let label = getResult1.output["label"];
      const thenResult0 = await interpret(fieldFormatterHandler.unregister({ formatterId: "badge" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: formatters have labels', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ formatterId: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }), acceptsTypes: fc.string(), optionsSchema: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ formatterId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('listForType'), input: fc.record({ fieldType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('updateOptions'), input: fc.record({ formatterId: fc.string({ minLength: 1, maxLength: 50 }), optionsSchema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ formatterId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = fieldFormatterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(fieldFormatterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: formatter without id', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ formatterId: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }), acceptsTypes: fc.string(), optionsSchema: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ formatterId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('listForType'), input: fc.record({ fieldType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('updateOptions'), input: fc.record({ formatterId: fc.string({ minLength: 1, maxLength: 50 }), optionsSchema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ formatterId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = fieldFormatterHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(fieldFormatterHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: formatter without id
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof fieldFormatterHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(fieldFormatterHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof fieldFormatterHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ formatterId: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }), acceptsTypes: fc.string(), optionsSchema: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = fieldFormatterHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
