// ArchitecturalFitness Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { architecturalFitnessHandler } from '../../handlers/ts/quality-analysis/architectural-fitness.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ArchitecturalFitness functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof architecturalFitnessHandler.register !== 'function') return;
      const result = await interpret(architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof architecturalFitnessHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "layering" -> ok', async () => {
      if (typeof architecturalFitnessHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(architecturalFitnessHandler.register({ functionId: "layer-check", name: "Layer Boundary Check", description: "Ensure UI does not import data layer", fitnessType: "layering", boundaryDefinition: {"layers":[{"name":"ui","allowedDependencies":["domain"]},{"name":"domain","allowedDependencies":["data"]},{"name":"data","allowedDependencies":[]}],"forbiddenPatterns":[{"from":"ui/**","to":"data/**","reason":"UI must not directly access data layer"}]} }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "duplicate_id" -> duplicate', async () => {
      if (typeof architecturalFitnessHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { functionId: "no-circular-deps", name: "Duplicate", description: "Should fail", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(architecturalFitnessHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

    it('fixture "invalid_type" -> error', async () => {
      if (typeof architecturalFitnessHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(architecturalFitnessHandler.register({ functionId: "bad", name: "Bad Type", description: "Unknown fitness type", fitnessType: "", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('verify', () => {
    it('builds a valid StorageProgram', () => {
      const program = architecturalFitnessHandler.verify({ functionId: "no-circular-deps" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = architecturalFitnessHandler.verify({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = architecturalFitnessHandler.verify({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = architecturalFitnessHandler.verify({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = architecturalFitnessHandler.verify({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof architecturalFitnessHandler.verify !== 'function') return;
      const result = await interpret(architecturalFitnessHandler.verify({ functionId: "no-circular-deps" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "verify_existing" -> ok', async () => {
      if (typeof architecturalFitnessHandler.verify !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { functionId: "no-circular-deps" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(architecturalFitnessHandler.verify({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "verify_unknown" -> unknownFunction', async () => {
      if (typeof architecturalFitnessHandler.verify !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(architecturalFitnessHandler.verify({ functionId: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unknownFunction'));
    });

  });

  describe('verifyAll', () => {
    it('builds a valid StorageProgram', () => {
      const program = architecturalFitnessHandler.verifyAll({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = architecturalFitnessHandler.verifyAll({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = architecturalFitnessHandler.verifyAll({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = architecturalFitnessHandler.verifyAll({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = architecturalFitnessHandler.verifyAll({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof architecturalFitnessHandler.verifyAll !== 'function') return;
      const result = await interpret(architecturalFitnessHandler.verifyAll({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "verify_all" -> ok', async () => {
      if (typeof architecturalFitnessHandler.verifyAll !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(architecturalFitnessHandler.verifyAll({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('enable', () => {
    it('builds a valid StorageProgram', () => {
      const program = architecturalFitnessHandler.enable({ functionId: "no-circular-deps" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = architecturalFitnessHandler.enable({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = architecturalFitnessHandler.enable({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = architecturalFitnessHandler.enable({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = architecturalFitnessHandler.enable({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof architecturalFitnessHandler.enable !== 'function') return;
      const result = await interpret(architecturalFitnessHandler.enable({ functionId: "no-circular-deps" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "enable_ok" -> ok', async () => {
      if (typeof architecturalFitnessHandler.enable !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { functionId: "no-circular-deps" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(architecturalFitnessHandler.enable({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('disable', () => {
    it('builds a valid StorageProgram', () => {
      const program = architecturalFitnessHandler.disable({ functionId: "no-circular-deps" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = architecturalFitnessHandler.disable({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = architecturalFitnessHandler.disable({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = architecturalFitnessHandler.disable({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = architecturalFitnessHandler.disable({ functionId: "no-circular-deps" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof architecturalFitnessHandler.disable !== 'function') return;
      const result = await interpret(architecturalFitnessHandler.disable({ functionId: "no-circular-deps" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "disable_ok" -> ok', async () => {
      if (typeof architecturalFitnessHandler.disable !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { functionId: "no-circular-deps" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(architecturalFitnessHandler.disable({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('trend', () => {
    it('builds a valid StorageProgram', () => {
      const program = architecturalFitnessHandler.trend({ functionId: "no-circular-deps", count: "10" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = architecturalFitnessHandler.trend({ functionId: "no-circular-deps", count: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = architecturalFitnessHandler.trend({ functionId: "no-circular-deps", count: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = architecturalFitnessHandler.trend({ functionId: "no-circular-deps", count: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = architecturalFitnessHandler.trend({ functionId: "no-circular-deps", count: "10" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof architecturalFitnessHandler.trend !== 'function') return;
      const result = await interpret(architecturalFitnessHandler.trend({ functionId: "no-circular-deps", count: "10" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "trend_ok" -> ok', async () => {
      if (typeof architecturalFitnessHandler.trend !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { functionId: "no-circular-deps", count: "10" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(architecturalFitnessHandler.trend({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "trend_unknown" -> unknownFunction', async () => {
      if (typeof architecturalFitnessHandler.trend !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(architecturalFitnessHandler.trend({ functionId: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unknownFunction'));
    });

  });


  describe('invariant examples', () => {
    it("register then verify", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(architecturalFitnessHandler.register({ functionId: "no-circular-deps", name: "No Circular Dependencies", description: "Detect circular module dependencies", fitnessType: "circularity", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      expect(registerResult0.variant).toBe("ok");
      let _function = registerResult0.output["function"];
      let a = _function;
      const thenResult0 = await interpret(architecturalFitnessHandler.verify({ functionId: "no-circular-deps" }), storage);
      expect(thenResult0.variant).toBe("passed");
    });

    it("disable then verify skips", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(architecturalFitnessHandler.register({ functionId: "skip-me", name: "Skippable", description: "Test disable", fitnessType: "naming", boundaryDefinition: {"layers":[],"forbiddenPatterns":[]} }), storage);
      expect(registerResult0.variant).toBe("ok");
      let _function = registerResult0.output["function"];
      let a = _function;
      const thenResult0 = await interpret(architecturalFitnessHandler.disable({ functionId: "skip-me" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(architecturalFitnessHandler.verify({ functionId: "skip-me" }), storage);
      expect(thenResult1.variant).toBe("disabled");
    });

  });

  describe('forall properties (PBT)', () => {
    it('forall: duplicate registration rejected', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.constantFrom("layer-check", "coupling-max"),
          async (fid) => {
            const storage = createInMemoryStorage();
            const result = await interpret(architecturalFitnessHandler.register({ functionId: fid, name: "Test", description: "Test", fitnessType: "structural", boundaryDefinition: {"type":"record","fields":[{"name":"layers","value":{"type":"list","items":[]}},{"name":"forbiddenPatterns","value":{"type":"list","items":[]}}]} }), storage);
            expect(result.variant).toBeDefined();
            expect(result.variant).toBe("ok");
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
