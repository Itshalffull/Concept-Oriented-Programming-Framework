// DevServer Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { devServerHandler } from '../../handlers/ts/dev-server.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('DevServer functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('start', () => {
    it('builds a valid StorageProgram', () => {
      const program = devServerHandler.start({ port: 1, watchDirs: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = devServerHandler.start({ port: 1, watchDirs: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = devServerHandler.start({ port: 1, watchDirs: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
      expect(variants).toContain('portInUse');
    });

    it('declares read and write sets', () => {
      const program = devServerHandler.start({ port: 1, watchDirs: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = devServerHandler.start({ port: 1, watchDirs: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof devServerHandler.start !== 'function') return;
      const result = await interpret(devServerHandler.start({ port: 1, watchDirs: 'test' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('stop', () => {
    it('builds a valid StorageProgram', () => {
      const program = devServerHandler.stop({ session: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = devServerHandler.stop({ session: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = devServerHandler.stop({ session: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
    });

    it('declares read and write sets', () => {
      const program = devServerHandler.stop({ session: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = devServerHandler.stop({ session: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof devServerHandler.stop !== 'function') return;
      const result = await interpret(devServerHandler.stop({ session: 'test' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('status', () => {
    it('builds a valid StorageProgram', () => {
      const program = devServerHandler.status({ session: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = devServerHandler.status({ session: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = devServerHandler.status({ session: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('running');
      expect(variants).toContain('stopped');
    });

    it('declares read and write sets', () => {
      const program = devServerHandler.status({ session: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = devServerHandler.status({ session: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof devServerHandler.status !== 'function') return;
      const result = await interpret(devServerHandler.status({ session: 'test' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invariant examples', () => {
    it("start then stop lifecycle", async () => {
      const storage = createInMemoryStorage();
      const startResult0 = await interpret(devServerHandler.start({ port: {"type":"literal","value":3000}, watchDirs: {"type":"list","items":[{"type":"literal","value":"./specs"},{"type":"literal","value":"./syncs"}]} }), storage);
      expect(startResult0.variant).toBe("ok");
      const session = startResult0.output["session"];
      const port = startResult0.output["port"];
      const url = startResult0.output["url"];
      const thenResult0 = await interpret(devServerHandler.stop({ session: {"type":"variable","name":"d"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("start then status shows running", async () => {
      const storage = createInMemoryStorage();
      const startResult0 = await interpret(devServerHandler.start({ port: {"type":"literal","value":3000}, watchDirs: {"type":"list","items":[{"type":"literal","value":"./specs"},{"type":"literal","value":"./syncs"}]} }), storage);
      expect(startResult0.variant).toBe("ok");
      const session = startResult0.output["session"];
      const port = startResult0.output["port"];
      const url = startResult0.output["url"];
      const thenResult0 = await interpret(devServerHandler.status({ session: {"type":"variable","name":"d"} }), storage);
      expect(thenResult0.variant).toBe("running");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: running sessions have a port', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ port: fc.integer({ min: 0, max: 1000 }), watchDirs: fc.string() }) }),
              fc.record({ action: fc.constant('stop'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('status'), input: fc.record({ session: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = devServerHandler[step.action];
              if (typeof actionFn === 'function') {
                const program = actionFn.call(devServerHandler, step.input as Record<string, unknown>);
                const result = await interpret(program, storage);
                expect(result.variant).toBeDefined();
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: session running on port zero', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('start'), input: fc.record({ port: fc.integer({ min: 0, max: 1000 }), watchDirs: fc.string() }) }),
              fc.record({ action: fc.constant('stop'), input: fc.record({ session: fc.string() }) }),
              fc.record({ action: fc.constant('status'), input: fc.record({ session: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = devServerHandler[step.action];
              if (typeof actionFn === 'function') {
                const program = actionFn.call(devServerHandler, step.input as Record<string, unknown>);
                const result = await interpret(program, storage);
                expect(result.variant).toBeDefined();
                // Never: session running on port zero
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('start requires: ', async () => {
      const storage = createInMemoryStorage();
      const result = await interpret(devServerHandler.start({  }), storage);
      expect(['error', 'invalid', 'missing', 'notFound']).toContain(result.variant);
    });

    it('start ensures on ok: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ port: fc.integer({ min: 1, max: 1000 }), watchDirs: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = devServerHandler.start(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            fc.pre(result.variant === "ok");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
