// CircuitBreaker Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { circuitBreakerHandler } from '../../handlers/ts/execution/circuit-breaker.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('CircuitBreaker imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('executes without crashing', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      try {
        const result = await circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "api_breaker" -> ok', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_endpoint" -> exists', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.configure({ endpoint: "", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }, storage);
      expect(result.variant).toBe('exists');
    });

  });

  describe('check', () => {
    it('executes without crashing', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      try {
        const result = await circuitBreakerHandler.check({ endpoint: "payments-api" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "healthy_endpoint" -> ok', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.check({ endpoint: "payments-api" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unconfigured_endpoint" -> notFound', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.check({ endpoint: "unknown-service" }, storage);
      expect(result.variant).toBe('notFound');
    });

  });

  describe('recordSuccess', () => {
    it('executes without crashing', async () => {
      if (typeof circuitBreakerHandler.recordSuccess !== 'function') return;
      try {
        const result = await circuitBreakerHandler.recordSuccess({ endpoint: "payments-api" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "success_recorded" -> ok', async () => {
      if (typeof circuitBreakerHandler.recordSuccess !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.recordSuccess({ endpoint: "payments-api" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "success_no_breaker" -> notFound', async () => {
      if (typeof circuitBreakerHandler.recordSuccess !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.recordSuccess({ endpoint: "unknown-service" }, storage);
      expect(result.variant).toBe('notFound');
    });

  });

  describe('recordFailure', () => {
    it('executes without crashing', async () => {
      if (typeof circuitBreakerHandler.recordFailure !== 'function') return;
      try {
        const result = await circuitBreakerHandler.recordFailure({ endpoint: "payments-api" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "failure_recorded" -> ok', async () => {
      if (typeof circuitBreakerHandler.recordFailure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.recordFailure({ endpoint: "payments-api" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "failure_no_breaker" -> notFound', async () => {
      if (typeof circuitBreakerHandler.recordFailure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.recordFailure({ endpoint: "unknown-service" }, storage);
      expect(result.variant).toBe('notFound');
    });

  });

  describe('reset', () => {
    it('executes without crashing', async () => {
      if (typeof circuitBreakerHandler.reset !== 'function') return;
      try {
        const result = await circuitBreakerHandler.reset({ endpoint: "payments-api" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "reset_breaker" -> ok', async () => {
      if (typeof circuitBreakerHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.reset({ endpoint: "payments-api" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reset_no_breaker" -> notFound', async () => {
      if (typeof circuitBreakerHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.reset({ endpoint: "unknown-service" }, storage);
      expect(result.variant).toBe('notFound');
    });

  });

  describe('get', () => {
    it('executes without crashing', async () => {
      if (typeof circuitBreakerHandler.get !== 'function') return;
      try {
        const result = await circuitBreakerHandler.get({ endpoint: "payments-api" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "get_status" -> ok', async () => {
      if (typeof circuitBreakerHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.get({ endpoint: "payments-api" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_no_breaker" -> notFound', async () => {
      if (typeof circuitBreakerHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.get({ endpoint: "unknown-service" }, storage);
      expect(result.variant).toBe('notFound');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof circuitBreakerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      let result: any;
      try {
        const r = circuitBreakerHandler.register({}, storage);
        result = r instanceof Promise ? await r : r;
        // If StorageProgram, interpret it
        if (result?.instructions && !result.variant) {
        }
      } catch { return; }
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('CircuitBreaker');
    });
  });

  describe('invariant examples', () => {
    it("newly configured breaker is closed", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await circuitBreakerHandler.configure({ endpoint: {"type":"literal","value":"openai-api"}, failureThreshold: {"type":"literal","value":5}, successThreshold: {"type":"literal","value":2}, resetTimeoutMs: {"type":"literal","value":30000} }, storage);
      expect(configureResult0.variant).toBe("ok");
      const breaker = configureResult0.output["breaker"];
      const thenResult0 = await circuitBreakerHandler.check({ endpoint: {"type":"literal","value":"openai-api"} }, storage);
      expect(thenResult0.variant).toBe("closed");
    });

    it("breaker trips after reaching failure threshold", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await circuitBreakerHandler.configure({ endpoint: {"type":"literal","value":"test-api"}, failureThreshold: {"type":"literal","value":2}, successThreshold: {"type":"literal","value":1}, resetTimeoutMs: {"type":"literal","value":1000} }, storage);
      expect(configureResult0.variant).toBe("ok");
      const breaker = configureResult0.output["breaker"];
      const recordFailureResult1 = await circuitBreakerHandler.recordFailure({ endpoint: {"type":"literal","value":"test-api"} }, storage);
      expect(recordFailureResult1.variant).toBe("ok");
      const breaker = recordFailureResult1.output["breaker"];
      const status = recordFailureResult1.output["status"];
      const recordFailureResult2 = await circuitBreakerHandler.recordFailure({ endpoint: {"type":"literal","value":"test-api"} }, storage);
      expect(recordFailureResult2.variant).toBe("tripped");
      const breaker = recordFailureResult2.output["breaker"];
      const failureCount = recordFailureResult2.output["failureCount"];
      const thenResult0 = await circuitBreakerHandler.get({ endpoint: {"type":"literal","value":"test-api"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: breakers have valid endpoint', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), failureThreshold: fc.integer({ min: 1, max: 1000 }), successThreshold: fc.integer({ min: 1, max: 1000 }), resetTimeoutMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordSuccess'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordFailure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = circuitBreakerHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(circuitBreakerHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: negative failure count', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), failureThreshold: fc.integer({ min: 1, max: 1000 }), successThreshold: fc.integer({ min: 1, max: 1000 }), resetTimeoutMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordSuccess'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordFailure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = circuitBreakerHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(circuitBreakerHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: negative failure count
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.configure({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('configure ensures on ok: ', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), failureThreshold: fc.integer({ min: 1, max: 1000 }), successThreshold: fc.integer({ min: 1, max: 1000 }), resetTimeoutMs: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await circuitBreakerHandler.configure(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('check handles empty input: ', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await circuitBreakerHandler.check({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('check ensures on closed: ', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await circuitBreakerHandler.check(input as Record<string, unknown>, storage);
            if (result.variant === "closed") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
