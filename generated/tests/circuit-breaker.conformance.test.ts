// CircuitBreaker Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { circuitBreakerHandler } from '../../handlers/ts/execution/circuit-breaker.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('CircuitBreaker functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      const result = await interpret(circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "api_breaker" -> ok', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_endpoint" -> exists', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(circuitBreakerHandler.configure({ endpoint: "", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('exists'));
    });

  });

  describe('check', () => {
    it('builds a valid StorageProgram', () => {
      const program = circuitBreakerHandler.check({ endpoint: "payments-api" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = circuitBreakerHandler.check({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = circuitBreakerHandler.check({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = circuitBreakerHandler.check({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = circuitBreakerHandler.check({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      const result = await interpret(circuitBreakerHandler.check({ endpoint: "payments-api" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "healthy_endpoint" -> ok', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_api_breaker = await interpret(circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_api_breaker?.output ?? {}));
      const _fixtureInput = { endpoint: "payments-api" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(circuitBreakerHandler.check({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unconfigured_endpoint" -> notFound', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(circuitBreakerHandler.check({ endpoint: "unknown-service" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('recordSuccess', () => {
    it('builds a valid StorageProgram', () => {
      const program = circuitBreakerHandler.recordSuccess({ endpoint: "payments-api" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = circuitBreakerHandler.recordSuccess({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = circuitBreakerHandler.recordSuccess({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = circuitBreakerHandler.recordSuccess({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = circuitBreakerHandler.recordSuccess({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof circuitBreakerHandler.recordSuccess !== 'function') return;
      const result = await interpret(circuitBreakerHandler.recordSuccess({ endpoint: "payments-api" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "success_recorded" -> ok', async () => {
      if (typeof circuitBreakerHandler.recordSuccess !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_api_breaker = await interpret(circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_api_breaker?.output ?? {}));
      const _fixtureInput = { endpoint: "payments-api" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(circuitBreakerHandler.recordSuccess({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "success_no_breaker" -> notFound', async () => {
      if (typeof circuitBreakerHandler.recordSuccess !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(circuitBreakerHandler.recordSuccess({ endpoint: "unknown-service" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('recordFailure', () => {
    it('builds a valid StorageProgram', () => {
      const program = circuitBreakerHandler.recordFailure({ endpoint: "payments-api" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = circuitBreakerHandler.recordFailure({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = circuitBreakerHandler.recordFailure({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = circuitBreakerHandler.recordFailure({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = circuitBreakerHandler.recordFailure({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof circuitBreakerHandler.recordFailure !== 'function') return;
      const result = await interpret(circuitBreakerHandler.recordFailure({ endpoint: "payments-api" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "failure_recorded" -> ok', async () => {
      if (typeof circuitBreakerHandler.recordFailure !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_api_breaker = await interpret(circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_api_breaker?.output ?? {}));
      const _fixtureInput = { endpoint: "payments-api" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(circuitBreakerHandler.recordFailure({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "failure_no_breaker" -> notFound', async () => {
      if (typeof circuitBreakerHandler.recordFailure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(circuitBreakerHandler.recordFailure({ endpoint: "unknown-service" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('reset', () => {
    it('builds a valid StorageProgram', () => {
      const program = circuitBreakerHandler.reset({ endpoint: "payments-api" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = circuitBreakerHandler.reset({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = circuitBreakerHandler.reset({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = circuitBreakerHandler.reset({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = circuitBreakerHandler.reset({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof circuitBreakerHandler.reset !== 'function') return;
      const result = await interpret(circuitBreakerHandler.reset({ endpoint: "payments-api" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "reset_breaker" -> ok', async () => {
      if (typeof circuitBreakerHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_api_breaker = await interpret(circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_api_breaker?.output ?? {}));
      const _fixtureInput = { endpoint: "payments-api" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(circuitBreakerHandler.reset({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "reset_no_breaker" -> notFound', async () => {
      if (typeof circuitBreakerHandler.reset !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(circuitBreakerHandler.reset({ endpoint: "unknown-service" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = circuitBreakerHandler.get({ endpoint: "payments-api" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = circuitBreakerHandler.get({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = circuitBreakerHandler.get({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = circuitBreakerHandler.get({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = circuitBreakerHandler.get({ endpoint: "payments-api" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof circuitBreakerHandler.get !== 'function') return;
      const result = await interpret(circuitBreakerHandler.get({ endpoint: "payments-api" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_status" -> ok', async () => {
      if (typeof circuitBreakerHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_api_breaker = await interpret(circuitBreakerHandler.configure({ endpoint: "payments-api", failureThreshold: "5", successThreshold: "2", resetTimeoutMs: "30000" }), storage);
      const _pool = Object.assign({}, (afterResult_api_breaker?.output ?? {}));
      const _fixtureInput = { endpoint: "payments-api" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(circuitBreakerHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_no_breaker" -> notFound', async () => {
      if (typeof circuitBreakerHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(circuitBreakerHandler.get({ endpoint: "unknown-service" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof circuitBreakerHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = circuitBreakerHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('CircuitBreaker');
    });
  });

  describe('invariant examples', () => {
    it("newly configured breaker is closed", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(circuitBreakerHandler.configure({ endpoint: {"type":"literal","value":"openai-api"}, failureThreshold: {"type":"literal","value":5}, successThreshold: {"type":"literal","value":2}, resetTimeoutMs: {"type":"literal","value":30000} }), storage);
      expect(configureResult0.variant).toBe("ok");
      let breaker = configureResult0.output["breaker"];
      const thenResult0 = await interpret(circuitBreakerHandler.check({ endpoint: {"type":"literal","value":"openai-api"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("breaker trips after reaching failure threshold", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(circuitBreakerHandler.configure({ endpoint: {"type":"literal","value":"test-api"}, failureThreshold: {"type":"literal","value":2}, successThreshold: {"type":"literal","value":1}, resetTimeoutMs: {"type":"literal","value":1000} }), storage);
      expect(configureResult0.variant).toBe("ok");
      let breaker = configureResult0.output["breaker"];
      const recordFailureResult1 = await interpret(circuitBreakerHandler.recordFailure({ endpoint: {"type":"literal","value":"test-api"} }), storage);
      expect(recordFailureResult1.variant).toBe("ok");
      breaker = recordFailureResult1.output["breaker"];
      let status = recordFailureResult1.output["status"];
      const recordFailureResult2 = await interpret(circuitBreakerHandler.recordFailure({ endpoint: {"type":"literal","value":"test-api"} }), storage);
      expect(recordFailureResult2.variant).toBe("ok");
      breaker = recordFailureResult2.output["breaker"];
      let failureCount = recordFailureResult2.output["failureCount"];
      const thenResult0 = await interpret(circuitBreakerHandler.get({ endpoint: {"type":"literal","value":"test-api"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: breakers have valid endpoint', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), failureThreshold: fc.integer({ min: 1, max: 1000 }), successThreshold: fc.integer({ min: 1, max: 1000 }), resetTimeoutMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordSuccess'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordFailure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = circuitBreakerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(circuitBreakerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: negative failure count', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), failureThreshold: fc.integer({ min: 1, max: 1000 }), successThreshold: fc.integer({ min: 1, max: 1000 }), resetTimeoutMs: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordSuccess'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordFailure'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('reset'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = circuitBreakerHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(circuitBreakerHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: negative failure count
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(circuitBreakerHandler.configure({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('configure ensures on ok: ', async () => {
      if (typeof circuitBreakerHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }), failureThreshold: fc.integer({ min: 1, max: 1000 }), successThreshold: fc.integer({ min: 1, max: 1000 }), resetTimeoutMs: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = circuitBreakerHandler.configure(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('check handles empty input: ', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(circuitBreakerHandler.check({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('check ensures on closed: ', async () => {
      if (typeof circuitBreakerHandler.check !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ endpoint: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = circuitBreakerHandler.check(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "closed") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
