// EmbeddingCache Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { embeddingCacheHandler } from '../../handlers/ts/embedding-cache.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('EmbeddingCache functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('warm', () => {
    it('builds a valid StorageProgram', () => {
      const program = embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof embeddingCacheHandler.warm !== 'function') return;
      const result = await interpret(embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_path" -> ok', async () => {
      if (typeof embeddingCacheHandler.warm !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_path" -> error', async () => {
      if (typeof embeddingCacheHandler.warm !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(embeddingCacheHandler.warm({ path: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('lookup', () => {
    it('builds a valid StorageProgram', () => {
      const program = embeddingCacheHandler.lookup({ digest: "sha256:abc123def456" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = embeddingCacheHandler.lookup({ digest: "sha256:abc123def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = embeddingCacheHandler.lookup({ digest: "sha256:abc123def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = embeddingCacheHandler.lookup({ digest: "sha256:abc123def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = embeddingCacheHandler.lookup({ digest: "sha256:abc123def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof embeddingCacheHandler.lookup !== 'function') return;
      const result = await interpret(embeddingCacheHandler.lookup({ digest: "sha256:abc123def456" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "known_digest" -> ok', async () => {
      if (typeof embeddingCacheHandler.lookup !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_path = await interpret(embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_path?.output ?? {}));
      const _fixtureInput = { digest: "sha256:abc123def456" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(embeddingCacheHandler.lookup({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_digest" -> error', async () => {
      if (typeof embeddingCacheHandler.lookup !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(embeddingCacheHandler.lookup({ digest: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('put', () => {
    it('builds a valid StorageProgram', () => {
      const program = embeddingCacheHandler.put({ digest: "sha256:abc123", vector: "[0.1,0.2,0.3]", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "3", sourceKind: "concept", sourceKey: "UserProfile" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = embeddingCacheHandler.put({ digest: "sha256:abc123", vector: "[0.1,0.2,0.3]", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "3", sourceKind: "concept", sourceKey: "UserProfile" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = embeddingCacheHandler.put({ digest: "sha256:abc123", vector: "[0.1,0.2,0.3]", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "3", sourceKind: "concept", sourceKey: "UserProfile" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = embeddingCacheHandler.put({ digest: "sha256:abc123", vector: "[0.1,0.2,0.3]", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "3", sourceKind: "concept", sourceKey: "UserProfile" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = embeddingCacheHandler.put({ digest: "sha256:abc123", vector: "[0.1,0.2,0.3]", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "3", sourceKind: "concept", sourceKey: "UserProfile" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof embeddingCacheHandler.put !== 'function') return;
      const result = await interpret(embeddingCacheHandler.put({ digest: "sha256:abc123", vector: "[0.1,0.2,0.3]", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "3", sourceKind: "concept", sourceKey: "UserProfile" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "store_embedding" -> ok', async () => {
      if (typeof embeddingCacheHandler.put !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_path = await interpret(embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" }), storage);
      const result = await interpret(embeddingCacheHandler.put({ digest: "sha256:abc123", vector: "[0.1,0.2,0.3]", model: afterResult_valid_path?.output?.["loaded"], dimensions: "3", sourceKind: "concept", sourceKey: "UserProfile" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_vector" -> error', async () => {
      if (typeof embeddingCacheHandler.put !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(embeddingCacheHandler.put({ digest: "sha256:abc123", vector: "", model: "text-embedding-ada-002", dimensions: "3", sourceKind: "concept", sourceKey: "UserProfile" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('flush', () => {
    it('builds a valid StorageProgram', () => {
      const program = embeddingCacheHandler.flush({ path: "/var/cache/embeddings.json" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = embeddingCacheHandler.flush({ path: "/var/cache/embeddings.json" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = embeddingCacheHandler.flush({ path: "/var/cache/embeddings.json" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = embeddingCacheHandler.flush({ path: "/var/cache/embeddings.json" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = embeddingCacheHandler.flush({ path: "/var/cache/embeddings.json" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof embeddingCacheHandler.flush !== 'function') return;
      const result = await interpret(embeddingCacheHandler.flush({ path: "/var/cache/embeddings.json" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "flush_to_disk" -> ok', async () => {
      if (typeof embeddingCacheHandler.flush !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_path = await interpret(embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_path?.output ?? {}));
      const _fixtureInput = { path: "/var/cache/embeddings.json" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(embeddingCacheHandler.flush({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_flush_path" -> error', async () => {
      if (typeof embeddingCacheHandler.flush !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(embeddingCacheHandler.flush({ path: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('evict', () => {
    it('builds a valid StorageProgram', () => {
      const program = embeddingCacheHandler.evict({ digest: "sha256:def456" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = embeddingCacheHandler.evict({ digest: "sha256:def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = embeddingCacheHandler.evict({ digest: "sha256:def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = embeddingCacheHandler.evict({ digest: "sha256:def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = embeddingCacheHandler.evict({ digest: "sha256:def456" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof embeddingCacheHandler.evict !== 'function') return;
      const result = await interpret(embeddingCacheHandler.evict({ digest: "sha256:def456" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "evict_existing" -> ok', async () => {
      if (typeof embeddingCacheHandler.evict !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_path = await interpret(embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_path?.output ?? {}));
      const _fixtureInput = { digest: "sha256:def456" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(embeddingCacheHandler.evict({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "evict_empty" -> error', async () => {
      if (typeof embeddingCacheHandler.evict !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(embeddingCacheHandler.evict({ digest: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('stats', () => {
    it('builds a valid StorageProgram', () => {
      const program = embeddingCacheHandler.stats({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = embeddingCacheHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = embeddingCacheHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = embeddingCacheHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = embeddingCacheHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof embeddingCacheHandler.stats !== 'function') return;
      const result = await interpret(embeddingCacheHandler.stats({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof embeddingCacheHandler.stats !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_path = await interpret(embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_path?.output ?? {}));
      const _fixtureInput = {  } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(embeddingCacheHandler.stats({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('lookupWithConfig', () => {
    it('builds a valid StorageProgram', () => {
      const program = embeddingCacheHandler.lookupWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = embeddingCacheHandler.lookupWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = embeddingCacheHandler.lookupWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = embeddingCacheHandler.lookupWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = embeddingCacheHandler.lookupWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof embeddingCacheHandler.lookupWithConfig !== 'function') return;
      const result = await interpret(embeddingCacheHandler.lookupWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "config_lookup" -> ok', async () => {
      if (typeof embeddingCacheHandler.lookupWithConfig !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_path = await interpret(embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" }), storage);
      const result = await interpret(embeddingCacheHandler.lookupWithConfig({ digest: "sha256:abc123", model: afterResult_valid_path?.output?.["loaded"], dimensions: "1536" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_model" -> error', async () => {
      if (typeof embeddingCacheHandler.lookupWithConfig !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(embeddingCacheHandler.lookupWithConfig({ digest: "sha256:abc123", model: "", dimensions: "1536" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('putWithConfig', () => {
    it('builds a valid StorageProgram', () => {
      const program = embeddingCacheHandler.putWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536", vector: "[0.1,0.2]", sourceKind: "concept", sourceKey: "User" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = embeddingCacheHandler.putWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536", vector: "[0.1,0.2]", sourceKind: "concept", sourceKey: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = embeddingCacheHandler.putWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536", vector: "[0.1,0.2]", sourceKind: "concept", sourceKey: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = embeddingCacheHandler.putWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536", vector: "[0.1,0.2]", sourceKind: "concept", sourceKey: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = embeddingCacheHandler.putWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536", vector: "[0.1,0.2]", sourceKind: "concept", sourceKey: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof embeddingCacheHandler.putWithConfig !== 'function') return;
      const result = await interpret(embeddingCacheHandler.putWithConfig({ digest: "sha256:abc123", model: {"type":"ref","fixture":"valid_path","field":"loaded"}, dimensions: "1536", vector: "[0.1,0.2]", sourceKind: "concept", sourceKey: "User" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "store_with_config" -> ok', async () => {
      if (typeof embeddingCacheHandler.putWithConfig !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_path = await interpret(embeddingCacheHandler.warm({ path: "/var/cache/embeddings.json" }), storage);
      const result = await interpret(embeddingCacheHandler.putWithConfig({ digest: "sha256:abc123", model: afterResult_valid_path?.output?.["loaded"], dimensions: "1536", vector: "[0.1,0.2]", sourceKind: "concept", sourceKey: "User" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_digest" -> error', async () => {
      if (typeof embeddingCacheHandler.putWithConfig !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(embeddingCacheHandler.putWithConfig({ digest: "", model: "text-embedding-ada-002", dimensions: "1536", vector: "[0.1]", sourceKind: "concept", sourceKey: "User" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof embeddingCacheHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = embeddingCacheHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('EmbeddingCache');
    });
  });

  describe('invariant examples', () => {
    it("put-then-lookup-2", async () => {
      const storage = createInMemoryStorage();
      const putResult0 = await interpret(embeddingCacheHandler.put({ digest: {"type":"literal","value":"abc123"}, vector: {"type":"literal","value":"[0.1,0.2,0.3]"}, model: {"type":"literal","value":"openai-code"}, dimensions: {"type":"literal","value":3}, sourceKind: {"type":"literal","value":"concept"}, sourceKey: {"type":"literal","value":"User"} }), storage);
      expect(putResult0.variant).toBe("stored");
      let entry = putResult0.output["entry"];
      const thenResult0 = await interpret(embeddingCacheHandler.lookup({ digest: {"type":"literal","value":"abc123"} }), storage);
      expect(thenResult0.variant).toBe("hit");
    });

    it("put-then-put", async () => {
      const storage = createInMemoryStorage();
      const putResult0 = await interpret(embeddingCacheHandler.put({ digest: {"type":"literal","value":"abc123"}, vector: {"type":"literal","value":"[0.1,0.2]"}, model: {"type":"literal","value":"codeBERT"}, dimensions: {"type":"literal","value":2}, sourceKind: {"type":"literal","value":"sync"}, sourceKey: {"type":"literal","value":"AuthSync"} }), storage);
      expect(putResult0.variant).toBe("stored");
      let entry = putResult0.output["entry"];
      const thenResult0 = await interpret(embeddingCacheHandler.put({ digest: {"type":"literal","value":"abc123"}, vector: {"type":"literal","value":"[0.9,0.8]"}, model: {"type":"literal","value":"codeBERT"}, dimensions: {"type":"literal","value":2}, sourceKind: {"type":"literal","value":"sync"}, sourceKey: {"type":"literal","value":"AuthSync"} }), storage);
      expect(thenResult0.variant).toBe("alreadyExists");
    });

    it("put-then-lookup", async () => {
      const storage = createInMemoryStorage();
      const putResult0 = await interpret(embeddingCacheHandler.put({ digest: {"type":"literal","value":"def456"}, vector: {"type":"literal","value":"[0.5]"}, model: {"type":"literal","value":"codeBERT"}, dimensions: {"type":"literal","value":1}, sourceKind: {"type":"literal","value":"widget"}, sourceKey: {"type":"literal","value":"TaskCard"} }), storage);
      expect(putResult0.variant).toBe("stored");
      let entry = putResult0.output["entry"];
      const evictResult1 = await interpret(embeddingCacheHandler.evict({ digest: {"type":"literal","value":"def456"} }), storage);
      expect(evictResult1.variant).toBe("ok");
      const thenResult0 = await interpret(embeddingCacheHandler.lookup({ digest: {"type":"literal","value":"def456"} }), storage);
      expect(thenResult0.variant).toBe("miss");
    });

    it("putWithConfig-then-lookupWithConfig-2", async () => {
      const storage = createInMemoryStorage();
      const putWithConfigResult0 = await interpret(embeddingCacheHandler.putWithConfig({ digest: {"type":"literal","value":"abc123"}, model: {"type":"literal","value":"openai-code"}, dimensions: {"type":"literal","value":1536}, vector: {"type":"literal","value":"[0.1,0.2]"}, sourceKind: {"type":"literal","value":"concept"}, sourceKey: {"type":"literal","value":"User"} }), storage);
      expect(putWithConfigResult0.variant).toBe("stored");
      let entry = putWithConfigResult0.output["entry"];
      const thenResult0 = await interpret(embeddingCacheHandler.lookupWithConfig({ digest: {"type":"literal","value":"abc123"}, model: {"type":"literal","value":"openai-code"}, dimensions: {"type":"literal","value":1536} }), storage);
      expect(thenResult0.variant).toBe("hit");
    });

    it("putWithConfig-then-lookupWithConfig", async () => {
      const storage = createInMemoryStorage();
      const putWithConfigResult0 = await interpret(embeddingCacheHandler.putWithConfig({ digest: {"type":"literal","value":"abc123"}, model: {"type":"literal","value":"openai-code"}, dimensions: {"type":"literal","value":1536}, vector: {"type":"literal","value":"[0.1,0.2]"}, sourceKind: {"type":"literal","value":"concept"}, sourceKey: {"type":"literal","value":"User"} }), storage);
      expect(putWithConfigResult0.variant).toBe("stored");
      let entry = putWithConfigResult0.output["entry"];
      const thenResult0 = await interpret(embeddingCacheHandler.lookupWithConfig({ digest: {"type":"literal","value":"abc123"}, model: {"type":"literal","value":"codeBERT"}, dimensions: {"type":"literal","value":768} }), storage);
      expect(thenResult0.variant).toBe("miss");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-digest', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('warm'), input: fc.record({ path: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('lookup'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('put'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }), vector: fc.string({ minLength: 1, maxLength: 50 }), model: fc.string({ minLength: 1, maxLength: 50 }), dimensions: fc.integer({ min: 1, max: 1000 }), sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceKey: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('flush'), input: fc.record({ path: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('evict'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('stats'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('lookupWithConfig'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }), model: fc.string({ minLength: 1, maxLength: 50 }), dimensions: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('putWithConfig'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }), model: fc.string({ minLength: 1, maxLength: 50 }), dimensions: fc.integer({ min: 1, max: 1000 }), vector: fc.string({ minLength: 1, maxLength: 50 }), sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceKey: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = embeddingCacheHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(embeddingCacheHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-vector', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('warm'), input: fc.record({ path: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('lookup'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('put'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }), vector: fc.string({ minLength: 1, maxLength: 50 }), model: fc.string({ minLength: 1, maxLength: 50 }), dimensions: fc.integer({ min: 1, max: 1000 }), sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceKey: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('flush'), input: fc.record({ path: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('evict'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('stats'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('lookupWithConfig'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }), model: fc.string({ minLength: 1, maxLength: 50 }), dimensions: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('putWithConfig'), input: fc.record({ digest: fc.string({ minLength: 1, maxLength: 50 }), model: fc.string({ minLength: 1, maxLength: 50 }), dimensions: fc.integer({ min: 1, max: 1000 }), vector: fc.string({ minLength: 1, maxLength: 50 }), sourceKind: fc.string({ minLength: 1, maxLength: 50 }), sourceKey: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = embeddingCacheHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(embeddingCacheHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-vector
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('warm handles empty input: ', async () => {
      if (typeof embeddingCacheHandler.warm !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(embeddingCacheHandler.warm({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('warm ensures on ok: ', async () => {
      if (typeof embeddingCacheHandler.warm !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ path: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = embeddingCacheHandler.warm(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
