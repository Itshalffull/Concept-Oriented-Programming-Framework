// ParamExpr Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { paramExprHandler } from '../../handlers/ts/app/param-expr.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ParamExpr functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('literal', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramExprHandler.literal({ expr: "lit-hello", value: "hello", valueType: "String" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramExprHandler.literal({ expr: "lit-hello", value: "hello", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramExprHandler.literal({ expr: "lit-hello", value: "hello", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramExprHandler.literal({ expr: "lit-hello", value: "hello", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramExprHandler.literal({ expr: "lit-hello", value: "hello", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramExprHandler.literal !== 'function') return;
      const result = await interpret(paramExprHandler.literal({ expr: "lit-hello", value: "hello", valueType: "String" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "literal_string" -> ok', async () => {
      if (typeof paramExprHandler.literal !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_coalesce_ok?.output ?? {}), (afterResult_conditional_ok?.output ?? {}), (afterResult_computed_concat?.output ?? {}), (afterResult_contextRef_row_id?.output ?? {}));
      const _fixtureInput = { expr: "lit-hello", value: "hello", valueType: "String" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramExprHandler.literal({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "literal_int" -> ok', async () => {
      if (typeof paramExprHandler.literal !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_coalesce_ok?.output ?? {}), (afterResult_conditional_ok?.output ?? {}), (afterResult_computed_concat?.output ?? {}), (afterResult_contextRef_row_id?.output ?? {}));
      const _fixtureInput = { expr: "lit-42", value: "42", valueType: "Int" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramExprHandler.literal({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "literal_empty_value" -> invalid', async () => {
      if (typeof paramExprHandler.literal !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.literal({ expr: "lit-bad", value: "", valueType: "String" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "literal_empty_expr" -> invalid', async () => {
      if (typeof paramExprHandler.literal !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.literal({ expr: "", value: "x", valueType: "String" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('contextRef', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramExprHandler.contextRef !== 'function') return;
      const result = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "contextRef_row_id" -> ok', async () => {
      if (typeof paramExprHandler.contextRef !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_coalesce_ok?.output ?? {}), (afterResult_conditional_ok?.output ?? {}), (afterResult_computed_concat?.output ?? {}));
      const _fixtureInput = { expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramExprHandler.contextRef({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "contextRef_nested" -> ok', async () => {
      if (typeof paramExprHandler.contextRef !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_coalesce_ok?.output ?? {}), (afterResult_conditional_ok?.output ?? {}), (afterResult_computed_concat?.output ?? {}));
      const _fixtureInput = { expr: "ref-addr", source: "row", path: "[\"address\",\"city\"]", valueType: "String" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramExprHandler.contextRef({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "contextRef_page" -> ok', async () => {
      if (typeof paramExprHandler.contextRef !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_coalesce_ok?.output ?? {}), (afterResult_conditional_ok?.output ?? {}), (afterResult_computed_concat?.output ?? {}));
      const _fixtureInput = { expr: "ref-page-title", source: "page", path: "[\"title\"]", valueType: "String" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramExprHandler.contextRef({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "contextRef_invalid_source" -> invalid', async () => {
      if (typeof paramExprHandler.contextRef !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.contextRef({ expr: "ref-bad", source: "invalid", path: "[\"x\"]", valueType: "String" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "contextRef_empty_path" -> invalid', async () => {
      if (typeof paramExprHandler.contextRef !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.contextRef({ expr: "ref-nopath", source: "row", path: "[]", valueType: "String" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('computed', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramExprHandler.computed !== 'function') return;
      const result = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "computed_concat" -> ok', async () => {
      if (typeof paramExprHandler.computed !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_coalesce_ok?.output ?? {}), (afterResult_conditional_ok?.output ?? {}));
      const _fixtureInput = { expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramExprHandler.computed({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "computed_upper" -> ok', async () => {
      if (typeof paramExprHandler.computed !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_coalesce_ok?.output ?? {}), (afterResult_conditional_ok?.output ?? {}));
      const _fixtureInput = { expr: "comp-upper", operator: "toUpperCase", operands: "[\"lit-hello\"]", valueType: "String" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramExprHandler.computed({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "computed_empty_op" -> invalid', async () => {
      if (typeof paramExprHandler.computed !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.computed({ expr: "comp-bad", operator: "", operands: "[\"lit-hello\"]", valueType: "String" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "computed_bad_operands" -> invalid', async () => {
      if (typeof paramExprHandler.computed !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.computed({ expr: "comp-bad2", operator: "concat", operands: "not-json", valueType: "String" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('conditional', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramExprHandler.conditional !== 'function') return;
      const result = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "conditional_ok" -> ok', async () => {
      if (typeof paramExprHandler.conditional !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const _pool = Object.assign({}, (afterResult_coalesce_ok?.output ?? {}));
      const _fixtureInput = { expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(paramExprHandler.conditional({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "conditional_empty_pred" -> invalid', async () => {
      if (typeof paramExprHandler.conditional !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.conditional({ expr: "cond-bad", predicateExpr: "", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('coalesce', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramExprHandler.coalesce !== 'function') return;
      const result = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "coalesce_ok" -> ok', async () => {
      if (typeof paramExprHandler.coalesce !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "coalesce_bad_json" -> invalid', async () => {
      if (typeof paramExprHandler.coalesce !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.coalesce({ expr: "coal-bad", alternatives: "not-json", valueType: "String" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "coalesce_empty_array" -> invalid', async () => {
      if (typeof paramExprHandler.coalesce !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.coalesce({ expr: "coal-empty", alternatives: "[]", valueType: "String" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramExprHandler.resolve({ expr: {"type":"ref","fixture":"literal_string","field":"expr"}, context: "{}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramExprHandler.resolve({ expr: {"type":"ref","fixture":"literal_string","field":"expr"}, context: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramExprHandler.resolve({ expr: {"type":"ref","fixture":"literal_string","field":"expr"}, context: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramExprHandler.resolve({ expr: {"type":"ref","fixture":"literal_string","field":"expr"}, context: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramExprHandler.resolve({ expr: {"type":"ref","fixture":"literal_string","field":"expr"}, context: "{}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramExprHandler.resolve !== 'function') return;
      const result = await interpret(paramExprHandler.resolve({ expr: {"type":"ref","fixture":"literal_string","field":"expr"}, context: "{}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_literal" -> ok', async () => {
      if (typeof paramExprHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const afterResult_literal_string = await interpret(paramExprHandler.literal({ expr: "lit-hello", value: "hello", valueType: "String" }), storage);
      const result = await interpret(paramExprHandler.resolve({ expr: afterResult_literal_string?.output?.["expr"], context: "{}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_contextRef" -> ok', async () => {
      if (typeof paramExprHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const result = await interpret(paramExprHandler.resolve({ expr: afterResult_contextRef_row_id?.output?.["expr"], context: "{\"row\": {\"id\": \"entity-123\"}}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_missing" -> notfound', async () => {
      if (typeof paramExprHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.resolve({ expr: "nonexistent", context: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "resolve_unresolvable" -> unresolvable', async () => {
      if (typeof paramExprHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const result = await interpret(paramExprHandler.resolve({ expr: afterResult_contextRef_row_id?.output?.["expr"], context: "{\"row\": {}}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unresolvable'));
    });

  });

  describe('validate', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramExprHandler.validate({ expr: {"type":"ref","fixture":"contextRef_row_id","field":"expr"}, availableFields: "[\"row.id\"]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramExprHandler.validate({ expr: {"type":"ref","fixture":"contextRef_row_id","field":"expr"}, availableFields: "[\"row.id\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramExprHandler.validate({ expr: {"type":"ref","fixture":"contextRef_row_id","field":"expr"}, availableFields: "[\"row.id\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramExprHandler.validate({ expr: {"type":"ref","fixture":"contextRef_row_id","field":"expr"}, availableFields: "[\"row.id\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramExprHandler.validate({ expr: {"type":"ref","fixture":"contextRef_row_id","field":"expr"}, availableFields: "[\"row.id\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramExprHandler.validate !== 'function') return;
      const result = await interpret(paramExprHandler.validate({ expr: {"type":"ref","fixture":"contextRef_row_id","field":"expr"}, availableFields: "[\"row.id\"]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "validate_ok" -> ok', async () => {
      if (typeof paramExprHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const result = await interpret(paramExprHandler.validate({ expr: afterResult_contextRef_row_id?.output?.["expr"], availableFields: "[\"row.id\"]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_missing" -> invalid', async () => {
      if (typeof paramExprHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const result = await interpret(paramExprHandler.validate({ expr: afterResult_contextRef_row_id?.output?.["expr"], availableFields: "[]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "validate_notfound" -> notfound', async () => {
      if (typeof paramExprHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.validate({ expr: "nonexistent", availableFields: "[]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('extractDependencies', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramExprHandler.extractDependencies({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramExprHandler.extractDependencies({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramExprHandler.extractDependencies({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramExprHandler.extractDependencies({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramExprHandler.extractDependencies({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramExprHandler.extractDependencies !== 'function') return;
      const result = await interpret(paramExprHandler.extractDependencies({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "extract_literal" -> ok', async () => {
      if (typeof paramExprHandler.extractDependencies !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const afterResult_literal_string = await interpret(paramExprHandler.literal({ expr: "lit-hello", value: "hello", valueType: "String" }), storage);
      const result = await interpret(paramExprHandler.extractDependencies({ expr: afterResult_literal_string?.output?.["expr"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "extract_contextRef" -> ok', async () => {
      if (typeof paramExprHandler.extractDependencies !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const result = await interpret(paramExprHandler.extractDependencies({ expr: afterResult_contextRef_row_id?.output?.["expr"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "extract_missing" -> notfound', async () => {
      if (typeof paramExprHandler.extractDependencies !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.extractDependencies({ expr: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = paramExprHandler.get({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = paramExprHandler.get({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = paramExprHandler.get({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = paramExprHandler.get({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = paramExprHandler.get({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof paramExprHandler.get !== 'function') return;
      const result = await interpret(paramExprHandler.get({ expr: {"type":"ref","fixture":"literal_string","field":"expr"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_literal" -> ok', async () => {
      if (typeof paramExprHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const afterResult_literal_string = await interpret(paramExprHandler.literal({ expr: "lit-hello", value: "hello", valueType: "String" }), storage);
      const result = await interpret(paramExprHandler.get({ expr: afterResult_literal_string?.output?.["expr"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_contextRef" -> ok', async () => {
      if (typeof paramExprHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_coalesce_ok = await interpret(paramExprHandler.coalesce({ expr: "coal-1", alternatives: "[\"ref-row-id\",\"lit-hello\"]", valueType: "String" }), storage);
      const afterResult_conditional_ok = await interpret(paramExprHandler.conditional({ expr: "cond-1", predicateExpr: "lit-hello", thenExpr: "lit-hello", elseExpr: "lit-42", valueType: "String" }), storage);
      const afterResult_computed_concat = await interpret(paramExprHandler.computed({ expr: "comp-concat", operator: "concat", operands: "[\"lit-hello\",\"lit-42\"]", valueType: "String" }), storage);
      const afterResult_contextRef_row_id = await interpret(paramExprHandler.contextRef({ expr: "ref-row-id", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      const result = await interpret(paramExprHandler.get({ expr: afterResult_contextRef_row_id?.output?.["expr"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof paramExprHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(paramExprHandler.get({ expr: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof paramExprHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = paramExprHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ParamExpr');
    });
  });

  describe('invariant examples', () => {
    it("literal registered and retrieved", async () => {
      const storage = createInMemoryStorage();
      const literalResult0 = await interpret(paramExprHandler.literal({ expr: "test-e", value: "hello", valueType: "String" }), storage);
      expect(literalResult0.variant).toBe("ok");
      let expr = literalResult0.output["expr"];
      let e = expr;
      const thenResult0 = await interpret(paramExprHandler.get({ expr: e }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("contextRef registered and resolved", async () => {
      const storage = createInMemoryStorage();
      const contextRefResult0 = await interpret(paramExprHandler.contextRef({ expr: "test-e", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      expect(contextRefResult0.variant).toBe("ok");
      let expr = contextRefResult0.output["expr"];
      let e = expr;
      const thenResult0 = await interpret(paramExprHandler.resolve({ expr: e, context: "{\"row\": {\"id\": \"abc\"}}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("literal resolves without context", async () => {
      const storage = createInMemoryStorage();
      const literalResult0 = await interpret(paramExprHandler.literal({ expr: "test-e", value: "constant", valueType: "String" }), storage);
      expect(literalResult0.variant).toBe("ok");
      let expr = literalResult0.output["expr"];
      let e = expr;
      const thenResult0 = await interpret(paramExprHandler.resolve({ expr: e, context: "{}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("contextRef validate detects missing field", async () => {
      const storage = createInMemoryStorage();
      const contextRefResult0 = await interpret(paramExprHandler.contextRef({ expr: "test-e", source: "row", path: "[\"id\"]", valueType: "String" }), storage);
      expect(contextRefResult0.variant).toBe("ok");
      let expr = contextRefResult0.output["expr"];
      let e = expr;
      const thenResult0 = await interpret(paramExprHandler.validate({ expr: e, availableFields: "[]" }), storage);
      expect(thenResult0.variant).toBe("invalid");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: expressions have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('literal'), input: fc.record({ expr: fc.string(), value: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('contextRef'), input: fc.record({ expr: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), path: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('computed'), input: fc.record({ expr: fc.string(), operator: fc.string({ minLength: 1, maxLength: 50 }), operands: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('conditional'), input: fc.record({ expr: fc.string(), predicateExpr: fc.string({ minLength: 1, maxLength: 50 }), thenExpr: fc.string({ minLength: 1, maxLength: 50 }), elseExpr: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('coalesce'), input: fc.record({ expr: fc.string(), alternatives: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ expr: fc.string(), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ expr: fc.string(), availableFields: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('extractDependencies'), input: fc.record({ expr: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ expr: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paramExprHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paramExprHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: contextRef without source or path', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('literal'), input: fc.record({ expr: fc.string(), value: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('contextRef'), input: fc.record({ expr: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), path: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('computed'), input: fc.record({ expr: fc.string(), operator: fc.string({ minLength: 1, maxLength: 50 }), operands: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('conditional'), input: fc.record({ expr: fc.string(), predicateExpr: fc.string({ minLength: 1, maxLength: 50 }), thenExpr: fc.string({ minLength: 1, maxLength: 50 }), elseExpr: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('coalesce'), input: fc.record({ expr: fc.string(), alternatives: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ expr: fc.string(), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ expr: fc.string(), availableFields: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('extractDependencies'), input: fc.record({ expr: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ expr: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paramExprHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paramExprHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: contextRef without source or path
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: literal without value', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('literal'), input: fc.record({ expr: fc.string(), value: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('contextRef'), input: fc.record({ expr: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), path: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('computed'), input: fc.record({ expr: fc.string(), operator: fc.string({ minLength: 1, maxLength: 50 }), operands: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('conditional'), input: fc.record({ expr: fc.string(), predicateExpr: fc.string({ minLength: 1, maxLength: 50 }), thenExpr: fc.string({ minLength: 1, maxLength: 50 }), elseExpr: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('coalesce'), input: fc.record({ expr: fc.string(), alternatives: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ expr: fc.string(), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ expr: fc.string(), availableFields: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('extractDependencies'), input: fc.record({ expr: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ expr: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = paramExprHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(paramExprHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: literal without value
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('literal handles empty input: ', async () => {
      if (typeof paramExprHandler.literal !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(paramExprHandler.literal({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('literal ensures on ok: ', async () => {
      if (typeof paramExprHandler.literal !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ expr: fc.string(), value: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = paramExprHandler.literal(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('contextRef handles empty input: ', async () => {
      if (typeof paramExprHandler.contextRef !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(paramExprHandler.contextRef({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('contextRef ensures on ok: ', async () => {
      if (typeof paramExprHandler.contextRef !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ expr: fc.string(), source: fc.string({ minLength: 1, maxLength: 50 }), path: fc.string({ minLength: 1, maxLength: 50 }), valueType: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = paramExprHandler.contextRef(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
