// StakeThreshold Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { stakeThresholdHandler } from '../../handlers/ts/app/governance/stake-threshold.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('StakeThreshold functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('configure', () => {
    it('builds a valid StorageProgram', () => {
      const program = stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stakeThresholdHandler.configure !== 'function') return;
      const result = await interpret(stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "configure_eth_stake" -> ok', async () => {
      if (typeof stakeThresholdHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "configure_zero_stake" -> error', async () => {
      if (typeof stakeThresholdHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(stakeThresholdHandler.configure({ minimumStake: "0.0", token: "ETH" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('deposit', () => {
    it('builds a valid StorageProgram', () => {
      const program = stakeThresholdHandler.deposit({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "100.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stakeThresholdHandler.deposit({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "100.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stakeThresholdHandler.deposit({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "100.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stakeThresholdHandler.deposit({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "100.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stakeThresholdHandler.deposit({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "100.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stakeThresholdHandler.deposit !== 'function') return;
      const result = await interpret(stakeThresholdHandler.deposit({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "100.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "deposit_hundred" -> ok', async () => {
      if (typeof stakeThresholdHandler.deposit !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_eth_stake = await interpret(stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" }), storage);
      const result = await interpret(stakeThresholdHandler.deposit({ config: afterResult_configure_eth_stake?.output?.["id"], candidate: "alice", amount: "100.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "deposit_negative" -> error', async () => {
      if (typeof stakeThresholdHandler.deposit !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(stakeThresholdHandler.deposit({ config: "stake-cfg-1", candidate: "alice", amount: "-10.0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('check', () => {
    it('builds a valid StorageProgram', () => {
      const program = stakeThresholdHandler.check({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stakeThresholdHandler.check({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stakeThresholdHandler.check({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stakeThresholdHandler.check({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stakeThresholdHandler.check({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stakeThresholdHandler.check !== 'function') return;
      const result = await interpret(stakeThresholdHandler.check({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "check_qualified" -> ok', async () => {
      if (typeof stakeThresholdHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_eth_stake = await interpret(stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" }), storage);
      const result = await interpret(stakeThresholdHandler.check({ config: afterResult_configure_eth_stake?.output?.["id"], candidate: "alice" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_missing_config" -> error', async () => {
      if (typeof stakeThresholdHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(stakeThresholdHandler.check({ config: "nonexistent", candidate: "alice" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('slash', () => {
    it('builds a valid StorageProgram', () => {
      const program = stakeThresholdHandler.slash({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "50.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = stakeThresholdHandler.slash({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "50.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = stakeThresholdHandler.slash({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "50.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = stakeThresholdHandler.slash({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "50.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = stakeThresholdHandler.slash({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "50.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof stakeThresholdHandler.slash !== 'function') return;
      const result = await interpret(stakeThresholdHandler.slash({ config: {"type":"ref","fixture":"configure_eth_stake","field":"id"}, candidate: "alice", amount: "50.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "slash_partial" -> ok', async () => {
      if (typeof stakeThresholdHandler.slash !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_configure_eth_stake = await interpret(stakeThresholdHandler.configure({ minimumStake: "100.0", token: "ETH", lockPeriodDays: "30" }), storage);
      const result = await interpret(stakeThresholdHandler.slash({ config: afterResult_configure_eth_stake?.output?.["id"], candidate: "alice", amount: "50.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "slash_no_balance" -> error', async () => {
      if (typeof stakeThresholdHandler.slash !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(stakeThresholdHandler.slash({ config: "stake-cfg-1", candidate: "unknown-user", amount: "10.0" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof stakeThresholdHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = stakeThresholdHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('StakeThreshold');
    });
  });

  describe('invariant examples', () => {
    it("configure-then-checkEligibility", async () => {
      const storage = createInMemoryStorage();
      const configureResult0 = await interpret(stakeThresholdHandler.configure({ minimumStake: 100, slashOnViolation: true }), storage);
      expect(configureResult0.variant).toBe("ok");
      let config = configureResult0.output["config"];
      let st = config;
      const thenResult0 = await interpret(stakeThresholdHandler.deposit({ config: st, participant: "test-p", amount: 100 }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(stakeThresholdHandler.checkEligibility({ config: st, participant: "test-p" }), storage);
      expect(thenResult1.variant).toBe("eligible");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-slashOnViolation', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ minimumStake: fc.string(), token: fc.string({ minLength: 1, maxLength: 50 }), lockPeriodDays: fc.string() }) }),
              fc.record({ action: fc.constant('deposit'), input: fc.record({ config: fc.string(), candidate: fc.string({ minLength: 1, maxLength: 50 }), amount: fc.string() }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ config: fc.string(), candidate: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('slash'), input: fc.record({ config: fc.string(), candidate: fc.string({ minLength: 1, maxLength: 50 }), amount: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = stakeThresholdHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(stakeThresholdHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-minimumStake', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('configure'), input: fc.record({ minimumStake: fc.string(), token: fc.string({ minLength: 1, maxLength: 50 }), lockPeriodDays: fc.string() }) }),
              fc.record({ action: fc.constant('deposit'), input: fc.record({ config: fc.string(), candidate: fc.string({ minLength: 1, maxLength: 50 }), amount: fc.string() }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ config: fc.string(), candidate: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('slash'), input: fc.record({ config: fc.string(), candidate: fc.string({ minLength: 1, maxLength: 50 }), amount: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = stakeThresholdHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(stakeThresholdHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-minimumStake
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('configure handles empty input: ', async () => {
      if (typeof stakeThresholdHandler.configure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(stakeThresholdHandler.configure({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('configure ensures on ok: ', async () => {
      if (typeof stakeThresholdHandler.configure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ minimumStake: fc.string(), token: fc.string({ minLength: 1, maxLength: 50 }), lockPeriodDays: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = stakeThresholdHandler.configure(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
