// DataSource Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { dataSourceHandler } from '../../handlers/ts/app/data-source.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('DataSource functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = dataSourceHandler.register({ name: "blog_api", uri: "https://blog.example.com/api", credentials: "token:abc123" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dataSourceHandler.register({ name: "blog_api", uri: "https://blog.example.com/api", credentials: "token:abc123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dataSourceHandler.register({ name: "blog_api", uri: "https://blog.example.com/api", credentials: "token:abc123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dataSourceHandler.register({ name: "blog_api", uri: "https://blog.example.com/api", credentials: "token:abc123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dataSourceHandler.register({ name: "blog_api", uri: "https://blog.example.com/api", credentials: "token:abc123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dataSourceHandler.register !== 'function') return;
      const result = await interpret(dataSourceHandler.register({ name: "blog_api", uri: "https://blog.example.com/api", credentials: "token:abc123" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_api" -> ok', async () => {
      if (typeof dataSourceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.register({ name: "blog_api", uri: "https://blog.example.com/api", credentials: "token:abc123" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_db" -> ok', async () => {
      if (typeof dataSourceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.register({ name: "analytics_db", uri: "postgres://db.internal:5432/analytics", credentials: "user:pass" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate" -> exists', async () => {
      if (typeof dataSourceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.register({ name: "blog_api", uri: "https://blog.example.com/api", credentials: "token:abc123" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('exists'));
    });

  });

  describe('connect', () => {
    it('builds a valid StorageProgram', () => {
      const program = dataSourceHandler.connect({ sourceId: "src-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dataSourceHandler.connect({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dataSourceHandler.connect({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dataSourceHandler.connect({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dataSourceHandler.connect({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dataSourceHandler.connect !== 'function') return;
      const result = await interpret(dataSourceHandler.connect({ sourceId: "src-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "connect_existing" -> ok', async () => {
      if (typeof dataSourceHandler.connect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.connect({ sourceId: "src-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "connect_missing" -> notfound', async () => {
      if (typeof dataSourceHandler.connect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.connect({ sourceId: "src-missing" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('discover', () => {
    it('builds a valid StorageProgram', () => {
      const program = dataSourceHandler.discover({ sourceId: "src-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dataSourceHandler.discover({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dataSourceHandler.discover({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dataSourceHandler.discover({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dataSourceHandler.discover({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dataSourceHandler.discover !== 'function') return;
      const result = await interpret(dataSourceHandler.discover({ sourceId: "src-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "discover_existing" -> ok', async () => {
      if (typeof dataSourceHandler.discover !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.discover({ sourceId: "src-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "discover_missing" -> notfound', async () => {
      if (typeof dataSourceHandler.discover !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.discover({ sourceId: "src-missing" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('healthCheck', () => {
    it('builds a valid StorageProgram', () => {
      const program = dataSourceHandler.healthCheck({ sourceId: "src-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dataSourceHandler.healthCheck({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dataSourceHandler.healthCheck({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dataSourceHandler.healthCheck({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dataSourceHandler.healthCheck({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dataSourceHandler.healthCheck !== 'function') return;
      const result = await interpret(dataSourceHandler.healthCheck({ sourceId: "src-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "health_existing" -> ok', async () => {
      if (typeof dataSourceHandler.healthCheck !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.healthCheck({ sourceId: "src-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "health_missing" -> notfound', async () => {
      if (typeof dataSourceHandler.healthCheck !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.healthCheck({ sourceId: "src-missing" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('deactivate', () => {
    it('builds a valid StorageProgram', () => {
      const program = dataSourceHandler.deactivate({ sourceId: "src-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = dataSourceHandler.deactivate({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = dataSourceHandler.deactivate({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = dataSourceHandler.deactivate({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = dataSourceHandler.deactivate({ sourceId: "src-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof dataSourceHandler.deactivate !== 'function') return;
      const result = await interpret(dataSourceHandler.deactivate({ sourceId: "src-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "deactivate_existing" -> ok', async () => {
      if (typeof dataSourceHandler.deactivate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.deactivate({ sourceId: "src-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "deactivate_missing" -> notfound', async () => {
      if (typeof dataSourceHandler.deactivate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(dataSourceHandler.deactivate({ sourceId: "src-missing" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof dataSourceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = dataSourceHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('DataSource');
    });
  });

  describe('invariant examples', () => {
    it("register-then-discover", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(dataSourceHandler.register({ name: {"type":"literal","value":"blog_api"}, uri: {"type":"literal","value":"https://blog.example.com/api"}, credentials: {"type":"literal","value":"token:abc"} }), storage);
      expect(registerResult0.variant).toBe("ok");
      const sourceId = registerResult0.output["sourceId"];
      const thenResult0 = await interpret(dataSourceHandler.connect({ sourceId: {"type":"literal","value":"src-1"} }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(dataSourceHandler.discover({ sourceId: {"type":"literal","value":"src-1"} }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), uri: fc.string({ minLength: 1, maxLength: 50 }), credentials: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('connect'), input: fc.record({ sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('discover'), input: fc.record({ sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('healthCheck'), input: fc.record({ sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deactivate'), input: fc.record({ sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = dataSourceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(dataSourceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-uri', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), uri: fc.string({ minLength: 1, maxLength: 50 }), credentials: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('connect'), input: fc.record({ sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('discover'), input: fc.record({ sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('healthCheck'), input: fc.record({ sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deactivate'), input: fc.record({ sourceId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = dataSourceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(dataSourceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-uri
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof dataSourceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(dataSourceHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof dataSourceHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), uri: fc.string({ minLength: 1, maxLength: 50 }), credentials: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = dataSourceHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
