// ScoreIndex Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { scoreIndexHandler } from '../../handlers/ts/score/score-index.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ScoreIndex functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('upsertConcept', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertConcept !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_user" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertConcept({ name: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('upsertSync', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertSync({ name: "onUserCreate", annotation: "eager", file: "/specs/syncs.sync" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertSync({ name: "onUserCreate", annotation: "eager", file: "/specs/syncs.sync" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertSync({ name: "onUserCreate", annotation: "eager", file: "/specs/syncs.sync" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertSync({ name: "onUserCreate", annotation: "eager", file: "/specs/syncs.sync" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertSync({ name: "onUserCreate", annotation: "eager", file: "/specs/syncs.sync" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertSync !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertSync({ name: "onUserCreate", annotation: "eager", file: "/specs/syncs.sync" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_sync" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertSync !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { name: "onUserCreate", annotation: "eager", file: "/specs/syncs.sync" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.upsertSync({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_sync_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertSync !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertSync({ name: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('upsertSymbol', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertSymbol({ name: "handleCreate", kind: "function", file: "/src/handler.ts", line: "42" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertSymbol({ name: "handleCreate", kind: "function", file: "/src/handler.ts", line: "42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertSymbol({ name: "handleCreate", kind: "function", file: "/src/handler.ts", line: "42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertSymbol({ name: "handleCreate", kind: "function", file: "/src/handler.ts", line: "42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertSymbol({ name: "handleCreate", kind: "function", file: "/src/handler.ts", line: "42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertSymbol !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertSymbol({ name: "handleCreate", kind: "function", file: "/src/handler.ts", line: "42" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_fn" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertSymbol !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { name: "handleCreate", kind: "function", file: "/src/handler.ts", line: "42" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.upsertSymbol({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_sym_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertSymbol !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertSymbol({ name: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('upsertFile', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertFile({ path: "/src/handler.ts", language: "typescript", role: "source" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertFile({ path: "/src/handler.ts", language: "typescript", role: "source" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertFile({ path: "/src/handler.ts", language: "typescript", role: "source" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertFile({ path: "/src/handler.ts", language: "typescript", role: "source" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertFile({ path: "/src/handler.ts", language: "typescript", role: "source" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertFile !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertFile({ path: "/src/handler.ts", language: "typescript", role: "source" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_file" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertFile !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { path: "/src/handler.ts", language: "typescript", role: "source" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.upsertFile({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_file_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertFile !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertFile({ path: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('upsertHandler', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertHandler({ concept: "Flag", language: "typescript", file: "/handlers/ts/flag.handler.ts", lineCount: "80" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertHandler({ concept: "Flag", language: "typescript", file: "/handlers/ts/flag.handler.ts", lineCount: "80" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertHandler({ concept: "Flag", language: "typescript", file: "/handlers/ts/flag.handler.ts", lineCount: "80" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertHandler({ concept: "Flag", language: "typescript", file: "/handlers/ts/flag.handler.ts", lineCount: "80" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertHandler({ concept: "Flag", language: "typescript", file: "/handlers/ts/flag.handler.ts", lineCount: "80" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertHandler !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertHandler({ concept: "Flag", language: "typescript", file: "/handlers/ts/flag.handler.ts", lineCount: "80" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_handler" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertHandler !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { concept: "Flag", language: "typescript", file: "/handlers/ts/flag.handler.ts", lineCount: "80" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.upsertHandler({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_handler_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertHandler !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertHandler({ concept: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('upsertWidgetImpl', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertWidgetImpl({ widget: "dialog", framework: "react", file: "/generated/Dialog.tsx", component: "Dialog" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertWidgetImpl({ widget: "dialog", framework: "react", file: "/generated/Dialog.tsx", component: "Dialog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertWidgetImpl({ widget: "dialog", framework: "react", file: "/generated/Dialog.tsx", component: "Dialog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertWidgetImpl({ widget: "dialog", framework: "react", file: "/generated/Dialog.tsx", component: "Dialog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertWidgetImpl({ widget: "dialog", framework: "react", file: "/generated/Dialog.tsx", component: "Dialog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertWidgetImpl !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertWidgetImpl({ widget: "dialog", framework: "react", file: "/generated/Dialog.tsx", component: "Dialog" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_widget" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertWidgetImpl !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { widget: "dialog", framework: "react", file: "/generated/Dialog.tsx", component: "Dialog" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.upsertWidgetImpl({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_widget_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertWidgetImpl !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertWidgetImpl({ widget: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('upsertThemeImpl', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertThemeImpl({ theme: "light", platform: "css", file: "/generated/light.css", tokenCount: "42" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertThemeImpl({ theme: "light", platform: "css", file: "/generated/light.css", tokenCount: "42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertThemeImpl({ theme: "light", platform: "css", file: "/generated/light.css", tokenCount: "42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertThemeImpl({ theme: "light", platform: "css", file: "/generated/light.css", tokenCount: "42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertThemeImpl({ theme: "light", platform: "css", file: "/generated/light.css", tokenCount: "42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertThemeImpl !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertThemeImpl({ theme: "light", platform: "css", file: "/generated/light.css", tokenCount: "42" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_theme" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertThemeImpl !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { theme: "light", platform: "css", file: "/generated/light.css", tokenCount: "42" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.upsertThemeImpl({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_theme_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertThemeImpl !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertThemeImpl({ theme: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('upsertDeployment', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertDeployment({ name: "conduit-prod", app: "conduit", file: "/deploy/prod.yaml" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertDeployment({ name: "conduit-prod", app: "conduit", file: "/deploy/prod.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertDeployment({ name: "conduit-prod", app: "conduit", file: "/deploy/prod.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertDeployment({ name: "conduit-prod", app: "conduit", file: "/deploy/prod.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertDeployment({ name: "conduit-prod", app: "conduit", file: "/deploy/prod.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertDeployment !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertDeployment({ name: "conduit-prod", app: "conduit", file: "/deploy/prod.yaml" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_deploy" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertDeployment !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { name: "conduit-prod", app: "conduit", file: "/deploy/prod.yaml" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.upsertDeployment({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_deploy_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertDeployment !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertDeployment({ name: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('upsertSuiteManifest', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertSuiteManifest({ name: "identity", version: "1.0.0", file: "/suites/identity/suite.yaml" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertSuiteManifest({ name: "identity", version: "1.0.0", file: "/suites/identity/suite.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertSuiteManifest({ name: "identity", version: "1.0.0", file: "/suites/identity/suite.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertSuiteManifest({ name: "identity", version: "1.0.0", file: "/suites/identity/suite.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertSuiteManifest({ name: "identity", version: "1.0.0", file: "/suites/identity/suite.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertSuiteManifest !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertSuiteManifest({ name: "identity", version: "1.0.0", file: "/suites/identity/suite.yaml" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_suite" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertSuiteManifest !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { name: "identity", version: "1.0.0", file: "/suites/identity/suite.yaml" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.upsertSuiteManifest({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_suite_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertSuiteManifest !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertSuiteManifest({ name: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('upsertInterface', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.upsertInterface({ name: "conduit-api", endpointCount: "12", file: "/interfaces/api.yaml" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.upsertInterface({ name: "conduit-api", endpointCount: "12", file: "/interfaces/api.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.upsertInterface({ name: "conduit-api", endpointCount: "12", file: "/interfaces/api.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.upsertInterface({ name: "conduit-api", endpointCount: "12", file: "/interfaces/api.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.upsertInterface({ name: "conduit-api", endpointCount: "12", file: "/interfaces/api.yaml" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.upsertInterface !== 'function') return;
      const result = await interpret(scoreIndexHandler.upsertInterface({ name: "conduit-api", endpointCount: "12", file: "/interfaces/api.yaml" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "upsert_iface" -> ok', async () => {
      if (typeof scoreIndexHandler.upsertInterface !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { name: "conduit-api", endpointCount: "12", file: "/interfaces/api.yaml" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.upsertInterface({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "upsert_iface_empty" -> error', async () => {
      if (typeof scoreIndexHandler.upsertInterface !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(scoreIndexHandler.upsertInterface({ name: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('removeByFile', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.removeByFile({ path: "/src/handler.ts" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.removeByFile({ path: "/src/handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.removeByFile({ path: "/src/handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.removeByFile({ path: "/src/handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.removeByFile({ path: "/src/handler.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.removeByFile !== 'function') return;
      const result = await interpret(scoreIndexHandler.removeByFile({ path: "/src/handler.ts" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_file" -> ok', async () => {
      if (typeof scoreIndexHandler.removeByFile !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { path: "/src/handler.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.removeByFile({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_empty" -> error', async () => {
      if (typeof scoreIndexHandler.removeByFile !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { path: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(scoreIndexHandler.removeByFile({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('clear', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.clear({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.clear({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.clear({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.clear({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.clear({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.clear !== 'function') return;
      const result = await interpret(scoreIndexHandler.clear({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof scoreIndexHandler.clear !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(scoreIndexHandler.clear({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('stats', () => {
    it('builds a valid StorageProgram', () => {
      const program = scoreIndexHandler.stats({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = scoreIndexHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = scoreIndexHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = scoreIndexHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = scoreIndexHandler.stats({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof scoreIndexHandler.stats !== 'function') return;
      const result = await interpret(scoreIndexHandler.stats({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_2" -> ok', async () => {
      if (typeof scoreIndexHandler.stats !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_upsert_user = await interpret(scoreIndexHandler.upsertConcept({ name: "User", purpose: "Manage user accounts", file: "/specs/user.concept" }), storage);
      const _pool = Object.assign({}, (afterResult_upsert_user?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(scoreIndexHandler.stats({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof scoreIndexHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = scoreIndexHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ScoreIndex');
    });
  });

  describe('invariant examples', () => {
    it("operational principle 1", async () => {
      const storage = createInMemoryStorage();
      const upsertConceptResult0 = await interpret(scoreIndexHandler.upsertConcept({ name: "Test", purpose: "A test concept", actions: "test-a", stateFields: "test-f", file: "/test.concept" }), storage);
      expect(upsertConceptResult0.variant).toBe("ok");
      let index = upsertConceptResult0.output["index"];
      let x = index;
      const thenResult0 = await interpret(scoreIndexHandler.stats({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every indexes entry has conceptName', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('upsertConcept'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), purpose: fc.string({ minLength: 1, maxLength: 50 }), actions: fc.string(), stateFields: fc.string(), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertSync'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), annotation: fc.string({ minLength: 1, maxLength: 50 }), triggers: fc.string(), effects: fc.string(), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertSymbol'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), file: fc.string({ minLength: 1, maxLength: 50 }), line: fc.integer({ min: 1, max: 1000 }), scope: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertFile'), input: fc.record({ path: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), role: fc.string({ minLength: 1, maxLength: 50 }), definitions: fc.string() }) }),
              fc.record({ action: fc.constant('upsertHandler'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), file: fc.string({ minLength: 1, maxLength: 50 }), actions: fc.string(), lineCount: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('upsertWidgetImpl'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }), framework: fc.string({ minLength: 1, maxLength: 50 }), file: fc.string({ minLength: 1, maxLength: 50 }), component: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertThemeImpl'), input: fc.record({ theme: fc.string({ minLength: 1, maxLength: 50 }), platform: fc.string({ minLength: 1, maxLength: 50 }), file: fc.string({ minLength: 1, maxLength: 50 }), tokenCount: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('upsertDeployment'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), app: fc.string({ minLength: 1, maxLength: 50 }), runtimes: fc.string(), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertSuiteManifest'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), version: fc.string({ minLength: 1, maxLength: 50 }), concepts: fc.string(), syncs: fc.string(), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertInterface'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), targets: fc.string(), endpointCount: fc.integer({ min: 1, max: 1000 }), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeByFile'), input: fc.record({ path: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clear'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('stats'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = scoreIndexHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(scoreIndexHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty conceptName in indexes', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('upsertConcept'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), purpose: fc.string({ minLength: 1, maxLength: 50 }), actions: fc.string(), stateFields: fc.string(), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertSync'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), annotation: fc.string({ minLength: 1, maxLength: 50 }), triggers: fc.string(), effects: fc.string(), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertSymbol'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), file: fc.string({ minLength: 1, maxLength: 50 }), line: fc.integer({ min: 1, max: 1000 }), scope: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertFile'), input: fc.record({ path: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), role: fc.string({ minLength: 1, maxLength: 50 }), definitions: fc.string() }) }),
              fc.record({ action: fc.constant('upsertHandler'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), file: fc.string({ minLength: 1, maxLength: 50 }), actions: fc.string(), lineCount: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('upsertWidgetImpl'), input: fc.record({ widget: fc.string({ minLength: 1, maxLength: 50 }), framework: fc.string({ minLength: 1, maxLength: 50 }), file: fc.string({ minLength: 1, maxLength: 50 }), component: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertThemeImpl'), input: fc.record({ theme: fc.string({ minLength: 1, maxLength: 50 }), platform: fc.string({ minLength: 1, maxLength: 50 }), file: fc.string({ minLength: 1, maxLength: 50 }), tokenCount: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('upsertDeployment'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), app: fc.string({ minLength: 1, maxLength: 50 }), runtimes: fc.string(), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertSuiteManifest'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), version: fc.string({ minLength: 1, maxLength: 50 }), concepts: fc.string(), syncs: fc.string(), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('upsertInterface'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), targets: fc.string(), endpointCount: fc.integer({ min: 1, max: 1000 }), file: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('removeByFile'), input: fc.record({ path: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('clear'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('stats'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = scoreIndexHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(scoreIndexHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty conceptName in indexes
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('upsertConcept handles empty input: ', async () => {
      if (typeof scoreIndexHandler.upsertConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(scoreIndexHandler.upsertConcept({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('upsertConcept ensures on ok: ', async () => {
      if (typeof scoreIndexHandler.upsertConcept !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), purpose: fc.string({ minLength: 1, maxLength: 50 }), actions: fc.string(), stateFields: fc.string(), file: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = scoreIndexHandler.upsertConcept(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
