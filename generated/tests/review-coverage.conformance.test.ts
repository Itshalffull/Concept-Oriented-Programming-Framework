// ReviewCoverage Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { reviewCoverageHandler } from '../../handlers/ts/quality-review/review-coverage.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ReviewCoverage functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('record', () => {
    it('builds a valid StorageProgram', () => {
      const program = reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof reviewCoverageHandler.record !== 'function') return;
      const result = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "record_ok" -> ok', async () => {
      if (typeof reviewCoverageHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "record_another" -> ok', async () => {
      if (typeof reviewCoverageHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(reviewCoverageHandler.record({ changeId: "pr-200", target: "api/users.ts", author: "bob", linesChanged: "45" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "record_duplicate" -> duplicate', async () => {
      if (typeof reviewCoverageHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_ok = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" }), storage);
      const _pool = Object.assign({}, (afterResult_record_ok?.output ?? {}));
      const _fixtureInput = { changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(reviewCoverageHandler.record({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('recordReview', () => {
    it('builds a valid StorageProgram', () => {
      const program = reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof reviewCoverageHandler.recordReview !== 'function') return;
      const result = await interpret(reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "review_ok" -> ok', async () => {
      if (typeof reviewCoverageHandler.recordReview !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_ok = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" }), storage);
      const _pool = Object.assign({}, (afterResult_record_ok?.output ?? {}));
      const _fixtureInput = { changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(reviewCoverageHandler.recordReview({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "review_missing" -> changeNotFound', async () => {
      if (typeof reviewCoverageHandler.recordReview !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(reviewCoverageHandler.recordReview({ changeId: "pr-999", reviewer: "carol", commentCount: "1", substantiveComments: "0", defectsFound: "0", suggestionsAccepted: "0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('changeNotFound'));
    });

  });

  describe('complete', () => {
    it('builds a valid StorageProgram', () => {
      const program = reviewCoverageHandler.complete({ changeId: "pr-142" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = reviewCoverageHandler.complete({ changeId: "pr-142" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = reviewCoverageHandler.complete({ changeId: "pr-142" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = reviewCoverageHandler.complete({ changeId: "pr-142" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = reviewCoverageHandler.complete({ changeId: "pr-142" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof reviewCoverageHandler.complete !== 'function') return;
      const result = await interpret(reviewCoverageHandler.complete({ changeId: "pr-142" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "complete_ok" -> ok', async () => {
      if (typeof reviewCoverageHandler.complete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_ok = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" }), storage);
      const afterResult_review_ok = await interpret(reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_record_ok?.output ?? {}), (afterResult_review_ok?.output ?? {}));
      const _fixtureInput = { changeId: "pr-142" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(reviewCoverageHandler.complete({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "complete_missing" -> changeNotFound', async () => {
      if (typeof reviewCoverageHandler.complete !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(reviewCoverageHandler.complete({ changeId: "pr-999" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('changeNotFound'));
    });

  });

  describe('metrics', () => {
    it('builds a valid StorageProgram', () => {
      const program = reviewCoverageHandler.metrics({ period: null, targets: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = reviewCoverageHandler.metrics({ period: null, targets: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = reviewCoverageHandler.metrics({ period: null, targets: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = reviewCoverageHandler.metrics({ period: null, targets: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = reviewCoverageHandler.metrics({ period: null, targets: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof reviewCoverageHandler.metrics !== 'function') return;
      const result = await interpret(reviewCoverageHandler.metrics({ period: null, targets: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "metrics_ok" -> ok', async () => {
      if (typeof reviewCoverageHandler.metrics !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_ok = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" }), storage);
      const afterResult_review_ok = await interpret(reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" }), storage);
      const afterResult_complete_ok = await interpret(reviewCoverageHandler.complete({ changeId: "pr-142" }), storage);
      const _pool = Object.assign({}, (afterResult_record_ok?.output ?? {}), (afterResult_review_ok?.output ?? {}), (afterResult_complete_ok?.output ?? {}));
      const _fixtureInput = { period: null, targets: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(reviewCoverageHandler.metrics({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "metrics_filtered" -> ok', async () => {
      if (typeof reviewCoverageHandler.metrics !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_ok = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" }), storage);
      const afterResult_review_ok = await interpret(reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" }), storage);
      const afterResult_complete_ok = await interpret(reviewCoverageHandler.complete({ changeId: "pr-142" }), storage);
      const _pool = Object.assign({}, (afterResult_record_ok?.output ?? {}), (afterResult_review_ok?.output ?? {}), (afterResult_complete_ok?.output ?? {}));
      const _fixtureInput = { period: null, targets: ["auth/login.ts"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(reviewCoverageHandler.metrics({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('authorStats', () => {
    it('builds a valid StorageProgram', () => {
      const program = reviewCoverageHandler.authorStats({ author: "alice" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = reviewCoverageHandler.authorStats({ author: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = reviewCoverageHandler.authorStats({ author: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = reviewCoverageHandler.authorStats({ author: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = reviewCoverageHandler.authorStats({ author: "alice" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof reviewCoverageHandler.authorStats !== 'function') return;
      const result = await interpret(reviewCoverageHandler.authorStats({ author: "alice" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "author_stats_ok" -> ok', async () => {
      if (typeof reviewCoverageHandler.authorStats !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_ok = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" }), storage);
      const afterResult_review_ok = await interpret(reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" }), storage);
      const afterResult_complete_ok = await interpret(reviewCoverageHandler.complete({ changeId: "pr-142" }), storage);
      const _pool = Object.assign({}, (afterResult_record_ok?.output ?? {}), (afterResult_review_ok?.output ?? {}), (afterResult_complete_ok?.output ?? {}));
      const _fixtureInput = { author: "alice" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(reviewCoverageHandler.authorStats({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "author_stats_empty" -> ok', async () => {
      if (typeof reviewCoverageHandler.authorStats !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_ok = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: "150" }), storage);
      const afterResult_review_ok = await interpret(reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: "4", substantiveComments: "2", defectsFound: "1", suggestionsAccepted: "1" }), storage);
      const afterResult_complete_ok = await interpret(reviewCoverageHandler.complete({ changeId: "pr-142" }), storage);
      const _pool = Object.assign({}, (afterResult_record_ok?.output ?? {}), (afterResult_review_ok?.output ?? {}), (afterResult_complete_ok?.output ?? {}));
      const _fixtureInput = { author: "unknown-person" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(reviewCoverageHandler.authorStats({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof reviewCoverageHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = reviewCoverageHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ReviewCoverage');
    });
  });

  describe('invariant examples', () => {
    it("record then review then complete", async () => {
      const storage = createInMemoryStorage();
      const recordResult0 = await interpret(reviewCoverageHandler.record({ changeId: "pr-142", target: "auth/login.ts", author: "alice", linesChanged: 150 }), storage);
      expect(recordResult0.variant).toBe("ok");
      let review = recordResult0.output["review"];
      let v = review;
      const recordReviewResult1 = await interpret(reviewCoverageHandler.recordReview({ changeId: "pr-142", reviewer: "bob", commentCount: 4, substantiveComments: 2, defectsFound: 1, suggestionsAccepted: 1 }), storage);
      expect(recordReviewResult1.variant).toBe("ok");
      review = recordReviewResult1.output["review"];
      let turnaround = recordReviewResult1.output["turnaround"];
      let _ = turnaround;
      const completeResult2 = await interpret(reviewCoverageHandler.complete({ changeId: "pr-142" }), storage);
      expect(completeResult2.variant).toBe("ok");
      review = completeResult2.output["review"];
      let totalTurnaround = completeResult2.output["totalTurnaround"];
      let totalRounds = completeResult2.output["totalRounds"];
      const thenResult0 = await interpret(reviewCoverageHandler.metrics({ period: false, targets: false }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('forall properties (PBT)', () => {
    it('forall: duplicate detection', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.constantFrom("pr-001", "pr-002"),
          async (id) => {
            const storage = createInMemoryStorage();
            const result = await interpret(reviewCoverageHandler.record({ changeId: id, target: "file.ts", author: "alice", linesChanged: 10 }), storage);
            expect(result.variant).toBeDefined();
            expect(result.variant).toBe("ok");
          },
        ),
        { numRuns: 100 },
      );
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: review without record', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('record'), input: fc.record({ changeId: fc.string({ minLength: 1, maxLength: 50 }), target: fc.string({ minLength: 1, maxLength: 50 }), author: fc.string({ minLength: 1, maxLength: 50 }), linesChanged: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('recordReview'), input: fc.record({ changeId: fc.string({ minLength: 1, maxLength: 50 }), reviewer: fc.string({ minLength: 1, maxLength: 50 }), commentCount: fc.integer({ min: 1, max: 1000 }), substantiveComments: fc.integer({ min: 1, max: 1000 }), defectsFound: fc.integer({ min: 1, max: 1000 }), suggestionsAccepted: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('complete'), input: fc.record({ changeId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('metrics'), input: fc.record({ period: fc.string(), targets: fc.string() }) }),
              fc.record({ action: fc.constant('authorStats'), input: fc.record({ author: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = reviewCoverageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(reviewCoverageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: review without record
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
