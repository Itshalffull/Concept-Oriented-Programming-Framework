// Notification Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { notificationHandler } from '../../handlers/ts/app/notification.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('Notification functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('registerChannel', () => {
    it('builds a valid StorageProgram', () => {
      const program = notificationHandler.registerChannel({ name: 'test-name', config: 'test-config' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = notificationHandler.registerChannel({ name: 'test-name', config: 'test-config' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = notificationHandler.registerChannel({ name: 'test-name', config: 'test-config' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = notificationHandler.registerChannel({ name: 'test-name', config: 'test-config' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = notificationHandler.registerChannel({ name: 'test-name', config: 'test-config' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof notificationHandler.registerChannel !== 'function') return;
      try {
        const result = await interpret(notificationHandler.registerChannel({ name: 'test-name', config: 'test-config' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('defineTemplate', () => {
    it('builds a valid StorageProgram', () => {
      const program = notificationHandler.defineTemplate({ notification: 'test', template: 'test-template' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = notificationHandler.defineTemplate({ notification: 'test', template: 'test-template' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = notificationHandler.defineTemplate({ notification: 'test', template: 'test-template' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = notificationHandler.defineTemplate({ notification: 'test', template: 'test-template' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = notificationHandler.defineTemplate({ notification: 'test', template: 'test-template' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof notificationHandler.defineTemplate !== 'function') return;
      try {
        const result = await interpret(notificationHandler.defineTemplate({ notification: 'test', template: 'test-template' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('subscribe', () => {
    it('builds a valid StorageProgram', () => {
      const program = notificationHandler.subscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = notificationHandler.subscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = notificationHandler.subscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = notificationHandler.subscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = notificationHandler.subscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof notificationHandler.subscribe !== 'function') return;
      try {
        const result = await interpret(notificationHandler.subscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('unsubscribe', () => {
    it('builds a valid StorageProgram', () => {
      const program = notificationHandler.unsubscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = notificationHandler.unsubscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = notificationHandler.unsubscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = notificationHandler.unsubscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = notificationHandler.unsubscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof notificationHandler.unsubscribe !== 'function') return;
      try {
        const result = await interpret(notificationHandler.unsubscribe({ user: 'test-user', eventType: 'test-eventType', channel: 'test-channel' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('notify', () => {
    it('builds a valid StorageProgram', () => {
      const program = notificationHandler.notify({ notification: 'test', user: 'test-user', template: 'test-template', data: 'test-data' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = notificationHandler.notify({ notification: 'test', user: 'test-user', template: 'test-template', data: 'test-data' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = notificationHandler.notify({ notification: 'test', user: 'test-user', template: 'test-template', data: 'test-data' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = notificationHandler.notify({ notification: 'test', user: 'test-user', template: 'test-template', data: 'test-data' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = notificationHandler.notify({ notification: 'test', user: 'test-user', template: 'test-template', data: 'test-data' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof notificationHandler.notify !== 'function') return;
      try {
        const result = await interpret(notificationHandler.notify({ notification: 'test', user: 'test-user', template: 'test-template', data: 'test-data' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('markRead', () => {
    it('builds a valid StorageProgram', () => {
      const program = notificationHandler.markRead({ notification: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = notificationHandler.markRead({ notification: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = notificationHandler.markRead({ notification: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = notificationHandler.markRead({ notification: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = notificationHandler.markRead({ notification: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof notificationHandler.markRead !== 'function') return;
      try {
        const result = await interpret(notificationHandler.markRead({ notification: 'test' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('getUnread', () => {
    it('builds a valid StorageProgram', () => {
      const program = notificationHandler.getUnread({ user: 'test-user' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = notificationHandler.getUnread({ user: 'test-user' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = notificationHandler.getUnread({ user: 'test-user' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = notificationHandler.getUnread({ user: 'test-user' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = notificationHandler.getUnread({ user: 'test-user' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof notificationHandler.getUnread !== 'function') return;
      try {
        const result = await interpret(notificationHandler.getUnread({ user: 'test-user' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("registerChannel then defineTemplate", async () => {
      const storage = createInMemoryStorage();
      const registerChannelResult0 = await interpret(notificationHandler.registerChannel({ name: {"type":"variable","name":"c"}, config: {"type":"variable","name":"cfg"} }), storage);
      expect(registerChannelResult0.variant).toBe("ok");
      const thenResult0 = await interpret(notificationHandler.defineTemplate({ notification: {"type":"variable","name":"n"}, template: {"type":"variable","name":"t"} }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(notificationHandler.subscribe({ user: {"type":"variable","name":"u"}, eventType: {"type":"variable","name":"e"}, channel: {"type":"variable","name":"c"} }), storage);
      expect(thenResult1.variant).toBe("ok");
      const thenResult2 = await interpret(notificationHandler.notify({ notification: {"type":"variable","name":"n"}, user: {"type":"variable","name":"u"}, template: {"type":"variable","name":"t"}, data: {"type":"variable","name":"d"} }), storage);
      expect(thenResult2.variant).toBe("ok");
      const thenResult3 = await interpret(notificationHandler.getUnread({ user: {"type":"variable","name":"u"} }), storage);
      expect(thenResult3.variant).toBe("ok");
    });

    it("notify then markRead", async () => {
      const storage = createInMemoryStorage();
      const notifyResult0 = await interpret(notificationHandler.notify({ notification: {"type":"variable","name":"n"}, user: {"type":"variable","name":"u"}, template: {"type":"variable","name":"t"}, data: {"type":"variable","name":"d"} }), storage);
      expect(notifyResult0.variant).toBe("ok");
      const thenResult0 = await interpret(notificationHandler.markRead({ notification: {"type":"variable","name":"n"} }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(notificationHandler.getUnread({ user: {"type":"variable","name":"u"} }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: channels have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerChannel'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), config: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('defineTemplate'), input: fc.record({ notification: fc.string(), template: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('subscribe'), input: fc.record({ user: fc.string({ minLength: 1, maxLength: 20 }), eventType: fc.string({ minLength: 1, maxLength: 20 }), channel: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('unsubscribe'), input: fc.record({ user: fc.string({ minLength: 1, maxLength: 20 }), eventType: fc.string({ minLength: 1, maxLength: 20 }), channel: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('notify'), input: fc.record({ notification: fc.string(), user: fc.string({ minLength: 1, maxLength: 20 }), template: fc.string({ minLength: 1, maxLength: 20 }), data: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('markRead'), input: fc.record({ notification: fc.string() }) }),
              fc.record({ action: fc.constant('getUnread'), input: fc.record({ user: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = notificationHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(notificationHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in channels', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerChannel'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), config: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('defineTemplate'), input: fc.record({ notification: fc.string(), template: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('subscribe'), input: fc.record({ user: fc.string({ minLength: 1, maxLength: 20 }), eventType: fc.string({ minLength: 1, maxLength: 20 }), channel: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('unsubscribe'), input: fc.record({ user: fc.string({ minLength: 1, maxLength: 20 }), eventType: fc.string({ minLength: 1, maxLength: 20 }), channel: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('notify'), input: fc.record({ notification: fc.string(), user: fc.string({ minLength: 1, maxLength: 20 }), template: fc.string({ minLength: 1, maxLength: 20 }), data: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('markRead'), input: fc.record({ notification: fc.string() }) }),
              fc.record({ action: fc.constant('getUnread'), input: fc.record({ user: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = notificationHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(notificationHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                  // Never: orphaned entry in channels
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('registerChannel handles empty input: ', async () => {
      if (typeof notificationHandler.registerChannel !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(notificationHandler.registerChannel({  }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('registerChannel ensures on ok: ', async () => {
      if (typeof notificationHandler.registerChannel !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = notificationHandler.registerChannel(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('defineTemplate handles empty input: ', async () => {
      if (typeof notificationHandler.defineTemplate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(notificationHandler.defineTemplate({  }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('defineTemplate ensures on ok: ', async () => {
      if (typeof notificationHandler.defineTemplate !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ notification: fc.string(), template: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = notificationHandler.defineTemplate(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('subscribe handles empty input: ', async () => {
      if (typeof notificationHandler.subscribe !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(notificationHandler.subscribe({  }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('subscribe ensures on ok: ', async () => {
      if (typeof notificationHandler.subscribe !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ user: fc.string({ minLength: 1, maxLength: 50 }), eventType: fc.string({ minLength: 1, maxLength: 50 }), channel: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = notificationHandler.subscribe(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
