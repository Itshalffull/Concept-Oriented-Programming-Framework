// CompletionCoverage Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { completionCoverageHandler } from '../../handlers/ts/monadic/completion-coverage.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('CompletionCoverage functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('check', () => {
    it('builds a valid StorageProgram', () => {
      const program = completionCoverageHandler.check({ concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\",\"error\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = completionCoverageHandler.check({ concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\",\"error\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = completionCoverageHandler.check({ concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\",\"error\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = completionCoverageHandler.check({ concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\",\"error\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = completionCoverageHandler.check({ concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\",\"error\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof completionCoverageHandler.check !== 'function') return;
      const result = await interpret(completionCoverageHandler.check({ concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\",\"error\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "full_coverage" -> ok', async () => {
      if (typeof completionCoverageHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_report_user = await interpret(completionCoverageHandler.report({ concept: "User" }), storage);
      const _pool = Object.assign({}, (afterResult_report_user?.output ?? {}));
      const _fixtureInput = { concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\",\"error\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(completionCoverageHandler.check({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "missing_sync" -> ok', async () => {
      if (typeof completionCoverageHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_report_user = await interpret(completionCoverageHandler.report({ concept: "User" }), storage);
      const _pool = Object.assign({}, (afterResult_report_user?.output ?? {}));
      const _fixtureInput = { concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"}]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(completionCoverageHandler.check({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "invalid_json_input" -> error', async () => {
      if (typeof completionCoverageHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(completionCoverageHandler.check({ concept: "User", action: "register", declaredVariants: "not json", extractedVariants: "not json", syncPatterns: "not json" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('report', () => {
    it('builds a valid StorageProgram', () => {
      const program = completionCoverageHandler.report({ concept: "User" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = completionCoverageHandler.report({ concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = completionCoverageHandler.report({ concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = completionCoverageHandler.report({ concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = completionCoverageHandler.report({ concept: "User" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof completionCoverageHandler.report !== 'function') return;
      const result = await interpret(completionCoverageHandler.report({ concept: "User" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "report_user" -> ok', async () => {
      if (typeof completionCoverageHandler.report !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_full_coverage = await interpret(completionCoverageHandler.check({ concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\",\"error\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_full_coverage?.output ?? {}));
      const _fixtureInput = { concept: "User" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(completionCoverageHandler.report({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

    it('fixture "report_empty" -> error', async () => {
      if (typeof completionCoverageHandler.report !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(completionCoverageHandler.report({ concept: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listUncovered', () => {
    it('builds a valid StorageProgram', () => {
      const program = completionCoverageHandler.listUncovered({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = completionCoverageHandler.listUncovered({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = completionCoverageHandler.listUncovered({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = completionCoverageHandler.listUncovered({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = completionCoverageHandler.listUncovered({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof completionCoverageHandler.listUncovered !== 'function') return;
      const result = await interpret(completionCoverageHandler.listUncovered({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof completionCoverageHandler.listUncovered !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_full_coverage = await interpret(completionCoverageHandler.check({ concept: "User", action: "register", declaredVariants: "[\"ok\",\"error\"]", extractedVariants: "[\"ok\",\"error\"]", syncPatterns: "[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_full_coverage?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(completionCoverageHandler.listUncovered({ ..._fixtureInput }), storage);
      const _isErr = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErr(result.variant), `expected success variant but got '${result.variant}'`).toBe(false);
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof completionCoverageHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = completionCoverageHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('CompletionCoverage');
    });
  });

  describe('invariant examples', () => {
    it("full coverage returns covered", async () => {
      const storage = createInMemoryStorage();
      const checkResult0 = await interpret(completionCoverageHandler.check({ concept: {"type":"literal","value":"User"}, action: {"type":"literal","value":"register"}, declaredVariants: {"type":"literal","value":"[\"ok\",\"error\"]"}, extractedVariants: {"type":"literal","value":"[\"ok\",\"error\"]"}, syncPatterns: {"type":"literal","value":"[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"},{\"sync\":\"RegistrationError\",\"variant\":\"error\"}]"} }), storage);
      expect(checkResult0.variant).toBe("covered");
      let report = checkResult0.output["report"];
      const thenResult0 = await interpret(completionCoverageHandler.report({ concept: {"type":"literal","value":"User"} }), storage);
      const _isErrA0 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErrA0(thenResult0.variant), `assertion 0: expected success but got '${thenResult0.variant}'`).toBe(false);
    });

    it("missing sync returns uncovered with diagnostics", async () => {
      const storage = createInMemoryStorage();
      const checkResult0 = await interpret(completionCoverageHandler.check({ concept: {"type":"literal","value":"User"}, action: {"type":"literal","value":"register"}, declaredVariants: {"type":"literal","value":"[\"ok\",\"error\"]"}, extractedVariants: {"type":"literal","value":"[\"ok\"]"}, syncPatterns: {"type":"literal","value":"[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"}]"} }), storage);
      expect(checkResult0.variant).toBe("uncovered");
      let report = checkResult0.output["report"];
      let uncoveredVariants = checkResult0.output["uncoveredVariants"];
      let orphanedPatterns = checkResult0.output["orphanedPatterns"];
    });

    it("listUncovered aggregates all gaps", async () => {
      const storage = createInMemoryStorage();
      const checkResult0 = await interpret(completionCoverageHandler.check({ concept: {"type":"literal","value":"User"}, action: {"type":"literal","value":"register"}, declaredVariants: {"type":"literal","value":"[\"ok\",\"error\"]"}, extractedVariants: {"type":"literal","value":"[\"ok\"]"}, syncPatterns: {"type":"literal","value":"[{\"sync\":\"RegisterUser\",\"variant\":\"ok\"}]"} }), storage);
      expect(checkResult0.variant).toBe("uncovered");
      let report = checkResult0.output["report"];
      let uncoveredVariants = checkResult0.output["uncoveredVariants"];
      let orphanedPatterns = checkResult0.output["orphanedPatterns"];
      const thenResult0 = await interpret(completionCoverageHandler.listUncovered({  }), storage);
      const _isErrA0 = (v: string) => !v || /error|invalid|not.?found|forbidden|unauthorized|unavailable|unsupported/i.test(v);
      expect(_isErrA0(thenResult0.variant), `assertion 0: expected success but got '${thenResult0.variant}'`).toBe(false);
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: reports track concept and action', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('check'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), declaredVariants: fc.string({ minLength: 1, maxLength: 50 }), extractedVariants: fc.string({ minLength: 1, maxLength: 50 }), syncPatterns: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('report'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listUncovered'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = completionCoverageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(completionCoverageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: covered report with uncovered variants', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('check'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), declaredVariants: fc.string({ minLength: 1, maxLength: 50 }), extractedVariants: fc.string({ minLength: 1, maxLength: 50 }), syncPatterns: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('report'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listUncovered'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = completionCoverageHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(completionCoverageHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: covered report with uncovered variants
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('check handles empty input: ', async () => {
      if (typeof completionCoverageHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(completionCoverageHandler.check({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('check ensures on covered: ', async () => {
      if (typeof completionCoverageHandler.check !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), declaredVariants: fc.string({ minLength: 1, maxLength: 50 }), extractedVariants: fc.string({ minLength: 1, maxLength: 50 }), syncPatterns: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = completionCoverageHandler.check(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "covered") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
