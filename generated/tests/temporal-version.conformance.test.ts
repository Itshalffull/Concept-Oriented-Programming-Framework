// TemporalVersion Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { temporalVersionHandler } from '../../handlers/ts/temporal-version.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('TemporalVersion functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('record', () => {
    it('builds a valid StorageProgram', () => {
      const program = temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof temporalVersionHandler.record !== 'function') return;
      const result = await interpret(temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "record_version" -> ok', async () => {
      if (typeof temporalVersionHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "record_empty_hash" -> error', async () => {
      if (typeof temporalVersionHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(temporalVersionHandler.record({ contentHash: "", validFrom: null, validTo: null, metadata: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('asOf', () => {
    it('builds a valid StorageProgram', () => {
      const program = temporalVersionHandler.asOf({ systemTime: "2025-06-15T12:00:00Z", validTime: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = temporalVersionHandler.asOf({ systemTime: "2025-06-15T12:00:00Z", validTime: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = temporalVersionHandler.asOf({ systemTime: "2025-06-15T12:00:00Z", validTime: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = temporalVersionHandler.asOf({ systemTime: "2025-06-15T12:00:00Z", validTime: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = temporalVersionHandler.asOf({ systemTime: "2025-06-15T12:00:00Z", validTime: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof temporalVersionHandler.asOf !== 'function') return;
      const result = await interpret(temporalVersionHandler.asOf({ systemTime: "2025-06-15T12:00:00Z", validTime: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "query_system_time" -> ok', async () => {
      if (typeof temporalVersionHandler.asOf !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_version = await interpret(temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_version?.output ?? {}));
      const _fixtureInput = { systemTime: "2025-06-15T12:00:00Z", validTime: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(temporalVersionHandler.asOf({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_both_times" -> ok', async () => {
      if (typeof temporalVersionHandler.asOf !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_version = await interpret(temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_version?.output ?? {}));
      const _fixtureInput = { systemTime: "2025-06-15T12:00:00Z", validTime: "2025-01-01T00:00:00Z" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(temporalVersionHandler.asOf({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "query_far_future" -> error', async () => {
      if (typeof temporalVersionHandler.asOf !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(temporalVersionHandler.asOf({ systemTime: "2099-01-01T00:00:00Z", validTime: null }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('between', () => {
    it('builds a valid StorageProgram', () => {
      const program = temporalVersionHandler.between({ start: "2025-01-01T00:00:00Z", end: "2025-12-31T23:59:59Z", dimension: "system" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = temporalVersionHandler.between({ start: "2025-01-01T00:00:00Z", end: "2025-12-31T23:59:59Z", dimension: "system" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = temporalVersionHandler.between({ start: "2025-01-01T00:00:00Z", end: "2025-12-31T23:59:59Z", dimension: "system" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = temporalVersionHandler.between({ start: "2025-01-01T00:00:00Z", end: "2025-12-31T23:59:59Z", dimension: "system" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = temporalVersionHandler.between({ start: "2025-01-01T00:00:00Z", end: "2025-12-31T23:59:59Z", dimension: "system" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof temporalVersionHandler.between !== 'function') return;
      const result = await interpret(temporalVersionHandler.between({ start: "2025-01-01T00:00:00Z", end: "2025-12-31T23:59:59Z", dimension: "system" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "between_system" -> ok', async () => {
      if (typeof temporalVersionHandler.between !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_version = await interpret(temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_version?.output ?? {}));
      const _fixtureInput = { start: "2025-01-01T00:00:00Z", end: "2025-12-31T23:59:59Z", dimension: "system" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(temporalVersionHandler.between({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "between_invalid_dim" -> error', async () => {
      if (typeof temporalVersionHandler.between !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(temporalVersionHandler.between({ start: "2025-01-01T00:00:00Z", end: "2025-12-31T23:59:59Z", dimension: "bogus" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('current', () => {
    it('builds a valid StorageProgram', () => {
      const program = temporalVersionHandler.current({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = temporalVersionHandler.current({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = temporalVersionHandler.current({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = temporalVersionHandler.current({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = temporalVersionHandler.current({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof temporalVersionHandler.current !== 'function') return;
      const result = await interpret(temporalVersionHandler.current({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof temporalVersionHandler.current !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_version = await interpret(temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" }), storage);
      const _pool = Object.assign({}, (afterResult_record_version?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(temporalVersionHandler.current({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('supersede', () => {
    it('builds a valid StorageProgram', () => {
      const program = temporalVersionHandler.supersede({ versionId: {"type":"ref","fixture":"record_version","field":"versionId"}, contentHash: "sha256:newcontent789" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = temporalVersionHandler.supersede({ versionId: {"type":"ref","fixture":"record_version","field":"versionId"}, contentHash: "sha256:newcontent789" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = temporalVersionHandler.supersede({ versionId: {"type":"ref","fixture":"record_version","field":"versionId"}, contentHash: "sha256:newcontent789" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = temporalVersionHandler.supersede({ versionId: {"type":"ref","fixture":"record_version","field":"versionId"}, contentHash: "sha256:newcontent789" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = temporalVersionHandler.supersede({ versionId: {"type":"ref","fixture":"record_version","field":"versionId"}, contentHash: "sha256:newcontent789" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof temporalVersionHandler.supersede !== 'function') return;
      const result = await interpret(temporalVersionHandler.supersede({ versionId: {"type":"ref","fixture":"record_version","field":"versionId"}, contentHash: "sha256:newcontent789" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "supersede_existing" -> ok', async () => {
      if (typeof temporalVersionHandler.supersede !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_record_version = await interpret(temporalVersionHandler.record({ contentHash: "sha256:abc123def456", validFrom: "2025-01-01T00:00:00Z", validTo: null, metadata: "{\"author\":\"alice\"}" }), storage);
      const result = await interpret(temporalVersionHandler.supersede({ versionId: afterResult_record_version?.output?.["versionId"], contentHash: "sha256:newcontent789" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "supersede_empty_hash" -> error', async () => {
      if (typeof temporalVersionHandler.supersede !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(temporalVersionHandler.supersede({ versionId: "temporal-version-1", contentHash: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof temporalVersionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = temporalVersionHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('TemporalVersion');
    });
  });

  describe('invariant examples', () => {
    it("record then asOf", async () => {
      const storage = createInMemoryStorage();
      const recordResult0 = await interpret(temporalVersionHandler.record({ contentHash: "test-h", validFrom: "test-vf", validTo: "test-_", metadata: "test-_" }), storage);
      expect(recordResult0.variant).toBe("ok");
      let versionId = recordResult0.output["versionId"];
      let v = versionId;
      const thenResult0 = await interpret(temporalVersionHandler.asOf({ systemTime: "test-_", validTime: "test-vf" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("record then current", async () => {
      const storage = createInMemoryStorage();
      const recordResult0 = await interpret(temporalVersionHandler.record({ contentHash: "test-h", validFrom: "test-_", validTo: "test-_", metadata: "test-_" }), storage);
      expect(recordResult0.variant).toBe("ok");
      let versionId = recordResult0.output["versionId"];
      let v = versionId;
      const thenResult0 = await interpret(temporalVersionHandler.current({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: versions have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('record'), input: fc.record({ contentHash: fc.string({ minLength: 1, maxLength: 50 }), validFrom: fc.string(), validTo: fc.string(), metadata: fc.string() }) }),
              fc.record({ action: fc.constant('asOf'), input: fc.record({ systemTime: fc.string(), validTime: fc.string() }) }),
              fc.record({ action: fc.constant('between'), input: fc.record({ start: fc.string({ minLength: 1, maxLength: 50 }), end: fc.string({ minLength: 1, maxLength: 50 }), dimension: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('current'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('supersede'), input: fc.record({ versionId: fc.string(), contentHash: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = temporalVersionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(temporalVersionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned entry in versions', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('record'), input: fc.record({ contentHash: fc.string({ minLength: 1, maxLength: 50 }), validFrom: fc.string(), validTo: fc.string(), metadata: fc.string() }) }),
              fc.record({ action: fc.constant('asOf'), input: fc.record({ systemTime: fc.string(), validTime: fc.string() }) }),
              fc.record({ action: fc.constant('between'), input: fc.record({ start: fc.string({ minLength: 1, maxLength: 50 }), end: fc.string({ minLength: 1, maxLength: 50 }), dimension: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('current'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('supersede'), input: fc.record({ versionId: fc.string(), contentHash: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = temporalVersionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(temporalVersionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned entry in versions
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('record handles empty input: ', async () => {
      if (typeof temporalVersionHandler.record !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(temporalVersionHandler.record({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('record ensures on ok: ', async () => {
      if (typeof temporalVersionHandler.record !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ contentHash: fc.string({ minLength: 1, maxLength: 50 }), validFrom: fc.string(), validTo: fc.string(), metadata: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = temporalVersionHandler.record(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('between handles empty input: ', async () => {
      if (typeof temporalVersionHandler.between !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(temporalVersionHandler.between({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('between ensures on ok: ', async () => {
      if (typeof temporalVersionHandler.between !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ start: fc.string({ minLength: 1, maxLength: 50 }), end: fc.string({ minLength: 1, maxLength: 50 }), dimension: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = temporalVersionHandler.between(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('supersede handles empty input: ', async () => {
      if (typeof temporalVersionHandler.supersede !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(temporalVersionHandler.supersede({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('supersede ensures on ok: ', async () => {
      if (typeof temporalVersionHandler.supersede !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ versionId: fc.string(), contentHash: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = temporalVersionHandler.supersede(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
