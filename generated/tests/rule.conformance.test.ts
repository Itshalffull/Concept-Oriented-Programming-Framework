// Rule Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { ruleHandler } from '../../handlers/ts/quality-measurement/rule.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Rule functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('define', () => {
    it('builds a valid StorageProgram', () => {
      const program = ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ruleHandler.define !== 'function') return;
      const result = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "define_complexity" -> ok', async () => {
      if (typeof ruleHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "define_naming" -> ok', async () => {
      if (typeof ruleHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ruleHandler.define({ ruleId: "no-single-letter-vars", name: "No Single Letter Variables", description: "Variable names must be descriptive, not single characters", category: "naming", severity: "minor", effort: "trivial", tags: ["readability","style"], cleanCodeAttribute: "consistent" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "define_minimal" -> ok', async () => {
      if (typeof ruleHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ruleHandler.define({ ruleId: "unused-imports", name: "Unused Imports", description: "Remove unused import statements", category: "maintainability", severity: "info" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "define_duplicate" -> duplicate', async () => {
      if (typeof ruleHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}));
      const _fixtureInput = { ruleId: "max-cognitive-complexity", name: "Duplicate Rule", description: "Should conflict", category: "complexity", severity: "major" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.define({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('evaluate', () => {
    it('builds a valid StorageProgram', () => {
      const program = ruleHandler.evaluate({ ruleId: "max-cognitive-complexity", targets: ["src/handlers/auth.ts","src/handlers/user.ts"] });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ruleHandler.evaluate({ ruleId: "max-cognitive-complexity", targets: ["src/handlers/auth.ts","src/handlers/user.ts"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ruleHandler.evaluate({ ruleId: "max-cognitive-complexity", targets: ["src/handlers/auth.ts","src/handlers/user.ts"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ruleHandler.evaluate({ ruleId: "max-cognitive-complexity", targets: ["src/handlers/auth.ts","src/handlers/user.ts"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ruleHandler.evaluate({ ruleId: "max-cognitive-complexity", targets: ["src/handlers/auth.ts","src/handlers/user.ts"] });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ruleHandler.evaluate !== 'function') return;
      const result = await interpret(ruleHandler.evaluate({ ruleId: "max-cognitive-complexity", targets: ["src/handlers/auth.ts","src/handlers/user.ts"] }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "evaluate_ok" -> ok', async () => {
      if (typeof ruleHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}));
      const _fixtureInput = { ruleId: "max-cognitive-complexity", targets: ["src/handlers/auth.ts","src/handlers/user.ts"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.evaluate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "evaluate_disabled" -> disabled', async () => {
      if (typeof ruleHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const afterResult_disable_ok = await interpret(ruleHandler.disable({ ruleId: "max-cognitive-complexity" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}), (afterResult_disable_ok?.output ?? {}));
      const _fixtureInput = { ruleId: "max-cognitive-complexity", targets: ["src/app.ts"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.evaluate({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('disabled'));
    });

    it('fixture "evaluate_unknown" -> unknownRule', async () => {
      if (typeof ruleHandler.evaluate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ruleHandler.evaluate({ ruleId: "nonexistent-rule", targets: ["src/app.ts"] }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unknownRule'));
    });

  });

  describe('enable', () => {
    it('builds a valid StorageProgram', () => {
      const program = ruleHandler.enable({ ruleId: "max-cognitive-complexity" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ruleHandler.enable({ ruleId: "max-cognitive-complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ruleHandler.enable({ ruleId: "max-cognitive-complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ruleHandler.enable({ ruleId: "max-cognitive-complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ruleHandler.enable({ ruleId: "max-cognitive-complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ruleHandler.enable !== 'function') return;
      const result = await interpret(ruleHandler.enable({ ruleId: "max-cognitive-complexity" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "enable_ok" -> ok', async () => {
      if (typeof ruleHandler.enable !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const afterResult_disable_ok = await interpret(ruleHandler.disable({ ruleId: "max-cognitive-complexity" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}), (afterResult_disable_ok?.output ?? {}));
      const _fixtureInput = { ruleId: "max-cognitive-complexity" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.enable({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "enable_already" -> alreadyEnabled', async () => {
      if (typeof ruleHandler.enable !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}));
      const _fixtureInput = { ruleId: "max-cognitive-complexity" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.enable({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('alreadyEnabled'));
    });

    it('fixture "enable_unknown" -> unknownRule', async () => {
      if (typeof ruleHandler.enable !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ruleHandler.enable({ ruleId: "nonexistent-rule" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unknownRule'));
    });

  });

  describe('disable', () => {
    it('builds a valid StorageProgram', () => {
      const program = ruleHandler.disable({ ruleId: "max-cognitive-complexity" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ruleHandler.disable({ ruleId: "max-cognitive-complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ruleHandler.disable({ ruleId: "max-cognitive-complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ruleHandler.disable({ ruleId: "max-cognitive-complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ruleHandler.disable({ ruleId: "max-cognitive-complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ruleHandler.disable !== 'function') return;
      const result = await interpret(ruleHandler.disable({ ruleId: "max-cognitive-complexity" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "disable_ok" -> ok', async () => {
      if (typeof ruleHandler.disable !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}));
      const _fixtureInput = { ruleId: "max-cognitive-complexity" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.disable({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "disable_already_disabled" -> alreadyDisabled', async () => {
      if (typeof ruleHandler.disable !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const afterResult_disable_ok = await interpret(ruleHandler.disable({ ruleId: "max-cognitive-complexity" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}), (afterResult_disable_ok?.output ?? {}));
      const _fixtureInput = { ruleId: "max-cognitive-complexity" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.disable({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('alreadyDisabled'));
    });

    it('fixture "disable_unknown" -> unknownRule', async () => {
      if (typeof ruleHandler.disable !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ruleHandler.disable({ ruleId: "nonexistent-rule" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unknownRule'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = ruleHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ruleHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ruleHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ruleHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ruleHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ruleHandler.list !== 'function') return;
      const result = await interpret(ruleHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof ruleHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(ruleHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_category" -> ok', async () => {
      if (typeof ruleHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}));
      const _fixtureInput = { category: "complexity" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_severity" -> ok', async () => {
      if (typeof ruleHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_naming = await interpret(ruleHandler.define({ ruleId: "no-single-letter-vars", name: "No Single Letter Variables", description: "Variable names must be descriptive, not single characters", category: "naming", severity: "minor", effort: "trivial", tags: ["readability","style"], cleanCodeAttribute: "consistent" }), storage);
      const _pool = Object.assign({}, (afterResult_define_naming?.output ?? {}));
      const _fixtureInput = { severity: "minor" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_tags" -> ok', async () => {
      if (typeof ruleHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_define_complexity = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      const _pool = Object.assign({}, (afterResult_define_complexity?.output ?? {}));
      const _fixtureInput = { tags: ["maintainability"] } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ruleHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof ruleHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = ruleHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Rule');
    });
  });

  describe('invariant examples', () => {
    it("define then list", async () => {
      const storage = createInMemoryStorage();
      const defineResult0 = await interpret(ruleHandler.define({ ruleId: "cyclomatic-complexity", name: "Cyclomatic Complexity", description: "Limit cyclomatic complexity per function", category: "complexity", severity: "major" }), storage);
      expect(defineResult0.variant).toBe("ok");
      let rule = defineResult0.output["rule"];
      let r = rule;
      const thenResult0 = await interpret(ruleHandler.list({ category: "complexity" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("disable prevents evaluation", async () => {
      const storage = createInMemoryStorage();
      const defineResult0 = await interpret(ruleHandler.define({ ruleId: "max-cognitive-complexity", name: "Maximum Cognitive Complexity", description: "Function cognitive complexity must not exceed threshold", category: "complexity", severity: "major", effort: "medium", tags: ["maintainability"], cleanCodeAttribute: "intentional" }), storage);
      expect(defineResult0.variant).toBe("ok");
      let rule = defineResult0.output["rule"];
      let r = rule;
      const disableResult1 = await interpret(ruleHandler.disable({ ruleId: "max-cognitive-complexity" }), storage);
      expect(disableResult1.variant).toBe("ok");
      rule = disableResult1.output["rule"];
      const thenResult0 = await interpret(ruleHandler.evaluate({ ruleId: "max-cognitive-complexity", targets: ["any.ts"] }), storage);
      expect(thenResult0.variant).toBe("disabled");
    });

  });

  describe('forall properties (PBT)', () => {
    it('forall: duplicate ruleId rejected', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.constantFrom("rule-a", "rule-b"),
          async (id) => {
            const storage = createInMemoryStorage();
            const result = await interpret(ruleHandler.define({ ruleId: id, name: "Test Rule", description: "A test", category: "complexity", severity: "info" }), storage);
            expect(result.variant).toBeDefined();
            expect(result.variant).toBe("ok");
          },
        ),
        { numRuns: 100 },
      );
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: unknown rule evaluates', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('define'), input: fc.record({ ruleId: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), severity: fc.string({ minLength: 1, maxLength: 50 }), effort: fc.string(), tags: fc.string(), cleanCodeAttribute: fc.string() }) }),
              fc.record({ action: fc.constant('evaluate'), input: fc.record({ ruleId: fc.string({ minLength: 1, maxLength: 50 }), targets: fc.string() }) }),
              fc.record({ action: fc.constant('enable'), input: fc.record({ ruleId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('disable'), input: fc.record({ ruleId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ category: fc.string(), severity: fc.string(), tags: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = ruleHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(ruleHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: unknown rule evaluates
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
