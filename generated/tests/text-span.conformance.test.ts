// TextSpan Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { textSpanHandler } from '../../handlers/ts/app/text-span.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('TextSpan functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.create !== 'function') return;
      const result = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "create_ok" -> ok', async () => {
      if (typeof textSpanHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_citation" -> ok', async () => {
      if (typeof textSpanHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.create({ span: "span-2", entityRef: "entity-42", startAnchor: "anchor-a", endAnchor: "anchor-b", kind: "citation", label: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "create_empty_entity" -> error', async () => {
      if (typeof textSpanHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.create({ span: "span-3", entityRef: "", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: null }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "create_empty_kind" -> error', async () => {
      if (typeof textSpanHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.create({ span: "span-4", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "", label: null }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "create_duplicate" -> error', async () => {
      if (typeof textSpanHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.create({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.resolve({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.resolve({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.resolve({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.resolve({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.resolve({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.resolve !== 'function') return;
      const result = await interpret(textSpanHandler.resolve({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_ok" -> ok', async () => {
      if (typeof textSpanHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.resolve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_missing" -> notfound', async () => {
      if (typeof textSpanHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.resolve({ span: "nonexistent", currentContent: "{\"blocks\":[]}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('getText', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.getText({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.getText({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.getText({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.getText({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.getText({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.getText !== 'function') return;
      const result = await interpret(textSpanHandler.getText({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "getText_ok" -> ok', async () => {
      if (typeof textSpanHandler.getText !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.getText({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "getText_missing" -> notfound', async () => {
      if (typeof textSpanHandler.getText !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.getText({ span: "nonexistent", currentContent: "{\"blocks\":[]}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('getFragments', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.getFragments({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.getFragments({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.getFragments({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.getFragments({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.getFragments({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.getFragments !== 'function') return;
      const result = await interpret(textSpanHandler.getFragments({ span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "getFragments_ok" -> ok', async () => {
      if (typeof textSpanHandler.getFragments !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1", currentContent: "{\"blocks\":[{\"id\":\"b1\",\"content\":\"Hello world\"}]}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.getFragments({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "getFragments_missing" -> notfound', async () => {
      if (typeof textSpanHandler.getFragments !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.getFragments({ span: "nonexistent", currentContent: "{\"blocks\":[]}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('resize', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.resize({ span: "span-1", newStartAnchor: "anchor-new-start", newEndAnchor: "anchor-new-end" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.resize({ span: "span-1", newStartAnchor: "anchor-new-start", newEndAnchor: "anchor-new-end" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.resize({ span: "span-1", newStartAnchor: "anchor-new-start", newEndAnchor: "anchor-new-end" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.resize({ span: "span-1", newStartAnchor: "anchor-new-start", newEndAnchor: "anchor-new-end" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.resize({ span: "span-1", newStartAnchor: "anchor-new-start", newEndAnchor: "anchor-new-end" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.resize !== 'function') return;
      const result = await interpret(textSpanHandler.resize({ span: "span-1", newStartAnchor: "anchor-new-start", newEndAnchor: "anchor-new-end" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resize_ok" -> ok', async () => {
      if (typeof textSpanHandler.resize !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1", newStartAnchor: "anchor-new-start", newEndAnchor: "anchor-new-end" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.resize({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resize_missing" -> notfound', async () => {
      if (typeof textSpanHandler.resize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.resize({ span: "nonexistent", newStartAnchor: "anchor-a", newEndAnchor: "anchor-b" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('split', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.split({ span: "span-1", splitAnchor: "anchor-mid" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.split({ span: "span-1", splitAnchor: "anchor-mid" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.split({ span: "span-1", splitAnchor: "anchor-mid" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.split({ span: "span-1", splitAnchor: "anchor-mid" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.split({ span: "span-1", splitAnchor: "anchor-mid" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.split !== 'function') return;
      const result = await interpret(textSpanHandler.split({ span: "span-1", splitAnchor: "anchor-mid" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "split_ok" -> ok', async () => {
      if (typeof textSpanHandler.split !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1", splitAnchor: "anchor-mid" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.split({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "split_missing" -> notfound', async () => {
      if (typeof textSpanHandler.split !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.split({ span: "nonexistent", splitAnchor: "anchor-mid" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "split_empty_anchor" -> error', async () => {
      if (typeof textSpanHandler.split !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1", splitAnchor: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.split({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('merge', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.merge({ spanA: "span-1", spanB: "span-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.merge({ spanA: "span-1", spanB: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.merge({ spanA: "span-1", spanB: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.merge({ spanA: "span-1", spanB: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.merge({ spanA: "span-1", spanB: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.merge !== 'function') return;
      const result = await interpret(textSpanHandler.merge({ spanA: "span-1", spanB: "span-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "merge_ok" -> ok', async () => {
      if (typeof textSpanHandler.merge !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { spanA: "span-1", spanB: "span-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.merge({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "merge_missing" -> error', async () => {
      if (typeof textSpanHandler.merge !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.merge({ spanA: "nonexistent", spanB: "nonexistent-b" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('setKind', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.setKind({ span: "span-1", kind: "citation" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.setKind({ span: "span-1", kind: "citation" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.setKind({ span: "span-1", kind: "citation" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.setKind({ span: "span-1", kind: "citation" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.setKind({ span: "span-1", kind: "citation" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.setKind !== 'function') return;
      const result = await interpret(textSpanHandler.setKind({ span: "span-1", kind: "citation" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "setKind_ok" -> ok', async () => {
      if (typeof textSpanHandler.setKind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1", kind: "citation" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.setKind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "setKind_missing" -> notfound', async () => {
      if (typeof textSpanHandler.setKind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.setKind({ span: "nonexistent", kind: "citation" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('setLabel', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.setLabel({ span: "span-1", label: "Updated label" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.setLabel({ span: "span-1", label: "Updated label" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.setLabel({ span: "span-1", label: "Updated label" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.setLabel({ span: "span-1", label: "Updated label" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.setLabel({ span: "span-1", label: "Updated label" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.setLabel !== 'function') return;
      const result = await interpret(textSpanHandler.setLabel({ span: "span-1", label: "Updated label" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "setLabel_ok" -> ok', async () => {
      if (typeof textSpanHandler.setLabel !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1", label: "Updated label" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.setLabel({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "setLabel_missing" -> notfound', async () => {
      if (typeof textSpanHandler.setLabel !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.setLabel({ span: "nonexistent", label: "label" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.get({ span: "span-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.get({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.get({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.get({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.get({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.get !== 'function') return;
      const result = await interpret(textSpanHandler.get({ span: "span-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof textSpanHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.get({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof textSpanHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.get({ span: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.list({ entityRef: "entity-42" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.list({ entityRef: "entity-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.list({ entityRef: "entity-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.list({ entityRef: "entity-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.list({ entityRef: "entity-42" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.list !== 'function') return;
      const result = await interpret(textSpanHandler.list({ entityRef: "entity-42" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_ok" -> ok', async () => {
      if (typeof textSpanHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { entityRef: "entity-42" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof textSpanHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { entityRef: "no-such-entity" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('listByKind', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.listByKind({ entityRef: "entity-42", kind: "highlight" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.listByKind({ entityRef: "entity-42", kind: "highlight" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.listByKind({ entityRef: "entity-42", kind: "highlight" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.listByKind({ entityRef: "entity-42", kind: "highlight" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.listByKind({ entityRef: "entity-42", kind: "highlight" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.listByKind !== 'function') return;
      const result = await interpret(textSpanHandler.listByKind({ entityRef: "entity-42", kind: "highlight" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "listByKind_ok" -> ok', async () => {
      if (typeof textSpanHandler.listByKind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { entityRef: "entity-42", kind: "highlight" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.listByKind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "listByKind_empty" -> ok', async () => {
      if (typeof textSpanHandler.listByKind !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { entityRef: "entity-42", kind: "redaction" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.listByKind({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('delete', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.delete({ span: "span-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.delete({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.delete({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.delete({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.delete({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.delete !== 'function') return;
      const result = await interpret(textSpanHandler.delete({ span: "span-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "delete_ok" -> ok', async () => {
      if (typeof textSpanHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.delete({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "delete_missing" -> notfound', async () => {
      if (typeof textSpanHandler.delete !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.delete({ span: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('markStale', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.markStale({ span: "span-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.markStale({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.markStale({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.markStale({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.markStale({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.markStale !== 'function') return;
      const result = await interpret(textSpanHandler.markStale({ span: "span-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "markStale_ok" -> ok', async () => {
      if (typeof textSpanHandler.markStale !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.markStale({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "markStale_missing" -> notfound', async () => {
      if (typeof textSpanHandler.markStale !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.markStale({ span: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('markBroken', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.markBroken({ span: "span-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.markBroken({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.markBroken({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.markBroken({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.markBroken({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.markBroken !== 'function') return;
      const result = await interpret(textSpanHandler.markBroken({ span: "span-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "markBroken_ok" -> ok', async () => {
      if (typeof textSpanHandler.markBroken !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.markBroken({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "markBroken_missing" -> notfound', async () => {
      if (typeof textSpanHandler.markBroken !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.markBroken({ span: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('markActive', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.markActive({ span: "span-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.markActive({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.markActive({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.markActive({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.markActive({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.markActive !== 'function') return;
      const result = await interpret(textSpanHandler.markActive({ span: "span-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "markActive_ok" -> ok', async () => {
      if (typeof textSpanHandler.markActive !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.markActive({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "markActive_missing" -> notfound', async () => {
      if (typeof textSpanHandler.markActive !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.markActive({ span: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('getVersionInfo', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.getVersionInfo({ span: "span-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.getVersionInfo({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.getVersionInfo({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.getVersionInfo({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.getVersionInfo({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.getVersionInfo !== 'function') return;
      const result = await interpret(textSpanHandler.getVersionInfo({ span: "span-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "getVersionInfo_ok" -> ok', async () => {
      if (typeof textSpanHandler.getVersionInfo !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.getVersionInfo({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "getVersionInfo_missing" -> notfound', async () => {
      if (typeof textSpanHandler.getVersionInfo !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.getVersionInfo({ span: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('getOriginalText', () => {
    it('builds a valid StorageProgram', () => {
      const program = textSpanHandler.getOriginalText({ span: "span-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = textSpanHandler.getOriginalText({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = textSpanHandler.getOriginalText({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = textSpanHandler.getOriginalText({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = textSpanHandler.getOriginalText({ span: "span-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof textSpanHandler.getOriginalText !== 'function') return;
      const result = await interpret(textSpanHandler.getOriginalText({ span: "span-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "getOriginalText_ok" -> ok', async () => {
      if (typeof textSpanHandler.getOriginalText !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_create_ok = await interpret(textSpanHandler.create({ span: "span-1", entityRef: "entity-42", startAnchor: "anchor-start", endAnchor: "anchor-end", kind: "highlight", label: "My highlight" }), storage);
      const _pool = Object.assign({}, (afterResult_create_ok?.output ?? {}));
      const _fixtureInput = { span: "span-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(textSpanHandler.getOriginalText({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "getOriginalText_missing" -> notfound', async () => {
      if (typeof textSpanHandler.getOriginalText !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(textSpanHandler.getOriginalText({ span: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof textSpanHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = textSpanHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('TextSpan');
    });
  });

  describe('invariant examples', () => {
    it("create then get", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(textSpanHandler.create({ span: "s1", entityRef: "doc-1", startAnchor: "a-start", endAnchor: "a-end", kind: "highlight", label: "test" }), storage);
      expect(createResult0.variant).toBe("ok");
      let span = createResult0.output["span"];
      let s = span;
      const thenResult0 = await interpret(textSpanHandler.get({ span: "s1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("create then delete", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(textSpanHandler.create({ span: "s1", entityRef: "doc-1", startAnchor: "a-start", endAnchor: "a-end", kind: "highlight", label: false }), storage);
      expect(createResult0.variant).toBe("ok");
      let span = createResult0.output["span"];
      let s = span;
      const thenResult0 = await interpret(textSpanHandler.delete({ span: "s1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("create then list", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(textSpanHandler.create({ span: "s1", entityRef: "doc-1", startAnchor: "a-start", endAnchor: "a-end", kind: "highlight", label: false }), storage);
      expect(createResult0.variant).toBe("ok");
      let span = createResult0.output["span"];
      let s = span;
      const thenResult0 = await interpret(textSpanHandler.list({ entityRef: "doc-1" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: orphan spans', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ span: fc.string(), entityRef: fc.string({ minLength: 1, maxLength: 50 }), startAnchor: fc.string({ minLength: 1, maxLength: 50 }), endAnchor: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ span: fc.string(), currentContent: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getText'), input: fc.record({ span: fc.string(), currentContent: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getFragments'), input: fc.record({ span: fc.string(), currentContent: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resize'), input: fc.record({ span: fc.string(), newStartAnchor: fc.string({ minLength: 1, maxLength: 50 }), newEndAnchor: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('split'), input: fc.record({ span: fc.string(), splitAnchor: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('merge'), input: fc.record({ spanA: fc.string(), spanB: fc.string() }) }),
              fc.record({ action: fc.constant('setKind'), input: fc.record({ span: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setLabel'), input: fc.record({ span: fc.string(), label: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ entityRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listByKind'), input: fc.record({ entityRef: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('markStale'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('markBroken'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('markActive'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('getVersionInfo'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('getOriginalText'), input: fc.record({ span: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = textSpanHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(textSpanHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphan spans
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: status valid', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ span: fc.string(), entityRef: fc.string({ minLength: 1, maxLength: 50 }), startAnchor: fc.string({ minLength: 1, maxLength: 50 }), endAnchor: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ span: fc.string(), currentContent: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getText'), input: fc.record({ span: fc.string(), currentContent: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getFragments'), input: fc.record({ span: fc.string(), currentContent: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resize'), input: fc.record({ span: fc.string(), newStartAnchor: fc.string({ minLength: 1, maxLength: 50 }), newEndAnchor: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('split'), input: fc.record({ span: fc.string(), splitAnchor: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('merge'), input: fc.record({ spanA: fc.string(), spanB: fc.string() }) }),
              fc.record({ action: fc.constant('setKind'), input: fc.record({ span: fc.string(), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setLabel'), input: fc.record({ span: fc.string(), label: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ entityRef: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listByKind'), input: fc.record({ entityRef: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('delete'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('markStale'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('markBroken'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('markActive'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('getVersionInfo'), input: fc.record({ span: fc.string() }) }),
              fc.record({ action: fc.constant('getOriginalText'), input: fc.record({ span: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = textSpanHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(textSpanHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
