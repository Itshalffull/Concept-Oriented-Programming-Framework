// QueryPurityProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { queryPurityProviderHandler } from '../../handlers/ts/view/providers/query-purity-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('QueryPurityProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('analyze', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryPurityProviderHandler.analyze !== 'function') return;
      const result = await interpret(queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "analyze_read_only" -> ok', async () => {
      if (typeof queryPurityProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "analyze_read_write" -> ok', async () => {
      if (typeof queryPurityProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"invoke\",\"concept\":\"Article\",\"action\":\"archive\",\"args\":{},\"bindAs\":\"result\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[\"Article/archive\"]}}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "analyze_pure" -> ok', async () => {
      if (typeof queryPurityProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryPurityProviderHandler.analyze({ program: "{\"instructions\":[],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "analyze_invalid" -> error', async () => {
      if (typeof queryPurityProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryPurityProviderHandler.analyze({ program: "not valid json{{{" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = queryPurityProviderHandler.get({ result: {"type":"ref","fixture":"analyze_read_only","field":"result"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = queryPurityProviderHandler.get({ result: {"type":"ref","fixture":"analyze_read_only","field":"result"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = queryPurityProviderHandler.get({ result: {"type":"ref","fixture":"analyze_read_only","field":"result"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = queryPurityProviderHandler.get({ result: {"type":"ref","fixture":"analyze_read_only","field":"result"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = queryPurityProviderHandler.get({ result: {"type":"ref","fixture":"analyze_read_only","field":"result"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof queryPurityProviderHandler.get !== 'function') return;
      const result = await interpret(queryPurityProviderHandler.get({ result: {"type":"ref","fixture":"analyze_read_only","field":"result"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof queryPurityProviderHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_read_only = await interpret(queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" }), storage);
      const result = await interpret(queryPurityProviderHandler.get({ result: afterResult_analyze_read_only?.output?.["result"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof queryPurityProviderHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(queryPurityProviderHandler.get({ result: "nonexistent-result-id" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof queryPurityProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = queryPurityProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('QueryPurityProvider');
    });
  });

  describe('invariant examples', () => {
    it("scan and filter yields read-only purity", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let result = analyzeResult0.output["result"];
      let r = result;
      let readFields = analyzeResult0.output["readFields"];
      let rf = readFields;
      let invokedActions = analyzeResult0.output["invokedActions"];
      let ia = invokedActions;
      let purity = analyzeResult0.output["purity"];
      let p = purity;
    });

    it("scan and invoke yields read-write purity", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"invoke\",\"concept\":\"Article\",\"action\":\"archive\",\"args\":{},\"bindAs\":\"result\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[\"Article/archive\"]}}" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let result = analyzeResult0.output["result"];
      let r = result;
      let readFields = analyzeResult0.output["readFields"];
      let rf = readFields;
      let invokedActions = analyzeResult0.output["invokedActions"];
      let ia = invokedActions;
      let purity = analyzeResult0.output["purity"];
      let p = purity;
    });

    it("empty program yields pure purity", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(queryPurityProviderHandler.analyze({ program: "{\"instructions\":[],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let result = analyzeResult0.output["result"];
      let r = result;
      let readFields = analyzeResult0.output["readFields"];
      let rf = readFields;
      let invokedActions = analyzeResult0.output["invokedActions"];
      let ia = invokedActions;
      let purity = analyzeResult0.output["purity"];
      let p = purity;
    });

    it("get after analyze returns stored result", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(queryPurityProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"scan\",\"source\":\"articles\",\"bindAs\":\"rows\"},{\"tag\":\"filter\",\"predicate\":{\"type\":\"eq\",\"field\":\"status\",\"value\":\"active\"},\"bindAs\":\"filtered\"}],\"terminated\":false,\"effects\":{\"reads\":[],\"writes\":[],\"completionVariants\":[],\"performs\":[],\"invocations\":[]}}" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let result = analyzeResult0.output["result"];
      let r = result;
      let readFields = analyzeResult0.output["readFields"];
      let rf = readFields;
      let invokedActions = analyzeResult0.output["invokedActions"];
      let ia = invokedActions;
      let purity = analyzeResult0.output["purity"];
      let p = purity;
      const thenResult0 = await interpret(queryPurityProviderHandler.get({ result: r }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: purity is one of pure, read-only, or read-write', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('analyze'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ result: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryPurityProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryPurityProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: results with invoked actions are read-write', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('analyze'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ result: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryPurityProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryPurityProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: read-only result with non-empty invoked actions', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('analyze'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ result: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = queryPurityProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(queryPurityProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: read-only result with non-empty invoked actions
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('analyze handles empty input: ', async () => {
      if (typeof queryPurityProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(queryPurityProviderHandler.analyze({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('analyze ensures on ok: ', async () => {
      if (typeof queryPurityProviderHandler.analyze !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = queryPurityProviderHandler.analyze(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('get handles empty input: ', async () => {
      if (typeof queryPurityProviderHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(queryPurityProviderHandler.get({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('get ensures on ok: ', async () => {
      if (typeof queryPurityProviderHandler.get !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ result: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = queryPurityProviderHandler.get(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('get ensures on notfound: ', async () => {
      if (typeof queryPurityProviderHandler.get !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ result: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = queryPurityProviderHandler.get(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "notfound") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
