// ExtensionMessaging Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { extensionMessagingHandler } from '../../handlers/ts/extension/extension-messaging.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ExtensionMessaging functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('registerChannel', () => {
    it('builds a valid StorageProgram', () => {
      const program = extensionMessagingHandler.registerChannel({ name: 'test-name', schema: 'test-schema' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = extensionMessagingHandler.registerChannel({ name: 'test-name', schema: 'test-schema' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = extensionMessagingHandler.registerChannel({ name: 'test-name', schema: 'test-schema' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = extensionMessagingHandler.registerChannel({ name: 'test-name', schema: 'test-schema' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = extensionMessagingHandler.registerChannel({ name: 'test-name', schema: 'test-schema' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof extensionMessagingHandler.registerChannel !== 'function') return;
      const result = await interpret(extensionMessagingHandler.registerChannel({ name: 'test-name', schema: 'test-schema' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('send', () => {
    it('builds a valid StorageProgram', () => {
      const program = extensionMessagingHandler.send({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = extensionMessagingHandler.send({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = extensionMessagingHandler.send({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = extensionMessagingHandler.send({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = extensionMessagingHandler.send({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof extensionMessagingHandler.send !== 'function') return;
      const result = await interpret(extensionMessagingHandler.send({ channel: 'test', sender: 'test-sender', payload: 'test-payload' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('request', () => {
    it('builds a valid StorageProgram', () => {
      const program = extensionMessagingHandler.request({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = extensionMessagingHandler.request({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = extensionMessagingHandler.request({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = extensionMessagingHandler.request({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = extensionMessagingHandler.request({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof extensionMessagingHandler.request !== 'function') return;
      const result = await interpret(extensionMessagingHandler.request({ channel: 'test', sender: 'test-sender', payload: 'test-payload' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('respond', () => {
    it('builds a valid StorageProgram', () => {
      const program = extensionMessagingHandler.respond({ channel: 'test', requestId: 'test-requestId', payload: 'test-payload' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = extensionMessagingHandler.respond({ channel: 'test', requestId: 'test-requestId', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = extensionMessagingHandler.respond({ channel: 'test', requestId: 'test-requestId', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = extensionMessagingHandler.respond({ channel: 'test', requestId: 'test-requestId', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = extensionMessagingHandler.respond({ channel: 'test', requestId: 'test-requestId', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof extensionMessagingHandler.respond !== 'function') return;
      const result = await interpret(extensionMessagingHandler.respond({ channel: 'test', requestId: 'test-requestId', payload: 'test-payload' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('broadcast', () => {
    it('builds a valid StorageProgram', () => {
      const program = extensionMessagingHandler.broadcast({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = extensionMessagingHandler.broadcast({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = extensionMessagingHandler.broadcast({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = extensionMessagingHandler.broadcast({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = extensionMessagingHandler.broadcast({ channel: 'test', sender: 'test-sender', payload: 'test-payload' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof extensionMessagingHandler.broadcast !== 'function') return;
      const result = await interpret(extensionMessagingHandler.broadcast({ channel: 'test', sender: 'test-sender', payload: 'test-payload' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('subscribe', () => {
    it('builds a valid StorageProgram', () => {
      const program = extensionMessagingHandler.subscribe({ channel: 'test', subscriber: 'test-subscriber' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = extensionMessagingHandler.subscribe({ channel: 'test', subscriber: 'test-subscriber' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = extensionMessagingHandler.subscribe({ channel: 'test', subscriber: 'test-subscriber' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = extensionMessagingHandler.subscribe({ channel: 'test', subscriber: 'test-subscriber' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = extensionMessagingHandler.subscribe({ channel: 'test', subscriber: 'test-subscriber' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof extensionMessagingHandler.subscribe !== 'function') return;
      const result = await interpret(extensionMessagingHandler.subscribe({ channel: 'test', subscriber: 'test-subscriber' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('unsubscribe', () => {
    it('builds a valid StorageProgram', () => {
      const program = extensionMessagingHandler.unsubscribe({ channel: 'test', subscriber: 'test-subscriber' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = extensionMessagingHandler.unsubscribe({ channel: 'test', subscriber: 'test-subscriber' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = extensionMessagingHandler.unsubscribe({ channel: 'test', subscriber: 'test-subscriber' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = extensionMessagingHandler.unsubscribe({ channel: 'test', subscriber: 'test-subscriber' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = extensionMessagingHandler.unsubscribe({ channel: 'test', subscriber: 'test-subscriber' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof extensionMessagingHandler.unsubscribe !== 'function') return;
      const result = await interpret(extensionMessagingHandler.unsubscribe({ channel: 'test', subscriber: 'test-subscriber' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof extensionMessagingHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = extensionMessagingHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ExtensionMessaging');
    });
  });

  describe('invariant examples', () => {
    it("registerChannel-then-send", async () => {
      const storage = createInMemoryStorage();
      const registerChannelResult0 = await interpret(extensionMessagingHandler.registerChannel({ name: "test-n", schema: "test-_" }), storage);
      expect(registerChannelResult0.variant).toBe("ok");
      let channel = registerChannelResult0.output["channel"];
      let c = channel;
      const thenResult0 = await interpret(extensionMessagingHandler.subscribe({ channel: c, subscriber: "test-_" }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(extensionMessagingHandler.send({ channel: c, sender: "test-_", payload: "test-_" }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-channelName', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerChannel'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('send'), input: fc.record({ channel: fc.string(), sender: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('request'), input: fc.record({ channel: fc.string(), sender: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('respond'), input: fc.record({ channel: fc.string(), requestId: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('broadcast'), input: fc.record({ channel: fc.string(), sender: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('subscribe'), input: fc.record({ channel: fc.string(), subscriber: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unsubscribe'), input: fc.record({ channel: fc.string(), subscriber: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = extensionMessagingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(extensionMessagingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-schema', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerChannel'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), schema: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('send'), input: fc.record({ channel: fc.string(), sender: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('request'), input: fc.record({ channel: fc.string(), sender: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('respond'), input: fc.record({ channel: fc.string(), requestId: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('broadcast'), input: fc.record({ channel: fc.string(), sender: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('subscribe'), input: fc.record({ channel: fc.string(), subscriber: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('unsubscribe'), input: fc.record({ channel: fc.string(), subscriber: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = extensionMessagingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(extensionMessagingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-schema
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('registerChannel handles empty input: ', async () => {
      if (typeof extensionMessagingHandler.registerChannel !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(extensionMessagingHandler.registerChannel({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('registerChannel ensures on ok: ', async () => {
      if (typeof extensionMessagingHandler.registerChannel !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), schema: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = extensionMessagingHandler.registerChannel(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
