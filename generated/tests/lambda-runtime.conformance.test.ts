// LambdaRuntime Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { lambdaRuntimeHandler } from '../../handlers/ts/deploy/lambda-runtime.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('LambdaRuntime functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('provision', () => {
    it('builds a valid StorageProgram', () => {
      const program = lambdaRuntimeHandler.provision({ concept: 'test-concept', memory: 1, timeout: 1, region: 'test-region' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lambdaRuntimeHandler.provision({ concept: 'test-concept', memory: 1, timeout: 1, region: 'test-region' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lambdaRuntimeHandler.provision({ concept: 'test-concept', memory: 1, timeout: 1, region: 'test-region' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lambdaRuntimeHandler.provision({ concept: 'test-concept', memory: 1, timeout: 1, region: 'test-region' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lambdaRuntimeHandler.provision({ concept: 'test-concept', memory: 1, timeout: 1, region: 'test-region' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof lambdaRuntimeHandler.provision !== 'function') return;
      try {
        const result = await interpret(lambdaRuntimeHandler.provision({ concept: 'test-concept', memory: 1, timeout: 1, region: 'test-region' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('deploy', () => {
    it('builds a valid StorageProgram', () => {
      const program = lambdaRuntimeHandler.deploy({ function: 'test', artifactLocation: 'test-artifactLocation' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lambdaRuntimeHandler.deploy({ function: 'test', artifactLocation: 'test-artifactLocation' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lambdaRuntimeHandler.deploy({ function: 'test', artifactLocation: 'test-artifactLocation' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lambdaRuntimeHandler.deploy({ function: 'test', artifactLocation: 'test-artifactLocation' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lambdaRuntimeHandler.deploy({ function: 'test', artifactLocation: 'test-artifactLocation' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof lambdaRuntimeHandler.deploy !== 'function') return;
      try {
        const result = await interpret(lambdaRuntimeHandler.deploy({ function: 'test', artifactLocation: 'test-artifactLocation' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('setTrafficWeight', () => {
    it('builds a valid StorageProgram', () => {
      const program = lambdaRuntimeHandler.setTrafficWeight({ function: 'test', aliasWeight: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lambdaRuntimeHandler.setTrafficWeight({ function: 'test', aliasWeight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lambdaRuntimeHandler.setTrafficWeight({ function: 'test', aliasWeight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lambdaRuntimeHandler.setTrafficWeight({ function: 'test', aliasWeight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lambdaRuntimeHandler.setTrafficWeight({ function: 'test', aliasWeight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof lambdaRuntimeHandler.setTrafficWeight !== 'function') return;
      try {
        const result = await interpret(lambdaRuntimeHandler.setTrafficWeight({ function: 'test', aliasWeight: 1 }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('rollback', () => {
    it('builds a valid StorageProgram', () => {
      const program = lambdaRuntimeHandler.rollback({ function: 'test', targetVersion: 'test-targetVersion' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lambdaRuntimeHandler.rollback({ function: 'test', targetVersion: 'test-targetVersion' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lambdaRuntimeHandler.rollback({ function: 'test', targetVersion: 'test-targetVersion' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lambdaRuntimeHandler.rollback({ function: 'test', targetVersion: 'test-targetVersion' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lambdaRuntimeHandler.rollback({ function: 'test', targetVersion: 'test-targetVersion' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof lambdaRuntimeHandler.rollback !== 'function') return;
      try {
        const result = await interpret(lambdaRuntimeHandler.rollback({ function: 'test', targetVersion: 'test-targetVersion' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('destroy', () => {
    it('builds a valid StorageProgram', () => {
      const program = lambdaRuntimeHandler.destroy({ function: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = lambdaRuntimeHandler.destroy({ function: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = lambdaRuntimeHandler.destroy({ function: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = lambdaRuntimeHandler.destroy({ function: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = lambdaRuntimeHandler.destroy({ function: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes without crashing', async () => {
      if (typeof lambdaRuntimeHandler.destroy !== 'function') return;
      try {
        const result = await interpret(lambdaRuntimeHandler.destroy({ function: 'test' }), storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("provision-then-deploy", async () => {
      const storage = createInMemoryStorage();
      const provisionResult0 = await interpret(lambdaRuntimeHandler.provision({ concept: {"type":"literal","value":"User"}, memory: {"type":"literal","value":256}, timeout: {"type":"literal","value":30}, region: {"type":"literal","value":"us-east-1"} }), storage);
      expect(provisionResult0.variant).toBe("ok");
      const function = provisionResult0.output["function"];
      const functionArn = provisionResult0.output["functionArn"];
      const endpoint = provisionResult0.output["endpoint"];
      const thenResult0 = await interpret(lambdaRuntimeHandler.deploy({ function: {"type":"variable","name":"f"}, artifactLocation: {"type":"literal","value":"s3://bucket/user.zip"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-functionArn', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('provision'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), memory: fc.integer({ min: 1, max: 1000 }), timeout: fc.integer({ min: 1, max: 1000 }), region: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deploy'), input: fc.record({ function: fc.string(), artifactLocation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setTrafficWeight'), input: fc.record({ function: fc.string(), aliasWeight: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ function: fc.string(), targetVersion: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('destroy'), input: fc.record({ function: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = lambdaRuntimeHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(lambdaRuntimeHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-roleArn', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('provision'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), memory: fc.integer({ min: 1, max: 1000 }), timeout: fc.integer({ min: 1, max: 1000 }), region: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deploy'), input: fc.record({ function: fc.string(), artifactLocation: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('setTrafficWeight'), input: fc.record({ function: fc.string(), aliasWeight: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('rollback'), input: fc.record({ function: fc.string(), targetVersion: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('destroy'), input: fc.record({ function: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = lambdaRuntimeHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const program = actionFn.call(lambdaRuntimeHandler, step.input as Record<string, unknown>);
                  const result = await interpret(program, storage);
                  expect(result.variant).toBeDefined();
                  // Never: orphaned-roleArn
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('provision handles empty input: ', async () => {
      if (typeof lambdaRuntimeHandler.provision !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(lambdaRuntimeHandler.provision({  }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('provision ensures on ok: ', async () => {
      if (typeof lambdaRuntimeHandler.provision !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), memory: fc.integer({ min: 1, max: 1000 }), timeout: fc.integer({ min: 1, max: 1000 }), region: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = lambdaRuntimeHandler.provision(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
