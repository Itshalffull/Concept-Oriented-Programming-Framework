// RuntimeFlow Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { runtimeFlowHandler } from '../../handlers/ts/score/runtime-flow.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('RuntimeFlow functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('correlate', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeFlowHandler.correlate({ flowId: "f-123" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeFlowHandler.correlate({ flowId: "f-123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeFlowHandler.correlate({ flowId: "f-123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeFlowHandler.correlate({ flowId: "f-123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeFlowHandler.correlate({ flowId: "f-123" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeFlowHandler.correlate !== 'function') return;
      const result = await interpret(runtimeFlowHandler.correlate({ flowId: "f-123" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "correlate_flow" -> ok', async () => {
      if (typeof runtimeFlowHandler.correlate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_compare_flow = await interpret(runtimeFlowHandler.compareToStatic({ flow: "runtime-flow-1" }), storage);
      const _pool = Object.assign({}, (afterResult_compare_flow?.output ?? {}));
      const _fixtureInput = { flowId: "f-123" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(runtimeFlowHandler.correlate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "correlate_empty" -> error', async () => {
      if (typeof runtimeFlowHandler.correlate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeFlowHandler.correlate({ flowId: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByAction', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeFlowHandler.findByAction({ action: "User/create", since: "2026-01-01T00:00:00Z" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeFlowHandler.findByAction({ action: "User/create", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeFlowHandler.findByAction({ action: "User/create", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeFlowHandler.findByAction({ action: "User/create", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeFlowHandler.findByAction({ action: "User/create", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeFlowHandler.findByAction !== 'function') return;
      const result = await interpret(runtimeFlowHandler.findByAction({ action: "User/create", since: "2026-01-01T00:00:00Z" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_create" -> ok', async () => {
      if (typeof runtimeFlowHandler.findByAction !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_correlate_flow = await interpret(runtimeFlowHandler.correlate({ flowId: "f-123" }), storage);
      const _pool = Object.assign({}, (afterResult_correlate_flow?.output ?? {}));
      const _fixtureInput = { action: "User/create", since: "2026-01-01T00:00:00Z" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(runtimeFlowHandler.findByAction({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_empty" -> error', async () => {
      if (typeof runtimeFlowHandler.findByAction !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeFlowHandler.findByAction({ action: "", since: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findBySync', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeFlowHandler.findBySync({ sync: "onUserCreate", since: "2026-01-01T00:00:00Z" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeFlowHandler.findBySync({ sync: "onUserCreate", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeFlowHandler.findBySync({ sync: "onUserCreate", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeFlowHandler.findBySync({ sync: "onUserCreate", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeFlowHandler.findBySync({ sync: "onUserCreate", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeFlowHandler.findBySync !== 'function') return;
      const result = await interpret(runtimeFlowHandler.findBySync({ sync: "onUserCreate", since: "2026-01-01T00:00:00Z" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_sync" -> ok', async () => {
      if (typeof runtimeFlowHandler.findBySync !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_correlate_flow = await interpret(runtimeFlowHandler.correlate({ flowId: "f-123" }), storage);
      const _pool = Object.assign({}, (afterResult_correlate_flow?.output ?? {}));
      const _fixtureInput = { sync: "onUserCreate", since: "2026-01-01T00:00:00Z" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(runtimeFlowHandler.findBySync({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_empty_sync" -> error', async () => {
      if (typeof runtimeFlowHandler.findBySync !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeFlowHandler.findBySync({ sync: "", since: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByVariant', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeFlowHandler.findByVariant({ variant: "ok", since: "2026-01-01T00:00:00Z" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeFlowHandler.findByVariant({ variant: "ok", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeFlowHandler.findByVariant({ variant: "ok", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeFlowHandler.findByVariant({ variant: "ok", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeFlowHandler.findByVariant({ variant: "ok", since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeFlowHandler.findByVariant !== 'function') return;
      const result = await interpret(runtimeFlowHandler.findByVariant({ variant: "ok", since: "2026-01-01T00:00:00Z" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_ok" -> ok', async () => {
      if (typeof runtimeFlowHandler.findByVariant !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_correlate_flow = await interpret(runtimeFlowHandler.correlate({ flowId: "f-123" }), storage);
      const _pool = Object.assign({}, (afterResult_correlate_flow?.output ?? {}));
      const _fixtureInput = { variant: "ok", since: "2026-01-01T00:00:00Z" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(runtimeFlowHandler.findByVariant({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_empty_variant" -> error', async () => {
      if (typeof runtimeFlowHandler.findByVariant !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeFlowHandler.findByVariant({ variant: "", since: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findFailures', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeFlowHandler.findFailures({ since: "2026-01-01T00:00:00Z" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeFlowHandler.findFailures({ since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeFlowHandler.findFailures({ since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeFlowHandler.findFailures({ since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeFlowHandler.findFailures({ since: "2026-01-01T00:00:00Z" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeFlowHandler.findFailures !== 'function') return;
      const result = await interpret(runtimeFlowHandler.findFailures({ since: "2026-01-01T00:00:00Z" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_recent" -> ok', async () => {
      if (typeof runtimeFlowHandler.findFailures !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_correlate_flow = await interpret(runtimeFlowHandler.correlate({ flowId: "f-123" }), storage);
      const _pool = Object.assign({}, (afterResult_correlate_flow?.output ?? {}));
      const _fixtureInput = { since: "2026-01-01T00:00:00Z" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(runtimeFlowHandler.findFailures({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_all" -> ok', async () => {
      if (typeof runtimeFlowHandler.findFailures !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_correlate_flow = await interpret(runtimeFlowHandler.correlate({ flowId: "f-123" }), storage);
      const _pool = Object.assign({}, (afterResult_correlate_flow?.output ?? {}));
      const _fixtureInput = { since: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(runtimeFlowHandler.findFailures({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('compareToStatic', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeFlowHandler.compareToStatic({ flow: "runtime-flow-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeFlowHandler.compareToStatic({ flow: "runtime-flow-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeFlowHandler.compareToStatic({ flow: "runtime-flow-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeFlowHandler.compareToStatic({ flow: "runtime-flow-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeFlowHandler.compareToStatic({ flow: "runtime-flow-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeFlowHandler.compareToStatic !== 'function') return;
      const result = await interpret(runtimeFlowHandler.compareToStatic({ flow: "runtime-flow-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compare_flow" -> ok', async () => {
      if (typeof runtimeFlowHandler.compareToStatic !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_correlate_flow = await interpret(runtimeFlowHandler.correlate({ flowId: "f-123" }), storage);
      const _pool = Object.assign({}, (afterResult_correlate_flow?.output ?? {}));
      const _fixtureInput = { flow: "runtime-flow-1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(runtimeFlowHandler.compareToStatic({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compare_missing" -> error', async () => {
      if (typeof runtimeFlowHandler.compareToStatic !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeFlowHandler.compareToStatic({ flow: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('sourceLocations', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeFlowHandler.sourceLocations({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeFlowHandler.sourceLocations({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeFlowHandler.sourceLocations({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeFlowHandler.sourceLocations({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeFlowHandler.sourceLocations({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeFlowHandler.sourceLocations !== 'function') return;
      const result = await interpret(runtimeFlowHandler.sourceLocations({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "locations_flow" -> ok', async () => {
      if (typeof runtimeFlowHandler.sourceLocations !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_correlate_flow = await interpret(runtimeFlowHandler.correlate({ flowId: "f-123" }), storage);
      const result = await interpret(runtimeFlowHandler.sourceLocations({ flow: afterResult_correlate_flow?.output?.["flow"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "locations_missing" -> error', async () => {
      if (typeof runtimeFlowHandler.sourceLocations !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeFlowHandler.sourceLocations({ flow: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = runtimeFlowHandler.get({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = runtimeFlowHandler.get({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = runtimeFlowHandler.get({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = runtimeFlowHandler.get({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = runtimeFlowHandler.get({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof runtimeFlowHandler.get !== 'function') return;
      const result = await interpret(runtimeFlowHandler.get({ flow: {"type":"ref","fixture":"correlate_flow","field":"flow"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_flow" -> ok', async () => {
      if (typeof runtimeFlowHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_correlate_flow = await interpret(runtimeFlowHandler.correlate({ flowId: "f-123" }), storage);
      const result = await interpret(runtimeFlowHandler.get({ flow: afterResult_correlate_flow?.output?.["flow"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> error', async () => {
      if (typeof runtimeFlowHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(runtimeFlowHandler.get({ flow: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof runtimeFlowHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = runtimeFlowHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('RuntimeFlow');
    });
  });

  describe('invariant examples', () => {
    it("correlated flow is retrievable", async () => {
      const storage = createInMemoryStorage();
      const correlateResult0 = await interpret(runtimeFlowHandler.correlate({ flowId: {"type":"literal","value":"f-123"} }), storage);
      expect(correlateResult0.variant).toBe("ok");
      let flow = correlateResult0.output["flow"];
      const thenResult0 = await interpret(runtimeFlowHandler.get({ flow: {"type":"variable","name":"f"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every flows entry has flowId', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('correlate'), input: fc.record({ flowId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByAction'), input: fc.record({ action: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBySync'), input: fc.record({ sync: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByVariant'), input: fc.record({ variant: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findFailures'), input: fc.record({ since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compareToStatic'), input: fc.record({ flow: fc.string() }) }),
              fc.record({ action: fc.constant('sourceLocations'), input: fc.record({ flow: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ flow: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = runtimeFlowHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(runtimeFlowHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty flowId in flows', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('correlate'), input: fc.record({ flowId: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByAction'), input: fc.record({ action: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findBySync'), input: fc.record({ sync: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByVariant'), input: fc.record({ variant: fc.string({ minLength: 1, maxLength: 50 }), since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findFailures'), input: fc.record({ since: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('compareToStatic'), input: fc.record({ flow: fc.string() }) }),
              fc.record({ action: fc.constant('sourceLocations'), input: fc.record({ flow: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ flow: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = runtimeFlowHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(runtimeFlowHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty flowId in flows
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('correlate handles empty input: ', async () => {
      if (typeof runtimeFlowHandler.correlate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(runtimeFlowHandler.correlate({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('correlate ensures on ok: ', async () => {
      if (typeof runtimeFlowHandler.correlate !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ flowId: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = runtimeFlowHandler.correlate(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
