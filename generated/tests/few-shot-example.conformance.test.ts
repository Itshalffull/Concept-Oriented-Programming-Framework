// FewShotExample Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { fewShotExampleHandler } from '../../handlers/ts/llm-prompt/few-shot-example.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('FewShotExample functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('createPool', () => {
    it('builds a valid StorageProgram', () => {
      const program = fewShotExampleHandler.createPool({ strategy: 'test-strategy', k: 1, diversity_weight: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fewShotExampleHandler.createPool({ strategy: 'test-strategy', k: 1, diversity_weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fewShotExampleHandler.createPool({ strategy: 'test-strategy', k: 1, diversity_weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fewShotExampleHandler.createPool({ strategy: 'test-strategy', k: 1, diversity_weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fewShotExampleHandler.createPool({ strategy: 'test-strategy', k: 1, diversity_weight: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fewShotExampleHandler.createPool !== 'function') return;
      const result = await interpret(fewShotExampleHandler.createPool({ strategy: 'test-strategy', k: 1, diversity_weight: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('add', () => {
    it('builds a valid StorageProgram', () => {
      const program = fewShotExampleHandler.add({ pool: 'test', input: 'test-input', output: 'test-output', metadata: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fewShotExampleHandler.add({ pool: 'test', input: 'test-input', output: 'test-output', metadata: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fewShotExampleHandler.add({ pool: 'test', input: 'test-input', output: 'test-output', metadata: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fewShotExampleHandler.add({ pool: 'test', input: 'test-input', output: 'test-output', metadata: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fewShotExampleHandler.add({ pool: 'test', input: 'test-input', output: 'test-output', metadata: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fewShotExampleHandler.add !== 'function') return;
      const result = await interpret(fewShotExampleHandler.add({ pool: 'test', input: 'test-input', output: 'test-output', metadata: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('select', () => {
    it('builds a valid StorageProgram', () => {
      const program = fewShotExampleHandler.select({ pool: 'test', input: 'test-input', k: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fewShotExampleHandler.select({ pool: 'test', input: 'test-input', k: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fewShotExampleHandler.select({ pool: 'test', input: 'test-input', k: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fewShotExampleHandler.select({ pool: 'test', input: 'test-input', k: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fewShotExampleHandler.select({ pool: 'test', input: 'test-input', k: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fewShotExampleHandler.select !== 'function') return;
      const result = await interpret(fewShotExampleHandler.select({ pool: 'test', input: 'test-input', k: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('optimize', () => {
    it('builds a valid StorageProgram', () => {
      const program = fewShotExampleHandler.optimize({ pool: 'test', metric: 'test-metric', training_set: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fewShotExampleHandler.optimize({ pool: 'test', metric: 'test-metric', training_set: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fewShotExampleHandler.optimize({ pool: 'test', metric: 'test-metric', training_set: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fewShotExampleHandler.optimize({ pool: 'test', metric: 'test-metric', training_set: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fewShotExampleHandler.optimize({ pool: 'test', metric: 'test-metric', training_set: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fewShotExampleHandler.optimize !== 'function') return;
      const result = await interpret(fewShotExampleHandler.optimize({ pool: 'test', metric: 'test-metric', training_set: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('embed', () => {
    it('builds a valid StorageProgram', () => {
      const program = fewShotExampleHandler.embed({ pool: 'test', model_id: 'test-model_id' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fewShotExampleHandler.embed({ pool: 'test', model_id: 'test-model_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fewShotExampleHandler.embed({ pool: 'test', model_id: 'test-model_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fewShotExampleHandler.embed({ pool: 'test', model_id: 'test-model_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fewShotExampleHandler.embed({ pool: 'test', model_id: 'test-model_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fewShotExampleHandler.embed !== 'function') return;
      const result = await interpret(fewShotExampleHandler.embed({ pool: 'test', model_id: 'test-model_id' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('remove', () => {
    it('builds a valid StorageProgram', () => {
      const program = fewShotExampleHandler.remove({ pool: 'test', example_id: 'test-example_id' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fewShotExampleHandler.remove({ pool: 'test', example_id: 'test-example_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = fewShotExampleHandler.remove({ pool: 'test', example_id: 'test-example_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = fewShotExampleHandler.remove({ pool: 'test', example_id: 'test-example_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fewShotExampleHandler.remove({ pool: 'test', example_id: 'test-example_id' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof fewShotExampleHandler.remove !== 'function') return;
      const result = await interpret(fewShotExampleHandler.remove({ pool: 'test', example_id: 'test-example_id' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof fewShotExampleHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = fewShotExampleHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('FewShotExample');
    });
  });

  describe('invariant examples', () => {
    it("createPool-then-select", async () => {
      const storage = createInMemoryStorage();
      const createPoolResult0 = await interpret(fewShotExampleHandler.createPool({ strategy: "semantic_similarity", k: 3, diversity_weight: 0.5 }), storage);
      expect(createPoolResult0.variant).toBe("ok");
      let pool = createPoolResult0.output["pool"];
      let f = pool;
      const addResult1 = await interpret(fewShotExampleHandler.add({ pool: f, input: "What is 2+2?", output: "4", metadata: "test-_" }), storage);
      expect(addResult1.variant).toBe("ok");
      let example_id = addResult1.output["example_id"];
      let _ = example_id;
      const thenResult0 = await interpret(fewShotExampleHandler.select({ pool: f, input: "What is 3+3?", k: _ }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-metadata', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('createPool'), input: fc.record({ strategy: fc.string({ minLength: 1, maxLength: 50 }), k: fc.integer({ min: 1, max: 1000 }), diversity_weight: fc.string() }) }),
              fc.record({ action: fc.constant('add'), input: fc.record({ pool: fc.string(), input: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }), metadata: fc.string() }) }),
              fc.record({ action: fc.constant('select'), input: fc.record({ pool: fc.string(), input: fc.string({ minLength: 1, maxLength: 50 }), k: fc.string() }) }),
              fc.record({ action: fc.constant('optimize'), input: fc.record({ pool: fc.string(), metric: fc.string({ minLength: 1, maxLength: 50 }), training_set: fc.string() }) }),
              fc.record({ action: fc.constant('embed'), input: fc.record({ pool: fc.string(), model_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ pool: fc.string(), example_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = fewShotExampleHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(fewShotExampleHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('add handles empty input: ', async () => {
      if (typeof fewShotExampleHandler.add !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(fewShotExampleHandler.add({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('add ensures on ok: ', async () => {
      if (typeof fewShotExampleHandler.add !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ pool: fc.string(), input: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }), metadata: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = fewShotExampleHandler.add(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
