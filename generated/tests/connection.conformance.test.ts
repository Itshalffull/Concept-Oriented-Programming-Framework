// Connection Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { connectionHandler } from '../../handlers/ts/bind/connection.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Connection functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('connect', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectionHandler.connect !== 'function') return;
      const result = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "connect_ws" -> ok', async () => {
      if (typeof connectionHandler.connect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "connect_http" -> ok', async () => {
      if (typeof connectionHandler.connect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectionHandler.connect({ connection: "k-002", endpoint: "http://localhost:4000/kernel", transportAdapter: "http", credentials: "Bearer tok-abc123" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "connect_in_process" -> ok', async () => {
      if (typeof connectionHandler.connect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectionHandler.connect({ connection: "k-003", endpoint: "in-process://default", transportAdapter: "in-process", credentials: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "connect_unreachable" -> unreachable', async () => {
      if (typeof connectionHandler.connect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectionHandler.connect({ connection: "k-bad", endpoint: "ws://localhost:9999/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unreachable'));
    });

    it('fixture "connect_unauthorized" -> unauthorized', async () => {
      if (typeof connectionHandler.connect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectionHandler.connect({ connection: "k-auth", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: "Bearer invalid" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unauthorized'));
    });

  });

  describe('discover', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectionHandler.discover({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, depth: "list", concept: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectionHandler.discover({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, depth: "list", concept: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectionHandler.discover({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, depth: "list", concept: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectionHandler.discover({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, depth: "list", concept: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectionHandler.discover({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, depth: "list", concept: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectionHandler.discover !== 'function') return;
      const result = await interpret(connectionHandler.discover({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, depth: "list", concept: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "discover_list" -> ok', async () => {
      if (typeof connectionHandler.discover !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.discover({ connection: afterResult_connect_ws?.output?.["connection"], depth: "list", concept: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "discover_manifest" -> ok', async () => {
      if (typeof connectionHandler.discover !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.discover({ connection: afterResult_connect_ws?.output?.["connection"], depth: "manifest", concept: "Task" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "discover_full" -> ok', async () => {
      if (typeof connectionHandler.discover !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.discover({ connection: afterResult_connect_ws?.output?.["connection"], depth: "full", concept: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "discover_disconnected" -> disconnected', async () => {
      if (typeof connectionHandler.discover !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectionHandler.discover({ connection: "k-never-connected", depth: "list", concept: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('disconnected'));
    });

  });

  describe('invoke', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectionHandler.invoke({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: "create", input: "{\"name\":\"Write specs\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectionHandler.invoke({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: "create", input: "{\"name\":\"Write specs\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectionHandler.invoke({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: "create", input: "{\"name\":\"Write specs\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectionHandler.invoke({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: "create", input: "{\"name\":\"Write specs\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectionHandler.invoke({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: "create", input: "{\"name\":\"Write specs\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectionHandler.invoke !== 'function') return;
      const result = await interpret(connectionHandler.invoke({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: "create", input: "{\"name\":\"Write specs\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "invoke_ok" -> ok', async () => {
      if (typeof connectionHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.invoke({ connection: afterResult_connect_ws?.output?.["connection"], concept: "Task", action: "create", input: "{\"name\":\"Write specs\"}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invoke_not_found_concept" -> not_found', async () => {
      if (typeof connectionHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.invoke({ connection: afterResult_connect_ws?.output?.["connection"], concept: "NonExistent", action: "create", input: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

    it('fixture "invoke_not_found_action" -> not_found', async () => {
      if (typeof connectionHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.invoke({ connection: afterResult_connect_ws?.output?.["connection"], concept: "Task", action: "nonExistentAction", input: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

    it('fixture "invoke_unauthorized" -> unauthorized', async () => {
      if (typeof connectionHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.invoke({ connection: afterResult_connect_ws?.output?.["connection"], concept: "AdminPanel", action: "delete", input: "{\"id\":\"item-1\"}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('unauthorized'));
    });

    it('fixture "invoke_disconnected" -> error', async () => {
      if (typeof connectionHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectionHandler.invoke({ connection: "k-never-connected", concept: "Task", action: "create", input: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('observe', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectionHandler.observe({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectionHandler.observe({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectionHandler.observe({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectionHandler.observe({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectionHandler.observe({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectionHandler.observe !== 'function') return;
      const result = await interpret(connectionHandler.observe({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"}, concept: "Task", action: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "observe_all_actions" -> ok', async () => {
      if (typeof connectionHandler.observe !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.observe({ connection: afterResult_connect_ws?.output?.["connection"], concept: "Task", action: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "observe_specific_action" -> ok', async () => {
      if (typeof connectionHandler.observe !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.observe({ connection: afterResult_connect_ws?.output?.["connection"], concept: "Task", action: "create" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "observe_not_supported" -> not_supported', async () => {
      if (typeof connectionHandler.observe !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_http = await interpret(connectionHandler.connect({ connection: "k-002", endpoint: "http://localhost:4000/kernel", transportAdapter: "http", credentials: "Bearer tok-abc123" }), storage);
      const result = await interpret(connectionHandler.observe({ connection: afterResult_connect_http?.output?.["connection"], concept: "Task", action: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_supported'));
    });

    it('fixture "observe_disconnected" -> disconnected', async () => {
      if (typeof connectionHandler.observe !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(connectionHandler.observe({ connection: "k-never-connected", concept: "Task", action: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('disconnected'));
    });

  });

  describe('disconnect', () => {
    it('builds a valid StorageProgram', () => {
      const program = connectionHandler.disconnect({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = connectionHandler.disconnect({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = connectionHandler.disconnect({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = connectionHandler.disconnect({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = connectionHandler.disconnect({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof connectionHandler.disconnect !== 'function') return;
      const result = await interpret(connectionHandler.disconnect({ connection: {"type":"ref","fixture":"connect_ws","field":"connection"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "disconnect_ok" -> ok', async () => {
      if (typeof connectionHandler.disconnect !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_connect_ws = await interpret(connectionHandler.connect({ connection: "k-001", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: null }), storage);
      const result = await interpret(connectionHandler.disconnect({ connection: afterResult_connect_ws?.output?.["connection"] }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof connectionHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = connectionHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Connection');
    });
  });

  describe('invariant examples', () => {
    it("connect then discover concept list", async () => {
      const storage = createInMemoryStorage();
      const connectResult0 = await interpret(connectionHandler.connect({ connection: "test-k", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: false }), storage);
      expect(connectResult0.variant).toBe("ok");
      let connection = connectResult0.output["connection"];
      let k = connection;
      const thenResult0 = await interpret(connectionHandler.discover({ connection: k, depth: "list", concept: false }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("connect then invoke an action", async () => {
      const storage = createInMemoryStorage();
      const connectResult0 = await interpret(connectionHandler.connect({ connection: "test-k", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: false }), storage);
      expect(connectResult0.variant).toBe("ok");
      let connection = connectResult0.output["connection"];
      let k = connection;
      const thenResult0 = await interpret(connectionHandler.invoke({ connection: k, concept: "Task", action: "create", input: "{\"name\":\"Write specs\"}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("connect then observe a concept stream", async () => {
      const storage = createInMemoryStorage();
      const connectResult0 = await interpret(connectionHandler.connect({ connection: "test-k", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: false }), storage);
      expect(connectResult0.variant).toBe("ok");
      let connection = connectResult0.output["connection"];
      let k = connection;
      const thenResult0 = await interpret(connectionHandler.observe({ connection: k, concept: "Task", action: false }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("invoke after disconnect fails", async () => {
      const storage = createInMemoryStorage();
      const connectResult0 = await interpret(connectionHandler.connect({ connection: "test-k", endpoint: "ws://localhost:3000/kernel", transportAdapter: "websocket", credentials: false }), storage);
      expect(connectResult0.variant).toBe("ok");
      let connection = connectResult0.output["connection"];
      let k = connection;
      const disconnectResult1 = await interpret(connectionHandler.disconnect({ connection: k }), storage);
      expect(disconnectResult1.variant).toBe("ok");
      connection = disconnectResult1.output["connection"];
      const thenResult0 = await interpret(connectionHandler.invoke({ connection: k, concept: "Task", action: "create", input: "{}" }), storage);
      expect(thenResult0.variant).toBe("error");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: connected session has registered concepts', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('connect'), input: fc.record({ connection: fc.string(), endpoint: fc.string({ minLength: 1, maxLength: 50 }), transportAdapter: fc.string({ minLength: 1, maxLength: 50 }), credentials: fc.string() }) }),
              fc.record({ action: fc.constant('discover'), input: fc.record({ connection: fc.string(), concept: fc.string(), depth: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ connection: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('observe'), input: fc.record({ connection: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string() }) }),
              fc.record({ action: fc.constant('disconnect'), input: fc.record({ connection: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = connectionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(connectionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: session token without connected status', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('connect'), input: fc.record({ connection: fc.string(), endpoint: fc.string({ minLength: 1, maxLength: 50 }), transportAdapter: fc.string({ minLength: 1, maxLength: 50 }), credentials: fc.string() }) }),
              fc.record({ action: fc.constant('discover'), input: fc.record({ connection: fc.string(), concept: fc.string(), depth: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ connection: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('observe'), input: fc.record({ connection: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string() }) }),
              fc.record({ action: fc.constant('disconnect'), input: fc.record({ connection: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = connectionHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(connectionHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: session token without connected status
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('connect handles empty input: ', async () => {
      if (typeof connectionHandler.connect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(connectionHandler.connect({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('connect ensures on ok: ', async () => {
      if (typeof connectionHandler.connect !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ connection: fc.string(), endpoint: fc.string({ minLength: 1, maxLength: 50 }), transportAdapter: fc.string({ minLength: 1, maxLength: 50 }), credentials: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = connectionHandler.connect(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('invoke handles empty input: ', async () => {
      if (typeof connectionHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(connectionHandler.invoke({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('invoke ensures on ok: ', async () => {
      if (typeof connectionHandler.invoke !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ connection: fc.string(), concept: fc.string({ minLength: 1, maxLength: 50 }), action: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = connectionHandler.invoke(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('disconnect handles empty input: ', async () => {
      if (typeof connectionHandler.disconnect !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(connectionHandler.disconnect({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('disconnect ensures on ok: ', async () => {
      if (typeof connectionHandler.disconnect !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ connection: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = connectionHandler.disconnect(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
