// Blackboard Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { blackboardHandler } from '../../handlers/ts/llm-agent/blackboard.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Blackboard functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('builds a valid StorageProgram', () => {
      const program = blackboardHandler.create({ entry_schema: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = blackboardHandler.create({ entry_schema: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = blackboardHandler.create({ entry_schema: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = blackboardHandler.create({ entry_schema: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = blackboardHandler.create({ entry_schema: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof blackboardHandler.create !== 'function') return;
      const result = await interpret(blackboardHandler.create({ entry_schema: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('post', () => {
    it('builds a valid StorageProgram', () => {
      const program = blackboardHandler.post({ board: 'test', agent_id: 'test-agent_id', entry_type: 'test-entry_type', content: 'test-content', confidence: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = blackboardHandler.post({ board: 'test', agent_id: 'test-agent_id', entry_type: 'test-entry_type', content: 'test-content', confidence: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = blackboardHandler.post({ board: 'test', agent_id: 'test-agent_id', entry_type: 'test-entry_type', content: 'test-content', confidence: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = blackboardHandler.post({ board: 'test', agent_id: 'test-agent_id', entry_type: 'test-entry_type', content: 'test-content', confidence: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = blackboardHandler.post({ board: 'test', agent_id: 'test-agent_id', entry_type: 'test-entry_type', content: 'test-content', confidence: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof blackboardHandler.post !== 'function') return;
      const result = await interpret(blackboardHandler.post({ board: 'test', agent_id: 'test-agent_id', entry_type: 'test-entry_type', content: 'test-content', confidence: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('query', () => {
    it('builds a valid StorageProgram', () => {
      const program = blackboardHandler.query({ board: 'test', entry_type: 'test', filters: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = blackboardHandler.query({ board: 'test', entry_type: 'test', filters: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = blackboardHandler.query({ board: 'test', entry_type: 'test', filters: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = blackboardHandler.query({ board: 'test', entry_type: 'test', filters: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = blackboardHandler.query({ board: 'test', entry_type: 'test', filters: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof blackboardHandler.query !== 'function') return;
      const result = await interpret(blackboardHandler.query({ board: 'test', entry_type: 'test', filters: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('subscribe', () => {
    it('builds a valid StorageProgram', () => {
      const program = blackboardHandler.subscribe({ board: 'test', agent_id: 'test-agent_id', entry_types: 'test', condition: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = blackboardHandler.subscribe({ board: 'test', agent_id: 'test-agent_id', entry_types: 'test', condition: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = blackboardHandler.subscribe({ board: 'test', agent_id: 'test-agent_id', entry_types: 'test', condition: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = blackboardHandler.subscribe({ board: 'test', agent_id: 'test-agent_id', entry_types: 'test', condition: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = blackboardHandler.subscribe({ board: 'test', agent_id: 'test-agent_id', entry_types: 'test', condition: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof blackboardHandler.subscribe !== 'function') return;
      const result = await interpret(blackboardHandler.subscribe({ board: 'test', agent_id: 'test-agent_id', entry_types: 'test', condition: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('challenge', () => {
    it('builds a valid StorageProgram', () => {
      const program = blackboardHandler.challenge({ board: 'test', entry_id: 'test-entry_id', challenger_agent_id: 'test-challenger_agent_id', counter_evidence: 'test-counter_evidence' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = blackboardHandler.challenge({ board: 'test', entry_id: 'test-entry_id', challenger_agent_id: 'test-challenger_agent_id', counter_evidence: 'test-counter_evidence' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = blackboardHandler.challenge({ board: 'test', entry_id: 'test-entry_id', challenger_agent_id: 'test-challenger_agent_id', counter_evidence: 'test-counter_evidence' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = blackboardHandler.challenge({ board: 'test', entry_id: 'test-entry_id', challenger_agent_id: 'test-challenger_agent_id', counter_evidence: 'test-counter_evidence' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = blackboardHandler.challenge({ board: 'test', entry_id: 'test-entry_id', challenger_agent_id: 'test-challenger_agent_id', counter_evidence: 'test-counter_evidence' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof blackboardHandler.challenge !== 'function') return;
      const result = await interpret(blackboardHandler.challenge({ board: 'test', entry_id: 'test-entry_id', challenger_agent_id: 'test-challenger_agent_id', counter_evidence: 'test-counter_evidence' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = blackboardHandler.resolve({ board: 'test', entry_ids: 'test', strategy: 'test-strategy' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = blackboardHandler.resolve({ board: 'test', entry_ids: 'test', strategy: 'test-strategy' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = blackboardHandler.resolve({ board: 'test', entry_ids: 'test', strategy: 'test-strategy' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = blackboardHandler.resolve({ board: 'test', entry_ids: 'test', strategy: 'test-strategy' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = blackboardHandler.resolve({ board: 'test', entry_ids: 'test', strategy: 'test-strategy' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof blackboardHandler.resolve !== 'function') return;
      const result = await interpret(blackboardHandler.resolve({ board: 'test', entry_ids: 'test', strategy: 'test-strategy' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('snapshot', () => {
    it('builds a valid StorageProgram', () => {
      const program = blackboardHandler.snapshot({ board: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = blackboardHandler.snapshot({ board: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = blackboardHandler.snapshot({ board: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = blackboardHandler.snapshot({ board: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = blackboardHandler.snapshot({ board: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof blackboardHandler.snapshot !== 'function') return;
      const result = await interpret(blackboardHandler.snapshot({ board: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof blackboardHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = blackboardHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Blackboard');
    });
  });

  describe('invariant examples', () => {
    it("create-then-query", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await interpret(blackboardHandler.create({ entry_schema: [{"entry_type":"finding","schema":"{\"type\": \"object\"}"}] }), storage);
      expect(createResult0.variant).toBe("ok");
      let board = createResult0.output["board"];
      let b = board;
      const postResult1 = await interpret(blackboardHandler.post({ board: b, agent_id: "a1", entry_type: "finding", content: "{\"fact\": \"X\"}", confidence: 0.9 }), storage);
      expect(postResult1.variant).toBe("ok");
      let entry_id = postResult1.output["entry_id"];
      let _ = entry_id;
      const thenResult0 = await interpret(blackboardHandler.query({ board: b, entry_type: "finding", filters: _ }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-content', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ entry_schema: fc.string() }) }),
              fc.record({ action: fc.constant('post'), input: fc.record({ board: fc.string(), agent_id: fc.string({ minLength: 1, maxLength: 50 }), entry_type: fc.string({ minLength: 1, maxLength: 50 }), content: fc.string({ minLength: 1, maxLength: 50 }), confidence: fc.string() }) }),
              fc.record({ action: fc.constant('query'), input: fc.record({ board: fc.string(), entry_type: fc.string(), filters: fc.string() }) }),
              fc.record({ action: fc.constant('subscribe'), input: fc.record({ board: fc.string(), agent_id: fc.string({ minLength: 1, maxLength: 50 }), entry_types: fc.string(), condition: fc.string() }) }),
              fc.record({ action: fc.constant('challenge'), input: fc.record({ board: fc.string(), entry_id: fc.string({ minLength: 1, maxLength: 50 }), challenger_agent_id: fc.string({ minLength: 1, maxLength: 50 }), counter_evidence: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ board: fc.string(), entry_ids: fc.string(), strategy: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('snapshot'), input: fc.record({ board: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = blackboardHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(blackboardHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof blackboardHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(blackboardHandler.create({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('create ensures on ok: ', async () => {
      if (typeof blackboardHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ entry_schema: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = blackboardHandler.create(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
