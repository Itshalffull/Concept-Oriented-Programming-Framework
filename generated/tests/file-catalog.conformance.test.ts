// FileCatalog Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { fileCatalogHandler } from '../../handlers/ts/app/file-catalog.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('FileCatalog functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('discover', () => {
    it('builds a valid StorageProgram', () => {
      const program = fileCatalogHandler.discover({ base_paths: 'test-base_paths' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fileCatalogHandler.discover({ base_paths: 'test-base_paths' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = fileCatalogHandler.discover({ base_paths: 'test-base_paths' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
      expect(variants).toContain('error');
    });

    it('declares read and write sets', () => {
      const program = fileCatalogHandler.discover({ base_paths: 'test-base_paths' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fileCatalogHandler.discover({ base_paths: 'test-base_paths' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof fileCatalogHandler.discover !== 'function') return;
      const result = await interpret(fileCatalogHandler.discover({ base_paths: 'test-base_paths' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('registerProvider', () => {
    it('builds a valid StorageProgram', () => {
      const program = fileCatalogHandler.registerProvider({ provider_name: 'test-provider_name', kind: 'test-kind', file_pattern: 'test-file_pattern' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fileCatalogHandler.registerProvider({ provider_name: 'test-provider_name', kind: 'test-kind', file_pattern: 'test-file_pattern' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = fileCatalogHandler.registerProvider({ provider_name: 'test-provider_name', kind: 'test-kind', file_pattern: 'test-file_pattern' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
      expect(variants).toContain('already_registered');
    });

    it('declares read and write sets', () => {
      const program = fileCatalogHandler.registerProvider({ provider_name: 'test-provider_name', kind: 'test-kind', file_pattern: 'test-file_pattern' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fileCatalogHandler.registerProvider({ provider_name: 'test-provider_name', kind: 'test-kind', file_pattern: 'test-file_pattern' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof fileCatalogHandler.registerProvider !== 'function') return;
      const result = await interpret(fileCatalogHandler.registerProvider({ provider_name: 'test-provider_name', kind: 'test-kind', file_pattern: 'test-file_pattern' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = fileCatalogHandler.get({ name: 'test-name', kind: 'test-kind' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fileCatalogHandler.get({ name: 'test-name', kind: 'test-kind' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = fileCatalogHandler.get({ name: 'test-name', kind: 'test-kind' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
      expect(variants).toContain('notfound');
    });

    it('declares read and write sets', () => {
      const program = fileCatalogHandler.get({ name: 'test-name', kind: 'test-kind' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fileCatalogHandler.get({ name: 'test-name', kind: 'test-kind' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof fileCatalogHandler.get !== 'function') return;
      const result = await interpret(fileCatalogHandler.get({ name: 'test-name', kind: 'test-kind' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = fileCatalogHandler.list({ kind: 'test-kind' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fileCatalogHandler.list({ kind: 'test-kind' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = fileCatalogHandler.list({ kind: 'test-kind' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
    });

    it('declares read and write sets', () => {
      const program = fileCatalogHandler.list({ kind: 'test-kind' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fileCatalogHandler.list({ kind: 'test-kind' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof fileCatalogHandler.list !== 'function') return;
      const result = await interpret(fileCatalogHandler.list({ kind: 'test-kind' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('listForSuite', () => {
    it('builds a valid StorageProgram', () => {
      const program = fileCatalogHandler.listForSuite({ suite: 'test-suite' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fileCatalogHandler.listForSuite({ suite: 'test-suite' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = fileCatalogHandler.listForSuite({ suite: 'test-suite' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
    });

    it('declares read and write sets', () => {
      const program = fileCatalogHandler.listForSuite({ suite: 'test-suite' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fileCatalogHandler.listForSuite({ suite: 'test-suite' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof fileCatalogHandler.listForSuite !== 'function') return;
      const result = await interpret(fileCatalogHandler.listForSuite({ suite: 'test-suite' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('syncFilePathsForSuite', () => {
    it('builds a valid StorageProgram', () => {
      const program = fileCatalogHandler.syncFilePathsForSuite({ suite: 'test-suite', search_paths: 'test-search_paths' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = fileCatalogHandler.syncFilePathsForSuite({ suite: 'test-suite', search_paths: 'test-search_paths' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('covers all declared variants', () => {
      const program = fileCatalogHandler.syncFilePathsForSuite({ suite: 'test-suite', search_paths: 'test-search_paths' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = extractCompletionVariants(program);
      expect(variants).toContain('ok');
      expect(variants).toContain('error');
    });

    it('declares read and write sets', () => {
      const program = fileCatalogHandler.syncFilePathsForSuite({ suite: 'test-suite', search_paths: 'test-search_paths' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = fileCatalogHandler.syncFilePathsForSuite({ suite: 'test-suite', search_paths: 'test-search_paths' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('executes successfully', async () => {
      if (typeof fileCatalogHandler.syncFilePathsForSuite !== 'function') return;
      const result = await interpret(fileCatalogHandler.syncFilePathsForSuite({ suite: 'test-suite', search_paths: 'test-search_paths' }), storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
      expect(typeof result.variant).toBe('string');
    });

  });

  describe('invariant examples', () => {
    it("registerProvider-then-list", async () => {
      const storage = createInMemoryStorage();
      const registerProviderResult0 = await interpret(fileCatalogHandler.registerProvider({ provider_name: {"type":"literal","value":"concept"}, kind: {"type":"literal","value":"concept"}, file_pattern: {"type":"literal","value":"*.concept"} }), storage);
      expect(registerProviderResult0.variant).toBe("ok");
      const thenResult0 = await interpret(fileCatalogHandler.discover({ base_paths: {"type":"literal","value":"/test/specs"} }), storage);
      expect(thenResult0.variant).toBe("ok");
      const thenResult1 = await interpret(fileCatalogHandler.list({ kind: {"type":"literal","value":"concept"} }), storage);
      expect(thenResult1.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-path', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('discover'), input: fc.record({ base_paths: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('registerProvider'), input: fc.record({ provider_name: fc.string({ minLength: 1, maxLength: 20 }), kind: fc.string({ minLength: 1, maxLength: 20 }), file_pattern: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), kind: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('listForSuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('syncFilePathsForSuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 20 }), search_paths: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = fileCatalogHandler[step.action];
              if (typeof actionFn === 'function') {
                const program = actionFn.call(fileCatalogHandler, step.input as Record<string, unknown>);
                const result = await interpret(program, storage);
                expect(result.variant).toBeDefined();
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-kind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('discover'), input: fc.record({ base_paths: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('registerProvider'), input: fc.record({ provider_name: fc.string({ minLength: 1, maxLength: 20 }), kind: fc.string({ minLength: 1, maxLength: 20 }), file_pattern: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 20 }), kind: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ kind: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('listForSuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('syncFilePathsForSuite'), input: fc.record({ suite: fc.string({ minLength: 1, maxLength: 20 }), search_paths: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = fileCatalogHandler[step.action];
              if (typeof actionFn === 'function') {
                const program = actionFn.call(fileCatalogHandler, step.input as Record<string, unknown>);
                const result = await interpret(program, storage);
                expect(result.variant).toBeDefined();
                // Never: orphaned-kind
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('registerProvider requires: ', async () => {
      const storage = createInMemoryStorage();
      const result = await interpret(fileCatalogHandler.registerProvider({  }), storage);
      expect(['error', 'invalid', 'missing', 'notFound']).toContain(result.variant);
    });

    it('registerProvider ensures on ok: ', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.record({ provider_name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }), file_pattern: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const program = fileCatalogHandler.registerProvider(input as Record<string, unknown>);
            const result = await interpret(program, storage);
            fc.pre(result.variant === "ok");
            expect(result.output).toBeDefined();
          },
        ),
        { numRuns: 100 },
      );
    });

  });

});
