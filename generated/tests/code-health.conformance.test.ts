// CodeHealth Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { codeHealthHandler } from '../../handlers\ts\quality-analysis\code-health.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('CodeHealth functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('measure', () => {
    it('builds a valid StorageProgram', () => {
      const program = codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof codeHealthHandler.measure !== 'function') return;
      const result = await interpret(codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "measure_healthy" -> ok', async () => {
      if (typeof codeHealthHandler.measure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "measure_unhealthy" -> ok', async () => {
      if (typeof codeHealthHandler.measure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(codeHealthHandler.measure({ target: "src/legacy/big-ball-of-mud.ts", complexity: "95.0", testCoverage: "5.0", duplication: "60.0", depFreshness: "20.0", securityVulns: "80.0", docCoverage: "5.0", codeChurn: "90.0", coupling: "85.0", techDebt: "80.0", staticViolations: "75.0" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "measure_empty_target" -> invalid', async () => {
      if (typeof codeHealthHandler.measure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(codeHealthHandler.measure({ target: "", complexity: "10.0", testCoverage: "80.0", duplication: "5.0", depFreshness: "80.0", securityVulns: "0.0", docCoverage: "70.0", codeChurn: "15.0", coupling: "25.0", techDebt: "8.0", staticViolations: "1.0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "measure_out_of_range" -> invalid', async () => {
      if (typeof codeHealthHandler.measure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(codeHealthHandler.measure({ target: "src/foo.ts", complexity: "-5.0", testCoverage: "80.0", duplication: "5.0", depFreshness: "80.0", securityVulns: "0.0", docCoverage: "70.0", codeChurn: "15.0", coupling: "25.0", techDebt: "8.0", staticViolations: "1.0" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = codeHealthHandler.get({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = codeHealthHandler.get({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = codeHealthHandler.get({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = codeHealthHandler.get({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = codeHealthHandler.get({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof codeHealthHandler.get !== 'function') return;
      const result = await interpret(codeHealthHandler.get({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_ok" -> ok', async () => {
      if (typeof codeHealthHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_measure_healthy = await interpret(codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" }), storage);
      const result = await interpret(codeHealthHandler.get({ snapshot: afterResult_measure_healthy?.output?.["snapshot"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof codeHealthHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(codeHealthHandler.get({ snapshot: "nonexistent-snapshot" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('latest', () => {
    it('builds a valid StorageProgram', () => {
      const program = codeHealthHandler.latest({ target: "src/auth/login.ts" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = codeHealthHandler.latest({ target: "src/auth/login.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = codeHealthHandler.latest({ target: "src/auth/login.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = codeHealthHandler.latest({ target: "src/auth/login.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = codeHealthHandler.latest({ target: "src/auth/login.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof codeHealthHandler.latest !== 'function') return;
      const result = await interpret(codeHealthHandler.latest({ target: "src/auth/login.ts" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "latest_ok" -> ok', async () => {
      if (typeof codeHealthHandler.latest !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_measure_healthy = await interpret(codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" }), storage);
      const _pool = Object.assign({}, (afterResult_measure_healthy?.output ?? {}));
      const _fixtureInput = { target: "src/auth/login.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(codeHealthHandler.latest({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "latest_missing" -> notfound', async () => {
      if (typeof codeHealthHandler.latest !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(codeHealthHandler.latest({ target: "src/does-not-exist.ts" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('history', () => {
    it('builds a valid StorageProgram', () => {
      const program = codeHealthHandler.history({ target: "src/auth/login.ts", limit: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = codeHealthHandler.history({ target: "src/auth/login.ts", limit: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = codeHealthHandler.history({ target: "src/auth/login.ts", limit: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = codeHealthHandler.history({ target: "src/auth/login.ts", limit: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = codeHealthHandler.history({ target: "src/auth/login.ts", limit: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof codeHealthHandler.history !== 'function') return;
      const result = await interpret(codeHealthHandler.history({ target: "src/auth/login.ts", limit: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "history_ok" -> ok', async () => {
      if (typeof codeHealthHandler.history !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_measure_healthy = await interpret(codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" }), storage);
      const _pool = Object.assign({}, (afterResult_measure_healthy?.output ?? {}));
      const _fixtureInput = { target: "src/auth/login.ts", limit: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(codeHealthHandler.history({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "history_limited" -> ok', async () => {
      if (typeof codeHealthHandler.history !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_measure_healthy = await interpret(codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" }), storage);
      const _pool = Object.assign({}, (afterResult_measure_healthy?.output ?? {}));
      const _fixtureInput = { target: "src/auth/login.ts", limit: "1" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(codeHealthHandler.history({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "history_missing" -> notfound', async () => {
      if (typeof codeHealthHandler.history !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(codeHealthHandler.history({ target: "nonexistent.ts", limit: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('compare', () => {
    it('builds a valid StorageProgram', () => {
      const program = codeHealthHandler.compare({ target: "src/auth/login.ts", snapshotA: {"type":"ref","fixture":"measure_healthy","field":"snapshot"}, snapshotB: {"type":"ref","fixture":"measure_unhealthy","field":"snapshot"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = codeHealthHandler.compare({ target: "src/auth/login.ts", snapshotA: {"type":"ref","fixture":"measure_healthy","field":"snapshot"}, snapshotB: {"type":"ref","fixture":"measure_unhealthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = codeHealthHandler.compare({ target: "src/auth/login.ts", snapshotA: {"type":"ref","fixture":"measure_healthy","field":"snapshot"}, snapshotB: {"type":"ref","fixture":"measure_unhealthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = codeHealthHandler.compare({ target: "src/auth/login.ts", snapshotA: {"type":"ref","fixture":"measure_healthy","field":"snapshot"}, snapshotB: {"type":"ref","fixture":"measure_unhealthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = codeHealthHandler.compare({ target: "src/auth/login.ts", snapshotA: {"type":"ref","fixture":"measure_healthy","field":"snapshot"}, snapshotB: {"type":"ref","fixture":"measure_unhealthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof codeHealthHandler.compare !== 'function') return;
      const result = await interpret(codeHealthHandler.compare({ target: "src/auth/login.ts", snapshotA: {"type":"ref","fixture":"measure_healthy","field":"snapshot"}, snapshotB: {"type":"ref","fixture":"measure_unhealthy","field":"snapshot"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "compare_ok" -> ok', async () => {
      if (typeof codeHealthHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_measure_unhealthy = await interpret(codeHealthHandler.measure({ target: "src/legacy/big-ball-of-mud.ts", complexity: "95.0", testCoverage: "5.0", duplication: "60.0", depFreshness: "20.0", securityVulns: "80.0", docCoverage: "5.0", codeChurn: "90.0", coupling: "85.0", techDebt: "80.0", staticViolations: "75.0" }), storage);
      const result = await interpret(codeHealthHandler.compare({ target: "src/auth/login.ts", snapshotA: undefined /* unresolved ref: $measure_healthy.snapshot */, snapshotB: afterResult_measure_unhealthy?.output?.["snapshot"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "compare_missing" -> notfound', async () => {
      if (typeof codeHealthHandler.compare !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(codeHealthHandler.compare({ target: "src/auth/login.ts", snapshotA: "nonexistent-a", snapshotB: "nonexistent-b" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('topOffenders', () => {
    it('builds a valid StorageProgram', () => {
      const program = codeHealthHandler.topOffenders({ limit: null, grade: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = codeHealthHandler.topOffenders({ limit: null, grade: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = codeHealthHandler.topOffenders({ limit: null, grade: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = codeHealthHandler.topOffenders({ limit: null, grade: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = codeHealthHandler.topOffenders({ limit: null, grade: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof codeHealthHandler.topOffenders !== 'function') return;
      const result = await interpret(codeHealthHandler.topOffenders({ limit: null, grade: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "top_offenders_default" -> ok', async () => {
      if (typeof codeHealthHandler.topOffenders !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_measure_unhealthy = await interpret(codeHealthHandler.measure({ target: "src/legacy/big-ball-of-mud.ts", complexity: "95.0", testCoverage: "5.0", duplication: "60.0", depFreshness: "20.0", securityVulns: "80.0", docCoverage: "5.0", codeChurn: "90.0", coupling: "85.0", techDebt: "80.0", staticViolations: "75.0" }), storage);
      const _pool = Object.assign({}, (afterResult_measure_unhealthy?.output ?? {}));
      const _fixtureInput = { limit: null, grade: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(codeHealthHandler.topOffenders({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "top_offenders_limited" -> ok', async () => {
      if (typeof codeHealthHandler.topOffenders !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_measure_unhealthy = await interpret(codeHealthHandler.measure({ target: "src/legacy/big-ball-of-mud.ts", complexity: "95.0", testCoverage: "5.0", duplication: "60.0", depFreshness: "20.0", securityVulns: "80.0", docCoverage: "5.0", codeChurn: "90.0", coupling: "85.0", techDebt: "80.0", staticViolations: "75.0" }), storage);
      const _pool = Object.assign({}, (afterResult_measure_unhealthy?.output ?? {}));
      const _fixtureInput = { limit: "5", grade: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(codeHealthHandler.topOffenders({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "top_offenders_grade" -> ok', async () => {
      if (typeof codeHealthHandler.topOffenders !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_measure_unhealthy = await interpret(codeHealthHandler.measure({ target: "src/legacy/big-ball-of-mud.ts", complexity: "95.0", testCoverage: "5.0", duplication: "60.0", depFreshness: "20.0", securityVulns: "80.0", docCoverage: "5.0", codeChurn: "90.0", coupling: "85.0", techDebt: "80.0", staticViolations: "75.0" }), storage);
      const _pool = Object.assign({}, (afterResult_measure_unhealthy?.output ?? {}));
      const _fixtureInput = { limit: null, grade: "C" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(codeHealthHandler.topOffenders({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('breakdown', () => {
    it('builds a valid StorageProgram', () => {
      const program = codeHealthHandler.breakdown({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = codeHealthHandler.breakdown({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = codeHealthHandler.breakdown({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = codeHealthHandler.breakdown({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = codeHealthHandler.breakdown({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof codeHealthHandler.breakdown !== 'function') return;
      const result = await interpret(codeHealthHandler.breakdown({ snapshot: {"type":"ref","fixture":"measure_healthy","field":"snapshot"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "breakdown_ok" -> ok', async () => {
      if (typeof codeHealthHandler.breakdown !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_measure_healthy = await interpret(codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: "15.0", testCoverage: "88.0", duplication: "4.0", depFreshness: "90.0", securityVulns: "0.0", docCoverage: "75.0", codeChurn: "20.0", coupling: "30.0", techDebt: "10.0", staticViolations: "2.0" }), storage);
      const result = await interpret(codeHealthHandler.breakdown({ snapshot: afterResult_measure_healthy?.output?.["snapshot"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "breakdown_missing" -> notfound', async () => {
      if (typeof codeHealthHandler.breakdown !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(codeHealthHandler.breakdown({ snapshot: "nonexistent-snapshot" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof codeHealthHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = codeHealthHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('CodeHealth');
    });
  });

  describe('invariant examples', () => {
    it("measure then get", async () => {
      const storage = createInMemoryStorage();
      const measureResult0 = await interpret(codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: 15, testCoverage: 88, duplication: 4, depFreshness: 90, securityVulns: 0, docCoverage: 75, codeChurn: 20, coupling: 30, techDebt: 10, staticViolations: 2 }), storage);
      expect(measureResult0.variant).toBe("ok");
      let snapshot = measureResult0.output["snapshot"];
      let s = snapshot;
      let healthScore = measureResult0.output["healthScore"];
      let _ = healthScore;
      let grade = measureResult0.output["grade"];
      const thenResult0 = await interpret(codeHealthHandler.get({ snapshot: s }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("measure then latest", async () => {
      const storage = createInMemoryStorage();
      const measureResult0 = await interpret(codeHealthHandler.measure({ target: "src/auth/login.ts", complexity: 15, testCoverage: 88, duplication: 4, depFreshness: 90, securityVulns: 0, docCoverage: 75, codeChurn: 20, coupling: 30, techDebt: 10, staticViolations: 2 }), storage);
      expect(measureResult0.variant).toBe("ok");
      let snapshot = measureResult0.output["snapshot"];
      let _ = snapshot;
      let healthScore = measureResult0.output["healthScore"];
      let h = healthScore;
      let grade = measureResult0.output["grade"];
      const thenResult0 = await interpret(codeHealthHandler.latest({ target: "src/auth/login.ts" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("measure twice then history shows both", async () => {
      const storage = createInMemoryStorage();
      const measureResult0 = await interpret(codeHealthHandler.measure({ target: "src/utils.ts", complexity: 20, testCoverage: 70, duplication: 8, depFreshness: 80, securityVulns: 0, docCoverage: 60, codeChurn: 25, coupling: 35, techDebt: 15, staticViolations: 3 }), storage);
      expect(measureResult0.variant).toBe("ok");
      let snapshot = measureResult0.output["snapshot"];
      let _ = snapshot;
      let healthScore = measureResult0.output["healthScore"];
      let grade = measureResult0.output["grade"];
      const thenResult0 = await interpret(codeHealthHandler.history({ target: "src/utils.ts", limit: false }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("invalid target rejected", async () => {
      const storage = createInMemoryStorage();
      const measureResult0 = await interpret(codeHealthHandler.measure({ target: "", complexity: 10, testCoverage: 80, duplication: 5, depFreshness: 80, securityVulns: 0, docCoverage: 70, codeChurn: 15, coupling: 25, techDebt: 8, staticViolations: 1 }), storage);
      expect(measureResult0.variant).toBe("invalid");
      let message = measureResult0.output["message"];
      let _ = message;
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('never: orphan signals', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('measure'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), complexity: fc.string(), testCoverage: fc.string(), duplication: fc.string(), depFreshness: fc.string(), securityVulns: fc.string(), docCoverage: fc.string(), codeChurn: fc.string(), coupling: fc.string(), techDebt: fc.string(), staticViolations: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ snapshot: fc.string() }) }),
              fc.record({ action: fc.constant('latest'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('history'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), limit: fc.string() }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), snapshotA: fc.string(), snapshotB: fc.string() }) }),
              fc.record({ action: fc.constant('topOffenders'), input: fc.record({ limit: fc.string(), grade: fc.string() }) }),
              fc.record({ action: fc.constant('breakdown'), input: fc.record({ snapshot: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = codeHealthHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(codeHealthHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphan signals
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: grade consistent with score', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('measure'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), complexity: fc.string(), testCoverage: fc.string(), duplication: fc.string(), depFreshness: fc.string(), securityVulns: fc.string(), docCoverage: fc.string(), codeChurn: fc.string(), coupling: fc.string(), techDebt: fc.string(), staticViolations: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ snapshot: fc.string() }) }),
              fc.record({ action: fc.constant('latest'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('history'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), limit: fc.string() }) }),
              fc.record({ action: fc.constant('compare'), input: fc.record({ target: fc.string({ minLength: 1, maxLength: 50 }), snapshotA: fc.string(), snapshotB: fc.string() }) }),
              fc.record({ action: fc.constant('topOffenders'), input: fc.record({ limit: fc.string(), grade: fc.string() }) }),
              fc.record({ action: fc.constant('breakdown'), input: fc.record({ snapshot: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = codeHealthHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(codeHealthHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
