// Hotspot Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { hotspotHandler } from '../../handlers/ts/quality-analysis/hotspot.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Hotspot functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('analyze', () => {
    it('builds a valid StorageProgram', () => {
      const program = hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof hotspotHandler.analyze !== 'function') return;
      const result = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "analyze_files" -> ok', async () => {
      if (typeof hotspotHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "analyze_defaults" -> ok', async () => {
      if (typeof hotspotHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(hotspotHandler.analyze({ targets: ["src/index.ts"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "analyze_empty" -> noHistory', async () => {
      if (typeof hotspotHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(hotspotHandler.analyze({ targets: [] }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('noHistory'));
    });

  });

  describe('rank', () => {
    it('builds a valid StorageProgram', () => {
      const program = hotspotHandler.rank({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = hotspotHandler.rank({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = hotspotHandler.rank({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = hotspotHandler.rank({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = hotspotHandler.rank({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof hotspotHandler.rank !== 'function') return;
      const result = await interpret(hotspotHandler.rank({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "rank_default" -> ok', async () => {
      if (typeof hotspotHandler.rank !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_files = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_files?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(hotspotHandler.rank({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "rank_limited" -> ok', async () => {
      if (typeof hotspotHandler.rank !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_files = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_files?.output ?? {}));
      const _fixtureInput = { limit: "2" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(hotspotHandler.rank({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "rank_threshold" -> ok', async () => {
      if (typeof hotspotHandler.rank !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_files = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_files?.output ?? {}));
      const _fixtureInput = { threshold: "0.5" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(hotspotHandler.rank({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('trend', () => {
    it('builds a valid StorageProgram', () => {
      const program = hotspotHandler.trend({ target: "auth/login.ts" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = hotspotHandler.trend({ target: "auth/login.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = hotspotHandler.trend({ target: "auth/login.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = hotspotHandler.trend({ target: "auth/login.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = hotspotHandler.trend({ target: "auth/login.ts" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof hotspotHandler.trend !== 'function') return;
      const result = await interpret(hotspotHandler.trend({ target: "auth/login.ts" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "trend_existing" -> ok', async () => {
      if (typeof hotspotHandler.trend !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_files = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_files?.output ?? {}));
      const _fixtureInput = { target: "auth/login.ts" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(hotspotHandler.trend({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "trend_missing" -> notTracked', async () => {
      if (typeof hotspotHandler.trend !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(hotspotHandler.trend({ target: "nonexistent/file.ts" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notTracked'));
    });

  });

  describe('distribution', () => {
    it('builds a valid StorageProgram', () => {
      const program = hotspotHandler.distribution({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = hotspotHandler.distribution({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = hotspotHandler.distribution({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = hotspotHandler.distribution({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = hotspotHandler.distribution({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof hotspotHandler.distribution !== 'function') return;
      const result = await interpret(hotspotHandler.distribution({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "distribution_ok" -> ok', async () => {
      if (typeof hotspotHandler.distribution !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_files = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"], period: "6m", complexityMetric: "cognitive_complexity" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_files?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(hotspotHandler.distribution({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof hotspotHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = hotspotHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('Hotspot');
    });
  });

  describe('invariant examples', () => {
    it("analyze then rank", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"] }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let hotspots = analyzeResult0.output["hotspots"];
      let hs = hotspots;
      const thenResult0 = await interpret(hotspotHandler.rank({ limit: 3 }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("analyze then distribution", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts","auth/register.ts","utils/helpers.ts"] }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let hotspots = analyzeResult0.output["hotspots"];
      let _ = hotspots;
      const thenResult0 = await interpret(hotspotHandler.distribution({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("trend requires prior analysis", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(hotspotHandler.analyze({ targets: ["auth/login.ts"] }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let hotspots = analyzeResult0.output["hotspots"];
      let _ = hotspots;
      const thenResult0 = await interpret(hotspotHandler.trend({ target: "auth/login.ts" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

});
