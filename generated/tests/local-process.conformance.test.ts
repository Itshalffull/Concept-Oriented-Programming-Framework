// LocalProcess Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { localProcessHandler } from '../../handlers/ts/execution/local-process.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('LocalProcess functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('initialize', () => {
    it('builds a valid StorageProgram', () => {
      const program = localProcessHandler.initialize({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = localProcessHandler.initialize({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = localProcessHandler.initialize({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = localProcessHandler.initialize({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = localProcessHandler.initialize({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof localProcessHandler.initialize !== 'function') return;
      const result = await interpret(localProcessHandler.initialize({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof localProcessHandler.initialize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(localProcessHandler.initialize({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('registerRuntime', () => {
    it('builds a valid StorageProgram', () => {
      const program = localProcessHandler.registerRuntime({ runtime: "wasm", providerName: "WasmProvider" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = localProcessHandler.registerRuntime({ runtime: "wasm", providerName: "WasmProvider" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = localProcessHandler.registerRuntime({ runtime: "wasm", providerName: "WasmProvider" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = localProcessHandler.registerRuntime({ runtime: "wasm", providerName: "WasmProvider" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = localProcessHandler.registerRuntime({ runtime: "wasm", providerName: "WasmProvider" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof localProcessHandler.registerRuntime !== 'function') return;
      const result = await interpret(localProcessHandler.registerRuntime({ runtime: "wasm", providerName: "WasmProvider" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_wasm" -> ok', async () => {
      if (typeof localProcessHandler.registerRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(localProcessHandler.listRuntimes({  }), storage);
      const result = await interpret(localProcessHandler.registerRuntime({ runtime: "wasm", providerName: "WasmProvider" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof localProcessHandler.registerRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(localProcessHandler.registerRuntime({ runtime: "wasm", providerName: "AnotherWasmProvider" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('dispatch', () => {
    it('builds a valid StorageProgram', () => {
      const program = localProcessHandler.dispatch({ runtime: "wasm", operation: "execute", moduleRef: "image-classifier.wasm", input: "{\"image\": \"base64data\"}", config: "{\"memoryLimit\": 256}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = localProcessHandler.dispatch({ runtime: "wasm", operation: "execute", moduleRef: "image-classifier.wasm", input: "{\"image\": \"base64data\"}", config: "{\"memoryLimit\": 256}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = localProcessHandler.dispatch({ runtime: "wasm", operation: "execute", moduleRef: "image-classifier.wasm", input: "{\"image\": \"base64data\"}", config: "{\"memoryLimit\": 256}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = localProcessHandler.dispatch({ runtime: "wasm", operation: "execute", moduleRef: "image-classifier.wasm", input: "{\"image\": \"base64data\"}", config: "{\"memoryLimit\": 256}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = localProcessHandler.dispatch({ runtime: "wasm", operation: "execute", moduleRef: "image-classifier.wasm", input: "{\"image\": \"base64data\"}", config: "{\"memoryLimit\": 256}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof localProcessHandler.dispatch !== 'function') return;
      const result = await interpret(localProcessHandler.dispatch({ runtime: "wasm", operation: "execute", moduleRef: "image-classifier.wasm", input: "{\"image\": \"base64data\"}", config: "{\"memoryLimit\": 256}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "wasm_inference" -> ok', async () => {
      if (typeof localProcessHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(localProcessHandler.listRuntimes({  }), storage);
      const result = await interpret(localProcessHandler.dispatch({ runtime: "wasm", operation: "execute", moduleRef: "image-classifier.wasm", input: "{\"image\": \"base64data\"}", config: "{\"memoryLimit\": 256}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_runtime" -> runtimeNotFound', async () => {
      if (typeof localProcessHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(localProcessHandler.listRuntimes({  }), storage);
      const result = await interpret(localProcessHandler.dispatch({ runtime: "cuda", operation: "compute", moduleRef: "model.bin", input: "{}", config: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('runtimeNotFound'));
    });

    it('fixture "failed_execution" -> error', async () => {
      if (typeof localProcessHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(localProcessHandler.listRuntimes({  }), storage);
      const result = await interpret(localProcessHandler.dispatch({ runtime: "wasm", operation: "execute", moduleRef: "broken-module.wasm", input: "{}", config: "{}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listRuntimes', () => {
    it('builds a valid StorageProgram', () => {
      const program = localProcessHandler.listRuntimes({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = localProcessHandler.listRuntimes({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = localProcessHandler.listRuntimes({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = localProcessHandler.listRuntimes({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = localProcessHandler.listRuntimes({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof localProcessHandler.listRuntimes !== 'function') return;
      const result = await interpret(localProcessHandler.listRuntimes({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof localProcessHandler.listRuntimes !== 'function') return;
      const storage = createInMemoryStorage();
      await interpret(localProcessHandler.listRuntimes({  }), storage);
      const result = await interpret(localProcessHandler.listRuntimes({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof localProcessHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = localProcessHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('LocalProcess');
    });
  });

  describe('invariant examples', () => {
    it("dispatch fails without registered runtime", async () => {
      const storage = createInMemoryStorage();
      const dispatchResult0 = await interpret(localProcessHandler.dispatch({ runtime: {"type":"literal","value":"onnx"}, operation: {"type":"literal","value":"infer"}, moduleRef: {"type":"literal","value":"model"}, input: {"type":"literal","value":"{}"}, config: {"type":"literal","value":"{}"} }), storage);
      expect(dispatchResult0.variant).toBe("runtimeNotFound");
      const runtime = dispatchResult0.output["runtime"];
      const thenResult0 = await interpret(localProcessHandler.registerRuntime({ runtime: {"type":"literal","value":"onnx"}, providerName: {"type":"literal","value":"OnnxProvider"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate runtime registration rejected", async () => {
      const storage = createInMemoryStorage();
      const registerRuntimeResult0 = await interpret(localProcessHandler.registerRuntime({ runtime: {"type":"literal","value":"wasm"}, providerName: {"type":"literal","value":"WasmProvider"} }), storage);
      expect(registerRuntimeResult0.variant).toBe("ok");
      const thenResult0 = await interpret(localProcessHandler.registerRuntime({ runtime: {"type":"literal","value":"wasm"}, providerName: {"type":"literal","value":"AnotherProvider"} }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: processes have valid runtime', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('initialize'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('registerRuntime'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }), providerName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dispatch'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }), moduleRef: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listRuntimes'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = localProcessHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(localProcessHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: process without module reference', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('initialize'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('registerRuntime'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }), providerName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dispatch'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }), moduleRef: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listRuntimes'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = localProcessHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(localProcessHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: process without module reference
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('dispatch handles empty input: ', async () => {
      if (typeof localProcessHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(localProcessHandler.dispatch({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('dispatch ensures on ok: ', async () => {
      if (typeof localProcessHandler.dispatch !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }), moduleRef: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = localProcessHandler.dispatch(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('registerRuntime handles empty input: ', async () => {
      if (typeof localProcessHandler.registerRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(localProcessHandler.registerRuntime({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('registerRuntime ensures on ok: ', async () => {
      if (typeof localProcessHandler.registerRuntime !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }), providerName: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = localProcessHandler.registerRuntime(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
