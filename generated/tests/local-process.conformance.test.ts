// LocalProcess Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { localProcessHandler } from '../../handlers/ts/execution/local-process.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('LocalProcess imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('initialize', () => {
    it('executes without crashing', async () => {
      if (typeof localProcessHandler.initialize !== 'function') return;
      try {
        const result = await localProcessHandler.initialize({  }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('registerRuntime', () => {
    it('executes without crashing', async () => {
      if (typeof localProcessHandler.registerRuntime !== 'function') return;
      try {
        const result = await localProcessHandler.registerRuntime({ runtime: 'test-runtime', providerName: 'test-providerName' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('dispatch', () => {
    it('executes without crashing', async () => {
      if (typeof localProcessHandler.dispatch !== 'function') return;
      try {
        const result = await localProcessHandler.dispatch({ runtime: 'test-runtime', operation: 'test-operation', moduleRef: 'test-moduleRef', input: 'test-input', config: 'test-config' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('listRuntimes', () => {
    it('executes without crashing', async () => {
      if (typeof localProcessHandler.listRuntimes !== 'function') return;
      try {
        const result = await localProcessHandler.listRuntimes({  }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("dispatch fails without registered runtime", async () => {
      const storage = createInMemoryStorage();
      const dispatchResult0 = await localProcessHandler.dispatch({ runtime: {"type":"literal","value":"onnx"}, operation: {"type":"literal","value":"infer"}, moduleRef: {"type":"literal","value":"model"}, input: {"type":"literal","value":"{}"}, config: {"type":"literal","value":"{}"} }, storage);
      expect(dispatchResult0.variant).toBe("runtimeNotFound");
      const runtime = dispatchResult0.output["runtime"];
      const thenResult0 = await localProcessHandler.registerRuntime({ runtime: {"type":"literal","value":"onnx"}, providerName: {"type":"literal","value":"OnnxProvider"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate runtime registration rejected", async () => {
      const storage = createInMemoryStorage();
      const registerRuntimeResult0 = await localProcessHandler.registerRuntime({ runtime: {"type":"literal","value":"wasm"}, providerName: {"type":"literal","value":"WasmProvider"} }, storage);
      expect(registerRuntimeResult0.variant).toBe("ok");
      const thenResult0 = await localProcessHandler.registerRuntime({ runtime: {"type":"literal","value":"wasm"}, providerName: {"type":"literal","value":"AnotherProvider"} }, storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: processes have valid runtime', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('initialize'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('registerRuntime'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 20 }), providerName: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('dispatch'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 20 }), operation: fc.string({ minLength: 1, maxLength: 20 }), moduleRef: fc.string({ minLength: 1, maxLength: 20 }), input: fc.string({ minLength: 1, maxLength: 20 }), config: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('listRuntimes'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = localProcessHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(localProcessHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: process without module reference', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('initialize'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('registerRuntime'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 20 }), providerName: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('dispatch'), input: fc.record({ runtime: fc.string({ minLength: 1, maxLength: 20 }), operation: fc.string({ minLength: 1, maxLength: 20 }), moduleRef: fc.string({ minLength: 1, maxLength: 20 }), input: fc.string({ minLength: 1, maxLength: 20 }), config: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('listRuntimes'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = localProcessHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(localProcessHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: process without module reference
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('dispatch handles empty input: ', async () => {
      if (typeof localProcessHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await localProcessHandler.dispatch({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('dispatch ensures on ok: ', async () => {
      if (typeof localProcessHandler.dispatch !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }), moduleRef: fc.string({ minLength: 1, maxLength: 50 }), input: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await localProcessHandler.dispatch(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('registerRuntime handles empty input: ', async () => {
      if (typeof localProcessHandler.registerRuntime !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await localProcessHandler.registerRuntime({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('registerRuntime ensures on ok: ', async () => {
      if (typeof localProcessHandler.registerRuntime !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ runtime: fc.string({ minLength: 1, maxLength: 50 }), providerName: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await localProcessHandler.registerRuntime(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
