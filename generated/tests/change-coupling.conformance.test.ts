// ChangeCoupling Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { changeCouplingHandler } from '../../handlers/ts/quality-analysis/change-coupling.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ChangeCoupling functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('analyze', () => {
    it('builds a valid StorageProgram', () => {
      const program = changeCouplingHandler.analyze({ period: "6m", minStrength: "0.5" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = changeCouplingHandler.analyze({ period: "6m", minStrength: "0.5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = changeCouplingHandler.analyze({ period: "6m", minStrength: "0.5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = changeCouplingHandler.analyze({ period: "6m", minStrength: "0.5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = changeCouplingHandler.analyze({ period: "6m", minStrength: "0.5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof changeCouplingHandler.analyze !== 'function') return;
      const result = await interpret(changeCouplingHandler.analyze({ period: "6m", minStrength: "0.5" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "analyze_all" -> ok', async () => {
      if (typeof changeCouplingHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(changeCouplingHandler.analyze({ period: "6m", minStrength: "0.5" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "analyze_targets" -> ok', async () => {
      if (typeof changeCouplingHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(changeCouplingHandler.analyze({ targets: ["auth/login.ts","auth/session.ts"], period: "12m" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "analyze_defaults" -> ok', async () => {
      if (typeof changeCouplingHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(changeCouplingHandler.analyze({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('neighbors', () => {
    it('builds a valid StorageProgram', () => {
      const program = changeCouplingHandler.neighbors({ target: "auth/login.ts", limit: "5" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = changeCouplingHandler.neighbors({ target: "auth/login.ts", limit: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = changeCouplingHandler.neighbors({ target: "auth/login.ts", limit: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = changeCouplingHandler.neighbors({ target: "auth/login.ts", limit: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = changeCouplingHandler.neighbors({ target: "auth/login.ts", limit: "5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof changeCouplingHandler.neighbors !== 'function') return;
      const result = await interpret(changeCouplingHandler.neighbors({ target: "auth/login.ts", limit: "5" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "neighbors_existing" -> ok', async () => {
      if (typeof changeCouplingHandler.neighbors !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_all = await interpret(changeCouplingHandler.analyze({ period: "6m", minStrength: "0.5" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_all?.output ?? {}));
      const _fixtureInput = { target: "auth/login.ts", limit: "5" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(changeCouplingHandler.neighbors({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "neighbors_missing" -> notTracked', async () => {
      if (typeof changeCouplingHandler.neighbors !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(changeCouplingHandler.neighbors({ target: "nonexistent/file.ts" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notTracked'));
    });

  });

  describe('clusters', () => {
    it('builds a valid StorageProgram', () => {
      const program = changeCouplingHandler.clusters({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = changeCouplingHandler.clusters({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = changeCouplingHandler.clusters({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = changeCouplingHandler.clusters({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = changeCouplingHandler.clusters({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof changeCouplingHandler.clusters !== 'function') return;
      const result = await interpret(changeCouplingHandler.clusters({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "clusters_ok" -> ok', async () => {
      if (typeof changeCouplingHandler.clusters !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_analyze_all = await interpret(changeCouplingHandler.analyze({ period: "6m", minStrength: "0.5" }), storage);
      const _pool = Object.assign({}, (afterResult_analyze_all?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(changeCouplingHandler.clusters({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof changeCouplingHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = changeCouplingHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ChangeCoupling');
    });
  });

  describe('invariant examples', () => {
    it("analyze then clusters", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(changeCouplingHandler.analyze({ period: "6m", minStrength: 0.5 }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let couplings = analyzeResult0.output["couplings"];
      let cs = couplings;
      const thenResult0 = await interpret(changeCouplingHandler.clusters({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("analyze then neighbors", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(changeCouplingHandler.analyze({ targets: ["auth/login.ts","auth/session.ts"], period: "12m" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let couplings = analyzeResult0.output["couplings"];
      let _ = couplings;
      const thenResult0 = await interpret(changeCouplingHandler.neighbors({ target: "auth/login.ts" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

});
