// WasmProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { wasmProviderHandler } from '../../handlers/ts/execution/providers/wasm-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('WasmProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = wasmProviderHandler.register({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = wasmProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = wasmProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = wasmProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = wasmProviderHandler.register({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof wasmProviderHandler.register !== 'function') return;
      const result = await interpret(wasmProviderHandler.register({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof wasmProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(wasmProviderHandler.register({  }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('load', () => {
    it('builds a valid StorageProgram', () => {
      const program = wasmProviderHandler.load({ name: "tokenizer", wasmPath: "/modules/tokenizer.wasm", memoryLimit: "65536" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = wasmProviderHandler.load({ name: "tokenizer", wasmPath: "/modules/tokenizer.wasm", memoryLimit: "65536" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = wasmProviderHandler.load({ name: "tokenizer", wasmPath: "/modules/tokenizer.wasm", memoryLimit: "65536" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = wasmProviderHandler.load({ name: "tokenizer", wasmPath: "/modules/tokenizer.wasm", memoryLimit: "65536" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = wasmProviderHandler.load({ name: "tokenizer", wasmPath: "/modules/tokenizer.wasm", memoryLimit: "65536" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof wasmProviderHandler.load !== 'function') return;
      const result = await interpret(wasmProviderHandler.load({ name: "tokenizer", wasmPath: "/modules/tokenizer.wasm", memoryLimit: "65536" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "load_tokenizer" -> ok', async () => {
      if (typeof wasmProviderHandler.load !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(wasmProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { name: "tokenizer", wasmPath: "/modules/tokenizer.wasm", memoryLimit: "65536" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(wasmProviderHandler.load({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "load_large_module" -> ok', async () => {
      if (typeof wasmProviderHandler.load !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(wasmProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { name: "parser", wasmPath: "/modules/parser.wasm", memoryLimit: "131072" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(wasmProviderHandler.load({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('execute', () => {
    it('builds a valid StorageProgram', () => {
      const program = wasmProviderHandler.execute({ module: "tokenizer", function: "tokenize", args: "[\"hello world\"]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = wasmProviderHandler.execute({ module: "tokenizer", function: "tokenize", args: "[\"hello world\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = wasmProviderHandler.execute({ module: "tokenizer", function: "tokenize", args: "[\"hello world\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = wasmProviderHandler.execute({ module: "tokenizer", function: "tokenize", args: "[\"hello world\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = wasmProviderHandler.execute({ module: "tokenizer", function: "tokenize", args: "[\"hello world\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof wasmProviderHandler.execute !== 'function') return;
      const result = await interpret(wasmProviderHandler.execute({ module: "tokenizer", function: "tokenize", args: "[\"hello world\"]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "call_tokenize" -> ok', async () => {
      if (typeof wasmProviderHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(wasmProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { module: "tokenizer", function: "tokenize", args: "[\"hello world\"]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(wasmProviderHandler.execute({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "execute_unknown_module" -> notFound', async () => {
      if (typeof wasmProviderHandler.execute !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(wasmProviderHandler.execute({ module: "nonexistent", function: "run", args: "[]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notFound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = wasmProviderHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = wasmProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = wasmProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = wasmProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = wasmProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof wasmProviderHandler.list !== 'function') return;
      const result = await interpret(wasmProviderHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof wasmProviderHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid = await interpret(wasmProviderHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_valid?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(wasmProviderHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof wasmProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = wasmProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('WasmProvider');
    });
  });

  describe('invariant examples', () => {
    it("execute fails for unknown module", async () => {
      const storage = createInMemoryStorage();
      const loadResult0 = await interpret(wasmProviderHandler.load({ name: {"type":"literal","value":"tokenizer"}, wasmPath: {"type":"literal","value":"/models/tokenizer.wasm"}, memoryLimit: {"type":"literal","value":65536} }), storage);
      expect(loadResult0.variant).toBe("ok");
      let module = loadResult0.output["module"];
      const thenResult0 = await interpret(wasmProviderHandler.execute({ module: {"type":"literal","value":"unknown"}, function: {"type":"literal","value":"tokenize"}, args: {"type":"literal","value":"[]"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: modules have valid name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('load'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), wasmPath: fc.string({ minLength: 1, maxLength: 50 }), memoryLimit: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ module: fc.string({ minLength: 1, maxLength: 50 }), function: fc.string({ minLength: 1, maxLength: 50 }), args: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = wasmProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(wasmProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: module without wasm path', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('load'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), wasmPath: fc.string({ minLength: 1, maxLength: 50 }), memoryLimit: fc.integer({ min: 1, max: 1000 }) }) }),
              fc.record({ action: fc.constant('execute'), input: fc.record({ module: fc.string({ minLength: 1, maxLength: 50 }), function: fc.string({ minLength: 1, maxLength: 50 }), args: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = wasmProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(wasmProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: module without wasm path
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('load handles empty input: ', async () => {
      if (typeof wasmProviderHandler.load !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(wasmProviderHandler.load({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('load ensures on ok: ', async () => {
      if (typeof wasmProviderHandler.load !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), wasmPath: fc.string({ minLength: 1, maxLength: 50 }), memoryLimit: fc.integer({ min: 1, max: 1000 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = wasmProviderHandler.load(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
