// AlloySolverEndpoint Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { alloySolverEndpointHandler } from '../../handlers/ts/execution/instances/alloy-solver-endpoint.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('AlloySolverEndpoint functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = alloySolverEndpointHandler.register({ name: "alloy-local", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = alloySolverEndpointHandler.register({ name: "alloy-local", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = alloySolverEndpointHandler.register({ name: "alloy-local", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = alloySolverEndpointHandler.register({ name: "alloy-local", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = alloySolverEndpointHandler.register({ name: "alloy-local", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof alloySolverEndpointHandler.register !== 'function') return;
      const result = await interpret(alloySolverEndpointHandler.register({ name: "alloy-local", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "alloy_default" -> ok', async () => {
      if (typeof alloySolverEndpointHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(alloySolverEndpointHandler.register({ name: "alloy-local", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "alloy_custom" -> ok', async () => {
      if (typeof alloySolverEndpointHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(alloySolverEndpointHandler.register({ name: "alloy-ci", jarPath: "/usr/share/alloy/alloy6.jar", scope: "10", timeout: "120000", options: "-Xmx4g" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_name" -> error', async () => {
      if (typeof alloySolverEndpointHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(alloySolverEndpointHandler.register({ name: "", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('check', () => {
    it('builds a valid StorageProgram', () => {
      const program = alloySolverEndpointHandler.check({ name: "alloy-local", model: "sig Node { edges: set Node }", predicate: "run { some Node } for 5" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = alloySolverEndpointHandler.check({ name: "alloy-local", model: "sig Node { edges: set Node }", predicate: "run { some Node } for 5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = alloySolverEndpointHandler.check({ name: "alloy-local", model: "sig Node { edges: set Node }", predicate: "run { some Node } for 5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = alloySolverEndpointHandler.check({ name: "alloy-local", model: "sig Node { edges: set Node }", predicate: "run { some Node } for 5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = alloySolverEndpointHandler.check({ name: "alloy-local", model: "sig Node { edges: set Node }", predicate: "run { some Node } for 5" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof alloySolverEndpointHandler.check !== 'function') return;
      const result = await interpret(alloySolverEndpointHandler.check({ name: "alloy-local", model: "sig Node { edges: set Node }", predicate: "run { some Node } for 5" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "check_simple_model" -> ok', async () => {
      if (typeof alloySolverEndpointHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_alloy_default = await interpret(alloySolverEndpointHandler.register({ name: "alloy-local", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" }), storage);
      const _pool = Object.assign({}, (afterResult_alloy_default?.output ?? {}));
      const _fixtureInput = { name: "alloy-local", model: "sig Node { edges: set Node }", predicate: "run { some Node } for 5" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(alloySolverEndpointHandler.check({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "check_missing_endpoint" -> error', async () => {
      if (typeof alloySolverEndpointHandler.check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(alloySolverEndpointHandler.check({ name: "nonexistent", model: "sig A {}", predicate: "run {}" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = alloySolverEndpointHandler.resolve({ name: "alloy-local" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = alloySolverEndpointHandler.resolve({ name: "alloy-local" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = alloySolverEndpointHandler.resolve({ name: "alloy-local" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = alloySolverEndpointHandler.resolve({ name: "alloy-local" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = alloySolverEndpointHandler.resolve({ name: "alloy-local" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof alloySolverEndpointHandler.resolve !== 'function') return;
      const result = await interpret(alloySolverEndpointHandler.resolve({ name: "alloy-local" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_existing" -> ok', async () => {
      if (typeof alloySolverEndpointHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_alloy_default = await interpret(alloySolverEndpointHandler.register({ name: "alloy-local", jarPath: "/opt/alloy/alloy.jar", scope: "5", timeout: "60000", options: "" }), storage);
      const _pool = Object.assign({}, (afterResult_alloy_default?.output ?? {}));
      const _fixtureInput = { name: "alloy-local" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(alloySolverEndpointHandler.resolve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_missing" -> error', async () => {
      if (typeof alloySolverEndpointHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(alloySolverEndpointHandler.resolve({ name: "nonexistent" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("check after register", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(alloySolverEndpointHandler.register({ name: {"type":"literal","value":"alloy-local"}, jarPath: {"type":"literal","value":"/opt/alloy/alloy.jar"}, scope: {"type":"literal","value":5}, timeout: {"type":"literal","value":60000}, options: {"type":"literal","value":""} }), storage);
      expect(registerResult0.variant).toBe("ok");
      let endpoint = registerResult0.output["endpoint"];
      const thenResult0 = await interpret(alloySolverEndpointHandler.resolve({ name: {"type":"literal","value":"alloy-local"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: endpoints have valid name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), jarPath: fc.string({ minLength: 1, maxLength: 50 }), scope: fc.integer({ min: 1, max: 1000 }), timeout: fc.integer({ min: 1, max: 1000 }), options: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), model: fc.string({ minLength: 1, maxLength: 50 }), predicate: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = alloySolverEndpointHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(alloySolverEndpointHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: endpoint without JAR path', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), jarPath: fc.string({ minLength: 1, maxLength: 50 }), scope: fc.integer({ min: 1, max: 1000 }), timeout: fc.integer({ min: 1, max: 1000 }), options: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('check'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), model: fc.string({ minLength: 1, maxLength: 50 }), predicate: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = alloySolverEndpointHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(alloySolverEndpointHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: endpoint without JAR path
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof alloySolverEndpointHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(alloySolverEndpointHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof alloySolverEndpointHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), jarPath: fc.string({ minLength: 1, maxLength: 50 }), scope: fc.integer({ min: 1, max: 1000 }), timeout: fc.integer({ min: 1, max: 1000 }), options: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = alloySolverEndpointHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
