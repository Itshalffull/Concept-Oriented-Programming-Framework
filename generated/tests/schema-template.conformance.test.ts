// SchemaTemplate Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { schemaTemplateHandler } from '../../handlers/ts/app/schema-template.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SchemaTemplate functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaTemplateHandler.register !== 'function') return;
      const result = await interpret(schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_ok" -> ok', async () => {
      if (typeof schemaTemplateHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_minimal" -> ok', async () => {
      if (typeof schemaTemplateHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaTemplateHandler.register({ name: "tasks", label: "Tasks", description: "Simple task list.", category: "project", icon: "☑", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof schemaTemplateHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { name: "article", label: "Article", description: "Duplicate.", category: "content", icon: "📝", fields: "[]", properties: "{}", sampleData: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaTemplateHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('apply', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaTemplateHandler.apply({ name: "article", targetSchemaName: "my-blog" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaTemplateHandler.apply({ name: "article", targetSchemaName: "my-blog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaTemplateHandler.apply({ name: "article", targetSchemaName: "my-blog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaTemplateHandler.apply({ name: "article", targetSchemaName: "my-blog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaTemplateHandler.apply({ name: "article", targetSchemaName: "my-blog" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaTemplateHandler.apply !== 'function') return;
      const result = await interpret(schemaTemplateHandler.apply({ name: "article", targetSchemaName: "my-blog" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "apply_ok" -> ok', async () => {
      if (typeof schemaTemplateHandler.apply !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { name: "article", targetSchemaName: "my-blog" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaTemplateHandler.apply({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "apply_missing" -> not_found', async () => {
      if (typeof schemaTemplateHandler.apply !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaTemplateHandler.apply({ name: "nonexistent-template", targetSchemaName: "my-blog" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaTemplateHandler.list({ category: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaTemplateHandler.list({ category: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaTemplateHandler.list({ category: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaTemplateHandler.list({ category: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaTemplateHandler.list({ category: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaTemplateHandler.list !== 'function') return;
      const result = await interpret(schemaTemplateHandler.list({ category: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof schemaTemplateHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { category: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaTemplateHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_by_category" -> ok', async () => {
      if (typeof schemaTemplateHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { category: "content" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaTemplateHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('preview', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaTemplateHandler.preview({ name: "article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaTemplateHandler.preview({ name: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaTemplateHandler.preview({ name: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaTemplateHandler.preview({ name: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaTemplateHandler.preview({ name: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaTemplateHandler.preview !== 'function') return;
      const result = await interpret(schemaTemplateHandler.preview({ name: "article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "preview_ok" -> ok', async () => {
      if (typeof schemaTemplateHandler.preview !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { name: "article" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaTemplateHandler.preview({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "preview_missing" -> not_found', async () => {
      if (typeof schemaTemplateHandler.preview !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaTemplateHandler.preview({ name: "nonexistent-template" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });

  describe('remove', () => {
    it('builds a valid StorageProgram', () => {
      const program = schemaTemplateHandler.remove({ name: "article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = schemaTemplateHandler.remove({ name: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = schemaTemplateHandler.remove({ name: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = schemaTemplateHandler.remove({ name: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = schemaTemplateHandler.remove({ name: "article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof schemaTemplateHandler.remove !== 'function') return;
      const result = await interpret(schemaTemplateHandler.remove({ name: "article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_ok" -> ok', async () => {
      if (typeof schemaTemplateHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(schemaTemplateHandler.register({ name: "article", label: "Article", description: "Blog article with title, body, and author.", category: "content", icon: "📝", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { name: "article" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(schemaTemplateHandler.remove({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_missing" -> not_found', async () => {
      if (typeof schemaTemplateHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(schemaTemplateHandler.remove({ name: "nonexistent-template" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

  });


  describe('invariant examples', () => {
    it("register then preview", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(schemaTemplateHandler.register({ name: "events", label: "Events", description: "Event calendar.", category: "content", icon: "📅", fields: "[{\"fieldId\":\"title\",\"fieldType\":\"text\",\"label\":\"Title\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let t = id;
      const thenResult0 = await interpret(schemaTemplateHandler.preview({ name: "events" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register then list", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(schemaTemplateHandler.register({ name: "contacts", label: "Contacts", description: "CRM contacts.", category: "crm", icon: "👤", fields: "[{\"fieldId\":\"firstName\",\"fieldType\":\"text\",\"label\":\"First Name\",\"required\":true}]", properties: "{}", sampleData: "[]" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let t = id;
      const thenResult0 = await interpret(schemaTemplateHandler.list({ category: "crm" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate registration rejected", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(schemaTemplateHandler.register({ name: "tasks", label: "Tasks", description: "Task board.", category: "project", icon: "☑", fields: "[]", properties: "{}", sampleData: "" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let t = id;
      const thenResult0 = await interpret(schemaTemplateHandler.register({ name: "tasks", label: "Tasks", description: "Duplicate.", category: "project", icon: "☑", fields: "[]", properties: "{}", sampleData: "" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: name is stable after registration', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), icon: fc.string({ minLength: 1, maxLength: 50 }), fields: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string({ minLength: 1, maxLength: 50 }), sampleData: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('apply'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), targetSchemaName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ category: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('preview'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = schemaTemplateHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(schemaTemplateHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: template without fields definition', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), label: fc.string({ minLength: 1, maxLength: 50 }), description: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), icon: fc.string({ minLength: 1, maxLength: 50 }), fields: fc.string({ minLength: 1, maxLength: 50 }), properties: fc.string({ minLength: 1, maxLength: 50 }), sampleData: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('apply'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), targetSchemaName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({ category: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('preview'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = schemaTemplateHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(schemaTemplateHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: template without fields definition
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
