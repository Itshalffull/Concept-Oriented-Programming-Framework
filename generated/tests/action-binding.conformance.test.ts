// ActionBinding Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { actionBindingHandler } from '../../handlers/ts/app/action-binding.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ActionBinding functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('bind', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionBindingHandler.bind !== 'function') return;
      const result = await interpret(actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "bind_simple" -> ok', async () => {
      if (typeof actionBindingHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "bind_with_confirm" -> ok', async () => {
      if (typeof actionBindingHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionBindingHandler.bind({ binding: "invoice-void-btn", target: "Invoice/void", parameterMap: "{\"invoiceId\": \"row.id\"}", confirmWhen: "true", executionPolicy: "pessimistic", label: "Void Invoice", icon: "x-circle", buttonVariant: "destructive" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "bind_empty_target" -> invalid', async () => {
      if (typeof actionBindingHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionBindingHandler.bind({ binding: "bad-btn", target: "", parameterMap: "{}", executionPolicy: "auto" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "bind_bad_policy" -> invalid', async () => {
      if (typeof actionBindingHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionBindingHandler.bind({ binding: "bad-policy", target: "Task/complete", parameterMap: "{}", executionPolicy: "invalid-policy" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "bind_bad_json" -> invalid', async () => {
      if (typeof actionBindingHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionBindingHandler.bind({ binding: "bad-json", target: "Task/complete", parameterMap: "not-json", executionPolicy: "auto" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionBindingHandler.get({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionBindingHandler.get({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionBindingHandler.get({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionBindingHandler.get({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionBindingHandler.get({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionBindingHandler.get !== 'function') return;
      const result = await interpret(actionBindingHandler.get({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_existing" -> ok', async () => {
      if (typeof actionBindingHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_bind_simple = await interpret(actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" }), storage);
      const result = await interpret(actionBindingHandler.get({ binding: afterResult_bind_simple?.output?.["binding"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> notfound', async () => {
      if (typeof actionBindingHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionBindingHandler.get({ binding: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('invoke', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionBindingHandler.invoke({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"}, context: "{\"row\": {\"id\": \"task-1\"}}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionBindingHandler.invoke({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"}, context: "{\"row\": {\"id\": \"task-1\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionBindingHandler.invoke({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"}, context: "{\"row\": {\"id\": \"task-1\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionBindingHandler.invoke({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"}, context: "{\"row\": {\"id\": \"task-1\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionBindingHandler.invoke({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"}, context: "{\"row\": {\"id\": \"task-1\"}}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionBindingHandler.invoke !== 'function') return;
      const result = await interpret(actionBindingHandler.invoke({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"}, context: "{\"row\": {\"id\": \"task-1\"}}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "invoke_simple" -> ok', async () => {
      if (typeof actionBindingHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_bind_simple = await interpret(actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" }), storage);
      const result = await interpret(actionBindingHandler.invoke({ binding: afterResult_bind_simple?.output?.["binding"], context: "{\"row\": {\"id\": \"task-1\"}}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invoke_needs_confirm" -> confirming', async () => {
      if (typeof actionBindingHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_bind_with_confirm = await interpret(actionBindingHandler.bind({ binding: "invoice-void-btn", target: "Invoice/void", parameterMap: "{\"invoiceId\": \"row.id\"}", confirmWhen: "true", executionPolicy: "pessimistic", label: "Void Invoice", icon: "x-circle", buttonVariant: "destructive" }), storage);
      const result = await interpret(actionBindingHandler.invoke({ binding: afterResult_bind_with_confirm?.output?.["binding"], context: "{\"row\": {\"id\": \"inv-1\"}}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('confirming'));
    });

    it('fixture "invoke_missing" -> notfound', async () => {
      if (typeof actionBindingHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionBindingHandler.invoke({ binding: "nonexistent", context: "{}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('confirm', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionBindingHandler.confirm({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionBindingHandler.confirm({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionBindingHandler.confirm({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionBindingHandler.confirm({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionBindingHandler.confirm({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionBindingHandler.confirm !== 'function') return;
      const result = await interpret(actionBindingHandler.confirm({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "confirm_pending" -> ok', async () => {
      if (typeof actionBindingHandler.confirm !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_bind_with_confirm = await interpret(actionBindingHandler.bind({ binding: "invoice-void-btn", target: "Invoice/void", parameterMap: "{\"invoiceId\": \"row.id\"}", confirmWhen: "true", executionPolicy: "pessimistic", label: "Void Invoice", icon: "x-circle", buttonVariant: "destructive" }), storage);
      const afterResult_invoke_needs_confirm = await interpret(actionBindingHandler.invoke({ binding: afterResult_bind_with_confirm?.output?.["binding"], context: "{\"row\": {\"id\": \"inv-1\"}}" }), storage);
      const result = await interpret(actionBindingHandler.confirm({ binding: afterResult_bind_with_confirm?.output?.["binding"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "confirm_not_pending" -> expired', async () => {
      if (typeof actionBindingHandler.confirm !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_bind_simple = await interpret(actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" }), storage);
      const result = await interpret(actionBindingHandler.confirm({ binding: afterResult_bind_simple?.output?.["binding"] }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('expired'));
    });

  });

  describe('cancel', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionBindingHandler.cancel({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionBindingHandler.cancel({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionBindingHandler.cancel({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionBindingHandler.cancel({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionBindingHandler.cancel({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionBindingHandler.cancel !== 'function') return;
      const result = await interpret(actionBindingHandler.cancel({ binding: {"type":"ref","fixture":"bind_with_confirm","field":"binding"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "cancel_pending" -> ok', async () => {
      if (typeof actionBindingHandler.cancel !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_bind_with_confirm = await interpret(actionBindingHandler.bind({ binding: "invoice-void-btn", target: "Invoice/void", parameterMap: "{\"invoiceId\": \"row.id\"}", confirmWhen: "true", executionPolicy: "pessimistic", label: "Void Invoice", icon: "x-circle", buttonVariant: "destructive" }), storage);
      const afterResult_invoke_needs_confirm = await interpret(actionBindingHandler.invoke({ binding: afterResult_bind_with_confirm?.output?.["binding"], context: "{\"row\": {\"id\": \"inv-1\"}}" }), storage);
      const result = await interpret(actionBindingHandler.cancel({ binding: afterResult_bind_with_confirm?.output?.["binding"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "cancel_missing" -> notfound', async () => {
      if (typeof actionBindingHandler.cancel !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionBindingHandler.cancel({ binding: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionBindingHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionBindingHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionBindingHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionBindingHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionBindingHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionBindingHandler.list !== 'function') return;
      const result = await interpret(actionBindingHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_all" -> ok', async () => {
      if (typeof actionBindingHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_bind_simple = await interpret(actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" }), storage);
      const _pool = Object.assign({}, (afterResult_bind_simple?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(actionBindingHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('remove', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionBindingHandler.remove({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionBindingHandler.remove({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionBindingHandler.remove({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionBindingHandler.remove({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionBindingHandler.remove({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionBindingHandler.remove !== 'function') return;
      const result = await interpret(actionBindingHandler.remove({ binding: {"type":"ref","fixture":"bind_simple","field":"binding"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "remove_existing" -> ok', async () => {
      if (typeof actionBindingHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_bind_simple = await interpret(actionBindingHandler.bind({ binding: "task-complete-btn", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" }), storage);
      const result = await interpret(actionBindingHandler.remove({ binding: afterResult_bind_simple?.output?.["binding"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "remove_missing" -> notfound', async () => {
      if (typeof actionBindingHandler.remove !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionBindingHandler.remove({ binding: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof actionBindingHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = actionBindingHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ActionBinding');
    });
  });

  describe('invariant examples', () => {
    it("bind then invoke", async () => {
      const storage = createInMemoryStorage();
      const bindResult0 = await interpret(actionBindingHandler.bind({ binding: "test-b", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" }), storage);
      expect(bindResult0.variant).toBe("ok");
      let binding = bindResult0.output["binding"];
      let b = binding;
      const thenResult0 = await interpret(actionBindingHandler.invoke({ binding: b, context: "{\"row\": {\"id\": \"task-1\"}}" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("bind with confirm then invoke returns confirming", async () => {
      const storage = createInMemoryStorage();
      const bindResult0 = await interpret(actionBindingHandler.bind({ binding: "test-b", target: "Invoice/void", parameterMap: "{\"invoiceId\": \"row.id\"}", confirmWhen: "true", executionPolicy: "pessimistic" }), storage);
      expect(bindResult0.variant).toBe("ok");
      let binding = bindResult0.output["binding"];
      let b = binding;
      const thenResult0 = await interpret(actionBindingHandler.invoke({ binding: b, context: "{\"row\": {\"id\": \"inv-1\"}}" }), storage);
      expect(thenResult0.variant).toBe("confirming");
    });

    it("bind then get returns config", async () => {
      const storage = createInMemoryStorage();
      const bindResult0 = await interpret(actionBindingHandler.bind({ binding: "test-b", target: "Task/complete", parameterMap: "{\"taskId\": \"row.id\"}", executionPolicy: "auto" }), storage);
      expect(bindResult0.variant).toBe("ok");
      let binding = bindResult0.output["binding"];
      let b = binding;
      const thenResult0 = await interpret(actionBindingHandler.get({ binding: b }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: bindings have required fields', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('bind'), input: fc.record({ binding: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }), parameterMap: fc.string({ minLength: 1, maxLength: 50 }), precondition: fc.string(), confirmWhen: fc.string(), executionPolicy: fc.string({ minLength: 1, maxLength: 50 }), retryPolicy: fc.string(), reversalAction: fc.string(), label: fc.string(), icon: fc.string(), buttonVariant: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ binding: fc.string(), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('confirm'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('cancel'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ binding: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionBindingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(actionBindingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned binding', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('bind'), input: fc.record({ binding: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }), parameterMap: fc.string({ minLength: 1, maxLength: 50 }), precondition: fc.string(), confirmWhen: fc.string(), executionPolicy: fc.string({ minLength: 1, maxLength: 50 }), retryPolicy: fc.string(), reversalAction: fc.string(), label: fc.string(), icon: fc.string(), buttonVariant: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('invoke'), input: fc.record({ binding: fc.string(), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('confirm'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('cancel'), input: fc.record({ binding: fc.string() }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('remove'), input: fc.record({ binding: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionBindingHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(actionBindingHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned binding
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('bind handles empty input: ', async () => {
      if (typeof actionBindingHandler.bind !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionBindingHandler.bind({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('bind ensures on ok: ', async () => {
      if (typeof actionBindingHandler.bind !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ binding: fc.string(), target: fc.string({ minLength: 1, maxLength: 50 }), parameterMap: fc.string({ minLength: 1, maxLength: 50 }), precondition: fc.string(), confirmWhen: fc.string(), executionPolicy: fc.string({ minLength: 1, maxLength: 50 }), retryPolicy: fc.string(), reversalAction: fc.string(), label: fc.string(), icon: fc.string(), buttonVariant: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = actionBindingHandler.bind(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('invoke handles empty input: ', async () => {
      if (typeof actionBindingHandler.invoke !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionBindingHandler.invoke({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('confirm handles empty input: ', async () => {
      if (typeof actionBindingHandler.confirm !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionBindingHandler.confirm({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

});
