// ParallelismProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { parallelismProviderHandler } from '../../handlers/ts/monadic/providers/parallelism-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ParallelismProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('analyze', () => {
    it('builds a valid StorageProgram', () => {
      const program = parallelismProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"get\",\"relation\":\"users\",\"key\":\"u1\",\"bindAs\":\"userResult\"},{\"tag\":\"get\",\"relation\":\"orders\",\"key\":\"o1\",\"bindAs\":\"orderResult\"},{\"tag\":\"pure\",\"value\":{\"variant\":\"ok\"}}]}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = parallelismProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"get\",\"relation\":\"users\",\"key\":\"u1\",\"bindAs\":\"userResult\"},{\"tag\":\"get\",\"relation\":\"orders\",\"key\":\"o1\",\"bindAs\":\"orderResult\"},{\"tag\":\"pure\",\"value\":{\"variant\":\"ok\"}}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = parallelismProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"get\",\"relation\":\"users\",\"key\":\"u1\",\"bindAs\":\"userResult\"},{\"tag\":\"get\",\"relation\":\"orders\",\"key\":\"o1\",\"bindAs\":\"orderResult\"},{\"tag\":\"pure\",\"value\":{\"variant\":\"ok\"}}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = parallelismProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"get\",\"relation\":\"users\",\"key\":\"u1\",\"bindAs\":\"userResult\"},{\"tag\":\"get\",\"relation\":\"orders\",\"key\":\"o1\",\"bindAs\":\"orderResult\"},{\"tag\":\"pure\",\"value\":{\"variant\":\"ok\"}}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = parallelismProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"get\",\"relation\":\"users\",\"key\":\"u1\",\"bindAs\":\"userResult\"},{\"tag\":\"get\",\"relation\":\"orders\",\"key\":\"o1\",\"bindAs\":\"orderResult\"},{\"tag\":\"pure\",\"value\":{\"variant\":\"ok\"}}]}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof parallelismProviderHandler.analyze !== 'function') return;
      const result = await interpret(parallelismProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"get\",\"relation\":\"users\",\"key\":\"u1\",\"bindAs\":\"userResult\"},{\"tag\":\"get\",\"relation\":\"orders\",\"key\":\"o1\",\"bindAs\":\"orderResult\"},{\"tag\":\"pure\",\"value\":{\"variant\":\"ok\"}}]}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "independent_gets" -> ok', async () => {
      if (typeof parallelismProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parallelismProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"get\",\"relation\":\"users\",\"key\":\"u1\",\"bindAs\":\"userResult\"},{\"tag\":\"get\",\"relation\":\"orders\",\"key\":\"o1\",\"bindAs\":\"orderResult\"},{\"tag\":\"pure\",\"value\":{\"variant\":\"ok\"}}]}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "linear_chain" -> ok', async () => {
      if (typeof parallelismProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parallelismProviderHandler.analyze({ program: "{\"instructions\":[{\"tag\":\"get\",\"relation\":\"users\",\"key\":\"u1\",\"bindAs\":\"userResult\"},{\"tag\":\"pureFrom\",\"fn\":\"identity\"}]}" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invalid_json" -> error', async () => {
      if (typeof parallelismProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parallelismProviderHandler.analyze({ program: "not valid json{{{" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof parallelismProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = parallelismProviderHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('ParallelismProvider');
    });
  });

  describe('invariant examples', () => {
    it("independent gets yield parallelism >= 2", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(parallelismProviderHandler.analyze({ program: "get(users, u1, userResult); get(orders, o1, orderResult)" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let result = analyzeResult0.output["result"];
      let p = result;
      let layers = analyzeResult0.output["layers"];
      let l = layers;
      let dependencyEdges = analyzeResult0.output["dependencyEdges"];
      let e = dependencyEdges;
      let criticalPathLength = analyzeResult0.output["criticalPathLength"];
      let cpl = criticalPathLength;
      let maxParallelism = analyzeResult0.output["maxParallelism"];
      let mp = maxParallelism;
      let speedupRatio = analyzeResult0.output["speedupRatio"];
      let sr = speedupRatio;
    });

    it("dependent chain has critical path length 2", async () => {
      const storage = createInMemoryStorage();
      const analyzeResult0 = await interpret(parallelismProviderHandler.analyze({ program: "get(users, u1, userResult); pureFrom(fn)" }), storage);
      expect(analyzeResult0.variant).toBe("ok");
      let result = analyzeResult0.output["result"];
      let p = result;
      let layers = analyzeResult0.output["layers"];
      let l = layers;
      let dependencyEdges = analyzeResult0.output["dependencyEdges"];
      let e = dependencyEdges;
      let criticalPathLength = analyzeResult0.output["criticalPathLength"];
      let cpl = criticalPathLength;
      let maxParallelism = analyzeResult0.output["maxParallelism"];
      let mp = maxParallelism;
      let speedupRatio = analyzeResult0.output["speedupRatio"];
      let sr = speedupRatio;
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: critical path is at least 1', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('analyze'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = parallelismProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(parallelismProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: speedup ratio is positive', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('analyze'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = parallelismProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(parallelismProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('analyze handles empty input: ', async () => {
      if (typeof parallelismProviderHandler.analyze !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(parallelismProviderHandler.analyze({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('analyze ensures on ok: ', async () => {
      if (typeof parallelismProviderHandler.analyze !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = parallelismProviderHandler.analyze(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
