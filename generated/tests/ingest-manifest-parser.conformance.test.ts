// IngestManifestParser Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { ingestManifestParserHandler } from '../../handlers/ts/framework/ingest-manifest-parser.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('IngestManifestParser functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('parse', () => {
    it('builds a valid StorageProgram', () => {
      const program = ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ingestManifestParserHandler.parse !== 'function') return;
      const result = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_devto" -> ok', async () => {
      if (typeof ingestManifestParserHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}));
      const _fixtureInput = { yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ingestManifestParserHandler.parse({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "minimal_valid" -> ok', async () => {
      if (typeof ingestManifestParserHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}));
      const _fixtureInput = { yaml: "sources:\n  svc:\n    displayName: \"My Service\"\n    baseUrl: \"https://api.example.com\"\n    specType: \"rest\"\n    auth:\n      type: api-key\n      header: \"X-Api-Key\"\n      value: \"${API_KEY}\"\n    concepts:\n      Item:\n        list:\n          method: GET\n          path: /items\n          transform:\n            id: \"$.id\"\n            name: \"$.name\"\n" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(ingestManifestParserHandler.parse({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invalid_yaml" -> invalid', async () => {
      if (typeof ingestManifestParserHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ingestManifestParserHandler.parse({ yaml: "sources: [\nnot: valid: yaml: {{{" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "missing_sources" -> invalid', async () => {
      if (typeof ingestManifestParserHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ingestManifestParserHandler.parse({ yaml: "displayName: \"Oops\"\nbaseUrl: \"https://api.example.com\"" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "missing_base_url" -> invalid', async () => {
      if (typeof ingestManifestParserHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  svc:\n    displayName: \"Svc\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${TOKEN}\"\n    concepts: {}\n" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "missing_auth" -> invalid', async () => {
      if (typeof ingestManifestParserHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  svc:\n    displayName: \"Svc\"\n    baseUrl: \"https://api.example.com\"\n    specType: \"rest\"\n    concepts: {}\n" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('validate', () => {
    it('builds a valid StorageProgram', () => {
      const program = ingestManifestParserHandler.validate({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ingestManifestParserHandler.validate({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ingestManifestParserHandler.validate({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ingestManifestParserHandler.validate({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ingestManifestParserHandler.validate({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ingestManifestParserHandler.validate !== 'function') return;
      const result = await interpret(ingestManifestParserHandler.validate({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "validate_ok" -> ok', async () => {
      if (typeof ingestManifestParserHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_valid_devto = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      const result = await interpret(ingestManifestParserHandler.validate({ manifest: afterResult_valid_devto?.output?.["manifest"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "validate_invalid_auth" -> ok', async () => {
      if (typeof ingestManifestParserHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_minimal_valid = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  svc:\n    displayName: \"My Service\"\n    baseUrl: \"https://api.example.com\"\n    specType: \"rest\"\n    auth:\n      type: api-key\n      header: \"X-Api-Key\"\n      value: \"${API_KEY}\"\n    concepts:\n      Item:\n        list:\n          method: GET\n          path: /items\n          transform:\n            id: \"$.id\"\n            name: \"$.name\"\n" }), storage);
      const result = await interpret(ingestManifestParserHandler.validate({ manifest: afterResult_minimal_valid?.output?.["manifest"] }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('merge', () => {
    it('builds a valid StorageProgram', () => {
      const program = ingestManifestParserHandler.merge({ base: {"type":"ref","fixture":"valid_devto","field":"manifest"}, override: {"type":"ref","fixture":"minimal_valid","field":"manifest"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ingestManifestParserHandler.merge({ base: {"type":"ref","fixture":"valid_devto","field":"manifest"}, override: {"type":"ref","fixture":"minimal_valid","field":"manifest"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ingestManifestParserHandler.merge({ base: {"type":"ref","fixture":"valid_devto","field":"manifest"}, override: {"type":"ref","fixture":"minimal_valid","field":"manifest"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ingestManifestParserHandler.merge({ base: {"type":"ref","fixture":"valid_devto","field":"manifest"}, override: {"type":"ref","fixture":"minimal_valid","field":"manifest"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ingestManifestParserHandler.merge({ base: {"type":"ref","fixture":"valid_devto","field":"manifest"}, override: {"type":"ref","fixture":"minimal_valid","field":"manifest"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ingestManifestParserHandler.merge !== 'function') return;
      const result = await interpret(ingestManifestParserHandler.merge({ base: {"type":"ref","fixture":"valid_devto","field":"manifest"}, override: {"type":"ref","fixture":"minimal_valid","field":"manifest"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "merge_ok" -> ok', async () => {
      if (typeof ingestManifestParserHandler.merge !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_valid_devto = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      const afterResult_minimal_valid = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  svc:\n    displayName: \"My Service\"\n    baseUrl: \"https://api.example.com\"\n    specType: \"rest\"\n    auth:\n      type: api-key\n      header: \"X-Api-Key\"\n      value: \"${API_KEY}\"\n    concepts:\n      Item:\n        list:\n          method: GET\n          path: /items\n          transform:\n            id: \"$.id\"\n            name: \"$.name\"\n" }), storage);
      const result = await interpret(ingestManifestParserHandler.merge({ base: afterResult_valid_devto?.output?.["manifest"], override: afterResult_minimal_valid?.output?.["manifest"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "merge_same_source" -> ok', async () => {
      if (typeof ingestManifestParserHandler.merge !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_valid_devto = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      const result = await interpret(ingestManifestParserHandler.merge({ base: afterResult_valid_devto?.output?.["manifest"], override: afterResult_valid_devto?.output?.["manifest"] }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('getSource', () => {
    it('builds a valid StorageProgram', () => {
      const program = ingestManifestParserHandler.getSource({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ingestManifestParserHandler.getSource({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ingestManifestParserHandler.getSource({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ingestManifestParserHandler.getSource({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ingestManifestParserHandler.getSource({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ingestManifestParserHandler.getSource !== 'function') return;
      const result = await interpret(ingestManifestParserHandler.getSource({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_source_ok" -> ok', async () => {
      if (typeof ingestManifestParserHandler.getSource !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_valid_devto = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      const result = await interpret(ingestManifestParserHandler.getSource({ manifest: afterResult_valid_devto?.output?.["manifest"], sourceName: "dev_to" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_source_missing" -> notfound', async () => {
      if (typeof ingestManifestParserHandler.getSource !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_valid_devto = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      const result = await interpret(ingestManifestParserHandler.getSource({ manifest: afterResult_valid_devto?.output?.["manifest"], sourceName: "nonexistent_source" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('getConcept', () => {
    it('builds a valid StorageProgram', () => {
      const program = ingestManifestParserHandler.getConcept({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to", conceptName: "Article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ingestManifestParserHandler.getConcept({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to", conceptName: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ingestManifestParserHandler.getConcept({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to", conceptName: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ingestManifestParserHandler.getConcept({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to", conceptName: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ingestManifestParserHandler.getConcept({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to", conceptName: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ingestManifestParserHandler.getConcept !== 'function') return;
      const result = await interpret(ingestManifestParserHandler.getConcept({ manifest: {"type":"ref","fixture":"valid_devto","field":"manifest"}, sourceName: "dev_to", conceptName: "Article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_concept_ok" -> ok', async () => {
      if (typeof ingestManifestParserHandler.getConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_valid_devto = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      const result = await interpret(ingestManifestParserHandler.getConcept({ manifest: afterResult_valid_devto?.output?.["manifest"], sourceName: "dev_to", conceptName: "Article" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_concept_missing_source" -> notfound', async () => {
      if (typeof ingestManifestParserHandler.getConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_valid_devto = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      const result = await interpret(ingestManifestParserHandler.getConcept({ manifest: afterResult_valid_devto?.output?.["manifest"], sourceName: "no_such_source", conceptName: "Article" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

    it('fixture "get_concept_missing_concept" -> notfound', async () => {
      if (typeof ingestManifestParserHandler.getConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_valid_devto = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      const result = await interpret(ingestManifestParserHandler.getConcept({ manifest: afterResult_valid_devto?.output?.["manifest"], sourceName: "dev_to", conceptName: "NoSuchConcept" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = ingestManifestParserHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = ingestManifestParserHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = ingestManifestParserHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = ingestManifestParserHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = ingestManifestParserHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof ingestManifestParserHandler.list !== 'function') return;
      const result = await interpret(ingestManifestParserHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof ingestManifestParserHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(ingestManifestParserHandler.list({  }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_after_parse" -> ok', async () => {
      if (typeof ingestManifestParserHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_list_empty = await interpret(ingestManifestParserHandler.list({  }), storage);
      const afterResult_valid_devto = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  dev_to:\n    displayName: \"DEV.to\"\n    baseUrl: \"https://dev.to/api\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${DEV_TO_TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          params:\n            username: \"${DEV_TO_USERNAME}\"\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n            tags: \"$.tags\"\n        get:\n          method: GET\n          path: /articles/{id}\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n            body: \"$.body_markdown\"\n    webhooks:\n      endpoint: /webhooks/dev_to\n      events:\n        article_created: Article/create\n        article_updated: Article/update\n" }), storage);
      const _pool = Object.assign({}, (afterResult_list_empty?.output ?? {}), (afterResult_valid_devto?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(ingestManifestParserHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof ingestManifestParserHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = ingestManifestParserHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('IngestManifestParser');
    });
  });

  describe('invariant examples', () => {
    it("parse then retrieve source", async () => {
      const storage = createInMemoryStorage();
      const parseResult0 = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  svc:\n    displayName: \"Svc\"\n    baseUrl: \"https://api.example.com\"\n    specType: \"rest\"\n    auth:\n      type: api-key\n      header: \"X-Api-Key\"\n      value: \"${KEY}\"\n    concepts:\n      Item:\n        list:\n          method: GET\n          path: /items\n          transform:\n            id: \"$.id\"\n            name: \"$.name\"\n" }), storage);
      expect(parseResult0.variant).toBe("ok");
      let manifest = parseResult0.output["manifest"];
      let m = manifest;
      const thenResult0 = await interpret(ingestManifestParserHandler.getSource({ manifest: m, sourceName: "svc" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("parse then retrieve concept mappings", async () => {
      const storage = createInMemoryStorage();
      const parseResult0 = await interpret(ingestManifestParserHandler.parse({ yaml: "sources:\n  svc:\n    displayName: \"Svc\"\n    baseUrl: \"https://api.example.com\"\n    specType: \"rest\"\n    auth:\n      type: bearer\n      token: \"${TOKEN}\"\n    concepts:\n      Article:\n        list:\n          method: GET\n          path: /articles\n          transform:\n            id: \"$.id\"\n            title: \"$.title\"\n" }), storage);
      expect(parseResult0.variant).toBe("ok");
      let manifest = parseResult0.output["manifest"];
      let m = manifest;
      const thenResult0 = await interpret(ingestManifestParserHandler.getConcept({ manifest: m, sourceName: "svc", conceptName: "Article" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("parse invalid yaml fails", async () => {
      const storage = createInMemoryStorage();
      const parseResult0 = await interpret(ingestManifestParserHandler.parse({ yaml: "" }), storage);
      expect(parseResult0.variant).toBe("invalid");
      let message = parseResult0.output["message"];
      let msg = message;
      let errors = parseResult0.output["errors"];
      let errs = errors;
      const thenResult0 = await interpret(ingestManifestParserHandler.list({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: all manifests have a baseUrl', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('parse'), input: fc.record({ yaml: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ manifest: fc.string() }) }),
              fc.record({ action: fc.constant('merge'), input: fc.record({ base: fc.string(), override: fc.string() }) }),
              fc.record({ action: fc.constant('getSource'), input: fc.record({ manifest: fc.string(), sourceName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getConcept'), input: fc.record({ manifest: fc.string(), sourceName: fc.string({ minLength: 1, maxLength: 50 }), conceptName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = ingestManifestParserHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(ingestManifestParserHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: manifests stored without raw yaml', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('parse'), input: fc.record({ yaml: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ manifest: fc.string() }) }),
              fc.record({ action: fc.constant('merge'), input: fc.record({ base: fc.string(), override: fc.string() }) }),
              fc.record({ action: fc.constant('getSource'), input: fc.record({ manifest: fc.string(), sourceName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('getConcept'), input: fc.record({ manifest: fc.string(), sourceName: fc.string({ minLength: 1, maxLength: 50 }), conceptName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = ingestManifestParserHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(ingestManifestParserHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: manifests stored without raw yaml
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('validate handles empty input: ', async () => {
      if (typeof ingestManifestParserHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(ingestManifestParserHandler.validate({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

});
