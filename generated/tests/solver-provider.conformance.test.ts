// SolverProvider Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { solverProviderHandler } from '../../handlers/ts/suites/formal-verification/solver-provider.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SolverProvider functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof solverProviderHandler.register !== 'function') return;
      const result = await interpret(solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_register" -> ok', async () => {
      if (typeof solverProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_name" -> invalid', async () => {
      if (typeof solverProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(solverProviderHandler.register({ provider_id: "z3", name: "", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\"]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('dispatch', () => {
    it('builds a valid StorageProgram', () => {
      const program = solverProviderHandler.dispatch({ property_ref: {"type":"ref","fixture":"valid_register","field":"id"}, formal_language: "smtlib", kind: "invariant" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = solverProviderHandler.dispatch({ property_ref: {"type":"ref","fixture":"valid_register","field":"id"}, formal_language: "smtlib", kind: "invariant" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = solverProviderHandler.dispatch({ property_ref: {"type":"ref","fixture":"valid_register","field":"id"}, formal_language: "smtlib", kind: "invariant" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = solverProviderHandler.dispatch({ property_ref: {"type":"ref","fixture":"valid_register","field":"id"}, formal_language: "smtlib", kind: "invariant" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = solverProviderHandler.dispatch({ property_ref: {"type":"ref","fixture":"valid_register","field":"id"}, formal_language: "smtlib", kind: "invariant" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof solverProviderHandler.dispatch !== 'function') return;
      const result = await interpret(solverProviderHandler.dispatch({ property_ref: {"type":"ref","fixture":"valid_register","field":"id"}, formal_language: "smtlib", kind: "invariant" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_dispatch" -> ok', async () => {
      if (typeof solverProviderHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" }), storage);
      const result = await interpret(solverProviderHandler.dispatch({ property_ref: afterResult_valid_register?.output?.["id"], formal_language: "smtlib", kind: "invariant" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "no_matching_provider" -> no_provider', async () => {
      if (typeof solverProviderHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(solverProviderHandler.dispatch({ property_ref: "prop-2", formal_language: "coq", kind: "liveness" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('no_provider'));
    });

  });

  describe('dispatch_batch', () => {
    it('builds a valid StorageProgram', () => {
      const program = solverProviderHandler.dispatch_batch({ property_refs: "[\"prop-1\",\"prop-2\"]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = solverProviderHandler.dispatch_batch({ property_refs: "[\"prop-1\",\"prop-2\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = solverProviderHandler.dispatch_batch({ property_refs: "[\"prop-1\",\"prop-2\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = solverProviderHandler.dispatch_batch({ property_refs: "[\"prop-1\",\"prop-2\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = solverProviderHandler.dispatch_batch({ property_refs: "[\"prop-1\",\"prop-2\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof solverProviderHandler.dispatch_batch !== 'function') return;
      const result = await interpret(solverProviderHandler.dispatch_batch({ property_refs: "[\"prop-1\",\"prop-2\"]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_batch" -> ok', async () => {
      if (typeof solverProviderHandler.dispatch_batch !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { property_refs: "[\"prop-1\",\"prop-2\"]" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(solverProviderHandler.dispatch_batch({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_batch" -> invalid', async () => {
      if (typeof solverProviderHandler.dispatch_batch !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(solverProviderHandler.dispatch_batch({ property_refs: "[]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('health_check', () => {
    it('builds a valid StorageProgram', () => {
      const program = solverProviderHandler.health_check({ provider_id: "z3" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = solverProviderHandler.health_check({ provider_id: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = solverProviderHandler.health_check({ provider_id: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = solverProviderHandler.health_check({ provider_id: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = solverProviderHandler.health_check({ provider_id: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof solverProviderHandler.health_check !== 'function') return;
      const result = await interpret(solverProviderHandler.health_check({ provider_id: "z3" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_health" -> ok', async () => {
      if (typeof solverProviderHandler.health_check !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { provider_id: "z3" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(solverProviderHandler.health_check({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "health_missing" -> notfound', async () => {
      if (typeof solverProviderHandler.health_check !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(solverProviderHandler.health_check({ provider_id: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('list', () => {
    it('builds a valid StorageProgram', () => {
      const program = solverProviderHandler.list({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = solverProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = solverProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = solverProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = solverProviderHandler.list({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof solverProviderHandler.list !== 'function') return;
      const result = await interpret(solverProviderHandler.list({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof solverProviderHandler.list !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(solverProviderHandler.list({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('unregister', () => {
    it('builds a valid StorageProgram', () => {
      const program = solverProviderHandler.unregister({ provider_id: "z3" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = solverProviderHandler.unregister({ provider_id: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = solverProviderHandler.unregister({ provider_id: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = solverProviderHandler.unregister({ provider_id: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = solverProviderHandler.unregister({ provider_id: "z3" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof solverProviderHandler.unregister !== 'function') return;
      const result = await interpret(solverProviderHandler.unregister({ provider_id: "z3" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_unregister" -> ok', async () => {
      if (typeof solverProviderHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_register = await interpret(solverProviderHandler.register({ provider_id: "z3", name: "Z3 SMT Solver", supported_languages: "[\"smtlib\"]", supported_kinds: "[\"invariant\",\"precondition\",\"postcondition\",\"safety\"]", priority: "1" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_register?.output ?? {}));
      const _fixtureInput = { provider_id: "z3" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) _fixtureInput[k] = v;
      }
      const result = await interpret(solverProviderHandler.unregister({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unregister_missing" -> notfound', async () => {
      if (typeof solverProviderHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(solverProviderHandler.unregister({ provider_id: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });


  describe('invariant examples', () => {
    it("register-then-dispatch", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(solverProviderHandler.register({ provider_id: {"type":"literal","value":"z3"}, supported_languages: {"type":"list","items":[{"type":"literal","value":"smtlib"}]}, supported_kinds: {"type":"list","items":[{"type":"literal","value":"invariant"},{"type":"literal","value":"precondition"},{"type":"literal","value":"postcondition"},{"type":"literal","value":"safety"}]}, capabilities: {"type":"list","items":[{"type":"literal","value":"smt"},{"type":"literal","value":"quantifiers"},{"type":"literal","value":"theories"}]}, priority: {"type":"literal","value":1} }), storage);
      expect(registerResult0.variant).toBe("ok");
      let provider = registerResult0.output["provider"];
      const thenResult0 = await interpret(solverProviderHandler.dispatch({ property_ref: {"type":"literal","value":"prop-1"}, formal_language: {"type":"literal","value":"smtlib"}, kind: {"type":"literal","value":"invariant"}, timeout_ms: {"type":"literal","value":5000} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-provider_id', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider_id: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), supported_languages: fc.string({ minLength: 1, maxLength: 50 }), supported_kinds: fc.string({ minLength: 1, maxLength: 50 }), endpoint: fc.string(), priority: fc.string() }) }),
              fc.record({ action: fc.constant('dispatch'), input: fc.record({ property_ref: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dispatch_batch'), input: fc.record({ property_refs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('health_check'), input: fc.record({ provider_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ provider_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = solverProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(solverProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-supported_languages', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider_id: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), supported_languages: fc.string({ minLength: 1, maxLength: 50 }), supported_kinds: fc.string({ minLength: 1, maxLength: 50 }), endpoint: fc.string(), priority: fc.string() }) }),
              fc.record({ action: fc.constant('dispatch'), input: fc.record({ property_ref: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dispatch_batch'), input: fc.record({ property_refs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('health_check'), input: fc.record({ provider_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('list'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ provider_id: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = solverProviderHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(solverProviderHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-supported_languages
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof solverProviderHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(solverProviderHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof solverProviderHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ provider_id: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), supported_languages: fc.string({ minLength: 1, maxLength: 50 }), supported_kinds: fc.string({ minLength: 1, maxLength: 50 }), endpoint: fc.string(), priority: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = solverProviderHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
