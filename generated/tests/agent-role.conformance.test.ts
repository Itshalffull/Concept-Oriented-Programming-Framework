// AgentRole Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { agentRoleHandler } from '../../handlers/ts/llm-agent/agent-role.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('AgentRole functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('define', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRoleHandler.define({ name: 'test-name', capabilities: 'test', constraints: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRoleHandler.define({ name: 'test-name', capabilities: 'test', constraints: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRoleHandler.define({ name: 'test-name', capabilities: 'test', constraints: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRoleHandler.define({ name: 'test-name', capabilities: 'test', constraints: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRoleHandler.define({ name: 'test-name', capabilities: 'test', constraints: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRoleHandler.define !== 'function') return;
      const result = await interpret(agentRoleHandler.define({ name: 'test-name', capabilities: 'test', constraints: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('bid', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRoleHandler.bid({ role: 'test', task_description: 'test-task_description', task_type: 'test-task_type' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRoleHandler.bid({ role: 'test', task_description: 'test-task_description', task_type: 'test-task_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRoleHandler.bid({ role: 'test', task_description: 'test-task_description', task_type: 'test-task_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRoleHandler.bid({ role: 'test', task_description: 'test-task_description', task_type: 'test-task_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRoleHandler.bid({ role: 'test', task_description: 'test-task_description', task_type: 'test-task_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRoleHandler.bid !== 'function') return;
      const result = await interpret(agentRoleHandler.bid({ role: 'test', task_description: 'test-task_description', task_type: 'test-task_type' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('match', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRoleHandler.match({ task_type: 'test-task_type' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRoleHandler.match({ task_type: 'test-task_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRoleHandler.match({ task_type: 'test-task_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRoleHandler.match({ task_type: 'test-task_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRoleHandler.match({ task_type: 'test-task_type' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRoleHandler.match !== 'function') return;
      const result = await interpret(agentRoleHandler.match({ task_type: 'test-task_type' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('recordOutcome', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRoleHandler.recordOutcome({ role: 'test', task_type: 'test-task_type', success: true, latency_ms: 1, cost: 1 });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRoleHandler.recordOutcome({ role: 'test', task_type: 'test-task_type', success: true, latency_ms: 1, cost: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRoleHandler.recordOutcome({ role: 'test', task_type: 'test-task_type', success: true, latency_ms: 1, cost: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRoleHandler.recordOutcome({ role: 'test', task_type: 'test-task_type', success: true, latency_ms: 1, cost: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRoleHandler.recordOutcome({ role: 'test', task_type: 'test-task_type', success: true, latency_ms: 1, cost: 1 });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRoleHandler.recordOutcome !== 'function') return;
      const result = await interpret(agentRoleHandler.recordOutcome({ role: 'test', task_type: 'test-task_type', success: true, latency_ms: 1, cost: 1 }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('getAvailability', () => {
    it('builds a valid StorageProgram', () => {
      const program = agentRoleHandler.getAvailability({ role: 'test' });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = agentRoleHandler.getAvailability({ role: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = agentRoleHandler.getAvailability({ role: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = agentRoleHandler.getAvailability({ role: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = agentRoleHandler.getAvailability({ role: 'test' });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof agentRoleHandler.getAvailability !== 'function') return;
      const result = await interpret(agentRoleHandler.getAvailability({ role: 'test' }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof agentRoleHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = agentRoleHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('AgentRole');
    });
  });

  describe('invariant examples', () => {
    it("define-then-bid", async () => {
      const storage = createInMemoryStorage();
      const defineResult0 = await interpret(agentRoleHandler.define({ name: "researcher", capabilities: [{"task_type":"search","proficiency":0.9}], constraints: {"max_concurrent":3,"required_tools":["web_search"],"expertise_domains":["science"]} }), storage);
      expect(defineResult0.variant).toBe("ok");
      let role = defineResult0.output["role"];
      let k = role;
      const thenResult0 = await interpret(agentRoleHandler.bid({ role: k, task_description: "Find papers on X", task_type: "search" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('define'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), capabilities: fc.string(), constraints: fc.string() }) }),
              fc.record({ action: fc.constant('bid'), input: fc.record({ role: fc.string(), task_description: fc.string({ minLength: 1, maxLength: 50 }), task_type: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ task_type: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordOutcome'), input: fc.record({ role: fc.string(), task_type: fc.string({ minLength: 1, maxLength: 50 }), success: fc.boolean(), latency_ms: fc.integer({ min: 1, max: 1000 }), cost: fc.string() }) }),
              fc.record({ action: fc.constant('getAvailability'), input: fc.record({ role: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = agentRoleHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(agentRoleHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('define'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), capabilities: fc.string(), constraints: fc.string() }) }),
              fc.record({ action: fc.constant('bid'), input: fc.record({ role: fc.string(), task_description: fc.string({ minLength: 1, maxLength: 50 }), task_type: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('match'), input: fc.record({ task_type: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('recordOutcome'), input: fc.record({ role: fc.string(), task_type: fc.string({ minLength: 1, maxLength: 50 }), success: fc.boolean(), latency_ms: fc.integer({ min: 1, max: 1000 }), cost: fc.string() }) }),
              fc.record({ action: fc.constant('getAvailability'), input: fc.record({ role: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = agentRoleHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(agentRoleHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-name
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('define handles empty input: ', async () => {
      if (typeof agentRoleHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(agentRoleHandler.define({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('define ensures on ok: ', async () => {
      if (typeof agentRoleHandler.define !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), capabilities: fc.string(), constraints: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = agentRoleHandler.define(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
