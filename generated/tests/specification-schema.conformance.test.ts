// SpecificationSchema Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { specificationSchemaHandler } from '../../handlers/ts/suites/formal-verification/specification-schema.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('SpecificationSchema functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('define', () => {
    it('builds a valid StorageProgram', () => {
      const program = specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof specificationSchemaHandler.define !== 'function') return;
      const result = await interpret(specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_define" -> ok', async () => {
      if (typeof specificationSchemaHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "bad_category" -> invalid', async () => {
      if (typeof specificationSchemaHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(specificationSchemaHandler.define({ name: "test-schema", category: "unknown_category", pattern_type: "absence", template_text: "G(p)", formal_language: "ltl", parameters: "[]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "missing_name" -> invalid', async () => {
      if (typeof specificationSchemaHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(specificationSchemaHandler.define({ name: "", category: "dwyer_pattern", pattern_type: "absence", template_text: "G(p)", formal_language: "ltl", parameters: "[]" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('instantiate', () => {
    it('builds a valid StorageProgram', () => {
      const program = specificationSchemaHandler.instantiate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = specificationSchemaHandler.instantiate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = specificationSchemaHandler.instantiate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = specificationSchemaHandler.instantiate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = specificationSchemaHandler.instantiate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof specificationSchemaHandler.instantiate !== 'function') return;
      const result = await interpret(specificationSchemaHandler.instantiate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_instantiate" -> ok', async () => {
      if (typeof specificationSchemaHandler.instantiate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(specificationSchemaHandler.instantiate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_param" -> invalid', async () => {
      if (typeof specificationSchemaHandler.instantiate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { schema_id: "ss-abc123", param_values: "{}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(specificationSchemaHandler.instantiate({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

    it('fixture "bad_json" -> invalid', async () => {
      if (typeof specificationSchemaHandler.instantiate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(specificationSchemaHandler.instantiate({ schema_id: "ss-abc123", param_values: "not-json" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('validate', () => {
    it('builds a valid StorageProgram', () => {
      const program = specificationSchemaHandler.validate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = specificationSchemaHandler.validate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = specificationSchemaHandler.validate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = specificationSchemaHandler.validate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = specificationSchemaHandler.validate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof specificationSchemaHandler.validate !== 'function') return;
      const result = await interpret(specificationSchemaHandler.validate({ schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_validate" -> ok', async () => {
      if (typeof specificationSchemaHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { schema_id: "ss-abc123", param_values: "{\"function\":\"transfer\"}" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(specificationSchemaHandler.validate({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "bad_param_values" -> invalid', async () => {
      if (typeof specificationSchemaHandler.validate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(specificationSchemaHandler.validate({ schema_id: "ss-abc123", param_values: "not-json" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('list_by_category', () => {
    it('builds a valid StorageProgram', () => {
      const program = specificationSchemaHandler.list_by_category({ category: "smart_contract" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = specificationSchemaHandler.list_by_category({ category: "smart_contract" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = specificationSchemaHandler.list_by_category({ category: "smart_contract" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = specificationSchemaHandler.list_by_category({ category: "smart_contract" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = specificationSchemaHandler.list_by_category({ category: "smart_contract" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof specificationSchemaHandler.list_by_category !== 'function') return;
      const result = await interpret(specificationSchemaHandler.list_by_category({ category: "smart_contract" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_list" -> ok', async () => {
      if (typeof specificationSchemaHandler.list_by_category !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { category: "smart_contract" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(specificationSchemaHandler.list_by_category({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_category" -> error', async () => {
      if (typeof specificationSchemaHandler.list_by_category !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { category: "nonexistent_category" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(specificationSchemaHandler.list_by_category({ ..._fixtureInput }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('search', () => {
    it('builds a valid StorageProgram', () => {
      const program = specificationSchemaHandler.search({ query: "reentrancy" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = specificationSchemaHandler.search({ query: "reentrancy" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = specificationSchemaHandler.search({ query: "reentrancy" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = specificationSchemaHandler.search({ query: "reentrancy" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = specificationSchemaHandler.search({ query: "reentrancy" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof specificationSchemaHandler.search !== 'function') return;
      const result = await interpret(specificationSchemaHandler.search({ query: "reentrancy" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "valid_search" -> ok', async () => {
      if (typeof specificationSchemaHandler.search !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_valid_define = await interpret(specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: "[{\"name\":\"function\",\"type\":\"String\",\"description\":\"Function to guard\"}]" }), storage);
      const _pool = Object.assign({}, (afterResult_valid_define?.output ?? {}));
      const _fixtureInput = { query: "reentrancy" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(specificationSchemaHandler.search({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "empty_query" -> invalid', async () => {
      if (typeof specificationSchemaHandler.search !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(specificationSchemaHandler.search({ query: "" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid'));
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof specificationSchemaHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = specificationSchemaHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      const name = result.output?.name ?? result.name;
      expect(name).toBe('SpecificationSchema');
    });
  });

  describe('invariant examples', () => {
    it("define-then-instantiate", async () => {
      const storage = createInMemoryStorage();
      const defineResult0 = await interpret(specificationSchemaHandler.define({ name: "reentrancy-guard", category: "smart_contract", pattern_type: "absence", template_text: "always (call_depth(${function}) <= 1)", formal_language: "smtlib", parameters: {"type":"list","items":[{"type":"record","fields":[{"name":"name","value":{"type":"literal","value":"function"}},{"name":"type","value":{"type":"literal","value":"String"}},{"name":"description","value":{"type":"literal","value":"Function to guard"}}]}]} }), storage);
      expect(defineResult0.variant).toBe("ok");
      let schema = defineResult0.output["schema"];
      let s = schema;
      const thenResult0 = await interpret(specificationSchemaHandler.instantiate({ schema: s, parameter_values: {"type":"record","fields":[{"name":"function","value":{"type":"literal","value":"transfer"}}]}, target_symbol: "clef/concept/Token" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: valid-name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('define'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), pattern_type: fc.string({ minLength: 1, maxLength: 50 }), template_text: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string({ minLength: 1, maxLength: 50 }), parameters: fc.string() }) }),
              fc.record({ action: fc.constant('instantiate'), input: fc.record({ schema: fc.string(), parameter_values: fc.string(), target_symbol: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ schema: fc.string(), parameter_values: fc.string() }) }),
              fc.record({ action: fc.constant('list_by_category'), input: fc.record({ category: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('search'), input: fc.record({ query: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = specificationSchemaHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(specificationSchemaHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: orphaned-category', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('define'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), pattern_type: fc.string({ minLength: 1, maxLength: 50 }), template_text: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string({ minLength: 1, maxLength: 50 }), parameters: fc.string() }) }),
              fc.record({ action: fc.constant('instantiate'), input: fc.record({ schema: fc.string(), parameter_values: fc.string(), target_symbol: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('validate'), input: fc.record({ schema: fc.string(), parameter_values: fc.string() }) }),
              fc.record({ action: fc.constant('list_by_category'), input: fc.record({ category: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('search'), input: fc.record({ query: fc.string({ minLength: 1, maxLength: 50 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = specificationSchemaHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(specificationSchemaHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: orphaned-category
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('define handles empty input: ', async () => {
      if (typeof specificationSchemaHandler.define !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(specificationSchemaHandler.define({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('define ensures on ok: ', async () => {
      if (typeof specificationSchemaHandler.define !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), category: fc.string({ minLength: 1, maxLength: 50 }), pattern_type: fc.string({ minLength: 1, maxLength: 50 }), template_text: fc.string({ minLength: 1, maxLength: 50 }), formal_language: fc.string({ minLength: 1, maxLength: 50 }), parameters: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = specificationSchemaHandler.define(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
