// ExternalCall Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { externalCallHandler } from '../../handlers/ts/execution/external-call.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('ExternalCall imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('initialize', () => {
    it('executes without crashing', async () => {
      if (typeof externalCallHandler.initialize !== 'function') return;
      try {
        const result = await externalCallHandler.initialize({  }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof externalCallHandler.initialize !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await externalCallHandler.initialize({  }, storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('registerProtocol', () => {
    it('executes without crashing', async () => {
      if (typeof externalCallHandler.registerProtocol !== 'function') return;
      try {
        const result = await externalCallHandler.registerProtocol({ protocol: "http", providerName: "HttpProvider" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "register_http" -> ok', async () => {
      if (typeof externalCallHandler.registerProtocol !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await externalCallHandler.registerProtocol({ protocol: "http", providerName: "HttpProvider" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof externalCallHandler.registerProtocol !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await externalCallHandler.registerProtocol({ protocol: "http", providerName: "AnotherHttpProvider" }, storage);
      expect(result.variant).toBe('duplicate');
    });

  });

  describe('dispatch', () => {
    it('executes without crashing', async () => {
      if (typeof externalCallHandler.dispatch !== 'function') return;
      try {
        const result = await externalCallHandler.dispatch({ protocol: "http", operation: "GET", endpoint: "https://api.example.com/users", payload: "{}", config: "{\"timeout\": 5000}" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "http_get" -> ok', async () => {
      if (typeof externalCallHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await externalCallHandler.dispatch({ protocol: "http", operation: "GET", endpoint: "https://api.example.com/users", payload: "{}", config: "{\"timeout\": 5000}" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "missing_protocol" -> protocolNotFound', async () => {
      if (typeof externalCallHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await externalCallHandler.dispatch({ protocol: "ftp", operation: "LIST", endpoint: "ftp://files.example.com", payload: "{}", config: "{}" }, storage);
      expect(result.variant).toBe('protocolNotFound');
    });

    it('fixture "bad_request" -> error', async () => {
      if (typeof externalCallHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await externalCallHandler.dispatch({ protocol: "http", operation: "POST", endpoint: "https://api.example.com/invalid", payload: "{\"broken\": true}", config: "{}" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('listProtocols', () => {
    it('executes without crashing', async () => {
      if (typeof externalCallHandler.listProtocols !== 'function') return;
      try {
        const result = await externalCallHandler.listProtocols({  }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "valid" -> ok', async () => {
      if (typeof externalCallHandler.listProtocols !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await externalCallHandler.listProtocols({  }, storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof externalCallHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      let result: any;
      try {
        const r = externalCallHandler.register({}, storage);
        result = r instanceof Promise ? await r : r;
        // If StorageProgram, interpret it
        if (result?.instructions && !result.variant) {
        }
      } catch { return; }
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('ExternalCall');
    });
  });

  describe('invariant examples', () => {
    it("dispatch fails without registered protocol", async () => {
      const storage = createInMemoryStorage();
      const dispatchResult0 = await externalCallHandler.dispatch({ protocol: {"type":"literal","value":"http"}, operation: {"type":"literal","value":"GET"}, endpoint: {"type":"literal","value":"test"}, payload: {"type":"literal","value":"{}"}, config: {"type":"literal","value":"{}"} }, storage);
      expect(dispatchResult0.variant).toBe("protocolNotFound");
      const protocol = dispatchResult0.output["protocol"];
      const thenResult0 = await externalCallHandler.registerProtocol({ protocol: {"type":"literal","value":"http"}, providerName: {"type":"literal","value":"HttpProvider"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate protocol registration rejected", async () => {
      const storage = createInMemoryStorage();
      const registerProtocolResult0 = await externalCallHandler.registerProtocol({ protocol: {"type":"literal","value":"grpc"}, providerName: {"type":"literal","value":"GrpcProvider"} }, storage);
      expect(registerProtocolResult0.variant).toBe("ok");
      const thenResult0 = await externalCallHandler.registerProtocol({ protocol: {"type":"literal","value":"grpc"}, providerName: {"type":"literal","value":"AnotherProvider"} }, storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: calls have valid protocol', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('initialize'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('registerProtocol'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), providerName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dispatch'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }), endpoint: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listProtocols'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = externalCallHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(externalCallHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: call without endpoint', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('initialize'), input: fc.record({  }) }),
              fc.record({ action: fc.constant('registerProtocol'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), providerName: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('dispatch'), input: fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }), endpoint: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listProtocols'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = externalCallHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(externalCallHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: call without endpoint
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('dispatch handles empty input: ', async () => {
      if (typeof externalCallHandler.dispatch !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await externalCallHandler.dispatch({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('dispatch ensures on ok: ', async () => {
      if (typeof externalCallHandler.dispatch !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), operation: fc.string({ minLength: 1, maxLength: 50 }), endpoint: fc.string({ minLength: 1, maxLength: 50 }), payload: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await externalCallHandler.dispatch(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('registerProtocol handles empty input: ', async () => {
      if (typeof externalCallHandler.registerProtocol !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await externalCallHandler.registerProtocol({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('registerProtocol ensures on ok: ', async () => {
      if (typeof externalCallHandler.registerProtocol !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ protocol: fc.string({ minLength: 1, maxLength: 50 }), providerName: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await externalCallHandler.registerProtocol(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
