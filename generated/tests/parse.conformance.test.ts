// Parse Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { parseHandler } from '../../handlers/ts/app/parse.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('Parse functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof parseHandler.register !== 'function') return;
      const result = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_ok" -> ok', async () => {
      if (typeof parseHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_katex" -> ok', async () => {
      if (typeof parseHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parseHandler.register({ provider: "katex-parse", language: "latex", config: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_html" -> ok', async () => {
      if (typeof parseHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parseHandler.register({ provider: "domparser-parse", language: "html", config: "" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_provider" -> error', async () => {
      if (typeof parseHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parseHandler.register({ provider: "", language: "markdown", config: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_empty_language" -> error', async () => {
      if (typeof parseHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parseHandler.register({ provider: "micromark-parse", language: "", config: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof parseHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { provider: "micromark-parse-v2", language: "markdown", config: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(parseHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('parse', () => {
    it('builds a valid StorageProgram', () => {
      const program = parseHandler.parse({ language: "markdown", text: "# Hello\n\nParagraph text." });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = parseHandler.parse({ language: "markdown", text: "# Hello\n\nParagraph text." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = parseHandler.parse({ language: "markdown", text: "# Hello\n\nParagraph text." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = parseHandler.parse({ language: "markdown", text: "# Hello\n\nParagraph text." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = parseHandler.parse({ language: "markdown", text: "# Hello\n\nParagraph text." });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof parseHandler.parse !== 'function') return;
      const result = await interpret(parseHandler.parse({ language: "markdown", text: "# Hello\n\nParagraph text." }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "parse_ok" -> ok', async () => {
      if (typeof parseHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { language: "markdown", text: "# Hello\n\nParagraph text." } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(parseHandler.parse({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "parse_empty_text" -> ok', async () => {
      if (typeof parseHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { language: "markdown", text: "" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(parseHandler.parse({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "parse_no_provider" -> no_provider', async () => {
      if (typeof parseHandler.parse !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parseHandler.parse({ language: "rust", text: "fn main() {}" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('no_provider'));
    });

  });

  describe('deregister', () => {
    it('builds a valid StorageProgram', () => {
      const program = parseHandler.deregister({ provider: "micromark-parse", language: "markdown" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = parseHandler.deregister({ provider: "micromark-parse", language: "markdown" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = parseHandler.deregister({ provider: "micromark-parse", language: "markdown" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = parseHandler.deregister({ provider: "micromark-parse", language: "markdown" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = parseHandler.deregister({ provider: "micromark-parse", language: "markdown" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof parseHandler.deregister !== 'function') return;
      const result = await interpret(parseHandler.deregister({ provider: "micromark-parse", language: "markdown" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "deregister_ok" -> ok', async () => {
      if (typeof parseHandler.deregister !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { provider: "micromark-parse", language: "markdown" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(parseHandler.deregister({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "deregister_not_found" -> not_found', async () => {
      if (typeof parseHandler.deregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parseHandler.deregister({ provider: "micromark-parse", language: "rust" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('not_found'));
    });

    it('fixture "deregister_empty_language" -> error', async () => {
      if (typeof parseHandler.deregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(parseHandler.deregister({ provider: "micromark-parse", language: "" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('listLanguages', () => {
    it('builds a valid StorageProgram', () => {
      const program = parseHandler.listLanguages({  });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = parseHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = parseHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = parseHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = parseHandler.listLanguages({  });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof parseHandler.listLanguages !== 'function') return;
      const result = await interpret(parseHandler.listLanguages({  }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "list_empty" -> ok', async () => {
      if (typeof parseHandler.listLanguages !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(parseHandler.listLanguages({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "list_after_register" -> ok', async () => {
      if (typeof parseHandler.listLanguages !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_ok = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      const _pool = Object.assign({}, (afterResult_register_ok?.output ?? {}));
      const _fixtureInput = { ..._pool } as Record<string, unknown>;
      const result = await interpret(parseHandler.listLanguages({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

  });


  describe('invariant examples', () => {
    it("register then parse returns bytes", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let p = id;
      const thenResult0 = await interpret(parseHandler.parse({ language: "markdown", text: "# Title" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("parse unknown language returns no_provider", async () => {
      const storage = createInMemoryStorage();
    });

    it("register then listLanguages includes language", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let _ = id;
      const thenResult0 = await interpret(parseHandler.listLanguages({  }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate registration is rejected", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(parseHandler.register({ provider: "micromark-parse", language: "markdown", config: "" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let id = registerResult0.output["id"];
      let _ = id;
      const thenResult0 = await interpret(parseHandler.register({ provider: "micromark-parse-v2", language: "markdown", config: "" }), storage);
      expect(thenResult0.variant).toBe("duplicate");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every provider has a non-empty language', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('parse'), input: fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deregister'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listLanguages'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = parseHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(parseHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: every provider has a non-empty provider identifier', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('parse'), input: fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deregister'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listLanguages'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = parseHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(parseHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: two providers for the same language', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }) }),
              fc.record({ action: fc.constant('parse'), input: fc.record({ language: fc.string({ minLength: 1, maxLength: 50 }), text: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('deregister'), input: fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listLanguages'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = parseHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(parseHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: two providers for the same language
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof parseHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(parseHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof parseHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ provider: fc.string({ minLength: 1, maxLength: 50 }), language: fc.string({ minLength: 1, maxLength: 50 }), config: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = parseHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
