// ProgramAnalysis Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { programAnalysisHandler } from '../../handlers/ts/monadic/program-analysis.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('ProgramAnalysis imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('registerProvider', () => {
    it('executes without crashing', async () => {
      if (typeof programAnalysisHandler.registerProvider !== 'function') return;
      try {
        const result = await programAnalysisHandler.registerProvider({ name: 'test-name', kind: 'test-kind' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('run', () => {
    it('executes without crashing', async () => {
      if (typeof programAnalysisHandler.run !== 'function') return;
      try {
        const result = await programAnalysisHandler.run({ program: 'test-program', provider: 'test-provider' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('runAll', () => {
    it('executes without crashing', async () => {
      if (typeof programAnalysisHandler.runAll !== 'function') return;
      try {
        const result = await programAnalysisHandler.runAll({ program: 'test-program' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('listProviders', () => {
    it('executes without crashing', async () => {
      if (typeof programAnalysisHandler.listProviders !== 'function') return;
      try {
        const result = await programAnalysisHandler.listProviders({  }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("registered provider can run analysis", async () => {
      const storage = createInMemoryStorage();
      const registerProviderResult0 = await programAnalysisHandler.registerProvider({ name: {"type":"literal","value":"read-write-sets"}, kind: {"type":"literal","value":"structural"} }, storage);
      expect(registerProviderResult0.variant).toBe("ok");
      const thenResult0 = await programAnalysisHandler.run({ program: {"type":"literal","value":"get(users, u1)"}, provider: {"type":"literal","value":"read-write-sets"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("unregistered provider returns providerNotFound", async () => {
      const storage = createInMemoryStorage();
      const runResult0 = await programAnalysisHandler.run({ program: {"type":"literal","value":"get(users, u1)"}, provider: {"type":"literal","value":"nonexistent"} }, storage);
      expect(runResult0.variant).toBe("providerNotFound");
      const thenResult0 = await programAnalysisHandler.listProviders({  }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("runAll dispatches to all registered providers", async () => {
      const storage = createInMemoryStorage();
      const registerProviderResult0 = await programAnalysisHandler.registerProvider({ name: {"type":"literal","value":"read-write-sets"}, kind: {"type":"literal","value":"structural"} }, storage);
      expect(registerProviderResult0.variant).toBe("ok");
      const registerProviderResult1 = await programAnalysisHandler.registerProvider({ name: {"type":"literal","value":"commutativity"}, kind: {"type":"literal","value":"relational"} }, storage);
      expect(registerProviderResult1.variant).toBe("ok");
      const thenResult0 = await programAnalysisHandler.runAll({ program: {"type":"literal","value":"get(users, u1)"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("duplicate provider registration returns exists", async () => {
      const storage = createInMemoryStorage();
      const registerProviderResult0 = await programAnalysisHandler.registerProvider({ name: {"type":"literal","value":"read-write-sets"}, kind: {"type":"literal","value":"structural"} }, storage);
      expect(registerProviderResult0.variant).toBe("ok");
      const thenResult0 = await programAnalysisHandler.registerProvider({ name: {"type":"literal","value":"read-write-sets"}, kind: {"type":"literal","value":"structural"} }, storage);
      expect(thenResult0.variant).toBe("exists");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: registered providers have kind', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerProvider'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('run'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), provider: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('runAll'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listProviders'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = programAnalysisHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(programAnalysisHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty provider kind registered', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('registerProvider'), input: fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('run'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), provider: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('runAll'), input: fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('listProviders'), input: fc.record({  }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = programAnalysisHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(programAnalysisHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: empty provider kind registered
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('registerProvider handles empty input: ', async () => {
      if (typeof programAnalysisHandler.registerProvider !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await programAnalysisHandler.registerProvider({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('registerProvider ensures on ok: ', async () => {
      if (typeof programAnalysisHandler.registerProvider !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ name: fc.string({ minLength: 1, maxLength: 50 }), kind: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await programAnalysisHandler.registerProvider(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('run handles empty input: ', async () => {
      if (typeof programAnalysisHandler.run !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await programAnalysisHandler.run({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('run ensures on ok: ', async () => {
      if (typeof programAnalysisHandler.run !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string({ minLength: 1, maxLength: 50 }), provider: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await programAnalysisHandler.run(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
