// StorageProgram Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { storageProgramHandler } from '../../handlers/ts/monadic/storage-program.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('StorageProgram imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('create', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.create !== 'function') return;
      try {
        const result = await storageProgramHandler.create({ program: 'test' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('get', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.get !== 'function') return;
      try {
        const result = await storageProgramHandler.get({ program: 'test', relation: 'test-relation', key: 'test-key', bindAs: 'test-bindAs' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('find', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.find !== 'function') return;
      try {
        const result = await storageProgramHandler.find({ program: 'test', relation: 'test-relation', criteria: 'test-criteria', bindAs: 'test-bindAs' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('put', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.put !== 'function') return;
      try {
        const result = await storageProgramHandler.put({ program: 'test', relation: 'test-relation', key: 'test-key', value: 'test-value' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('del', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.del !== 'function') return;
      try {
        const result = await storageProgramHandler.del({ program: 'test', relation: 'test-relation', key: 'test-key' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('branch', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.branch !== 'function') return;
      try {
        const result = await storageProgramHandler.branch({ program: 'test', condition: 'test-condition', thenBranch: 'test', elseBranch: 'test' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('pure', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.pure !== 'function') return;
      try {
        const result = await storageProgramHandler.pure({ program: 'test', variant: 'test-variant', output: 'test-output' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('compose', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.compose !== 'function') return;
      try {
        const result = await storageProgramHandler.compose({ first: 'test', second: 'test', bindAs: 'test-bindAs' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('getLens', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.getLens !== 'function') return;
      try {
        const result = await storageProgramHandler.getLens({ program: 'test', lens: 'test-lens', bindAs: 'test-bindAs' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('putLens', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.putLens !== 'function') return;
      try {
        const result = await storageProgramHandler.putLens({ program: 'test', lens: 'test-lens', value: 'test-value' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('modifyLens', () => {
    it('executes without crashing', async () => {
      if (typeof storageProgramHandler.modifyLens !== 'function') return;
      try {
        const result = await storageProgramHandler.modifyLens({ program: 'test', lens: 'test-lens', fn: 'test-fn' }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

  });

  describe('invariant examples', () => {
    it("create, get, put, pure terminates program", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await storageProgramHandler.create({ program: {"type":"variable","name":"p"} }, storage);
      expect(createResult0.variant).toBe("ok");
      const getResult1 = await storageProgramHandler.get({ program: {"type":"variable","name":"p"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, bindAs: {"type":"literal","value":"user"} }, storage);
      expect(getResult1.variant).toBe("ok");
      const program = getResult1.output["program"];
      const putResult2 = await storageProgramHandler.put({ program: {"type":"variable","name":"p"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, value: {"type":"literal","value":"updated"} }, storage);
      expect(putResult2.variant).toBe("ok");
      const program = putResult2.output["program"];
      const pureResult3 = await storageProgramHandler.pure({ program: {"type":"variable","name":"p"}, variant: {"type":"literal","value":"ok"}, output: {"type":"literal","value":"done"} }, storage);
      expect(pureResult3.variant).toBe("ok");
      const program = pureResult3.output["program"];
      expect(pResult.output["terminated"]).toBe({"type":"literal","value":true});
    });

    it("sealed program rejects new instructions", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await storageProgramHandler.create({ program: {"type":"variable","name":"p"} }, storage);
      expect(createResult0.variant).toBe("ok");
      const pureResult1 = await storageProgramHandler.pure({ program: {"type":"variable","name":"p"}, variant: {"type":"literal","value":"ok"}, output: {"type":"literal","value":"done"} }, storage);
      expect(pureResult1.variant).toBe("ok");
      const program = pureResult1.output["program"];
      const thenResult0 = await storageProgramHandler.get({ program: {"type":"variable","name":"p"}, relation: {"type":"literal","value":"users"}, key: {"type":"literal","value":"u1"}, bindAs: {"type":"literal","value":"x"} }, storage);
      expect(thenResult0.variant).toBe("sealed");
    });

    it("compose chains two programs", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await storageProgramHandler.create({ program: {"type":"variable","name":"a"} }, storage);
      expect(createResult0.variant).toBe("ok");
      const createResult1 = await storageProgramHandler.create({ program: {"type":"variable","name":"b"} }, storage);
      expect(createResult1.variant).toBe("ok");
      const thenResult0 = await storageProgramHandler.compose({ first: {"type":"variable","name":"a"}, second: {"type":"variable","name":"b"}, bindAs: {"type":"literal","value":"result"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("getLens appends lens read instruction", async () => {
      const storage = createInMemoryStorage();
      const createResult0 = await storageProgramHandler.create({ program: {"type":"variable","name":"p"} }, storage);
      expect(createResult0.variant).toBe("ok");
      const thenResult0 = await storageProgramHandler.getLens({ program: {"type":"variable","name":"p"}, lens: {"type":"literal","value":"users.u1.email"}, bindAs: {"type":"literal","value":"email"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: terminated programs have instructions', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ program: fc.string(), relation: fc.string({ minLength: 1, maxLength: 20 }), key: fc.string({ minLength: 1, maxLength: 20 }), bindAs: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('find'), input: fc.record({ program: fc.string(), relation: fc.string({ minLength: 1, maxLength: 20 }), criteria: fc.string({ minLength: 1, maxLength: 20 }), bindAs: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('put'), input: fc.record({ program: fc.string(), relation: fc.string({ minLength: 1, maxLength: 20 }), key: fc.string({ minLength: 1, maxLength: 20 }), value: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('del'), input: fc.record({ program: fc.string(), relation: fc.string({ minLength: 1, maxLength: 20 }), key: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('branch'), input: fc.record({ program: fc.string(), condition: fc.string({ minLength: 1, maxLength: 20 }), thenBranch: fc.string(), elseBranch: fc.string() }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 20 }), output: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('getLens'), input: fc.record({ program: fc.string(), lens: fc.string({ minLength: 1, maxLength: 20 }), bindAs: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('putLens'), input: fc.record({ program: fc.string(), lens: fc.string({ minLength: 1, maxLength: 20 }), value: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('modifyLens'), input: fc.record({ program: fc.string(), lens: fc.string({ minLength: 1, maxLength: 20 }), fn: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = storageProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(storageProgramHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: instruction appended after termination', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('create'), input: fc.record({ program: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ program: fc.string(), relation: fc.string({ minLength: 1, maxLength: 20 }), key: fc.string({ minLength: 1, maxLength: 20 }), bindAs: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('find'), input: fc.record({ program: fc.string(), relation: fc.string({ minLength: 1, maxLength: 20 }), criteria: fc.string({ minLength: 1, maxLength: 20 }), bindAs: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('put'), input: fc.record({ program: fc.string(), relation: fc.string({ minLength: 1, maxLength: 20 }), key: fc.string({ minLength: 1, maxLength: 20 }), value: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('del'), input: fc.record({ program: fc.string(), relation: fc.string({ minLength: 1, maxLength: 20 }), key: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('branch'), input: fc.record({ program: fc.string(), condition: fc.string({ minLength: 1, maxLength: 20 }), thenBranch: fc.string(), elseBranch: fc.string() }) }),
              fc.record({ action: fc.constant('pure'), input: fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 20 }), output: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('compose'), input: fc.record({ first: fc.string(), second: fc.string(), bindAs: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('getLens'), input: fc.record({ program: fc.string(), lens: fc.string({ minLength: 1, maxLength: 20 }), bindAs: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('putLens'), input: fc.record({ program: fc.string(), lens: fc.string({ minLength: 1, maxLength: 20 }), value: fc.string({ minLength: 1, maxLength: 20 }) }) }),
              fc.record({ action: fc.constant('modifyLens'), input: fc.record({ program: fc.string(), lens: fc.string({ minLength: 1, maxLength: 20 }), fn: fc.string({ minLength: 1, maxLength: 20 }) }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = storageProgramHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(storageProgramHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: instruction appended after termination
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('create handles empty input: ', async () => {
      if (typeof storageProgramHandler.create !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await storageProgramHandler.create({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('create ensures on ok: ', async () => {
      if (typeof storageProgramHandler.create !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await storageProgramHandler.create(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('pure handles empty input: ', async () => {
      if (typeof storageProgramHandler.pure !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await storageProgramHandler.pure({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('pure ensures on ok: ', async () => {
      if (typeof storageProgramHandler.pure !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ program: fc.string(), variant: fc.string({ minLength: 1, maxLength: 50 }), output: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await storageProgramHandler.pure(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
