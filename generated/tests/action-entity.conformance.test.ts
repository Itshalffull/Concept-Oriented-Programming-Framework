// ActionEntity Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { actionEntityHandler } from '../../handlers/ts/score/action-entity.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('ActionEntity functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const result = await interpret(actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_create" -> ok', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_minimal" -> ok', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionEntityHandler.register({ concept: "User", name: "delete", params: "[]", variantRefs: "[]" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_concept" -> error', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionEntityHandler.register({ concept: "", name: "create", params: "[]", variantRefs: "[]" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('findByConcept', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionEntityHandler.findByConcept({ concept: "Article" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionEntityHandler.findByConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionEntityHandler.findByConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionEntityHandler.findByConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionEntityHandler.findByConcept({ concept: "Article" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionEntityHandler.findByConcept !== 'function') return;
      const result = await interpret(actionEntityHandler.findByConcept({ concept: "Article" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "find_article" -> ok', async () => {
      if (typeof actionEntityHandler.findByConcept !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }), storage));
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "User", name: "delete", params: "[]", variantRefs: "[]" }), storage));
      const result = await interpret(actionEntityHandler.findByConcept({ concept: "Article" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_empty" -> ok', async () => {
      if (typeof actionEntityHandler.findByConcept !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }), storage));
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "User", name: "delete", params: "[]", variantRefs: "[]" }), storage));
      const result = await interpret(actionEntityHandler.findByConcept({ concept: "" }), storage);
      expect(result.variant).toBe('ok');
    });

  });

  describe('triggeringSyncs', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionEntityHandler.triggeringSyncs({ action: "action-entity-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionEntityHandler.triggeringSyncs({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionEntityHandler.triggeringSyncs({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionEntityHandler.triggeringSyncs({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionEntityHandler.triggeringSyncs({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionEntityHandler.triggeringSyncs !== 'function') return;
      const result = await interpret(actionEntityHandler.triggeringSyncs({ action: "action-entity-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "triggering_valid" -> ok', async () => {
      if (typeof actionEntityHandler.triggeringSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }), storage));
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "User", name: "delete", params: "[]", variantRefs: "[]" }), storage));
      const result = await interpret(actionEntityHandler.triggeringSyncs({ action: "action-entity-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "triggering_missing" -> error', async () => {
      if (typeof actionEntityHandler.triggeringSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionEntityHandler.triggeringSyncs({ action: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('invokingSyncs', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionEntityHandler.invokingSyncs({ action: "action-entity-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionEntityHandler.invokingSyncs({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionEntityHandler.invokingSyncs({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionEntityHandler.invokingSyncs({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionEntityHandler.invokingSyncs({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionEntityHandler.invokingSyncs !== 'function') return;
      const result = await interpret(actionEntityHandler.invokingSyncs({ action: "action-entity-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "invoking_valid" -> ok', async () => {
      if (typeof actionEntityHandler.invokingSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }), storage));
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "User", name: "delete", params: "[]", variantRefs: "[]" }), storage));
      const result = await interpret(actionEntityHandler.invokingSyncs({ action: "action-entity-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invoking_missing" -> error', async () => {
      if (typeof actionEntityHandler.invokingSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionEntityHandler.invokingSyncs({ action: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('implementations', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionEntityHandler.implementations({ action: "action-entity-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionEntityHandler.implementations({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionEntityHandler.implementations({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionEntityHandler.implementations({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionEntityHandler.implementations({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionEntityHandler.implementations !== 'function') return;
      const result = await interpret(actionEntityHandler.implementations({ action: "action-entity-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "impl_valid" -> ok', async () => {
      if (typeof actionEntityHandler.implementations !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }), storage));
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "User", name: "delete", params: "[]", variantRefs: "[]" }), storage));
      const result = await interpret(actionEntityHandler.implementations({ action: "action-entity-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "impl_missing" -> error', async () => {
      if (typeof actionEntityHandler.implementations !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionEntityHandler.implementations({ action: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('interfaceExposures', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionEntityHandler.interfaceExposures({ action: "action-entity-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionEntityHandler.interfaceExposures({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionEntityHandler.interfaceExposures({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionEntityHandler.interfaceExposures({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionEntityHandler.interfaceExposures({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionEntityHandler.interfaceExposures !== 'function') return;
      const result = await interpret(actionEntityHandler.interfaceExposures({ action: "action-entity-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "exposures_valid" -> ok', async () => {
      if (typeof actionEntityHandler.interfaceExposures !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }), storage));
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "User", name: "delete", params: "[]", variantRefs: "[]" }), storage));
      const result = await interpret(actionEntityHandler.interfaceExposures({ action: "action-entity-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "exposures_missing" -> error', async () => {
      if (typeof actionEntityHandler.interfaceExposures !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionEntityHandler.interfaceExposures({ action: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('get', () => {
    it('builds a valid StorageProgram', () => {
      const program = actionEntityHandler.get({ action: "action-entity-1" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = actionEntityHandler.get({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = actionEntityHandler.get({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = actionEntityHandler.get({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = actionEntityHandler.get({ action: "action-entity-1" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof actionEntityHandler.get !== 'function') return;
      const result = await interpret(actionEntityHandler.get({ action: "action-entity-1" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "get_valid" -> ok', async () => {
      if (typeof actionEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }), storage));
      await safeInvoke(async () => await interpret(actionEntityHandler.register({ concept: "User", name: "delete", params: "[]", variantRefs: "[]" }), storage));
      const result = await interpret(actionEntityHandler.get({ action: "action-entity-1" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> error', async () => {
      if (typeof actionEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(actionEntityHandler.get({ action: "nonexistent-id" }), storage);
      expect(result.variant).not.toBe('ok');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const program = actionEntityHandler.register({});
      // If it's a StorageProgram, interpret it
      const result = (program?.instructions && !program.variant)
        ? await interpret(program, storage)
        : program;
      if (!result?.variant) return; // handler does not support register introspection
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('ActionEntity');
    });
  });

  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(actionEntityHandler.register({ concept: {"type":"literal","value":"Article"}, name: {"type":"literal","value":"create"}, params: {"type":"literal","value":"[]"}, variantRefs: {"type":"literal","value":"[]"} }), storage);
      expect(registerResult0.variant).toBe("ok");
      const action = registerResult0.output["action"];
      const thenResult0 = await interpret(actionEntityHandler.get({ action: {"type":"variable","name":"a"} }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every actionsSet entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string({ minLength: 1, maxLength: 50 }), variantRefs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('triggeringSyncs'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('invokingSyncs'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('implementations'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('interfaceExposures'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ action: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(actionEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in actionsSet', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string({ minLength: 1, maxLength: 50 }), variantRefs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('triggeringSyncs'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('invokingSyncs'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('implementations'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('interfaceExposures'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ action: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(actionEntityHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: empty name in actionsSet
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(actionEntityHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string({ minLength: 1, maxLength: 50 }), variantRefs: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = actionEntityHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
