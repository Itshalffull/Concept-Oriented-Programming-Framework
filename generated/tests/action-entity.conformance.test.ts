// ActionEntity Imperative Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates action execution and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { actionEntityHandler } from '../../handlers/ts/score/action-entity.handler.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

describe('ActionEntity imperative handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('executes without crashing', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      try {
        const result = await actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "register_create" -> ok', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.register({ concept: "Article", name: "create", params: "[{\"name\":\"title\",\"type\":\"String\"}]", variantRefs: "[\"ok\",\"error\"]" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_minimal" -> ok', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.register({ concept: "User", name: "delete", params: "[]", variantRefs: "[]" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_empty_concept" -> error', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.register({ concept: "", name: "create", params: "[]", variantRefs: "[]" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('findByConcept', () => {
    it('executes without crashing', async () => {
      if (typeof actionEntityHandler.findByConcept !== 'function') return;
      try {
        const result = await actionEntityHandler.findByConcept({ concept: "Article" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "find_article" -> ok', async () => {
      if (typeof actionEntityHandler.findByConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.findByConcept({ concept: "Article" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "find_empty" -> error', async () => {
      if (typeof actionEntityHandler.findByConcept !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.findByConcept({ concept: "" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('triggeringSyncs', () => {
    it('executes without crashing', async () => {
      if (typeof actionEntityHandler.triggeringSyncs !== 'function') return;
      try {
        const result = await actionEntityHandler.triggeringSyncs({ action: "action-entity-1" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "triggering_valid" -> ok', async () => {
      if (typeof actionEntityHandler.triggeringSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.triggeringSyncs({ action: "action-entity-1" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "triggering_missing" -> error', async () => {
      if (typeof actionEntityHandler.triggeringSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.triggeringSyncs({ action: "nonexistent-id" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('invokingSyncs', () => {
    it('executes without crashing', async () => {
      if (typeof actionEntityHandler.invokingSyncs !== 'function') return;
      try {
        const result = await actionEntityHandler.invokingSyncs({ action: "action-entity-1" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "invoking_valid" -> ok', async () => {
      if (typeof actionEntityHandler.invokingSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.invokingSyncs({ action: "action-entity-1" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "invoking_missing" -> error', async () => {
      if (typeof actionEntityHandler.invokingSyncs !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.invokingSyncs({ action: "nonexistent-id" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('implementations', () => {
    it('executes without crashing', async () => {
      if (typeof actionEntityHandler.implementations !== 'function') return;
      try {
        const result = await actionEntityHandler.implementations({ action: "action-entity-1" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "impl_valid" -> ok', async () => {
      if (typeof actionEntityHandler.implementations !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.implementations({ action: "action-entity-1" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "impl_missing" -> error', async () => {
      if (typeof actionEntityHandler.implementations !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.implementations({ action: "nonexistent-id" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('interfaceExposures', () => {
    it('executes without crashing', async () => {
      if (typeof actionEntityHandler.interfaceExposures !== 'function') return;
      try {
        const result = await actionEntityHandler.interfaceExposures({ action: "action-entity-1" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "exposures_valid" -> ok', async () => {
      if (typeof actionEntityHandler.interfaceExposures !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.interfaceExposures({ action: "action-entity-1" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "exposures_missing" -> error', async () => {
      if (typeof actionEntityHandler.interfaceExposures !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.interfaceExposures({ action: "nonexistent-id" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('get', () => {
    it('executes without crashing', async () => {
      if (typeof actionEntityHandler.get !== 'function') return;
      try {
        const result = await actionEntityHandler.get({ action: "action-entity-1" }, storage);
        expect(result).toBeDefined();
        expect(result.variant).toBeDefined();
        expect(typeof result.variant).toBe('string');
      } catch (e) {
        // Handler may throw on invalid default inputs (e.g. JSON parse) — that's acceptable
        expect(e).toBeDefined();
      }
    });

    it('fixture "get_valid" -> ok', async () => {
      if (typeof actionEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.get({ action: "action-entity-1" }, storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "get_missing" -> error', async () => {
      if (typeof actionEntityHandler.get !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.get({ action: "nonexistent-id" }, storage);
      expect(result.variant).toBe('error');
    });

  });

  describe('register()', () => {
    it('declares concept name', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      let result: any;
      try {
        const r = actionEntityHandler.register({}, storage);
        result = r instanceof Promise ? await r : r;
        // If StorageProgram, interpret it
        if (result?.instructions && !result.variant) {
        }
      } catch { return; }
      expect(result.variant).toBe('ok');
      expect(result.name).toBe('ActionEntity');
    });
  });

  describe('invariant examples', () => {
    it("registered entity is retrievable", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await actionEntityHandler.register({ concept: {"type":"literal","value":"Article"}, name: {"type":"literal","value":"create"}, params: {"type":"literal","value":"[]"}, variantRefs: {"type":"literal","value":"[]"} }, storage);
      expect(registerResult0.variant).toBe("ok");
      const action = registerResult0.output["action"];
      const thenResult0 = await actionEntityHandler.get({ action: {"type":"variable","name":"a"} }, storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: every actionsSet entry has name', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string({ minLength: 1, maxLength: 50 }), variantRefs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('triggeringSyncs'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('invokingSyncs'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('implementations'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('interfaceExposures'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ action: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(actionEntityHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: empty name in actionsSet', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string({ minLength: 1, maxLength: 50 }), variantRefs: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('findByConcept'), input: fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('triggeringSyncs'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('invokingSyncs'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('implementations'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('interfaceExposures'), input: fc.record({ action: fc.string() }) }),
              fc.record({ action: fc.constant('get'), input: fc.record({ action: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = actionEntityHandler[step.action];
              if (typeof actionFn === 'function') {
                try {
                  const result = await actionFn.call(actionEntityHandler, step.input as Record<string, unknown>, storage);
                  expect(result.variant).toBeDefined();
                  // Never: empty name in actionsSet
                } catch { /* handler may throw on random inputs */ }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await actionEntityHandler.register({  }, storage);
      expect(result).toBeDefined();
      expect(result.variant).toBeDefined();
    });

    it('register ensures on ok: ', async () => {
      if (typeof actionEntityHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ concept: fc.string({ minLength: 1, maxLength: 50 }), name: fc.string({ minLength: 1, maxLength: 50 }), params: fc.string({ minLength: 1, maxLength: 50 }), variantRefs: fc.string({ minLength: 1, maxLength: 50 }) }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await actionEntityHandler.register(input as Record<string, unknown>, storage);
            if (result.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

});
