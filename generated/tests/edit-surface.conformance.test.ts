// EditSurface Functional Handler Conformance Tests
//
// Auto-generated by TestGen from concept spec invariants.
// Validates StorageProgram construction, purity, variant coverage,
// read/write sets, interpreted execution, and invariant conformance.

import { describe, it, expect, beforeEach } from 'vitest';
import fc from 'fast-check';
import { editSurfaceHandler } from '../../handlers/ts/app/edit-surface.handler.js';
import {
  classifyPurity,
  extractCompletionVariants,
  extractReadSet,
  extractWriteSet,
  extractPerformSet,
} from '../../runtime/storage-program.js';
import { interpret } from '../../runtime/interpreter.js';
import { createInMemoryStorage } from '../../runtime/adapters/storage.js';

const safeInvoke = async (fn: () => any): Promise<any> => {
  let r: any;
  r = (() => { try { return { ok: true, value: fn() }; } catch (e: any) { return { ok: false, message: e?.message }; } })();
  if (!r.ok) return { variant: '_thrown', message: r.message };
  if (r.value?.then) return r.value.catch((e: any) => ({ variant: '_thrown', message: e?.message }));
  return r.value;
};

describe('EditSurface functional handler', () => {
  let storage: ReturnType<typeof createInMemoryStorage>;

  beforeEach(() => {
    storage = createInMemoryStorage();
  });

  describe('register', () => {
    it('builds a valid StorageProgram', () => {
      const program = editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const result = await interpret(editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "register_block_editor" -> ok', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_standalone" -> ok', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.register({ surface: "article-standalone", schema_ref: "article", context: "standalone", command_bindings: ["cmd-save","cmd-publish"], panel_widgets: ["panel-outline","panel-versions"], toolbar_widget: null, context_menu_bindings: [], input_rule_refs: [], picker_refs: [], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_inspector" -> ok', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.register({ surface: "schema-inspector", schema_ref: "meeting-notes", context: "inspector", command_bindings: [], panel_widgets: ["panel-properties"], toolbar_widget: null, context_menu_bindings: [], input_rule_refs: [], picker_refs: [], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_canvas" -> ok', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.register({ surface: "diagram-canvas", schema_ref: "diagram", context: "canvas", command_bindings: ["cmd-add-node","cmd-connect"], panel_widgets: ["panel-shape-library"], toolbar_widget: "canvas-toolbar", context_menu_bindings: ["ctx-group","ctx-ungroup"], input_rule_refs: [], picker_refs: ["picker-node-type"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_preview_only" -> ok', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.register({ surface: "content-preview", schema_ref: "article", context: "preview-only", command_bindings: [], panel_widgets: [], toolbar_widget: null, context_menu_bindings: [], input_rule_refs: [], picker_refs: [], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_page_level_compilable" -> ok', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.register({ surface: "agent-persona-page", schema_ref: "agent-persona", context: "page-level", command_bindings: ["cmd-run-agent","cmd-test-persona"], panel_widgets: ["panel-memory","panel-tools"], toolbar_widget: "persona-toolbar", context_menu_bindings: [], input_rule_refs: [], picker_refs: ["picker-tool-binding"], compile_action_ref: "ContentCompiler/compile", status_decoration_ref: "compilation-status-badge", output_preview_ref: "prompt-preview-panel", consumers_panel_ref: "consumers-rail" }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "register_invalid_context" -> invalid_context', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.register({ surface: "bad-surface", schema_ref: "article", context: "floating", command_bindings: [], panel_widgets: [], toolbar_widget: null, context_menu_bindings: [], input_rule_refs: [], picker_refs: [], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('invalid_context'));
    });

    it('fixture "register_empty_schema" -> error', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.register({ surface: "empty-schema-surface", schema_ref: "", context: "block-editor", command_bindings: [], panel_widgets: [], toolbar_widget: null, context_menu_bindings: [], input_rule_refs: [], picker_refs: [], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      expect(result.variant).not.toBe('ok');
    });

    it('fixture "register_duplicate" -> duplicate', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_block_editor = await interpret(editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      const _pool = Object.assign({}, (afterResult_register_block_editor?.output ?? {}));
      const _fixtureInput = { surface: "para-block-editor-2", schema_ref: "paragraph", context: "block-editor", command_bindings: [], panel_widgets: [], toolbar_widget: null, context_menu_bindings: [], input_rule_refs: [], picker_refs: [], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(editSurfaceHandler.register({ ..._fixtureInput }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('duplicate'));
    });

  });

  describe('unregister', () => {
    it('builds a valid StorageProgram', () => {
      const program = editSurfaceHandler.unregister({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = editSurfaceHandler.unregister({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = editSurfaceHandler.unregister({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = editSurfaceHandler.unregister({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = editSurfaceHandler.unregister({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof editSurfaceHandler.unregister !== 'function') return;
      const result = await interpret(editSurfaceHandler.unregister({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "unregister_ok" -> ok', async () => {
      if (typeof editSurfaceHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_block_editor = await interpret(editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      const result = await interpret(editSurfaceHandler.unregister({ surface: afterResult_register_block_editor?.output?.["surface"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "unregister_missing" -> notfound', async () => {
      if (typeof editSurfaceHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.unregister({ surface: "nonexistent-surface" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('resolve', () => {
    it('builds a valid StorageProgram', () => {
      const program = editSurfaceHandler.resolve({ schema_ref: "paragraph", context: "block-editor" });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = editSurfaceHandler.resolve({ schema_ref: "paragraph", context: "block-editor" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = editSurfaceHandler.resolve({ schema_ref: "paragraph", context: "block-editor" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = editSurfaceHandler.resolve({ schema_ref: "paragraph", context: "block-editor" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = editSurfaceHandler.resolve({ schema_ref: "paragraph", context: "block-editor" });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof editSurfaceHandler.resolve !== 'function') return;
      const result = await interpret(editSurfaceHandler.resolve({ schema_ref: "paragraph", context: "block-editor" }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "resolve_ok" -> ok', async () => {
      if (typeof editSurfaceHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_block_editor = await interpret(editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      const _pool = Object.assign({}, (afterResult_register_block_editor?.output ?? {}));
      const _fixtureInput = { schema_ref: "paragraph", context: "block-editor" } as Record<string, unknown>;
      for (const [k, v] of Object.entries(_pool)) {
        if (k in _fixtureInput && v !== undefined) {
          const cur = _fixtureInput[k];
          const isPlaceholder = cur === null || cur === undefined || (typeof cur === 'string' && cur.startsWith('test-'));
          if (isPlaceholder) _fixtureInput[k] = v;
        }
      }
      const result = await interpret(editSurfaceHandler.resolve({ ..._fixtureInput }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "resolve_no_match" -> no_match', async () => {
      if (typeof editSurfaceHandler.resolve !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.resolve({ schema_ref: "unknown-schema-xyz", context: "block-editor" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('no_match'));
    });

  });

  describe('activate', () => {
    it('builds a valid StorageProgram', () => {
      const program = editSurfaceHandler.activate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = editSurfaceHandler.activate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = editSurfaceHandler.activate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = editSurfaceHandler.activate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = editSurfaceHandler.activate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof editSurfaceHandler.activate !== 'function') return;
      const result = await interpret(editSurfaceHandler.activate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "activate_ok" -> ok', async () => {
      if (typeof editSurfaceHandler.activate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_block_editor = await interpret(editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      const result = await interpret(editSurfaceHandler.activate({ surface: afterResult_register_block_editor?.output?.["surface"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "activate_compilable" -> ok', async () => {
      if (typeof editSurfaceHandler.activate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_page_level_compilable = await interpret(editSurfaceHandler.register({ surface: "agent-persona-page", schema_ref: "agent-persona", context: "page-level", command_bindings: ["cmd-run-agent","cmd-test-persona"], panel_widgets: ["panel-memory","panel-tools"], toolbar_widget: "persona-toolbar", context_menu_bindings: [], input_rule_refs: [], picker_refs: ["picker-tool-binding"], compile_action_ref: "ContentCompiler/compile", status_decoration_ref: "compilation-status-badge", output_preview_ref: "prompt-preview-panel", consumers_panel_ref: "consumers-rail" }), storage);
      const result = await interpret(editSurfaceHandler.activate({ surface: afterResult_register_page_level_compilable?.output?.["surface"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "activate_missing" -> notfound', async () => {
      if (typeof editSurfaceHandler.activate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.activate({ surface: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });

  describe('deactivate', () => {
    it('builds a valid StorageProgram', () => {
      const program = editSurfaceHandler.deactivate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      expect(program).toBeDefined();
      expect(program.instructions).toBeDefined();
      expect(Array.isArray(program.instructions)).toBe(true);
      expect(program.instructions.length).toBeGreaterThan(0);
    });

    it('has classifiable purity', () => {
      const program = editSurfaceHandler.deactivate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const purity = classifyPurity(program);
      expect(['pure', 'read-only', 'read-write']).toContain(purity);
    });

    it('declares completion variants', () => {
      const program = editSurfaceHandler.deactivate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const variants = program.effects?.completionVariants ?? extractCompletionVariants(program);
      expect(variants.size).toBeGreaterThan(0);
    });

    it('declares read and write sets', () => {
      const program = editSurfaceHandler.deactivate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const reads = extractReadSet(program);
      const writes = extractWriteSet(program);
      const purity = classifyPurity(program);
      if (purity === 'read-only') {
        expect(reads.size).toBeGreaterThan(0);
      } else if (purity === 'read-write') {
        expect(writes.size).toBeGreaterThan(0);
      }
    });

    it('has trackable transport effects', () => {
      const program = editSurfaceHandler.deactivate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} });
      if (!program?.instructions) return; // skip non-StorageProgram handlers
      const effects = extractPerformSet(program);
      expect(effects).toBeDefined();
    });

    it('produces a result', async () => {
      if (typeof editSurfaceHandler.deactivate !== 'function') return;
      const result = await interpret(editSurfaceHandler.deactivate({ surface: {"type":"ref","fixture":"register_block_editor","field":"surface"} }), storage);
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('fixture "deactivate_ok" -> ok', async () => {
      if (typeof editSurfaceHandler.deactivate !== 'function') return;
      const storage = createInMemoryStorage();
      const afterResult_register_block_editor = await interpret(editSurfaceHandler.register({ surface: "para-block-editor", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold","cmd-italic","cmd-link"], panel_widgets: [], toolbar_widget: "inline-toolbar", context_menu_bindings: ["ctx-cut","ctx-copy","ctx-paste"], input_rule_refs: ["rule-markdown-bold","rule-markdown-italic"], picker_refs: ["picker-emoji"], compile_action_ref: null, status_decoration_ref: null, output_preview_ref: null, consumers_panel_ref: null }), storage);
      const result = await interpret(editSurfaceHandler.deactivate({ surface: afterResult_register_block_editor?.output?.["surface"] }), storage);
      expect(result.variant).toBe('ok');
    });

    it('fixture "deactivate_missing" -> notfound', async () => {
      if (typeof editSurfaceHandler.deactivate !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await interpret(editSurfaceHandler.deactivate({ surface: "nonexistent" }), storage);
      const normalize = (v: string) => v?.toLowerCase().replace(/_/g, '');
      expect(normalize(result.variant)).toBe(normalize('notfound'));
    });

  });


  describe('invariant examples', () => {
    it("register then resolve returns the same surface", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(editSurfaceHandler.register({ surface: "test-s", schema_ref: "paragraph", context: "block-editor", command_bindings: ["cmd-bold"], panel_widgets: [], toolbar_widget: false, context_menu_bindings: [], input_rule_refs: [], picker_refs: [], compile_action_ref: false, status_decoration_ref: false, output_preview_ref: false, consumers_panel_ref: false }), storage);
      expect(registerResult0.variant).toBe("ok");
      let surface = registerResult0.output["surface"];
      let s = surface;
      const thenResult0 = await interpret(editSurfaceHandler.resolve({ schema_ref: "paragraph", context: "block-editor" }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("register then activate returns a bundle", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(editSurfaceHandler.register({ surface: "test-s", schema_ref: "agent-persona", context: "page-level", command_bindings: ["cmd-run"], panel_widgets: ["panel-tools"], toolbar_widget: "persona-toolbar", context_menu_bindings: [], input_rule_refs: [], picker_refs: [], compile_action_ref: "ContentCompiler/compile", status_decoration_ref: "status-badge", output_preview_ref: "preview-panel", consumers_panel_ref: "consumers-rail" }), storage);
      expect(registerResult0.variant).toBe("ok");
      let surface = registerResult0.output["surface"];
      let s = surface;
      const thenResult0 = await interpret(editSurfaceHandler.activate({ surface: s }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

    it("compile bundle fields all null for non-compilable surface", async () => {
      const storage = createInMemoryStorage();
      const registerResult0 = await interpret(editSurfaceHandler.register({ surface: "test-s", schema_ref: "paragraph", context: "block-editor", command_bindings: [], panel_widgets: [], toolbar_widget: false, context_menu_bindings: [], input_rule_refs: [], picker_refs: [], compile_action_ref: false, status_decoration_ref: false, output_preview_ref: false, consumers_panel_ref: false }), storage);
      expect(registerResult0.variant).toBe("ok");
      let surface = registerResult0.output["surface"];
      let s = surface;
      const thenResult0 = await interpret(editSurfaceHandler.activate({ surface: s }), storage);
      expect(thenResult0.variant).toBe("ok");
    });

  });

  describe('state invariants (stateful PBT)', () => {
    it('always: registered surfaces have non-empty schema_ref', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ surface: fc.string(), schema_ref: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }), command_bindings: fc.string(), panel_widgets: fc.string(), toolbar_widget: fc.string(), context_menu_bindings: fc.string(), input_rule_refs: fc.string(), picker_refs: fc.string(), compile_action_ref: fc.string(), status_decoration_ref: fc.string(), output_preview_ref: fc.string(), consumers_panel_ref: fc.string() }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ surface: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ schema_ref: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('activate'), input: fc.record({ surface: fc.string() }) }),
              fc.record({ action: fc.constant('deactivate'), input: fc.record({ surface: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = editSurfaceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(editSurfaceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('always: context is one of six enum values', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ surface: fc.string(), schema_ref: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }), command_bindings: fc.string(), panel_widgets: fc.string(), toolbar_widget: fc.string(), context_menu_bindings: fc.string(), input_rule_refs: fc.string(), picker_refs: fc.string(), compile_action_ref: fc.string(), status_decoration_ref: fc.string(), output_preview_ref: fc.string(), consumers_panel_ref: fc.string() }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ surface: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ schema_ref: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('activate'), input: fc.record({ surface: fc.string() }) }),
              fc.record({ action: fc.constant('deactivate'), input: fc.record({ surface: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = editSurfaceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(editSurfaceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('never: two surfaces with same schema_ref and context', async () => {
      await fc.assert(
        fc.asyncProperty(
          fc.array(
            fc.oneof(
              fc.record({ action: fc.constant('register'), input: fc.record({ surface: fc.string(), schema_ref: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }), command_bindings: fc.string(), panel_widgets: fc.string(), toolbar_widget: fc.string(), context_menu_bindings: fc.string(), input_rule_refs: fc.string(), picker_refs: fc.string(), compile_action_ref: fc.string(), status_decoration_ref: fc.string(), output_preview_ref: fc.string(), consumers_panel_ref: fc.string() }) }),
              fc.record({ action: fc.constant('unregister'), input: fc.record({ surface: fc.string() }) }),
              fc.record({ action: fc.constant('resolve'), input: fc.record({ schema_ref: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }) }) }),
              fc.record({ action: fc.constant('activate'), input: fc.record({ surface: fc.string() }) }),
              fc.record({ action: fc.constant('deactivate'), input: fc.record({ surface: fc.string() }) }),
            ),
            { minLength: 1, maxLength: 5 },
          ),
          async (actionSequence) => {
            const storage = createInMemoryStorage();
            for (const step of actionSequence) {
              const actionFn = editSurfaceHandler[step.action];
              if (typeof actionFn === 'function') {
                const result = await safeInvoke(async () => {
                  const program = actionFn.call(editSurfaceHandler, step.input as Record<string, unknown>);
                  return interpret(program, storage);
                });
                // Every action should return a result with a variant
                if (result?.variant !== undefined) {
                  expect(typeof result.variant).toBe('string');
                }
                // Never: two surfaces with same schema_ref and context
              }
            }
          },
        ),
        { numRuns: 50 },
      );
    });

  });

  describe('action contracts (PBT)', () => {
    it('register handles empty input: ', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(editSurfaceHandler.register({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

    it('register ensures on ok: ', async () => {
      if (typeof editSurfaceHandler.register !== 'function') return;
      let seen = false;
      await fc.assert(
        fc.asyncProperty(
          fc.record({ surface: fc.string(), schema_ref: fc.string({ minLength: 1, maxLength: 50 }), context: fc.string({ minLength: 1, maxLength: 50 }), command_bindings: fc.string(), panel_widgets: fc.string(), toolbar_widget: fc.string(), context_menu_bindings: fc.string(), input_rule_refs: fc.string(), picker_refs: fc.string(), compile_action_ref: fc.string(), status_decoration_ref: fc.string(), output_preview_ref: fc.string(), consumers_panel_ref: fc.string() }),
          async (input) => {
            const storage = createInMemoryStorage();
            const result = await safeInvoke(async () => {
              const program = editSurfaceHandler.register(input as Record<string, unknown>);
              return interpret(program, storage);
            });
            if (result?.variant === "ok") {
              seen = true;
              expect(result.output).toBeDefined();
            }
          },
        ),
        { numRuns: 50 },
      );
    });

    it('unregister handles empty input: ', async () => {
      if (typeof editSurfaceHandler.unregister !== 'function') return;
      const storage = createInMemoryStorage();
      const result = await safeInvoke(async () => await interpret(editSurfaceHandler.unregister({  }), storage));
      // Empty input should produce a defined result with a variant
      expect(result).toBeDefined();
      if (result.variant !== undefined) {
        expect(typeof result.variant).toBe('string');
      }
    });

  });

});
