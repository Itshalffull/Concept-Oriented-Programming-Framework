// Auto-generated by COPF Interface Kit — rest target
// Concept: Comment
// Do not edit manually; regenerate with: copf interface generate

import { Hono } from 'hono';
import type { Context } from 'hono';

const app = new Hono();

// POST /articles/{target}/comments — create
app.post('/articles/:target/comments', async (c: Context) => {
  const body = await c.req.json();
  const result = await c.var.kernel.handleRequest({
    method: 'create',
    ...body,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 201);
});

// DELETE /articles/{target}/comments/{comment} — delete
app.delete('/articles/:target/comments/:comment', async (c: Context) => {
  const query = c.req.query();
  const result = await c.var.kernel.handleRequest({
    method: 'delete',
    ...query,
  });
  if (result.variant === 'notFound') return c.json({ errors: { body: ['not found'] } }, 404);
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

// GET /articles/{target}/comments — list
app.get('/articles/:target/comments', async (c: Context) => {
  const query = c.req.query();
  const result = await c.var.kernel.handleRequest({
    method: 'list',
    ...query,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

export { app as commentRouter };
