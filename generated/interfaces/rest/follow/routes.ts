// Auto-generated by COPF Interface Kit — rest target
// Concept: Follow
// Do not edit manually; regenerate with: copf interface generate

import { Hono } from 'hono';
import type { Context } from 'hono';

const app = new Hono();

// POST /profiles/{target}/follow — follow
app.post('/profiles/:target/follow', async (c: Context) => {
  const body = await c.req.json();
  const result = await c.var.kernel.handleRequest({
    method: 'follow',
    ...body,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

// DELETE /profiles/{target}/follow — unfollow
app.delete('/profiles/:target/follow', async (c: Context) => {
  const query = c.req.query();
  const result = await c.var.kernel.handleRequest({
    method: 'unfollow',
    ...query,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

// GET /profiles/{target}/follow — isFollowing
app.get('/profiles/:target/follow', async (c: Context) => {
  const query = c.req.query();
  const result = await c.var.kernel.handleRequest({
    method: 'isFollowing',
    ...query,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

export { app as followRouter };
