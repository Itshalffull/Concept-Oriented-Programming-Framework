// Auto-generated by Clef Clef Bind — rest target
// Concept: Favorite
// Do not edit manually; regenerate with: clef interface generate

import { Hono } from 'hono';
import type { Context } from 'hono';

const app = new Hono();

// POST /articles/{article}/favorite — favorite
app.post('/articles/:article/favorite', async (c: Context) => {
  const body = await c.req.json();
  const result = await c.var.kernel.handleRequest({
    method: 'favorite',
    ...body,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

// DELETE /articles/{article}/favorite — unfavorite
app.delete('/articles/:article/favorite', async (c: Context) => {
  const query = c.req.query();
  const result = await c.var.kernel.handleRequest({
    method: 'unfavorite',
    ...query,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

// GET /articles/{article}/favorite — isFavorited
app.get('/articles/:article/favorite', async (c: Context) => {
  const query = c.req.query();
  const result = await c.var.kernel.handleRequest({
    method: 'isFavorited',
    ...query,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

// GET /articles/{article}/favorite/count — count
app.get('/articles/:article/favorite/count', async (c: Context) => {
  const query = c.req.query();
  const result = await c.var.kernel.handleRequest({
    method: 'count',
    ...query,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

export { app as favoriteRouter };
