// Auto-generated by Clef Clef Bind — rest target
// Concept: Tag
// Do not edit manually; regenerate with: clef interface generate

import { Hono } from 'hono';
import type { Context } from 'hono';

const app = new Hono();

// POST /tags — add
app.post('/tags', async (c: Context) => {
  const body = await c.req.json();
  const result = await c.var.kernel.handleRequest({
    method: 'add',
    ...body,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 201);
});

// DELETE /tags/{tag} — remove
app.delete('/tags/:tag', async (c: Context) => {
  const query = c.req.query();
  const result = await c.var.kernel.handleRequest({
    method: 'remove',
    ...query,
  });
  if (result.variant === 'notFound') return c.json({ errors: { body: ['not found'] } }, 404);
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

// GET /tags — list
app.get('/tags', async (c: Context) => {
  const query = c.req.query();
  const result = await c.var.kernel.handleRequest({
    method: 'list',
    ...query,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

export { app as tagRouter };
