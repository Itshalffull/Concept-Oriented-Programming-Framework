// Auto-generated by COPF Interface Kit — rest target
// Concept: Password
// Do not edit manually; regenerate with: copf interface generate

import { Hono } from 'hono';
import type { Context } from 'hono';

const app = new Hono();

// PUT /users/password — set
app.put('/users/password', async (c: Context) => {
  const body = await c.req.json();
  const result = await c.var.kernel.handleRequest({
    method: 'set',
    ...body,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

// POST /users/password/check — check
app.post('/users/password/check', async (c: Context) => {
  const body = await c.req.json();
  const result = await c.var.kernel.handleRequest({
    method: 'check',
    ...body,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

// POST /users/password/validate — validate
app.post('/users/password/validate', async (c: Context) => {
  const body = await c.req.json();
  const result = await c.var.kernel.handleRequest({
    method: 'validate',
    ...body,
  });
  if (result.error) return c.json({ errors: { body: [result.error] } }, 422);
  return c.json(result.body, 200);
});

export { app as passwordRouter };
