// Auto-generated by COPF Interface Kit — swift-sdk target
// Concept: Tag
// Do not edit manually; regenerate with: copf interface generate

import Foundation

// --- Error Type ---

public enum ClientError: Error, LocalizedError {
    case invalidResponse
    case apiError(status: Int, body: String)

    public var errorDescription: String? {
        switch self {
        case .invalidResponse:
            return "Invalid HTTP response"
        case .apiError(let status, let body):
            return "API error \(status): \(body)"
        }
    }
}

// --- TagClient ---

public class TagClient {

    let baseURL: URL
    var headers: [String: String]
    private let encoder = JSONEncoder()
    private let decoder = JSONDecoder()

    public init(baseURL: URL, headers: [String: String] = [:]) {
        self.baseURL = baseURL
        self.headers = headers
    }

    // --- Request/Response Types ---

    public struct AddInput: Codable {
        public let tag: String
        public let article: String
    }

    public struct AddOutput: Codable {
        public let tag: String
    }

    public struct RemoveInput: Codable {
        public let tag: String
        public let article: String
    }

    public struct RemoveOutput: Codable {
        public let tag: String
    }

    public struct ListInput: Codable {}

    public struct ListOutput: Codable {
        public let tags: String
    }

    // --- Client Methods ---

    /// add — POST /api/tags
    public func add(input: AddInput) async throws -> AddOutput {
        let url = baseURL.appendingPathComponent("/api/tags")
        var request = URLRequest(url: url)
        request.httpMethod = "POST"
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        for (key, value) in headers {
            request.setValue(value, forHTTPHeaderField: key)
        }
        request.httpBody = try encoder.encode(input)
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else {
            throw ClientError.invalidResponse
        }
        guard httpResponse.statusCode < 400 else {
            let body = String(data: data, encoding: .utf8) ?? ""
            throw ClientError.apiError(status: httpResponse.statusCode, body: body)
        }
        return try decoder.decode(AddOutput.self, from: data)
    }

    /// remove — DELETE /api/tags/{id}
    public func remove(id: String) async throws -> RemoveOutput {
        let path = "/api/tags/{id}".replacingOccurrences(of: "{id}", with: id)
        let url = baseURL.appendingPathComponent(path)
        var request = URLRequest(url: url)
        request.httpMethod = "DELETE"
        for (key, value) in headers {
            request.setValue(value, forHTTPHeaderField: key)
        }
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else {
            throw ClientError.invalidResponse
        }
        guard httpResponse.statusCode < 400 else {
            let body = String(data: data, encoding: .utf8) ?? ""
            throw ClientError.apiError(status: httpResponse.statusCode, body: body)
        }
        return try decoder.decode(RemoveOutput.self, from: data)
    }

    /// list — GET /api/tags
    public func list() async throws -> ListOutput {
        let url = baseURL.appendingPathComponent("/api/tags")
        var request = URLRequest(url: url)
        request.httpMethod = "GET"
        for (key, value) in headers {
            request.setValue(value, forHTTPHeaderField: key)
        }
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else {
            throw ClientError.invalidResponse
        }
        guard httpResponse.statusCode < 400 else {
            let body = String(data: data, encoding: .utf8) ?? ""
            throw ClientError.apiError(status: httpResponse.statusCode, body: body)
        }
        return try decoder.decode(ListOutput.self, from: data)
    }
}
