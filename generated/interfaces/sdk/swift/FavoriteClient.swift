// Auto-generated by Clef Interface Kit — swift-sdk target
// Concept: Favorite
// Do not edit manually; regenerate with: clef interface generate

import Foundation

// --- Error Type ---

public enum ClientError: Error, LocalizedError {
    case invalidResponse
    case apiError(status: Int, body: String)

    public var errorDescription: String? {
        switch self {
        case .invalidResponse:
            return "Invalid HTTP response"
        case .apiError(let status, let body):
            return "API error \(status): \(body)"
        }
    }
}

// --- FavoriteClient ---

public class FavoriteClient {

    let baseURL: URL
    var headers: [String: String]
    private let encoder = JSONEncoder()
    private let decoder = JSONDecoder()

    public init(baseURL: URL, headers: [String: String] = [:]) {
        self.baseURL = baseURL
        self.headers = headers
    }

    // --- Request/Response Types ---

    public struct FavoriteInput: Codable {
        public let user: String
        public let article: String
    }

    public struct FavoriteOutput: Codable {
        public let user: String
        public let article: String
    }

    public struct UnfavoriteInput: Codable {
        public let user: String
        public let article: String
    }

    public struct UnfavoriteOutput: Codable {
        public let user: String
        public let article: String
    }

    public struct IsFavoritedInput: Codable {
        public let user: String
        public let article: String
    }

    public struct IsFavoritedOutput: Codable {
        public let favorited: Bool
    }

    public struct CountInput: Codable {
        public let article: String
    }

    public struct CountOutput: Codable {
        public let count: Int64
    }

    // --- Client Methods ---

    /// favorite — POST /api/favorites/{id}/favorite
    public func favorite(input: FavoriteInput) async throws -> FavoriteOutput {
        let path = "/api/favorites/{id}/favorite".replacingOccurrences(of: "{id}", with: "")
        let url = baseURL.appendingPathComponent(path)
        var request = URLRequest(url: url)
        request.httpMethod = "POST"
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        for (key, value) in headers {
            request.setValue(value, forHTTPHeaderField: key)
        }
        request.httpBody = try encoder.encode(input)
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else {
            throw ClientError.invalidResponse
        }
        guard httpResponse.statusCode < 400 else {
            let body = String(data: data, encoding: .utf8) ?? ""
            throw ClientError.apiError(status: httpResponse.statusCode, body: body)
        }
        return try decoder.decode(FavoriteOutput.self, from: data)
    }

    /// unfavorite — POST /api/favorites/{id}/unfavorite
    public func unfavorite(input: UnfavoriteInput) async throws -> UnfavoriteOutput {
        let path = "/api/favorites/{id}/unfavorite".replacingOccurrences(of: "{id}", with: "")
        let url = baseURL.appendingPathComponent(path)
        var request = URLRequest(url: url)
        request.httpMethod = "POST"
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        for (key, value) in headers {
            request.setValue(value, forHTTPHeaderField: key)
        }
        request.httpBody = try encoder.encode(input)
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else {
            throw ClientError.invalidResponse
        }
        guard httpResponse.statusCode < 400 else {
            let body = String(data: data, encoding: .utf8) ?? ""
            throw ClientError.apiError(status: httpResponse.statusCode, body: body)
        }
        return try decoder.decode(UnfavoriteOutput.self, from: data)
    }

    /// isFavorited — POST /api/favorites/{id}/is-favorited
    public func isFavorited(input: IsFavoritedInput) async throws -> IsFavoritedOutput {
        let path = "/api/favorites/{id}/is-favorited".replacingOccurrences(of: "{id}", with: "")
        let url = baseURL.appendingPathComponent(path)
        var request = URLRequest(url: url)
        request.httpMethod = "POST"
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        for (key, value) in headers {
            request.setValue(value, forHTTPHeaderField: key)
        }
        request.httpBody = try encoder.encode(input)
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else {
            throw ClientError.invalidResponse
        }
        guard httpResponse.statusCode < 400 else {
            let body = String(data: data, encoding: .utf8) ?? ""
            throw ClientError.apiError(status: httpResponse.statusCode, body: body)
        }
        return try decoder.decode(IsFavoritedOutput.self, from: data)
    }

    /// count — POST /api/favorites/{id}/count
    public func count(input: CountInput) async throws -> CountOutput {
        let path = "/api/favorites/{id}/count".replacingOccurrences(of: "{id}", with: "")
        let url = baseURL.appendingPathComponent(path)
        var request = URLRequest(url: url)
        request.httpMethod = "POST"
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        for (key, value) in headers {
            request.setValue(value, forHTTPHeaderField: key)
        }
        request.httpBody = try encoder.encode(input)
        let (data, response) = try await URLSession.shared.data(for: request)
        guard let httpResponse = response as? HTTPURLResponse else {
            throw ClientError.invalidResponse
        }
        guard httpResponse.statusCode < 400 else {
            let body = String(data: data, encoding: .utf8) ?? ""
            throw ClientError.apiError(status: httpResponse.statusCode, body: body)
        }
        return try decoder.decode(CountOutput.self, from: data)
    }
}
