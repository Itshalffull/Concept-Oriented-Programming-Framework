// Auto-generated by Clef Interface Kit — java-sdk target
// Concept: User
// Do not edit manually; regenerate with: clef interface generate
package com.clef.client;

import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.util.*;
import com.fasterxml.jackson.databind.ObjectMapper;

public class UserClient {

    private final String baseUrl;
    private final Map<String, String> headers;
    private final HttpClient client;
    private final ObjectMapper mapper;

    public UserClient(String baseUrl) {
        this(baseUrl, new HashMap<>());
    }

    public UserClient(String baseUrl, Map<String, String> headers) {
        this.baseUrl = baseUrl;
        this.headers = headers;
        this.client = HttpClient.newHttpClient();
        this.mapper = new ObjectMapper();
    }

    private String[] flatHeaders() {
        List<String> flat = new ArrayList<>();
        for (Map.Entry<String, String> entry : headers.entrySet()) {
            flat.add(entry.getKey());
            flat.add(entry.getValue());
        }
        return flat.toArray(new String[0]);
    }

    // --- Request/Response Types ---

    public static class RegisterInput {
        private String user;
        private String name;
        private String email;

        public RegisterInput() {}

        public String getUser() { return this.user; }
        public void setUser(String user) { this.user = user; }
        public String getName() { return this.name; }
        public void setName(String name) { this.name = name; }
        public String getEmail() { return this.email; }
        public void setEmail(String email) { this.email = email; }
    }

    public static class RegisterOutput {
        private String user;

        public RegisterOutput() {}

        public String getUser() { return this.user; }
        public void setUser(String user) { this.user = user; }
    }

    // --- Client Methods ---

    /**
     * register — POST /api/users/{id}/register
     */
    public RegisterOutput register(RegisterInput input) throws Exception {
        String url = this.baseUrl + "/api/users/{id}/register".replace("{id}", "");
        String body = mapper.writeValueAsString(input);
        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create(url))
            .method("POST", HttpRequest.BodyPublishers.ofString(body))
            .header("Content-Type", "application/json")
            .headers(flatHeaders())
            .build();
        HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
        if (response.statusCode() >= 400) {
            throw new Exception("register failed with status " + response.statusCode() + ": " + response.body());
        }
        return mapper.readValue(response.body(), RegisterOutput.class);
    }
}
