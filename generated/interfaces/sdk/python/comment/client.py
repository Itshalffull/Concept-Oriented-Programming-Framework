# Auto-generated by Clef Interface Kit — python-sdk target
# Concept: Comment
# Do not edit manually; regenerate with: clef interface generate

from __future__ import annotations

import httpx


class CommentClient:
    """Async HTTP client for the Comment concept."""

    def __init__(self, base_url: str, headers: dict[str, str] | None = None) -> None:
        self.base_url = base_url.rstrip("/")
        self.headers = headers or {}

    async def create(self, comment: str, body: str, target: str, author: str) -> dict:
        """create — POST /api/comments"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/comments",
                json={"comment": comment, "body": body, "target": target, "author": author},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()

    async def delete(self, comment: str) -> dict:
        """delete — DELETE /api/comments/{id}"""
        async with httpx.AsyncClient() as client:
            resp = await client.delete(
                f"{self.base_url}/api/comments/{comment}",
                headers=self.headers,
            )
            resp.raise_for_status()
            return resp.json()

    async def list(self, target: str) -> dict:
        """list — GET /api/comments"""
        async with httpx.AsyncClient() as client:
            resp = await client.get(
                f"{self.base_url}/api/comments",
                params={"target": target},
                headers=self.headers,
            )
            resp.raise_for_status()
            return resp.json()
