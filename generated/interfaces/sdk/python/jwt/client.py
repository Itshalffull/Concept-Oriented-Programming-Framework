# Auto-generated by COPF Interface Kit — python-sdk target
# Concept: JWT
# Do not edit manually; regenerate with: copf interface generate

from __future__ import annotations

import httpx


class JWTClient:
    """Async HTTP client for the JWT concept."""

    def __init__(self, base_url: str, headers: dict[str, str] | None = None) -> None:
        self.base_url = base_url.rstrip("/")
        self.headers = headers or {}

    async def generate(self, user: str) -> dict:
        """generate — POST /api/jwts/{id}/generate"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/jwts/{user}/generate",
                json={"user": user},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()

    async def verify(self, token: str) -> dict:
        """verify — POST /api/jwts/{id}/verify"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/jwts/{token}/verify",
                json={"token": token},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()
