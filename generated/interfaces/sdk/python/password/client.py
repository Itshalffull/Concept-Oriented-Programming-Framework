# Auto-generated by Clef Interface Kit — python-sdk target
# Concept: Password
# Do not edit manually; regenerate with: clef interface generate

from __future__ import annotations

import httpx


class PasswordClient:
    """Async HTTP client for the Password concept."""

    def __init__(self, base_url: str, headers: dict[str, str] | None = None) -> None:
        self.base_url = base_url.rstrip("/")
        self.headers = headers or {}

    async def set(self, user: str, password: str) -> dict:
        """set — POST /api/passwords/{id}/set"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/passwords/{user}/set",
                json={"user": user, "password": password},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()

    async def check(self, user: str, password: str) -> dict:
        """check — POST /api/passwords/{id}/check"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/passwords/{user}/check",
                json={"user": user, "password": password},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()

    async def validate(self, password: str) -> dict:
        """validate — POST /api/passwords/{id}/validate"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/passwords/{password}/validate",
                json={"password": password},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()
