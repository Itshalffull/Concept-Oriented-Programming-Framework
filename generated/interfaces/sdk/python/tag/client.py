# Auto-generated by COPF Interface Kit — python-sdk target
# Concept: Tag
# Do not edit manually; regenerate with: copf interface generate

from __future__ import annotations

import httpx


class TagClient:
    """Async HTTP client for the Tag concept."""

    def __init__(self, base_url: str, headers: dict[str, str] | None = None) -> None:
        self.base_url = base_url.rstrip("/")
        self.headers = headers or {}

    async def add(self, tag: str, article: str) -> dict:
        """add — POST /api/tags"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/tags",
                json={"tag": tag, "article": article},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()

    async def remove(self, tag: str) -> dict:
        """remove — DELETE /api/tags/{id}"""
        async with httpx.AsyncClient() as client:
            resp = await client.delete(
                f"{self.base_url}/api/tags/{tag}",
                headers=self.headers,
            )
            resp.raise_for_status()
            return resp.json()

    async def list(self) -> dict:
        """list — GET /api/tags"""
        async with httpx.AsyncClient() as client:
            resp = await client.get(
                f"{self.base_url}/api/tags",
                headers=self.headers,
            )
            resp.raise_for_status()
            return resp.json()
