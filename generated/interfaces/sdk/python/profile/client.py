# Auto-generated by COPF Interface Kit — python-sdk target
# Concept: Profile
# Do not edit manually; regenerate with: copf interface generate

from __future__ import annotations

import httpx


class ProfileClient:
    """Async HTTP client for the Profile concept."""

    def __init__(self, base_url: str, headers: dict[str, str] | None = None) -> None:
        self.base_url = base_url.rstrip("/")
        self.headers = headers or {}

    async def update(self, user: str, bio: str, image: str) -> dict:
        """update — PUT /api/profiles/{id}"""
        async with httpx.AsyncClient() as client:
            resp = await client.put(
                f"{self.base_url}/api/profiles/{user}",
                json={"user": user, "bio": bio, "image": image},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()

    async def get(self, user: str) -> dict:
        """get — GET /api/profiles/{id}"""
        async with httpx.AsyncClient() as client:
            resp = await client.get(
                f"{self.base_url}/api/profiles/{user}",
                headers=self.headers,
            )
            resp.raise_for_status()
            return resp.json()
