# Auto-generated by COPF Interface Kit — python-sdk target
# Concept: Follow
# Do not edit manually; regenerate with: copf interface generate

from __future__ import annotations

import httpx


class FollowClient:
    """Async HTTP client for the Follow concept."""

    def __init__(self, base_url: str, headers: dict[str, str] | None = None) -> None:
        self.base_url = base_url.rstrip("/")
        self.headers = headers or {}

    async def follow(self, user: str, target: str) -> dict:
        """follow — POST /api/follows/{id}/follow"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/follows/{user}/follow",
                json={"user": user, "target": target},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()

    async def unfollow(self, user: str, target: str) -> dict:
        """unfollow — POST /api/follows/{id}/unfollow"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/follows/{user}/unfollow",
                json={"user": user, "target": target},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()

    async def is_following(self, user: str, target: str) -> dict:
        """isFollowing — POST /api/follows/{id}/is-following"""
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                f"{self.base_url}/api/follows/{user}/is-following",
                json={"user": user, "target": target},
                headers={**self.headers, "Content-Type": "application/json"},
            )
            resp.raise_for_status()
            return resp.json()
