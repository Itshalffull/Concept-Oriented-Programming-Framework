// Auto-generated by Clef Interface Kit — rust-sdk target
// Concept: Profile
// Do not edit manually; regenerate with: clef interface generate

use serde::{Deserialize, Serialize};
use reqwest::Client;

// --- Error Type ---

#[derive(Debug)]
pub enum Error {
    Request(reqwest::Error),
    Api { status: u16, body: String },
}

impl std::fmt::Display for Error {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Error::Request(e) => write!(f, "request error: {e}"),
            Error::Api { status, body } => write!(f, "API error {status}: {body}"),
        }
    }
}

impl std::error::Error for Error {}

// --- Request/Response Types ---

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateProfileInput {
    pub user: String,
    pub bio: String,
    pub image: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateProfileOutput {
    pub user: String,
    pub bio: String,
    pub image: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetProfileInput {
    pub user: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetProfileOutput {
    pub user: String,
    pub bio: String,
    pub image: String,
}

// --- Client ---

pub struct ProfileClient {
    base_url: String,
    headers: Vec<(String, String)>,
    client: Client,
}

impl ProfileClient {
    pub fn new(base_url: impl Into<String>) -> Self {
        Self {
            base_url: base_url.into(),
            headers: Vec::new(),
            client: Client::new(),
        }
    }

    pub fn with_header(mut self, key: impl Into<String>, value: impl Into<String>) -> Self {
        self.headers.push((key.into(), value.into()));
        self
    }

    pub async fn update(&self, input: UpdateProfileInput) -> Result<UpdateProfileOutput, Error> {
        let url = format!("{}/api/profiles/{}",  self.base_url, "");
        let mut req = self.client.put(&url)
            .json(&input);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<UpdateProfileOutput>().await.map_err(Error::Request)
    }

    pub async fn get(&self, id: &str) -> Result<GetProfileOutput, Error> {
        let url = format!("{}/api/profiles/{}",  self.base_url, id);
        let mut req = self.client.get(&url);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<GetProfileOutput>().await.map_err(Error::Request)
    }
}
