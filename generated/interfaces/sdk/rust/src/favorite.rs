// Auto-generated by COPF Interface Kit — rust-sdk target
// Concept: Favorite
// Do not edit manually; regenerate with: copf interface generate

use serde::{Deserialize, Serialize};
use reqwest::Client;

// --- Error Type ---

#[derive(Debug)]
pub enum Error {
    Request(reqwest::Error),
    Api { status: u16, body: String },
}

impl std::fmt::Display for Error {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Error::Request(e) => write!(f, "request error: {e}"),
            Error::Api { status, body } => write!(f, "API error {status}: {body}"),
        }
    }
}

impl std::error::Error for Error {}

// --- Request/Response Types ---

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct FavoriteFavoriteInput {
    pub user: String,
    pub article: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct FavoriteFavoriteOutput {
    pub user: String,
    pub article: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UnfavoriteFavoriteInput {
    pub user: String,
    pub article: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UnfavoriteFavoriteOutput {
    pub user: String,
    pub article: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IsFavoritedFavoriteInput {
    pub user: String,
    pub article: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IsFavoritedFavoriteOutput {
    pub favorited: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CountFavoriteInput {
    pub article: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CountFavoriteOutput {
    pub count: i64,
}

// --- Client ---

pub struct FavoriteClient {
    base_url: String,
    headers: Vec<(String, String)>,
    client: Client,
}

impl FavoriteClient {
    pub fn new(base_url: impl Into<String>) -> Self {
        Self {
            base_url: base_url.into(),
            headers: Vec::new(),
            client: Client::new(),
        }
    }

    pub fn with_header(mut self, key: impl Into<String>, value: impl Into<String>) -> Self {
        self.headers.push((key.into(), value.into()));
        self
    }

    pub async fn favorite(&self, input: FavoriteFavoriteInput) -> Result<FavoriteFavoriteOutput, Error> {
        let url = format!("{}/api/favorites/{}/favorite",  self.base_url, "");
        let mut req = self.client.post(&url)
            .json(&input);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<FavoriteFavoriteOutput>().await.map_err(Error::Request)
    }

    pub async fn unfavorite(&self, input: UnfavoriteFavoriteInput) -> Result<UnfavoriteFavoriteOutput, Error> {
        let url = format!("{}/api/favorites/{}/unfavorite",  self.base_url, "");
        let mut req = self.client.post(&url)
            .json(&input);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<UnfavoriteFavoriteOutput>().await.map_err(Error::Request)
    }

    pub async fn is_favorited(&self, input: IsFavoritedFavoriteInput) -> Result<IsFavoritedFavoriteOutput, Error> {
        let url = format!("{}/api/favorites/{}/is-favorited",  self.base_url, "");
        let mut req = self.client.post(&url)
            .json(&input);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<IsFavoritedFavoriteOutput>().await.map_err(Error::Request)
    }

    pub async fn count(&self, input: CountFavoriteInput) -> Result<CountFavoriteOutput, Error> {
        let url = format!("{}/api/favorites/{}/count",  self.base_url, "");
        let mut req = self.client.post(&url)
            .json(&input);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<CountFavoriteOutput>().await.map_err(Error::Request)
    }
}
