// Auto-generated by COPF Interface Kit — rust-sdk target
// Concept: Comment
// Do not edit manually; regenerate with: copf interface generate

use serde::{Deserialize, Serialize};
use reqwest::Client;

// --- Error Type ---

#[derive(Debug)]
pub enum Error {
    Request(reqwest::Error),
    Api { status: u16, body: String },
}

impl std::fmt::Display for Error {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Error::Request(e) => write!(f, "request error: {e}"),
            Error::Api { status, body } => write!(f, "API error {status}: {body}"),
        }
    }
}

impl std::error::Error for Error {}

// --- Request/Response Types ---

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateCommentInput {
    pub comment: String,
    pub body: String,
    pub target: String,
    pub author: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateCommentOutput {
    pub comment: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteCommentInput {
    pub comment: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteCommentOutput {
    pub comment: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListCommentInput {
    pub target: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListCommentOutput {
    pub comments: String,
}

// --- Client ---

pub struct CommentClient {
    base_url: String,
    headers: Vec<(String, String)>,
    client: Client,
}

impl CommentClient {
    pub fn new(base_url: impl Into<String>) -> Self {
        Self {
            base_url: base_url.into(),
            headers: Vec::new(),
            client: Client::new(),
        }
    }

    pub fn with_header(mut self, key: impl Into<String>, value: impl Into<String>) -> Self {
        self.headers.push((key.into(), value.into()));
        self
    }

    pub async fn create(&self, input: CreateCommentInput) -> Result<CreateCommentOutput, Error> {
        let url = format!("{}/api/comments", self.base_url);
        let mut req = self.client.post(&url)
            .json(&input);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<CreateCommentOutput>().await.map_err(Error::Request)
    }

    pub async fn delete(&self, id: &str) -> Result<DeleteCommentOutput, Error> {
        let url = format!("{}/api/comments/{}",  self.base_url, id);
        let mut req = self.client.delete(&url);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<DeleteCommentOutput>().await.map_err(Error::Request)
    }

    pub async fn list(&self) -> Result<ListCommentOutput, Error> {
        let url = format!("{}/api/comments", self.base_url);
        let mut req = self.client.get(&url);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<ListCommentOutput>().await.map_err(Error::Request)
    }
}
