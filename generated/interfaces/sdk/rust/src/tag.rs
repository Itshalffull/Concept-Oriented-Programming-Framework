// Auto-generated by Clef Interface Kit — rust-sdk target
// Concept: Tag
// Do not edit manually; regenerate with: clef interface generate

use serde::{Deserialize, Serialize};
use reqwest::Client;

// --- Error Type ---

#[derive(Debug)]
pub enum Error {
    Request(reqwest::Error),
    Api { status: u16, body: String },
}

impl std::fmt::Display for Error {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Error::Request(e) => write!(f, "request error: {e}"),
            Error::Api { status, body } => write!(f, "API error {status}: {body}"),
        }
    }
}

impl std::error::Error for Error {}

// --- Request/Response Types ---

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AddTagInput {
    pub tag: String,
    pub article: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AddTagOutput {
    pub tag: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RemoveTagInput {
    pub tag: String,
    pub article: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RemoveTagOutput {
    pub tag: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListTagInput {}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListTagOutput {
    pub tags: String,
}

// --- Client ---

pub struct TagClient {
    base_url: String,
    headers: Vec<(String, String)>,
    client: Client,
}

impl TagClient {
    pub fn new(base_url: impl Into<String>) -> Self {
        Self {
            base_url: base_url.into(),
            headers: Vec::new(),
            client: Client::new(),
        }
    }

    pub fn with_header(mut self, key: impl Into<String>, value: impl Into<String>) -> Self {
        self.headers.push((key.into(), value.into()));
        self
    }

    pub async fn add(&self, input: AddTagInput) -> Result<AddTagOutput, Error> {
        let url = format!("{}/api/tags", self.base_url);
        let mut req = self.client.post(&url)
            .json(&input);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<AddTagOutput>().await.map_err(Error::Request)
    }

    pub async fn remove(&self, id: &str) -> Result<RemoveTagOutput, Error> {
        let url = format!("{}/api/tags/{}",  self.base_url, id);
        let mut req = self.client.delete(&url);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<RemoveTagOutput>().await.map_err(Error::Request)
    }

    pub async fn list(&self) -> Result<ListTagOutput, Error> {
        let url = format!("{}/api/tags", self.base_url);
        let mut req = self.client.get(&url);
        for (k, v) in &self.headers {
            req = req.header(k.as_str(), v.as_str());
        }
        let resp = req.send().await.map_err(Error::Request)?;
        if !resp.status().is_success() {
            let status = resp.status().as_u16();
            let body = resp.text().await.unwrap_or_default();
            return Err(Error::Api { status, body });
        }
        resp.json::<ListTagOutput>().await.map_err(Error::Request)
    }
}
