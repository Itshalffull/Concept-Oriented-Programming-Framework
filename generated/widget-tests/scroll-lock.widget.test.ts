// Auto-generated by generate-widget-tests.ts from scroll-lock.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: ScrollLock', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["unlocked"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('unlocked');
    });

    it('should transition from unlocked to locked on LOCK', () => {
      const transition = { from: 'unlocked', event: 'LOCK', target: 'locked' };
      expect(transition.from).toBe('unlocked');
      expect(transition.event).toBe('LOCK');
      expect(transition.target).toBe('locked');
      // Target state 'locked' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from locked to unlocked on UNLOCK', () => {
      const transition = { from: 'locked', event: 'UNLOCK', target: 'unlocked' };
      expect(transition.from).toBe('locked');
      expect(transition.event).toBe('UNLOCK');
      expect(transition.target).toBe('unlocked');
      // Target state 'unlocked' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"unlocked","events":["LOCK"]},{"name":"locked","events":["UNLOCK"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["unlocked","locked"]);
      const baseNames = new Set(["unlocked","locked"]);
      expect(validNames.has('locked') || baseNames.has('locked')).toBe(true);
      expect(validNames.has('unlocked') || baseNames.has('unlocked')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="root" to root', () => {
      const binding = {"part":"root","dataPart":"root"};
      expect(binding.dataPart).toBe('root');
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if ? active then locked else unlocked" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-scroll-lock on root', () => {
      const attr = { name: 'data-scroll-lock', value: "if ? active then true else false" };
      expect(attr.name).toBe('data-scroll-lock');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-preserve-gap on root', () => {
      const attr = { name: 'data-preserve-gap', value: "if ? preserveScrollbarGap then true else false" };
      expect(attr.name).toBe('data-preserve-gap');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root"]);
      expect(anatomyParts.has('root')).toBe(true);
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "none"', () => {
      expect('none').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "none"', () => {
      expect('none').toBeTruthy();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "active" with type "Bool"', () => {
      const prop = { name: 'active', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('active');
      expect(prop.type).toBeTruthy();
    });

    it('should default "active" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "preserveScrollbarGap" with type "Bool"', () => {
      const prop = { name: 'preserveScrollbarGap', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('preserveScrollbarGap');
      expect(prop.type).toBeTruthy();
    });

    it('should default "preserveScrollbarGap" to "true"', () => {
      expect("true").toBe("true");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root"];
      expect(parts.length).toBe(1);
      expect(parts).toContain('root');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
    });

  });

});
