// Auto-generated by generate-widget-tests.ts from calendar-display.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: CalendarDisplay', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["monthView","focus.unfocused"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('monthView');
      expect(initialStates).toContain('focus.unfocused');
    });

    it('should transition from monthView to weekView on SWITCH_WEEK', () => {
      const transition = { from: 'monthView', event: 'SWITCH_WEEK', target: 'weekView' };
      expect(transition.from).toBe('monthView');
      expect(transition.event).toBe('SWITCH_WEEK');
      expect(transition.target).toBe('weekView');
      // Target state 'weekView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from monthView to dayView on SWITCH_DAY', () => {
      const transition = { from: 'monthView', event: 'SWITCH_DAY', target: 'dayView' };
      expect(transition.from).toBe('monthView');
      expect(transition.event).toBe('SWITCH_DAY');
      expect(transition.target).toBe('dayView');
      // Target state 'dayView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from monthView to navigating on NAVIGATE_PREV', () => {
      const transition = { from: 'monthView', event: 'NAVIGATE_PREV', target: 'navigating' };
      expect(transition.from).toBe('monthView');
      expect(transition.event).toBe('NAVIGATE_PREV');
      expect(transition.target).toBe('navigating');
      // Target state 'navigating' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from monthView to navigating on NAVIGATE_NEXT', () => {
      const transition = { from: 'monthView', event: 'NAVIGATE_NEXT', target: 'navigating' };
      expect(transition.from).toBe('monthView');
      expect(transition.event).toBe('NAVIGATE_NEXT');
      expect(transition.target).toBe('navigating');
      // Target state 'navigating' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from monthView to monthView on GO_TODAY', () => {
      const transition = { from: 'monthView', event: 'GO_TODAY', target: 'monthView' };
      expect(transition.from).toBe('monthView');
      expect(transition.event).toBe('GO_TODAY');
      expect(transition.target).toBe('monthView');
      // Target state 'monthView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from monthView to monthView on SELECT_DATE', () => {
      const transition = { from: 'monthView', event: 'SELECT_DATE', target: 'monthView' };
      expect(transition.from).toBe('monthView');
      expect(transition.event).toBe('SELECT_DATE');
      expect(transition.target).toBe('monthView');
      // Target state 'monthView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from monthView to monthView on ACTIVATE_EVENT', () => {
      const transition = { from: 'monthView', event: 'ACTIVATE_EVENT', target: 'monthView' };
      expect(transition.from).toBe('monthView');
      expect(transition.event).toBe('ACTIVATE_EVENT');
      expect(transition.target).toBe('monthView');
      // Target state 'monthView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from weekView to monthView on SWITCH_MONTH', () => {
      const transition = { from: 'weekView', event: 'SWITCH_MONTH', target: 'monthView' };
      expect(transition.from).toBe('weekView');
      expect(transition.event).toBe('SWITCH_MONTH');
      expect(transition.target).toBe('monthView');
      // Target state 'monthView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from weekView to dayView on SWITCH_DAY', () => {
      const transition = { from: 'weekView', event: 'SWITCH_DAY', target: 'dayView' };
      expect(transition.from).toBe('weekView');
      expect(transition.event).toBe('SWITCH_DAY');
      expect(transition.target).toBe('dayView');
      // Target state 'dayView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from weekView to navigating on NAVIGATE_PREV', () => {
      const transition = { from: 'weekView', event: 'NAVIGATE_PREV', target: 'navigating' };
      expect(transition.from).toBe('weekView');
      expect(transition.event).toBe('NAVIGATE_PREV');
      expect(transition.target).toBe('navigating');
      // Target state 'navigating' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from weekView to navigating on NAVIGATE_NEXT', () => {
      const transition = { from: 'weekView', event: 'NAVIGATE_NEXT', target: 'navigating' };
      expect(transition.from).toBe('weekView');
      expect(transition.event).toBe('NAVIGATE_NEXT');
      expect(transition.target).toBe('navigating');
      // Target state 'navigating' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from weekView to weekView on GO_TODAY', () => {
      const transition = { from: 'weekView', event: 'GO_TODAY', target: 'weekView' };
      expect(transition.from).toBe('weekView');
      expect(transition.event).toBe('GO_TODAY');
      expect(transition.target).toBe('weekView');
      // Target state 'weekView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from weekView to weekView on SELECT_DATE', () => {
      const transition = { from: 'weekView', event: 'SELECT_DATE', target: 'weekView' };
      expect(transition.from).toBe('weekView');
      expect(transition.event).toBe('SELECT_DATE');
      expect(transition.target).toBe('weekView');
      // Target state 'weekView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from weekView to weekView on ACTIVATE_EVENT', () => {
      const transition = { from: 'weekView', event: 'ACTIVATE_EVENT', target: 'weekView' };
      expect(transition.from).toBe('weekView');
      expect(transition.event).toBe('ACTIVATE_EVENT');
      expect(transition.target).toBe('weekView');
      // Target state 'weekView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dayView to monthView on SWITCH_MONTH', () => {
      const transition = { from: 'dayView', event: 'SWITCH_MONTH', target: 'monthView' };
      expect(transition.from).toBe('dayView');
      expect(transition.event).toBe('SWITCH_MONTH');
      expect(transition.target).toBe('monthView');
      // Target state 'monthView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dayView to weekView on SWITCH_WEEK', () => {
      const transition = { from: 'dayView', event: 'SWITCH_WEEK', target: 'weekView' };
      expect(transition.from).toBe('dayView');
      expect(transition.event).toBe('SWITCH_WEEK');
      expect(transition.target).toBe('weekView');
      // Target state 'weekView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dayView to navigating on NAVIGATE_PREV', () => {
      const transition = { from: 'dayView', event: 'NAVIGATE_PREV', target: 'navigating' };
      expect(transition.from).toBe('dayView');
      expect(transition.event).toBe('NAVIGATE_PREV');
      expect(transition.target).toBe('navigating');
      // Target state 'navigating' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dayView to navigating on NAVIGATE_NEXT', () => {
      const transition = { from: 'dayView', event: 'NAVIGATE_NEXT', target: 'navigating' };
      expect(transition.from).toBe('dayView');
      expect(transition.event).toBe('NAVIGATE_NEXT');
      expect(transition.target).toBe('navigating');
      // Target state 'navigating' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dayView to dayView on GO_TODAY', () => {
      const transition = { from: 'dayView', event: 'GO_TODAY', target: 'dayView' };
      expect(transition.from).toBe('dayView');
      expect(transition.event).toBe('GO_TODAY');
      expect(transition.target).toBe('dayView');
      // Target state 'dayView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dayView to dayView on ACTIVATE_EVENT', () => {
      const transition = { from: 'dayView', event: 'ACTIVATE_EVENT', target: 'dayView' };
      expect(transition.from).toBe('dayView');
      expect(transition.event).toBe('ACTIVATE_EVENT');
      expect(transition.target).toBe('dayView');
      // Target state 'dayView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from navigating to monthView on NAVIGATE_COMPLETE_MONTH', () => {
      const transition = { from: 'navigating', event: 'NAVIGATE_COMPLETE_MONTH', target: 'monthView' };
      expect(transition.from).toBe('navigating');
      expect(transition.event).toBe('NAVIGATE_COMPLETE_MONTH');
      expect(transition.target).toBe('monthView');
      // Target state 'monthView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from navigating to weekView on NAVIGATE_COMPLETE_WEEK', () => {
      const transition = { from: 'navigating', event: 'NAVIGATE_COMPLETE_WEEK', target: 'weekView' };
      expect(transition.from).toBe('navigating');
      expect(transition.event).toBe('NAVIGATE_COMPLETE_WEEK');
      expect(transition.target).toBe('weekView');
      // Target state 'weekView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from navigating to dayView on NAVIGATE_COMPLETE_DAY', () => {
      const transition = { from: 'navigating', event: 'NAVIGATE_COMPLETE_DAY', target: 'dayView' };
      expect(transition.from).toBe('navigating');
      expect(transition.event).toBe('NAVIGATE_COMPLETE_DAY');
      expect(transition.target).toBe('dayView');
      // Target state 'dayView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.unfocused to dateFocused on FOCUS_DATE', () => {
      const transition = { from: 'focus.unfocused', event: 'FOCUS_DATE', target: 'dateFocused' };
      expect(transition.from).toBe('focus.unfocused');
      expect(transition.event).toBe('FOCUS_DATE');
      expect(transition.target).toBe('dateFocused');
      // Target state 'dateFocused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.dateFocused to unfocused on BLUR', () => {
      const transition = { from: 'focus.dateFocused', event: 'BLUR', target: 'unfocused' };
      expect(transition.from).toBe('focus.dateFocused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('unfocused');
      // Target state 'unfocused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.dateFocused to dateFocused on NAVIGATE_DATE', () => {
      const transition = { from: 'focus.dateFocused', event: 'NAVIGATE_DATE', target: 'dateFocused' };
      expect(transition.from).toBe('focus.dateFocused');
      expect(transition.event).toBe('NAVIGATE_DATE');
      expect(transition.target).toBe('dateFocused');
      // Target state 'dateFocused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"monthView","events":["SWITCH_WEEK","SWITCH_DAY","NAVIGATE_PREV","NAVIGATE_NEXT","GO_TODAY","SELECT_DATE","ACTIVATE_EVENT"]},{"name":"weekView","events":["SWITCH_MONTH","SWITCH_DAY","NAVIGATE_PREV","NAVIGATE_NEXT","GO_TODAY","SELECT_DATE","ACTIVATE_EVENT"]},{"name":"dayView","events":["SWITCH_MONTH","SWITCH_WEEK","NAVIGATE_PREV","NAVIGATE_NEXT","GO_TODAY","ACTIVATE_EVENT"]},{"name":"navigating","events":["NAVIGATE_COMPLETE_MONTH","NAVIGATE_COMPLETE_WEEK","NAVIGATE_COMPLETE_DAY"]},{"name":"focus.unfocused","events":["FOCUS_DATE"]},{"name":"focus.dateFocused","events":["BLUR","NAVIGATE_DATE"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["monthView","weekView","dayView","navigating","focus.unfocused","focus.dateFocused"]);
      const baseNames = new Set(["monthView","weekView","dayView","navigating","unfocused","dateFocused"]);
      expect(validNames.has('weekView') || baseNames.has('weekView')).toBe(true);
      expect(validNames.has('dayView') || baseNames.has('dayView')).toBe(true);
      expect(validNames.has('navigating') || baseNames.has('navigating')).toBe(true);
      expect(validNames.has('navigating') || baseNames.has('navigating')).toBe(true);
      expect(validNames.has('monthView') || baseNames.has('monthView')).toBe(true);
      expect(validNames.has('monthView') || baseNames.has('monthView')).toBe(true);
      expect(validNames.has('monthView') || baseNames.has('monthView')).toBe(true);
      expect(validNames.has('monthView') || baseNames.has('monthView')).toBe(true);
      expect(validNames.has('dayView') || baseNames.has('dayView')).toBe(true);
      expect(validNames.has('navigating') || baseNames.has('navigating')).toBe(true);
      expect(validNames.has('navigating') || baseNames.has('navigating')).toBe(true);
      expect(validNames.has('weekView') || baseNames.has('weekView')).toBe(true);
      expect(validNames.has('weekView') || baseNames.has('weekView')).toBe(true);
      expect(validNames.has('weekView') || baseNames.has('weekView')).toBe(true);
      expect(validNames.has('monthView') || baseNames.has('monthView')).toBe(true);
      expect(validNames.has('weekView') || baseNames.has('weekView')).toBe(true);
      expect(validNames.has('navigating') || baseNames.has('navigating')).toBe(true);
      expect(validNames.has('navigating') || baseNames.has('navigating')).toBe(true);
      expect(validNames.has('dayView') || baseNames.has('dayView')).toBe(true);
      expect(validNames.has('dayView') || baseNames.has('dayView')).toBe(true);
      expect(validNames.has('monthView') || baseNames.has('monthView')).toBe(true);
      expect(validNames.has('weekView') || baseNames.has('weekView')).toBe(true);
      expect(validNames.has('dayView') || baseNames.has('dayView')).toBe(true);
      expect(validNames.has('dateFocused') || baseNames.has('dateFocused')).toBe(true);
      expect(validNames.has('unfocused') || baseNames.has('unfocused')).toBe(true);
      expect(validNames.has('dateFocused') || baseNames.has('dateFocused')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="root" to root', () => {
      const binding = {"part":"root","dataPart":"root"};
      expect(binding.dataPart).toBe('root');
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if ? loading then loading else if state = = navigating then navigating else state" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-view on root', () => {
      const attr = { name: 'data-view', value: "? view" };
      expect(attr.name).toBe('data-view');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="header" to header', () => {
      const binding = {"part":"header","dataPart":"header"};
      expect(binding.dataPart).toBe('header');
    });

    it('should bind data-part="navigation" to navigation', () => {
      const binding = {"part":"navigation","dataPart":"navigation"};
      expect(binding.dataPart).toBe('navigation');
    });

    it('should bind data-part="prev-button" to prevButton', () => {
      const binding = {"part":"prevButton","dataPart":"prev-button"};
      expect(binding.dataPart).toBe('prev-button');
    });

    it('should bind onClick handler on prevButton', () => {
      const handler = { event: 'onClick', action: "send ( NAVIGATE_PREV )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on prevButton', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( NAVIGATE_PREV )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on prevButton', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( NAVIGATE_PREV )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="next-button" to nextButton', () => {
      const binding = {"part":"nextButton","dataPart":"next-button"};
      expect(binding.dataPart).toBe('next-button');
    });

    it('should bind onClick handler on nextButton', () => {
      const handler = { event: 'onClick', action: "send ( NAVIGATE_NEXT )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on nextButton', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( NAVIGATE_NEXT )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on nextButton', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( NAVIGATE_NEXT )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="today-button" to todayButton', () => {
      const binding = {"part":"todayButton","dataPart":"today-button"};
      expect(binding.dataPart).toBe('today-button');
    });

    it('should bind onClick handler on todayButton', () => {
      const handler = { event: 'onClick', action: "send ( GO_TODAY )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on todayButton', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( GO_TODAY )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on todayButton', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( GO_TODAY )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="title" to title', () => {
      const binding = {"part":"title","dataPart":"title"};
      expect(binding.dataPart).toBe('title');
    });

    it('should bind data-part="view-switcher" to viewSwitcher', () => {
      const binding = {"part":"viewSwitcher","dataPart":"view-switcher"};
      expect(binding.dataPart).toBe('view-switcher');
    });

    it('should bind data-part="month-tab" to monthTab', () => {
      const binding = {"part":"monthTab","dataPart":"month-tab"};
      expect(binding.dataPart).toBe('month-tab');
    });

    it('should bind onClick handler on monthTab', () => {
      const handler = { event: 'onClick', action: "send ( SWITCH_MONTH )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on monthTab', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( SWITCH_MONTH )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on monthTab', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( SWITCH_MONTH )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="week-tab" to weekTab', () => {
      const binding = {"part":"weekTab","dataPart":"week-tab"};
      expect(binding.dataPart).toBe('week-tab');
    });

    it('should bind onClick handler on weekTab', () => {
      const handler = { event: 'onClick', action: "send ( SWITCH_WEEK )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on weekTab', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( SWITCH_WEEK )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on weekTab', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( SWITCH_WEEK )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="day-tab" to dayTab', () => {
      const binding = {"part":"dayTab","dataPart":"day-tab"};
      expect(binding.dataPart).toBe('day-tab');
    });

    it('should bind onClick handler on dayTab', () => {
      const handler = { event: 'onClick', action: "send ( SWITCH_DAY )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on dayTab', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( SWITCH_DAY )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on dayTab', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( SWITCH_DAY )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="grid" to grid', () => {
      const binding = {"part":"grid","dataPart":"grid"};
      expect(binding.dataPart).toBe('grid');
    });

    it('should bind data-part="week-row" to weekRow', () => {
      const binding = {"part":"weekRow","dataPart":"week-row"};
      expect(binding.dataPart).toBe('week-row');
    });

    it('should bind data-part="day-cell" to dayCell', () => {
      const binding = {"part":"dayCell","dataPart":"day-cell"};
      expect(binding.dataPart).toBe('day-cell');
    });

    it('should bind data-today on dayCell', () => {
      const attr = { name: 'data-today', value: "if self . isToday then true else false" };
      expect(attr.name).toBe('data-today');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-selected on dayCell', () => {
      const attr = { name: 'data-selected', value: "if self . isSelected then true else false" };
      expect(attr.name).toBe('data-selected');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-has-events on dayCell', () => {
      const attr = { name: 'data-has-events', value: "if self . eventCount > 0 then true else false" };
      expect(attr.name).toBe('data-has-events');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on dayCell', () => {
      const handler = { event: 'onClick', action: "send ( SELECT_DATE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on dayCell', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS_DATE )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on dayCell', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowLeft handler on dayCell', () => {
      const handler = { event: 'onKeyDown-ArrowLeft', action: "send ( NAVIGATE_DAY_BACK )" };
      expect(handler.event).toBe('onKeyDown-ArrowLeft');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowRight handler on dayCell', () => {
      const handler = { event: 'onKeyDown-ArrowRight', action: "send ( NAVIGATE_DAY_FORWARD )" };
      expect(handler.event).toBe('onKeyDown-ArrowRight');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on dayCell', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "send ( NAVIGATE_WEEK_BACK )" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on dayCell', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "send ( NAVIGATE_WEEK_FORWARD )" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on dayCell', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( SELECT_DATE )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on dayCell', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( SELECT_DATE )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-PageUp handler on dayCell', () => {
      const handler = { event: 'onKeyDown-PageUp', action: "send ( NAVIGATE_PREV )" };
      expect(handler.event).toBe('onKeyDown-PageUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-PageDown handler on dayCell', () => {
      const handler = { event: 'onKeyDown-PageDown', action: "send ( NAVIGATE_NEXT )" };
      expect(handler.event).toBe('onKeyDown-PageDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Home handler on dayCell', () => {
      const handler = { event: 'onKeyDown-Home', action: "send ( NAVIGATE_WEEK_START )" };
      expect(handler.event).toBe('onKeyDown-Home');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-End handler on dayCell', () => {
      const handler = { event: 'onKeyDown-End', action: "send ( NAVIGATE_WEEK_END )" };
      expect(handler.event).toBe('onKeyDown-End');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="empty-cell" to emptyCell', () => {
      const binding = {"part":"emptyCell","dataPart":"empty-cell"};
      expect(binding.dataPart).toBe('empty-cell');
    });

    it('should bind data-part="day-number" to dayNumber', () => {
      const binding = {"part":"dayNumber","dataPart":"day-number"};
      expect(binding.dataPart).toBe('day-number');
    });

    it('should bind data-part="event-list" to eventList', () => {
      const binding = {"part":"eventList","dataPart":"event-list"};
      expect(binding.dataPart).toBe('event-list');
    });

    it('should bind data-part="event-item" to eventItem', () => {
      const binding = {"part":"eventItem","dataPart":"event-item"};
      expect(binding.dataPart).toBe('event-item');
    });

    it('should bind onClick handler on eventItem', () => {
      const handler = { event: 'onClick', action: "send ( ACTIVATE_EVENT )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on eventItem', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( ACTIVATE_EVENT )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on eventItem', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( ACTIVATE_EVENT )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="overflow-badge" to overflowBadge', () => {
      const binding = {"part":"overflowBadge","dataPart":"overflow-badge"};
      expect(binding.dataPart).toBe('overflow-badge');
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","header","navigation","prevButton","nextButton","todayButton","title","viewSwitcher","monthTab","weekTab","dayTab","grid","weekRow","dayCell","emptyCell","dayNumber","eventList","eventItem","overflowBadge"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('header')).toBe(true);
      expect(anatomyParts.has('navigation')).toBe(true);
      expect(anatomyParts.has('prevButton')).toBe(true);
      expect(anatomyParts.has('nextButton')).toBe(true);
      expect(anatomyParts.has('todayButton')).toBe(true);
      expect(anatomyParts.has('title')).toBe(true);
      expect(anatomyParts.has('viewSwitcher')).toBe(true);
      expect(anatomyParts.has('monthTab')).toBe(true);
      expect(anatomyParts.has('weekTab')).toBe(true);
      expect(anatomyParts.has('dayTab')).toBe(true);
      expect(anatomyParts.has('grid')).toBe(true);
      expect(anatomyParts.has('weekRow')).toBe(true);
      expect(anatomyParts.has('dayCell')).toBe(true);
      expect(anatomyParts.has('emptyCell')).toBe(true);
      expect(anatomyParts.has('dayNumber')).toBe(true);
      expect(anatomyParts.has('eventList')).toBe(true);
      expect(anatomyParts.has('eventItem')).toBe(true);
      expect(anatomyParts.has('overflowBadge')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowLeft key with action: NAVIGATE_DAY_BACK', () => {
      const binding = { key: 'ArrowLeft', action: 'NAVIGATE_DAY_BACK' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('NAVIGATE_DAY_BACK');
    });

    it('should handle ArrowRight key with action: NAVIGATE_DAY_FORWARD', () => {
      const binding = { key: 'ArrowRight', action: 'NAVIGATE_DAY_FORWARD' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('NAVIGATE_DAY_FORWARD');
    });

    it('should handle ArrowUp key with action: NAVIGATE_WEEK_BACK', () => {
      const binding = { key: 'ArrowUp', action: 'NAVIGATE_WEEK_BACK' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('NAVIGATE_WEEK_BACK');
    });

    it('should handle ArrowDown key with action: NAVIGATE_WEEK_FORWARD', () => {
      const binding = { key: 'ArrowDown', action: 'NAVIGATE_WEEK_FORWARD' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('NAVIGATE_WEEK_FORWARD');
    });

    it('should handle Enter key with action: SELECT_DATE', () => {
      const binding = { key: 'Enter', action: 'SELECT_DATE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('SELECT_DATE');
    });

    it('should handle Space key with action: SELECT_DATE', () => {
      const binding = { key: 'Space', action: 'SELECT_DATE' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('SELECT_DATE');
    });

    it('should handle Home key with action: NAVIGATE_WEEK_START', () => {
      const binding = { key: 'Home', action: 'NAVIGATE_WEEK_START' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('NAVIGATE_WEEK_START');
    });

    it('should handle End key with action: NAVIGATE_WEEK_END', () => {
      const binding = { key: 'End', action: 'NAVIGATE_WEEK_END' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('NAVIGATE_WEEK_END');
    });

    it('should handle PageUp key with action: NAVIGATE_PREV', () => {
      const binding = { key: 'PageUp', action: 'NAVIGATE_PREV' };
      expect(binding.key).toBe('PageUp');
      expect(binding.action).toBe('NAVIGATE_PREV');
    });

    it('should handle PageDown key with action: NAVIGATE_NEXT', () => {
      const binding = { key: 'PageDown', action: 'NAVIGATE_NEXT' };
      expect(binding.key).toBe('PageDown');
      expect(binding.action).toBe('NAVIGATE_NEXT');
    });

    it('should handle Tab key with action: FOCUS_OUT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_OUT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_OUT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SWITCH_WEEK","SWITCH_DAY","NAVIGATE_PREV","NAVIGATE_NEXT","GO_TODAY","SELECT_DATE","ACTIVATE_EVENT","SWITCH_MONTH","NAVIGATE_COMPLETE_MONTH","NAVIGATE_COMPLETE_WEEK","NAVIGATE_COMPLETE_DAY","FOCUS_DATE","BLUR","NAVIGATE_DATE"]);
      const keyboardActions = ["NAVIGATE_DAY_BACK","NAVIGATE_DAY_FORWARD","NAVIGATE_WEEK_BACK","NAVIGATE_WEEK_FORWARD","SELECT_DATE","SELECT_DATE","NAVIGATE_WEEK_START","NAVIGATE_WEEK_END","NAVIGATE_PREV","NAVIGATE_NEXT","FOCUS_OUT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "grid"', () => {
      expect('grid').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "dayCell"', () => {
      expect('dayCell').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "application" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "calendar display" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? ariaLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on grid', () => {
      const ariaAttr = { part: 'grid', name: 'role', value: "grid" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on grid', () => {
      const ariaAttr = { part: 'grid', name: 'aria-label', value: "self . periodLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-busy on grid', () => {
      const ariaAttr = { part: 'grid', name: 'aria-busy', value: "if state = = navigating then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on weekRow', () => {
      const ariaAttr = { part: 'weekRow', name: 'role', value: "row" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on dayCell', () => {
      const ariaAttr = { part: 'dayCell', name: 'role', value: "gridcell" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on dayCell', () => {
      const ariaAttr = { part: 'dayCell', name: 'aria-label', value: "self . dateLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on dayCell', () => {
      const ariaAttr = { part: 'dayCell', name: 'aria-selected', value: "if self . isSelected then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on eventItem', () => {
      const ariaAttr = { part: 'eventItem', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on eventItem', () => {
      const ariaAttr = { part: 'eventItem', name: 'aria-label', value: "self . eventLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on prevButton', () => {
      const ariaAttr = { part: 'prevButton', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on prevButton', () => {
      const ariaAttr = { part: 'prevButton', name: 'aria-label', value: "if state = = monthView then Previous month else if state = = weekView then Previous week else Previous day" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on nextButton', () => {
      const ariaAttr = { part: 'nextButton', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on nextButton', () => {
      const ariaAttr = { part: 'nextButton', name: 'aria-label', value: "if state = = monthView then Next month else if state = = weekView then Next week else Next day" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on monthTab', () => {
      const ariaAttr = { part: 'monthTab', name: 'role', value: "tab" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on monthTab', () => {
      const ariaAttr = { part: 'monthTab', name: 'aria-selected', value: "if state = = monthView then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on monthTab', () => {
      const ariaAttr = { part: 'monthTab', name: 'aria-label', value: "Month view" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on weekTab', () => {
      const ariaAttr = { part: 'weekTab', name: 'role', value: "tab" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on weekTab', () => {
      const ariaAttr = { part: 'weekTab', name: 'aria-selected', value: "if state = = weekView then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on weekTab', () => {
      const ariaAttr = { part: 'weekTab', name: 'aria-label', value: "Week view" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on dayTab', () => {
      const ariaAttr = { part: 'dayTab', name: 'role', value: "tab" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on dayTab', () => {
      const ariaAttr = { part: 'dayTab', name: 'aria-selected', value: "if state = = dayView then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on dayTab', () => {
      const ariaAttr = { part: 'dayTab', name: 'aria-label', value: "Day view" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "application" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-roledescription', value: "calendar display" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? ariaLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part navigation', () => {
      const connectAria = { part: 'navigation', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part navigation', () => {
      const connectAria = { part: 'navigation', attr: 'aria-label', value: "Calendar navigation" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part prevButton', () => {
      const connectAria = { part: 'prevButton', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part prevButton', () => {
      const connectAria = { part: 'prevButton', attr: 'aria-label', value: "if ? view = = month then Previous month else if ? view = = week then Previous week else Previous day" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part nextButton', () => {
      const connectAria = { part: 'nextButton', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part nextButton', () => {
      const connectAria = { part: 'nextButton', attr: 'aria-label', value: "if ? view = = month then Next month else if ? view = = week then Next week else Next day" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part todayButton', () => {
      const connectAria = { part: 'todayButton', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part todayButton', () => {
      const connectAria = { part: 'todayButton', attr: 'aria-label', value: "Go to today" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part title', () => {
      const connectAria = { part: 'title', attr: 'role', value: "heading" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-live on connect part title', () => {
      const connectAria = { part: 'title', attr: 'aria-live', value: "polite" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-atomic on connect part title', () => {
      const connectAria = { part: 'title', attr: 'aria-atomic', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part viewSwitcher', () => {
      const connectAria = { part: 'viewSwitcher', attr: 'role', value: "tablist" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part viewSwitcher', () => {
      const connectAria = { part: 'viewSwitcher', attr: 'aria-label', value: "Calendar view options" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part monthTab', () => {
      const connectAria = { part: 'monthTab', attr: 'role', value: "tab" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part monthTab', () => {
      const connectAria = { part: 'monthTab', attr: 'aria-selected', value: "if state = = monthView then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part monthTab', () => {
      const connectAria = { part: 'monthTab', attr: 'aria-label', value: "Month view" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part weekTab', () => {
      const connectAria = { part: 'weekTab', attr: 'role', value: "tab" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part weekTab', () => {
      const connectAria = { part: 'weekTab', attr: 'aria-selected', value: "if state = = weekView then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part weekTab', () => {
      const connectAria = { part: 'weekTab', attr: 'aria-label', value: "Week view" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part dayTab', () => {
      const connectAria = { part: 'dayTab', attr: 'role', value: "tab" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part dayTab', () => {
      const connectAria = { part: 'dayTab', attr: 'aria-selected', value: "if state = = dayView then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part dayTab', () => {
      const connectAria = { part: 'dayTab', attr: 'aria-label', value: "Day view" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part grid', () => {
      const connectAria = { part: 'grid', attr: 'role', value: "grid" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part grid', () => {
      const connectAria = { part: 'grid', attr: 'aria-label', value: "self . periodLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-busy on connect part grid', () => {
      const connectAria = { part: 'grid', attr: 'aria-busy', value: "if state = = navigating then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part weekRow', () => {
      const connectAria = { part: 'weekRow', attr: 'role', value: "row" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part dayCell', () => {
      const connectAria = { part: 'dayCell', attr: 'role', value: "gridcell" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part dayCell', () => {
      const connectAria = { part: 'dayCell', attr: 'aria-label', value: "self . dateLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part dayCell', () => {
      const connectAria = { part: 'dayCell', attr: 'aria-selected', value: "if self . isSelected then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part emptyCell', () => {
      const connectAria = { part: 'emptyCell', attr: 'role', value: "gridcell" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part emptyCell', () => {
      const connectAria = { part: 'emptyCell', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part dayNumber', () => {
      const connectAria = { part: 'dayNumber', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part eventList', () => {
      const connectAria = { part: 'eventList', attr: 'role', value: "list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part eventList', () => {
      const connectAria = { part: 'eventList', attr: 'aria-label', value: "concat ( Events on  , self . dateLabel )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part eventItem', () => {
      const connectAria = { part: 'eventItem', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part eventItem', () => {
      const connectAria = { part: 'eventItem', attr: 'aria-label', value: "self . eventLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part overflowBadge', () => {
      const connectAria = { part: 'overflowBadge', attr: 'aria-label', value: "concat ( self . hiddenCount ,  more events )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "items" with type "list Object"', () => {
      const prop = { name: 'items', type: 'list Object' };
      expect(prop.name).toBe('items');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "dateField" with type "String"', () => {
      const prop = { name: 'dateField', type: 'String', defaultValue: "date" };
      expect(prop.name).toBe('dateField');
      expect(prop.type).toBeTruthy();
    });

    it('should default "dateField" to "date"', () => {
      expect("date").toBe("date");
    });

    it('should declare prop "labelField" with type "String"', () => {
      const prop = { name: 'labelField', type: 'String', defaultValue: "name" };
      expect(prop.name).toBe('labelField');
      expect(prop.type).toBeTruthy();
    });

    it('should default "labelField" to "name"', () => {
      expect("name").toBe("name");
    });

    it('should declare prop "view" with type "union month | week | day"', () => {
      const prop = { name: 'view', type: 'union month | week | day', defaultValue: "month" };
      expect(prop.name).toBe('view');
      expect(prop.type).toBeTruthy();
    });

    it('should default "view" to "month"', () => {
      expect("month").toBe("month");
    });

    it('should declare prop "onItemClick" with type "option Function"', () => {
      const prop = { name: 'onItemClick', type: 'option Function' };
      expect(prop.name).toBe('onItemClick');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "onDayClick" with type "option Function"', () => {
      const prop = { name: 'onDayClick', type: 'option Function' };
      expect(prop.name).toBe('onDayClick');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "ariaLabel" with type "String"', () => {
      const prop = { name: 'ariaLabel', type: 'String', defaultValue: "Calendar display" };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "ariaLabel" to "Calendar display"', () => {
      expect("Calendar display").toBe("Calendar display");
    });

    it('should declare prop "maxEventsPerCell" with type "Int"', () => {
      const prop = { name: 'maxEventsPerCell', type: 'Int', defaultValue: "3" };
      expect(prop.name).toBe('maxEventsPerCell');
      expect(prop.type).toBeTruthy();
    });

    it('should default "maxEventsPerCell" to "3"', () => {
      expect("3").toBe("3");
    });

    it('should declare prop "loading" with type "Bool"', () => {
      const prop = { name: 'loading', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('loading');
      expect(prop.type).toBeTruthy();
    });

    it('should default "loading" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "emptyMessage" with type "String"', () => {
      const prop = { name: 'emptyMessage', type: 'String', defaultValue: "No items" };
      expect(prop.name).toBe('emptyMessage');
      expect(prop.type).toBeTruthy();
    });

    it('should default "emptyMessage" to "No items"', () => {
      expect("No items").toBe("No items");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","header","navigation","prevButton","nextButton","todayButton","title","viewSwitcher","monthTab","weekTab","dayTab","grid","weekRow","dayCell","emptyCell","dayNumber","eventList","eventItem","overflowBadge"];
      expect(parts.length).toBe(19);
      expect(parts).toContain('root');
      expect(parts).toContain('header');
      expect(parts).toContain('navigation');
      expect(parts).toContain('prevButton');
      expect(parts).toContain('nextButton');
      expect(parts).toContain('todayButton');
      expect(parts).toContain('title');
      expect(parts).toContain('viewSwitcher');
      expect(parts).toContain('monthTab');
      expect(parts).toContain('weekTab');
      expect(parts).toContain('dayTab');
      expect(parts).toContain('grid');
      expect(parts).toContain('weekRow');
      expect(parts).toContain('dayCell');
      expect(parts).toContain('emptyCell');
      expect(parts).toContain('dayNumber');
      expect(parts).toContain('eventList');
      expect(parts).toContain('eventItem');
      expect(parts).toContain('overflowBadge');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // header: container
      expect('container').toBeTruthy(); // navigation: container
      expect('action').toBeTruthy(); // prevButton: action
      expect('action').toBeTruthy(); // nextButton: action
      expect('action').toBeTruthy(); // todayButton: action
      expect('text').toBeTruthy(); // title: text
      expect('container').toBeTruthy(); // viewSwitcher: container
      expect('action').toBeTruthy(); // monthTab: action
      expect('action').toBeTruthy(); // weekTab: action
      expect('action').toBeTruthy(); // dayTab: action
      expect('container').toBeTruthy(); // grid: container
      expect('container').toBeTruthy(); // weekRow: container
      expect('container').toBeTruthy(); // dayCell: container
      expect('container').toBeTruthy(); // emptyCell: container
      expect('text').toBeTruthy(); // dayNumber: text
      expect('container').toBeTruthy(); // eventList: container
      expect('action').toBeTruthy(); // eventItem: action
      expect('text').toBeTruthy(); // overflowBadge: text
    });

  });

});
