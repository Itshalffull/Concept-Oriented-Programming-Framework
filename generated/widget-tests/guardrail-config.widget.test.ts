// Auto-generated by generate-widget-tests.ts from guardrail-config.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: GuardrailConfig', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["viewing"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('viewing');
    });

    it('should transition from viewing to ruleSelected on SELECT_RULE', () => {
      const transition = { from: 'viewing', event: 'SELECT_RULE', target: 'ruleSelected' };
      expect(transition.from).toBe('viewing');
      expect(transition.event).toBe('SELECT_RULE');
      expect(transition.target).toBe('ruleSelected');
      // Target state 'ruleSelected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from viewing to testing on TEST', () => {
      const transition = { from: 'viewing', event: 'TEST', target: 'testing' };
      expect(transition.from).toBe('viewing');
      expect(transition.event).toBe('TEST');
      expect(transition.target).toBe('testing');
      // Target state 'testing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from viewing to adding on ADD_RULE', () => {
      const transition = { from: 'viewing', event: 'ADD_RULE', target: 'adding' };
      expect(transition.from).toBe('viewing');
      expect(transition.event).toBe('ADD_RULE');
      expect(transition.target).toBe('adding');
      // Target state 'adding' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from ruleSelected to viewing on DESELECT', () => {
      const transition = { from: 'ruleSelected', event: 'DESELECT', target: 'viewing' };
      expect(transition.from).toBe('ruleSelected');
      expect(transition.event).toBe('DESELECT');
      expect(transition.target).toBe('viewing');
      // Target state 'viewing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from testing to viewing on TEST_COMPLETE', () => {
      const transition = { from: 'testing', event: 'TEST_COMPLETE', target: 'viewing' };
      expect(transition.from).toBe('testing');
      expect(transition.event).toBe('TEST_COMPLETE');
      expect(transition.target).toBe('viewing');
      // Target state 'viewing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from adding to viewing on SAVE', () => {
      const transition = { from: 'adding', event: 'SAVE', target: 'viewing' };
      expect(transition.from).toBe('adding');
      expect(transition.event).toBe('SAVE');
      expect(transition.target).toBe('viewing');
      // Target state 'viewing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from adding to viewing on CANCEL', () => {
      const transition = { from: 'adding', event: 'CANCEL', target: 'viewing' };
      expect(transition.from).toBe('adding');
      expect(transition.event).toBe('CANCEL');
      expect(transition.target).toBe('viewing');
      // Target state 'viewing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"viewing","events":["SELECT_RULE","TEST","ADD_RULE"]},{"name":"ruleSelected","events":["DESELECT"]},{"name":"testing","events":["TEST_COMPLETE"]},{"name":"adding","events":["SAVE","CANCEL"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["viewing","ruleSelected","testing","adding"]);
      const baseNames = new Set(["viewing","ruleSelected","testing","adding"]);
      expect(validNames.has('ruleSelected') || baseNames.has('ruleSelected')).toBe(true);
      expect(validNames.has('testing') || baseNames.has('testing')).toBe(true);
      expect(validNames.has('adding') || baseNames.has('adding')).toBe(true);
      expect(validNames.has('viewing') || baseNames.has('viewing')).toBe(true);
      expect(validNames.has('viewing') || baseNames.has('viewing')).toBe(true);
      expect(validNames.has('viewing') || baseNames.has('viewing')).toBe(true);
      expect(validNames.has('viewing') || baseNames.has('viewing')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowDown key with action: FOCUS_NEXT', () => {
      const binding = { key: 'ArrowDown', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle ArrowUp key with action: FOCUS_PREV', () => {
      const binding = { key: 'ArrowUp', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should handle Enter key with action: SELECT_RULE', () => {
      const binding = { key: 'Enter', action: 'SELECT_RULE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('SELECT_RULE');
    });

    it('should handle t key with action: TEST', () => {
      const binding = { key: 't', action: 'TEST' };
      expect(binding.key).toBe('t');
      expect(binding.action).toBe('TEST');
    });

    it('should handle Escape key with action: DESELECT', () => {
      const binding = { key: 'Escape', action: 'DESELECT' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('DESELECT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SELECT_RULE","TEST","ADD_RULE","DESELECT","TEST_COMPLETE","SAVE","CANCEL"]);
      const keyboardActions = ["FOCUS_NEXT","FOCUS_PREV","SELECT_RULE","TEST","DESELECT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "form"', () => {
      expect('form').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "ruleList"', () => {
      expect('ruleList').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "form" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "concat ( Guardrail config:  , name )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on ruleList', () => {
      const ariaAttr = { part: 'ruleList', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on ruleList', () => {
      const ariaAttr = { part: 'ruleList', name: 'aria-label', value: "Guardrail rules" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on ruleItem', () => {
      const ariaAttr = { part: 'ruleItem', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on ruleItem', () => {
      const ariaAttr = { part: 'ruleItem', name: 'aria-label', value: "concat ( self . name ,  —  , self . severity )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on testPanel', () => {
      const ariaAttr = { part: 'testPanel', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on testPanel', () => {
      const ariaAttr = { part: 'testPanel', name: 'aria-label', value: "Rule tester" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "showHistory" with type "Bool"', () => {
      const prop = { name: 'showHistory', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showHistory');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showHistory" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showTest" with type "Bool"', () => {
      const prop = { name: 'showTest', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showTest');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showTest" to "true"', () => {
      expect("true").toBe("true");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","header","ruleList","ruleItem","ruleToggle","ruleName","ruleSeverity","ruleHistory","addButton","testPanel","testInput","testResult"];
      expect(parts.length).toBe(12);
      expect(parts).toContain('root');
      expect(parts).toContain('header');
      expect(parts).toContain('ruleList');
      expect(parts).toContain('ruleItem');
      expect(parts).toContain('ruleToggle');
      expect(parts).toContain('ruleName');
      expect(parts).toContain('ruleSeverity');
      expect(parts).toContain('ruleHistory');
      expect(parts).toContain('addButton');
      expect(parts).toContain('testPanel');
      expect(parts).toContain('testInput');
      expect(parts).toContain('testResult');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // header: container
      expect('container').toBeTruthy(); // ruleList: container
      expect('container').toBeTruthy(); // ruleItem: container
      expect('widget').toBeTruthy(); // ruleToggle: widget
      expect('text').toBeTruthy(); // ruleName: text
      expect('container').toBeTruthy(); // ruleSeverity: container
      expect('container').toBeTruthy(); // ruleHistory: container
      expect('action').toBeTruthy(); // addButton: action
      expect('container').toBeTruthy(); // testPanel: container
      expect('widget').toBeTruthy(); // testInput: widget
      expect('container').toBeTruthy(); // testResult: container
    });

  });

});
