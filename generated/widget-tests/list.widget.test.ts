// Auto-generated by generate-widget-tests.ts from list.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: List', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["item.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('item.idle');
    });

    it('should transition from item.idle to hovered on HOVER', () => {
      const transition = { from: 'item.idle', event: 'HOVER', target: 'hovered' };
      expect(transition.from).toBe('item.idle');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('hovered');
      // Target state 'hovered' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.idle to selected on SELECT', () => {
      const transition = { from: 'item.idle', event: 'SELECT', target: 'selected' };
      expect(transition.from).toBe('item.idle');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.idle to focused on FOCUS', () => {
      const transition = { from: 'item.idle', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('item.idle');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.hovered to idle on UNHOVER', () => {
      const transition = { from: 'item.hovered', event: 'UNHOVER', target: 'idle' };
      expect(transition.from).toBe('item.hovered');
      expect(transition.event).toBe('UNHOVER');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.hovered to selected on SELECT', () => {
      const transition = { from: 'item.hovered', event: 'SELECT', target: 'selected' };
      expect(transition.from).toBe('item.hovered');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.hovered to focused on FOCUS', () => {
      const transition = { from: 'item.hovered', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('item.hovered');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.focused to idle on BLUR', () => {
      const transition = { from: 'item.focused', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('item.focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.focused to selected on SELECT', () => {
      const transition = { from: 'item.focused', event: 'SELECT', target: 'selected' };
      expect(transition.from).toBe('item.focused');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.focused to focused on HOVER', () => {
      const transition = { from: 'item.focused', event: 'HOVER', target: 'focused' };
      expect(transition.from).toBe('item.focused');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.selected to idle on DESELECT', () => {
      const transition = { from: 'item.selected', event: 'DESELECT', target: 'idle' };
      expect(transition.from).toBe('item.selected');
      expect(transition.event).toBe('DESELECT');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.selected to selected on HOVER', () => {
      const transition = { from: 'item.selected', event: 'HOVER', target: 'selected' };
      expect(transition.from).toBe('item.selected');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from disabled to enabled on ENABLE', () => {
      const transition = { from: 'disabled', event: 'ENABLE', target: 'enabled' };
      expect(transition.from).toBe('disabled');
      expect(transition.event).toBe('ENABLE');
      expect(transition.target).toBe('enabled');
      // Target state 'enabled' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from enabled to disabled on DISABLE', () => {
      const transition = { from: 'enabled', event: 'DISABLE', target: 'disabled' };
      expect(transition.from).toBe('enabled');
      expect(transition.event).toBe('DISABLE');
      expect(transition.target).toBe('disabled');
      // Target state 'disabled' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"item.idle","events":["HOVER","SELECT","FOCUS"]},{"name":"item.hovered","events":["UNHOVER","SELECT","FOCUS"]},{"name":"item.focused","events":["BLUR","SELECT","HOVER"]},{"name":"item.selected","events":["DESELECT","HOVER"]},{"name":"disabled","events":["ENABLE"]},{"name":"enabled","events":["DISABLE"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["item.idle","item.hovered","item.focused","item.selected","disabled","enabled"]);
      const baseNames = new Set(["idle","hovered","focused","selected","disabled","enabled"]);
      expect(validNames.has('hovered') || baseNames.has('hovered')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('enabled') || baseNames.has('enabled')).toBe(true);
      expect(validNames.has('disabled') || baseNames.has('disabled')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-selectable on root', () => {
      const attr = { name: 'data-selectable', value: "if ? selectable then true else false" };
      expect(attr.name).toBe('data-selectable');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-dividers on root', () => {
      const attr = { name: 'data-dividers', value: "if ? dividers then true else false" };
      expect(attr.name).toBe('data-dividers');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on item', () => {
      const attr = { name: 'data-state', value: "if state . item = = selected then selected else if state . item = = hovered then hovered else if state . item = = focused then focused else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on item', () => {
      const handler = { event: 'onClick', action: "if ? selectable then send ( SELECT ) else noop" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on item', () => {
      const handler = { event: 'onKeyDown-Enter', action: "if ? selectable then send ( SELECT ) else noop" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on item', () => {
      const handler = { event: 'onKeyDown-Space', action: "if ? selectable then send ( SELECT ) else noop" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on item', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "send ( NAVIGATE_PREV )" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on item', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "send ( NAVIGATE_NEXT )" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Home handler on item', () => {
      const handler = { event: 'onKeyDown-Home', action: "send ( NAVIGATE_FIRST )" };
      expect(handler.event).toBe('onKeyDown-Home');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-End handler on item', () => {
      const handler = { event: 'onKeyDown-End', action: "send ( NAVIGATE_LAST )" };
      expect(handler.event).toBe('onKeyDown-End');
      expect(handler.action).toBeDefined();
    });

    it('should bind onMouseEnter handler on item', () => {
      const handler = { event: 'onMouseEnter', action: "send ( HOVER )" };
      expect(handler.event).toBe('onMouseEnter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onMouseLeave handler on item', () => {
      const handler = { event: 'onMouseLeave', action: "send ( UNHOVER )" };
      expect(handler.event).toBe('onMouseLeave');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on item', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on item', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="item-icon" to itemIcon', () => {
      const binding = {"part":"itemIcon","dataPart":"item-icon"};
      expect(binding.dataPart).toBe('item-icon');
    });

    it('should bind data-part="item-label" to itemLabel', () => {
      const binding = {"part":"itemLabel","dataPart":"item-label"};
      expect(binding.dataPart).toBe('item-label');
    });

    it('should bind data-part="item-description" to itemDescription', () => {
      const binding = {"part":"itemDescription","dataPart":"item-description"};
      expect(binding.dataPart).toBe('item-description');
    });

    it('should bind data-part="item-action" to itemAction', () => {
      const binding = {"part":"itemAction","dataPart":"item-action"};
      expect(binding.dataPart).toBe('item-action');
    });

    it('should bind onClick handler on itemAction', () => {
      const handler = { event: 'onClick', action: "send ( ITEM_ACTION )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="divider" to divider', () => {
      const binding = {"part":"divider","dataPart":"divider"};
      expect(binding.dataPart).toBe('divider');
    });

    it('should bind data-visible on divider', () => {
      const attr = { name: 'data-visible', value: "if ? dividers then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","item","itemIcon","itemLabel","itemDescription","itemAction","divider"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('item')).toBe(true);
      expect(anatomyParts.has('itemIcon')).toBe(true);
      expect(anatomyParts.has('itemLabel')).toBe(true);
      expect(anatomyParts.has('itemDescription')).toBe(true);
      expect(anatomyParts.has('itemAction')).toBe(true);
      expect(anatomyParts.has('divider')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowUp key with action: NAVIGATE_PREV', () => {
      const binding = { key: 'ArrowUp', action: 'NAVIGATE_PREV' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('NAVIGATE_PREV');
    });

    it('should handle ArrowDown key with action: NAVIGATE_NEXT', () => {
      const binding = { key: 'ArrowDown', action: 'NAVIGATE_NEXT' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('NAVIGATE_NEXT');
    });

    it('should handle Home key with action: NAVIGATE_FIRST', () => {
      const binding = { key: 'Home', action: 'NAVIGATE_FIRST' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('NAVIGATE_FIRST');
    });

    it('should handle End key with action: NAVIGATE_LAST', () => {
      const binding = { key: 'End', action: 'NAVIGATE_LAST' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('NAVIGATE_LAST');
    });

    it('should handle Enter key with action: SELECT', () => {
      const binding = { key: 'Enter', action: 'SELECT' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('SELECT');
    });

    it('should handle Space key with action: SELECT', () => {
      const binding = { key: 'Space', action: 'SELECT' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('SELECT');
    });

    it('should handle Tab key with action: FOCUS_OUT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_OUT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_OUT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["HOVER","SELECT","FOCUS","UNHOVER","BLUR","DESELECT","ENABLE","DISABLE"]);
      const keyboardActions = ["NAVIGATE_PREV","NAVIGATE_NEXT","NAVIGATE_FIRST","NAVIGATE_LAST","SELECT","SELECT","FOCUS_OUT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "list"', () => {
      expect('list').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "item"', () => {
      expect('item').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "if ? selectable then listbox else list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? ariaLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-multiselectable on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-multiselectable', value: "if ? multiSelect then true else undefined" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on item', () => {
      const ariaAttr = { part: 'item', name: 'role', value: "if ? selectable then option else listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-selected', value: "if ? selectable then ( if state . item = = selected then true else false ) else undefined" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-disabled on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-disabled', value: "if ? disabled then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "if ? selectable then listbox else list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? ariaLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-multiselectable on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-multiselectable', value: "if ? multiSelect then true else undefined" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part item', () => {
      const connectAria = { part: 'item', attr: 'role', value: "if ? selectable then option else listitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-selected', value: "if ? selectable then ( if state . item = = selected then true else false ) else undefined" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-disabled on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-disabled', value: "if ? disabled then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part itemIcon', () => {
      const connectAria = { part: 'itemIcon', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part itemAction', () => {
      const connectAria = { part: 'itemAction', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part itemAction', () => {
      const connectAria = { part: 'itemAction', attr: 'aria-label', value: "self . actionLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part divider', () => {
      const connectAria = { part: 'divider', attr: 'role', value: "separator" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part divider', () => {
      const connectAria = { part: 'divider', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "items" with type "list ListItem"', () => {
      const prop = { name: 'items', type: 'list ListItem' };
      expect(prop.name).toBe('items');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "selectable" with type "Bool"', () => {
      const prop = { name: 'selectable', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('selectable');
      expect(prop.type).toBeTruthy();
    });

    it('should default "selectable" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "multiSelect" with type "Bool"', () => {
      const prop = { name: 'multiSelect', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('multiSelect');
      expect(prop.type).toBeTruthy();
    });

    it('should default "multiSelect" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "dividers" with type "Bool"', () => {
      const prop = { name: 'dividers', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('dividers');
      expect(prop.type).toBeTruthy();
    });

    it('should default "dividers" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "ariaLabel" with type "option String"', () => {
      const prop = { name: 'ariaLabel', type: 'option String' };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","item","itemIcon","itemLabel","itemDescription","itemAction","divider"];
      expect(parts.length).toBe(7);
      expect(parts).toContain('root');
      expect(parts).toContain('item');
      expect(parts).toContain('itemIcon');
      expect(parts).toContain('itemLabel');
      expect(parts).toContain('itemDescription');
      expect(parts).toContain('itemAction');
      expect(parts).toContain('divider');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // item: container
      expect('container').toBeTruthy(); // itemIcon: container
      expect('text').toBeTruthy(); // itemLabel: text
      expect('text').toBeTruthy(); // itemDescription: text
      expect('action').toBeTruthy(); // itemAction: action
      expect('container').toBeTruthy(); // divider: container
    });

  });

});
