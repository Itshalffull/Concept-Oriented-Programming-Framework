// Auto-generated by generate-widget-tests.ts from code-block.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: CodeBlock', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('idle');
    });

    it('should transition from idle to copied on COPY', () => {
      const transition = { from: 'idle', event: 'COPY', target: 'copied' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('COPY');
      expect(transition.target).toBe('copied');
      // Target state 'copied' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to hovered on HOVER', () => {
      const transition = { from: 'idle', event: 'HOVER', target: 'hovered' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('hovered');
      // Target state 'hovered' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to focused on FOCUS', () => {
      const transition = { from: 'idle', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to idle on UNHOVER', () => {
      const transition = { from: 'hovered', event: 'UNHOVER', target: 'idle' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('UNHOVER');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to copied on COPY', () => {
      const transition = { from: 'hovered', event: 'COPY', target: 'copied' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('COPY');
      expect(transition.target).toBe('copied');
      // Target state 'copied' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to idle on BLUR', () => {
      const transition = { from: 'focused', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to copied on COPY', () => {
      const transition = { from: 'focused', event: 'COPY', target: 'copied' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('COPY');
      expect(transition.target).toBe('copied');
      // Target state 'copied' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from copied to idle on RESET', () => {
      const transition = { from: 'copied', event: 'RESET', target: 'idle' };
      expect(transition.from).toBe('copied');
      expect(transition.event).toBe('RESET');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from copied to idle on COPY_TIMEOUT', () => {
      const transition = { from: 'copied', event: 'COPY_TIMEOUT', target: 'idle' };
      expect(transition.from).toBe('copied');
      expect(transition.event).toBe('COPY_TIMEOUT');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"idle","events":["COPY","HOVER","FOCUS"]},{"name":"hovered","events":["UNHOVER","COPY"]},{"name":"focused","events":["BLUR","COPY"]},{"name":"copied","events":["RESET","COPY_TIMEOUT"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["idle","hovered","focused","copied"]);
      const baseNames = new Set(["idle","hovered","focused","copied"]);
      expect(validNames.has('copied') || baseNames.has('copied')).toBe(true);
      expect(validNames.has('hovered') || baseNames.has('hovered')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('copied') || baseNames.has('copied')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('copied') || baseNames.has('copied')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="code-block" to root', () => {
      const binding = {"part":"root","dataPart":"code-block"};
      expect(binding.dataPart).toBe('code-block');
    });

    it('should bind data-language on root', () => {
      const attr = { name: 'data-language', value: "? language" };
      expect(attr.name).toBe('data-language');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "state" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-line-numbers on root', () => {
      const attr = { name: 'data-line-numbers', value: "if ? showLineNumbers then true else false" };
      expect(attr.name).toBe('data-line-numbers');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-wrap on root', () => {
      const attr = { name: 'data-wrap', value: "if ? wrapLines then true else false" };
      expect(attr.name).toBe('data-wrap');
      expect(attr.value).toBeDefined();
    });

    it('should bind onPointerEnter handler on root', () => {
      const handler = { event: 'onPointerEnter', action: "send ( HOVER )" };
      expect(handler.event).toBe('onPointerEnter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerLeave handler on root', () => {
      const handler = { event: 'onPointerLeave', action: "send ( UNHOVER )" };
      expect(handler.event).toBe('onPointerLeave');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on root', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on root', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="header" to header', () => {
      const binding = {"part":"header","dataPart":"header"};
      expect(binding.dataPart).toBe('header');
    });

    it('should bind data-visible on header', () => {
      const attr = { name: 'data-visible', value: "if ? showHeader then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="language" to language', () => {
      const binding = {"part":"language","dataPart":"language"};
      expect(binding.dataPart).toBe('language');
    });

    it('should bind data-part="copy-button" to copyButton', () => {
      const binding = {"part":"copyButton","dataPart":"copy-button"};
      expect(binding.dataPart).toBe('copy-button');
    });

    it('should bind data-state on copyButton', () => {
      const attr = { name: 'data-state', value: "if state = = copied then copied else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on copyButton', () => {
      const handler = { event: 'onClick', action: "send ( COPY )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on copyButton', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( COPY )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="line-numbers" to lineNumbers', () => {
      const binding = {"part":"lineNumbers","dataPart":"line-numbers"};
      expect(binding.dataPart).toBe('line-numbers');
    });

    it('should bind data-visible on lineNumbers', () => {
      const attr = { name: 'data-visible', value: "if ? showLineNumbers then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-count on lineNumbers', () => {
      const attr = { name: 'data-count', value: "self . lineCount" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="code" to code', () => {
      const binding = {"part":"code","dataPart":"code"};
      expect(binding.dataPart).toBe('code');
    });

    it('should bind data-language on code', () => {
      const attr = { name: 'data-language', value: "? language" };
      expect(attr.name).toBe('data-language');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-highlight-lines on code', () => {
      const attr = { name: 'data-highlight-lines', value: "? highlightLines" };
      expect(attr.name).toBe('data-highlight-lines');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-wrap on code', () => {
      const attr = { name: 'data-wrap', value: "if ? wrapLines then true else false" };
      expect(attr.name).toBe('data-wrap');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","header","language","copyButton","lineNumbers","code"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('header')).toBe(true);
      expect(anatomyParts.has('language')).toBe(true);
      expect(anatomyParts.has('copyButton')).toBe(true);
      expect(anatomyParts.has('lineNumbers')).toBe(true);
      expect(anatomyParts.has('code')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Control+c key with action: COPY', () => {
      const binding = { key: 'Control+c', action: 'COPY' };
      expect(binding.key).toBe('Control+c');
      expect(binding.action).toBe('COPY');
    });

    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["COPY","HOVER","FOCUS","UNHOVER","BLUR","RESET","COPY_TIMEOUT"]);
      const keyboardActions = ["COPY","FOCUS_NEXT","FOCUS_PREV"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "region"', () => {
      expect('region').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "code"', () => {
      expect('code').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "concat ( ? language ,  code block )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "code block" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on header', () => {
      const ariaAttr = { part: 'header', name: 'role', value: "presentation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on language', () => {
      const ariaAttr = { part: 'language', name: 'role', value: "status" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on language', () => {
      const ariaAttr = { part: 'language', name: 'aria-label', value: "concat ( Language:  , ? language )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on copyButton', () => {
      const ariaAttr = { part: 'copyButton', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on copyButton', () => {
      const ariaAttr = { part: 'copyButton', name: 'aria-label', value: "if state = = copied then Copied to clipboard else Copy code to clipboard" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on copyButton', () => {
      const ariaAttr = { part: 'copyButton', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on lineNumbers', () => {
      const ariaAttr = { part: 'lineNumbers', name: 'role', value: "presentation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on lineNumbers', () => {
      const ariaAttr = { part: 'lineNumbers', name: 'aria-hidden', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on code', () => {
      const ariaAttr = { part: 'code', name: 'role', value: "code" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on code', () => {
      const ariaAttr = { part: 'code', name: 'aria-label', value: "concat ( ? language ,  source code )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-readonly on code', () => {
      const ariaAttr = { part: 'code', name: 'aria-readonly', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "region" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "if ? ariaLabel then ? ariaLabel else concat ( ? language ,  code block )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-roledescription', value: "code block" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part language', () => {
      const connectAria = { part: 'language', attr: 'aria-label', value: "concat ( Language:  , ? language )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part copyButton', () => {
      const connectAria = { part: 'copyButton', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part copyButton', () => {
      const connectAria = { part: 'copyButton', attr: 'aria-label', value: "if state = = copied then Copied to clipboard else Copy code to clipboard" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-live on connect part copyButton', () => {
      const connectAria = { part: 'copyButton', attr: 'aria-live', value: "polite" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part lineNumbers', () => {
      const connectAria = { part: 'lineNumbers', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part code', () => {
      const connectAria = { part: 'code', attr: 'role', value: "code" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part code', () => {
      const connectAria = { part: 'code', attr: 'aria-label', value: "concat ( ? language ,  source code )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-readonly on connect part code', () => {
      const connectAria = { part: 'code', attr: 'aria-readonly', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "code" with type "String"', () => {
      const prop = { name: 'code', type: 'String' };
      expect(prop.name).toBe('code');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "language" with type "String"', () => {
      const prop = { name: 'language', type: 'String', defaultValue: "plaintext" };
      expect(prop.name).toBe('language');
      expect(prop.type).toBeTruthy();
    });

    it('should default "language" to "plaintext"', () => {
      expect("plaintext").toBe("plaintext");
    });

    it('should declare prop "showLineNumbers" with type "Bool"', () => {
      const prop = { name: 'showLineNumbers', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showLineNumbers');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showLineNumbers" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "highlightLines" with type "option list Int"', () => {
      const prop = { name: 'highlightLines', type: 'option list Int' };
      expect(prop.name).toBe('highlightLines');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "showHeader" with type "Bool"', () => {
      const prop = { name: 'showHeader', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showHeader');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showHeader" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "wrapLines" with type "Bool"', () => {
      const prop = { name: 'wrapLines', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('wrapLines');
      expect(prop.type).toBeTruthy();
    });

    it('should default "wrapLines" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "maxHeight" with type "option String"', () => {
      const prop = { name: 'maxHeight', type: 'option String' };
      expect(prop.name).toBe('maxHeight');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "ariaLabel" with type "option String"', () => {
      const prop = { name: 'ariaLabel', type: 'option String' };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","header","language","copyButton","lineNumbers","code"];
      expect(parts.length).toBe(6);
      expect(parts).toContain('root');
      expect(parts).toContain('header');
      expect(parts).toContain('language');
      expect(parts).toContain('copyButton');
      expect(parts).toContain('lineNumbers');
      expect(parts).toContain('code');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // header: container
      expect('text').toBeTruthy(); // language: text
      expect('action').toBeTruthy(); // copyButton: action
      expect('container').toBeTruthy(); // lineNumbers: container
      expect('container').toBeTruthy(); // code: container
    });

  });

});
