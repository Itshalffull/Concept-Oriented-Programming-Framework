// Auto-generated by generate-widget-tests.ts from badge.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: Badge', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["display.static"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('display.static');
    });

    it('should transition from display.static to dot on SET_DOT', () => {
      const transition = { from: 'display.static', event: 'SET_DOT', target: 'dot' };
      expect(transition.from).toBe('display.static');
      expect(transition.event).toBe('SET_DOT');
      expect(transition.target).toBe('dot');
      // Target state 'dot' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from display.dot to static on SET_LABEL', () => {
      const transition = { from: 'display.dot', event: 'SET_LABEL', target: 'static' };
      expect(transition.from).toBe('display.dot');
      expect(transition.event).toBe('SET_LABEL');
      expect(transition.target).toBe('static');
      // Target state 'static' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"display.static","events":["SET_DOT"]},{"name":"display.dot","events":["SET_LABEL"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["display.static","display.dot"]);
      const baseNames = new Set(["static","dot"]);
      expect(validNames.has('dot') || baseNames.has('dot')).toBe(true);
      expect(validNames.has('static') || baseNames.has('static')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-variant on root', () => {
      const attr = { name: 'data-variant', value: "? variant" };
      expect(attr.name).toBe('data-variant');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-color on root', () => {
      const attr = { name: 'data-color', value: "? color" };
      expect(attr.name).toBe('data-color');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if ? variant = = dot then dot else static" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","label"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('label')).toBe(true);
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "status"', () => {
      expect('status').toBeTruthy();
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "status" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "if ? label then ? label else ( if ? variant = = dot then Status indicator else Badge )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on label', () => {
      const ariaAttr = { part: 'label', name: 'aria-hidden', value: "if ? variant = = dot then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "status" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "label" with type "option String"', () => {
      const prop = { name: 'label', type: 'option String' };
      expect(prop.name).toBe('label');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "variant" with type "filled | outline | dot"', () => {
      const prop = { name: 'variant', type: 'filled | outline | dot', defaultValue: "filled" };
      expect(prop.name).toBe('variant');
      expect(prop.type).toBeTruthy();
    });

    it('should default "variant" to "filled"', () => {
      expect("filled").toBe("filled");
    });

    it('should declare prop "color" with type "option String"', () => {
      const prop = { name: 'color', type: 'option String' };
      expect(prop.name).toBe('color');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "max" with type "option Int"', () => {
      const prop = { name: 'max', type: 'option Int' };
      expect(prop.name).toBe('max');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","label"];
      expect(parts.length).toBe(2);
      expect(parts).toContain('root');
      expect(parts).toContain('label');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('text').toBeTruthy(); // label: text
    });

  });

});
