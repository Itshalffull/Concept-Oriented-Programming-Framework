// Auto-generated by generate-widget-tests.ts from pagination.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: Pagination', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["position.atFirst","focus.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('position.atFirst');
      expect(initialStates).toContain('focus.idle');
    });

    it('should transition from position.atFirst to middle on NAVIGATE_NEXT', () => {
      const transition = { from: 'position.atFirst', event: 'NAVIGATE_NEXT', target: 'middle' };
      expect(transition.from).toBe('position.atFirst');
      expect(transition.event).toBe('NAVIGATE_NEXT');
      expect(transition.target).toBe('middle');
      // Target state 'middle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from position.atFirst to middle on NAVIGATE_TO', () => {
      const transition = { from: 'position.atFirst', event: 'NAVIGATE_TO', target: 'middle' };
      expect(transition.from).toBe('position.atFirst');
      expect(transition.event).toBe('NAVIGATE_TO');
      expect(transition.target).toBe('middle');
      // Target state 'middle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from position.middle to middle on NAVIGATE_NEXT', () => {
      const transition = { from: 'position.middle', event: 'NAVIGATE_NEXT', target: 'middle' };
      expect(transition.from).toBe('position.middle');
      expect(transition.event).toBe('NAVIGATE_NEXT');
      expect(transition.target).toBe('middle');
      // Target state 'middle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from position.middle to middle on NAVIGATE_PREV', () => {
      const transition = { from: 'position.middle', event: 'NAVIGATE_PREV', target: 'middle' };
      expect(transition.from).toBe('position.middle');
      expect(transition.event).toBe('NAVIGATE_PREV');
      expect(transition.target).toBe('middle');
      // Target state 'middle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from position.middle to middle on NAVIGATE_TO', () => {
      const transition = { from: 'position.middle', event: 'NAVIGATE_TO', target: 'middle' };
      expect(transition.from).toBe('position.middle');
      expect(transition.event).toBe('NAVIGATE_TO');
      expect(transition.target).toBe('middle');
      // Target state 'middle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from position.middle to atFirst on REACH_FIRST', () => {
      const transition = { from: 'position.middle', event: 'REACH_FIRST', target: 'atFirst' };
      expect(transition.from).toBe('position.middle');
      expect(transition.event).toBe('REACH_FIRST');
      expect(transition.target).toBe('atFirst');
      // Target state 'atFirst' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from position.middle to atLast on REACH_LAST', () => {
      const transition = { from: 'position.middle', event: 'REACH_LAST', target: 'atLast' };
      expect(transition.from).toBe('position.middle');
      expect(transition.event).toBe('REACH_LAST');
      expect(transition.target).toBe('atLast');
      // Target state 'atLast' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from position.atLast to middle on NAVIGATE_PREV', () => {
      const transition = { from: 'position.atLast', event: 'NAVIGATE_PREV', target: 'middle' };
      expect(transition.from).toBe('position.atLast');
      expect(transition.event).toBe('NAVIGATE_PREV');
      expect(transition.target).toBe('middle');
      // Target state 'middle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from position.atLast to middle on NAVIGATE_TO', () => {
      const transition = { from: 'position.atLast', event: 'NAVIGATE_TO', target: 'middle' };
      expect(transition.from).toBe('position.atLast');
      expect(transition.event).toBe('NAVIGATE_TO');
      expect(transition.target).toBe('middle');
      // Target state 'middle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.idle to focused on FOCUS', () => {
      const transition = { from: 'focus.idle', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('focus.idle');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.focused to idle on BLUR', () => {
      const transition = { from: 'focus.focused', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('focus.focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"position.atFirst","events":["NAVIGATE_NEXT","NAVIGATE_TO"]},{"name":"position.middle","events":["NAVIGATE_NEXT","NAVIGATE_PREV","NAVIGATE_TO","REACH_FIRST","REACH_LAST"]},{"name":"position.atLast","events":["NAVIGATE_PREV","NAVIGATE_TO"]},{"name":"focus.idle","events":["FOCUS"]},{"name":"focus.focused","events":["BLUR"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["position.atFirst","position.middle","position.atLast","focus.idle","focus.focused"]);
      const baseNames = new Set(["atFirst","middle","atLast","idle","focused"]);
      expect(validNames.has('middle') || baseNames.has('middle')).toBe(true);
      expect(validNames.has('middle') || baseNames.has('middle')).toBe(true);
      expect(validNames.has('middle') || baseNames.has('middle')).toBe(true);
      expect(validNames.has('middle') || baseNames.has('middle')).toBe(true);
      expect(validNames.has('middle') || baseNames.has('middle')).toBe(true);
      expect(validNames.has('atFirst') || baseNames.has('atFirst')).toBe(true);
      expect(validNames.has('atLast') || baseNames.has('atLast')).toBe(true);
      expect(validNames.has('middle') || baseNames.has('middle')).toBe(true);
      expect(validNames.has('middle') || baseNames.has('middle')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-disabled on root', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-disabled on prevButton', () => {
      const attr = { name: 'data-disabled', value: "if ? page <= 1 then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on prevButton', () => {
      const handler = { event: 'onClick', action: "send ( NAVIGATE_PREV )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-disabled on nextButton', () => {
      const attr = { name: 'data-disabled', value: "if ? page >= ? totalPages then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on nextButton', () => {
      const handler = { event: 'onClick', action: "send ( NAVIGATE_NEXT )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="items" to items', () => {
      const binding = {"part":"items","dataPart":"items"};
      expect(binding.dataPart).toBe('items');
    });

    it('should bind data-selected on item', () => {
      const attr = { name: 'data-selected', value: "if item . page = = ? page then true else false" };
      expect(attr.name).toBe('data-selected');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on item', () => {
      const handler = { event: 'onClick', action: "send ( NAVIGATE_TO )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="ellipsis" to ellipsis', () => {
      const binding = {"part":"ellipsis","dataPart":"ellipsis"};
      expect(binding.dataPart).toBe('ellipsis');
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","prevButton","nextButton","items","item","ellipsis"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('prevButton')).toBe(true);
      expect(anatomyParts.has('nextButton')).toBe(true);
      expect(anatomyParts.has('items')).toBe(true);
      expect(anatomyParts.has('item')).toBe(true);
      expect(anatomyParts.has('ellipsis')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Enter key with action: NAVIGATE_TO', () => {
      const binding = { key: 'Enter', action: 'NAVIGATE_TO' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('NAVIGATE_TO');
    });

    it('should handle Space key with action: NAVIGATE_TO', () => {
      const binding = { key: 'Space', action: 'NAVIGATE_TO' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('NAVIGATE_TO');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["NAVIGATE_NEXT","NAVIGATE_TO","NAVIGATE_PREV","REACH_FIRST","REACH_LAST","FOCUS","BLUR"]);
      const keyboardActions = ["FOCUS_NEXT","NAVIGATE_TO","NAVIGATE_TO"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "navigation"', () => {
      expect('navigation').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "item"', () => {
      expect('item').toBeTruthy();
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "navigation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Pagination" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on prevButton', () => {
      const ariaAttr = { part: 'prevButton', name: 'aria-label', value: "Previous page" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-disabled on prevButton', () => {
      const ariaAttr = { part: 'prevButton', name: 'aria-disabled', value: "if state . position = = atFirst then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on nextButton', () => {
      const ariaAttr = { part: 'nextButton', name: 'aria-label', value: "Next page" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-disabled on nextButton', () => {
      const ariaAttr = { part: 'nextButton', name: 'aria-disabled', value: "if state . position = = atLast then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-label', value: "concat ( Page  , item . page )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-current on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-current', value: "if item . page = = ? page then page else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "navigation" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "Pagination" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part prevButton', () => {
      const connectAria = { part: 'prevButton', attr: 'aria-label', value: "Previous page" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-disabled on connect part prevButton', () => {
      const connectAria = { part: 'prevButton', attr: 'aria-disabled', value: "if ? page <= 1 then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part nextButton', () => {
      const connectAria = { part: 'nextButton', attr: 'aria-label', value: "Next page" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-disabled on connect part nextButton', () => {
      const connectAria = { part: 'nextButton', attr: 'aria-disabled', value: "if ? page >= ? totalPages then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-label', value: "concat ( Page  , item . page )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-current on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-current', value: "if item . page = = ? page then page else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part ellipsis', () => {
      const connectAria = { part: 'ellipsis', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "page" with type "Int"', () => {
      const prop = { name: 'page', type: 'Int', defaultValue: "1" };
      expect(prop.name).toBe('page');
      expect(prop.type).toBeTruthy();
    });

    it('should default "page" to "1"', () => {
      expect("1").toBe("1");
    });

    it('should declare prop "totalPages" with type "Int"', () => {
      const prop = { name: 'totalPages', type: 'Int' };
      expect(prop.name).toBe('totalPages');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "siblingCount" with type "Int"', () => {
      const prop = { name: 'siblingCount', type: 'Int', defaultValue: "1" };
      expect(prop.name).toBe('siblingCount');
      expect(prop.type).toBeTruthy();
    });

    it('should default "siblingCount" to "1"', () => {
      expect("1").toBe("1");
    });

    it('should declare prop "boundaryCount" with type "Int"', () => {
      const prop = { name: 'boundaryCount', type: 'Int', defaultValue: "1" };
      expect(prop.name).toBe('boundaryCount');
      expect(prop.type).toBeTruthy();
    });

    it('should default "boundaryCount" to "1"', () => {
      expect("1").toBe("1");
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","prevButton","nextButton","items","item","ellipsis"];
      expect(parts.length).toBe(6);
      expect(parts).toContain('root');
      expect(parts).toContain('prevButton');
      expect(parts).toContain('nextButton');
      expect(parts).toContain('items');
      expect(parts).toContain('item');
      expect(parts).toContain('ellipsis');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('action').toBeTruthy(); // prevButton: action
      expect('action').toBeTruthy(); // nextButton: action
      expect('container').toBeTruthy(); // items: container
      expect('action').toBeTruthy(); // item: action
      expect('text').toBeTruthy(); // ellipsis: text
    });

  });

});
