// Auto-generated by generate-widget-tests.ts from slider.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: Slider', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["interaction.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('interaction.idle');
    });

    it('should transition from interaction.idle to dragging on POINTER_DOWN', () => {
      const transition = { from: 'interaction.idle', event: 'POINTER_DOWN', target: 'dragging' };
      expect(transition.from).toBe('interaction.idle');
      expect(transition.event).toBe('POINTER_DOWN');
      expect(transition.target).toBe('dragging');
      // Target state 'dragging' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.idle to focused on FOCUS', () => {
      const transition = { from: 'interaction.idle', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('interaction.idle');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.focused to idle on BLUR', () => {
      const transition = { from: 'interaction.focused', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('interaction.focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.focused to dragging on POINTER_DOWN', () => {
      const transition = { from: 'interaction.focused', event: 'POINTER_DOWN', target: 'dragging' };
      expect(transition.from).toBe('interaction.focused');
      expect(transition.event).toBe('POINTER_DOWN');
      expect(transition.target).toBe('dragging');
      // Target state 'dragging' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.dragging to idle on POINTER_UP', () => {
      const transition = { from: 'interaction.dragging', event: 'POINTER_UP', target: 'idle' };
      expect(transition.from).toBe('interaction.dragging');
      expect(transition.event).toBe('POINTER_UP');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"interaction.idle","events":["POINTER_DOWN","FOCUS"]},{"name":"interaction.focused","events":["BLUR","POINTER_DOWN"]},{"name":"interaction.dragging","events":["POINTER_UP"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["interaction.idle","interaction.focused","interaction.dragging"]);
      const baseNames = new Set(["idle","focused","dragging"]);
      expect(validNames.has('dragging') || baseNames.has('dragging')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('dragging') || baseNames.has('dragging')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-orientation on root', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if state . interaction = = dragging then dragging else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-disabled on root', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-orientation on track', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on track', () => {
      const handler = { event: 'onClick', action: "send ( TRACK_CLICK )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-orientation on range', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on thumb', () => {
      const attr = { name: 'data-state', value: "if state . interaction = = dragging then dragging else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onPointerDown handler on thumb', () => {
      const handler = { event: 'onPointerDown', action: "send ( POINTER_DOWN )" };
      expect(handler.event).toBe('onPointerDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerUp handler on thumb', () => {
      const handler = { event: 'onPointerUp', action: "send ( POINTER_UP )" };
      expect(handler.event).toBe('onPointerUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on thumb', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on thumb', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowRight handler on thumb', () => {
      const handler = { event: 'onKeyDown-ArrowRight', action: "send ( INCREASE )" };
      expect(handler.event).toBe('onKeyDown-ArrowRight');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on thumb', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "send ( INCREASE )" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowLeft handler on thumb', () => {
      const handler = { event: 'onKeyDown-ArrowLeft', action: "send ( DECREASE )" };
      expect(handler.event).toBe('onKeyDown-ArrowLeft');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on thumb', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "send ( DECREASE )" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Home handler on thumb', () => {
      const handler = { event: 'onKeyDown-Home', action: "send ( SET_MIN )" };
      expect(handler.event).toBe('onKeyDown-Home');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-End handler on thumb', () => {
      const handler = { event: 'onKeyDown-End', action: "send ( SET_MAX )" };
      expect(handler.event).toBe('onKeyDown-End');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-PageUp handler on thumb', () => {
      const handler = { event: 'onKeyDown-PageUp', action: "send ( INCREASE_LARGE )" };
      expect(handler.event).toBe('onKeyDown-PageUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-PageDown handler on thumb', () => {
      const handler = { event: 'onKeyDown-PageDown', action: "send ( DECREASE_LARGE )" };
      expect(handler.event).toBe('onKeyDown-PageDown');
      expect(handler.action).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","label","track","range","thumb","output"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('label')).toBe(true);
      expect(anatomyParts.has('track')).toBe(true);
      expect(anatomyParts.has('range')).toBe(true);
      expect(anatomyParts.has('thumb')).toBe(true);
      expect(anatomyParts.has('output')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowRight key with action: INCREASE', () => {
      const binding = { key: 'ArrowRight', action: 'INCREASE' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('INCREASE');
    });

    it('should handle ArrowUp key with action: INCREASE', () => {
      const binding = { key: 'ArrowUp', action: 'INCREASE' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('INCREASE');
    });

    it('should handle ArrowLeft key with action: DECREASE', () => {
      const binding = { key: 'ArrowLeft', action: 'DECREASE' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('DECREASE');
    });

    it('should handle ArrowDown key with action: DECREASE', () => {
      const binding = { key: 'ArrowDown', action: 'DECREASE' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('DECREASE');
    });

    it('should handle Home key with action: SET_MIN', () => {
      const binding = { key: 'Home', action: 'SET_MIN' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('SET_MIN');
    });

    it('should handle End key with action: SET_MAX', () => {
      const binding = { key: 'End', action: 'SET_MAX' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('SET_MAX');
    });

    it('should handle PageUp key with action: INCREASE_LARGE', () => {
      const binding = { key: 'PageUp', action: 'INCREASE_LARGE' };
      expect(binding.key).toBe('PageUp');
      expect(binding.action).toBe('INCREASE_LARGE');
    });

    it('should handle PageDown key with action: DECREASE_LARGE', () => {
      const binding = { key: 'PageDown', action: 'DECREASE_LARGE' };
      expect(binding.key).toBe('PageDown');
      expect(binding.action).toBe('DECREASE_LARGE');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["POINTER_DOWN","FOCUS","BLUR","POINTER_UP"]);
      const keyboardActions = ["INCREASE","INCREASE","DECREASE","DECREASE","SET_MIN","SET_MAX","INCREASE_LARGE","DECREASE_LARGE"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "slider"', () => {
      expect('slider').toBeTruthy();
    });

    it('should bind role on thumb', () => {
      const ariaAttr = { part: 'thumb', name: 'role', value: "slider" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on thumb', () => {
      const ariaAttr = { part: 'thumb', name: 'aria-label', value: "? label" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-valuenow on thumb', () => {
      const ariaAttr = { part: 'thumb', name: 'aria-valuenow', value: "? value" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-valuemin on thumb', () => {
      const ariaAttr = { part: 'thumb', name: 'aria-valuemin', value: "? min" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-valuemax on thumb', () => {
      const ariaAttr = { part: 'thumb', name: 'aria-valuemax', value: "? max" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-valuetext on thumb', () => {
      const ariaAttr = { part: 'thumb', name: 'aria-valuetext', value: "concat ( ? value )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-orientation on thumb', () => {
      const ariaAttr = { part: 'thumb', name: 'aria-orientation', value: "? orientation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-disabled on thumb', () => {
      const ariaAttr = { part: 'thumb', name: 'aria-disabled', value: "if ? disabled then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on output', () => {
      const ariaAttr = { part: 'output', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind for on output', () => {
      const ariaAttr = { part: 'output', name: 'for', value: "thumb" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "value" with type "Float"', () => {
      const prop = { name: 'value', type: 'Float' };
      expect(prop.name).toBe('value');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "min" with type "Float"', () => {
      const prop = { name: 'min', type: 'Float', defaultValue: "0" };
      expect(prop.name).toBe('min');
      expect(prop.type).toBeTruthy();
    });

    it('should default "min" to "0"', () => {
      expect("0").toBe("0");
    });

    it('should declare prop "max" with type "Float"', () => {
      const prop = { name: 'max', type: 'Float', defaultValue: "100" };
      expect(prop.name).toBe('max');
      expect(prop.type).toBeTruthy();
    });

    it('should default "max" to "100"', () => {
      expect("100").toBe("100");
    });

    it('should declare prop "step" with type "Float"', () => {
      const prop = { name: 'step', type: 'Float', defaultValue: "1" };
      expect(prop.name).toBe('step');
      expect(prop.type).toBeTruthy();
    });

    it('should default "step" to "1"', () => {
      expect("1").toBe("1");
    });

    it('should declare prop "orientation" with type "horizontal | vertical"', () => {
      const prop = { name: 'orientation', type: 'horizontal | vertical', defaultValue: "horizontal" };
      expect(prop.name).toBe('orientation');
      expect(prop.type).toBeTruthy();
    });

    it('should default "orientation" to "horizontal"', () => {
      expect("horizontal").toBe("horizontal");
    });

    it('should declare prop "label" with type "String"', () => {
      const prop = { name: 'label', type: 'String' };
      expect(prop.name).toBe('label');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "name" with type "option String"', () => {
      const prop = { name: 'name', type: 'option String' };
      expect(prop.name).toBe('name');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","label","track","range","thumb","output"];
      expect(parts.length).toBe(6);
      expect(parts).toContain('root');
      expect(parts).toContain('label');
      expect(parts).toContain('track');
      expect(parts).toContain('range');
      expect(parts).toContain('thumb');
      expect(parts).toContain('output');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('text').toBeTruthy(); // label: text
      expect('container').toBeTruthy(); // track: container
      expect('container').toBeTruthy(); // range: container
      expect('action').toBeTruthy(); // thumb: action
      expect('text').toBeTruthy(); // output: text
    });

  });

});
