// Auto-generated by generate-widget-tests.ts from navigation-menu.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: NavigationMenu', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["item.closed","mobile.collapsed","focus.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('item.closed');
      expect(initialStates).toContain('mobile.collapsed');
      expect(initialStates).toContain('focus.idle');
    });

    it('should transition from item.closed to open on OPEN', () => {
      const transition = { from: 'item.closed', event: 'OPEN', target: 'open' };
      expect(transition.from).toBe('item.closed');
      expect(transition.event).toBe('OPEN');
      expect(transition.target).toBe('open');
      // Target state 'open' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.closed to open on HOVER', () => {
      const transition = { from: 'item.closed', event: 'HOVER', target: 'open' };
      expect(transition.from).toBe('item.closed');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('open');
      // Target state 'open' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.open to closed on CLOSE', () => {
      const transition = { from: 'item.open', event: 'CLOSE', target: 'closed' };
      expect(transition.from).toBe('item.open');
      expect(transition.event).toBe('CLOSE');
      expect(transition.target).toBe('closed');
      // Target state 'closed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.open to closed on UNHOVER', () => {
      const transition = { from: 'item.open', event: 'UNHOVER', target: 'closed' };
      expect(transition.from).toBe('item.open');
      expect(transition.event).toBe('UNHOVER');
      expect(transition.target).toBe('closed');
      // Target state 'closed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.open to closed on SELECT', () => {
      const transition = { from: 'item.open', event: 'SELECT', target: 'closed' };
      expect(transition.from).toBe('item.open');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('closed');
      // Target state 'closed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from mobile.collapsed to expanded on TOGGLE_MOBILE', () => {
      const transition = { from: 'mobile.collapsed', event: 'TOGGLE_MOBILE', target: 'expanded' };
      expect(transition.from).toBe('mobile.collapsed');
      expect(transition.event).toBe('TOGGLE_MOBILE');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from mobile.expanded to collapsed on TOGGLE_MOBILE', () => {
      const transition = { from: 'mobile.expanded', event: 'TOGGLE_MOBILE', target: 'collapsed' };
      expect(transition.from).toBe('mobile.expanded');
      expect(transition.event).toBe('TOGGLE_MOBILE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from mobile.expanded to collapsed on NAVIGATE', () => {
      const transition = { from: 'mobile.expanded', event: 'NAVIGATE', target: 'collapsed' };
      expect(transition.from).toBe('mobile.expanded');
      expect(transition.event).toBe('NAVIGATE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.idle to focused on FOCUS', () => {
      const transition = { from: 'focus.idle', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('focus.idle');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.focused to idle on BLUR', () => {
      const transition = { from: 'focus.focused', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('focus.focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.focused to focused on NAVIGATE_NEXT', () => {
      const transition = { from: 'focus.focused', event: 'NAVIGATE_NEXT', target: 'focused' };
      expect(transition.from).toBe('focus.focused');
      expect(transition.event).toBe('NAVIGATE_NEXT');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.focused to focused on NAVIGATE_PREV', () => {
      const transition = { from: 'focus.focused', event: 'NAVIGATE_PREV', target: 'focused' };
      expect(transition.from).toBe('focus.focused');
      expect(transition.event).toBe('NAVIGATE_PREV');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"item.closed","events":["OPEN","HOVER"]},{"name":"item.open","events":["CLOSE","UNHOVER","SELECT"]},{"name":"mobile.collapsed","events":["TOGGLE_MOBILE"]},{"name":"mobile.expanded","events":["TOGGLE_MOBILE","NAVIGATE"]},{"name":"focus.idle","events":["FOCUS"]},{"name":"focus.focused","events":["BLUR","NAVIGATE_NEXT","NAVIGATE_PREV"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["item.closed","item.open","mobile.collapsed","mobile.expanded","focus.idle","focus.focused"]);
      const baseNames = new Set(["closed","open","collapsed","expanded","idle","focused"]);
      expect(validNames.has('open') || baseNames.has('open')).toBe(true);
      expect(validNames.has('open') || baseNames.has('open')).toBe(true);
      expect(validNames.has('closed') || baseNames.has('closed')).toBe(true);
      expect(validNames.has('closed') || baseNames.has('closed')).toBe(true);
      expect(validNames.has('closed') || baseNames.has('closed')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-orientation on root', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-orientation on list', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on item', () => {
      const attr = { name: 'data-state', value: "if state . item = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onPointerEnter handler on item', () => {
      const handler = { event: 'onPointerEnter', action: "send ( HOVER )" };
      expect(handler.event).toBe('onPointerEnter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerLeave handler on item', () => {
      const handler = { event: 'onPointerLeave', action: "send ( UNHOVER )" };
      expect(handler.event).toBe('onPointerLeave');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-state on trigger', () => {
      const attr = { name: 'data-state', value: "if state . item = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on trigger', () => {
      const handler = { event: 'onClick', action: "send ( if state . item = = closed then OPEN else CLOSE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown handler on trigger', () => {
      const handler = { event: 'onKeyDown', action: "send ( NAVIGATE )" };
      expect(handler.event).toBe('onKeyDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on trigger', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on trigger', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-active on link', () => {
      const attr = { name: 'data-active', value: "if link . active then true else false" };
      expect(attr.name).toBe('data-active');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on link', () => {
      const handler = { event: 'onClick', action: "send ( NAVIGATE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on link', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on link', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-state on content', () => {
      const attr = { name: 'data-state', value: "if state . item = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-orientation on content', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on indicator', () => {
      const attr = { name: 'data-state', value: "if state . item = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-orientation on indicator', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on viewport', () => {
      const attr = { name: 'data-state', value: "if state . item = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-orientation on viewport', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","list","item","trigger","link","content","indicator","viewport"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('list')).toBe(true);
      expect(anatomyParts.has('item')).toBe(true);
      expect(anatomyParts.has('trigger')).toBe(true);
      expect(anatomyParts.has('link')).toBe(true);
      expect(anatomyParts.has('content')).toBe(true);
      expect(anatomyParts.has('indicator')).toBe(true);
      expect(anatomyParts.has('viewport')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowRight key with action: NAVIGATE_NEXT', () => {
      const binding = { key: 'ArrowRight', action: 'NAVIGATE_NEXT' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('NAVIGATE_NEXT');
    });

    it('should handle ArrowLeft key with action: NAVIGATE_PREV', () => {
      const binding = { key: 'ArrowLeft', action: 'NAVIGATE_PREV' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('NAVIGATE_PREV');
    });

    it('should handle ArrowDown key with action: OPEN', () => {
      const binding = { key: 'ArrowDown', action: 'OPEN' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('OPEN');
    });

    it('should handle Enter key with action: ACTIVATE', () => {
      const binding = { key: 'Enter', action: 'ACTIVATE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('ACTIVATE');
    });

    it('should handle Space key with action: ACTIVATE', () => {
      const binding = { key: 'Space', action: 'ACTIVATE' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('ACTIVATE');
    });

    it('should handle Escape key with action: CLOSE', () => {
      const binding = { key: 'Escape', action: 'CLOSE' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('CLOSE');
    });

    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["OPEN","HOVER","CLOSE","UNHOVER","SELECT","TOGGLE_MOBILE","NAVIGATE","FOCUS","BLUR","NAVIGATE_NEXT","NAVIGATE_PREV"]);
      const keyboardActions = ["NAVIGATE_NEXT","NAVIGATE_PREV","OPEN","ACTIVATE","ACTIVATE","CLOSE","FOCUS_NEXT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "navigation"', () => {
      expect('navigation').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "first"', () => {
      expect('first').toBeTruthy();
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "navigation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Main navigation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on list', () => {
      const ariaAttr = { part: 'list', name: 'role', value: "menubar" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-orientation on list', () => {
      const ariaAttr = { part: 'list', name: 'aria-orientation', value: "? orientation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on trigger', () => {
      const ariaAttr = { part: 'trigger', name: 'role', value: "menuitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-haspopup on trigger', () => {
      const ariaAttr = { part: 'trigger', name: 'aria-haspopup', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on trigger', () => {
      const ariaAttr = { part: 'trigger', name: 'aria-expanded', value: "if state . item = = open then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-controls on trigger', () => {
      const ariaAttr = { part: 'trigger', name: 'aria-controls', value: "content" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on link', () => {
      const ariaAttr = { part: 'link', name: 'role', value: "menuitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-current on link', () => {
      const ariaAttr = { part: 'link', name: 'aria-current', value: "if link . active then page else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on content', () => {
      const ariaAttr = { part: 'content', name: 'role', value: "menu" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-labelledby on content', () => {
      const ariaAttr = { part: 'content', name: 'aria-labelledby', value: "trigger" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on indicator', () => {
      const ariaAttr = { part: 'indicator', name: 'aria-hidden', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on viewport', () => {
      const ariaAttr = { part: 'viewport', name: 'aria-hidden', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "navigation" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "Main navigation" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part list', () => {
      const connectAria = { part: 'list', attr: 'role', value: "menubar" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-orientation on connect part list', () => {
      const connectAria = { part: 'list', attr: 'aria-orientation', value: "? orientation" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part trigger', () => {
      const connectAria = { part: 'trigger', attr: 'role', value: "menuitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-haspopup on connect part trigger', () => {
      const connectAria = { part: 'trigger', attr: 'aria-haspopup', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part trigger', () => {
      const connectAria = { part: 'trigger', attr: 'aria-expanded', value: "if state . item = = open then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-controls on connect part trigger', () => {
      const connectAria = { part: 'trigger', attr: 'aria-controls', value: "content" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part link', () => {
      const connectAria = { part: 'link', attr: 'role', value: "menuitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-current on connect part link', () => {
      const connectAria = { part: 'link', attr: 'aria-current', value: "if link . active then page else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part content', () => {
      const connectAria = { part: 'content', attr: 'role', value: "menu" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-labelledby on connect part content', () => {
      const connectAria = { part: 'content', attr: 'aria-labelledby', value: "trigger" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part indicator', () => {
      const connectAria = { part: 'indicator', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part viewport', () => {
      const connectAria = { part: 'viewport', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "items" with type "list NavigationItem"', () => {
      const prop = { name: 'items', type: 'list NavigationItem' };
      expect(prop.name).toBe('items');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "orientation" with type "union horizontal | vertical"', () => {
      const prop = { name: 'orientation', type: 'union horizontal | vertical', defaultValue: "horizontal" };
      expect(prop.name).toBe('orientation');
      expect(prop.type).toBeTruthy();
    });

    it('should default "orientation" to "horizontal"', () => {
      expect("horizontal").toBe("horizontal");
    });

    it('should declare prop "delayDuration" with type "Int"', () => {
      const prop = { name: 'delayDuration', type: 'Int', defaultValue: "200" };
      expect(prop.name).toBe('delayDuration');
      expect(prop.type).toBeTruthy();
    });

    it('should default "delayDuration" to "200"', () => {
      expect("200").toBe("200");
    });

    it('should declare prop "skipDelayDuration" with type "Int"', () => {
      const prop = { name: 'skipDelayDuration', type: 'Int', defaultValue: "300" };
      expect(prop.name).toBe('skipDelayDuration');
      expect(prop.type).toBeTruthy();
    });

    it('should default "skipDelayDuration" to "300"', () => {
      expect("300").toBe("300");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","list","item","trigger","link","content","indicator","viewport"];
      expect(parts.length).toBe(8);
      expect(parts).toContain('root');
      expect(parts).toContain('list');
      expect(parts).toContain('item');
      expect(parts).toContain('trigger');
      expect(parts).toContain('link');
      expect(parts).toContain('content');
      expect(parts).toContain('indicator');
      expect(parts).toContain('viewport');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // list: container
      expect('container').toBeTruthy(); // item: container
      expect('action').toBeTruthy(); // trigger: action
      expect('action').toBeTruthy(); // link: action
      expect('container').toBeTruthy(); // content: container
      expect('container').toBeTruthy(); // indicator: container
      expect('container').toBeTruthy(); // viewport: container
    });

  });

});
