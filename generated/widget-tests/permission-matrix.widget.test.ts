// Auto-generated by generate-widget-tests.ts from permission-matrix.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: PermissionMatrix', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["group.expanded","saving.idle","permission.denied"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('group.expanded');
      expect(initialStates).toContain('saving.idle');
      expect(initialStates).toContain('permission.denied');
    });

    it('should transition from group.expanded to collapsed on COLLAPSE', () => {
      const transition = { from: 'group.expanded', event: 'COLLAPSE', target: 'collapsed' };
      expect(transition.from).toBe('group.expanded');
      expect(transition.event).toBe('COLLAPSE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from group.collapsed to expanded on EXPAND', () => {
      const transition = { from: 'group.collapsed', event: 'EXPAND', target: 'expanded' };
      expect(transition.from).toBe('group.collapsed');
      expect(transition.event).toBe('EXPAND');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from saving.idle to pending on CHANGE', () => {
      const transition = { from: 'saving.idle', event: 'CHANGE', target: 'pending' };
      expect(transition.from).toBe('saving.idle');
      expect(transition.event).toBe('CHANGE');
      expect(transition.target).toBe('pending');
      // Target state 'pending' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from saving.pending to saving on SAVE', () => {
      const transition = { from: 'saving.pending', event: 'SAVE', target: 'saving' };
      expect(transition.from).toBe('saving.pending');
      expect(transition.event).toBe('SAVE');
      expect(transition.target).toBe('saving');
      // Target state 'saving' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from saving.pending to pending on CHANGE', () => {
      const transition = { from: 'saving.pending', event: 'CHANGE', target: 'pending' };
      expect(transition.from).toBe('saving.pending');
      expect(transition.event).toBe('CHANGE');
      expect(transition.target).toBe('pending');
      // Target state 'pending' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from saving.pending to idle on DISCARD', () => {
      const transition = { from: 'saving.pending', event: 'DISCARD', target: 'idle' };
      expect(transition.from).toBe('saving.pending');
      expect(transition.event).toBe('DISCARD');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from saving.saving to idle on SAVE_COMPLETE', () => {
      const transition = { from: 'saving.saving', event: 'SAVE_COMPLETE', target: 'idle' };
      expect(transition.from).toBe('saving.saving');
      expect(transition.event).toBe('SAVE_COMPLETE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from saving.saving to pending on SAVE_ERROR', () => {
      const transition = { from: 'saving.saving', event: 'SAVE_ERROR', target: 'pending' };
      expect(transition.from).toBe('saving.saving');
      expect(transition.event).toBe('SAVE_ERROR');
      expect(transition.target).toBe('pending');
      // Target state 'pending' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from permission.denied to granted on GRANT', () => {
      const transition = { from: 'permission.denied', event: 'GRANT', target: 'granted' };
      expect(transition.from).toBe('permission.denied');
      expect(transition.event).toBe('GRANT');
      expect(transition.target).toBe('granted');
      // Target state 'granted' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from permission.granted to denied on REVOKE', () => {
      const transition = { from: 'permission.granted', event: 'REVOKE', target: 'denied' };
      expect(transition.from).toBe('permission.granted');
      expect(transition.event).toBe('REVOKE');
      expect(transition.target).toBe('denied');
      // Target state 'denied' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"group.expanded","events":["COLLAPSE"]},{"name":"group.collapsed","events":["EXPAND"]},{"name":"saving.idle","events":["CHANGE"]},{"name":"saving.pending","events":["SAVE","CHANGE","DISCARD"]},{"name":"saving.saving","events":["SAVE_COMPLETE","SAVE_ERROR"]},{"name":"permission.denied","events":["GRANT"]},{"name":"permission.granted","events":["REVOKE"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["group.expanded","group.collapsed","saving.idle","saving.pending","saving.saving","permission.denied","permission.granted"]);
      const baseNames = new Set(["expanded","collapsed","idle","pending","saving","denied","granted"]);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('pending') || baseNames.has('pending')).toBe(true);
      expect(validNames.has('saving') || baseNames.has('saving')).toBe(true);
      expect(validNames.has('pending') || baseNames.has('pending')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('pending') || baseNames.has('pending')).toBe(true);
      expect(validNames.has('granted') || baseNames.has('granted')).toBe(true);
      expect(validNames.has('denied') || baseNames.has('denied')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-disabled on root', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-readonly on root', () => {
      const attr = { name: 'data-readonly', value: "if ? readOnly then true else false" };
      expect(attr.name).toBe('data-readonly');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if state . saving = = pending then unsaved else if state . saving = = saving then saving else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="role-header" to roleHeader', () => {
      const binding = {"part":"roleHeader","dataPart":"role-header"};
      expect(binding.dataPart).toBe('role-header');
    });

    it('should bind data-role on roleCell', () => {
      const attr = { name: 'data-role', value: "self . roleKey" };
      expect(attr.name).toBe('data-role');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="role-description" to roleDescription', () => {
      const binding = {"part":"roleDescription","dataPart":"role-description"};
      expect(binding.dataPart).toBe('role-description');
    });

    it('should bind data-resource on resourceGroup', () => {
      const attr = { name: 'data-resource', value: "self . resourceKey" };
      expect(attr.name).toBe('data-resource');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on resourceGroup', () => {
      const attr = { name: 'data-state', value: "if state . group = = expanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="resource-label" to resourceLabel', () => {
      const binding = {"part":"resourceLabel","dataPart":"resource-label"};
      expect(binding.dataPart).toBe('resource-label');
    });

    it('should bind onClick handler on resourceLabel', () => {
      const handler = { event: 'onClick', action: "if ? collapsible then send ( if state . group = = expanded then COLLAPSE else EXPAND ) else noop" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-resource on resourceCheckbox', () => {
      const attr = { name: 'data-resource', value: "self . resourceKey" };
      expect(attr.name).toBe('data-resource');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-role on resourceCheckbox', () => {
      const attr = { name: 'data-role', value: "self . roleKey" };
      expect(attr.name).toBe('data-role');
      expect(attr.value).toBeDefined();
    });

    it('should bind onChange handler on resourceCheckbox', () => {
      const handler = { event: 'onChange', action: "send ( TOGGLE_RESOURCE , { resource : self . resourceKey , role : self . roleKey } )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-action on resourceRow', () => {
      const attr = { name: 'data-action', value: "self . actionKey" };
      expect(attr.name).toBe('data-action');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-resource on resourceRow', () => {
      const attr = { name: 'data-resource', value: "self . resourceKey" };
      expect(attr.name).toBe('data-resource');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="action-label" to actionLabel', () => {
      const binding = {"part":"actionLabel","dataPart":"action-label"};
      expect(binding.dataPart).toBe('action-label');
    });

    it('should bind data-role on actionCell', () => {
      const attr = { name: 'data-role', value: "self . roleKey" };
      expect(attr.name).toBe('data-role');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-action on actionCell', () => {
      const attr = { name: 'data-action', value: "self . actionKey" };
      expect(attr.name).toBe('data-action');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-resource on actionCell', () => {
      const attr = { name: 'data-resource', value: "self . resourceKey" };
      expect(attr.name).toBe('data-resource');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-inherited on actionCheckbox', () => {
      const attr = { name: 'data-inherited', value: "if self . inherited then true else false" };
      expect(attr.name).toBe('data-inherited');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-granted on actionCheckbox', () => {
      const attr = { name: 'data-granted', value: "if self . granted then true else false" };
      expect(attr.name).toBe('data-granted');
      expect(attr.value).toBeDefined();
    });

    it('should bind onChange handler on actionCheckbox', () => {
      const handler = { event: 'onChange', action: "send ( TOGGLE_PERMISSION , { resource : self . resourceKey , action : self . actionKey , role : self . roleKey } )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowRight handler on actionCheckbox', () => {
      const handler = { event: 'onKeyDown-ArrowRight', action: "send ( NAVIGATE_RIGHT )" };
      expect(handler.event).toBe('onKeyDown-ArrowRight');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowLeft handler on actionCheckbox', () => {
      const handler = { event: 'onKeyDown-ArrowLeft', action: "send ( NAVIGATE_LEFT )" };
      expect(handler.event).toBe('onKeyDown-ArrowLeft');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on actionCheckbox', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "send ( NAVIGATE_DOWN )" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on actionCheckbox', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "send ( NAVIGATE_UP )" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-role on bulkToggle', () => {
      const attr = { name: 'data-role', value: "self . roleKey" };
      expect(attr.name).toBe('data-role');
      expect(attr.value).toBeDefined();
    });

    it('should bind onChange handler on bulkToggle', () => {
      const handler = { event: 'onChange', action: "send ( TOGGLE_ALL_ROLE , { role : self . roleKey } )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","roleHeader","roleCell","roleDescription","resourceGroup","resourceLabel","resourceCheckbox","resourceRow","actionLabel","actionCell","actionCheckbox","bulkToggle"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('roleHeader')).toBe(true);
      expect(anatomyParts.has('roleCell')).toBe(true);
      expect(anatomyParts.has('roleDescription')).toBe(true);
      expect(anatomyParts.has('resourceGroup')).toBe(true);
      expect(anatomyParts.has('resourceLabel')).toBe(true);
      expect(anatomyParts.has('resourceCheckbox')).toBe(true);
      expect(anatomyParts.has('resourceRow')).toBe(true);
      expect(anatomyParts.has('actionLabel')).toBe(true);
      expect(anatomyParts.has('actionCell')).toBe(true);
      expect(anatomyParts.has('actionCheckbox')).toBe(true);
      expect(anatomyParts.has('bulkToggle')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Space key with action: TOGGLE_PERMISSION', () => {
      const binding = { key: 'Space', action: 'TOGGLE_PERMISSION' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('TOGGLE_PERMISSION');
    });

    it('should handle Enter key with action: TOGGLE_PERMISSION', () => {
      const binding = { key: 'Enter', action: 'TOGGLE_PERMISSION' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('TOGGLE_PERMISSION');
    });

    it('should handle ArrowRight key with action: NAVIGATE_RIGHT', () => {
      const binding = { key: 'ArrowRight', action: 'NAVIGATE_RIGHT' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('NAVIGATE_RIGHT');
    });

    it('should handle ArrowLeft key with action: NAVIGATE_LEFT', () => {
      const binding = { key: 'ArrowLeft', action: 'NAVIGATE_LEFT' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('NAVIGATE_LEFT');
    });

    it('should handle ArrowDown key with action: NAVIGATE_DOWN', () => {
      const binding = { key: 'ArrowDown', action: 'NAVIGATE_DOWN' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('NAVIGATE_DOWN');
    });

    it('should handle ArrowUp key with action: NAVIGATE_UP', () => {
      const binding = { key: 'ArrowUp', action: 'NAVIGATE_UP' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('NAVIGATE_UP');
    });

    it('should handle Home key with action: NAVIGATE_ROW_START', () => {
      const binding = { key: 'Home', action: 'NAVIGATE_ROW_START' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('NAVIGATE_ROW_START');
    });

    it('should handle End key with action: NAVIGATE_ROW_END', () => {
      const binding = { key: 'End', action: 'NAVIGATE_ROW_END' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('NAVIGATE_ROW_END');
    });

    it('should handle Tab key with action: FOCUS_OUT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_OUT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_OUT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["COLLAPSE","EXPAND","CHANGE","SAVE","DISCARD","SAVE_COMPLETE","SAVE_ERROR","GRANT","REVOKE"]);
      const keyboardActions = ["TOGGLE_PERMISSION","TOGGLE_PERMISSION","NAVIGATE_RIGHT","NAVIGATE_LEFT","NAVIGATE_DOWN","NAVIGATE_UP","NAVIGATE_ROW_START","NAVIGATE_ROW_END","FOCUS_OUT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "grid"', () => {
      expect('grid').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "actionCheckbox"', () => {
      expect('actionCheckbox').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "grid" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Permission matrix" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-colcount on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-colcount', value: "roleCount + 1" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on roleHeader', () => {
      const ariaAttr = { part: 'roleHeader', name: 'role', value: "row" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on roleCell', () => {
      const ariaAttr = { part: 'roleCell', name: 'role', value: "columnheader" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-colindex on roleCell', () => {
      const ariaAttr = { part: 'roleCell', name: 'aria-colindex', value: "self . colIndex" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on resourceGroup', () => {
      const ariaAttr = { part: 'resourceGroup', name: 'role', value: "rowgroup" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on resourceGroup', () => {
      const ariaAttr = { part: 'resourceGroup', name: 'aria-label', value: "self . resourceName" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on resourceGroup', () => {
      const ariaAttr = { part: 'resourceGroup', name: 'aria-expanded', value: "if state . group = = expanded then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on resourceLabel', () => {
      const ariaAttr = { part: 'resourceLabel', name: 'role', value: "rowheader" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-colindex on resourceLabel', () => {
      const ariaAttr = { part: 'resourceLabel', name: 'aria-colindex', value: "1" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on resourceCheckbox', () => {
      const ariaAttr = { part: 'resourceCheckbox', name: 'aria-label', value: "concat ( self . resourceName ,  for  , self . roleName )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-checked on resourceCheckbox', () => {
      const ariaAttr = { part: 'resourceCheckbox', name: 'aria-checked', value: "if allActionsGranted ( self . resource , self . role ) then true else if someActionsGranted ( self . resource , self . role ) then mixed else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on resourceRow', () => {
      const ariaAttr = { part: 'resourceRow', name: 'role', value: "row" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-rowindex on resourceRow', () => {
      const ariaAttr = { part: 'resourceRow', name: 'aria-rowindex', value: "self . rowIndex" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on actionLabel', () => {
      const ariaAttr = { part: 'actionLabel', name: 'role', value: "rowheader" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-colindex on actionLabel', () => {
      const ariaAttr = { part: 'actionLabel', name: 'aria-colindex', value: "1" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on actionCell', () => {
      const ariaAttr = { part: 'actionCell', name: 'role', value: "gridcell" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-colindex on actionCell', () => {
      const ariaAttr = { part: 'actionCell', name: 'aria-colindex', value: "self . colIndex" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on actionCheckbox', () => {
      const ariaAttr = { part: 'actionCheckbox', name: 'aria-label', value: "concat ( self . actionName ,   , self . resourceName ,  for  , self . roleName )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-checked on actionCheckbox', () => {
      const ariaAttr = { part: 'actionCheckbox', name: 'aria-checked', value: "if self . granted then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on bulkToggle', () => {
      const ariaAttr = { part: 'bulkToggle', name: 'aria-label', value: "concat ( Toggle all permissions for  , self . roleName )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-checked on bulkToggle', () => {
      const ariaAttr = { part: 'bulkToggle', name: 'aria-checked', value: "if allGranted ( self . role ) then true else if someGranted ( self . role ) then mixed else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "grid" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "Permission matrix" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-colcount on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-colcount', value: "? roles . length + 1" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part roleHeader', () => {
      const connectAria = { part: 'roleHeader', attr: 'role', value: "row" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part roleCell', () => {
      const connectAria = { part: 'roleCell', attr: 'role', value: "columnheader" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-colindex on connect part roleCell', () => {
      const connectAria = { part: 'roleCell', attr: 'aria-colindex', value: "self . colIndex" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part resourceGroup', () => {
      const connectAria = { part: 'resourceGroup', attr: 'role', value: "rowgroup" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part resourceGroup', () => {
      const connectAria = { part: 'resourceGroup', attr: 'aria-label', value: "self . resourceName" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part resourceGroup', () => {
      const connectAria = { part: 'resourceGroup', attr: 'aria-expanded', value: "if state . group = = expanded then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part resourceLabel', () => {
      const connectAria = { part: 'resourceLabel', attr: 'role', value: "rowheader" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-colindex on connect part resourceLabel', () => {
      const connectAria = { part: 'resourceLabel', attr: 'aria-colindex', value: "1" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part resourceCheckbox', () => {
      const connectAria = { part: 'resourceCheckbox', attr: 'aria-label', value: "concat ( All  , self . resourceName ,  permissions for  , self . roleName )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-checked on connect part resourceCheckbox', () => {
      const connectAria = { part: 'resourceCheckbox', attr: 'aria-checked', value: "if allActionsGranted ( self . resourceKey , self . roleKey , ? permissions ) then true else if someActionsGranted ( self . resourceKey , self . roleKey , ? permissions ) then mixed else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part resourceRow', () => {
      const connectAria = { part: 'resourceRow', attr: 'role', value: "row" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-rowindex on connect part resourceRow', () => {
      const connectAria = { part: 'resourceRow', attr: 'aria-rowindex', value: "self . rowIndex" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part actionLabel', () => {
      const connectAria = { part: 'actionLabel', attr: 'role', value: "rowheader" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-colindex on connect part actionLabel', () => {
      const connectAria = { part: 'actionLabel', attr: 'aria-colindex', value: "1" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part actionCell', () => {
      const connectAria = { part: 'actionCell', attr: 'role', value: "gridcell" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-colindex on connect part actionCell', () => {
      const connectAria = { part: 'actionCell', attr: 'aria-colindex', value: "self . colIndex" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part actionCheckbox', () => {
      const connectAria = { part: 'actionCheckbox', attr: 'aria-label', value: "concat ( self . actionName ,   , self . resourceName ,  for  , self . roleName )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-checked on connect part actionCheckbox', () => {
      const connectAria = { part: 'actionCheckbox', attr: 'aria-checked', value: "if self . granted then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part bulkToggle', () => {
      const connectAria = { part: 'bulkToggle', attr: 'aria-label', value: "concat ( Toggle all permissions for  , self . roleName )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-checked on connect part bulkToggle', () => {
      const connectAria = { part: 'bulkToggle', attr: 'aria-checked', value: "if allGranted ( self . roleKey , ? permissions ) then true else if someGranted ( self . roleKey , ? permissions ) then mixed else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "roles" with type "list RoleDef"', () => {
      const prop = { name: 'roles', type: 'list RoleDef' };
      expect(prop.name).toBe('roles');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "resources" with type "list ResourceDef"', () => {
      const prop = { name: 'resources', type: 'list ResourceDef' };
      expect(prop.name).toBe('resources');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "permissions" with type "Map < String , Map < String , list String > >"', () => {
      const prop = { name: 'permissions', type: 'Map < String , Map < String , list String > >' };
      expect(prop.name).toBe('permissions');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "readOnly" with type "Bool"', () => {
      const prop = { name: 'readOnly', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('readOnly');
      expect(prop.type).toBeTruthy();
    });

    it('should default "readOnly" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "showBulkToggle" with type "Bool"', () => {
      const prop = { name: 'showBulkToggle', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('showBulkToggle');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showBulkToggle" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "showDescriptions" with type "Bool"', () => {
      const prop = { name: 'showDescriptions', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('showDescriptions');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showDescriptions" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "collapsible" with type "Bool"', () => {
      const prop = { name: 'collapsible', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('collapsible');
      expect(prop.type).toBeTruthy();
    });

    it('should default "collapsible" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "onChange" with type "option Function"', () => {
      const prop = { name: 'onChange', type: 'option Function' };
      expect(prop.name).toBe('onChange');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "onSave" with type "option Function"', () => {
      const prop = { name: 'onSave', type: 'option Function' };
      expect(prop.name).toBe('onSave');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","roleHeader","roleCell","roleDescription","resourceGroup","resourceLabel","resourceCheckbox","resourceRow","actionLabel","actionCell","actionCheckbox","bulkToggle"];
      expect(parts.length).toBe(12);
      expect(parts).toContain('root');
      expect(parts).toContain('roleHeader');
      expect(parts).toContain('roleCell');
      expect(parts).toContain('roleDescription');
      expect(parts).toContain('resourceGroup');
      expect(parts).toContain('resourceLabel');
      expect(parts).toContain('resourceCheckbox');
      expect(parts).toContain('resourceRow');
      expect(parts).toContain('actionLabel');
      expect(parts).toContain('actionCell');
      expect(parts).toContain('actionCheckbox');
      expect(parts).toContain('bulkToggle');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // roleHeader: container
      expect('text').toBeTruthy(); // roleCell: text
      expect('text').toBeTruthy(); // roleDescription: text
      expect('container').toBeTruthy(); // resourceGroup: container
      expect('text').toBeTruthy(); // resourceLabel: text
      expect('widget').toBeTruthy(); // resourceCheckbox: widget
      expect('container').toBeTruthy(); // resourceRow: container
      expect('text').toBeTruthy(); // actionLabel: text
      expect('container').toBeTruthy(); // actionCell: container
      expect('widget').toBeTruthy(); // actionCheckbox: widget
      expect('widget').toBeTruthy(); // bulkToggle: widget
    });

  });

});
