// Auto-generated by generate-widget-tests.ts from button.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: Button', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('idle');
    });

    it('should transition from idle to hovered on HOVER', () => {
      const transition = { from: 'idle', event: 'HOVER', target: 'hovered' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('hovered');
      // Target state 'hovered' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to focused on FOCUS', () => {
      const transition = { from: 'idle', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to idle on UNHOVER', () => {
      const transition = { from: 'hovered', event: 'UNHOVER', target: 'idle' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('UNHOVER');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to pressed on PRESS', () => {
      const transition = { from: 'hovered', event: 'PRESS', target: 'pressed' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('PRESS');
      expect(transition.target).toBe('pressed');
      // Target state 'pressed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to focused on FOCUS', () => {
      const transition = { from: 'hovered', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to idle on BLUR', () => {
      const transition = { from: 'focused', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to pressed on PRESS', () => {
      const transition = { from: 'focused', event: 'PRESS', target: 'pressed' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('PRESS');
      expect(transition.target).toBe('pressed');
      // Target state 'pressed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to focused on HOVER', () => {
      const transition = { from: 'focused', event: 'HOVER', target: 'focused' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from pressed to idle on RELEASE', () => {
      const transition = { from: 'pressed', event: 'RELEASE', target: 'idle' };
      expect(transition.from).toBe('pressed');
      expect(transition.event).toBe('RELEASE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from pressed to idle on ACTIVATE', () => {
      const transition = { from: 'pressed', event: 'ACTIVATE', target: 'idle' };
      expect(transition.from).toBe('pressed');
      expect(transition.event).toBe('ACTIVATE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from disabled to enabled on ENABLE', () => {
      const transition = { from: 'disabled', event: 'ENABLE', target: 'enabled' };
      expect(transition.from).toBe('disabled');
      expect(transition.event).toBe('ENABLE');
      expect(transition.target).toBe('enabled');
      // Target state 'enabled' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from enabled to disabled on DISABLE', () => {
      const transition = { from: 'enabled', event: 'DISABLE', target: 'disabled' };
      expect(transition.from).toBe('enabled');
      expect(transition.event).toBe('DISABLE');
      expect(transition.target).toBe('disabled');
      // Target state 'disabled' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading to ready on LOAD_COMPLETE', () => {
      const transition = { from: 'loading', event: 'LOAD_COMPLETE', target: 'ready' };
      expect(transition.from).toBe('loading');
      expect(transition.event).toBe('LOAD_COMPLETE');
      expect(transition.target).toBe('ready');
      // Target state 'ready' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from ready to loading on LOAD_START', () => {
      const transition = { from: 'ready', event: 'LOAD_START', target: 'loading' };
      expect(transition.from).toBe('ready');
      expect(transition.event).toBe('LOAD_START');
      expect(transition.target).toBe('loading');
      // Target state 'loading' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"idle","events":["HOVER","FOCUS"]},{"name":"hovered","events":["UNHOVER","PRESS","FOCUS"]},{"name":"focused","events":["BLUR","PRESS","HOVER"]},{"name":"pressed","events":["RELEASE","ACTIVATE"]},{"name":"disabled","events":["ENABLE"]},{"name":"enabled","events":["DISABLE"]},{"name":"loading","events":["LOAD_COMPLETE"]},{"name":"ready","events":["LOAD_START"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["idle","hovered","focused","pressed","disabled","enabled","loading","ready"]);
      const baseNames = new Set(["idle","hovered","focused","pressed","disabled","enabled","loading","ready"]);
      expect(validNames.has('hovered') || baseNames.has('hovered')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('pressed') || baseNames.has('pressed')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('pressed') || baseNames.has('pressed')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('enabled') || baseNames.has('enabled')).toBe(true);
      expect(validNames.has('disabled') || baseNames.has('disabled')).toBe(true);
      expect(validNames.has('ready') || baseNames.has('ready')).toBe(true);
      expect(validNames.has('loading') || baseNames.has('loading')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-variant on root', () => {
      const attr = { name: 'data-variant', value: "? variant" };
      expect(attr.name).toBe('data-variant');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-size on root', () => {
      const attr = { name: 'data-size', value: "? size" };
      expect(attr.name).toBe('data-size');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if ? loading then loading else if ? disabled then disabled else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on root', () => {
      const handler = { event: 'onClick', action: "send ( ACTIVATE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown handler on root', () => {
      const handler = { event: 'onKeyDown', action: "send ( ACTIVATE )" };
      expect(handler.event).toBe('onKeyDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onMouseEnter handler on root', () => {
      const handler = { event: 'onMouseEnter', action: "send ( HOVER )" };
      expect(handler.event).toBe('onMouseEnter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onMouseLeave handler on root', () => {
      const handler = { event: 'onMouseLeave', action: "send ( UNHOVER )" };
      expect(handler.event).toBe('onMouseLeave');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on root', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on root', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerDown handler on root', () => {
      const handler = { event: 'onPointerDown', action: "send ( PRESS )" };
      expect(handler.event).toBe('onPointerDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerUp handler on root', () => {
      const handler = { event: 'onPointerUp', action: "send ( RELEASE )" };
      expect(handler.event).toBe('onPointerUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="label" to label', () => {
      const binding = {"part":"label","dataPart":"label"};
      expect(binding.dataPart).toBe('label');
    });

    it('should bind data-size on label', () => {
      const attr = { name: 'data-size', value: "? size" };
      expect(attr.name).toBe('data-size');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="icon" to icon', () => {
      const binding = {"part":"icon","dataPart":"icon"};
      expect(binding.dataPart).toBe('icon');
    });

    it('should bind data-position on icon', () => {
      const attr = { name: 'data-position', value: "? iconPosition" };
      expect(attr.name).toBe('data-position');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="spinner" to spinner', () => {
      const binding = {"part":"spinner","dataPart":"spinner"};
      expect(binding.dataPart).toBe('spinner');
    });

    it('should bind data-visible on spinner', () => {
      const attr = { name: 'data-visible', value: "if ? loading then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","label","icon","spinner"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('label')).toBe(true);
      expect(anatomyParts.has('icon')).toBe(true);
      expect(anatomyParts.has('spinner')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: ACTIVATE', () => {
      const binding = { key: 'Enter', action: 'ACTIVATE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('ACTIVATE');
    });

    it('should handle Space key with action: ACTIVATE', () => {
      const binding = { key: 'Space', action: 'ACTIVATE' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('ACTIVATE');
    });

    it('should handle Tab key with action: FOCUS', () => {
      const binding = { key: 'Tab', action: 'FOCUS' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["HOVER","FOCUS","UNHOVER","PRESS","BLUR","RELEASE","ACTIVATE","ENABLE","DISABLE","LOAD_COMPLETE","LOAD_START"]);
      const keyboardActions = ["ACTIVATE","ACTIVATE","FOCUS"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "button"', () => {
      expect('button').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "root"', () => {
      expect('root').toBeTruthy();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-disabled on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-disabled', value: "if ? disabled then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-busy on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-busy', value: "if ? loading then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part icon', () => {
      const connectAria = { part: 'icon', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part spinner', () => {
      const connectAria = { part: 'spinner', attr: 'aria-hidden', value: "if ? loading then false else true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "variant" with type "union filled | outline | text | danger"', () => {
      const prop = { name: 'variant', type: 'union filled | outline | text | danger', defaultValue: "filled" };
      expect(prop.name).toBe('variant');
      expect(prop.type).toBeTruthy();
    });

    it('should default "variant" to "filled"', () => {
      expect("filled").toBe("filled");
    });

    it('should declare prop "size" with type "union sm | md | lg"', () => {
      const prop = { name: 'size', type: 'union sm | md | lg', defaultValue: "md" };
      expect(prop.name).toBe('size');
      expect(prop.type).toBeTruthy();
    });

    it('should default "size" to "md"', () => {
      expect("md").toBe("md");
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "loading" with type "Bool"', () => {
      const prop = { name: 'loading', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('loading');
      expect(prop.type).toBeTruthy();
    });

    it('should default "loading" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "type" with type "union button | submit | reset"', () => {
      const prop = { name: 'type', type: 'union button | submit | reset', defaultValue: "button" };
      expect(prop.name).toBe('type');
      expect(prop.type).toBeTruthy();
    });

    it('should default "type" to "button"', () => {
      expect("button").toBe("button");
    });

    it('should declare prop "iconPosition" with type "union start | end"', () => {
      const prop = { name: 'iconPosition', type: 'union start | end', defaultValue: "start" };
      expect(prop.name).toBe('iconPosition');
      expect(prop.type).toBeTruthy();
    });

    it('should default "iconPosition" to "start"', () => {
      expect("start").toBe("start");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","label","icon","spinner"];
      expect(parts.length).toBe(4);
      expect(parts).toContain('root');
      expect(parts).toContain('label');
      expect(parts).toContain('icon');
      expect(parts).toContain('spinner');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('action').toBeTruthy(); // root: action
      expect('text').toBeTruthy(); // label: text
      expect('container').toBeTruthy(); // icon: container
      expect('container').toBeTruthy(); // spinner: container
    });

  });

});
