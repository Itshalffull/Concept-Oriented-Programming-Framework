// Auto-generated by generate-widget-tests.ts from outliner.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: Outliner', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["item.expanded","editing.viewing","zoom.topLevel","drag.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('item.expanded');
      expect(initialStates).toContain('editing.viewing');
      expect(initialStates).toContain('zoom.topLevel');
      expect(initialStates).toContain('drag.idle');
    });

    it('should transition from item.expanded to collapsed on COLLAPSE', () => {
      const transition = { from: 'item.expanded', event: 'COLLAPSE', target: 'collapsed' };
      expect(transition.from).toBe('item.expanded');
      expect(transition.event).toBe('COLLAPSE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.collapsed to expanded on EXPAND', () => {
      const transition = { from: 'item.collapsed', event: 'EXPAND', target: 'expanded' };
      expect(transition.from).toBe('item.collapsed');
      expect(transition.event).toBe('EXPAND');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing.viewing to editing on EDIT', () => {
      const transition = { from: 'editing.viewing', event: 'EDIT', target: 'editing' };
      expect(transition.from).toBe('editing.viewing');
      expect(transition.event).toBe('EDIT');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing.viewing to focused on FOCUS', () => {
      const transition = { from: 'editing.viewing', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('editing.viewing');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing.editing to viewing on BLUR', () => {
      const transition = { from: 'editing.editing', event: 'BLUR', target: 'viewing' };
      expect(transition.from).toBe('editing.editing');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('viewing');
      // Target state 'viewing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing.editing to viewing on CONFIRM', () => {
      const transition = { from: 'editing.editing', event: 'CONFIRM', target: 'viewing' };
      expect(transition.from).toBe('editing.editing');
      expect(transition.event).toBe('CONFIRM');
      expect(transition.target).toBe('viewing');
      // Target state 'viewing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing.editing to viewing on ESCAPE', () => {
      const transition = { from: 'editing.editing', event: 'ESCAPE', target: 'viewing' };
      expect(transition.from).toBe('editing.editing');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('viewing');
      // Target state 'viewing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing.focused to editing on EDIT', () => {
      const transition = { from: 'editing.focused', event: 'EDIT', target: 'editing' };
      expect(transition.from).toBe('editing.focused');
      expect(transition.event).toBe('EDIT');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing.focused to viewing on BLUR', () => {
      const transition = { from: 'editing.focused', event: 'BLUR', target: 'viewing' };
      expect(transition.from).toBe('editing.focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('viewing');
      // Target state 'viewing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from zoom.topLevel to zoomed on ZOOM_IN', () => {
      const transition = { from: 'zoom.topLevel', event: 'ZOOM_IN', target: 'zoomed' };
      expect(transition.from).toBe('zoom.topLevel');
      expect(transition.event).toBe('ZOOM_IN');
      expect(transition.target).toBe('zoomed');
      // Target state 'zoomed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from zoom.zoomed to topLevel on ZOOM_OUT', () => {
      const transition = { from: 'zoom.zoomed', event: 'ZOOM_OUT', target: 'topLevel' };
      expect(transition.from).toBe('zoom.zoomed');
      expect(transition.event).toBe('ZOOM_OUT');
      expect(transition.target).toBe('topLevel');
      // Target state 'topLevel' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from zoom.zoomed to zoomed on ZOOM_IN', () => {
      const transition = { from: 'zoom.zoomed', event: 'ZOOM_IN', target: 'zoomed' };
      expect(transition.from).toBe('zoom.zoomed');
      expect(transition.event).toBe('ZOOM_IN');
      expect(transition.target).toBe('zoomed');
      // Target state 'zoomed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from drag.idle to dragging on DRAG_START', () => {
      const transition = { from: 'drag.idle', event: 'DRAG_START', target: 'dragging' };
      expect(transition.from).toBe('drag.idle');
      expect(transition.event).toBe('DRAG_START');
      expect(transition.target).toBe('dragging');
      // Target state 'dragging' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from drag.dragging to idle on DROP', () => {
      const transition = { from: 'drag.dragging', event: 'DROP', target: 'idle' };
      expect(transition.from).toBe('drag.dragging');
      expect(transition.event).toBe('DROP');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from drag.dragging to idle on ESCAPE', () => {
      const transition = { from: 'drag.dragging', event: 'ESCAPE', target: 'idle' };
      expect(transition.from).toBe('drag.dragging');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"item.expanded","events":["COLLAPSE"]},{"name":"item.collapsed","events":["EXPAND"]},{"name":"editing.viewing","events":["EDIT","FOCUS"]},{"name":"editing.editing","events":["BLUR","CONFIRM","ESCAPE"]},{"name":"editing.focused","events":["EDIT","BLUR"]},{"name":"zoom.topLevel","events":["ZOOM_IN"]},{"name":"zoom.zoomed","events":["ZOOM_OUT","ZOOM_IN"]},{"name":"drag.idle","events":["DRAG_START"]},{"name":"drag.dragging","events":["DROP","ESCAPE"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["item.expanded","item.collapsed","editing.viewing","editing.editing","editing.focused","zoom.topLevel","zoom.zoomed","drag.idle","drag.dragging"]);
      const baseNames = new Set(["expanded","collapsed","viewing","editing","focused","topLevel","zoomed","idle","dragging"]);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('viewing') || baseNames.has('viewing')).toBe(true);
      expect(validNames.has('viewing') || baseNames.has('viewing')).toBe(true);
      expect(validNames.has('viewing') || baseNames.has('viewing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('viewing') || baseNames.has('viewing')).toBe(true);
      expect(validNames.has('zoomed') || baseNames.has('zoomed')).toBe(true);
      expect(validNames.has('topLevel') || baseNames.has('topLevel')).toBe(true);
      expect(validNames.has('zoomed') || baseNames.has('zoomed')).toBe(true);
      expect(validNames.has('dragging') || baseNames.has('dragging')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-zoom-depth on root', () => {
      const attr = { name: 'data-zoom-depth', value: "? zoomPath . length" };
      expect(attr.name).toBe('data-zoom-depth');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-readonly on root', () => {
      const attr = { name: 'data-readonly', value: "if ? readOnly then true else false" };
      expect(attr.name).toBe('data-readonly');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-draggable on root', () => {
      const attr = { name: 'data-draggable', value: "if ? draggable then true else false" };
      expect(attr.name).toBe('data-draggable');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="breadcrumb" to breadcrumb', () => {
      const binding = {"part":"breadcrumb","dataPart":"breadcrumb"};
      expect(binding.dataPart).toBe('breadcrumb');
    });

    it('should bind data-visible on breadcrumb', () => {
      const attr = { name: 'data-visible', value: "if ? zoomPath . length > 0 then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-depth on item', () => {
      const attr = { name: 'data-depth', value: "self . depth" };
      expect(attr.name).toBe('data-depth');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on item', () => {
      const attr = { name: 'data-state', value: "if state . item = = collapsed then collapsed else expanded" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-editing on item', () => {
      const attr = { name: 'data-editing', value: "if state . editing = = editing then true else false" };
      expect(attr.name).toBe('data-editing');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-has-children on item', () => {
      const attr = { name: 'data-has-children', value: "if self . hasChildren then true else false" };
      expect(attr.name).toBe('data-has-children');
      expect(attr.value).toBeDefined();
    });

    it('should bind onFocus handler on item', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS , { id : self . id } )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on item', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "send ( FOCUS_PREV )" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on item', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "send ( FOCUS_NEXT )" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowLeft handler on item', () => {
      const handler = { event: 'onKeyDown-ArrowLeft', action: "send ( COLLAPSE_OR_PARENT )" };
      expect(handler.event).toBe('onKeyDown-ArrowLeft');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowRight handler on item', () => {
      const handler = { event: 'onKeyDown-ArrowRight', action: "send ( EXPAND_OR_FIRST_CHILD )" };
      expect(handler.event).toBe('onKeyDown-ArrowRight');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Tab handler on item', () => {
      const handler = { event: 'onKeyDown-Tab', action: "send ( INDENT , { id : self . id } )" };
      expect(handler.event).toBe('onKeyDown-Tab');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="bullet" to bullet', () => {
      const binding = {"part":"bullet","dataPart":"bullet"};
      expect(binding.dataPart).toBe('bullet');
    });

    it('should bind data-has-children on bullet', () => {
      const attr = { name: 'data-has-children', value: "if self . hasChildren then true else false" };
      expect(attr.name).toBe('data-has-children');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-visible on bullet', () => {
      const attr = { name: 'data-visible', value: "if ? showBullets then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on bullet', () => {
      const handler = { event: 'onClick', action: "send ( ZOOM_IN , { id : self . id } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="collapse-toggle" to collapseToggle', () => {
      const binding = {"part":"collapseToggle","dataPart":"collapse-toggle"};
      expect(binding.dataPart).toBe('collapse-toggle');
    });

    it('should bind data-visible on collapseToggle', () => {
      const attr = { name: 'data-visible', value: "if self . hasChildren and ? collapsible then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on collapseToggle', () => {
      const handler = { event: 'onClick', action: "send ( TOGGLE_COLLAPSE , { id : self . id } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="content" to content', () => {
      const binding = {"part":"content","dataPart":"content"};
      expect(binding.dataPart).toBe('content');
    });

    it('should bind data-placeholder on content', () => {
      const attr = { name: 'data-placeholder', value: "? placeholder" };
      expect(attr.name).toBe('data-placeholder');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-empty on content', () => {
      const attr = { name: 'data-empty', value: "if self . textEmpty then true else false" };
      expect(attr.name).toBe('data-empty');
      expect(attr.value).toBeDefined();
    });

    it('should bind onInput handler on content', () => {
      const handler = { event: 'onInput', action: "send ( INPUT , { id : self . id , value : self . textContent } )" };
      expect(handler.event).toBe('onInput');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on content', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( CONFIRM_OR_NEW_SIBLING , { id : self . id } )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Backspace handler on content', () => {
      const handler = { event: 'onKeyDown-Backspace', action: "send ( MERGE_OR_DELETE , { id : self . id } )" };
      expect(handler.event).toBe('onKeyDown-Backspace');
      expect(handler.action).toBeDefined();
    });

    it('should bind onDoubleClick handler on content', () => {
      const handler = { event: 'onDoubleClick', action: "send ( EDIT , { id : self . id } )" };
      expect(handler.event).toBe('onDoubleClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on content', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR , { id : self . id } )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="children" to children', () => {
      const binding = {"part":"children","dataPart":"children"};
      expect(binding.dataPart).toBe('children');
    });

    it('should bind data-depth on children', () => {
      const attr = { name: 'data-depth', value: "self . depth" };
      expect(attr.name).toBe('data-depth');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-visible on children', () => {
      const attr = { name: 'data-visible', value: "if state . item = = expanded then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="drag-handle" to dragHandle', () => {
      const binding = {"part":"dragHandle","dataPart":"drag-handle"};
      expect(binding.dataPart).toBe('drag-handle');
    });

    it('should bind data-visible on dragHandle', () => {
      const attr = { name: 'data-visible', value: "if ? draggable and not ? readOnly then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onDragStart handler on dragHandle', () => {
      const handler = { event: 'onDragStart', action: "send ( DRAG_START , { id : self . id } )" };
      expect(handler.event).toBe('onDragStart');
      expect(handler.action).toBeDefined();
    });

    it('should bind onDragEnd handler on dragHandle', () => {
      const handler = { event: 'onDragEnd', action: "send ( DROP )" };
      expect(handler.event).toBe('onDragEnd');
      expect(handler.action).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","breadcrumb","item","bullet","collapseToggle","content","children","dragHandle"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('breadcrumb')).toBe(true);
      expect(anatomyParts.has('item')).toBe(true);
      expect(anatomyParts.has('bullet')).toBe(true);
      expect(anatomyParts.has('collapseToggle')).toBe(true);
      expect(anatomyParts.has('content')).toBe(true);
      expect(anatomyParts.has('children')).toBe(true);
      expect(anatomyParts.has('dragHandle')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: CONFIRM_OR_NEW_SIBLING', () => {
      const binding = { key: 'Enter', action: 'CONFIRM_OR_NEW_SIBLING' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('CONFIRM_OR_NEW_SIBLING');
    });

    it('should handle Tab key with action: INDENT', () => {
      const binding = { key: 'Tab', action: 'INDENT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('INDENT');
    });

    it('should handle Shift+Tab key with action: OUTDENT', () => {
      const binding = { key: 'Shift+Tab', action: 'OUTDENT' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('OUTDENT');
    });

    it('should handle ArrowUp key with action: FOCUS_PREV', () => {
      const binding = { key: 'ArrowUp', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should handle ArrowDown key with action: FOCUS_NEXT', () => {
      const binding = { key: 'ArrowDown', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle ArrowLeft key with action: COLLAPSE_OR_PARENT', () => {
      const binding = { key: 'ArrowLeft', action: 'COLLAPSE_OR_PARENT' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('COLLAPSE_OR_PARENT');
    });

    it('should handle ArrowRight key with action: EXPAND_OR_FIRST_CHILD', () => {
      const binding = { key: 'ArrowRight', action: 'EXPAND_OR_FIRST_CHILD' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('EXPAND_OR_FIRST_CHILD');
    });

    it('should handle Home key with action: FOCUS_FIRST', () => {
      const binding = { key: 'Home', action: 'FOCUS_FIRST' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('FOCUS_FIRST');
    });

    it('should handle End key with action: FOCUS_LAST', () => {
      const binding = { key: 'End', action: 'FOCUS_LAST' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('FOCUS_LAST');
    });

    it('should handle Escape key with action: ESCAPE', () => {
      const binding = { key: 'Escape', action: 'ESCAPE' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('ESCAPE');
    });

    it('should handle Control+ArrowUp key with action: MOVE_UP', () => {
      const binding = { key: 'Control+ArrowUp', action: 'MOVE_UP' };
      expect(binding.key).toBe('Control+ArrowUp');
      expect(binding.action).toBe('MOVE_UP');
    });

    it('should handle Control+ArrowDown key with action: MOVE_DOWN', () => {
      const binding = { key: 'Control+ArrowDown', action: 'MOVE_DOWN' };
      expect(binding.key).toBe('Control+ArrowDown');
      expect(binding.action).toBe('MOVE_DOWN');
    });

    it('should handle Space key with action: TOGGLE_COLLAPSE', () => {
      const binding = { key: 'Space', action: 'TOGGLE_COLLAPSE' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('TOGGLE_COLLAPSE');
    });

    it('should handle Control+Period key with action: ZOOM_IN', () => {
      const binding = { key: 'Control+Period', action: 'ZOOM_IN' };
      expect(binding.key).toBe('Control+Period');
      expect(binding.action).toBe('ZOOM_IN');
    });

    it('should handle Control+Comma key with action: ZOOM_OUT', () => {
      const binding = { key: 'Control+Comma', action: 'ZOOM_OUT' };
      expect(binding.key).toBe('Control+Comma');
      expect(binding.action).toBe('ZOOM_OUT');
    });

    it('should handle Backspace key with action: MERGE_OR_DELETE', () => {
      const binding = { key: 'Backspace', action: 'MERGE_OR_DELETE' };
      expect(binding.key).toBe('Backspace');
      expect(binding.action).toBe('MERGE_OR_DELETE');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["COLLAPSE","EXPAND","EDIT","FOCUS","BLUR","CONFIRM","ESCAPE","ZOOM_IN","ZOOM_OUT","DRAG_START","DROP"]);
      const keyboardActions = ["CONFIRM_OR_NEW_SIBLING","INDENT","OUTDENT","FOCUS_PREV","FOCUS_NEXT","COLLAPSE_OR_PARENT","EXPAND_OR_FIRST_CHILD","FOCUS_FIRST","FOCUS_LAST","ESCAPE","MOVE_UP","MOVE_DOWN","TOGGLE_COLLAPSE","ZOOM_IN","ZOOM_OUT","MERGE_OR_DELETE"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "tree"', () => {
      expect('tree').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "item"', () => {
      expect('item').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "tree" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? ariaLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-multiselectable on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-multiselectable', value: "false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on item', () => {
      const ariaAttr = { part: 'item', name: 'role', value: "treeitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-expanded', value: "if self . hasChildren then ( if state . item = = expanded then true else false ) else undefined" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-level on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-level', value: "self . depth" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-posinset on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-posinset', value: "self . index" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-setsize on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-setsize', value: "self . siblingCount" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-grabbed on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-grabbed', value: "if state . drag = = dragging then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-selected', value: "if self . focused then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on children', () => {
      const ariaAttr = { part: 'children', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on collapseToggle', () => {
      const ariaAttr = { part: 'collapseToggle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on collapseToggle', () => {
      const ariaAttr = { part: 'collapseToggle', name: 'aria-label', value: "if state . item = = expanded then Collapse else Expand" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on collapseToggle', () => {
      const ariaAttr = { part: 'collapseToggle', name: 'aria-expanded', value: "if state . item = = expanded then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on bullet', () => {
      const ariaAttr = { part: 'bullet', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on bullet', () => {
      const ariaAttr = { part: 'bullet', name: 'aria-label', value: "Zoom into item" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on content', () => {
      const ariaAttr = { part: 'content', name: 'role', value: "textbox" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-multiline on content', () => {
      const ariaAttr = { part: 'content', name: 'aria-multiline', value: "false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on content', () => {
      const ariaAttr = { part: 'content', name: 'aria-label', value: "Item content" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on dragHandle', () => {
      const ariaAttr = { part: 'dragHandle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on dragHandle', () => {
      const ariaAttr = { part: 'dragHandle', name: 'aria-label', value: "Drag to reorder" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on dragHandle', () => {
      const ariaAttr = { part: 'dragHandle', name: 'aria-roledescription', value: "drag handle" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "tree" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? ariaLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part breadcrumb', () => {
      const connectAria = { part: 'breadcrumb', attr: 'aria-label', value: "Zoom navigation" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part item', () => {
      const connectAria = { part: 'item', attr: 'role', value: "treeitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-expanded', value: "if self . hasChildren and ? collapsible then ( if state . item = = expanded then true else false ) else undefined" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-level on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-level', value: "self . depth" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-posinset on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-posinset', value: "self . index" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-setsize on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-setsize', value: "self . siblingCount" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-grabbed on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-grabbed', value: "if state . drag = = dragging then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-selected', value: "if self . focused then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part bullet', () => {
      const connectAria = { part: 'bullet', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part bullet', () => {
      const connectAria = { part: 'bullet', attr: 'aria-label', value: "Zoom into item" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part collapseToggle', () => {
      const connectAria = { part: 'collapseToggle', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part collapseToggle', () => {
      const connectAria = { part: 'collapseToggle', attr: 'aria-label', value: "if state . item = = expanded then Collapse else Expand" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part collapseToggle', () => {
      const connectAria = { part: 'collapseToggle', attr: 'aria-expanded', value: "if state . item = = expanded then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part children', () => {
      const connectAria = { part: 'children', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part dragHandle', () => {
      const connectAria = { part: 'dragHandle', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part dragHandle', () => {
      const connectAria = { part: 'dragHandle', attr: 'aria-label', value: "Drag to reorder" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part dragHandle', () => {
      const connectAria = { part: 'dragHandle', attr: 'aria-roledescription', value: "drag handle" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "items" with type "list OutlineItem"', () => {
      const prop = { name: 'items', type: 'list OutlineItem' };
      expect(prop.name).toBe('items');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "ariaLabel" with type "String"', () => {
      const prop = { name: 'ariaLabel', type: 'String', defaultValue: "Outliner" };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "ariaLabel" to "Outliner"', () => {
      expect("Outliner").toBe("Outliner");
    });

    it('should declare prop "zoomPath" with type "list String"', () => {
      const prop = { name: 'zoomPath', type: 'list String', defaultValue: "[ ]" };
      expect(prop.name).toBe('zoomPath');
      expect(prop.type).toBeTruthy();
    });

    it('should default "zoomPath" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "readOnly" with type "Bool"', () => {
      const prop = { name: 'readOnly', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('readOnly');
      expect(prop.type).toBeTruthy();
    });

    it('should default "readOnly" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "collapsible" with type "Bool"', () => {
      const prop = { name: 'collapsible', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('collapsible');
      expect(prop.type).toBeTruthy();
    });

    it('should default "collapsible" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "draggable" with type "Bool"', () => {
      const prop = { name: 'draggable', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('draggable');
      expect(prop.type).toBeTruthy();
    });

    it('should default "draggable" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "placeholder" with type "String"', () => {
      const prop = { name: 'placeholder', type: 'String', defaultValue: "New item..." };
      expect(prop.name).toBe('placeholder');
      expect(prop.type).toBeTruthy();
    });

    it('should default "placeholder" to "New item..."', () => {
      expect("New item...").toBe("New item...");
    });

    it('should declare prop "showBullets" with type "Bool"', () => {
      const prop = { name: 'showBullets', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showBullets');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showBullets" to "true"', () => {
      expect("true").toBe("true");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","breadcrumb","item","bullet","collapseToggle","content","children","dragHandle"];
      expect(parts.length).toBe(8);
      expect(parts).toContain('root');
      expect(parts).toContain('breadcrumb');
      expect(parts).toContain('item');
      expect(parts).toContain('bullet');
      expect(parts).toContain('collapseToggle');
      expect(parts).toContain('content');
      expect(parts).toContain('children');
      expect(parts).toContain('dragHandle');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('widget').toBeTruthy(); // breadcrumb: widget
      expect('container').toBeTruthy(); // item: container
      expect('action').toBeTruthy(); // bullet: action
      expect('action').toBeTruthy(); // collapseToggle: action
      expect('action').toBeTruthy(); // content: action
      expect('container').toBeTruthy(); // children: container
      expect('action').toBeTruthy(); // dragHandle: action
    });

  });

});
