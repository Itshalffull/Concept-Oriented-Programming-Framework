// Auto-generated by generate-widget-tests.ts from file-browser.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: FileBrowser', () => {
  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","toolbar","uploadButton","newFolderButton","searchInput","viewToggle","sortMenu","breadcrumb","dropZone","content","grid","list","fileItem","fileIcon","fileName","fileMeta","detailSidebar","detailPreview","detailProperties","multiSelectBar","emptyState","loadingState"];
      expect(parts.length).toBe(22);
      expect(parts).toContain('root');
      expect(parts).toContain('toolbar');
      expect(parts).toContain('uploadButton');
      expect(parts).toContain('newFolderButton');
      expect(parts).toContain('searchInput');
      expect(parts).toContain('viewToggle');
      expect(parts).toContain('sortMenu');
      expect(parts).toContain('breadcrumb');
      expect(parts).toContain('dropZone');
      expect(parts).toContain('content');
      expect(parts).toContain('grid');
      expect(parts).toContain('list');
      expect(parts).toContain('fileItem');
      expect(parts).toContain('fileIcon');
      expect(parts).toContain('fileName');
      expect(parts).toContain('fileMeta');
      expect(parts).toContain('detailSidebar');
      expect(parts).toContain('detailPreview');
      expect(parts).toContain('detailProperties');
      expect(parts).toContain('multiSelectBar');
      expect(parts).toContain('emptyState');
      expect(parts).toContain('loadingState');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // toolbar: container
      expect('action').toBeTruthy(); // uploadButton: action
      expect('action').toBeTruthy(); // newFolderButton: action
      expect('widget').toBeTruthy(); // searchInput: widget
      expect('widget').toBeTruthy(); // viewToggle: widget
      expect('widget').toBeTruthy(); // sortMenu: widget
      expect('widget').toBeTruthy(); // breadcrumb: widget
      expect('container').toBeTruthy(); // dropZone: container
      expect('container').toBeTruthy(); // content: container
      expect('widget').toBeTruthy(); // grid: widget
      expect('widget').toBeTruthy(); // list: widget
      expect('container').toBeTruthy(); // fileItem: container
      expect('container').toBeTruthy(); // fileIcon: container
      expect('text').toBeTruthy(); // fileName: text
      expect('text').toBeTruthy(); // fileMeta: text
      expect('container').toBeTruthy(); // detailSidebar: container
      expect('container').toBeTruthy(); // detailPreview: container
      expect('container').toBeTruthy(); // detailProperties: container
      expect('container').toBeTruthy(); // multiSelectBar: container
      expect('container').toBeTruthy(); // emptyState: container
      expect('container').toBeTruthy(); // loadingState: container
    });

  });

});
