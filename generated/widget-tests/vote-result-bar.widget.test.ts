// Auto-generated by generate-widget-tests.ts from vote-result-bar.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: VoteResultBar', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('idle');
    });

    it('should transition from idle to segmentHovered on HOVER_SEGMENT', () => {
      const transition = { from: 'idle', event: 'HOVER_SEGMENT', target: 'segmentHovered' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('HOVER_SEGMENT');
      expect(transition.target).toBe('segmentHovered');
      // Target state 'segmentHovered' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to animating on ANIMATE_IN', () => {
      const transition = { from: 'idle', event: 'ANIMATE_IN', target: 'animating' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('ANIMATE_IN');
      expect(transition.target).toBe('animating');
      // Target state 'animating' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from animating to idle on ANIMATION_END', () => {
      const transition = { from: 'animating', event: 'ANIMATION_END', target: 'idle' };
      expect(transition.from).toBe('animating');
      expect(transition.event).toBe('ANIMATION_END');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from segmentHovered to idle on UNHOVER', () => {
      const transition = { from: 'segmentHovered', event: 'UNHOVER', target: 'idle' };
      expect(transition.from).toBe('segmentHovered');
      expect(transition.event).toBe('UNHOVER');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"idle","events":["HOVER_SEGMENT","ANIMATE_IN"]},{"name":"animating","events":["ANIMATION_END"]},{"name":"segmentHovered","events":["UNHOVER"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["idle","animating","segmentHovered"]);
      const baseNames = new Set(["idle","animating","segmentHovered"]);
      expect(validNames.has('segmentHovered') || baseNames.has('segmentHovered')).toBe(true);
      expect(validNames.has('animating') || baseNames.has('animating')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowRight key with action: FOCUS_NEXT_SEGMENT', () => {
      const binding = { key: 'ArrowRight', action: 'FOCUS_NEXT_SEGMENT' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('FOCUS_NEXT_SEGMENT');
    });

    it('should handle ArrowLeft key with action: FOCUS_PREV_SEGMENT', () => {
      const binding = { key: 'ArrowLeft', action: 'FOCUS_PREV_SEGMENT' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('FOCUS_PREV_SEGMENT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["HOVER_SEGMENT","ANIMATE_IN","ANIMATION_END","UNHOVER"]);
      const keyboardActions = ["FOCUS_NEXT_SEGMENT","FOCUS_PREV_SEGMENT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "img"', () => {
      expect('img').toBeTruthy();
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "img" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Vote results" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "vote result bar" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on segment', () => {
      const ariaAttr = { part: 'segment', name: 'role', value: "img" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on segment', () => {
      const ariaAttr = { part: 'segment', name: 'aria-label', value: "concat ( self . choice , :  , self . count ,  votes ( , self . percent , %) )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on quorumMarker', () => {
      const ariaAttr = { part: 'quorumMarker', name: 'role', value: "img" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on quorumMarker', () => {
      const ariaAttr = { part: 'quorumMarker', name: 'aria-label', value: "concat ( Quorum threshold at  , self . threshold , % )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on totalLabel', () => {
      const ariaAttr = { part: 'totalLabel', name: 'aria-label', value: "concat ( Total votes:  , total )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "variant" with type "union binary | multi | weighted"', () => {
      const prop = { name: 'variant', type: 'union binary | multi | weighted', defaultValue: "binary" };
      expect(prop.name).toBe('variant');
      expect(prop.type).toBeTruthy();
    });

    it('should default "variant" to "binary"', () => {
      expect("binary").toBe("binary");
    });

    it('should declare prop "showLabels" with type "Bool"', () => {
      const prop = { name: 'showLabels', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showLabels');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showLabels" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showQuorum" with type "Bool"', () => {
      const prop = { name: 'showQuorum', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('showQuorum');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showQuorum" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "quorumThreshold" with type "Float"', () => {
      const prop = { name: 'quorumThreshold', type: 'Float', defaultValue: "0" };
      expect(prop.name).toBe('quorumThreshold');
      expect(prop.type).toBeTruthy();
    });

    it('should default "quorumThreshold" to "0"', () => {
      expect("0").toBe("0");
    });

    it('should declare prop "animate" with type "Bool"', () => {
      const prop = { name: 'animate', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('animate');
      expect(prop.type).toBeTruthy();
    });

    it('should default "animate" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "size" with type "union sm | md | lg"', () => {
      const prop = { name: 'size', type: 'union sm | md | lg', defaultValue: "md" };
      expect(prop.name).toBe('size');
      expect(prop.type).toBeTruthy();
    });

    it('should default "size" to "md"', () => {
      expect("md").toBe("md");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","bar","segment","segmentLabel","quorumMarker","totalLabel"];
      expect(parts.length).toBe(6);
      expect(parts).toContain('root');
      expect(parts).toContain('bar');
      expect(parts).toContain('segment');
      expect(parts).toContain('segmentLabel');
      expect(parts).toContain('quorumMarker');
      expect(parts).toContain('totalLabel');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // bar: container
      expect('container').toBeTruthy(); // segment: container
      expect('text').toBeTruthy(); // segmentLabel: text
      expect('container').toBeTruthy(); // quorumMarker: container
      expect('text').toBeTruthy(); // totalLabel: text
    });

  });

});
