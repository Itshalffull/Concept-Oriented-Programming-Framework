// Auto-generated by generate-widget-tests.ts from filter-builder.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: FilterBuilder', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["filterCount.empty","row.idle","validity.incomplete"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('filterCount.empty');
      expect(initialStates).toContain('row.idle');
      expect(initialStates).toContain('validity.incomplete');
    });

    it('should transition from filterCount.empty to hasFilters on ADD_FILTER', () => {
      const transition = { from: 'filterCount.empty', event: 'ADD_FILTER', target: 'hasFilters' };
      expect(transition.from).toBe('filterCount.empty');
      expect(transition.event).toBe('ADD_FILTER');
      expect(transition.target).toBe('hasFilters');
      // Target state 'hasFilters' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from filterCount.empty to hasFilters on LOAD_FILTERS', () => {
      const transition = { from: 'filterCount.empty', event: 'LOAD_FILTERS', target: 'hasFilters' };
      expect(transition.from).toBe('filterCount.empty');
      expect(transition.event).toBe('LOAD_FILTERS');
      expect(transition.target).toBe('hasFilters');
      // Target state 'hasFilters' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from filterCount.hasFilters to empty on REMOVE_LAST', () => {
      const transition = { from: 'filterCount.hasFilters', event: 'REMOVE_LAST', target: 'empty' };
      expect(transition.from).toBe('filterCount.hasFilters');
      expect(transition.event).toBe('REMOVE_LAST');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from filterCount.hasFilters to empty on CLEAR_ALL', () => {
      const transition = { from: 'filterCount.hasFilters', event: 'CLEAR_ALL', target: 'empty' };
      expect(transition.from).toBe('filterCount.hasFilters');
      expect(transition.event).toBe('CLEAR_ALL');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from filterCount.hasFilters to hasFilters on ADD_FILTER', () => {
      const transition = { from: 'filterCount.hasFilters', event: 'ADD_FILTER', target: 'hasFilters' };
      expect(transition.from).toBe('filterCount.hasFilters');
      expect(transition.event).toBe('ADD_FILTER');
      expect(transition.target).toBe('hasFilters');
      // Target state 'hasFilters' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from row.idle to editing on FOCUS_FIELD', () => {
      const transition = { from: 'row.idle', event: 'FOCUS_FIELD', target: 'editing' };
      expect(transition.from).toBe('row.idle');
      expect(transition.event).toBe('FOCUS_FIELD');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from row.idle to editing on FOCUS_OPERATOR', () => {
      const transition = { from: 'row.idle', event: 'FOCUS_OPERATOR', target: 'editing' };
      expect(transition.from).toBe('row.idle');
      expect(transition.event).toBe('FOCUS_OPERATOR');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from row.idle to editing on FOCUS_VALUE', () => {
      const transition = { from: 'row.idle', event: 'FOCUS_VALUE', target: 'editing' };
      expect(transition.from).toBe('row.idle');
      expect(transition.event).toBe('FOCUS_VALUE');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from row.editing to idle on BLUR', () => {
      const transition = { from: 'row.editing', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('row.editing');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from row.editing to idle on COMMIT', () => {
      const transition = { from: 'row.editing', event: 'COMMIT', target: 'idle' };
      expect(transition.from).toBe('row.editing');
      expect(transition.event).toBe('COMMIT');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from row.editing to idle on CANCEL', () => {
      const transition = { from: 'row.editing', event: 'CANCEL', target: 'idle' };
      expect(transition.from).toBe('row.editing');
      expect(transition.event).toBe('CANCEL');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from validity.incomplete to valid on VALIDATE_PASS', () => {
      const transition = { from: 'validity.incomplete', event: 'VALIDATE_PASS', target: 'valid' };
      expect(transition.from).toBe('validity.incomplete');
      expect(transition.event).toBe('VALIDATE_PASS');
      expect(transition.target).toBe('valid');
      // Target state 'valid' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from validity.valid to incomplete on FIELD_CHANGE', () => {
      const transition = { from: 'validity.valid', event: 'FIELD_CHANGE', target: 'incomplete' };
      expect(transition.from).toBe('validity.valid');
      expect(transition.event).toBe('FIELD_CHANGE');
      expect(transition.target).toBe('incomplete');
      // Target state 'incomplete' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from validity.valid to incomplete on OPERATOR_CHANGE', () => {
      const transition = { from: 'validity.valid', event: 'OPERATOR_CHANGE', target: 'incomplete' };
      expect(transition.from).toBe('validity.valid');
      expect(transition.event).toBe('OPERATOR_CHANGE');
      expect(transition.target).toBe('incomplete');
      // Target state 'incomplete' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from validity.valid to incomplete on VALUE_CLEAR', () => {
      const transition = { from: 'validity.valid', event: 'VALUE_CLEAR', target: 'incomplete' };
      expect(transition.from).toBe('validity.valid');
      expect(transition.event).toBe('VALUE_CLEAR');
      expect(transition.target).toBe('incomplete');
      // Target state 'incomplete' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from validity.valid to valid on VALIDATE_PASS', () => {
      const transition = { from: 'validity.valid', event: 'VALIDATE_PASS', target: 'valid' };
      expect(transition.from).toBe('validity.valid');
      expect(transition.event).toBe('VALIDATE_PASS');
      expect(transition.target).toBe('valid');
      // Target state 'valid' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"filterCount.empty","events":["ADD_FILTER","LOAD_FILTERS"]},{"name":"filterCount.hasFilters","events":["REMOVE_LAST","CLEAR_ALL","ADD_FILTER"]},{"name":"row.idle","events":["FOCUS_FIELD","FOCUS_OPERATOR","FOCUS_VALUE"]},{"name":"row.editing","events":["BLUR","COMMIT","CANCEL"]},{"name":"validity.incomplete","events":["VALIDATE_PASS"]},{"name":"validity.valid","events":["FIELD_CHANGE","OPERATOR_CHANGE","VALUE_CLEAR","VALIDATE_PASS"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["filterCount.empty","filterCount.hasFilters","row.idle","row.editing","validity.incomplete","validity.valid"]);
      const baseNames = new Set(["empty","hasFilters","idle","editing","incomplete","valid"]);
      expect(validNames.has('hasFilters') || baseNames.has('hasFilters')).toBe(true);
      expect(validNames.has('hasFilters') || baseNames.has('hasFilters')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('hasFilters') || baseNames.has('hasFilters')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('valid') || baseNames.has('valid')).toBe(true);
      expect(validNames.has('incomplete') || baseNames.has('incomplete')).toBe(true);
      expect(validNames.has('incomplete') || baseNames.has('incomplete')).toBe(true);
      expect(validNames.has('incomplete') || baseNames.has('incomplete')).toBe(true);
      expect(validNames.has('valid') || baseNames.has('valid')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if ? filters . length = = 0 then empty else has-filters" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-logic on root', () => {
      const attr = { name: 'data-logic', value: "? logic" };
      expect(attr.name).toBe('data-logic');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-disabled on root', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on addButton', () => {
      const handler = { event: 'onClick', action: "send ( ADD_FILTER )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-state on filterRow', () => {
      const attr = { name: 'data-state', value: "if state . row = = editing then editing else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-valid on filterRow', () => {
      const attr = { name: 'data-valid', value: "if state . validity = = valid then true else false" };
      expect(attr.name).toBe('data-valid');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="field-selector" to fieldSelector', () => {
      const binding = {"part":"fieldSelector","dataPart":"field-selector"};
      expect(binding.dataPart).toBe('field-selector');
    });

    it('should bind onChange handler on fieldSelector', () => {
      const handler = { event: 'onChange', action: "send ( FIELD_CHANGE )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on fieldSelector', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS_FIELD )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on fieldSelector', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="operator-selector" to operatorSelector', () => {
      const binding = {"part":"operatorSelector","dataPart":"operator-selector"};
      expect(binding.dataPart).toBe('operator-selector');
    });

    it('should bind onChange handler on operatorSelector', () => {
      const handler = { event: 'onChange', action: "send ( OPERATOR_CHANGE )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on operatorSelector', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS_OPERATOR )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on operatorSelector', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="value-input" to valueInput', () => {
      const binding = {"part":"valueInput","dataPart":"value-input"};
      expect(binding.dataPart).toBe('value-input');
    });

    it('should bind onChange handler on valueInput', () => {
      const handler = { event: 'onChange', action: "send ( VALUE_CHANGE )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on valueInput', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS_VALUE )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on valueInput', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind onClick handler on removeButton', () => {
      const handler = { event: 'onClick', action: "send ( REMOVE_FILTER )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-logic on logicToggle', () => {
      const attr = { name: 'data-logic', value: "self . logic" };
      expect(attr.name).toBe('data-logic');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on logicToggle', () => {
      const handler = { event: 'onClick', action: "send ( TOGGLE_LOGIC )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-depth on group', () => {
      const attr = { name: 'data-depth', value: "self . depth" };
      expect(attr.name).toBe('data-depth');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-logic on group', () => {
      const attr = { name: 'data-logic', value: "self . logic" };
      expect(attr.name).toBe('data-logic');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on addGroupButton', () => {
      const handler = { event: 'onClick', action: "send ( ADD_GROUP )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="group-header" to groupHeader', () => {
      const binding = {"part":"groupHeader","dataPart":"group-header"};
      expect(binding.dataPart).toBe('group-header');
    });

    it('should bind data-depth on groupHeader', () => {
      const attr = { name: 'data-depth', value: "self . depth" };
      expect(attr.name).toBe('data-depth');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on ungroupButton', () => {
      const handler = { event: 'onClick', action: "send ( UNGROUP )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","addButton","filterRow","fieldSelector","operatorSelector","valueInput","removeButton","logicToggle","group","addGroupButton","groupHeader","ungroupButton"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('addButton')).toBe(true);
      expect(anatomyParts.has('filterRow')).toBe(true);
      expect(anatomyParts.has('fieldSelector')).toBe(true);
      expect(anatomyParts.has('operatorSelector')).toBe(true);
      expect(anatomyParts.has('valueInput')).toBe(true);
      expect(anatomyParts.has('removeButton')).toBe(true);
      expect(anatomyParts.has('logicToggle')).toBe(true);
      expect(anatomyParts.has('group')).toBe(true);
      expect(anatomyParts.has('addGroupButton')).toBe(true);
      expect(anatomyParts.has('groupHeader')).toBe(true);
      expect(anatomyParts.has('ungroupButton')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: COMMIT', () => {
      const binding = { key: 'Enter', action: 'COMMIT' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('COMMIT');
    });

    it('should handle Escape key with action: CANCEL', () => {
      const binding = { key: 'Escape', action: 'CANCEL' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('CANCEL');
    });

    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should handle Delete key with action: REMOVE_FILTER', () => {
      const binding = { key: 'Delete', action: 'REMOVE_FILTER' };
      expect(binding.key).toBe('Delete');
      expect(binding.action).toBe('REMOVE_FILTER');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["ADD_FILTER","LOAD_FILTERS","REMOVE_LAST","CLEAR_ALL","FOCUS_FIELD","FOCUS_OPERATOR","FOCUS_VALUE","BLUR","COMMIT","CANCEL","VALIDATE_PASS","FIELD_CHANGE","OPERATOR_CHANGE","VALUE_CLEAR"]);
      const keyboardActions = ["COMMIT","CANCEL","FOCUS_NEXT","FOCUS_PREV","REMOVE_FILTER"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "group"', () => {
      expect('group').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "addButton"', () => {
      expect('addButton').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Filter builder" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on filterRow', () => {
      const ariaAttr = { part: 'filterRow', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on filterRow', () => {
      const ariaAttr = { part: 'filterRow', name: 'aria-label', value: "Filter row" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on logicToggle', () => {
      const ariaAttr = { part: 'logicToggle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on logicToggle', () => {
      const ariaAttr = { part: 'logicToggle', name: 'aria-label', value: "Toggle logic operator" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-pressed on logicToggle', () => {
      const ariaAttr = { part: 'logicToggle', name: 'aria-pressed', value: "if self . logic = = or then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on group', () => {
      const ariaAttr = { part: 'group', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on group', () => {
      const ariaAttr = { part: 'group', name: 'aria-label', value: "Filter group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on addButton', () => {
      const ariaAttr = { part: 'addButton', name: 'aria-label', value: "Add filter" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on removeButton', () => {
      const ariaAttr = { part: 'removeButton', name: 'aria-label', value: "Remove filter" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "Filter builder" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part addButton', () => {
      const connectAria = { part: 'addButton', attr: 'aria-label', value: "Add filter" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part filterRow', () => {
      const connectAria = { part: 'filterRow', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part filterRow', () => {
      const connectAria = { part: 'filterRow', attr: 'aria-label', value: "Filter row" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part fieldSelector', () => {
      const connectAria = { part: 'fieldSelector', attr: 'aria-label', value: "Filter field" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part operatorSelector', () => {
      const connectAria = { part: 'operatorSelector', attr: 'aria-label', value: "Filter operator" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part valueInput', () => {
      const connectAria = { part: 'valueInput', attr: 'aria-label', value: "Filter value" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part removeButton', () => {
      const connectAria = { part: 'removeButton', attr: 'aria-label', value: "Remove filter" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part logicToggle', () => {
      const connectAria = { part: 'logicToggle', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part logicToggle', () => {
      const connectAria = { part: 'logicToggle', attr: 'aria-label', value: "Toggle logic operator" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-pressed on connect part logicToggle', () => {
      const connectAria = { part: 'logicToggle', attr: 'aria-pressed', value: "if self . logic = = or then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part group', () => {
      const connectAria = { part: 'group', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part group', () => {
      const connectAria = { part: 'group', attr: 'aria-label', value: "Filter group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part addGroupButton', () => {
      const connectAria = { part: 'addGroupButton', attr: 'aria-label', value: "Add filter group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part ungroupButton', () => {
      const connectAria = { part: 'ungroupButton', attr: 'aria-label', value: "Ungroup filters" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "filters" with type "list FilterRow"', () => {
      const prop = { name: 'filters', type: 'list FilterRow', defaultValue: "[ ]" };
      expect(prop.name).toBe('filters');
      expect(prop.type).toBeTruthy();
    });

    it('should default "filters" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "logic" with type "union and | or"', () => {
      const prop = { name: 'logic', type: 'union and | or', defaultValue: "and" };
      expect(prop.name).toBe('logic');
      expect(prop.type).toBeTruthy();
    });

    it('should default "logic" to "and"', () => {
      expect("and").toBe("and");
    });

    it('should declare prop "fields" with type "list FieldDef"', () => {
      const prop = { name: 'fields', type: 'list FieldDef' };
      expect(prop.name).toBe('fields');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "operators" with type "list OperatorDef"', () => {
      const prop = { name: 'operators', type: 'list OperatorDef' };
      expect(prop.name).toBe('operators');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "maxDepth" with type "Int"', () => {
      const prop = { name: 'maxDepth', type: 'Int', defaultValue: "3" };
      expect(prop.name).toBe('maxDepth');
      expect(prop.type).toBeTruthy();
    });

    it('should default "maxDepth" to "3"', () => {
      expect("3").toBe("3");
    });

    it('should declare prop "maxFilters" with type "Int"', () => {
      const prop = { name: 'maxFilters', type: 'Int', defaultValue: "20" };
      expect(prop.name).toBe('maxFilters');
      expect(prop.type).toBeTruthy();
    });

    it('should default "maxFilters" to "20"', () => {
      expect("20").toBe("20");
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "allowGroups" with type "Bool"', () => {
      const prop = { name: 'allowGroups', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('allowGroups');
      expect(prop.type).toBeTruthy();
    });

    it('should default "allowGroups" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "onChange" with type "option Function"', () => {
      const prop = { name: 'onChange', type: 'option Function' };
      expect(prop.name).toBe('onChange');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","addButton","filterRow","fieldSelector","operatorSelector","valueInput","removeButton","logicToggle","group","addGroupButton","groupHeader","ungroupButton"];
      expect(parts.length).toBe(12);
      expect(parts).toContain('root');
      expect(parts).toContain('addButton');
      expect(parts).toContain('filterRow');
      expect(parts).toContain('fieldSelector');
      expect(parts).toContain('operatorSelector');
      expect(parts).toContain('valueInput');
      expect(parts).toContain('removeButton');
      expect(parts).toContain('logicToggle');
      expect(parts).toContain('group');
      expect(parts).toContain('addGroupButton');
      expect(parts).toContain('groupHeader');
      expect(parts).toContain('ungroupButton');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('action').toBeTruthy(); // addButton: action
      expect('container').toBeTruthy(); // filterRow: container
      expect('widget').toBeTruthy(); // fieldSelector: widget
      expect('widget').toBeTruthy(); // operatorSelector: widget
      expect('widget').toBeTruthy(); // valueInput: widget
      expect('action').toBeTruthy(); // removeButton: action
      expect('action').toBeTruthy(); // logicToggle: action
      expect('container').toBeTruthy(); // group: container
      expect('action').toBeTruthy(); // addGroupButton: action
      expect('container').toBeTruthy(); // groupHeader: container
      expect('action').toBeTruthy(); // ungroupButton: action
    });

  });

});
