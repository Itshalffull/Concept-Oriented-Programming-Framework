// Auto-generated by generate-widget-tests.ts from number-input.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: NumberInput', () => {
  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","label","input","incrementButton","decrementButton","description","error"];
      expect(parts.length).toBe(7);
      expect(parts).toContain('root');
      expect(parts).toContain('label');
      expect(parts).toContain('input');
      expect(parts).toContain('incrementButton');
      expect(parts).toContain('decrementButton');
      expect(parts).toContain('description');
      expect(parts).toContain('error');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('text').toBeTruthy(); // label: text
      expect('action').toBeTruthy(); // input: action
      expect('action').toBeTruthy(); // incrementButton: action
      expect('action').toBeTruthy(); // decrementButton: action
      expect('text').toBeTruthy(); // description: text
      expect('text').toBeTruthy(); // error: text
    });

  });

});
