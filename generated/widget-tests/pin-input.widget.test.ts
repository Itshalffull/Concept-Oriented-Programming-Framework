// Auto-generated by generate-widget-tests.ts from pin-input.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: PinInput', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["completion.empty","focus.unfocused"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('completion.empty');
      expect(initialStates).toContain('focus.unfocused');
    });

    it('should transition from completion.empty to partial on INPUT', () => {
      const transition = { from: 'completion.empty', event: 'INPUT', target: 'partial' };
      expect(transition.from).toBe('completion.empty');
      expect(transition.event).toBe('INPUT');
      expect(transition.target).toBe('partial');
      // Target state 'partial' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from completion.empty to partial on PASTE', () => {
      const transition = { from: 'completion.empty', event: 'PASTE', target: 'partial' };
      expect(transition.from).toBe('completion.empty');
      expect(transition.event).toBe('PASTE');
      expect(transition.target).toBe('partial');
      // Target state 'partial' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from completion.partial to complete on FILL_ALL', () => {
      const transition = { from: 'completion.partial', event: 'FILL_ALL', target: 'complete' };
      expect(transition.from).toBe('completion.partial');
      expect(transition.event).toBe('FILL_ALL');
      expect(transition.target).toBe('complete');
      // Target state 'complete' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from completion.partial to empty on CLEAR_ALL', () => {
      const transition = { from: 'completion.partial', event: 'CLEAR_ALL', target: 'empty' };
      expect(transition.from).toBe('completion.partial');
      expect(transition.event).toBe('CLEAR_ALL');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from completion.partial to partial on INPUT', () => {
      const transition = { from: 'completion.partial', event: 'INPUT', target: 'partial' };
      expect(transition.from).toBe('completion.partial');
      expect(transition.event).toBe('INPUT');
      expect(transition.target).toBe('partial');
      // Target state 'partial' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from completion.partial to partial on DELETE_CHAR', () => {
      const transition = { from: 'completion.partial', event: 'DELETE_CHAR', target: 'partial' };
      expect(transition.from).toBe('completion.partial');
      expect(transition.event).toBe('DELETE_CHAR');
      expect(transition.target).toBe('partial');
      // Target state 'partial' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from completion.complete to partial on DELETE_CHAR', () => {
      const transition = { from: 'completion.complete', event: 'DELETE_CHAR', target: 'partial' };
      expect(transition.from).toBe('completion.complete');
      expect(transition.event).toBe('DELETE_CHAR');
      expect(transition.target).toBe('partial');
      // Target state 'partial' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from completion.complete to empty on CLEAR_ALL', () => {
      const transition = { from: 'completion.complete', event: 'CLEAR_ALL', target: 'empty' };
      expect(transition.from).toBe('completion.complete');
      expect(transition.event).toBe('CLEAR_ALL');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.unfocused to focused on FOCUS', () => {
      const transition = { from: 'focus.unfocused', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('focus.unfocused');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.focused to unfocused on BLUR', () => {
      const transition = { from: 'focus.focused', event: 'BLUR', target: 'unfocused' };
      expect(transition.from).toBe('focus.focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('unfocused');
      // Target state 'unfocused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"completion.empty","events":["INPUT","PASTE"]},{"name":"completion.partial","events":["FILL_ALL","CLEAR_ALL","INPUT","DELETE_CHAR"]},{"name":"completion.complete","events":["DELETE_CHAR","CLEAR_ALL"]},{"name":"focus.unfocused","events":["FOCUS"]},{"name":"focus.focused","events":["BLUR"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["completion.empty","completion.partial","completion.complete","focus.unfocused","focus.focused"]);
      const baseNames = new Set(["empty","partial","complete","unfocused","focused"]);
      expect(validNames.has('partial') || baseNames.has('partial')).toBe(true);
      expect(validNames.has('partial') || baseNames.has('partial')).toBe(true);
      expect(validNames.has('complete') || baseNames.has('complete')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('partial') || baseNames.has('partial')).toBe(true);
      expect(validNames.has('partial') || baseNames.has('partial')).toBe(true);
      expect(validNames.has('partial') || baseNames.has('partial')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('unfocused') || baseNames.has('unfocused')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="root" to root', () => {
      const binding = {"part":"root","dataPart":"root"};
      expect(binding.dataPart).toBe('root');
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if state . completion = = complete then complete else if state . completion = = partial then partial else empty" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-disabled on root', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="label" to label', () => {
      const binding = {"part":"label","dataPart":"label"};
      expect(binding.dataPart).toBe('label');
    });

    it('should bind data-disabled on label', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="input" to input', () => {
      const binding = {"part":"input","dataPart":"input"};
      expect(binding.dataPart).toBe('input');
    });

    it('should bind data-state on input', () => {
      const attr = { name: 'data-state', value: "if input . value then filled else empty" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-focused on input', () => {
      const attr = { name: 'data-focused', value: "if input . isFocused then true else false" };
      expect(attr.name).toBe('data-focused');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-index on input', () => {
      const attr = { name: 'data-index', value: "input . index" };
      expect(attr.name).toBe('data-index');
      expect(attr.value).toBeDefined();
    });

    it('should bind onInput handler on input', () => {
      const handler = { event: 'onInput', action: "send ( INPUT , { index : input . index , value : input . inputValue } )" };
      expect(handler.event).toBe('onInput');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on input', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS , { index : input . index } )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on input', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPaste handler on input', () => {
      const handler = { event: 'onPaste', action: "send ( PASTE )" };
      expect(handler.event).toBe('onPaste');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Backspace handler on input', () => {
      const handler = { event: 'onKeyDown-Backspace', action: "send ( DELETE_BACK , { index : input . index } )" };
      expect(handler.event).toBe('onKeyDown-Backspace');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Delete handler on input', () => {
      const handler = { event: 'onKeyDown-Delete', action: "send ( DELETE_FORWARD , { index : input . index } )" };
      expect(handler.event).toBe('onKeyDown-Delete');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowLeft handler on input', () => {
      const handler = { event: 'onKeyDown-ArrowLeft', action: "send ( FOCUS_PREV , { index : input . index } )" };
      expect(handler.event).toBe('onKeyDown-ArrowLeft');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowRight handler on input', () => {
      const handler = { event: 'onKeyDown-ArrowRight', action: "send ( FOCUS_NEXT , { index : input . index } )" };
      expect(handler.event).toBe('onKeyDown-ArrowRight');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Home handler on input', () => {
      const handler = { event: 'onKeyDown-Home', action: "send ( FOCUS_FIRST )" };
      expect(handler.event).toBe('onKeyDown-Home');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-End handler on input', () => {
      const handler = { event: 'onKeyDown-End', action: "send ( FOCUS_LAST )" };
      expect(handler.event).toBe('onKeyDown-End');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="separator" to separator', () => {
      const binding = {"part":"separator","dataPart":"separator"};
      expect(binding.dataPart).toBe('separator');
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","label","input","separator"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('label')).toBe(true);
      expect(anatomyParts.has('input')).toBe(true);
      expect(anatomyParts.has('separator')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Backspace key with action: DELETE_BACK', () => {
      const binding = { key: 'Backspace', action: 'DELETE_BACK' };
      expect(binding.key).toBe('Backspace');
      expect(binding.action).toBe('DELETE_BACK');
    });

    it('should handle Delete key with action: DELETE_FORWARD', () => {
      const binding = { key: 'Delete', action: 'DELETE_FORWARD' };
      expect(binding.key).toBe('Delete');
      expect(binding.action).toBe('DELETE_FORWARD');
    });

    it('should handle ArrowLeft key with action: FOCUS_PREV', () => {
      const binding = { key: 'ArrowLeft', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should handle ArrowRight key with action: FOCUS_NEXT', () => {
      const binding = { key: 'ArrowRight', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Home key with action: FOCUS_FIRST', () => {
      const binding = { key: 'Home', action: 'FOCUS_FIRST' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('FOCUS_FIRST');
    });

    it('should handle End key with action: FOCUS_LAST', () => {
      const binding = { key: 'End', action: 'FOCUS_LAST' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('FOCUS_LAST');
    });

    it('should handle Mod+V key with action: PASTE', () => {
      const binding = { key: 'Mod+V', action: 'PASTE' };
      expect(binding.key).toBe('Mod+V');
      expect(binding.action).toBe('PASTE');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["INPUT","PASTE","FILL_ALL","CLEAR_ALL","DELETE_CHAR","FOCUS","BLUR"]);
      const keyboardActions = ["DELETE_BACK","DELETE_FORWARD","FOCUS_PREV","FOCUS_NEXT","FOCUS_FIRST","FOCUS_LAST","PASTE"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "group"', () => {
      expect('group').toBeTruthy();
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? label" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "PIN input" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on input', () => {
      const ariaAttr = { part: 'input', name: 'role', value: "textbox" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on input', () => {
      const ariaAttr = { part: 'input', name: 'aria-label', value: "concat ( Digit  , input . index + 1 ,  of  , ? length )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-required on input', () => {
      const ariaAttr = { part: 'input', name: 'aria-required', value: "if ? required then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-disabled on input', () => {
      const ariaAttr = { part: 'input', name: 'aria-disabled', value: "if ? disabled then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-invalid on input', () => {
      const ariaAttr = { part: 'input', name: 'aria-invalid', value: "if state . validation = = invalid then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind inputmode on input', () => {
      const ariaAttr = { part: 'input', name: 'inputmode', value: "if ? type = = numeric then numeric else text" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind autocomplete on input', () => {
      const ariaAttr = { part: 'input', name: 'autocomplete', value: "one-time-code" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on separator', () => {
      const ariaAttr = { part: 'separator', name: 'role', value: "separator" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on separator', () => {
      const ariaAttr = { part: 'separator', name: 'aria-hidden', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? label" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-roledescription', value: "PIN input" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part input', () => {
      const connectAria = { part: 'input', attr: 'role', value: "textbox" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part input', () => {
      const connectAria = { part: 'input', attr: 'aria-label', value: "concat ( Digit  , input . index + 1 ,  of  , ? length )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-required on connect part input', () => {
      const connectAria = { part: 'input', attr: 'aria-required', value: "if ? required then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-disabled on connect part input', () => {
      const connectAria = { part: 'input', attr: 'aria-disabled', value: "if ? disabled then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part separator', () => {
      const connectAria = { part: 'separator', attr: 'role', value: "separator" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part separator', () => {
      const connectAria = { part: 'separator', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "length" with type "Int"', () => {
      const prop = { name: 'length', type: 'Int', defaultValue: "6" };
      expect(prop.name).toBe('length');
      expect(prop.type).toBeTruthy();
    });

    it('should default "length" to "6"', () => {
      expect("6").toBe("6");
    });

    it('should declare prop "type" with type "numeric | alphanumeric"', () => {
      const prop = { name: 'type', type: 'numeric | alphanumeric', defaultValue: "numeric" };
      expect(prop.name).toBe('type');
      expect(prop.type).toBeTruthy();
    });

    it('should default "type" to "numeric"', () => {
      expect("numeric").toBe("numeric");
    });

    it('should declare prop "mask" with type "Bool"', () => {
      const prop = { name: 'mask', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('mask');
      expect(prop.type).toBeTruthy();
    });

    it('should default "mask" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "required" with type "Bool"', () => {
      const prop = { name: 'required', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('required');
      expect(prop.type).toBeTruthy();
    });

    it('should default "required" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "autoFocus" with type "Bool"', () => {
      const prop = { name: 'autoFocus', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('autoFocus');
      expect(prop.type).toBeTruthy();
    });

    it('should default "autoFocus" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "placeholder" with type "String"', () => {
      const prop = { name: 'placeholder', type: 'String', defaultValue: "" };
      expect(prop.name).toBe('placeholder');
      expect(prop.type).toBeTruthy();
    });

    it('should default "placeholder" to ""', () => {
      expect("").toBe("");
    });

    it('should declare prop "name" with type "option String"', () => {
      const prop = { name: 'name', type: 'option String' };
      expect(prop.name).toBe('name');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "label" with type "String"', () => {
      const prop = { name: 'label', type: 'String', defaultValue: "Verification code" };
      expect(prop.name).toBe('label');
      expect(prop.type).toBeTruthy();
    });

    it('should default "label" to "Verification code"', () => {
      expect("Verification code").toBe("Verification code");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","label","input","separator"];
      expect(parts.length).toBe(4);
      expect(parts).toContain('root');
      expect(parts).toContain('label');
      expect(parts).toContain('input');
      expect(parts).toContain('separator');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('text').toBeTruthy(); // label: text
      expect('action').toBeTruthy(); // input: action
      expect('container').toBeTruthy(); // separator: container
    });

  });

});
