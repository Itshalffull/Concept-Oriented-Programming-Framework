// Auto-generated by generate-widget-tests.ts from faceted-search.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: FacetedSearch', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["search.idle","facetGroup.expanded","facetShowMore.truncated"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('search.idle');
      expect(initialStates).toContain('facetGroup.expanded');
      expect(initialStates).toContain('facetShowMore.truncated');
    });

    it('should transition from search.idle to searching on SEARCH', () => {
      const transition = { from: 'search.idle', event: 'SEARCH', target: 'searching' };
      expect(transition.from).toBe('search.idle');
      expect(transition.event).toBe('SEARCH');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.idle to searching on APPLY_FACET', () => {
      const transition = { from: 'search.idle', event: 'APPLY_FACET', target: 'searching' };
      expect(transition.from).toBe('search.idle');
      expect(transition.event).toBe('APPLY_FACET');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.idle to searching on REMOVE_FACET', () => {
      const transition = { from: 'search.idle', event: 'REMOVE_FACET', target: 'searching' };
      expect(transition.from).toBe('search.idle');
      expect(transition.event).toBe('REMOVE_FACET');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.idle to searching on CLEAR_ALL', () => {
      const transition = { from: 'search.idle', event: 'CLEAR_ALL', target: 'searching' };
      expect(transition.from).toBe('search.idle');
      expect(transition.event).toBe('CLEAR_ALL');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.searching to hasResults on SEARCH_COMPLETE', () => {
      const transition = { from: 'search.searching', event: 'SEARCH_COMPLETE', target: 'hasResults' };
      expect(transition.from).toBe('search.searching');
      expect(transition.event).toBe('SEARCH_COMPLETE');
      expect(transition.target).toBe('hasResults');
      // Target state 'hasResults' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.searching to noResults on SEARCH_COMPLETE_EMPTY', () => {
      const transition = { from: 'search.searching', event: 'SEARCH_COMPLETE_EMPTY', target: 'noResults' };
      expect(transition.from).toBe('search.searching');
      expect(transition.event).toBe('SEARCH_COMPLETE_EMPTY');
      expect(transition.target).toBe('noResults');
      // Target state 'noResults' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.searching to error on SEARCH_ERROR', () => {
      const transition = { from: 'search.searching', event: 'SEARCH_ERROR', target: 'error' };
      expect(transition.from).toBe('search.searching');
      expect(transition.event).toBe('SEARCH_ERROR');
      expect(transition.target).toBe('error');
      // Target state 'error' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.hasResults to searching on SEARCH', () => {
      const transition = { from: 'search.hasResults', event: 'SEARCH', target: 'searching' };
      expect(transition.from).toBe('search.hasResults');
      expect(transition.event).toBe('SEARCH');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.hasResults to searching on APPLY_FACET', () => {
      const transition = { from: 'search.hasResults', event: 'APPLY_FACET', target: 'searching' };
      expect(transition.from).toBe('search.hasResults');
      expect(transition.event).toBe('APPLY_FACET');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.hasResults to searching on REMOVE_FACET', () => {
      const transition = { from: 'search.hasResults', event: 'REMOVE_FACET', target: 'searching' };
      expect(transition.from).toBe('search.hasResults');
      expect(transition.event).toBe('REMOVE_FACET');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.hasResults to searching on CLEAR_ALL', () => {
      const transition = { from: 'search.hasResults', event: 'CLEAR_ALL', target: 'searching' };
      expect(transition.from).toBe('search.hasResults');
      expect(transition.event).toBe('CLEAR_ALL');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.hasResults to searching on PAGE_CHANGE', () => {
      const transition = { from: 'search.hasResults', event: 'PAGE_CHANGE', target: 'searching' };
      expect(transition.from).toBe('search.hasResults');
      expect(transition.event).toBe('PAGE_CHANGE');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.noResults to searching on SEARCH', () => {
      const transition = { from: 'search.noResults', event: 'SEARCH', target: 'searching' };
      expect(transition.from).toBe('search.noResults');
      expect(transition.event).toBe('SEARCH');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.noResults to searching on REMOVE_FACET', () => {
      const transition = { from: 'search.noResults', event: 'REMOVE_FACET', target: 'searching' };
      expect(transition.from).toBe('search.noResults');
      expect(transition.event).toBe('REMOVE_FACET');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.noResults to searching on CLEAR_ALL', () => {
      const transition = { from: 'search.noResults', event: 'CLEAR_ALL', target: 'searching' };
      expect(transition.from).toBe('search.noResults');
      expect(transition.event).toBe('CLEAR_ALL');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.error to searching on RETRY', () => {
      const transition = { from: 'search.error', event: 'RETRY', target: 'searching' };
      expect(transition.from).toBe('search.error');
      expect(transition.event).toBe('RETRY');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from search.error to searching on SEARCH', () => {
      const transition = { from: 'search.error', event: 'SEARCH', target: 'searching' };
      expect(transition.from).toBe('search.error');
      expect(transition.event).toBe('SEARCH');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from facetGroup.expanded to collapsed on COLLAPSE_FACET', () => {
      const transition = { from: 'facetGroup.expanded', event: 'COLLAPSE_FACET', target: 'collapsed' };
      expect(transition.from).toBe('facetGroup.expanded');
      expect(transition.event).toBe('COLLAPSE_FACET');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from facetGroup.collapsed to expanded on EXPAND_FACET', () => {
      const transition = { from: 'facetGroup.collapsed', event: 'EXPAND_FACET', target: 'expanded' };
      expect(transition.from).toBe('facetGroup.collapsed');
      expect(transition.event).toBe('EXPAND_FACET');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from facetShowMore.truncated to full on SHOW_MORE', () => {
      const transition = { from: 'facetShowMore.truncated', event: 'SHOW_MORE', target: 'full' };
      expect(transition.from).toBe('facetShowMore.truncated');
      expect(transition.event).toBe('SHOW_MORE');
      expect(transition.target).toBe('full');
      // Target state 'full' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from facetShowMore.full to truncated on SHOW_LESS', () => {
      const transition = { from: 'facetShowMore.full', event: 'SHOW_LESS', target: 'truncated' };
      expect(transition.from).toBe('facetShowMore.full');
      expect(transition.event).toBe('SHOW_LESS');
      expect(transition.target).toBe('truncated');
      // Target state 'truncated' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"search.idle","events":["SEARCH","APPLY_FACET","REMOVE_FACET","CLEAR_ALL"]},{"name":"search.searching","events":["SEARCH_COMPLETE","SEARCH_COMPLETE_EMPTY","SEARCH_ERROR"]},{"name":"search.hasResults","events":["SEARCH","APPLY_FACET","REMOVE_FACET","CLEAR_ALL","PAGE_CHANGE"]},{"name":"search.noResults","events":["SEARCH","REMOVE_FACET","CLEAR_ALL"]},{"name":"search.error","events":["RETRY","SEARCH"]},{"name":"facetGroup.expanded","events":["COLLAPSE_FACET"]},{"name":"facetGroup.collapsed","events":["EXPAND_FACET"]},{"name":"facetShowMore.truncated","events":["SHOW_MORE"]},{"name":"facetShowMore.full","events":["SHOW_LESS"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["search.idle","search.searching","search.hasResults","search.noResults","search.error","facetGroup.expanded","facetGroup.collapsed","facetShowMore.truncated","facetShowMore.full"]);
      const baseNames = new Set(["idle","searching","hasResults","noResults","error","expanded","collapsed","truncated","full"]);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('hasResults') || baseNames.has('hasResults')).toBe(true);
      expect(validNames.has('noResults') || baseNames.has('noResults')).toBe(true);
      expect(validNames.has('error') || baseNames.has('error')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('full') || baseNames.has('full')).toBe(true);
      expect(validNames.has('truncated') || baseNames.has('truncated')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if ? loading then loading else if ? results . length = = 0 then empty else has-results" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="search-input" to searchInput', () => {
      const binding = {"part":"searchInput","dataPart":"search-input"};
      expect(binding.dataPart).toBe('search-input');
    });

    it('should bind onChange handler on searchInput', () => {
      const handler = { event: 'onChange', action: "send ( SEARCH )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Escape handler on searchInput', () => {
      const handler = { event: 'onKeyDown-Escape', action: "send ( CLEAR_SEARCH )" };
      expect(handler.event).toBe('onKeyDown-Escape');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="facet-sidebar" to facetSidebar', () => {
      const binding = {"part":"facetSidebar","dataPart":"facet-sidebar"};
      expect(binding.dataPart).toBe('facet-sidebar');
    });

    it('should bind data-facet on facetGroup', () => {
      const attr = { name: 'data-facet', value: "self . facetKey" };
      expect(attr.name).toBe('data-facet');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on facetGroup', () => {
      const attr = { name: 'data-state', value: "if state . facetGroup = = expanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="facet-label" to facetLabel', () => {
      const binding = {"part":"facetLabel","dataPart":"facet-label"};
      expect(binding.dataPart).toBe('facet-label');
    });

    it('should bind onClick handler on facetLabel', () => {
      const handler = { event: 'onClick', action: "if state . facetGroup = = expanded then send ( COLLAPSE_FACET ) else send ( EXPAND_FACET )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-facet on facetItem', () => {
      const attr = { name: 'data-facet', value: "self . facetKey" };
      expect(attr.name).toBe('data-facet');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-value on facetItem', () => {
      const attr = { name: 'data-value', value: "self . value" };
      expect(attr.name).toBe('data-value');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-active on facetItem', () => {
      const attr = { name: 'data-active', value: "if self . active then true else false" };
      expect(attr.name).toBe('data-active');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-count on facetItem', () => {
      const attr = { name: 'data-count', value: "self . count" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind onChange handler on facetItem', () => {
      const handler = { event: 'onChange', action: "if self . active then send ( REMOVE_FACET , { facet : self . facetKey , value : self . value } ) else send ( APPLY_FACET , { facet : self . facetKey , value : self . value } )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="facet-count" to facetCount', () => {
      const binding = {"part":"facetCount","dataPart":"facet-count"};
      expect(binding.dataPart).toBe('facet-count');
    });

    it('should bind data-state on facetShowMore', () => {
      const attr = { name: 'data-state', value: "if state . facetShowMore = = truncated then truncated else full" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on facetShowMore', () => {
      const handler = { event: 'onClick', action: "if state . facetShowMore = = truncated then send ( SHOW_MORE ) else send ( SHOW_LESS )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="range-slider" to rangeSlider', () => {
      const binding = {"part":"rangeSlider","dataPart":"range-slider"};
      expect(binding.dataPart).toBe('range-slider');
    });

    it('should bind data-facet on rangeSlider', () => {
      const attr = { name: 'data-facet', value: "self . facetKey" };
      expect(attr.name).toBe('data-facet');
      expect(attr.value).toBeDefined();
    });

    it('should bind onChange handler on rangeSlider', () => {
      const handler = { event: 'onChange', action: "send ( APPLY_FACET , { facet : self . facetKey , value : self . currentRange } )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-count on activeFilters', () => {
      const attr = { name: 'data-count', value: "? activeFilters . length" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-facet on activeChip', () => {
      const attr = { name: 'data-facet', value: "self . facetKey" };
      expect(attr.name).toBe('data-facet');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-value on activeChip', () => {
      const attr = { name: 'data-value', value: "self . value" };
      expect(attr.name).toBe('data-value');
      expect(attr.value).toBeDefined();
    });

    it('should bind onRemove handler on activeChip', () => {
      const handler = { event: 'onRemove', action: "send ( REMOVE_FACET , { facet : self . facetKey , value : self . value } )" };
      expect(handler.event).toBe('onRemove');
      expect(handler.action).toBeDefined();
    });

    it('should bind onClick handler on clearAllButton', () => {
      const handler = { event: 'onClick', action: "send ( CLEAR_ALL )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-state on results', () => {
      const attr = { name: 'data-state', value: "if ? loading then loading else if ? results . length = = 0 then empty else has-results" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="result-list" to resultList', () => {
      const binding = {"part":"resultList","dataPart":"result-list"};
      expect(binding.dataPart).toBe('result-list');
    });

    it('should bind data-count on resultList', () => {
      const attr = { name: 'data-count', value: "? results . length" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="pagination" to pagination', () => {
      const binding = {"part":"pagination","dataPart":"pagination"};
      expect(binding.dataPart).toBe('pagination');
    });

    it('should bind data-part="empty-state" to emptyState', () => {
      const binding = {"part":"emptyState","dataPart":"empty-state"};
      expect(binding.dataPart).toBe('empty-state');
    });

    it('should bind data-part="loading-state" to loadingState', () => {
      const binding = {"part":"loadingState","dataPart":"loading-state"};
      expect(binding.dataPart).toBe('loading-state');
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","searchInput","facetSidebar","facetGroup","facetLabel","facetItems","facetItem","facetCount","facetShowMore","rangeSlider","activeFilters","activeChip","clearAllButton","results","resultCount","resultList","pagination","emptyState","loadingState"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('searchInput')).toBe(true);
      expect(anatomyParts.has('facetSidebar')).toBe(true);
      expect(anatomyParts.has('facetGroup')).toBe(true);
      expect(anatomyParts.has('facetLabel')).toBe(true);
      expect(anatomyParts.has('facetItems')).toBe(true);
      expect(anatomyParts.has('facetItem')).toBe(true);
      expect(anatomyParts.has('facetCount')).toBe(true);
      expect(anatomyParts.has('facetShowMore')).toBe(true);
      expect(anatomyParts.has('rangeSlider')).toBe(true);
      expect(anatomyParts.has('activeFilters')).toBe(true);
      expect(anatomyParts.has('activeChip')).toBe(true);
      expect(anatomyParts.has('clearAllButton')).toBe(true);
      expect(anatomyParts.has('results')).toBe(true);
      expect(anatomyParts.has('resultCount')).toBe(true);
      expect(anatomyParts.has('resultList')).toBe(true);
      expect(anatomyParts.has('pagination')).toBe(true);
      expect(anatomyParts.has('emptyState')).toBe(true);
      expect(anatomyParts.has('loadingState')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: SEARCH', () => {
      const binding = { key: 'Enter', action: 'SEARCH' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('SEARCH');
    });

    it('should handle Escape key with action: CLEAR_SEARCH', () => {
      const binding = { key: 'Escape', action: 'CLEAR_SEARCH' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('CLEAR_SEARCH');
    });

    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should handle Space key with action: TOGGLE_FACET', () => {
      const binding = { key: 'Space', action: 'TOGGLE_FACET' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('TOGGLE_FACET');
    });

    it('should handle ArrowDown key with action: NAVIGATE_NEXT_FACET', () => {
      const binding = { key: 'ArrowDown', action: 'NAVIGATE_NEXT_FACET' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('NAVIGATE_NEXT_FACET');
    });

    it('should handle ArrowUp key with action: NAVIGATE_PREV_FACET', () => {
      const binding = { key: 'ArrowUp', action: 'NAVIGATE_PREV_FACET' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('NAVIGATE_PREV_FACET');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SEARCH","APPLY_FACET","REMOVE_FACET","CLEAR_ALL","SEARCH_COMPLETE","SEARCH_COMPLETE_EMPTY","SEARCH_ERROR","PAGE_CHANGE","RETRY","COLLAPSE_FACET","EXPAND_FACET","SHOW_MORE","SHOW_LESS"]);
      const keyboardActions = ["SEARCH","CLEAR_SEARCH","FOCUS_NEXT","FOCUS_PREV","TOGGLE_FACET","NAVIGATE_NEXT_FACET","NAVIGATE_PREV_FACET"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "search"', () => {
      expect('search').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "searchInput"', () => {
      expect('searchInput').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "search" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Faceted search" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on facetSidebar', () => {
      const ariaAttr = { part: 'facetSidebar', name: 'role', value: "complementary" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on facetSidebar', () => {
      const ariaAttr = { part: 'facetSidebar', name: 'aria-label', value: "Search filters" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on facetGroup', () => {
      const ariaAttr = { part: 'facetGroup', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-labelledby on facetGroup', () => {
      const ariaAttr = { part: 'facetGroup', name: 'aria-labelledby', value: "self . facetLabelId" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on facetGroup', () => {
      const ariaAttr = { part: 'facetGroup', name: 'aria-expanded', value: "if state . facetGroup = = expanded then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on facetItems', () => {
      const ariaAttr = { part: 'facetItems', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on facetItems', () => {
      const ariaAttr = { part: 'facetItems', name: 'aria-label', value: "self . facetName" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on activeFilters', () => {
      const ariaAttr = { part: 'activeFilters', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on activeFilters', () => {
      const ariaAttr = { part: 'activeFilters', name: 'aria-label', value: "Active filters" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on activeChip', () => {
      const ariaAttr = { part: 'activeChip', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on results', () => {
      const ariaAttr = { part: 'results', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on results', () => {
      const ariaAttr = { part: 'results', name: 'aria-label', value: "Search results" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on results', () => {
      const ariaAttr = { part: 'results', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on resultCount', () => {
      const ariaAttr = { part: 'resultCount', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-atomic on resultCount', () => {
      const ariaAttr = { part: 'resultCount', name: 'aria-atomic', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "search" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "Faceted search" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part searchInput', () => {
      const connectAria = { part: 'searchInput', attr: 'aria-label', value: "Search" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part facetSidebar', () => {
      const connectAria = { part: 'facetSidebar', attr: 'role', value: "complementary" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part facetSidebar', () => {
      const connectAria = { part: 'facetSidebar', attr: 'aria-label', value: "Search filters" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part facetGroup', () => {
      const connectAria = { part: 'facetGroup', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-labelledby on connect part facetGroup', () => {
      const connectAria = { part: 'facetGroup', attr: 'aria-labelledby', value: "concat ( facet-label- , self . facetKey )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part facetGroup', () => {
      const connectAria = { part: 'facetGroup', attr: 'aria-expanded', value: "if state . facetGroup = = expanded then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part facetItems', () => {
      const connectAria = { part: 'facetItems', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part facetItems', () => {
      const connectAria = { part: 'facetItems', attr: 'aria-label', value: "self . facetName" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part facetCount', () => {
      const connectAria = { part: 'facetCount', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part facetShowMore', () => {
      const connectAria = { part: 'facetShowMore', attr: 'aria-label', value: "if state . facetShowMore = = truncated then concat ( Show more  , self . facetName ) else concat ( Show less  , self . facetName )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part rangeSlider', () => {
      const connectAria = { part: 'rangeSlider', attr: 'aria-label', value: "concat ( self . facetName ,  range )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part activeFilters', () => {
      const connectAria = { part: 'activeFilters', attr: 'role', value: "list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part activeFilters', () => {
      const connectAria = { part: 'activeFilters', attr: 'aria-label', value: "Active filters" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part activeChip', () => {
      const connectAria = { part: 'activeChip', attr: 'role', value: "listitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part activeChip', () => {
      const connectAria = { part: 'activeChip', attr: 'aria-label', value: "concat ( self . facetName , :  , self . displayValue )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part clearAllButton', () => {
      const connectAria = { part: 'clearAllButton', attr: 'aria-label', value: "Clear all filters" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part results', () => {
      const connectAria = { part: 'results', attr: 'role', value: "region" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part results', () => {
      const connectAria = { part: 'results', attr: 'aria-label', value: "Search results" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-live on connect part results', () => {
      const connectAria = { part: 'results', attr: 'aria-live', value: "polite" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-busy on connect part results', () => {
      const connectAria = { part: 'results', attr: 'aria-busy', value: "if ? loading then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-live on connect part resultCount', () => {
      const connectAria = { part: 'resultCount', attr: 'aria-live', value: "polite" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-atomic on connect part resultCount', () => {
      const connectAria = { part: 'resultCount', attr: 'aria-atomic', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part pagination', () => {
      const connectAria = { part: 'pagination', attr: 'aria-label', value: "Search results pagination" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part emptyState', () => {
      const connectAria = { part: 'emptyState', attr: 'aria-hidden', value: "if ? results . length > 0 then true else if ? loading then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part loadingState', () => {
      const connectAria = { part: 'loadingState', attr: 'aria-hidden', value: "if not ? loading then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "query" with type "String"', () => {
      const prop = { name: 'query', type: 'String', defaultValue: "" };
      expect(prop.name).toBe('query');
      expect(prop.type).toBeTruthy();
    });

    it('should default "query" to ""', () => {
      expect("").toBe("");
    });

    it('should declare prop "facets" with type "list FacetDef"', () => {
      const prop = { name: 'facets', type: 'list FacetDef' };
      expect(prop.name).toBe('facets');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "activeFilters" with type "list ActiveFilter"', () => {
      const prop = { name: 'activeFilters', type: 'list ActiveFilter', defaultValue: "[ ]" };
      expect(prop.name).toBe('activeFilters');
      expect(prop.type).toBeTruthy();
    });

    it('should default "activeFilters" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "results" with type "list Object"', () => {
      const prop = { name: 'results', type: 'list Object', defaultValue: "[ ]" };
      expect(prop.name).toBe('results');
      expect(prop.type).toBeTruthy();
    });

    it('should default "results" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "totalCount" with type "Int"', () => {
      const prop = { name: 'totalCount', type: 'Int', defaultValue: "0" };
      expect(prop.name).toBe('totalCount');
      expect(prop.type).toBeTruthy();
    });

    it('should default "totalCount" to "0"', () => {
      expect("0").toBe("0");
    });

    it('should declare prop "page" with type "Int"', () => {
      const prop = { name: 'page', type: 'Int', defaultValue: "1" };
      expect(prop.name).toBe('page');
      expect(prop.type).toBeTruthy();
    });

    it('should default "page" to "1"', () => {
      expect("1").toBe("1");
    });

    it('should declare prop "pageSize" with type "Int"', () => {
      const prop = { name: 'pageSize', type: 'Int', defaultValue: "20" };
      expect(prop.name).toBe('pageSize');
      expect(prop.type).toBeTruthy();
    });

    it('should default "pageSize" to "20"', () => {
      expect("20").toBe("20");
    });

    it('should declare prop "loading" with type "Bool"', () => {
      const prop = { name: 'loading', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('loading');
      expect(prop.type).toBeTruthy();
    });

    it('should default "loading" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "facetTruncateAt" with type "Int"', () => {
      const prop = { name: 'facetTruncateAt', type: 'Int', defaultValue: "5" };
      expect(prop.name).toBe('facetTruncateAt');
      expect(prop.type).toBeTruthy();
    });

    it('should default "facetTruncateAt" to "5"', () => {
      expect("5").toBe("5");
    });

    it('should declare prop "showResultCount" with type "Bool"', () => {
      const prop = { name: 'showResultCount', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showResultCount');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showResultCount" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showPagination" with type "Bool"', () => {
      const prop = { name: 'showPagination', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showPagination');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showPagination" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "placeholder" with type "String"', () => {
      const prop = { name: 'placeholder', type: 'String', defaultValue: "Search..." };
      expect(prop.name).toBe('placeholder');
      expect(prop.type).toBeTruthy();
    });

    it('should default "placeholder" to "Search..."', () => {
      expect("Search...").toBe("Search...");
    });

    it('should declare prop "emptyMessage" with type "String"', () => {
      const prop = { name: 'emptyMessage', type: 'String', defaultValue: "No results found" };
      expect(prop.name).toBe('emptyMessage');
      expect(prop.type).toBeTruthy();
    });

    it('should default "emptyMessage" to "No results found"', () => {
      expect("No results found").toBe("No results found");
    });

    it('should declare prop "onChange" with type "option Function"', () => {
      const prop = { name: 'onChange', type: 'option Function' };
      expect(prop.name).toBe('onChange');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "onSearch" with type "option Function"', () => {
      const prop = { name: 'onSearch', type: 'option Function' };
      expect(prop.name).toBe('onSearch');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "onPageChange" with type "option Function"', () => {
      const prop = { name: 'onPageChange', type: 'option Function' };
      expect(prop.name).toBe('onPageChange');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","searchInput","facetSidebar","facetGroup","facetLabel","facetItems","facetItem","facetCount","facetShowMore","rangeSlider","activeFilters","activeChip","clearAllButton","results","resultCount","resultList","pagination","emptyState","loadingState"];
      expect(parts.length).toBe(19);
      expect(parts).toContain('root');
      expect(parts).toContain('searchInput');
      expect(parts).toContain('facetSidebar');
      expect(parts).toContain('facetGroup');
      expect(parts).toContain('facetLabel');
      expect(parts).toContain('facetItems');
      expect(parts).toContain('facetItem');
      expect(parts).toContain('facetCount');
      expect(parts).toContain('facetShowMore');
      expect(parts).toContain('rangeSlider');
      expect(parts).toContain('activeFilters');
      expect(parts).toContain('activeChip');
      expect(parts).toContain('clearAllButton');
      expect(parts).toContain('results');
      expect(parts).toContain('resultCount');
      expect(parts).toContain('resultList');
      expect(parts).toContain('pagination');
      expect(parts).toContain('emptyState');
      expect(parts).toContain('loadingState');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('widget').toBeTruthy(); // searchInput: widget
      expect('container').toBeTruthy(); // facetSidebar: container
      expect('container').toBeTruthy(); // facetGroup: container
      expect('text').toBeTruthy(); // facetLabel: text
      expect('container').toBeTruthy(); // facetItems: container
      expect('widget').toBeTruthy(); // facetItem: widget
      expect('text').toBeTruthy(); // facetCount: text
      expect('action').toBeTruthy(); // facetShowMore: action
      expect('widget').toBeTruthy(); // rangeSlider: widget
      expect('container').toBeTruthy(); // activeFilters: container
      expect('widget').toBeTruthy(); // activeChip: widget
      expect('action').toBeTruthy(); // clearAllButton: action
      expect('container').toBeTruthy(); // results: container
      expect('text').toBeTruthy(); // resultCount: text
      expect('container').toBeTruthy(); // resultList: container
      expect('widget').toBeTruthy(); // pagination: widget
      expect('container').toBeTruthy(); // emptyState: container
      expect('container').toBeTruthy(); // loadingState: container
    });

  });

});
