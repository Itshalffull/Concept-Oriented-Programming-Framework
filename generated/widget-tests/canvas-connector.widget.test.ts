// Auto-generated by generate-widget-tests.ts from canvas-connector.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: CanvasConnector', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('idle');
    });

    it('should transition from idle to selected on SELECT', () => {
      const transition = { from: 'idle', event: 'SELECT', target: 'selected' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to hovered on HOVER', () => {
      const transition = { from: 'idle', event: 'HOVER', target: 'hovered' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('hovered');
      // Target state 'hovered' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to idle on UNHOVER', () => {
      const transition = { from: 'hovered', event: 'UNHOVER', target: 'idle' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('UNHOVER');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to selected on SELECT', () => {
      const transition = { from: 'hovered', event: 'SELECT', target: 'selected' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selected to idle on DESELECT', () => {
      const transition = { from: 'selected', event: 'DESELECT', target: 'idle' };
      expect(transition.from).toBe('selected');
      expect(transition.event).toBe('DESELECT');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selected to draggingStart on DRAG_START_HANDLE', () => {
      const transition = { from: 'selected', event: 'DRAG_START_HANDLE', target: 'draggingStart' };
      expect(transition.from).toBe('selected');
      expect(transition.event).toBe('DRAG_START_HANDLE');
      expect(transition.target).toBe('draggingStart');
      // Target state 'draggingStart' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selected to draggingEnd on DRAG_END_HANDLE', () => {
      const transition = { from: 'selected', event: 'DRAG_END_HANDLE', target: 'draggingEnd' };
      expect(transition.from).toBe('selected');
      expect(transition.event).toBe('DRAG_END_HANDLE');
      expect(transition.target).toBe('draggingEnd');
      // Target state 'draggingEnd' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selected to deleted on DELETE', () => {
      const transition = { from: 'selected', event: 'DELETE', target: 'deleted' };
      expect(transition.from).toBe('selected');
      expect(transition.event).toBe('DELETE');
      expect(transition.target).toBe('deleted');
      // Target state 'deleted' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selected to editingLabel on EDIT_LABEL', () => {
      const transition = { from: 'selected', event: 'EDIT_LABEL', target: 'editingLabel' };
      expect(transition.from).toBe('selected');
      expect(transition.event).toBe('EDIT_LABEL');
      expect(transition.target).toBe('editingLabel');
      // Target state 'editingLabel' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from draggingStart to selected on DROP', () => {
      const transition = { from: 'draggingStart', event: 'DROP', target: 'selected' };
      expect(transition.from).toBe('draggingStart');
      expect(transition.event).toBe('DROP');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from draggingStart to selected on CONNECT', () => {
      const transition = { from: 'draggingStart', event: 'CONNECT', target: 'selected' };
      expect(transition.from).toBe('draggingStart');
      expect(transition.event).toBe('CONNECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from draggingStart to selected on ESCAPE', () => {
      const transition = { from: 'draggingStart', event: 'ESCAPE', target: 'selected' };
      expect(transition.from).toBe('draggingStart');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from draggingEnd to selected on DROP', () => {
      const transition = { from: 'draggingEnd', event: 'DROP', target: 'selected' };
      expect(transition.from).toBe('draggingEnd');
      expect(transition.event).toBe('DROP');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from draggingEnd to selected on CONNECT', () => {
      const transition = { from: 'draggingEnd', event: 'CONNECT', target: 'selected' };
      expect(transition.from).toBe('draggingEnd');
      expect(transition.event).toBe('CONNECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from draggingEnd to selected on ESCAPE', () => {
      const transition = { from: 'draggingEnd', event: 'ESCAPE', target: 'selected' };
      expect(transition.from).toBe('draggingEnd');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editingLabel to selected on CONFIRM', () => {
      const transition = { from: 'editingLabel', event: 'CONFIRM', target: 'selected' };
      expect(transition.from).toBe('editingLabel');
      expect(transition.event).toBe('CONFIRM');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editingLabel to selected on ESCAPE', () => {
      const transition = { from: 'editingLabel', event: 'ESCAPE', target: 'selected' };
      expect(transition.from).toBe('editingLabel');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editingLabel to selected on BLUR', () => {
      const transition = { from: 'editingLabel', event: 'BLUR', target: 'selected' };
      expect(transition.from).toBe('editingLabel');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"idle","events":["SELECT","HOVER"]},{"name":"hovered","events":["UNHOVER","SELECT"]},{"name":"selected","events":["DESELECT","DRAG_START_HANDLE","DRAG_END_HANDLE","DELETE","EDIT_LABEL"]},{"name":"draggingStart","events":["DROP","CONNECT","ESCAPE"]},{"name":"draggingEnd","events":["DROP","CONNECT","ESCAPE"]},{"name":"editingLabel","events":["CONFIRM","ESCAPE","BLUR"]},{"name":"deleted","events":[]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["idle","hovered","selected","draggingStart","draggingEnd","editingLabel","deleted"]);
      const baseNames = new Set(["idle","hovered","selected","draggingStart","draggingEnd","editingLabel","deleted"]);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('hovered') || baseNames.has('hovered')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('draggingStart') || baseNames.has('draggingStart')).toBe(true);
      expect(validNames.has('draggingEnd') || baseNames.has('draggingEnd')).toBe(true);
      expect(validNames.has('deleted') || baseNames.has('deleted')).toBe(true);
      expect(validNames.has('editingLabel') || baseNames.has('editingLabel')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="connector" to root', () => {
      const binding = {"part":"root","dataPart":"connector"};
      expect(binding.dataPart).toBe('connector');
    });

    it('should bind data-id on root', () => {
      const attr = { name: 'data-id', value: "? id" };
      expect(attr.name).toBe('data-id');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "state" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-start-node on root', () => {
      const attr = { name: 'data-start-node', value: "? startNodeId" };
      expect(attr.name).toBe('data-start-node');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-end-node on root', () => {
      const attr = { name: 'data-end-node', value: "? endNodeId" };
      expect(attr.name).toBe('data-end-node');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-line-style on root', () => {
      const attr = { name: 'data-line-style', value: "? lineStyle" };
      expect(attr.name).toBe('data-line-style');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on root', () => {
      const handler = { event: 'onClick', action: "send ( SELECT , { id : ? id } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onDoubleClick handler on root', () => {
      const handler = { event: 'onDoubleClick', action: "send ( EDIT_LABEL , { id : ? id } )" };
      expect(handler.event).toBe('onDoubleClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Delete handler on root', () => {
      const handler = { event: 'onKeyDown-Delete', action: "send ( DELETE , { id : ? id } )" };
      expect(handler.event).toBe('onKeyDown-Delete');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on root', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( EDIT_LABEL , { id : ? id } )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Escape handler on root', () => {
      const handler = { event: 'onKeyDown-Escape', action: "send ( ESCAPE )" };
      expect(handler.event).toBe('onKeyDown-Escape');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerEnter handler on root', () => {
      const handler = { event: 'onPointerEnter', action: "send ( HOVER )" };
      expect(handler.event).toBe('onPointerEnter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerLeave handler on root', () => {
      const handler = { event: 'onPointerLeave', action: "send ( UNHOVER )" };
      expect(handler.event).toBe('onPointerLeave');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="path" to path', () => {
      const binding = {"part":"path","dataPart":"path"};
      expect(binding.dataPart).toBe('path');
    });

    it('should bind data-line-style on path', () => {
      const attr = { name: 'data-line-style', value: "? lineStyle" };
      expect(attr.name).toBe('data-line-style');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="start-handle" to startHandle', () => {
      const binding = {"part":"startHandle","dataPart":"start-handle"};
      expect(binding.dataPart).toBe('start-handle');
    });

    it('should bind data-visible on startHandle', () => {
      const attr = { name: 'data-visible', value: "if state = = selected or state = = hovered then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onPointerDown handler on startHandle', () => {
      const handler = { event: 'onPointerDown', action: "send ( DRAG_START_HANDLE , { id : ? id } )" };
      expect(handler.event).toBe('onPointerDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerUp handler on startHandle', () => {
      const handler = { event: 'onPointerUp', action: "send ( DROP )" };
      expect(handler.event).toBe('onPointerUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="end-handle" to endHandle', () => {
      const binding = {"part":"endHandle","dataPart":"end-handle"};
      expect(binding.dataPart).toBe('end-handle');
    });

    it('should bind data-visible on endHandle', () => {
      const attr = { name: 'data-visible', value: "if state = = selected or state = = hovered then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onPointerDown handler on endHandle', () => {
      const handler = { event: 'onPointerDown', action: "send ( DRAG_END_HANDLE , { id : ? id } )" };
      expect(handler.event).toBe('onPointerDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerUp handler on endHandle', () => {
      const handler = { event: 'onPointerUp', action: "send ( DROP )" };
      expect(handler.event).toBe('onPointerUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="label" to label', () => {
      const binding = {"part":"label","dataPart":"label"};
      expect(binding.dataPart).toBe('label');
    });

    it('should bind data-visible on label', () => {
      const attr = { name: 'data-visible', value: "if ? label then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onInput handler on label', () => {
      const handler = { event: 'onInput', action: "send ( LABEL_CHANGE , { id : ? id , value : self . textContent } )" };
      expect(handler.event).toBe('onInput');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on label', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on label', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( CONFIRM )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Escape handler on label', () => {
      const handler = { event: 'onKeyDown-Escape', action: "send ( ESCAPE )" };
      expect(handler.event).toBe('onKeyDown-Escape');
      expect(handler.action).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","path","startHandle","endHandle","label"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('path')).toBe(true);
      expect(anatomyParts.has('startHandle')).toBe(true);
      expect(anatomyParts.has('endHandle')).toBe(true);
      expect(anatomyParts.has('label')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: EDIT_LABEL', () => {
      const binding = { key: 'Enter', action: 'EDIT_LABEL' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('EDIT_LABEL');
    });

    it('should handle Delete key with action: DELETE', () => {
      const binding = { key: 'Delete', action: 'DELETE' };
      expect(binding.key).toBe('Delete');
      expect(binding.action).toBe('DELETE');
    });

    it('should handle Backspace key with action: DELETE', () => {
      const binding = { key: 'Backspace', action: 'DELETE' };
      expect(binding.key).toBe('Backspace');
      expect(binding.action).toBe('DELETE');
    });

    it('should handle Escape key with action: ESCAPE', () => {
      const binding = { key: 'Escape', action: 'ESCAPE' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('ESCAPE');
    });

    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SELECT","HOVER","UNHOVER","DESELECT","DRAG_START_HANDLE","DRAG_END_HANDLE","DELETE","EDIT_LABEL","DROP","CONNECT","ESCAPE","CONFIRM","BLUR"]);
      const keyboardActions = ["EDIT_LABEL","DELETE","DELETE","ESCAPE","FOCUS_NEXT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "img"', () => {
      expect('img').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "root"', () => {
      expect('root').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "img" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "concat ( Connection from  , ? startNodeLabel ,  to  , ? endNodeLabel , if ? label then concat ( :  , ? label ) else  )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "connector" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-selected', value: "if state = = selected then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on path', () => {
      const ariaAttr = { part: 'path', name: 'role', value: "presentation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on path', () => {
      const ariaAttr = { part: 'path', name: 'aria-hidden', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on startHandle', () => {
      const ariaAttr = { part: 'startHandle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on startHandle', () => {
      const ariaAttr = { part: 'startHandle', name: 'aria-label', value: "concat ( Start handle, connected to  , ? startNodeLabel )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-grabbed on startHandle', () => {
      const ariaAttr = { part: 'startHandle', name: 'aria-grabbed', value: "if state = = draggingStart then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on endHandle', () => {
      const ariaAttr = { part: 'endHandle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on endHandle', () => {
      const ariaAttr = { part: 'endHandle', name: 'aria-label', value: "concat ( End handle, connected to  , ? endNodeLabel )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-grabbed on endHandle', () => {
      const ariaAttr = { part: 'endHandle', name: 'aria-grabbed', value: "if state = = draggingEnd then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on label', () => {
      const ariaAttr = { part: 'label', name: 'role', value: "textbox" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on label', () => {
      const ariaAttr = { part: 'label', name: 'aria-label', value: "Connector label" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "concat ( Connection from  , ? startNodeLabel ,  to  , ? endNodeLabel , if ? label then concat ( :  , ? label ) else  )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-roledescription', value: "connector" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-selected', value: "if state = = selected then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part path', () => {
      const connectAria = { part: 'path', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part startHandle', () => {
      const connectAria = { part: 'startHandle', attr: 'aria-label', value: "concat ( Start handle, connected to  , ? startNodeLabel )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-grabbed on connect part startHandle', () => {
      const connectAria = { part: 'startHandle', attr: 'aria-grabbed', value: "if state = = draggingStart then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part endHandle', () => {
      const connectAria = { part: 'endHandle', attr: 'aria-label', value: "concat ( End handle, connected to  , ? endNodeLabel )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-grabbed on connect part endHandle', () => {
      const connectAria = { part: 'endHandle', attr: 'aria-grabbed', value: "if state = = draggingEnd then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "startNodeId" with type "String"', () => {
      const prop = { name: 'startNodeId', type: 'String' };
      expect(prop.name).toBe('startNodeId');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "endNodeId" with type "String"', () => {
      const prop = { name: 'endNodeId', type: 'String' };
      expect(prop.name).toBe('endNodeId');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "startNodeLabel" with type "String"', () => {
      const prop = { name: 'startNodeLabel', type: 'String', defaultValue: "" };
      expect(prop.name).toBe('startNodeLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "startNodeLabel" to ""', () => {
      expect("").toBe("");
    });

    it('should declare prop "endNodeLabel" with type "String"', () => {
      const prop = { name: 'endNodeLabel', type: 'String', defaultValue: "" };
      expect(prop.name).toBe('endNodeLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "endNodeLabel" to ""', () => {
      expect("").toBe("");
    });

    it('should declare prop "startPort" with type "option String"', () => {
      const prop = { name: 'startPort', type: 'option String' };
      expect(prop.name).toBe('startPort');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "endPort" with type "option String"', () => {
      const prop = { name: 'endPort', type: 'option String' };
      expect(prop.name).toBe('endPort');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "label" with type "option String"', () => {
      const prop = { name: 'label', type: 'option String' };
      expect(prop.name).toBe('label');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "lineStyle" with type "union straight | curved | step"', () => {
      const prop = { name: 'lineStyle', type: 'union straight | curved | step', defaultValue: "curved" };
      expect(prop.name).toBe('lineStyle');
      expect(prop.type).toBeTruthy();
    });

    it('should default "lineStyle" to "curved"', () => {
      expect("curved").toBe("curved");
    });

    it('should declare prop "arrowStart" with type "Bool"', () => {
      const prop = { name: 'arrowStart', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('arrowStart');
      expect(prop.type).toBeTruthy();
    });

    it('should default "arrowStart" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "arrowEnd" with type "Bool"', () => {
      const prop = { name: 'arrowEnd', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('arrowEnd');
      expect(prop.type).toBeTruthy();
    });

    it('should default "arrowEnd" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "color" with type "String"', () => {
      const prop = { name: 'color', type: 'String', defaultValue: "#000000" };
      expect(prop.name).toBe('color');
      expect(prop.type).toBeTruthy();
    });

    it('should default "color" to "#000000"', () => {
      expect("#000000").toBe("#000000");
    });

    it('should declare prop "strokeWidth" with type "Float"', () => {
      const prop = { name: 'strokeWidth', type: 'Float', defaultValue: "2 . 0" };
      expect(prop.name).toBe('strokeWidth');
      expect(prop.type).toBeTruthy();
    });

    it('should default "strokeWidth" to "2 . 0"', () => {
      expect("2 . 0").toBe("2 . 0");
    });

    it('should declare prop "dashed" with type "Bool"', () => {
      const prop = { name: 'dashed', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('dashed');
      expect(prop.type).toBeTruthy();
    });

    it('should default "dashed" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "id" with type "String"', () => {
      const prop = { name: 'id', type: 'String' };
      expect(prop.name).toBe('id');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","path","startHandle","endHandle","label"];
      expect(parts.length).toBe(5);
      expect(parts).toContain('root');
      expect(parts).toContain('path');
      expect(parts).toContain('startHandle');
      expect(parts).toContain('endHandle');
      expect(parts).toContain('label');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // path: container
      expect('action').toBeTruthy(); // startHandle: action
      expect('action').toBeTruthy(); // endHandle: action
      expect('text').toBeTruthy(); // label: text
    });

  });

});
