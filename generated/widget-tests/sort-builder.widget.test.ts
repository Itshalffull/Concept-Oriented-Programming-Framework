// Auto-generated by generate-widget-tests.ts from sort-builder.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: SortBuilder', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["sortCount.empty","drag.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('sortCount.empty');
      expect(initialStates).toContain('drag.idle');
    });

    it('should transition from sortCount.empty to hasSorts on ADD_SORT', () => {
      const transition = { from: 'sortCount.empty', event: 'ADD_SORT', target: 'hasSorts' };
      expect(transition.from).toBe('sortCount.empty');
      expect(transition.event).toBe('ADD_SORT');
      expect(transition.target).toBe('hasSorts');
      // Target state 'hasSorts' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from sortCount.empty to hasSorts on LOAD_SORTS', () => {
      const transition = { from: 'sortCount.empty', event: 'LOAD_SORTS', target: 'hasSorts' };
      expect(transition.from).toBe('sortCount.empty');
      expect(transition.event).toBe('LOAD_SORTS');
      expect(transition.target).toBe('hasSorts');
      // Target state 'hasSorts' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from sortCount.hasSorts to empty on REMOVE_LAST', () => {
      const transition = { from: 'sortCount.hasSorts', event: 'REMOVE_LAST', target: 'empty' };
      expect(transition.from).toBe('sortCount.hasSorts');
      expect(transition.event).toBe('REMOVE_LAST');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from sortCount.hasSorts to empty on CLEAR_ALL', () => {
      const transition = { from: 'sortCount.hasSorts', event: 'CLEAR_ALL', target: 'empty' };
      expect(transition.from).toBe('sortCount.hasSorts');
      expect(transition.event).toBe('CLEAR_ALL');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from sortCount.hasSorts to hasSorts on ADD_SORT', () => {
      const transition = { from: 'sortCount.hasSorts', event: 'ADD_SORT', target: 'hasSorts' };
      expect(transition.from).toBe('sortCount.hasSorts');
      expect(transition.event).toBe('ADD_SORT');
      expect(transition.target).toBe('hasSorts');
      // Target state 'hasSorts' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from drag.idle to dragging on DRAG_START', () => {
      const transition = { from: 'drag.idle', event: 'DRAG_START', target: 'dragging' };
      expect(transition.from).toBe('drag.idle');
      expect(transition.event).toBe('DRAG_START');
      expect(transition.target).toBe('dragging');
      // Target state 'dragging' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from drag.dragging to idle on DRAG_END', () => {
      const transition = { from: 'drag.dragging', event: 'DRAG_END', target: 'idle' };
      expect(transition.from).toBe('drag.dragging');
      expect(transition.event).toBe('DRAG_END');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from drag.dragging to idle on DROP', () => {
      const transition = { from: 'drag.dragging', event: 'DROP', target: 'idle' };
      expect(transition.from).toBe('drag.dragging');
      expect(transition.event).toBe('DROP');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"sortCount.empty","events":["ADD_SORT","LOAD_SORTS"]},{"name":"sortCount.hasSorts","events":["REMOVE_LAST","CLEAR_ALL","ADD_SORT"]},{"name":"drag.idle","events":["DRAG_START"]},{"name":"drag.dragging","events":["DRAG_END","DROP"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["sortCount.empty","sortCount.hasSorts","drag.idle","drag.dragging"]);
      const baseNames = new Set(["empty","hasSorts","idle","dragging"]);
      expect(validNames.has('hasSorts') || baseNames.has('hasSorts')).toBe(true);
      expect(validNames.has('hasSorts') || baseNames.has('hasSorts')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('hasSorts') || baseNames.has('hasSorts')).toBe(true);
      expect(validNames.has('dragging') || baseNames.has('dragging')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if ? sorts . length = = 0 then empty else has-sorts" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-disabled on root', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on addButton', () => {
      const handler = { event: 'onClick', action: "send ( ADD_SORT )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-direction on sortRow', () => {
      const attr = { name: 'data-direction', value: "self . direction" };
      expect(attr.name).toBe('data-direction');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-dragging on sortRow', () => {
      const attr = { name: 'data-dragging', value: "if state . drag = = dragging then true else false" };
      expect(attr.name).toBe('data-dragging');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-priority on sortRow', () => {
      const attr = { name: 'data-priority', value: "self . index" };
      expect(attr.name).toBe('data-priority');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="field-selector" to fieldSelector', () => {
      const binding = {"part":"fieldSelector","dataPart":"field-selector"};
      expect(binding.dataPart).toBe('field-selector');
    });

    it('should bind onChange handler on fieldSelector', () => {
      const handler = { event: 'onChange', action: "send ( FIELD_CHANGE )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-direction on directionToggle', () => {
      const attr = { name: 'data-direction', value: "self . direction" };
      expect(attr.name).toBe('data-direction');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on directionToggle', () => {
      const handler = { event: 'onClick', action: "send ( TOGGLE_DIRECTION )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-dragging on dragHandle', () => {
      const attr = { name: 'data-dragging', value: "if state . drag = = dragging then true else false" };
      expect(attr.name).toBe('data-dragging');
      expect(attr.value).toBeDefined();
    });

    it('should bind onPointerDown handler on dragHandle', () => {
      const handler = { event: 'onPointerDown', action: "send ( DRAG_START )" };
      expect(handler.event).toBe('onPointerDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerUp handler on dragHandle', () => {
      const handler = { event: 'onPointerUp', action: "send ( DROP )" };
      expect(handler.event).toBe('onPointerUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on dragHandle', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "send ( MOVE_UP )" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on dragHandle', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "send ( MOVE_DOWN )" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onClick handler on removeButton', () => {
      const handler = { event: 'onClick', action: "send ( REMOVE_SORT )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="empty-state" to emptyState', () => {
      const binding = {"part":"emptyState","dataPart":"empty-state"};
      expect(binding.dataPart).toBe('empty-state');
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","addButton","sortRow","fieldSelector","directionToggle","dragHandle","removeButton","emptyState","priorityLabel"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('addButton')).toBe(true);
      expect(anatomyParts.has('sortRow')).toBe(true);
      expect(anatomyParts.has('fieldSelector')).toBe(true);
      expect(anatomyParts.has('directionToggle')).toBe(true);
      expect(anatomyParts.has('dragHandle')).toBe(true);
      expect(anatomyParts.has('removeButton')).toBe(true);
      expect(anatomyParts.has('emptyState')).toBe(true);
      expect(anatomyParts.has('priorityLabel')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: TOGGLE_DIRECTION', () => {
      const binding = { key: 'Enter', action: 'TOGGLE_DIRECTION' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('TOGGLE_DIRECTION');
    });

    it('should handle Space key with action: TOGGLE_DIRECTION', () => {
      const binding = { key: 'Space', action: 'TOGGLE_DIRECTION' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('TOGGLE_DIRECTION');
    });

    it('should handle Delete key with action: REMOVE_SORT', () => {
      const binding = { key: 'Delete', action: 'REMOVE_SORT' };
      expect(binding.key).toBe('Delete');
      expect(binding.action).toBe('REMOVE_SORT');
    });

    it('should handle ArrowUp key with action: MOVE_UP', () => {
      const binding = { key: 'ArrowUp', action: 'MOVE_UP' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('MOVE_UP');
    });

    it('should handle ArrowDown key with action: MOVE_DOWN', () => {
      const binding = { key: 'ArrowDown', action: 'MOVE_DOWN' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('MOVE_DOWN');
    });

    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["ADD_SORT","LOAD_SORTS","REMOVE_LAST","CLEAR_ALL","DRAG_START","DRAG_END","DROP"]);
      const keyboardActions = ["TOGGLE_DIRECTION","TOGGLE_DIRECTION","REMOVE_SORT","MOVE_UP","MOVE_DOWN","FOCUS_NEXT","FOCUS_PREV"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "list"', () => {
      expect('list').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "addButton"', () => {
      expect('addButton').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Sort criteria" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on sortRow', () => {
      const ariaAttr = { part: 'sortRow', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on sortRow', () => {
      const ariaAttr = { part: 'sortRow', name: 'aria-label', value: "Sort criterion" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on directionToggle', () => {
      const ariaAttr = { part: 'directionToggle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on directionToggle', () => {
      const ariaAttr = { part: 'directionToggle', name: 'aria-label', value: "Toggle sort direction" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-pressed on directionToggle', () => {
      const ariaAttr = { part: 'directionToggle', name: 'aria-pressed', value: "if self . direction = = descending then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on dragHandle', () => {
      const ariaAttr = { part: 'dragHandle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on dragHandle', () => {
      const ariaAttr = { part: 'dragHandle', name: 'aria-roledescription', value: "sortable" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on dragHandle', () => {
      const ariaAttr = { part: 'dragHandle', name: 'aria-label', value: "Reorder sort criterion" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "Sort criteria" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part addButton', () => {
      const connectAria = { part: 'addButton', attr: 'aria-label', value: "Add sort criterion" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part sortRow', () => {
      const connectAria = { part: 'sortRow', attr: 'role', value: "listitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part sortRow', () => {
      const connectAria = { part: 'sortRow', attr: 'aria-label', value: "concat ( Sort by  , self . fieldLabel )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part fieldSelector', () => {
      const connectAria = { part: 'fieldSelector', attr: 'aria-label', value: "Sort field" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part directionToggle', () => {
      const connectAria = { part: 'directionToggle', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part directionToggle', () => {
      const connectAria = { part: 'directionToggle', attr: 'aria-label', value: "if self . direction = = ascending then Sort ascending, click to change to descending else Sort descending, click to change to ascending" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-pressed on connect part directionToggle', () => {
      const connectAria = { part: 'directionToggle', attr: 'aria-pressed', value: "if self . direction = = descending then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part dragHandle', () => {
      const connectAria = { part: 'dragHandle', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part dragHandle', () => {
      const connectAria = { part: 'dragHandle', attr: 'aria-roledescription', value: "sortable" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part dragHandle', () => {
      const connectAria = { part: 'dragHandle', attr: 'aria-label', value: "concat ( Reorder sort criterion  , self . fieldLabel )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part removeButton', () => {
      const connectAria = { part: 'removeButton', attr: 'aria-label', value: "concat ( Remove sort by  , self . fieldLabel )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part emptyState', () => {
      const connectAria = { part: 'emptyState', attr: 'aria-hidden', value: "if ? sorts . length > 0 then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part priorityLabel', () => {
      const connectAria = { part: 'priorityLabel', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "sorts" with type "list SortCriterion"', () => {
      const prop = { name: 'sorts', type: 'list SortCriterion', defaultValue: "[ ]" };
      expect(prop.name).toBe('sorts');
      expect(prop.type).toBeTruthy();
    });

    it('should default "sorts" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "fields" with type "list FieldDef"', () => {
      const prop = { name: 'fields', type: 'list FieldDef' };
      expect(prop.name).toBe('fields');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "maxSorts" with type "Int"', () => {
      const prop = { name: 'maxSorts', type: 'Int', defaultValue: "5" };
      expect(prop.name).toBe('maxSorts');
      expect(prop.type).toBeTruthy();
    });

    it('should default "maxSorts" to "5"', () => {
      expect("5").toBe("5");
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "onChange" with type "option Function"', () => {
      const prop = { name: 'onChange', type: 'option Function' };
      expect(prop.name).toBe('onChange');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","addButton","sortRow","fieldSelector","directionToggle","dragHandle","removeButton","emptyState","priorityLabel"];
      expect(parts.length).toBe(9);
      expect(parts).toContain('root');
      expect(parts).toContain('addButton');
      expect(parts).toContain('sortRow');
      expect(parts).toContain('fieldSelector');
      expect(parts).toContain('directionToggle');
      expect(parts).toContain('dragHandle');
      expect(parts).toContain('removeButton');
      expect(parts).toContain('emptyState');
      expect(parts).toContain('priorityLabel');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('action').toBeTruthy(); // addButton: action
      expect('container').toBeTruthy(); // sortRow: container
      expect('widget').toBeTruthy(); // fieldSelector: widget
      expect('action').toBeTruthy(); // directionToggle: action
      expect('action').toBeTruthy(); // dragHandle: action
      expect('action').toBeTruthy(); // removeButton: action
      expect('container').toBeTruthy(); // emptyState: container
      expect('text').toBeTruthy(); // priorityLabel: text
    });

  });

});
