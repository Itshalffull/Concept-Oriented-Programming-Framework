// Auto-generated by generate-widget-tests.ts from tool-call-detail.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: ToolCallDetail', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('idle');
    });

    it('should transition from idle to idle on EXPAND_ARGS', () => {
      const transition = { from: 'idle', event: 'EXPAND_ARGS', target: 'idle' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('EXPAND_ARGS');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to idle on EXPAND_RESULT', () => {
      const transition = { from: 'idle', event: 'EXPAND_RESULT', target: 'idle' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('EXPAND_RESULT');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to retrying on RETRY', () => {
      const transition = { from: 'idle', event: 'RETRY', target: 'retrying' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('RETRY');
      expect(transition.target).toBe('retrying');
      // Target state 'retrying' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from retrying to idle on RETRY_COMPLETE', () => {
      const transition = { from: 'retrying', event: 'RETRY_COMPLETE', target: 'idle' };
      expect(transition.from).toBe('retrying');
      expect(transition.event).toBe('RETRY_COMPLETE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from retrying to idle on RETRY_ERROR', () => {
      const transition = { from: 'retrying', event: 'RETRY_ERROR', target: 'idle' };
      expect(transition.from).toBe('retrying');
      expect(transition.event).toBe('RETRY_ERROR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"idle","events":["EXPAND_ARGS","EXPAND_RESULT","RETRY"]},{"name":"retrying","events":["RETRY_COMPLETE","RETRY_ERROR"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["idle","retrying"]);
      const baseNames = new Set(["idle","retrying"]);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('retrying') || baseNames.has('retrying')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle r key with action: RETRY', () => {
      const binding = { key: 'r', action: 'RETRY' };
      expect(binding.key).toBe('r');
      expect(binding.action).toBe('RETRY');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["EXPAND_ARGS","EXPAND_RESULT","RETRY","RETRY_COMPLETE","RETRY_ERROR"]);
      const keyboardActions = ["RETRY"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "article"', () => {
      expect('article').toBeTruthy();
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "article" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "concat ( Tool call:  , toolName )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on argumentsPanel', () => {
      const ariaAttr = { part: 'argumentsPanel', name: 'role', value: "code" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on argumentsPanel', () => {
      const ariaAttr = { part: 'argumentsPanel', name: 'aria-label', value: "Arguments" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on resultPanel', () => {
      const ariaAttr = { part: 'resultPanel', name: 'role', value: "code" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on resultPanel', () => {
      const ariaAttr = { part: 'resultPanel', name: 'aria-label', value: "Result" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on errorPanel', () => {
      const ariaAttr = { part: 'errorPanel', name: 'role', value: "alert" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on errorPanel', () => {
      const ariaAttr = { part: 'errorPanel', name: 'aria-label', value: "Error details" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on retryButton', () => {
      const ariaAttr = { part: 'retryButton', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on retryButton', () => {
      const ariaAttr = { part: 'retryButton', name: 'aria-label', value: "Retry tool call" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "showTiming" with type "Bool"', () => {
      const prop = { name: 'showTiming', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showTiming');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showTiming" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showTokens" with type "Bool"', () => {
      const prop = { name: 'showTokens', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showTokens');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showTokens" to "true"', () => {
      expect("true").toBe("true");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","header","toolName","statusBadge","argumentsPanel","resultPanel","timingBar","tokenBadge","errorPanel","retryButton"];
      expect(parts.length).toBe(10);
      expect(parts).toContain('root');
      expect(parts).toContain('header');
      expect(parts).toContain('toolName');
      expect(parts).toContain('statusBadge');
      expect(parts).toContain('argumentsPanel');
      expect(parts).toContain('resultPanel');
      expect(parts).toContain('timingBar');
      expect(parts).toContain('tokenBadge');
      expect(parts).toContain('errorPanel');
      expect(parts).toContain('retryButton');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // header: container
      expect('text').toBeTruthy(); // toolName: text
      expect('container').toBeTruthy(); // statusBadge: container
      expect('container').toBeTruthy(); // argumentsPanel: container
      expect('container').toBeTruthy(); // resultPanel: container
      expect('container').toBeTruthy(); // timingBar: container
      expect('container').toBeTruthy(); // tokenBadge: container
      expect('container').toBeTruthy(); // errorPanel: container
      expect('action').toBeTruthy(); // retryButton: action
    });

  });

});
