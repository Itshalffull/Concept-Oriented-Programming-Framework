// Auto-generated by generate-widget-tests.ts from tool-invocation.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: ToolInvocation', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["collapsed","execution.pending"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('collapsed');
      expect(initialStates).toContain('execution.pending');
    });

    it('should transition from collapsed to expanded on EXPAND', () => {
      const transition = { from: 'collapsed', event: 'EXPAND', target: 'expanded' };
      expect(transition.from).toBe('collapsed');
      expect(transition.event).toBe('EXPAND');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from collapsed to hoveredCollapsed on HOVER', () => {
      const transition = { from: 'collapsed', event: 'HOVER', target: 'hoveredCollapsed' };
      expect(transition.from).toBe('collapsed');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('hoveredCollapsed');
      // Target state 'hoveredCollapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hoveredCollapsed to collapsed on LEAVE', () => {
      const transition = { from: 'hoveredCollapsed', event: 'LEAVE', target: 'collapsed' };
      expect(transition.from).toBe('hoveredCollapsed');
      expect(transition.event).toBe('LEAVE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hoveredCollapsed to expanded on EXPAND', () => {
      const transition = { from: 'hoveredCollapsed', event: 'EXPAND', target: 'expanded' };
      expect(transition.from).toBe('hoveredCollapsed');
      expect(transition.event).toBe('EXPAND');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expanded to collapsed on COLLAPSE', () => {
      const transition = { from: 'expanded', event: 'COLLAPSE', target: 'collapsed' };
      expect(transition.from).toBe('expanded');
      expect(transition.event).toBe('COLLAPSE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from execution.pending to running on INVOKE', () => {
      const transition = { from: 'execution.pending', event: 'INVOKE', target: 'running' };
      expect(transition.from).toBe('execution.pending');
      expect(transition.event).toBe('INVOKE');
      expect(transition.target).toBe('running');
      // Target state 'running' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from execution.running to succeeded on SUCCESS', () => {
      const transition = { from: 'execution.running', event: 'SUCCESS', target: 'succeeded' };
      expect(transition.from).toBe('execution.running');
      expect(transition.event).toBe('SUCCESS');
      expect(transition.target).toBe('succeeded');
      // Target state 'succeeded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from execution.running to failed on FAILURE', () => {
      const transition = { from: 'execution.running', event: 'FAILURE', target: 'failed' };
      expect(transition.from).toBe('execution.running');
      expect(transition.event).toBe('FAILURE');
      expect(transition.target).toBe('failed');
      // Target state 'failed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from execution.succeeded to pending on RESET', () => {
      const transition = { from: 'execution.succeeded', event: 'RESET', target: 'pending' };
      expect(transition.from).toBe('execution.succeeded');
      expect(transition.event).toBe('RESET');
      expect(transition.target).toBe('pending');
      // Target state 'pending' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from execution.failed to running on RETRY', () => {
      const transition = { from: 'execution.failed', event: 'RETRY', target: 'running' };
      expect(transition.from).toBe('execution.failed');
      expect(transition.event).toBe('RETRY');
      expect(transition.target).toBe('running');
      // Target state 'running' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from execution.failed to pending on RESET', () => {
      const transition = { from: 'execution.failed', event: 'RESET', target: 'pending' };
      expect(transition.from).toBe('execution.failed');
      expect(transition.event).toBe('RESET');
      expect(transition.target).toBe('pending');
      // Target state 'pending' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"collapsed","events":["EXPAND","HOVER"]},{"name":"hoveredCollapsed","events":["LEAVE","EXPAND"]},{"name":"expanded","events":["COLLAPSE"]},{"name":"execution.pending","events":["INVOKE"]},{"name":"execution.running","events":["SUCCESS","FAILURE"]},{"name":"execution.succeeded","events":["RESET"]},{"name":"execution.failed","events":["RETRY","RESET"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["collapsed","hoveredCollapsed","expanded","execution.pending","execution.running","execution.succeeded","execution.failed"]);
      const baseNames = new Set(["collapsed","hoveredCollapsed","expanded","pending","running","succeeded","failed"]);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('hoveredCollapsed') || baseNames.has('hoveredCollapsed')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('running') || baseNames.has('running')).toBe(true);
      expect(validNames.has('succeeded') || baseNames.has('succeeded')).toBe(true);
      expect(validNames.has('failed') || baseNames.has('failed')).toBe(true);
      expect(validNames.has('pending') || baseNames.has('pending')).toBe(true);
      expect(validNames.has('running') || baseNames.has('running')).toBe(true);
      expect(validNames.has('pending') || baseNames.has('pending')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: EXPAND', () => {
      const binding = { key: 'Enter', action: 'EXPAND' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('EXPAND');
    });

    it('should handle Escape key with action: COLLAPSE', () => {
      const binding = { key: 'Escape', action: 'COLLAPSE' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('COLLAPSE');
    });

    it('should handle r key with action: RETRY', () => {
      const binding = { key: 'r', action: 'RETRY' };
      expect(binding.key).toBe('r');
      expect(binding.action).toBe('RETRY');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["EXPAND","HOVER","LEAVE","COLLAPSE","INVOKE","SUCCESS","FAILURE","RESET","RETRY"]);
      const keyboardActions = ["EXPAND","COLLAPSE","RETRY"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "article"', () => {
      expect('article').toBeTruthy();
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "article" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "concat ( Tool call:  , toolName )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-expanded', value: "if state = = expanded then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on header', () => {
      const ariaAttr = { part: 'header', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on header', () => {
      const ariaAttr = { part: 'header', name: 'aria-label', value: "concat ( toolName ,  —  , status )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on argumentsBlock', () => {
      const ariaAttr = { part: 'argumentsBlock', name: 'role', value: "code" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on argumentsBlock', () => {
      const ariaAttr = { part: 'argumentsBlock', name: 'aria-label', value: "Tool arguments" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on resultBlock', () => {
      const ariaAttr = { part: 'resultBlock', name: 'role', value: "code" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on resultBlock', () => {
      const ariaAttr = { part: 'resultBlock', name: 'aria-label', value: "Tool result" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on retryButton', () => {
      const ariaAttr = { part: 'retryButton', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on retryButton', () => {
      const ariaAttr = { part: 'retryButton', name: 'aria-label', value: "Retry tool call" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "showArguments" with type "Bool"', () => {
      const prop = { name: 'showArguments', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showArguments');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showArguments" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showResult" with type "Bool"', () => {
      const prop = { name: 'showResult', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showResult');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showResult" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "defaultExpanded" with type "Bool"', () => {
      const prop = { name: 'defaultExpanded', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('defaultExpanded');
      expect(prop.type).toBeTruthy();
    });

    it('should default "defaultExpanded" to "false"', () => {
      expect("false").toBe("false");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","header","toolIcon","toolName","statusIcon","durationLabel","body","argumentsBlock","resultBlock","warningBadge","retryButton"];
      expect(parts.length).toBe(11);
      expect(parts).toContain('root');
      expect(parts).toContain('header');
      expect(parts).toContain('toolIcon');
      expect(parts).toContain('toolName');
      expect(parts).toContain('statusIcon');
      expect(parts).toContain('durationLabel');
      expect(parts).toContain('body');
      expect(parts).toContain('argumentsBlock');
      expect(parts).toContain('resultBlock');
      expect(parts).toContain('warningBadge');
      expect(parts).toContain('retryButton');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // header: container
      expect('container').toBeTruthy(); // toolIcon: container
      expect('text').toBeTruthy(); // toolName: text
      expect('container').toBeTruthy(); // statusIcon: container
      expect('text').toBeTruthy(); // durationLabel: text
      expect('container').toBeTruthy(); // body: container
      expect('container').toBeTruthy(); // argumentsBlock: container
      expect('container').toBeTruthy(); // resultBlock: container
      expect('container').toBeTruthy(); // warningBadge: container
      expect('action').toBeTruthy(); // retryButton: action
    });

  });

});
