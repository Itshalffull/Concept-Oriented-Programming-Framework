// Auto-generated by generate-widget-tests.ts from splitter.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: Splitter', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["interaction.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('interaction.idle');
    });

    it('should transition from interaction.idle to dragging on DRAG_START', () => {
      const transition = { from: 'interaction.idle', event: 'DRAG_START', target: 'dragging' };
      expect(transition.from).toBe('interaction.idle');
      expect(transition.event).toBe('DRAG_START');
      expect(transition.target).toBe('dragging');
      // Target state 'dragging' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.idle to focused on FOCUS', () => {
      const transition = { from: 'interaction.idle', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('interaction.idle');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.dragging to idle on DRAG_END', () => {
      const transition = { from: 'interaction.dragging', event: 'DRAG_END', target: 'idle' };
      expect(transition.from).toBe('interaction.dragging');
      expect(transition.event).toBe('DRAG_END');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.dragging to dragging on DRAG_MOVE', () => {
      const transition = { from: 'interaction.dragging', event: 'DRAG_MOVE', target: 'dragging' };
      expect(transition.from).toBe('interaction.dragging');
      expect(transition.event).toBe('DRAG_MOVE');
      expect(transition.target).toBe('dragging');
      // Target state 'dragging' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.focused to idle on BLUR', () => {
      const transition = { from: 'interaction.focused', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('interaction.focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.focused to dragging on DRAG_START', () => {
      const transition = { from: 'interaction.focused', event: 'DRAG_START', target: 'dragging' };
      expect(transition.from).toBe('interaction.focused');
      expect(transition.event).toBe('DRAG_START');
      expect(transition.target).toBe('dragging');
      // Target state 'dragging' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.focused to focused on RESIZE_INCREMENT', () => {
      const transition = { from: 'interaction.focused', event: 'RESIZE_INCREMENT', target: 'focused' };
      expect(transition.from).toBe('interaction.focused');
      expect(transition.event).toBe('RESIZE_INCREMENT');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.focused to focused on RESIZE_DECREMENT', () => {
      const transition = { from: 'interaction.focused', event: 'RESIZE_DECREMENT', target: 'focused' };
      expect(transition.from).toBe('interaction.focused');
      expect(transition.event).toBe('RESIZE_DECREMENT');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.focused to focused on RESIZE_MIN', () => {
      const transition = { from: 'interaction.focused', event: 'RESIZE_MIN', target: 'focused' };
      expect(transition.from).toBe('interaction.focused');
      expect(transition.event).toBe('RESIZE_MIN');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from interaction.focused to focused on RESIZE_MAX', () => {
      const transition = { from: 'interaction.focused', event: 'RESIZE_MAX', target: 'focused' };
      expect(transition.from).toBe('interaction.focused');
      expect(transition.event).toBe('RESIZE_MAX');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"interaction.idle","events":["DRAG_START","FOCUS"]},{"name":"interaction.dragging","events":["DRAG_END","DRAG_MOVE"]},{"name":"interaction.focused","events":["BLUR","DRAG_START","RESIZE_INCREMENT","RESIZE_DECREMENT","RESIZE_MIN","RESIZE_MAX"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["interaction.idle","interaction.dragging","interaction.focused"]);
      const baseNames = new Set(["idle","dragging","focused"]);
      expect(validNames.has('dragging') || baseNames.has('dragging')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('dragging') || baseNames.has('dragging')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('dragging') || baseNames.has('dragging')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-orientation on root', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-disabled on root', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "state . interaction" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="panel-before" to panelBefore', () => {
      const binding = {"part":"panelBefore","dataPart":"panel-before"};
      expect(binding.dataPart).toBe('panel-before');
    });

    it('should bind data-orientation on panelBefore', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-orientation on handle', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on handle', () => {
      const attr = { name: 'data-state', value: "state . interaction" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-disabled on handle', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind onPointerDown handler on handle', () => {
      const handler = { event: 'onPointerDown', action: "send ( DRAG_START )" };
      expect(handler.event).toBe('onPointerDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerMove handler on handle', () => {
      const handler = { event: 'onPointerMove', action: "send ( DRAG_MOVE )" };
      expect(handler.event).toBe('onPointerMove');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerUp handler on handle', () => {
      const handler = { event: 'onPointerUp', action: "send ( DRAG_END )" };
      expect(handler.event).toBe('onPointerUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown handler on handle', () => {
      const handler = { event: 'onKeyDown', action: "send ( RESIZE )" };
      expect(handler.event).toBe('onKeyDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on handle', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on handle', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="panel-after" to panelAfter', () => {
      const binding = {"part":"panelAfter","dataPart":"panel-after"};
      expect(binding.dataPart).toBe('panel-after');
    });

    it('should bind data-orientation on panelAfter', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","panelBefore","handle","panelAfter"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('panelBefore')).toBe(true);
      expect(anatomyParts.has('handle')).toBe(true);
      expect(anatomyParts.has('panelAfter')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowLeft key with action: RESIZE_DECREMENT', () => {
      const binding = { key: 'ArrowLeft', action: 'RESIZE_DECREMENT' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('RESIZE_DECREMENT');
    });

    it('should handle ArrowRight key with action: RESIZE_INCREMENT', () => {
      const binding = { key: 'ArrowRight', action: 'RESIZE_INCREMENT' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('RESIZE_INCREMENT');
    });

    it('should handle ArrowUp key with action: RESIZE_DECREMENT', () => {
      const binding = { key: 'ArrowUp', action: 'RESIZE_DECREMENT' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('RESIZE_DECREMENT');
    });

    it('should handle ArrowDown key with action: RESIZE_INCREMENT', () => {
      const binding = { key: 'ArrowDown', action: 'RESIZE_INCREMENT' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('RESIZE_INCREMENT');
    });

    it('should handle Home key with action: RESIZE_MIN', () => {
      const binding = { key: 'Home', action: 'RESIZE_MIN' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('RESIZE_MIN');
    });

    it('should handle End key with action: RESIZE_MAX', () => {
      const binding = { key: 'End', action: 'RESIZE_MAX' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('RESIZE_MAX');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["DRAG_START","FOCUS","DRAG_END","DRAG_MOVE","BLUR","RESIZE_INCREMENT","RESIZE_DECREMENT","RESIZE_MIN","RESIZE_MAX"]);
      const keyboardActions = ["RESIZE_DECREMENT","RESIZE_INCREMENT","RESIZE_DECREMENT","RESIZE_INCREMENT","RESIZE_MIN","RESIZE_MAX"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "separator"', () => {
      expect('separator').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "handle"', () => {
      expect('handle').toBeTruthy();
    });

    it('should bind role on handle', () => {
      const ariaAttr = { part: 'handle', name: 'role', value: "separator" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-orientation on handle', () => {
      const ariaAttr = { part: 'handle', name: 'aria-orientation', value: "? orientation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-valuenow on handle', () => {
      const ariaAttr = { part: 'handle', name: 'aria-valuenow', value: "? defaultSize" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-valuemin on handle', () => {
      const ariaAttr = { part: 'handle', name: 'aria-valuemin', value: "? min" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-valuemax on handle', () => {
      const ariaAttr = { part: 'handle', name: 'aria-valuemax', value: "? max" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on handle', () => {
      const ariaAttr = { part: 'handle', name: 'aria-label', value: "Resize" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part handle', () => {
      const connectAria = { part: 'handle', attr: 'role', value: "separator" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-orientation on connect part handle', () => {
      const connectAria = { part: 'handle', attr: 'aria-orientation', value: "? orientation" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-valuenow on connect part handle', () => {
      const connectAria = { part: 'handle', attr: 'aria-valuenow', value: "? defaultSize" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-valuemin on connect part handle', () => {
      const connectAria = { part: 'handle', attr: 'aria-valuemin', value: "? min" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-valuemax on connect part handle', () => {
      const connectAria = { part: 'handle', attr: 'aria-valuemax', value: "? max" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part handle', () => {
      const connectAria = { part: 'handle', attr: 'aria-label', value: "Resize" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "orientation" with type "union horizontal | vertical"', () => {
      const prop = { name: 'orientation', type: 'union horizontal | vertical', defaultValue: "horizontal" };
      expect(prop.name).toBe('orientation');
      expect(prop.type).toBeTruthy();
    });

    it('should default "orientation" to "horizontal"', () => {
      expect("horizontal").toBe("horizontal");
    });

    it('should declare prop "defaultSize" with type "Float"', () => {
      const prop = { name: 'defaultSize', type: 'Float', defaultValue: "50" };
      expect(prop.name).toBe('defaultSize');
      expect(prop.type).toBeTruthy();
    });

    it('should default "defaultSize" to "50"', () => {
      expect("50").toBe("50");
    });

    it('should declare prop "min" with type "Float"', () => {
      const prop = { name: 'min', type: 'Float', defaultValue: "10" };
      expect(prop.name).toBe('min');
      expect(prop.type).toBeTruthy();
    });

    it('should default "min" to "10"', () => {
      expect("10").toBe("10");
    });

    it('should declare prop "max" with type "Float"', () => {
      const prop = { name: 'max', type: 'Float', defaultValue: "90" };
      expect(prop.name).toBe('max');
      expect(prop.type).toBeTruthy();
    });

    it('should default "max" to "90"', () => {
      expect("90").toBe("90");
    });

    it('should declare prop "step" with type "Float"', () => {
      const prop = { name: 'step', type: 'Float', defaultValue: "1" };
      expect(prop.name).toBe('step');
      expect(prop.type).toBeTruthy();
    });

    it('should default "step" to "1"', () => {
      expect("1").toBe("1");
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","panelBefore","handle","panelAfter"];
      expect(parts.length).toBe(4);
      expect(parts).toContain('root');
      expect(parts).toContain('panelBefore');
      expect(parts).toContain('handle');
      expect(parts).toContain('panelAfter');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // panelBefore: container
      expect('action').toBeTruthy(); // handle: action
      expect('container').toBeTruthy(); // panelAfter: container
    });

  });

});
