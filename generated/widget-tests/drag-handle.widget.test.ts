// Auto-generated by generate-widget-tests.ts from drag-handle.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: DragHandle', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('idle');
    });

    it('should transition from idle to grabbed on GRAB', () => {
      const transition = { from: 'idle', event: 'GRAB', target: 'grabbed' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('GRAB');
      expect(transition.target).toBe('grabbed');
      // Target state 'grabbed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to hovered on HOVER', () => {
      const transition = { from: 'idle', event: 'HOVER', target: 'hovered' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('hovered');
      // Target state 'hovered' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to focused on FOCUS', () => {
      const transition = { from: 'idle', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to idle on UNHOVER', () => {
      const transition = { from: 'hovered', event: 'UNHOVER', target: 'idle' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('UNHOVER');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to grabbed on GRAB', () => {
      const transition = { from: 'hovered', event: 'GRAB', target: 'grabbed' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('GRAB');
      expect(transition.target).toBe('grabbed');
      // Target state 'grabbed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to idle on BLUR', () => {
      const transition = { from: 'focused', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to grabbed on GRAB', () => {
      const transition = { from: 'focused', event: 'GRAB', target: 'grabbed' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('GRAB');
      expect(transition.target).toBe('grabbed');
      // Target state 'grabbed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from grabbed to idle on RELEASE', () => {
      const transition = { from: 'grabbed', event: 'RELEASE', target: 'idle' };
      expect(transition.from).toBe('grabbed');
      expect(transition.event).toBe('RELEASE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from grabbed to dragging on MOVE', () => {
      const transition = { from: 'grabbed', event: 'MOVE', target: 'dragging' };
      expect(transition.from).toBe('grabbed');
      expect(transition.event).toBe('MOVE');
      expect(transition.target).toBe('dragging');
      // Target state 'dragging' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from grabbed to idle on ESCAPE', () => {
      const transition = { from: 'grabbed', event: 'ESCAPE', target: 'idle' };
      expect(transition.from).toBe('grabbed');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dragging to idle on DROP', () => {
      const transition = { from: 'dragging', event: 'DROP', target: 'idle' };
      expect(transition.from).toBe('dragging');
      expect(transition.event).toBe('DROP');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dragging to idle on ESCAPE', () => {
      const transition = { from: 'dragging', event: 'ESCAPE', target: 'idle' };
      expect(transition.from).toBe('dragging');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"idle","events":["GRAB","HOVER","FOCUS"]},{"name":"hovered","events":["UNHOVER","GRAB"]},{"name":"focused","events":["BLUR","GRAB"]},{"name":"grabbed","events":["RELEASE","MOVE","ESCAPE"]},{"name":"dragging","events":["DROP","ESCAPE"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["idle","hovered","focused","grabbed","dragging"]);
      const baseNames = new Set(["idle","hovered","focused","grabbed","dragging"]);
      expect(validNames.has('grabbed') || baseNames.has('grabbed')).toBe(true);
      expect(validNames.has('hovered') || baseNames.has('hovered')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('grabbed') || baseNames.has('grabbed')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('grabbed') || baseNames.has('grabbed')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('dragging') || baseNames.has('dragging')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="drag-handle" to root', () => {
      const binding = {"part":"root","dataPart":"drag-handle"};
      expect(binding.dataPart).toBe('drag-handle');
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "state" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-orientation on root', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should bind onPointerDown handler on root', () => {
      const handler = { event: 'onPointerDown', action: "if not ? disabled then send ( GRAB ) else noop" };
      expect(handler.event).toBe('onPointerDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerUp handler on root', () => {
      const handler = { event: 'onPointerUp', action: "send ( RELEASE )" };
      expect(handler.event).toBe('onPointerUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerMove handler on root', () => {
      const handler = { event: 'onPointerMove', action: "if state = = grabbed then send ( MOVE ) else noop" };
      expect(handler.event).toBe('onPointerMove');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerEnter handler on root', () => {
      const handler = { event: 'onPointerEnter', action: "send ( HOVER )" };
      expect(handler.event).toBe('onPointerEnter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerLeave handler on root', () => {
      const handler = { event: 'onPointerLeave', action: "send ( UNHOVER )" };
      expect(handler.event).toBe('onPointerLeave');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on root', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on root', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on root', () => {
      const handler = { event: 'onKeyDown-Space', action: "if state = = idle or state = = focused then send ( GRAB ) else send ( RELEASE )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on root', () => {
      const handler = { event: 'onKeyDown-Enter', action: "if state = = idle or state = = focused then send ( GRAB ) else send ( RELEASE )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on root', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "if state = = grabbed or state = = dragging then send ( MOVE_UP ) else noop" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on root', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "if state = = grabbed or state = = dragging then send ( MOVE_DOWN ) else noop" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowLeft handler on root', () => {
      const handler = { event: 'onKeyDown-ArrowLeft', action: "if state = = grabbed or state = = dragging then send ( MOVE_LEFT ) else noop" };
      expect(handler.event).toBe('onKeyDown-ArrowLeft');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowRight handler on root', () => {
      const handler = { event: 'onKeyDown-ArrowRight', action: "if state = = grabbed or state = = dragging then send ( MOVE_RIGHT ) else noop" };
      expect(handler.event).toBe('onKeyDown-ArrowRight');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Escape handler on root', () => {
      const handler = { event: 'onKeyDown-Escape', action: "send ( CANCEL )" };
      expect(handler.event).toBe('onKeyDown-Escape');
      expect(handler.action).toBeDefined();
    });

    it('should bind onDragStart handler on root', () => {
      const handler = { event: 'onDragStart', action: "send ( GRAB )" };
      expect(handler.event).toBe('onDragStart');
      expect(handler.action).toBeDefined();
    });

    it('should bind onDragEnd handler on root', () => {
      const handler = { event: 'onDragEnd', action: "send ( DROP )" };
      expect(handler.event).toBe('onDragEnd');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="icon" to icon', () => {
      const binding = {"part":"icon","dataPart":"icon"};
      expect(binding.dataPart).toBe('icon');
    });

    it('should bind data-orientation on icon', () => {
      const attr = { name: 'data-orientation', value: "? orientation" };
      expect(attr.name).toBe('data-orientation');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","icon"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('icon')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Space key with action: GRAB_OR_RELEASE', () => {
      const binding = { key: 'Space', action: 'GRAB_OR_RELEASE' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('GRAB_OR_RELEASE');
    });

    it('should handle Enter key with action: GRAB_OR_RELEASE', () => {
      const binding = { key: 'Enter', action: 'GRAB_OR_RELEASE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('GRAB_OR_RELEASE');
    });

    it('should handle ArrowUp key with action: MOVE_UP', () => {
      const binding = { key: 'ArrowUp', action: 'MOVE_UP' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('MOVE_UP');
    });

    it('should handle ArrowDown key with action: MOVE_DOWN', () => {
      const binding = { key: 'ArrowDown', action: 'MOVE_DOWN' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('MOVE_DOWN');
    });

    it('should handle ArrowLeft key with action: MOVE_LEFT', () => {
      const binding = { key: 'ArrowLeft', action: 'MOVE_LEFT' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('MOVE_LEFT');
    });

    it('should handle ArrowRight key with action: MOVE_RIGHT', () => {
      const binding = { key: 'ArrowRight', action: 'MOVE_RIGHT' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('MOVE_RIGHT');
    });

    it('should handle Escape key with action: CANCEL', () => {
      const binding = { key: 'Escape', action: 'CANCEL' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('CANCEL');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["GRAB","HOVER","FOCUS","UNHOVER","BLUR","RELEASE","MOVE","ESCAPE","DROP"]);
      const keyboardActions = ["GRAB_OR_RELEASE","GRAB_OR_RELEASE","MOVE_UP","MOVE_DOWN","MOVE_LEFT","MOVE_RIGHT","CANCEL"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "button"', () => {
      expect('button').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "root"', () => {
      expect('root').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Drag to reorder" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "drag handle" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-grabbed on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-grabbed', value: "if state = = grabbed or state = = dragging then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-describedby on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-describedby', value: "drag-instructions" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on icon', () => {
      const ariaAttr = { part: 'icon', name: 'role', value: "presentation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on icon', () => {
      const ariaAttr = { part: 'icon', name: 'aria-hidden', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? ariaLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-roledescription', value: "drag handle" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-grabbed on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-grabbed', value: "if state = = grabbed or state = = dragging then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-disabled on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-disabled', value: "if ? disabled then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part icon', () => {
      const connectAria = { part: 'icon', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "orientation" with type "union horizontal | vertical"', () => {
      const prop = { name: 'orientation', type: 'union horizontal | vertical', defaultValue: "vertical" };
      expect(prop.name).toBe('orientation');
      expect(prop.type).toBeTruthy();
    });

    it('should default "orientation" to "vertical"', () => {
      expect("vertical").toBe("vertical");
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "ariaLabel" with type "String"', () => {
      const prop = { name: 'ariaLabel', type: 'String', defaultValue: "Drag to reorder" };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "ariaLabel" to "Drag to reorder"', () => {
      expect("Drag to reorder").toBe("Drag to reorder");
    });

    it('should declare prop "itemIndex" with type "option Int"', () => {
      const prop = { name: 'itemIndex', type: 'option Int' };
      expect(prop.name).toBe('itemIndex');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","icon"];
      expect(parts.length).toBe(2);
      expect(parts).toContain('root');
      expect(parts).toContain('icon');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('action').toBeTruthy(); // root: action
      expect('container').toBeTruthy(); // icon: container
    });

  });

});
