// Auto-generated by generate-widget-tests.ts from prompt-template-editor.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: PromptTemplateEditor', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["editing"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('editing');
    });

    it('should transition from editing to editing on ADD_MESSAGE', () => {
      const transition = { from: 'editing', event: 'ADD_MESSAGE', target: 'editing' };
      expect(transition.from).toBe('editing');
      expect(transition.event).toBe('ADD_MESSAGE');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing to editing on REMOVE_MESSAGE', () => {
      const transition = { from: 'editing', event: 'REMOVE_MESSAGE', target: 'editing' };
      expect(transition.from).toBe('editing');
      expect(transition.event).toBe('REMOVE_MESSAGE');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing to editing on REORDER', () => {
      const transition = { from: 'editing', event: 'REORDER', target: 'editing' };
      expect(transition.from).toBe('editing');
      expect(transition.event).toBe('REORDER');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing to compiling on COMPILE', () => {
      const transition = { from: 'editing', event: 'COMPILE', target: 'compiling' };
      expect(transition.from).toBe('editing');
      expect(transition.event).toBe('COMPILE');
      expect(transition.target).toBe('compiling');
      // Target state 'compiling' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing to messageSelected on SELECT_MESSAGE', () => {
      const transition = { from: 'editing', event: 'SELECT_MESSAGE', target: 'messageSelected' };
      expect(transition.from).toBe('editing');
      expect(transition.event).toBe('SELECT_MESSAGE');
      expect(transition.target).toBe('messageSelected');
      // Target state 'messageSelected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from messageSelected to editing on DESELECT', () => {
      const transition = { from: 'messageSelected', event: 'DESELECT', target: 'editing' };
      expect(transition.from).toBe('messageSelected');
      expect(transition.event).toBe('DESELECT');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from messageSelected to messageSelected on SELECT_MESSAGE', () => {
      const transition = { from: 'messageSelected', event: 'SELECT_MESSAGE', target: 'messageSelected' };
      expect(transition.from).toBe('messageSelected');
      expect(transition.event).toBe('SELECT_MESSAGE');
      expect(transition.target).toBe('messageSelected');
      // Target state 'messageSelected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from compiling to editing on COMPILE_COMPLETE', () => {
      const transition = { from: 'compiling', event: 'COMPILE_COMPLETE', target: 'editing' };
      expect(transition.from).toBe('compiling');
      expect(transition.event).toBe('COMPILE_COMPLETE');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from compiling to editing on COMPILE_ERROR', () => {
      const transition = { from: 'compiling', event: 'COMPILE_ERROR', target: 'editing' };
      expect(transition.from).toBe('compiling');
      expect(transition.event).toBe('COMPILE_ERROR');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"editing","events":["ADD_MESSAGE","REMOVE_MESSAGE","REORDER","COMPILE","SELECT_MESSAGE"]},{"name":"messageSelected","events":["DESELECT","SELECT_MESSAGE"]},{"name":"compiling","events":["COMPILE_COMPLETE","COMPILE_ERROR"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["editing","messageSelected","compiling"]);
      const baseNames = new Set(["editing","messageSelected","compiling"]);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('compiling') || baseNames.has('compiling')).toBe(true);
      expect(validNames.has('messageSelected') || baseNames.has('messageSelected')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('messageSelected') || baseNames.has('messageSelected')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Control+Enter key with action: COMPILE', () => {
      const binding = { key: 'Control+Enter', action: 'COMPILE' };
      expect(binding.key).toBe('Control+Enter');
      expect(binding.action).toBe('COMPILE');
    });

    it('should handle Control+Shift+n key with action: ADD_MESSAGE', () => {
      const binding = { key: 'Control+Shift+n', action: 'ADD_MESSAGE' };
      expect(binding.key).toBe('Control+Shift+n');
      expect(binding.action).toBe('ADD_MESSAGE');
    });

    it('should handle Delete key with action: REMOVE_MESSAGE', () => {
      const binding = { key: 'Delete', action: 'REMOVE_MESSAGE' };
      expect(binding.key).toBe('Delete');
      expect(binding.action).toBe('REMOVE_MESSAGE');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["ADD_MESSAGE","REMOVE_MESSAGE","REORDER","COMPILE","SELECT_MESSAGE","DESELECT","COMPILE_COMPLETE","COMPILE_ERROR"]);
      const keyboardActions = ["COMPILE","ADD_MESSAGE","REMOVE_MESSAGE"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "form"', () => {
      expect('form').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "messageList"', () => {
      expect('messageList').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "form" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Prompt template editor" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on messageList', () => {
      const ariaAttr = { part: 'messageList', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on messageList', () => {
      const ariaAttr = { part: 'messageList', name: 'aria-label', value: "Template messages" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on messageBlock', () => {
      const ariaAttr = { part: 'messageBlock', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on messageBlock', () => {
      const ariaAttr = { part: 'messageBlock', name: 'aria-label', value: "concat ( self . role ,  message )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on addButton', () => {
      const ariaAttr = { part: 'addButton', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on addButton', () => {
      const ariaAttr = { part: 'addButton', name: 'aria-label', value: "Add message" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on tokenCount', () => {
      const ariaAttr = { part: 'tokenCount', name: 'role', value: "status" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on tokenCount', () => {
      const ariaAttr = { part: 'tokenCount', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "showParameters" with type "Bool"', () => {
      const prop = { name: 'showParameters', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showParameters');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showParameters" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showTokenCount" with type "Bool"', () => {
      const prop = { name: 'showTokenCount', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showTokenCount');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showTokenCount" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "maxMessages" with type "Int"', () => {
      const prop = { name: 'maxMessages', type: 'Int', defaultValue: "20" };
      expect(prop.name).toBe('maxMessages');
      expect(prop.type).toBeTruthy();
    });

    it('should default "maxMessages" to "20"', () => {
      expect("20").toBe("20");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","messageList","messageBlock","roleSelector","templateInput","variablePills","addButton","reorderHandle","deleteButton","parameterPanel","tokenCount"];
      expect(parts.length).toBe(11);
      expect(parts).toContain('root');
      expect(parts).toContain('messageList');
      expect(parts).toContain('messageBlock');
      expect(parts).toContain('roleSelector');
      expect(parts).toContain('templateInput');
      expect(parts).toContain('variablePills');
      expect(parts).toContain('addButton');
      expect(parts).toContain('reorderHandle');
      expect(parts).toContain('deleteButton');
      expect(parts).toContain('parameterPanel');
      expect(parts).toContain('tokenCount');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // messageList: container
      expect('container').toBeTruthy(); // messageBlock: container
      expect('widget').toBeTruthy(); // roleSelector: widget
      expect('widget').toBeTruthy(); // templateInput: widget
      expect('container').toBeTruthy(); // variablePills: container
      expect('action').toBeTruthy(); // addButton: action
      expect('container').toBeTruthy(); // reorderHandle: container
      expect('action').toBeTruthy(); // deleteButton: action
      expect('container').toBeTruthy(); // parameterPanel: container
      expect('text').toBeTruthy(); // tokenCount: text
    });

  });

});
