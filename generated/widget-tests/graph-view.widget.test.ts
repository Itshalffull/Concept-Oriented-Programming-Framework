// Auto-generated by generate-widget-tests.ts from graph-view.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: GraphView', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["globalView","simulation.running"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('globalView');
      expect(initialStates).toContain('simulation.running');
    });

    it('should transition from globalView to localView on SWITCH_LOCAL', () => {
      const transition = { from: 'globalView', event: 'SWITCH_LOCAL', target: 'localView' };
      expect(transition.from).toBe('globalView');
      expect(transition.event).toBe('SWITCH_LOCAL');
      expect(transition.target).toBe('localView');
      // Target state 'localView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from globalView to nodeSelected on SELECT_NODE', () => {
      const transition = { from: 'globalView', event: 'SELECT_NODE', target: 'nodeSelected' };
      expect(transition.from).toBe('globalView');
      expect(transition.event).toBe('SELECT_NODE');
      expect(transition.target).toBe('nodeSelected');
      // Target state 'nodeSelected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from globalView to searching on SEARCH', () => {
      const transition = { from: 'globalView', event: 'SEARCH', target: 'searching' };
      expect(transition.from).toBe('globalView');
      expect(transition.event).toBe('SEARCH');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from globalView to panning on PAN_START', () => {
      const transition = { from: 'globalView', event: 'PAN_START', target: 'panning' };
      expect(transition.from).toBe('globalView');
      expect(transition.event).toBe('PAN_START');
      expect(transition.target).toBe('panning');
      // Target state 'panning' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from globalView to globalView on ZOOM', () => {
      const transition = { from: 'globalView', event: 'ZOOM', target: 'globalView' };
      expect(transition.from).toBe('globalView');
      expect(transition.event).toBe('ZOOM');
      expect(transition.target).toBe('globalView');
      // Target state 'globalView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from localView to globalView on SWITCH_GLOBAL', () => {
      const transition = { from: 'localView', event: 'SWITCH_GLOBAL', target: 'globalView' };
      expect(transition.from).toBe('localView');
      expect(transition.event).toBe('SWITCH_GLOBAL');
      expect(transition.target).toBe('globalView');
      // Target state 'globalView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from localView to nodeSelected on SELECT_NODE', () => {
      const transition = { from: 'localView', event: 'SELECT_NODE', target: 'nodeSelected' };
      expect(transition.from).toBe('localView');
      expect(transition.event).toBe('SELECT_NODE');
      expect(transition.target).toBe('nodeSelected');
      // Target state 'nodeSelected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from localView to searching on SEARCH', () => {
      const transition = { from: 'localView', event: 'SEARCH', target: 'searching' };
      expect(transition.from).toBe('localView');
      expect(transition.event).toBe('SEARCH');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from localView to panning on PAN_START', () => {
      const transition = { from: 'localView', event: 'PAN_START', target: 'panning' };
      expect(transition.from).toBe('localView');
      expect(transition.event).toBe('PAN_START');
      expect(transition.target).toBe('panning');
      // Target state 'panning' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from nodeSelected to globalView on DESELECT', () => {
      const transition = { from: 'nodeSelected', event: 'DESELECT', target: 'globalView' };
      expect(transition.from).toBe('nodeSelected');
      expect(transition.event).toBe('DESELECT');
      expect(transition.target).toBe('globalView');
      // Target state 'globalView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from nodeSelected to nodeSelected on SELECT_NODE', () => {
      const transition = { from: 'nodeSelected', event: 'SELECT_NODE', target: 'nodeSelected' };
      expect(transition.from).toBe('nodeSelected');
      expect(transition.event).toBe('SELECT_NODE');
      expect(transition.target).toBe('nodeSelected');
      // Target state 'nodeSelected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from nodeSelected to localView on SWITCH_LOCAL', () => {
      const transition = { from: 'nodeSelected', event: 'SWITCH_LOCAL', target: 'localView' };
      expect(transition.from).toBe('nodeSelected');
      expect(transition.event).toBe('SWITCH_LOCAL');
      expect(transition.target).toBe('localView');
      // Target state 'localView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from nodeSelected to globalView on SWITCH_GLOBAL', () => {
      const transition = { from: 'nodeSelected', event: 'SWITCH_GLOBAL', target: 'globalView' };
      expect(transition.from).toBe('nodeSelected');
      expect(transition.event).toBe('SWITCH_GLOBAL');
      expect(transition.target).toBe('globalView');
      // Target state 'globalView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from searching to globalView on CLEAR_SEARCH', () => {
      const transition = { from: 'searching', event: 'CLEAR_SEARCH', target: 'globalView' };
      expect(transition.from).toBe('searching');
      expect(transition.event).toBe('CLEAR_SEARCH');
      expect(transition.target).toBe('globalView');
      // Target state 'globalView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from searching to nodeSelected on SELECT_NODE', () => {
      const transition = { from: 'searching', event: 'SELECT_NODE', target: 'nodeSelected' };
      expect(transition.from).toBe('searching');
      expect(transition.event).toBe('SELECT_NODE');
      expect(transition.target).toBe('nodeSelected');
      // Target state 'nodeSelected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from searching to searching on SEARCH_EMPTY', () => {
      const transition = { from: 'searching', event: 'SEARCH_EMPTY', target: 'searching' };
      expect(transition.from).toBe('searching');
      expect(transition.event).toBe('SEARCH_EMPTY');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from panning to globalView on PAN_END', () => {
      const transition = { from: 'panning', event: 'PAN_END', target: 'globalView' };
      expect(transition.from).toBe('panning');
      expect(transition.event).toBe('PAN_END');
      expect(transition.target).toBe('globalView');
      // Target state 'globalView' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from simulation.running to stabilized on STABILIZE', () => {
      const transition = { from: 'simulation.running', event: 'STABILIZE', target: 'stabilized' };
      expect(transition.from).toBe('simulation.running');
      expect(transition.event).toBe('STABILIZE');
      expect(transition.target).toBe('stabilized');
      // Target state 'stabilized' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from simulation.running to paused on PAUSE', () => {
      const transition = { from: 'simulation.running', event: 'PAUSE', target: 'paused' };
      expect(transition.from).toBe('simulation.running');
      expect(transition.event).toBe('PAUSE');
      expect(transition.target).toBe('paused');
      // Target state 'paused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from simulation.stabilized to running on REHEAT', () => {
      const transition = { from: 'simulation.stabilized', event: 'REHEAT', target: 'running' };
      expect(transition.from).toBe('simulation.stabilized');
      expect(transition.event).toBe('REHEAT');
      expect(transition.target).toBe('running');
      // Target state 'running' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from simulation.stabilized to running on DRAG_NODE', () => {
      const transition = { from: 'simulation.stabilized', event: 'DRAG_NODE', target: 'running' };
      expect(transition.from).toBe('simulation.stabilized');
      expect(transition.event).toBe('DRAG_NODE');
      expect(transition.target).toBe('running');
      // Target state 'running' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from simulation.paused to running on RESUME', () => {
      const transition = { from: 'simulation.paused', event: 'RESUME', target: 'running' };
      expect(transition.from).toBe('simulation.paused');
      expect(transition.event).toBe('RESUME');
      expect(transition.target).toBe('running');
      // Target state 'running' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"globalView","events":["SWITCH_LOCAL","SELECT_NODE","SEARCH","PAN_START","ZOOM"]},{"name":"localView","events":["SWITCH_GLOBAL","SELECT_NODE","SEARCH","PAN_START"]},{"name":"nodeSelected","events":["DESELECT","SELECT_NODE","SWITCH_LOCAL","SWITCH_GLOBAL"]},{"name":"searching","events":["CLEAR_SEARCH","SELECT_NODE","SEARCH_EMPTY"]},{"name":"panning","events":["PAN_END"]},{"name":"simulation.running","events":["STABILIZE","PAUSE"]},{"name":"simulation.stabilized","events":["REHEAT","DRAG_NODE"]},{"name":"simulation.paused","events":["RESUME"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["globalView","localView","nodeSelected","searching","panning","simulation.running","simulation.stabilized","simulation.paused"]);
      const baseNames = new Set(["globalView","localView","nodeSelected","searching","panning","running","stabilized","paused"]);
      expect(validNames.has('localView') || baseNames.has('localView')).toBe(true);
      expect(validNames.has('nodeSelected') || baseNames.has('nodeSelected')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('panning') || baseNames.has('panning')).toBe(true);
      expect(validNames.has('globalView') || baseNames.has('globalView')).toBe(true);
      expect(validNames.has('globalView') || baseNames.has('globalView')).toBe(true);
      expect(validNames.has('nodeSelected') || baseNames.has('nodeSelected')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('panning') || baseNames.has('panning')).toBe(true);
      expect(validNames.has('globalView') || baseNames.has('globalView')).toBe(true);
      expect(validNames.has('nodeSelected') || baseNames.has('nodeSelected')).toBe(true);
      expect(validNames.has('localView') || baseNames.has('localView')).toBe(true);
      expect(validNames.has('globalView') || baseNames.has('globalView')).toBe(true);
      expect(validNames.has('globalView') || baseNames.has('globalView')).toBe(true);
      expect(validNames.has('nodeSelected') || baseNames.has('nodeSelected')).toBe(true);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('globalView') || baseNames.has('globalView')).toBe(true);
      expect(validNames.has('stabilized') || baseNames.has('stabilized')).toBe(true);
      expect(validNames.has('paused') || baseNames.has('paused')).toBe(true);
      expect(validNames.has('running') || baseNames.has('running')).toBe(true);
      expect(validNames.has('running') || baseNames.has('running')).toBe(true);
      expect(validNames.has('running') || baseNames.has('running')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "state" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-view-mode on root', () => {
      const attr = { name: 'data-view-mode', value: "? viewMode" };
      expect(attr.name).toBe('data-view-mode');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-simulation on root', () => {
      const attr = { name: 'data-simulation', value: "if ? simulationRunning then running else paused" };
      expect(attr.name).toBe('data-simulation');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="canvas" to canvas', () => {
      const binding = {"part":"canvas","dataPart":"canvas"};
      expect(binding.dataPart).toBe('canvas');
    });

    it('should bind data-zoom on canvas', () => {
      const attr = { name: 'data-zoom', value: "? zoom" };
      expect(attr.name).toBe('data-zoom');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-pan-x on canvas', () => {
      const attr = { name: 'data-pan-x', value: "? panX" };
      expect(attr.name).toBe('data-pan-x');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-pan-y on canvas', () => {
      const attr = { name: 'data-pan-y', value: "? panY" };
      expect(attr.name).toBe('data-pan-y');
      expect(attr.value).toBeDefined();
    });

    it('should bind onWheel handler on canvas', () => {
      const handler = { event: 'onWheel', action: "send ( ZOOM , { delta : self . wheelDelta } )" };
      expect(handler.event).toBe('onWheel');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerDown handler on canvas', () => {
      const handler = { event: 'onPointerDown', action: "send ( PAN_START )" };
      expect(handler.event).toBe('onPointerDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerUp handler on canvas', () => {
      const handler = { event: 'onPointerUp', action: "send ( PAN_END )" };
      expect(handler.event).toBe('onPointerUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerMove handler on canvas', () => {
      const handler = { event: 'onPointerMove', action: "send ( PAN_MOVE , { x : self . x , y : self . y } )" };
      expect(handler.event).toBe('onPointerMove');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="filter-panel" to filterPanel', () => {
      const binding = {"part":"filterPanel","dataPart":"filter-panel"};
      expect(binding.dataPart).toBe('filter-panel');
    });

    it('should bind data-visible on filterPanel', () => {
      const attr = { name: 'data-visible', value: "if ? filterPanelOpen then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="search-input" to searchInput', () => {
      const binding = {"part":"searchInput","dataPart":"search-input"};
      expect(binding.dataPart).toBe('search-input');
    });

    it('should bind data-part="type-toggles" to typeToggles', () => {
      const binding = {"part":"typeToggles","dataPart":"type-toggles"};
      expect(binding.dataPart).toBe('type-toggles');
    });

    it('should bind data-part="display-controls" to displayControls', () => {
      const binding = {"part":"displayControls","dataPart":"display-controls"};
      expect(binding.dataPart).toBe('display-controls');
    });

    it('should bind data-part="node-size-slider" to nodeSizeSlider', () => {
      const binding = {"part":"nodeSizeSlider","dataPart":"node-size-slider"};
      expect(binding.dataPart).toBe('node-size-slider');
    });

    it('should bind data-part="link-thickness-slider" to linkThicknessSlider', () => {
      const binding = {"part":"linkThicknessSlider","dataPart":"link-thickness-slider"};
      expect(binding.dataPart).toBe('link-thickness-slider');
    });

    it('should bind data-part="force-controls" to forceControls', () => {
      const binding = {"part":"forceControls","dataPart":"force-controls"};
      expect(binding.dataPart).toBe('force-controls');
    });

    it('should bind data-part="minimap" to minimap', () => {
      const binding = {"part":"minimap","dataPart":"minimap"};
      expect(binding.dataPart).toBe('minimap');
    });

    it('should bind data-zoom on minimap', () => {
      const attr = { name: 'data-zoom', value: "? zoom" };
      expect(attr.name).toBe('data-zoom');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-pan-x on minimap', () => {
      const attr = { name: 'data-pan-x', value: "? panX" };
      expect(attr.name).toBe('data-pan-x');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-pan-y on minimap', () => {
      const attr = { name: 'data-pan-y', value: "? panY" };
      expect(attr.name).toBe('data-pan-y');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="detail-panel" to detailPanel', () => {
      const binding = {"part":"detailPanel","dataPart":"detail-panel"};
      expect(binding.dataPart).toBe('detail-panel');
    });

    it('should bind data-visible on detailPanel', () => {
      const attr = { name: 'data-visible', value: "if ? selectedNodeId then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-node-id on detailPanel', () => {
      const attr = { name: 'data-node-id', value: "? selectedNodeId" };
      expect(attr.name).toBe('data-node-id');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="mode-toggle" to modeToggle', () => {
      const binding = {"part":"modeToggle","dataPart":"mode-toggle"};
      expect(binding.dataPart).toBe('mode-toggle');
    });

    it('should bind data-mode on modeToggle', () => {
      const attr = { name: 'data-mode', value: "? viewMode" };
      expect(attr.name).toBe('data-mode');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","canvas","filterPanel","searchInput","typeToggles","displayControls","nodeSizeSlider","linkThicknessSlider","forceControls","minimap","detailPanel","modeToggle"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('canvas')).toBe(true);
      expect(anatomyParts.has('filterPanel')).toBe(true);
      expect(anatomyParts.has('searchInput')).toBe(true);
      expect(anatomyParts.has('typeToggles')).toBe(true);
      expect(anatomyParts.has('displayControls')).toBe(true);
      expect(anatomyParts.has('nodeSizeSlider')).toBe(true);
      expect(anatomyParts.has('linkThicknessSlider')).toBe(true);
      expect(anatomyParts.has('forceControls')).toBe(true);
      expect(anatomyParts.has('minimap')).toBe(true);
      expect(anatomyParts.has('detailPanel')).toBe(true);
      expect(anatomyParts.has('modeToggle')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Tab key with action: FOCUS_NEXT_NODE', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT_NODE' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT_NODE');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV_NODE', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV_NODE' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV_NODE');
    });

    it('should handle Enter key with action: SELECT_NODE', () => {
      const binding = { key: 'Enter', action: 'SELECT_NODE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('SELECT_NODE');
    });

    it('should handle Escape key with action: DESELECT', () => {
      const binding = { key: 'Escape', action: 'DESELECT' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('DESELECT');
    });

    it('should handle Control+f key with action: FOCUS_SEARCH', () => {
      const binding = { key: 'Control+f', action: 'FOCUS_SEARCH' };
      expect(binding.key).toBe('Control+f');
      expect(binding.action).toBe('FOCUS_SEARCH');
    });

    it('should handle Control+Plus key with action: ZOOM_IN', () => {
      const binding = { key: 'Control+Plus', action: 'ZOOM_IN' };
      expect(binding.key).toBe('Control+Plus');
      expect(binding.action).toBe('ZOOM_IN');
    });

    it('should handle Control+Minus key with action: ZOOM_OUT', () => {
      const binding = { key: 'Control+Minus', action: 'ZOOM_OUT' };
      expect(binding.key).toBe('Control+Minus');
      expect(binding.action).toBe('ZOOM_OUT');
    });

    it('should handle Control+0 key with action: ZOOM_FIT', () => {
      const binding = { key: 'Control+0', action: 'ZOOM_FIT' };
      expect(binding.key).toBe('Control+0');
      expect(binding.action).toBe('ZOOM_FIT');
    });

    it('should handle ArrowUp key with action: PAN_UP', () => {
      const binding = { key: 'ArrowUp', action: 'PAN_UP' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('PAN_UP');
    });

    it('should handle ArrowDown key with action: PAN_DOWN', () => {
      const binding = { key: 'ArrowDown', action: 'PAN_DOWN' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('PAN_DOWN');
    });

    it('should handle ArrowLeft key with action: PAN_LEFT', () => {
      const binding = { key: 'ArrowLeft', action: 'PAN_LEFT' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('PAN_LEFT');
    });

    it('should handle ArrowRight key with action: PAN_RIGHT', () => {
      const binding = { key: 'ArrowRight', action: 'PAN_RIGHT' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('PAN_RIGHT');
    });

    it('should handle g key with action: SWITCH_GLOBAL', () => {
      const binding = { key: 'g', action: 'SWITCH_GLOBAL' };
      expect(binding.key).toBe('g');
      expect(binding.action).toBe('SWITCH_GLOBAL');
    });

    it('should handle l key with action: SWITCH_LOCAL', () => {
      const binding = { key: 'l', action: 'SWITCH_LOCAL' };
      expect(binding.key).toBe('l');
      expect(binding.action).toBe('SWITCH_LOCAL');
    });

    it('should handle Space key with action: TOGGLE_SIMULATION', () => {
      const binding = { key: 'Space', action: 'TOGGLE_SIMULATION' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('TOGGLE_SIMULATION');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SWITCH_LOCAL","SELECT_NODE","SEARCH","PAN_START","ZOOM","SWITCH_GLOBAL","DESELECT","CLEAR_SEARCH","SEARCH_EMPTY","PAN_END","STABILIZE","PAUSE","REHEAT","DRAG_NODE","RESUME"]);
      const keyboardActions = ["FOCUS_NEXT_NODE","FOCUS_PREV_NODE","SELECT_NODE","DESELECT","FOCUS_SEARCH","ZOOM_IN","ZOOM_OUT","ZOOM_FIT","PAN_UP","PAN_DOWN","PAN_LEFT","PAN_RIGHT","SWITCH_GLOBAL","SWITCH_LOCAL","TOGGLE_SIMULATION"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "application"', () => {
      expect('application').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "canvas"', () => {
      expect('canvas').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "application" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? ariaLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "graph visualization" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on canvas', () => {
      const ariaAttr = { part: 'canvas', name: 'role', value: "img" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on canvas', () => {
      const ariaAttr = { part: 'canvas', name: 'aria-label', value: "concat ( Graph with  , ? nodeCount ,  nodes and  , ? edgeCount ,  edges )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on filterPanel', () => {
      const ariaAttr = { part: 'filterPanel', name: 'role', value: "complementary" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on filterPanel', () => {
      const ariaAttr = { part: 'filterPanel', name: 'aria-label', value: "Graph filters" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on searchInput', () => {
      const ariaAttr = { part: 'searchInput', name: 'role', value: "searchbox" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on searchInput', () => {
      const ariaAttr = { part: 'searchInput', name: 'aria-label', value: "Search nodes" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on typeToggles', () => {
      const ariaAttr = { part: 'typeToggles', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on typeToggles', () => {
      const ariaAttr = { part: 'typeToggles', name: 'aria-label', value: "Node type filters" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on displayControls', () => {
      const ariaAttr = { part: 'displayControls', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on displayControls', () => {
      const ariaAttr = { part: 'displayControls', name: 'aria-label', value: "Display settings" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on forceControls', () => {
      const ariaAttr = { part: 'forceControls', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on forceControls', () => {
      const ariaAttr = { part: 'forceControls', name: 'aria-label', value: "Force simulation controls" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on minimap', () => {
      const ariaAttr = { part: 'minimap', name: 'role', value: "img" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on minimap', () => {
      const ariaAttr = { part: 'minimap', name: 'aria-label', value: "Graph minimap" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on detailPanel', () => {
      const ariaAttr = { part: 'detailPanel', name: 'role', value: "complementary" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on detailPanel', () => {
      const ariaAttr = { part: 'detailPanel', name: 'aria-label', value: "Node details" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on detailPanel', () => {
      const ariaAttr = { part: 'detailPanel', name: 'aria-hidden', value: "if state != nodeSelected then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on modeToggle', () => {
      const ariaAttr = { part: 'modeToggle', name: 'role', value: "switch" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on modeToggle', () => {
      const ariaAttr = { part: 'modeToggle', name: 'aria-label', value: "Toggle local neighborhood view" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-checked on modeToggle', () => {
      const ariaAttr = { part: 'modeToggle', name: 'aria-checked', value: "if state = = localView then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "application" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? ariaLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-roledescription', value: "graph visualization" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part canvas', () => {
      const connectAria = { part: 'canvas', attr: 'role', value: "img" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part canvas', () => {
      const connectAria = { part: 'canvas', attr: 'aria-label', value: "concat ( Graph with  , ? nodeCount ,  nodes and  , ? edgeCount ,  edges )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part filterPanel', () => {
      const connectAria = { part: 'filterPanel', attr: 'role', value: "complementary" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part filterPanel', () => {
      const connectAria = { part: 'filterPanel', attr: 'aria-label', value: "Graph filters" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part filterPanel', () => {
      const connectAria = { part: 'filterPanel', attr: 'aria-hidden', value: "if not ? filterPanelOpen then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part searchInput', () => {
      const connectAria = { part: 'searchInput', attr: 'aria-label', value: "Search nodes" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part typeToggles', () => {
      const connectAria = { part: 'typeToggles', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part typeToggles', () => {
      const connectAria = { part: 'typeToggles', attr: 'aria-label', value: "Node type filters" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part displayControls', () => {
      const connectAria = { part: 'displayControls', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part displayControls', () => {
      const connectAria = { part: 'displayControls', attr: 'aria-label', value: "Display settings" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part nodeSizeSlider', () => {
      const connectAria = { part: 'nodeSizeSlider', attr: 'aria-label', value: "Node size" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part linkThicknessSlider', () => {
      const connectAria = { part: 'linkThicknessSlider', attr: 'aria-label', value: "Link thickness" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part forceControls', () => {
      const connectAria = { part: 'forceControls', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part forceControls', () => {
      const connectAria = { part: 'forceControls', attr: 'aria-label', value: "Force simulation controls" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part minimap', () => {
      const connectAria = { part: 'minimap', attr: 'role', value: "img" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part minimap', () => {
      const connectAria = { part: 'minimap', attr: 'aria-label', value: "Graph minimap" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part detailPanel', () => {
      const connectAria = { part: 'detailPanel', attr: 'role', value: "complementary" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part detailPanel', () => {
      const connectAria = { part: 'detailPanel', attr: 'aria-label', value: "Node details" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part detailPanel', () => {
      const connectAria = { part: 'detailPanel', attr: 'aria-hidden', value: "if not ? selectedNodeId then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part modeToggle', () => {
      const connectAria = { part: 'modeToggle', attr: 'aria-label', value: "Toggle local neighborhood view" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-checked on connect part modeToggle', () => {
      const connectAria = { part: 'modeToggle', attr: 'aria-checked', value: "if ? viewMode = = local then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "nodes" with type "list GraphNode"', () => {
      const prop = { name: 'nodes', type: 'list GraphNode' };
      expect(prop.name).toBe('nodes');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "edges" with type "list GraphEdge"', () => {
      const prop = { name: 'edges', type: 'list GraphEdge' };
      expect(prop.name).toBe('edges');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "ariaLabel" with type "String"', () => {
      const prop = { name: 'ariaLabel', type: 'String', defaultValue: "Graph View" };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "ariaLabel" to "Graph View"', () => {
      expect("Graph View").toBe("Graph View");
    });

    it('should declare prop "nodeCount" with type "Int"', () => {
      const prop = { name: 'nodeCount', type: 'Int', defaultValue: "0" };
      expect(prop.name).toBe('nodeCount');
      expect(prop.type).toBeTruthy();
    });

    it('should default "nodeCount" to "0"', () => {
      expect("0").toBe("0");
    });

    it('should declare prop "edgeCount" with type "Int"', () => {
      const prop = { name: 'edgeCount', type: 'Int', defaultValue: "0" };
      expect(prop.name).toBe('edgeCount');
      expect(prop.type).toBeTruthy();
    });

    it('should default "edgeCount" to "0"', () => {
      expect("0").toBe("0");
    });

    it('should declare prop "zoom" with type "Float"', () => {
      const prop = { name: 'zoom', type: 'Float', defaultValue: "1 . 0" };
      expect(prop.name).toBe('zoom');
      expect(prop.type).toBeTruthy();
    });

    it('should default "zoom" to "1 . 0"', () => {
      expect("1 . 0").toBe("1 . 0");
    });

    it('should declare prop "panX" with type "Float"', () => {
      const prop = { name: 'panX', type: 'Float', defaultValue: "0 . 0" };
      expect(prop.name).toBe('panX');
      expect(prop.type).toBeTruthy();
    });

    it('should default "panX" to "0 . 0"', () => {
      expect("0 . 0").toBe("0 . 0");
    });

    it('should declare prop "panY" with type "Float"', () => {
      const prop = { name: 'panY', type: 'Float', defaultValue: "0 . 0" };
      expect(prop.name).toBe('panY');
      expect(prop.type).toBeTruthy();
    });

    it('should default "panY" to "0 . 0"', () => {
      expect("0 . 0").toBe("0 . 0");
    });

    it('should declare prop "selectedNodeId" with type "option String"', () => {
      const prop = { name: 'selectedNodeId', type: 'option String' };
      expect(prop.name).toBe('selectedNodeId');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "searchQuery" with type "String"', () => {
      const prop = { name: 'searchQuery', type: 'String', defaultValue: "" };
      expect(prop.name).toBe('searchQuery');
      expect(prop.type).toBeTruthy();
    });

    it('should default "searchQuery" to ""', () => {
      expect("").toBe("");
    });

    it('should declare prop "visibleTypes" with type "list String"', () => {
      const prop = { name: 'visibleTypes', type: 'list String', defaultValue: "[ ]" };
      expect(prop.name).toBe('visibleTypes');
      expect(prop.type).toBeTruthy();
    });

    it('should default "visibleTypes" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "nodeSize" with type "Float"', () => {
      const prop = { name: 'nodeSize', type: 'Float', defaultValue: "8 . 0" };
      expect(prop.name).toBe('nodeSize');
      expect(prop.type).toBeTruthy();
    });

    it('should default "nodeSize" to "8 . 0"', () => {
      expect("8 . 0").toBe("8 . 0");
    });

    it('should declare prop "linkThickness" with type "Float"', () => {
      const prop = { name: 'linkThickness', type: 'Float', defaultValue: "1 . 5" };
      expect(prop.name).toBe('linkThickness');
      expect(prop.type).toBeTruthy();
    });

    it('should default "linkThickness" to "1 . 5"', () => {
      expect("1 . 5").toBe("1 . 5");
    });

    it('should declare prop "chargeStrength" with type "Float"', () => {
      const prop = { name: 'chargeStrength', type: 'Float', defaultValue: "300 . 0" };
      expect(prop.name).toBe('chargeStrength');
      expect(prop.type).toBeTruthy();
    });

    it('should default "chargeStrength" to "300 . 0"', () => {
      expect("300 . 0").toBe("300 . 0");
    });

    it('should declare prop "linkDistance" with type "Float"', () => {
      const prop = { name: 'linkDistance', type: 'Float', defaultValue: "100 . 0" };
      expect(prop.name).toBe('linkDistance');
      expect(prop.type).toBeTruthy();
    });

    it('should default "linkDistance" to "100 . 0"', () => {
      expect("100 . 0").toBe("100 . 0");
    });

    it('should declare prop "viewMode" with type "union global | local"', () => {
      const prop = { name: 'viewMode', type: 'union global | local', defaultValue: "global" };
      expect(prop.name).toBe('viewMode');
      expect(prop.type).toBeTruthy();
    });

    it('should default "viewMode" to "global"', () => {
      expect("global").toBe("global");
    });

    it('should declare prop "simulationRunning" with type "Bool"', () => {
      const prop = { name: 'simulationRunning', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('simulationRunning');
      expect(prop.type).toBeTruthy();
    });

    it('should default "simulationRunning" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "filterPanelOpen" with type "Bool"', () => {
      const prop = { name: 'filterPanelOpen', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('filterPanelOpen');
      expect(prop.type).toBeTruthy();
    });

    it('should default "filterPanelOpen" to "true"', () => {
      expect("true").toBe("true");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","canvas","filterPanel","searchInput","typeToggles","displayControls","nodeSizeSlider","linkThicknessSlider","forceControls","minimap","detailPanel","modeToggle"];
      expect(parts.length).toBe(12);
      expect(parts).toContain('root');
      expect(parts).toContain('canvas');
      expect(parts).toContain('filterPanel');
      expect(parts).toContain('searchInput');
      expect(parts).toContain('typeToggles');
      expect(parts).toContain('displayControls');
      expect(parts).toContain('nodeSizeSlider');
      expect(parts).toContain('linkThicknessSlider');
      expect(parts).toContain('forceControls');
      expect(parts).toContain('minimap');
      expect(parts).toContain('detailPanel');
      expect(parts).toContain('modeToggle');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // canvas: container
      expect('container').toBeTruthy(); // filterPanel: container
      expect('widget').toBeTruthy(); // searchInput: widget
      expect('container').toBeTruthy(); // typeToggles: container
      expect('container').toBeTruthy(); // displayControls: container
      expect('widget').toBeTruthy(); // nodeSizeSlider: widget
      expect('widget').toBeTruthy(); // linkThicknessSlider: widget
      expect('container').toBeTruthy(); // forceControls: container
      expect('container').toBeTruthy(); // minimap: container
      expect('container').toBeTruthy(); // detailPanel: container
      expect('widget').toBeTruthy(); // modeToggle: widget
    });

  });

});
