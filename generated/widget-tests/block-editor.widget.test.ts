// Auto-generated by generate-widget-tests.ts from block-editor.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: BlockEditor', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["editing","block.paragraph"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('editing');
      expect(initialStates).toContain('block.paragraph');
    });

    it('should transition from editing to idle on BLUR', () => {
      const transition = { from: 'editing', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('editing');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing to slashMenuOpen on SLASH', () => {
      const transition = { from: 'editing', event: 'SLASH', target: 'slashMenuOpen' };
      expect(transition.from).toBe('editing');
      expect(transition.event).toBe('SLASH');
      expect(transition.target).toBe('slashMenuOpen');
      // Target state 'slashMenuOpen' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing to selectionActive on SELECT_TEXT', () => {
      const transition = { from: 'editing', event: 'SELECT_TEXT', target: 'selectionActive' };
      expect(transition.from).toBe('editing');
      expect(transition.event).toBe('SELECT_TEXT');
      expect(transition.target).toBe('selectionActive');
      // Target state 'selectionActive' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from editing to dragging on DRAG_START', () => {
      const transition = { from: 'editing', event: 'DRAG_START', target: 'dragging' };
      expect(transition.from).toBe('editing');
      expect(transition.event).toBe('DRAG_START');
      expect(transition.target).toBe('dragging');
      // Target state 'dragging' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to editing on FOCUS', () => {
      const transition = { from: 'idle', event: 'FOCUS', target: 'editing' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from slashMenuOpen to editing on SELECT_BLOCK_TYPE', () => {
      const transition = { from: 'slashMenuOpen', event: 'SELECT_BLOCK_TYPE', target: 'editing' };
      expect(transition.from).toBe('slashMenuOpen');
      expect(transition.event).toBe('SELECT_BLOCK_TYPE');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from slashMenuOpen to editing on ESCAPE', () => {
      const transition = { from: 'slashMenuOpen', event: 'ESCAPE', target: 'editing' };
      expect(transition.from).toBe('slashMenuOpen');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from slashMenuOpen to idle on BLUR', () => {
      const transition = { from: 'slashMenuOpen', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('slashMenuOpen');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selectionActive to editing on DESELECT', () => {
      const transition = { from: 'selectionActive', event: 'DESELECT', target: 'editing' };
      expect(transition.from).toBe('selectionActive');
      expect(transition.event).toBe('DESELECT');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selectionActive to selectionActive on FORMAT', () => {
      const transition = { from: 'selectionActive', event: 'FORMAT', target: 'selectionActive' };
      expect(transition.from).toBe('selectionActive');
      expect(transition.event).toBe('FORMAT');
      expect(transition.target).toBe('selectionActive');
      // Target state 'selectionActive' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selectionActive to editing on ESCAPE', () => {
      const transition = { from: 'selectionActive', event: 'ESCAPE', target: 'editing' };
      expect(transition.from).toBe('selectionActive');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dragging to editing on DROP', () => {
      const transition = { from: 'dragging', event: 'DROP', target: 'editing' };
      expect(transition.from).toBe('dragging');
      expect(transition.event).toBe('DROP');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from dragging to editing on ESCAPE', () => {
      const transition = { from: 'dragging', event: 'ESCAPE', target: 'editing' };
      expect(transition.from).toBe('dragging');
      expect(transition.event).toBe('ESCAPE');
      expect(transition.target).toBe('editing');
      // Target state 'editing' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to heading on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'heading' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('heading');
      // Target state 'heading' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to bulletedList on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'bulletedList' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('bulletedList');
      // Target state 'bulletedList' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to numberedList on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'numberedList' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('numberedList');
      // Target state 'numberedList' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to checklist on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'checklist' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('checklist');
      // Target state 'checklist' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to quote on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'quote' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('quote');
      // Target state 'quote' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to codeBlock on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'codeBlock' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('codeBlock');
      // Target state 'codeBlock' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to table on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'table' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('table');
      // Target state 'table' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to image on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'image' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('image');
      // Target state 'image' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to embed on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'embed' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('embed');
      // Target state 'embed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to divider on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'divider' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('divider');
      // Target state 'divider' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to callout on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'callout' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('callout');
      // Target state 'callout' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from block.paragraph to toggle on CONVERT', () => {
      const transition = { from: 'block.paragraph', event: 'CONVERT', target: 'toggle' };
      expect(transition.from).toBe('block.paragraph');
      expect(transition.event).toBe('CONVERT');
      expect(transition.target).toBe('toggle');
      // Target state 'toggle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"editing","events":["BLUR","SLASH","SELECT_TEXT","DRAG_START"]},{"name":"idle","events":["FOCUS"]},{"name":"slashMenuOpen","events":["SELECT_BLOCK_TYPE","ESCAPE","BLUR"]},{"name":"selectionActive","events":["DESELECT","FORMAT","ESCAPE"]},{"name":"dragging","events":["DROP","ESCAPE"]},{"name":"block.paragraph","events":["CONVERT","CONVERT","CONVERT","CONVERT","CONVERT","CONVERT","CONVERT","CONVERT","CONVERT","CONVERT","CONVERT","CONVERT"]},{"name":"block.heading","events":[]},{"name":"block.bulletedList","events":[]},{"name":"block.numberedList","events":[]},{"name":"block.checklist","events":[]},{"name":"block.quote","events":[]},{"name":"block.codeBlock","events":[]},{"name":"block.table","events":[]},{"name":"block.image","events":[]},{"name":"block.embed","events":[]},{"name":"block.divider","events":[]},{"name":"block.callout","events":[]},{"name":"block.toggle","events":[]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["editing","idle","slashMenuOpen","selectionActive","dragging","block.paragraph","block.heading","block.bulletedList","block.numberedList","block.checklist","block.quote","block.codeBlock","block.table","block.image","block.embed","block.divider","block.callout","block.toggle"]);
      const baseNames = new Set(["editing","idle","slashMenuOpen","selectionActive","dragging","paragraph","heading","bulletedList","numberedList","checklist","quote","codeBlock","table","image","embed","divider","callout","toggle"]);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('slashMenuOpen') || baseNames.has('slashMenuOpen')).toBe(true);
      expect(validNames.has('selectionActive') || baseNames.has('selectionActive')).toBe(true);
      expect(validNames.has('dragging') || baseNames.has('dragging')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('selectionActive') || baseNames.has('selectionActive')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('editing') || baseNames.has('editing')).toBe(true);
      expect(validNames.has('heading') || baseNames.has('heading')).toBe(true);
      expect(validNames.has('bulletedList') || baseNames.has('bulletedList')).toBe(true);
      expect(validNames.has('numberedList') || baseNames.has('numberedList')).toBe(true);
      expect(validNames.has('checklist') || baseNames.has('checklist')).toBe(true);
      expect(validNames.has('quote') || baseNames.has('quote')).toBe(true);
      expect(validNames.has('codeBlock') || baseNames.has('codeBlock')).toBe(true);
      expect(validNames.has('table') || baseNames.has('table')).toBe(true);
      expect(validNames.has('image') || baseNames.has('image')).toBe(true);
      expect(validNames.has('embed') || baseNames.has('embed')).toBe(true);
      expect(validNames.has('divider') || baseNames.has('divider')).toBe(true);
      expect(validNames.has('callout') || baseNames.has('callout')).toBe(true);
      expect(validNames.has('toggle') || baseNames.has('toggle')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if state = = dragging then dragging else if state = = slashMenuOpen then slash-open else if state = = selectionActive then selecting else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-readonly on root', () => {
      const attr = { name: 'data-readonly', value: "if ? readOnly then true else false" };
      expect(attr.name).toBe('data-readonly');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-block-count on root', () => {
      const attr = { name: 'data-block-count', value: "? blocks . length" };
      expect(attr.name).toBe('data-block-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-empty on editor', () => {
      const attr = { name: 'data-empty', value: "if ? blocks . length = = 0 then true else false" };
      expect(attr.name).toBe('data-empty');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-block-type on block', () => {
      const attr = { name: 'data-block-type', value: "self . type" };
      expect(attr.name).toBe('data-block-type');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-block-id on block', () => {
      const attr = { name: 'data-block-id', value: "self . id" };
      expect(attr.name).toBe('data-block-id');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-focused on block', () => {
      const attr = { name: 'data-focused', value: "if self . focused then true else false" };
      expect(attr.name).toBe('data-focused');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-selected on block', () => {
      const attr = { name: 'data-selected', value: "if self . selected then true else false" };
      expect(attr.name).toBe('data-selected');
      expect(attr.value).toBeDefined();
    });

    it('should bind onFocus handler on block', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS_BLOCK , { id : self . id } )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on block', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( NEW_BLOCK , { after : self . id } )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Backspace handler on block', () => {
      const handler = { event: 'onKeyDown-Backspace', action: "send ( MERGE_OR_DELETE , { id : self . id } )" };
      expect(handler.event).toBe('onKeyDown-Backspace');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Tab handler on block', () => {
      const handler = { event: 'onKeyDown-Tab', action: "send ( INDENT , { id : self . id } )" };
      expect(handler.event).toBe('onKeyDown-Tab');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="drag-handle" to blockDragHandle', () => {
      const binding = {"part":"blockDragHandle","dataPart":"drag-handle"};
      expect(binding.dataPart).toBe('drag-handle');
    });

    it('should bind onDragStart handler on blockDragHandle', () => {
      const handler = { event: 'onDragStart', action: "send ( DRAG_START , { id : self . blockId } )" };
      expect(handler.event).toBe('onDragStart');
      expect(handler.action).toBeDefined();
    });

    it('should bind onDragEnd handler on blockDragHandle', () => {
      const handler = { event: 'onDragEnd', action: "send ( DROP )" };
      expect(handler.event).toBe('onDragEnd');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on blockDragHandle', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( DRAG_START , { id : self . blockId } )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on blockDragHandle', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "send ( MOVE_BLOCK_UP , { id : self . blockId } )" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on blockDragHandle', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "send ( MOVE_BLOCK_DOWN , { id : self . blockId } )" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="block-menu" to blockMenu', () => {
      const binding = {"part":"blockMenu","dataPart":"block-menu"};
      expect(binding.dataPart).toBe('block-menu');
    });

    it('should bind data-block-id on blockMenu', () => {
      const attr = { name: 'data-block-id', value: "self . blockId" };
      expect(attr.name).toBe('data-block-id');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="slash-menu" to slashMenu', () => {
      const binding = {"part":"slashMenu","dataPart":"slash-menu"};
      expect(binding.dataPart).toBe('slash-menu');
    });

    it('should bind data-state on slashMenu', () => {
      const attr = { name: 'data-state', value: "if state = = slashMenuOpen then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="selection-toolbar" to selectionToolbar', () => {
      const binding = {"part":"selectionToolbar","dataPart":"selection-toolbar"};
      expect(binding.dataPart).toBe('selection-toolbar');
    });

    it('should bind data-state on selectionToolbar', () => {
      const attr = { name: 'data-state', value: "if state = = selectionActive then visible else hidden" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="placeholder" to placeholder', () => {
      const binding = {"part":"placeholder","dataPart":"placeholder"};
      expect(binding.dataPart).toBe('placeholder');
    });

    it('should bind data-visible on placeholder', () => {
      const attr = { name: 'data-visible', value: "if self . blockEmpty then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","editor","block","blockDragHandle","blockMenu","slashMenu","selectionToolbar","placeholder"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('editor')).toBe(true);
      expect(anatomyParts.has('block')).toBe(true);
      expect(anatomyParts.has('blockDragHandle')).toBe(true);
      expect(anatomyParts.has('blockMenu')).toBe(true);
      expect(anatomyParts.has('slashMenu')).toBe(true);
      expect(anatomyParts.has('selectionToolbar')).toBe(true);
      expect(anatomyParts.has('placeholder')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: NEW_BLOCK', () => {
      const binding = { key: 'Enter', action: 'NEW_BLOCK' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('NEW_BLOCK');
    });

    it('should handle Backspace key with action: MERGE_OR_DELETE', () => {
      const binding = { key: 'Backspace', action: 'MERGE_OR_DELETE' };
      expect(binding.key).toBe('Backspace');
      expect(binding.action).toBe('MERGE_OR_DELETE');
    });

    it('should handle Tab key with action: INDENT', () => {
      const binding = { key: 'Tab', action: 'INDENT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('INDENT');
    });

    it('should handle Shift+Tab key with action: OUTDENT', () => {
      const binding = { key: 'Shift+Tab', action: 'OUTDENT' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('OUTDENT');
    });

    it('should handle ArrowUp key with action: FOCUS_PREV_BLOCK', () => {
      const binding = { key: 'ArrowUp', action: 'FOCUS_PREV_BLOCK' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('FOCUS_PREV_BLOCK');
    });

    it('should handle ArrowDown key with action: FOCUS_NEXT_BLOCK', () => {
      const binding = { key: 'ArrowDown', action: 'FOCUS_NEXT_BLOCK' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('FOCUS_NEXT_BLOCK');
    });

    it('should handle Slash key with action: SLASH', () => {
      const binding = { key: 'Slash', action: 'SLASH' };
      expect(binding.key).toBe('Slash');
      expect(binding.action).toBe('SLASH');
    });

    it('should handle Escape key with action: ESCAPE', () => {
      const binding = { key: 'Escape', action: 'ESCAPE' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('ESCAPE');
    });

    it('should handle Control+Shift+ArrowUp key with action: MOVE_BLOCK_UP', () => {
      const binding = { key: 'Control+Shift+ArrowUp', action: 'MOVE_BLOCK_UP' };
      expect(binding.key).toBe('Control+Shift+ArrowUp');
      expect(binding.action).toBe('MOVE_BLOCK_UP');
    });

    it('should handle Control+Shift+ArrowDown key with action: MOVE_BLOCK_DOWN', () => {
      const binding = { key: 'Control+Shift+ArrowDown', action: 'MOVE_BLOCK_DOWN' };
      expect(binding.key).toBe('Control+Shift+ArrowDown');
      expect(binding.action).toBe('MOVE_BLOCK_DOWN');
    });

    it('should handle Control+d key with action: DUPLICATE_BLOCK', () => {
      const binding = { key: 'Control+d', action: 'DUPLICATE_BLOCK' };
      expect(binding.key).toBe('Control+d');
      expect(binding.action).toBe('DUPLICATE_BLOCK');
    });

    it('should handle Control+b key with action: FORMAT_BOLD', () => {
      const binding = { key: 'Control+b', action: 'FORMAT_BOLD' };
      expect(binding.key).toBe('Control+b');
      expect(binding.action).toBe('FORMAT_BOLD');
    });

    it('should handle Control+i key with action: FORMAT_ITALIC', () => {
      const binding = { key: 'Control+i', action: 'FORMAT_ITALIC' };
      expect(binding.key).toBe('Control+i');
      expect(binding.action).toBe('FORMAT_ITALIC');
    });

    it('should handle Control+u key with action: FORMAT_UNDERLINE', () => {
      const binding = { key: 'Control+u', action: 'FORMAT_UNDERLINE' };
      expect(binding.key).toBe('Control+u');
      expect(binding.action).toBe('FORMAT_UNDERLINE');
    });

    it('should handle Control+k key with action: FORMAT_LINK', () => {
      const binding = { key: 'Control+k', action: 'FORMAT_LINK' };
      expect(binding.key).toBe('Control+k');
      expect(binding.action).toBe('FORMAT_LINK');
    });

    it('should handle Control+e key with action: FORMAT_CODE', () => {
      const binding = { key: 'Control+e', action: 'FORMAT_CODE' };
      expect(binding.key).toBe('Control+e');
      expect(binding.action).toBe('FORMAT_CODE');
    });

    it('should handle Control+z key with action: UNDO', () => {
      const binding = { key: 'Control+z', action: 'UNDO' };
      expect(binding.key).toBe('Control+z');
      expect(binding.action).toBe('UNDO');
    });

    it('should handle Control+Shift+z key with action: REDO', () => {
      const binding = { key: 'Control+Shift+z', action: 'REDO' };
      expect(binding.key).toBe('Control+Shift+z');
      expect(binding.action).toBe('REDO');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["BLUR","SLASH","SELECT_TEXT","DRAG_START","FOCUS","SELECT_BLOCK_TYPE","ESCAPE","DESELECT","FORMAT","DROP","CONVERT"]);
      const keyboardActions = ["NEW_BLOCK","MERGE_OR_DELETE","INDENT","OUTDENT","FOCUS_PREV_BLOCK","FOCUS_NEXT_BLOCK","SLASH","ESCAPE","MOVE_BLOCK_UP","MOVE_BLOCK_DOWN","DUPLICATE_BLOCK","FORMAT_BOLD","FORMAT_ITALIC","FORMAT_UNDERLINE","FORMAT_LINK","FORMAT_CODE","UNDO","REDO"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "textbox"', () => {
      expect('textbox').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "block"', () => {
      expect('block').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "textbox" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-multiline on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-multiline', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? ariaLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-readonly on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-readonly', value: "if ? readOnly then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on editor', () => {
      const ariaAttr = { part: 'editor', name: 'role', value: "document" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on editor', () => {
      const ariaAttr = { part: 'editor', name: 'aria-label', value: "Editor content" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on block', () => {
      const ariaAttr = { part: 'block', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on block', () => {
      const ariaAttr = { part: 'block', name: 'aria-label', value: "concat ( Block  , self . index , :  , self . type )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-grabbed on block', () => {
      const ariaAttr = { part: 'block', name: 'aria-grabbed', value: "if state = = dragging then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on block', () => {
      const ariaAttr = { part: 'block', name: 'aria-roledescription', value: "content block" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on blockDragHandle', () => {
      const ariaAttr = { part: 'blockDragHandle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on blockDragHandle', () => {
      const ariaAttr = { part: 'blockDragHandle', name: 'aria-label', value: "Drag to reorder block" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on blockDragHandle', () => {
      const ariaAttr = { part: 'blockDragHandle', name: 'aria-roledescription', value: "drag handle" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on placeholder', () => {
      const ariaAttr = { part: 'placeholder', name: 'aria-hidden', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part editor', () => {
      const connectAria = { part: 'editor', attr: 'role', value: "document" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part editor', () => {
      const connectAria = { part: 'editor', attr: 'aria-label', value: "Editor content" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part block', () => {
      const connectAria = { part: 'block', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part block', () => {
      const connectAria = { part: 'block', attr: 'aria-label', value: "concat ( Block  , self . index , :  , self . type )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-grabbed on connect part block', () => {
      const connectAria = { part: 'block', attr: 'aria-grabbed', value: "if state = = dragging then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part blockDragHandle', () => {
      const connectAria = { part: 'blockDragHandle', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part blockDragHandle', () => {
      const connectAria = { part: 'blockDragHandle', attr: 'aria-label', value: "Drag to reorder block" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part blockDragHandle', () => {
      const connectAria = { part: 'blockDragHandle', attr: 'aria-roledescription', value: "drag handle" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part blockMenu', () => {
      const connectAria = { part: 'blockMenu', attr: 'aria-label', value: "Block actions" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part slashMenu', () => {
      const connectAria = { part: 'slashMenu', attr: 'aria-label', value: "Block type palette" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part selectionToolbar', () => {
      const connectAria = { part: 'selectionToolbar', attr: 'aria-label', value: "Text formatting toolbar" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part placeholder', () => {
      const connectAria = { part: 'placeholder', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "blocks" with type "list BlockDef"', () => {
      const prop = { name: 'blocks', type: 'list BlockDef' };
      expect(prop.name).toBe('blocks');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "ariaLabel" with type "String"', () => {
      const prop = { name: 'ariaLabel', type: 'String', defaultValue: "Block editor" };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "ariaLabel" to "Block editor"', () => {
      expect("Block editor").toBe("Block editor");
    });

    it('should declare prop "readOnly" with type "Bool"', () => {
      const prop = { name: 'readOnly', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('readOnly');
      expect(prop.type).toBeTruthy();
    });

    it('should default "readOnly" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "placeholder" with type "String"', () => {
      const prop = { name: 'placeholder', type: 'String', defaultValue: "Type '/' for commands..." };
      expect(prop.name).toBe('placeholder');
      expect(prop.type).toBeTruthy();
    });

    it('should default "placeholder" to "Type '/' for commands..."', () => {
      expect("Type '/' for commands...").toBe("Type '/' for commands...");
    });

    it('should declare prop "blockTypes" with type "list String"', () => {
      const prop = { name: 'blockTypes', type: 'list String', defaultValue: "[ paragraph , heading-1 , heading-2 , heading-3 , heading-4 , heading-5 , heading-6 , bulleted-list , numbered-list , checklist , quote , code-block , table , image , embed , divider , callout , toggle ]" };
      expect(prop.name).toBe('blockTypes');
      expect(prop.type).toBeTruthy();
    });

    it('should default "blockTypes" to "[ paragraph , heading-1 , heading-2 , heading-3 , heading-4 , heading-5 , heading-6 , bulleted-list , numbered-list , checklist , quote , code-block , table , image , embed , divider , callout , toggle ]"', () => {
      expect("[ paragraph , heading-1 , heading-2 , heading-3 , heading-4 , heading-5 , heading-6 , bulleted-list , numbered-list , checklist , quote , code-block , table , image , embed , divider , callout , toggle ]").toBe("[ paragraph , heading-1 , heading-2 , heading-3 , heading-4 , heading-5 , heading-6 , bulleted-list , numbered-list , checklist , quote , code-block , table , image , embed , divider , callout , toggle ]");
    });

    it('should declare prop "autoFocus" with type "Bool"', () => {
      const prop = { name: 'autoFocus', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('autoFocus');
      expect(prop.type).toBeTruthy();
    });

    it('should default "autoFocus" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "spellCheck" with type "Bool"', () => {
      const prop = { name: 'spellCheck', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('spellCheck');
      expect(prop.type).toBeTruthy();
    });

    it('should default "spellCheck" to "true"', () => {
      expect("true").toBe("true");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","editor","block","blockDragHandle","blockMenu","slashMenu","selectionToolbar","placeholder"];
      expect(parts.length).toBe(8);
      expect(parts).toContain('root');
      expect(parts).toContain('editor');
      expect(parts).toContain('block');
      expect(parts).toContain('blockDragHandle');
      expect(parts).toContain('blockMenu');
      expect(parts).toContain('slashMenu');
      expect(parts).toContain('selectionToolbar');
      expect(parts).toContain('placeholder');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // editor: container
      expect('container').toBeTruthy(); // block: container
      expect('action').toBeTruthy(); // blockDragHandle: action
      expect('widget').toBeTruthy(); // blockMenu: widget
      expect('widget').toBeTruthy(); // slashMenu: widget
      expect('widget').toBeTruthy(); // selectionToolbar: widget
      expect('text').toBeTruthy(); // placeholder: text
    });

  });

});
