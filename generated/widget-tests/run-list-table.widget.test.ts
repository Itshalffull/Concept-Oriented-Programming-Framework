// Auto-generated by generate-widget-tests.ts from run-list-table.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: RunListTable', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('idle');
    });

    it('should transition from idle to rowSelected on SELECT_ROW', () => {
      const transition = { from: 'idle', event: 'SELECT_ROW', target: 'rowSelected' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('SELECT_ROW');
      expect(transition.target).toBe('rowSelected');
      // Target state 'rowSelected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to idle on SORT', () => {
      const transition = { from: 'idle', event: 'SORT', target: 'idle' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('SORT');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to idle on FILTER', () => {
      const transition = { from: 'idle', event: 'FILTER', target: 'idle' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('FILTER');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to idle on PAGE', () => {
      const transition = { from: 'idle', event: 'PAGE', target: 'idle' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('PAGE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from rowSelected to idle on DESELECT', () => {
      const transition = { from: 'rowSelected', event: 'DESELECT', target: 'idle' };
      expect(transition.from).toBe('rowSelected');
      expect(transition.event).toBe('DESELECT');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from rowSelected to rowSelected on SELECT_ROW', () => {
      const transition = { from: 'rowSelected', event: 'SELECT_ROW', target: 'rowSelected' };
      expect(transition.from).toBe('rowSelected');
      expect(transition.event).toBe('SELECT_ROW');
      expect(transition.target).toBe('rowSelected');
      // Target state 'rowSelected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"idle","events":["SELECT_ROW","SORT","FILTER","PAGE"]},{"name":"rowSelected","events":["DESELECT","SELECT_ROW"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["idle","rowSelected"]);
      const baseNames = new Set(["idle","rowSelected"]);
      expect(validNames.has('rowSelected') || baseNames.has('rowSelected')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('rowSelected') || baseNames.has('rowSelected')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowDown key with action: FOCUS_NEXT_ROW', () => {
      const binding = { key: 'ArrowDown', action: 'FOCUS_NEXT_ROW' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('FOCUS_NEXT_ROW');
    });

    it('should handle ArrowUp key with action: FOCUS_PREV_ROW', () => {
      const binding = { key: 'ArrowUp', action: 'FOCUS_PREV_ROW' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('FOCUS_PREV_ROW');
    });

    it('should handle Enter key with action: SELECT_ROW', () => {
      const binding = { key: 'Enter', action: 'SELECT_ROW' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('SELECT_ROW');
    });

    it('should handle Escape key with action: DESELECT', () => {
      const binding = { key: 'Escape', action: 'DESELECT' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('DESELECT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SELECT_ROW","SORT","FILTER","PAGE","DESELECT"]);
      const keyboardActions = ["FOCUS_NEXT_ROW","FOCUS_PREV_ROW","SELECT_ROW","DESELECT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "table"', () => {
      expect('table').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "table"', () => {
      expect('table').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Process runs" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on table', () => {
      const ariaAttr = { part: 'table', name: 'role', value: "table" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on table', () => {
      const ariaAttr = { part: 'table', name: 'aria-label', value: "Run list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on headerRow', () => {
      const ariaAttr = { part: 'headerRow', name: 'role', value: "row" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on dataRow', () => {
      const ariaAttr = { part: 'dataRow', name: 'role', value: "row" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on dataRow', () => {
      const ariaAttr = { part: 'dataRow', name: 'aria-selected', value: "if self . isSelected then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on statusCell', () => {
      const ariaAttr = { part: 'statusCell', name: 'role', value: "cell" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on statusCell', () => {
      const ariaAttr = { part: 'statusCell', name: 'aria-label', value: "concat ( Status:  , self . status )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "pageSize" with type "Int"', () => {
      const prop = { name: 'pageSize', type: 'Int', defaultValue: "20" };
      expect(prop.name).toBe('pageSize');
      expect(prop.type).toBeTruthy();
    });

    it('should default "pageSize" to "20"', () => {
      expect("20").toBe("20");
    });

    it('should declare prop "sortBy" with type "String"', () => {
      const prop = { name: 'sortBy', type: 'String', defaultValue: "started_at" };
      expect(prop.name).toBe('sortBy');
      expect(prop.type).toBeTruthy();
    });

    it('should default "sortBy" to "started_at"', () => {
      expect("started_at").toBe("started_at");
    });

    it('should declare prop "sortOrder" with type "union asc | desc"', () => {
      const prop = { name: 'sortOrder', type: 'union asc | desc', defaultValue: "desc" };
      expect(prop.name).toBe('sortOrder');
      expect(prop.type).toBeTruthy();
    });

    it('should default "sortOrder" to "desc"', () => {
      expect("desc").toBe("desc");
    });

    it('should declare prop "filterStatus" with type "option String"', () => {
      const prop = { name: 'filterStatus', type: 'option String' };
      expect(prop.name).toBe('filterStatus');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","filterBar","table","headerRow","dataRow","statusCell","nameCell","startCell","durationCell","outcomeCell","pagination"];
      expect(parts.length).toBe(11);
      expect(parts).toContain('root');
      expect(parts).toContain('filterBar');
      expect(parts).toContain('table');
      expect(parts).toContain('headerRow');
      expect(parts).toContain('dataRow');
      expect(parts).toContain('statusCell');
      expect(parts).toContain('nameCell');
      expect(parts).toContain('startCell');
      expect(parts).toContain('durationCell');
      expect(parts).toContain('outcomeCell');
      expect(parts).toContain('pagination');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // filterBar: container
      expect('container').toBeTruthy(); // table: container
      expect('container').toBeTruthy(); // headerRow: container
      expect('container').toBeTruthy(); // dataRow: container
      expect('container').toBeTruthy(); // statusCell: container
      expect('text').toBeTruthy(); // nameCell: text
      expect('text').toBeTruthy(); // startCell: text
      expect('text').toBeTruthy(); // durationCell: text
      expect('container').toBeTruthy(); // outcomeCell: container
      expect('widget').toBeTruthy(); // pagination: widget
    });

  });

});
