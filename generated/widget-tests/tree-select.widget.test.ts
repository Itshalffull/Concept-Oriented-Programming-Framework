// Auto-generated by generate-widget-tests.ts from tree-select.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: TreeSelect', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["item.collapsed","selection.unchecked","focus.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('item.collapsed');
      expect(initialStates).toContain('selection.unchecked');
      expect(initialStates).toContain('focus.idle');
    });

    it('should transition from item.collapsed to expanded on EXPAND', () => {
      const transition = { from: 'item.collapsed', event: 'EXPAND', target: 'expanded' };
      expect(transition.from).toBe('item.collapsed');
      expect(transition.event).toBe('EXPAND');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.collapsed to expanded on TOGGLE', () => {
      const transition = { from: 'item.collapsed', event: 'TOGGLE', target: 'expanded' };
      expect(transition.from).toBe('item.collapsed');
      expect(transition.event).toBe('TOGGLE');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.expanded to collapsed on COLLAPSE', () => {
      const transition = { from: 'item.expanded', event: 'COLLAPSE', target: 'collapsed' };
      expect(transition.from).toBe('item.expanded');
      expect(transition.event).toBe('COLLAPSE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from item.expanded to collapsed on TOGGLE', () => {
      const transition = { from: 'item.expanded', event: 'TOGGLE', target: 'collapsed' };
      expect(transition.from).toBe('item.expanded');
      expect(transition.event).toBe('TOGGLE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selection.unchecked to checked on CHECK', () => {
      const transition = { from: 'selection.unchecked', event: 'CHECK', target: 'checked' };
      expect(transition.from).toBe('selection.unchecked');
      expect(transition.event).toBe('CHECK');
      expect(transition.target).toBe('checked');
      // Target state 'checked' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selection.unchecked to indeterminate on PARTIAL_CHECK', () => {
      const transition = { from: 'selection.unchecked', event: 'PARTIAL_CHECK', target: 'indeterminate' };
      expect(transition.from).toBe('selection.unchecked');
      expect(transition.event).toBe('PARTIAL_CHECK');
      expect(transition.target).toBe('indeterminate');
      // Target state 'indeterminate' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selection.checked to unchecked on UNCHECK', () => {
      const transition = { from: 'selection.checked', event: 'UNCHECK', target: 'unchecked' };
      expect(transition.from).toBe('selection.checked');
      expect(transition.event).toBe('UNCHECK');
      expect(transition.target).toBe('unchecked');
      // Target state 'unchecked' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selection.checked to indeterminate on PARTIAL_CHECK', () => {
      const transition = { from: 'selection.checked', event: 'PARTIAL_CHECK', target: 'indeterminate' };
      expect(transition.from).toBe('selection.checked');
      expect(transition.event).toBe('PARTIAL_CHECK');
      expect(transition.target).toBe('indeterminate');
      // Target state 'indeterminate' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selection.indeterminate to checked on CHECK', () => {
      const transition = { from: 'selection.indeterminate', event: 'CHECK', target: 'checked' };
      expect(transition.from).toBe('selection.indeterminate');
      expect(transition.event).toBe('CHECK');
      expect(transition.target).toBe('checked');
      // Target state 'checked' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selection.indeterminate to unchecked on UNCHECK', () => {
      const transition = { from: 'selection.indeterminate', event: 'UNCHECK', target: 'unchecked' };
      expect(transition.from).toBe('selection.indeterminate');
      expect(transition.event).toBe('UNCHECK');
      expect(transition.target).toBe('unchecked');
      // Target state 'unchecked' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.idle to focused on FOCUS_ITEM', () => {
      const transition = { from: 'focus.idle', event: 'FOCUS_ITEM', target: 'focused' };
      expect(transition.from).toBe('focus.idle');
      expect(transition.event).toBe('FOCUS_ITEM');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.focused to idle on BLUR', () => {
      const transition = { from: 'focus.focused', event: 'BLUR', target: 'idle' };
      expect(transition.from).toBe('focus.focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.focused to focused on FOCUS_ITEM', () => {
      const transition = { from: 'focus.focused', event: 'FOCUS_ITEM', target: 'focused' };
      expect(transition.from).toBe('focus.focused');
      expect(transition.event).toBe('FOCUS_ITEM');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"item.collapsed","events":["EXPAND","TOGGLE"]},{"name":"item.expanded","events":["COLLAPSE","TOGGLE"]},{"name":"selection.unchecked","events":["CHECK","PARTIAL_CHECK"]},{"name":"selection.checked","events":["UNCHECK","PARTIAL_CHECK"]},{"name":"selection.indeterminate","events":["CHECK","UNCHECK"]},{"name":"focus.idle","events":["FOCUS_ITEM"]},{"name":"focus.focused","events":["BLUR","FOCUS_ITEM"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["item.collapsed","item.expanded","selection.unchecked","selection.checked","selection.indeterminate","focus.idle","focus.focused"]);
      const baseNames = new Set(["collapsed","expanded","unchecked","checked","indeterminate","idle","focused"]);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('checked') || baseNames.has('checked')).toBe(true);
      expect(validNames.has('indeterminate') || baseNames.has('indeterminate')).toBe(true);
      expect(validNames.has('unchecked') || baseNames.has('unchecked')).toBe(true);
      expect(validNames.has('indeterminate') || baseNames.has('indeterminate')).toBe(true);
      expect(validNames.has('checked') || baseNames.has('checked')).toBe(true);
      expect(validNames.has('unchecked') || baseNames.has('unchecked')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="root" to root', () => {
      const binding = {"part":"root","dataPart":"root"};
      expect(binding.dataPart).toBe('root');
    });

    it('should bind data-disabled on root', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on root', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "send ( FOCUS_NEXT )" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on root', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "send ( FOCUS_PREV )" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Home handler on root', () => {
      const handler = { event: 'onKeyDown-Home', action: "send ( FOCUS_FIRST )" };
      expect(handler.event).toBe('onKeyDown-Home');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-End handler on root', () => {
      const handler = { event: 'onKeyDown-End', action: "send ( FOCUS_LAST )" };
      expect(handler.event).toBe('onKeyDown-End');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="item" to item', () => {
      const binding = {"part":"item","dataPart":"item"};
      expect(binding.dataPart).toBe('item');
    });

    it('should bind data-state on item', () => {
      const attr = { name: 'data-state', value: "if item . state = = expanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-selected on item', () => {
      const attr = { name: 'data-selected', value: "if item . selection = = checked then true else false" };
      expect(attr.name).toBe('data-selected');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-indeterminate on item', () => {
      const attr = { name: 'data-indeterminate', value: "if item . selection = = indeterminate then true else false" };
      expect(attr.name).toBe('data-indeterminate');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-depth on item', () => {
      const attr = { name: 'data-depth', value: "item . depth" };
      expect(attr.name).toBe('data-depth');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-has-children on item', () => {
      const attr = { name: 'data-has-children', value: "if item . hasChildren then true else false" };
      expect(attr.name).toBe('data-has-children');
      expect(attr.value).toBeDefined();
    });

    it('should bind onKeyDown-ArrowLeft handler on item', () => {
      const handler = { event: 'onKeyDown-ArrowLeft', action: "send ( COLLAPSE_OR_PARENT , { itemId : item . id } )" };
      expect(handler.event).toBe('onKeyDown-ArrowLeft');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowRight handler on item', () => {
      const handler = { event: 'onKeyDown-ArrowRight', action: "send ( EXPAND_OR_CHILD , { itemId : item . id } )" };
      expect(handler.event).toBe('onKeyDown-ArrowRight');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on item', () => {
      const handler = { event: 'onKeyDown-Space', action: "if ? selectable then send ( TOGGLE_CHECK , { itemId : item . id } )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on item', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( ACTIVATE , { itemId : item . id } )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Asterisk handler on item', () => {
      const handler = { event: 'onKeyDown-Asterisk', action: "send ( EXPAND_ALL_SIBLINGS , { itemId : item . id } )" };
      expect(handler.event).toBe('onKeyDown-Asterisk');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on item', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS_ITEM , { itemId : item . id } )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on item', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="item-toggle" to itemToggle', () => {
      const binding = {"part":"itemToggle","dataPart":"item-toggle"};
      expect(binding.dataPart).toBe('item-toggle');
    });

    it('should bind data-state on itemToggle', () => {
      const attr = { name: 'data-state', value: "if item . state = = expanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-visible on itemToggle', () => {
      const attr = { name: 'data-visible', value: "if item . hasChildren then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on itemToggle', () => {
      const handler = { event: 'onClick', action: "send ( TOGGLE , { itemId : item . id } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="item-checkbox" to itemCheckbox', () => {
      const binding = {"part":"itemCheckbox","dataPart":"item-checkbox"};
      expect(binding.dataPart).toBe('item-checkbox');
    });

    it('should bind data-visible on itemCheckbox', () => {
      const attr = { name: 'data-visible', value: "if ? selectable then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onChange handler on itemCheckbox', () => {
      const handler = { event: 'onChange', action: "send ( TOGGLE_CHECK , { itemId : item . id } )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="item-label" to itemLabel', () => {
      const binding = {"part":"itemLabel","dataPart":"item-label"};
      expect(binding.dataPart).toBe('item-label');
    });

    it('should bind data-selected on itemLabel', () => {
      const attr = { name: 'data-selected', value: "if item . selection = = checked then true else false" };
      expect(attr.name).toBe('data-selected');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-disabled on itemLabel', () => {
      const attr = { name: 'data-disabled', value: "if item . disabled or ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="item-children" to itemChildren', () => {
      const binding = {"part":"itemChildren","dataPart":"item-children"};
      expect(binding.dataPart).toBe('item-children');
    });

    it('should bind data-state on itemChildren', () => {
      const attr = { name: 'data-state', value: "if item . state = = expanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-visible on itemChildren', () => {
      const attr = { name: 'data-visible', value: "if item . state = = expanded then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","item","itemToggle","itemCheckbox","itemLabel","itemChildren"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('item')).toBe(true);
      expect(anatomyParts.has('itemToggle')).toBe(true);
      expect(anatomyParts.has('itemCheckbox')).toBe(true);
      expect(anatomyParts.has('itemLabel')).toBe(true);
      expect(anatomyParts.has('itemChildren')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowDown key with action: FOCUS_NEXT', () => {
      const binding = { key: 'ArrowDown', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle ArrowUp key with action: FOCUS_PREV', () => {
      const binding = { key: 'ArrowUp', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should handle ArrowLeft key with action: COLLAPSE_OR_PARENT', () => {
      const binding = { key: 'ArrowLeft', action: 'COLLAPSE_OR_PARENT' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('COLLAPSE_OR_PARENT');
    });

    it('should handle ArrowRight key with action: EXPAND_OR_CHILD', () => {
      const binding = { key: 'ArrowRight', action: 'EXPAND_OR_CHILD' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('EXPAND_OR_CHILD');
    });

    it('should handle Home key with action: FOCUS_FIRST', () => {
      const binding = { key: 'Home', action: 'FOCUS_FIRST' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('FOCUS_FIRST');
    });

    it('should handle End key with action: FOCUS_LAST', () => {
      const binding = { key: 'End', action: 'FOCUS_LAST' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('FOCUS_LAST');
    });

    it('should handle Space key with action: TOGGLE_CHECK', () => {
      const binding = { key: 'Space', action: 'TOGGLE_CHECK' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('TOGGLE_CHECK');
    });

    it('should handle Enter key with action: ACTIVATE', () => {
      const binding = { key: 'Enter', action: 'ACTIVATE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('ACTIVATE');
    });

    it('should handle Asterisk key with action: EXPAND_ALL_SIBLINGS', () => {
      const binding = { key: 'Asterisk', action: 'EXPAND_ALL_SIBLINGS' };
      expect(binding.key).toBe('Asterisk');
      expect(binding.action).toBe('EXPAND_ALL_SIBLINGS');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["EXPAND","TOGGLE","COLLAPSE","CHECK","PARTIAL_CHECK","UNCHECK","FOCUS_ITEM","BLUR"]);
      const keyboardActions = ["FOCUS_NEXT","FOCUS_PREV","COLLAPSE_OR_PARENT","EXPAND_OR_CHILD","FOCUS_FIRST","FOCUS_LAST","TOGGLE_CHECK","ACTIVATE","EXPAND_ALL_SIBLINGS"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "tree"', () => {
      expect('tree').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "tree" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? label" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-multiselectable on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-multiselectable', value: "if ? multiSelect then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on item', () => {
      const ariaAttr = { part: 'item', name: 'role', value: "treeitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-expanded', value: "if item . hasChildren then ( if item . state = = expanded then true else false ) else none" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-selected', value: "if ? selectable then ( if item . selection = = checked then true else false ) else none" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-checked on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-checked', value: "if ? selectable and ? multiSelect then ( if item . selection = = checked then true else if item . selection = = indeterminate then mixed else false ) else none" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-level on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-level', value: "item . depth" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-setsize on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-setsize', value: "item . siblingCount" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-posinset on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-posinset', value: "item . positionInSet" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-label', value: "item . label" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-disabled on item', () => {
      const ariaAttr = { part: 'item', name: 'aria-disabled', value: "if item . disabled then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on itemToggle', () => {
      const ariaAttr = { part: 'itemToggle', name: 'aria-label', value: "if item . state = = expanded then Collapse else Expand" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on itemToggle', () => {
      const ariaAttr = { part: 'itemToggle', name: 'aria-hidden', value: "if item . hasChildren then false else true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on itemChildren', () => {
      const ariaAttr = { part: 'itemChildren', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "tree" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? label" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-multiselectable on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-multiselectable', value: "if ? multiSelect then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part item', () => {
      const connectAria = { part: 'item', attr: 'role', value: "treeitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-expanded', value: "if item . hasChildren then ( if item . state = = expanded then true else false ) else none" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-selected', value: "if ? selectable then ( if item . selection = = checked then true else false ) else none" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-checked on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-checked', value: "if ? selectable and ? multiSelect then ( if item . selection = = checked then true else if item . selection = = indeterminate then mixed else false ) else none" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-level on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-level', value: "item . depth" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-setsize on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-setsize', value: "item . siblingCount" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-posinset on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-posinset', value: "item . positionInSet" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-disabled on connect part item', () => {
      const connectAria = { part: 'item', attr: 'aria-disabled', value: "if item . disabled or ? disabled then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part itemToggle', () => {
      const connectAria = { part: 'itemToggle', attr: 'aria-label', value: "if item . state = = expanded then Collapse else Expand" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part itemToggle', () => {
      const connectAria = { part: 'itemToggle', attr: 'aria-hidden', value: "if not item . hasChildren then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part itemCheckbox', () => {
      const connectAria = { part: 'itemCheckbox', attr: 'aria-label', value: "concat ( Select  , item . label )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part itemChildren', () => {
      const connectAria = { part: 'itemChildren', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part itemChildren', () => {
      const connectAria = { part: 'itemChildren', attr: 'aria-hidden', value: "if item . state = = collapsed then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "items" with type "list TreeNode"', () => {
      const prop = { name: 'items', type: 'list TreeNode' };
      expect(prop.name).toBe('items');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "selectable" with type "Bool"', () => {
      const prop = { name: 'selectable', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('selectable');
      expect(prop.type).toBeTruthy();
    });

    it('should default "selectable" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "multiSelect" with type "Bool"', () => {
      const prop = { name: 'multiSelect', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('multiSelect');
      expect(prop.type).toBeTruthy();
    });

    it('should default "multiSelect" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "defaultExpanded" with type "set String"', () => {
      const prop = { name: 'defaultExpanded', type: 'set String' };
      expect(prop.name).toBe('defaultExpanded');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "label" with type "String"', () => {
      const prop = { name: 'label', type: 'String', defaultValue: "Tree" };
      expect(prop.name).toBe('label');
      expect(prop.type).toBeTruthy();
    });

    it('should default "label" to "Tree"', () => {
      expect("Tree").toBe("Tree");
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "value" with type "option set String"', () => {
      const prop = { name: 'value', type: 'option set String' };
      expect(prop.name).toBe('value');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","item","itemToggle","itemCheckbox","itemLabel","itemChildren"];
      expect(parts.length).toBe(6);
      expect(parts).toContain('root');
      expect(parts).toContain('item');
      expect(parts).toContain('itemToggle');
      expect(parts).toContain('itemCheckbox');
      expect(parts).toContain('itemLabel');
      expect(parts).toContain('itemChildren');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // item: container
      expect('action').toBeTruthy(); // itemToggle: action
      expect('widget').toBeTruthy(); // itemCheckbox: widget
      expect('text').toBeTruthy(); // itemLabel: text
      expect('container').toBeTruthy(); // itemChildren: container
    });

  });

});
