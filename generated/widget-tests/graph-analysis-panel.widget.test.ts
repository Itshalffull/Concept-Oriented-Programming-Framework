// Auto-generated by generate-widget-tests.ts from graph-analysis-panel.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: GraphAnalysisPanel', () => {
  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","category-tabs","algorithm-selector","config-form","run-button","status-bar","results-panel","scores-table","overlay-controls","overlay-toggle-list","report-section","report-format-selector","compare-button"];
      expect(parts.length).toBe(13);
      expect(parts).toContain('root');
      expect(parts).toContain('category-tabs');
      expect(parts).toContain('algorithm-selector');
      expect(parts).toContain('config-form');
      expect(parts).toContain('run-button');
      expect(parts).toContain('status-bar');
      expect(parts).toContain('results-panel');
      expect(parts).toContain('scores-table');
      expect(parts).toContain('overlay-controls');
      expect(parts).toContain('overlay-toggle-list');
      expect(parts).toContain('report-section');
      expect(parts).toContain('report-format-selector');
      expect(parts).toContain('compare-button');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // category-tabs: container
      expect('action').toBeTruthy(); // algorithm-selector: action
      expect('container').toBeTruthy(); // config-form: container
      expect('action').toBeTruthy(); // run-button: action
      expect('container').toBeTruthy(); // status-bar: container
      expect('container').toBeTruthy(); // results-panel: container
      expect('container').toBeTruthy(); // scores-table: container
      expect('container').toBeTruthy(); // overlay-controls: container
      expect('container').toBeTruthy(); // overlay-toggle-list: container
      expect('container').toBeTruthy(); // report-section: container
      expect('action').toBeTruthy(); // report-format-selector: action
      expect('action').toBeTruthy(); // compare-button: action
    });

  });

});
