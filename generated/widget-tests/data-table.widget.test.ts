// Auto-generated by generate-widget-tests.ts from data-table.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: DataTable', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["idle","row.unselected","focus.unfocused"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('idle');
      expect(initialStates).toContain('row.unselected');
      expect(initialStates).toContain('focus.unfocused');
    });

    it('should transition from idle to sorting on SORT', () => {
      const transition = { from: 'idle', event: 'SORT', target: 'sorting' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('SORT');
      expect(transition.target).toBe('sorting');
      // Target state 'sorting' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to loading on LOAD', () => {
      const transition = { from: 'idle', event: 'LOAD', target: 'loading' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('LOAD');
      expect(transition.target).toBe('loading');
      // Target state 'loading' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to empty on DATA_EMPTY', () => {
      const transition = { from: 'idle', event: 'DATA_EMPTY', target: 'empty' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('DATA_EMPTY');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to idle on SELECT_ROW', () => {
      const transition = { from: 'idle', event: 'SELECT_ROW', target: 'idle' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('SELECT_ROW');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to idle on DESELECT_ROW', () => {
      const transition = { from: 'idle', event: 'DESELECT_ROW', target: 'idle' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('DESELECT_ROW');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from sorting to idle on SORT_COMPLETE', () => {
      const transition = { from: 'sorting', event: 'SORT_COMPLETE', target: 'idle' };
      expect(transition.from).toBe('sorting');
      expect(transition.event).toBe('SORT_COMPLETE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from sorting to empty on SORT_COMPLETE_EMPTY', () => {
      const transition = { from: 'sorting', event: 'SORT_COMPLETE_EMPTY', target: 'empty' };
      expect(transition.from).toBe('sorting');
      expect(transition.event).toBe('SORT_COMPLETE_EMPTY');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading to idle on LOAD_COMPLETE', () => {
      const transition = { from: 'loading', event: 'LOAD_COMPLETE', target: 'idle' };
      expect(transition.from).toBe('loading');
      expect(transition.event).toBe('LOAD_COMPLETE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading to empty on LOAD_COMPLETE_EMPTY', () => {
      const transition = { from: 'loading', event: 'LOAD_COMPLETE_EMPTY', target: 'empty' };
      expect(transition.from).toBe('loading');
      expect(transition.event).toBe('LOAD_COMPLETE_EMPTY');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from empty to loading on LOAD', () => {
      const transition = { from: 'empty', event: 'LOAD', target: 'loading' };
      expect(transition.from).toBe('empty');
      expect(transition.event).toBe('LOAD');
      expect(transition.target).toBe('loading');
      // Target state 'loading' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from empty to idle on DATA_AVAILABLE', () => {
      const transition = { from: 'empty', event: 'DATA_AVAILABLE', target: 'idle' };
      expect(transition.from).toBe('empty');
      expect(transition.event).toBe('DATA_AVAILABLE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from row.unselected to selected on SELECT_ROW', () => {
      const transition = { from: 'row.unselected', event: 'SELECT_ROW', target: 'selected' };
      expect(transition.from).toBe('row.unselected');
      expect(transition.event).toBe('SELECT_ROW');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from row.selected to unselected on DESELECT_ROW', () => {
      const transition = { from: 'row.selected', event: 'DESELECT_ROW', target: 'unselected' };
      expect(transition.from).toBe('row.selected');
      expect(transition.event).toBe('DESELECT_ROW');
      expect(transition.target).toBe('unselected');
      // Target state 'unselected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.unfocused to cellFocused on FOCUS_CELL', () => {
      const transition = { from: 'focus.unfocused', event: 'FOCUS_CELL', target: 'cellFocused' };
      expect(transition.from).toBe('focus.unfocused');
      expect(transition.event).toBe('FOCUS_CELL');
      expect(transition.target).toBe('cellFocused');
      // Target state 'cellFocused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.cellFocused to unfocused on BLUR', () => {
      const transition = { from: 'focus.cellFocused', event: 'BLUR', target: 'unfocused' };
      expect(transition.from).toBe('focus.cellFocused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('unfocused');
      // Target state 'unfocused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focus.cellFocused to cellFocused on NAVIGATE', () => {
      const transition = { from: 'focus.cellFocused', event: 'NAVIGATE', target: 'cellFocused' };
      expect(transition.from).toBe('focus.cellFocused');
      expect(transition.event).toBe('NAVIGATE');
      expect(transition.target).toBe('cellFocused');
      // Target state 'cellFocused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"idle","events":["SORT","LOAD","DATA_EMPTY","SELECT_ROW","DESELECT_ROW"]},{"name":"sorting","events":["SORT_COMPLETE","SORT_COMPLETE_EMPTY"]},{"name":"loading","events":["LOAD_COMPLETE","LOAD_COMPLETE_EMPTY"]},{"name":"empty","events":["LOAD","DATA_AVAILABLE"]},{"name":"row.unselected","events":["SELECT_ROW"]},{"name":"row.selected","events":["DESELECT_ROW"]},{"name":"focus.unfocused","events":["FOCUS_CELL"]},{"name":"focus.cellFocused","events":["BLUR","NAVIGATE"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["idle","sorting","loading","empty","row.unselected","row.selected","focus.unfocused","focus.cellFocused"]);
      const baseNames = new Set(["idle","sorting","loading","empty","unselected","selected","unfocused","cellFocused"]);
      expect(validNames.has('sorting') || baseNames.has('sorting')).toBe(true);
      expect(validNames.has('loading') || baseNames.has('loading')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('loading') || baseNames.has('loading')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('unselected') || baseNames.has('unselected')).toBe(true);
      expect(validNames.has('cellFocused') || baseNames.has('cellFocused')).toBe(true);
      expect(validNames.has('unfocused') || baseNames.has('unfocused')).toBe(true);
      expect(validNames.has('cellFocused') || baseNames.has('cellFocused')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if ? loading then loading else if ? data . length = = 0 then empty else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-selectable on root', () => {
      const attr = { name: 'data-selectable', value: "if ? selectable then true else false" };
      expect(attr.name).toBe('data-selectable');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-sticky on header', () => {
      const attr = { name: 'data-sticky', value: "if ? stickyHeader then true else false" };
      expect(attr.name).toBe('data-sticky');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-sortable on headerCell', () => {
      const attr = { name: 'data-sortable', value: "if ? sortable then true else false" };
      expect(attr.name).toBe('data-sortable');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-sorted on headerCell', () => {
      const attr = { name: 'data-sorted', value: "if ? sortColumn = = self . column then true else false" };
      expect(attr.name).toBe('data-sorted');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-sort-direction on headerCell', () => {
      const attr = { name: 'data-sort-direction', value: "if ? sortColumn = = self . column then ? sortDirection else none" };
      expect(attr.name).toBe('data-sort-direction');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on headerCell', () => {
      const handler = { event: 'onClick', action: "if ? sortable then send ( SORT ) else noop" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on headerCell', () => {
      const handler = { event: 'onKeyDown-Enter', action: "if ? sortable then send ( SORT ) else noop" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-state on body', () => {
      const attr = { name: 'data-state', value: "if ? loading then loading else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on row', () => {
      const attr = { name: 'data-state', value: "if state . row = = selected then selected else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-selectable on row', () => {
      const attr = { name: 'data-selectable', value: "if ? selectable then true else false" };
      expect(attr.name).toBe('data-selectable');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on row', () => {
      const handler = { event: 'onClick', action: "if ? selectable then send ( SELECT_ROW ) else noop" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on row', () => {
      const handler = { event: 'onKeyDown-Space', action: "if ? selectable then send ( SELECT_ROW ) else noop" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="cell" to cell', () => {
      const binding = {"part":"cell","dataPart":"cell"};
      expect(binding.dataPart).toBe('cell');
    });

    it('should bind onFocus handler on cell', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS_CELL )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on cell', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowUp handler on cell', () => {
      const handler = { event: 'onKeyDown-ArrowUp', action: "send ( NAVIGATE_UP )" };
      expect(handler.event).toBe('onKeyDown-ArrowUp');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowDown handler on cell', () => {
      const handler = { event: 'onKeyDown-ArrowDown', action: "send ( NAVIGATE_DOWN )" };
      expect(handler.event).toBe('onKeyDown-ArrowDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowLeft handler on cell', () => {
      const handler = { event: 'onKeyDown-ArrowLeft', action: "send ( NAVIGATE_LEFT )" };
      expect(handler.event).toBe('onKeyDown-ArrowLeft');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-ArrowRight handler on cell', () => {
      const handler = { event: 'onKeyDown-ArrowRight', action: "send ( NAVIGATE_RIGHT )" };
      expect(handler.event).toBe('onKeyDown-ArrowRight');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="footer" to footer', () => {
      const binding = {"part":"footer","dataPart":"footer"};
      expect(binding.dataPart).toBe('footer');
    });

    it('should bind data-part="footer-row" to footerRow', () => {
      const binding = {"part":"footerRow","dataPart":"footer-row"};
      expect(binding.dataPart).toBe('footer-row');
    });

    it('should bind data-part="footer-cell" to footerCell', () => {
      const binding = {"part":"footerCell","dataPart":"footer-cell"};
      expect(binding.dataPart).toBe('footer-cell');
    });

    it('should bind data-direction on sortIndicator', () => {
      const attr = { name: 'data-direction', value: "? sortDirection" };
      expect(attr.name).toBe('data-direction');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-visible on sortIndicator', () => {
      const attr = { name: 'data-visible', value: "if ? sortColumn then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="pagination" to pagination', () => {
      const binding = {"part":"pagination","dataPart":"pagination"};
      expect(binding.dataPart).toBe('pagination');
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","header","headerRow","headerCell","body","row","cell","footer","footerRow","footerCell","sortIndicator","pagination"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('header')).toBe(true);
      expect(anatomyParts.has('headerRow')).toBe(true);
      expect(anatomyParts.has('headerCell')).toBe(true);
      expect(anatomyParts.has('body')).toBe(true);
      expect(anatomyParts.has('row')).toBe(true);
      expect(anatomyParts.has('cell')).toBe(true);
      expect(anatomyParts.has('footer')).toBe(true);
      expect(anatomyParts.has('footerRow')).toBe(true);
      expect(anatomyParts.has('footerCell')).toBe(true);
      expect(anatomyParts.has('sortIndicator')).toBe(true);
      expect(anatomyParts.has('pagination')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowUp key with action: NAVIGATE_UP', () => {
      const binding = { key: 'ArrowUp', action: 'NAVIGATE_UP' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('NAVIGATE_UP');
    });

    it('should handle ArrowDown key with action: NAVIGATE_DOWN', () => {
      const binding = { key: 'ArrowDown', action: 'NAVIGATE_DOWN' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('NAVIGATE_DOWN');
    });

    it('should handle ArrowLeft key with action: NAVIGATE_LEFT', () => {
      const binding = { key: 'ArrowLeft', action: 'NAVIGATE_LEFT' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('NAVIGATE_LEFT');
    });

    it('should handle ArrowRight key with action: NAVIGATE_RIGHT', () => {
      const binding = { key: 'ArrowRight', action: 'NAVIGATE_RIGHT' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('NAVIGATE_RIGHT');
    });

    it('should handle Enter key with action: SORT_OR_ACTIVATE', () => {
      const binding = { key: 'Enter', action: 'SORT_OR_ACTIVATE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('SORT_OR_ACTIVATE');
    });

    it('should handle Space key with action: SELECT_ROW', () => {
      const binding = { key: 'Space', action: 'SELECT_ROW' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('SELECT_ROW');
    });

    it('should handle Home key with action: NAVIGATE_ROW_START', () => {
      const binding = { key: 'Home', action: 'NAVIGATE_ROW_START' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('NAVIGATE_ROW_START');
    });

    it('should handle End key with action: NAVIGATE_ROW_END', () => {
      const binding = { key: 'End', action: 'NAVIGATE_ROW_END' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('NAVIGATE_ROW_END');
    });

    it('should handle Control+Home key with action: NAVIGATE_TABLE_START', () => {
      const binding = { key: 'Control+Home', action: 'NAVIGATE_TABLE_START' };
      expect(binding.key).toBe('Control+Home');
      expect(binding.action).toBe('NAVIGATE_TABLE_START');
    });

    it('should handle Control+End key with action: NAVIGATE_TABLE_END', () => {
      const binding = { key: 'Control+End', action: 'NAVIGATE_TABLE_END' };
      expect(binding.key).toBe('Control+End');
      expect(binding.action).toBe('NAVIGATE_TABLE_END');
    });

    it('should handle PageUp key with action: NAVIGATE_PAGE_UP', () => {
      const binding = { key: 'PageUp', action: 'NAVIGATE_PAGE_UP' };
      expect(binding.key).toBe('PageUp');
      expect(binding.action).toBe('NAVIGATE_PAGE_UP');
    });

    it('should handle PageDown key with action: NAVIGATE_PAGE_DOWN', () => {
      const binding = { key: 'PageDown', action: 'NAVIGATE_PAGE_DOWN' };
      expect(binding.key).toBe('PageDown');
      expect(binding.action).toBe('NAVIGATE_PAGE_DOWN');
    });

    it('should handle Tab key with action: FOCUS_OUT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_OUT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_OUT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SORT","LOAD","DATA_EMPTY","SELECT_ROW","DESELECT_ROW","SORT_COMPLETE","SORT_COMPLETE_EMPTY","LOAD_COMPLETE","LOAD_COMPLETE_EMPTY","DATA_AVAILABLE","FOCUS_CELL","BLUR","NAVIGATE"]);
      const keyboardActions = ["NAVIGATE_UP","NAVIGATE_DOWN","NAVIGATE_LEFT","NAVIGATE_RIGHT","SORT_OR_ACTIVATE","SELECT_ROW","NAVIGATE_ROW_START","NAVIGATE_ROW_END","NAVIGATE_TABLE_START","NAVIGATE_TABLE_END","NAVIGATE_PAGE_UP","NAVIGATE_PAGE_DOWN","FOCUS_OUT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "grid"', () => {
      expect('grid').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "headerCell"', () => {
      expect('headerCell').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "if ? sortable then grid else table" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? ariaLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-rowcount on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-rowcount', value: "? rowCount" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-colcount on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-colcount', value: "? colCount" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on headerCell', () => {
      const ariaAttr = { part: 'headerCell', name: 'role', value: "columnheader" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-sort on headerCell', () => {
      const ariaAttr = { part: 'headerCell', name: 'aria-sort', value: "if ? sortColumn = = self . column then ? sortDirection else none" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-colindex on headerCell', () => {
      const ariaAttr = { part: 'headerCell', name: 'aria-colindex', value: "self . index" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on row', () => {
      const ariaAttr = { part: 'row', name: 'role', value: "row" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-rowindex on row', () => {
      const ariaAttr = { part: 'row', name: 'aria-rowindex', value: "self . index" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on row', () => {
      const ariaAttr = { part: 'row', name: 'aria-selected', value: "if ? selectable then state . row else undefined" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on cell', () => {
      const ariaAttr = { part: 'cell', name: 'role', value: "gridcell" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-colindex on cell', () => {
      const ariaAttr = { part: 'cell', name: 'aria-colindex', value: "self . index" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "if ? sortable then grid else table" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? ariaLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-rowcount on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-rowcount', value: "? rowCount" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-colcount on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-colcount', value: "? colCount" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-busy on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-busy', value: "if ? loading then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part header', () => {
      const connectAria = { part: 'header', attr: 'role', value: "rowgroup" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part headerRow', () => {
      const connectAria = { part: 'headerRow', attr: 'role', value: "row" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part headerCell', () => {
      const connectAria = { part: 'headerCell', attr: 'role', value: "columnheader" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-sort on connect part headerCell', () => {
      const connectAria = { part: 'headerCell', attr: 'aria-sort', value: "if ? sortColumn = = self . column then ? sortDirection else none" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-colindex on connect part headerCell', () => {
      const connectAria = { part: 'headerCell', attr: 'aria-colindex', value: "self . index" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part body', () => {
      const connectAria = { part: 'body', attr: 'role', value: "rowgroup" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part row', () => {
      const connectAria = { part: 'row', attr: 'role', value: "row" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-rowindex on connect part row', () => {
      const connectAria = { part: 'row', attr: 'aria-rowindex', value: "self . index" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part row', () => {
      const connectAria = { part: 'row', attr: 'aria-selected', value: "if ? selectable then ( if state . row = = selected then true else false ) else undefined" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part cell', () => {
      const connectAria = { part: 'cell', attr: 'role', value: "gridcell" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-colindex on connect part cell', () => {
      const connectAria = { part: 'cell', attr: 'aria-colindex', value: "self . index" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part footer', () => {
      const connectAria = { part: 'footer', attr: 'role', value: "rowgroup" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part footerRow', () => {
      const connectAria = { part: 'footerRow', attr: 'role', value: "row" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part footerCell', () => {
      const connectAria = { part: 'footerCell', attr: 'role', value: "cell" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part sortIndicator', () => {
      const connectAria = { part: 'sortIndicator', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part pagination', () => {
      const connectAria = { part: 'pagination', attr: 'aria-label', value: "Table pagination" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "columns" with type "list ColumnDef"', () => {
      const prop = { name: 'columns', type: 'list ColumnDef' };
      expect(prop.name).toBe('columns');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "data" with type "list Object"', () => {
      const prop = { name: 'data', type: 'list Object' };
      expect(prop.name).toBe('data');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "sortable" with type "Bool"', () => {
      const prop = { name: 'sortable', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('sortable');
      expect(prop.type).toBeTruthy();
    });

    it('should default "sortable" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "selectable" with type "Bool"', () => {
      const prop = { name: 'selectable', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('selectable');
      expect(prop.type).toBeTruthy();
    });

    it('should default "selectable" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "stickyHeader" with type "Bool"', () => {
      const prop = { name: 'stickyHeader', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('stickyHeader');
      expect(prop.type).toBeTruthy();
    });

    it('should default "stickyHeader" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "sortColumn" with type "option String"', () => {
      const prop = { name: 'sortColumn', type: 'option String' };
      expect(prop.name).toBe('sortColumn');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "sortDirection" with type "union ascending | descending | none"', () => {
      const prop = { name: 'sortDirection', type: 'union ascending | descending | none', defaultValue: "none" };
      expect(prop.name).toBe('sortDirection');
      expect(prop.type).toBeTruthy();
    });

    it('should default "sortDirection" to "none"', () => {
      expect("none").toBe("none");
    });

    it('should declare prop "ariaLabel" with type "option String"', () => {
      const prop = { name: 'ariaLabel', type: 'option String' };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "rowCount" with type "option Int"', () => {
      const prop = { name: 'rowCount', type: 'option Int' };
      expect(prop.name).toBe('rowCount');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "colCount" with type "option Int"', () => {
      const prop = { name: 'colCount', type: 'option Int' };
      expect(prop.name).toBe('colCount');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "loading" with type "Bool"', () => {
      const prop = { name: 'loading', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('loading');
      expect(prop.type).toBeTruthy();
    });

    it('should default "loading" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "emptyMessage" with type "String"', () => {
      const prop = { name: 'emptyMessage', type: 'String', defaultValue: "No data available" };
      expect(prop.name).toBe('emptyMessage');
      expect(prop.type).toBeTruthy();
    });

    it('should default "emptyMessage" to "No data available"', () => {
      expect("No data available").toBe("No data available");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","header","headerRow","headerCell","body","row","cell","footer","footerRow","footerCell","sortIndicator","pagination"];
      expect(parts.length).toBe(12);
      expect(parts).toContain('root');
      expect(parts).toContain('header');
      expect(parts).toContain('headerRow');
      expect(parts).toContain('headerCell');
      expect(parts).toContain('body');
      expect(parts).toContain('row');
      expect(parts).toContain('cell');
      expect(parts).toContain('footer');
      expect(parts).toContain('footerRow');
      expect(parts).toContain('footerCell');
      expect(parts).toContain('sortIndicator');
      expect(parts).toContain('pagination');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // header: container
      expect('container').toBeTruthy(); // headerRow: container
      expect('action').toBeTruthy(); // headerCell: action
      expect('container').toBeTruthy(); // body: container
      expect('container').toBeTruthy(); // row: container
      expect('container').toBeTruthy(); // cell: container
      expect('container').toBeTruthy(); // footer: container
      expect('container').toBeTruthy(); // footerRow: container
      expect('container').toBeTruthy(); // footerCell: container
      expect('container').toBeTruthy(); // sortIndicator: container
      expect('widget').toBeTruthy(); // pagination: widget
    });

  });

});
