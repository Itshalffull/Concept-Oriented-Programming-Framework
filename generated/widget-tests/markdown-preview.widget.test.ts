// Auto-generated by generate-widget-tests.ts from markdown-preview.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: MarkdownPreview', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["static"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('static');
    });

    it('should transition from static to rendering on SOURCE_CHANGE', () => {
      const transition = { from: 'static', event: 'SOURCE_CHANGE', target: 'rendering' };
      expect(transition.from).toBe('static');
      expect(transition.event).toBe('SOURCE_CHANGE');
      expect(transition.target).toBe('rendering');
      // Target state 'rendering' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from rendering to static on RENDER_COMPLETE', () => {
      const transition = { from: 'rendering', event: 'RENDER_COMPLETE', target: 'static' };
      expect(transition.from).toBe('rendering');
      expect(transition.event).toBe('RENDER_COMPLETE');
      expect(transition.target).toBe('static');
      // Target state 'static' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"static","events":["SOURCE_CHANGE"]},{"name":"rendering","events":["RENDER_COMPLETE"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["static","rendering"]);
      const baseNames = new Set(["static","rendering"]);
      expect(validNames.has('rendering') || baseNames.has('rendering')).toBe(true);
      expect(validNames.has('static') || baseNames.has('static')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="markdown-preview" to root', () => {
      const binding = {"part":"root","dataPart":"markdown-preview"};
      expect(binding.dataPart).toBe('markdown-preview');
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "state" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-sanitized on root', () => {
      const attr = { name: 'data-sanitized', value: "if ? sanitize then true else false" };
      expect(attr.name).toBe('data-sanitized');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-syntax-highlight on root', () => {
      const attr = { name: 'data-syntax-highlight', value: "if ? syntaxHighlight then true else false" };
      expect(attr.name).toBe('data-syntax-highlight');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="content" to content', () => {
      const binding = {"part":"content","dataPart":"content"};
      expect(binding.dataPart).toBe('content');
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","content"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('content')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Tab key with action: FOCUS_NEXT_LINK', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT_LINK' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT_LINK');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV_LINK', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV_LINK' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV_LINK');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SOURCE_CHANGE","RENDER_COMPLETE"]);
      const keyboardActions = ["FOCUS_NEXT_LINK","FOCUS_PREV_LINK"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "document"', () => {
      expect('document').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "content"', () => {
      expect('content').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "document" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? ariaLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "markdown content" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on content', () => {
      const ariaAttr = { part: 'content', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on content', () => {
      const ariaAttr = { part: 'content', name: 'aria-label', value: "Rendered content" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "document" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? ariaLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-roledescription', value: "markdown content" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part content', () => {
      const connectAria = { part: 'content', attr: 'role', value: "region" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part content', () => {
      const connectAria = { part: 'content', attr: 'aria-label', value: "Rendered content" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "source" with type "String"', () => {
      const prop = { name: 'source', type: 'String' };
      expect(prop.name).toBe('source');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "sanitize" with type "Bool"', () => {
      const prop = { name: 'sanitize', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('sanitize');
      expect(prop.type).toBeTruthy();
    });

    it('should default "sanitize" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "ariaLabel" with type "String"', () => {
      const prop = { name: 'ariaLabel', type: 'String', defaultValue: "Markdown preview" };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "ariaLabel" to "Markdown preview"', () => {
      expect("Markdown preview").toBe("Markdown preview");
    });

    it('should declare prop "syntaxHighlight" with type "Bool"', () => {
      const prop = { name: 'syntaxHighlight', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('syntaxHighlight');
      expect(prop.type).toBeTruthy();
    });

    it('should default "syntaxHighlight" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "linkTarget" with type "union _self | _blank"', () => {
      const prop = { name: 'linkTarget', type: 'union _self | _blank', defaultValue: "_blank" };
      expect(prop.name).toBe('linkTarget');
      expect(prop.type).toBeTruthy();
    });

    it('should default "linkTarget" to "_blank"', () => {
      expect("_blank").toBe("_blank");
    });

    it('should declare prop "maxHeight" with type "option String"', () => {
      const prop = { name: 'maxHeight', type: 'option String' };
      expect(prop.name).toBe('maxHeight');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "className" with type "option String"', () => {
      const prop = { name: 'className', type: 'option String' };
      expect(prop.name).toBe('className');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","content"];
      expect(parts.length).toBe(2);
      expect(parts).toContain('root');
      expect(parts).toContain('content');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // content: container
    });

  });

});
