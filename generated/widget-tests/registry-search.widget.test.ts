// Auto-generated by generate-widget-tests.ts from registry-search.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: RegistrySearch', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('idle');
    });

    it('should transition from idle to searching on INPUT', () => {
      const transition = { from: 'idle', event: 'INPUT', target: 'searching' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('INPUT');
      expect(transition.target).toBe('searching');
      // Target state 'searching' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from idle to idle on SELECT_RESULT', () => {
      const transition = { from: 'idle', event: 'SELECT_RESULT', target: 'idle' };
      expect(transition.from).toBe('idle');
      expect(transition.event).toBe('SELECT_RESULT');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from searching to idle on RESULTS', () => {
      const transition = { from: 'searching', event: 'RESULTS', target: 'idle' };
      expect(transition.from).toBe('searching');
      expect(transition.event).toBe('RESULTS');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from searching to idle on CLEAR', () => {
      const transition = { from: 'searching', event: 'CLEAR', target: 'idle' };
      expect(transition.from).toBe('searching');
      expect(transition.event).toBe('CLEAR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"idle","events":["INPUT","SELECT_RESULT"]},{"name":"searching","events":["RESULTS","CLEAR"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["idle","searching"]);
      const baseNames = new Set(["idle","searching"]);
      expect(validNames.has('searching') || baseNames.has('searching')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowDown key with action: FOCUS_NEXT_RESULT', () => {
      const binding = { key: 'ArrowDown', action: 'FOCUS_NEXT_RESULT' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('FOCUS_NEXT_RESULT');
    });

    it('should handle ArrowUp key with action: FOCUS_PREV_RESULT', () => {
      const binding = { key: 'ArrowUp', action: 'FOCUS_PREV_RESULT' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('FOCUS_PREV_RESULT');
    });

    it('should handle Enter key with action: SELECT_RESULT', () => {
      const binding = { key: 'Enter', action: 'SELECT_RESULT' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('SELECT_RESULT');
    });

    it('should handle Escape key with action: CLEAR', () => {
      const binding = { key: 'Escape', action: 'CLEAR' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('CLEAR');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["INPUT","SELECT_RESULT","RESULTS","CLEAR"]);
      const keyboardActions = ["FOCUS_NEXT_RESULT","FOCUS_PREV_RESULT","SELECT_RESULT","CLEAR"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "search"', () => {
      expect('search').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "searchInput"', () => {
      expect('searchInput').toBeTruthy();
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "search" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Package registry search" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on searchInput', () => {
      const ariaAttr = { part: 'searchInput', name: 'role', value: "searchbox" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on searchInput', () => {
      const ariaAttr = { part: 'searchInput', name: 'aria-label', value: "Search packages" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on suggestions', () => {
      const ariaAttr = { part: 'suggestions', name: 'role', value: "listbox" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on suggestions', () => {
      const ariaAttr = { part: 'suggestions', name: 'aria-label', value: "Suggestions" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on resultList', () => {
      const ariaAttr = { part: 'resultList', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on resultList', () => {
      const ariaAttr = { part: 'resultList', name: 'aria-label', value: "Search results" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on resultCard', () => {
      const ariaAttr = { part: 'resultCard', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on resultCard', () => {
      const ariaAttr = { part: 'resultCard', name: 'aria-label', value: "concat ( self . name , @ , self . version )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "sortBy" with type "union relevance | downloads | date"', () => {
      const prop = { name: 'sortBy', type: 'union relevance | downloads | date', defaultValue: "relevance" };
      expect(prop.name).toBe('sortBy');
      expect(prop.type).toBeTruthy();
    });

    it('should default "sortBy" to "relevance"', () => {
      expect("relevance").toBe("relevance");
    });

    it('should declare prop "pageSize" with type "Int"', () => {
      const prop = { name: 'pageSize', type: 'Int', defaultValue: "20" };
      expect(prop.name).toBe('pageSize');
      expect(prop.type).toBeTruthy();
    });

    it('should default "pageSize" to "20"', () => {
      expect("20").toBe("20");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","searchInput","suggestions","filterBar","resultList","resultCard","cardName","cardVersion","cardDesc","cardKeywords","cardDownloads","cardDate","pagination","emptyState"];
      expect(parts.length).toBe(14);
      expect(parts).toContain('root');
      expect(parts).toContain('searchInput');
      expect(parts).toContain('suggestions');
      expect(parts).toContain('filterBar');
      expect(parts).toContain('resultList');
      expect(parts).toContain('resultCard');
      expect(parts).toContain('cardName');
      expect(parts).toContain('cardVersion');
      expect(parts).toContain('cardDesc');
      expect(parts).toContain('cardKeywords');
      expect(parts).toContain('cardDownloads');
      expect(parts).toContain('cardDate');
      expect(parts).toContain('pagination');
      expect(parts).toContain('emptyState');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('widget').toBeTruthy(); // searchInput: widget
      expect('container').toBeTruthy(); // suggestions: container
      expect('container').toBeTruthy(); // filterBar: container
      expect('container').toBeTruthy(); // resultList: container
      expect('container').toBeTruthy(); // resultCard: container
      expect('text').toBeTruthy(); // cardName: text
      expect('text').toBeTruthy(); // cardVersion: text
      expect('text').toBeTruthy(); // cardDesc: text
      expect('container').toBeTruthy(); // cardKeywords: container
      expect('text').toBeTruthy(); // cardDownloads: text
      expect('text').toBeTruthy(); // cardDate: text
      expect('widget').toBeTruthy(); // pagination: widget
      expect('container').toBeTruthy(); // emptyState: container
    });

  });

});
