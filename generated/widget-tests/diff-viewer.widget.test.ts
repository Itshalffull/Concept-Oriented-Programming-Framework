// Auto-generated by generate-widget-tests.ts from diff-viewer.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: DiffViewer', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["mode.sideBySide","loading.idle","expand.collapsed","navigation.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('mode.sideBySide');
      expect(initialStates).toContain('loading.idle');
      expect(initialStates).toContain('expand.collapsed');
      expect(initialStates).toContain('navigation.idle');
    });

    it('should transition from mode.sideBySide to unified on SWITCH_TO_UNIFIED', () => {
      const transition = { from: 'mode.sideBySide', event: 'SWITCH_TO_UNIFIED', target: 'unified' };
      expect(transition.from).toBe('mode.sideBySide');
      expect(transition.event).toBe('SWITCH_TO_UNIFIED');
      expect(transition.target).toBe('unified');
      // Target state 'unified' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from mode.unified to sideBySide on SWITCH_TO_SIDE_BY_SIDE', () => {
      const transition = { from: 'mode.unified', event: 'SWITCH_TO_SIDE_BY_SIDE', target: 'sideBySide' };
      expect(transition.from).toBe('mode.unified');
      expect(transition.event).toBe('SWITCH_TO_SIDE_BY_SIDE');
      expect(transition.target).toBe('sideBySide');
      // Target state 'sideBySide' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading.idle to loading on LOAD', () => {
      const transition = { from: 'loading.idle', event: 'LOAD', target: 'loading' };
      expect(transition.from).toBe('loading.idle');
      expect(transition.event).toBe('LOAD');
      expect(transition.target).toBe('loading');
      // Target state 'loading' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading.loading to idle on LOAD_COMPLETE', () => {
      const transition = { from: 'loading.loading', event: 'LOAD_COMPLETE', target: 'idle' };
      expect(transition.from).toBe('loading.loading');
      expect(transition.event).toBe('LOAD_COMPLETE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading.loading to error on LOAD_ERROR', () => {
      const transition = { from: 'loading.loading', event: 'LOAD_ERROR', target: 'error' };
      expect(transition.from).toBe('loading.loading');
      expect(transition.event).toBe('LOAD_ERROR');
      expect(transition.target).toBe('error');
      // Target state 'error' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading.error to loading on RETRY', () => {
      const transition = { from: 'loading.error', event: 'RETRY', target: 'loading' };
      expect(transition.from).toBe('loading.error');
      expect(transition.event).toBe('RETRY');
      expect(transition.target).toBe('loading');
      // Target state 'loading' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expand.collapsed to expanded on EXPAND', () => {
      const transition = { from: 'expand.collapsed', event: 'EXPAND', target: 'expanded' };
      expect(transition.from).toBe('expand.collapsed');
      expect(transition.event).toBe('EXPAND');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expand.expanded to collapsed on COLLAPSE', () => {
      const transition = { from: 'expand.expanded', event: 'COLLAPSE', target: 'collapsed' };
      expect(transition.from).toBe('expand.expanded');
      expect(transition.event).toBe('COLLAPSE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from navigation.idle to atChange on NAVIGATE_CHANGE', () => {
      const transition = { from: 'navigation.idle', event: 'NAVIGATE_CHANGE', target: 'atChange' };
      expect(transition.from).toBe('navigation.idle');
      expect(transition.event).toBe('NAVIGATE_CHANGE');
      expect(transition.target).toBe('atChange');
      // Target state 'atChange' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from navigation.atChange to atChange on NAVIGATE_CHANGE', () => {
      const transition = { from: 'navigation.atChange', event: 'NAVIGATE_CHANGE', target: 'atChange' };
      expect(transition.from).toBe('navigation.atChange');
      expect(transition.event).toBe('NAVIGATE_CHANGE');
      expect(transition.target).toBe('atChange');
      // Target state 'atChange' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from navigation.atChange to idle on SCROLL_AWAY', () => {
      const transition = { from: 'navigation.atChange', event: 'SCROLL_AWAY', target: 'idle' };
      expect(transition.from).toBe('navigation.atChange');
      expect(transition.event).toBe('SCROLL_AWAY');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"mode.sideBySide","events":["SWITCH_TO_UNIFIED"]},{"name":"mode.unified","events":["SWITCH_TO_SIDE_BY_SIDE"]},{"name":"loading.idle","events":["LOAD"]},{"name":"loading.loading","events":["LOAD_COMPLETE","LOAD_ERROR"]},{"name":"loading.error","events":["RETRY"]},{"name":"expand.collapsed","events":["EXPAND"]},{"name":"expand.expanded","events":["COLLAPSE"]},{"name":"navigation.idle","events":["NAVIGATE_CHANGE"]},{"name":"navigation.atChange","events":["NAVIGATE_CHANGE","SCROLL_AWAY"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["mode.sideBySide","mode.unified","loading.idle","loading.loading","loading.error","expand.collapsed","expand.expanded","navigation.idle","navigation.atChange"]);
      const baseNames = new Set(["sideBySide","unified","idle","loading","error","collapsed","expanded","atChange"]);
      expect(validNames.has('unified') || baseNames.has('unified')).toBe(true);
      expect(validNames.has('sideBySide') || baseNames.has('sideBySide')).toBe(true);
      expect(validNames.has('loading') || baseNames.has('loading')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('error') || baseNames.has('error')).toBe(true);
      expect(validNames.has('loading') || baseNames.has('loading')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('atChange') || baseNames.has('atChange')).toBe(true);
      expect(validNames.has('atChange') || baseNames.has('atChange')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-mode on root', () => {
      const attr = { name: 'data-mode', value: "if state . mode = = sideBySide then side-by-side else unified" };
      expect(attr.name).toBe('data-mode');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if ? loading then loading else idle" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="mode-toggle" to modeToggle', () => {
      const binding = {"part":"modeToggle","dataPart":"mode-toggle"};
      expect(binding.dataPart).toBe('mode-toggle');
    });

    it('should bind onChange handler on modeToggle', () => {
      const handler = { event: 'onChange', action: "if state . mode = = sideBySide then send ( SWITCH_TO_UNIFIED ) else send ( SWITCH_TO_SIDE_BY_SIDE )" };
      expect(handler.event).toBe('onChange');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="toolbar" to toolbar', () => {
      const binding = {"part":"toolbar","dataPart":"toolbar"};
      expect(binding.dataPart).toBe('toolbar');
    });

    it('should bind data-additions on changeStats', () => {
      const attr = { name: 'data-additions', value: "? additions" };
      expect(attr.name).toBe('data-additions');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-deletions on changeStats', () => {
      const attr = { name: 'data-deletions', value: "? deletions" };
      expect(attr.name).toBe('data-deletions');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on prevChangeButton', () => {
      const handler = { event: 'onClick', action: "send ( PREV_CHANGE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onClick handler on nextChangeButton', () => {
      const handler = { event: 'onClick', action: "send ( NEXT_CHANGE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="file-list" to fileList', () => {
      const binding = {"part":"fileList","dataPart":"file-list"};
      expect(binding.dataPart).toBe('file-list');
    });

    it('should bind data-file on fileItem', () => {
      const attr = { name: 'data-file', value: "self . fileName" };
      expect(attr.name).toBe('data-file');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-selected on fileItem', () => {
      const attr = { name: 'data-selected', value: "if self . selected then true else false" };
      expect(attr.name).toBe('data-selected');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on fileItem', () => {
      const handler = { event: 'onClick', action: "send ( SELECT_FILE , { file : self . fileName } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="file-item-name" to fileItemName', () => {
      const binding = {"part":"fileItemName","dataPart":"file-item-name"};
      expect(binding.dataPart).toBe('file-item-name');
    });

    it('should bind data-part="file-item-stats" to fileItemStats', () => {
      const binding = {"part":"fileItemStats","dataPart":"file-item-stats"};
      expect(binding.dataPart).toBe('file-item-stats');
    });

    it('should bind data-mode on diffPanel', () => {
      const attr = { name: 'data-mode', value: "if state . mode = = sideBySide then side-by-side else unified" };
      expect(attr.name).toBe('data-mode');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="left-pane" to leftPane', () => {
      const binding = {"part":"leftPane","dataPart":"left-pane"};
      expect(binding.dataPart).toBe('left-pane');
    });

    it('should bind data-part="right-pane" to rightPane', () => {
      const binding = {"part":"rightPane","dataPart":"right-pane"};
      expect(binding.dataPart).toBe('right-pane');
    });

    it('should bind data-part="left-header" to leftHeader', () => {
      const binding = {"part":"leftHeader","dataPart":"left-header"};
      expect(binding.dataPart).toBe('left-header');
    });

    it('should bind data-part="right-header" to rightHeader', () => {
      const binding = {"part":"rightHeader","dataPart":"right-header"};
      expect(binding.dataPart).toBe('right-header');
    });

    it('should bind data-part="hunk-header" to hunkHeader', () => {
      const binding = {"part":"hunkHeader","dataPart":"hunk-header"};
      expect(binding.dataPart).toBe('hunk-header');
    });

    it('should bind data-range on hunkHeader', () => {
      const attr = { name: 'data-range', value: "self . range" };
      expect(attr.name).toBe('data-range');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="line-number" to lineNumber', () => {
      const binding = {"part":"lineNumber","dataPart":"line-number"};
      expect(binding.dataPart).toBe('line-number');
    });

    it('should bind data-part="line-content" to lineContent', () => {
      const binding = {"part":"lineContent","dataPart":"line-content"};
      expect(binding.dataPart).toBe('line-content');
    });

    it('should bind data-part="added-line" to addedLine', () => {
      const binding = {"part":"addedLine","dataPart":"added-line"};
      expect(binding.dataPart).toBe('added-line');
    });

    it('should bind data-line on addedLine', () => {
      const attr = { name: 'data-line', value: "self . lineNumber" };
      expect(attr.name).toBe('data-line');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="removed-line" to removedLine', () => {
      const binding = {"part":"removedLine","dataPart":"removed-line"};
      expect(binding.dataPart).toBe('removed-line');
    });

    it('should bind data-line on removedLine', () => {
      const attr = { name: 'data-line', value: "self . lineNumber" };
      expect(attr.name).toBe('data-line');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="unchanged-line" to unchangedLine', () => {
      const binding = {"part":"unchangedLine","dataPart":"unchanged-line"};
      expect(binding.dataPart).toBe('unchanged-line');
    });

    it('should bind data-line on unchangedLine', () => {
      const attr = { name: 'data-line', value: "self . lineNumber" };
      expect(attr.name).toBe('data-line');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="inline-highlight" to inlineHighlight', () => {
      const binding = {"part":"inlineHighlight","dataPart":"inline-highlight"};
      expect(binding.dataPart).toBe('inline-highlight');
    });

    it('should bind data-type on inlineHighlight', () => {
      const attr = { name: 'data-type', value: "self . changeType" };
      expect(attr.name).toBe('data-type');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-start on expandButton', () => {
      const attr = { name: 'data-start', value: "self . startLine" };
      expect(attr.name).toBe('data-start');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-end on expandButton', () => {
      const attr = { name: 'data-end', value: "self . endLine" };
      expect(attr.name).toBe('data-end');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on expandButton', () => {
      const handler = { event: 'onClick', action: "send ( EXPAND , { start : self . startLine , end : self . endLine } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="collapsed-range" to collapsedRange', () => {
      const binding = {"part":"collapsedRange","dataPart":"collapsed-range"};
      expect(binding.dataPart).toBe('collapsed-range');
    });

    it('should bind data-line-count on collapsedRange', () => {
      const attr = { name: 'data-line-count', value: "self . lineCount" };
      expect(attr.name).toBe('data-line-count');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","modeToggle","toolbar","changeStats","prevChangeButton","nextChangeButton","fileList","fileItem","fileItemName","fileItemStats","diffPanel","leftPane","rightPane","leftHeader","rightHeader","hunkHeader","lineNumber","lineContent","addedLine","removedLine","unchangedLine","inlineHighlight","expandButton","collapsedRange"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('modeToggle')).toBe(true);
      expect(anatomyParts.has('toolbar')).toBe(true);
      expect(anatomyParts.has('changeStats')).toBe(true);
      expect(anatomyParts.has('prevChangeButton')).toBe(true);
      expect(anatomyParts.has('nextChangeButton')).toBe(true);
      expect(anatomyParts.has('fileList')).toBe(true);
      expect(anatomyParts.has('fileItem')).toBe(true);
      expect(anatomyParts.has('fileItemName')).toBe(true);
      expect(anatomyParts.has('fileItemStats')).toBe(true);
      expect(anatomyParts.has('diffPanel')).toBe(true);
      expect(anatomyParts.has('leftPane')).toBe(true);
      expect(anatomyParts.has('rightPane')).toBe(true);
      expect(anatomyParts.has('leftHeader')).toBe(true);
      expect(anatomyParts.has('rightHeader')).toBe(true);
      expect(anatomyParts.has('hunkHeader')).toBe(true);
      expect(anatomyParts.has('lineNumber')).toBe(true);
      expect(anatomyParts.has('lineContent')).toBe(true);
      expect(anatomyParts.has('addedLine')).toBe(true);
      expect(anatomyParts.has('removedLine')).toBe(true);
      expect(anatomyParts.has('unchangedLine')).toBe(true);
      expect(anatomyParts.has('inlineHighlight')).toBe(true);
      expect(anatomyParts.has('expandButton')).toBe(true);
      expect(anatomyParts.has('collapsedRange')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should handle N key with action: NEXT_CHANGE', () => {
      const binding = { key: 'N', action: 'NEXT_CHANGE' };
      expect(binding.key).toBe('N');
      expect(binding.action).toBe('NEXT_CHANGE');
    });

    it('should handle Shift+N key with action: PREV_CHANGE', () => {
      const binding = { key: 'Shift+N', action: 'PREV_CHANGE' };
      expect(binding.key).toBe('Shift+N');
      expect(binding.action).toBe('PREV_CHANGE');
    });

    it('should handle ArrowDown key with action: SCROLL_DOWN', () => {
      const binding = { key: 'ArrowDown', action: 'SCROLL_DOWN' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('SCROLL_DOWN');
    });

    it('should handle ArrowUp key with action: SCROLL_UP', () => {
      const binding = { key: 'ArrowUp', action: 'SCROLL_UP' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('SCROLL_UP');
    });

    it('should handle PageDown key with action: SCROLL_PAGE_DOWN', () => {
      const binding = { key: 'PageDown', action: 'SCROLL_PAGE_DOWN' };
      expect(binding.key).toBe('PageDown');
      expect(binding.action).toBe('SCROLL_PAGE_DOWN');
    });

    it('should handle PageUp key with action: SCROLL_PAGE_UP', () => {
      const binding = { key: 'PageUp', action: 'SCROLL_PAGE_UP' };
      expect(binding.key).toBe('PageUp');
      expect(binding.action).toBe('SCROLL_PAGE_UP');
    });

    it('should handle Home key with action: SCROLL_TOP', () => {
      const binding = { key: 'Home', action: 'SCROLL_TOP' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('SCROLL_TOP');
    });

    it('should handle End key with action: SCROLL_BOTTOM', () => {
      const binding = { key: 'End', action: 'SCROLL_BOTTOM' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('SCROLL_BOTTOM');
    });

    it('should handle M key with action: TOGGLE_MODE', () => {
      const binding = { key: 'M', action: 'TOGGLE_MODE' };
      expect(binding.key).toBe('M');
      expect(binding.action).toBe('TOGGLE_MODE');
    });

    it('should handle E key with action: EXPAND_ALL', () => {
      const binding = { key: 'E', action: 'EXPAND_ALL' };
      expect(binding.key).toBe('E');
      expect(binding.action).toBe('EXPAND_ALL');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SWITCH_TO_UNIFIED","SWITCH_TO_SIDE_BY_SIDE","LOAD","LOAD_COMPLETE","LOAD_ERROR","RETRY","EXPAND","COLLAPSE","NAVIGATE_CHANGE","SCROLL_AWAY"]);
      const keyboardActions = ["FOCUS_NEXT","FOCUS_PREV","NEXT_CHANGE","PREV_CHANGE","SCROLL_DOWN","SCROLL_UP","SCROLL_PAGE_DOWN","SCROLL_PAGE_UP","SCROLL_TOP","SCROLL_BOTTOM","TOGGLE_MODE","EXPAND_ALL"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "region"', () => {
      expect('region').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "modeToggle"', () => {
      expect('modeToggle').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Diff viewer" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on diffPanel', () => {
      const ariaAttr = { part: 'diffPanel', name: 'role', value: "document" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on diffPanel', () => {
      const ariaAttr = { part: 'diffPanel', name: 'aria-label', value: "if state . mode = = sideBySide then Side-by-side diff else Unified diff" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on diffPanel', () => {
      const ariaAttr = { part: 'diffPanel', name: 'aria-roledescription', value: "diff" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on leftPane', () => {
      const ariaAttr = { part: 'leftPane', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on leftPane', () => {
      const ariaAttr = { part: 'leftPane', name: 'aria-label', value: "Original version" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on rightPane', () => {
      const ariaAttr = { part: 'rightPane', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on rightPane', () => {
      const ariaAttr = { part: 'rightPane', name: 'aria-label', value: "Modified version" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on addedLine', () => {
      const ariaAttr = { part: 'addedLine', name: 'aria-label', value: "concat ( Added line  , self . lineNumber , :  , self . content )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on removedLine', () => {
      const ariaAttr = { part: 'removedLine', name: 'aria-label', value: "concat ( Removed line  , self . lineNumber , :  , self . content )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on unchangedLine', () => {
      const ariaAttr = { part: 'unchangedLine', name: 'aria-label', value: "concat ( Line  , self . lineNumber )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on changeStats', () => {
      const ariaAttr = { part: 'changeStats', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-atomic on changeStats', () => {
      const ariaAttr = { part: 'changeStats', name: 'aria-atomic', value: "true" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on fileList', () => {
      const ariaAttr = { part: 'fileList', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on fileList', () => {
      const ariaAttr = { part: 'fileList', name: 'aria-label', value: "Changed files" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on fileItem', () => {
      const ariaAttr = { part: 'fileItem', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on expandButton', () => {
      const ariaAttr = { part: 'expandButton', name: 'aria-label', value: "concat ( Expand  , self . lineCount ,  unchanged lines )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "region" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "if ? fileName then concat ( Diff viewer:  , ? fileName ) else Diff viewer" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-busy on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-busy', value: "if ? loading then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part modeToggle', () => {
      const connectAria = { part: 'modeToggle', attr: 'aria-label', value: "Diff view mode" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part toolbar', () => {
      const connectAria = { part: 'toolbar', attr: 'role', value: "toolbar" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part toolbar', () => {
      const connectAria = { part: 'toolbar', attr: 'aria-label', value: "Diff controls" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-live on connect part changeStats', () => {
      const connectAria = { part: 'changeStats', attr: 'aria-live', value: "polite" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-atomic on connect part changeStats', () => {
      const connectAria = { part: 'changeStats', attr: 'aria-atomic', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part changeStats', () => {
      const connectAria = { part: 'changeStats', attr: 'aria-label', value: "concat ( ? additions ,  additions,  , ? deletions ,  deletions )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part prevChangeButton', () => {
      const connectAria = { part: 'prevChangeButton', attr: 'aria-label', value: "Previous change" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part nextChangeButton', () => {
      const connectAria = { part: 'nextChangeButton', attr: 'aria-label', value: "Next change" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part fileList', () => {
      const connectAria = { part: 'fileList', attr: 'role', value: "list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part fileList', () => {
      const connectAria = { part: 'fileList', attr: 'aria-label', value: "Changed files" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part fileItem', () => {
      const connectAria = { part: 'fileItem', attr: 'role', value: "listitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part fileItemStats', () => {
      const connectAria = { part: 'fileItemStats', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part diffPanel', () => {
      const connectAria = { part: 'diffPanel', attr: 'role', value: "document" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part diffPanel', () => {
      const connectAria = { part: 'diffPanel', attr: 'aria-label', value: "if state . mode = = sideBySide then Side-by-side diff else Unified diff" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part diffPanel', () => {
      const connectAria = { part: 'diffPanel', attr: 'aria-roledescription', value: "diff" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part leftPane', () => {
      const connectAria = { part: 'leftPane', attr: 'role', value: "region" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part leftPane', () => {
      const connectAria = { part: 'leftPane', attr: 'aria-label', value: "Original version" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part rightPane', () => {
      const connectAria = { part: 'rightPane', attr: 'role', value: "region" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part rightPane', () => {
      const connectAria = { part: 'rightPane', attr: 'aria-label', value: "Modified version" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part hunkHeader', () => {
      const connectAria = { part: 'hunkHeader', attr: 'aria-label', value: "concat ( Hunk:  , self . range )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part lineNumber', () => {
      const connectAria = { part: 'lineNumber', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part addedLine', () => {
      const connectAria = { part: 'addedLine', attr: 'aria-label', value: "concat ( Added line  , self . lineNumber , :  , self . content )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part removedLine', () => {
      const connectAria = { part: 'removedLine', attr: 'aria-label', value: "concat ( Removed line  , self . lineNumber , :  , self . content )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part inlineHighlight', () => {
      const connectAria = { part: 'inlineHighlight', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part expandButton', () => {
      const connectAria = { part: 'expandButton', attr: 'aria-label', value: "concat ( Expand  , self . lineCount ,  unchanged lines )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part collapsedRange', () => {
      const connectAria = { part: 'collapsedRange', attr: 'aria-label', value: "concat ( self . lineCount ,  unchanged lines hidden )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "original" with type "String"', () => {
      const prop = { name: 'original', type: 'String', defaultValue: "" };
      expect(prop.name).toBe('original');
      expect(prop.type).toBeTruthy();
    });

    it('should default "original" to ""', () => {
      expect("").toBe("");
    });

    it('should declare prop "modified" with type "String"', () => {
      const prop = { name: 'modified', type: 'String', defaultValue: "" };
      expect(prop.name).toBe('modified');
      expect(prop.type).toBeTruthy();
    });

    it('should default "modified" to ""', () => {
      expect("").toBe("");
    });

    it('should declare prop "mode" with type "union side-by-side | unified"', () => {
      const prop = { name: 'mode', type: 'union side-by-side | unified', defaultValue: "side-by-side" };
      expect(prop.name).toBe('mode');
      expect(prop.type).toBeTruthy();
    });

    it('should default "mode" to "side-by-side"', () => {
      expect("side-by-side").toBe("side-by-side");
    });

    it('should declare prop "language" with type "option String"', () => {
      const prop = { name: 'language', type: 'option String' };
      expect(prop.name).toBe('language');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "fileName" with type "option String"', () => {
      const prop = { name: 'fileName', type: 'option String' };
      expect(prop.name).toBe('fileName');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "files" with type "list FileDiff"', () => {
      const prop = { name: 'files', type: 'list FileDiff', defaultValue: "[ ]" };
      expect(prop.name).toBe('files');
      expect(prop.type).toBeTruthy();
    });

    it('should default "files" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "contextLines" with type "Int"', () => {
      const prop = { name: 'contextLines', type: 'Int', defaultValue: "3" };
      expect(prop.name).toBe('contextLines');
      expect(prop.type).toBeTruthy();
    });

    it('should default "contextLines" to "3"', () => {
      expect("3").toBe("3");
    });

    it('should declare prop "showLineNumbers" with type "Bool"', () => {
      const prop = { name: 'showLineNumbers', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showLineNumbers');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showLineNumbers" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showInlineHighlight" with type "Bool"', () => {
      const prop = { name: 'showInlineHighlight', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showInlineHighlight');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showInlineHighlight" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showFileList" with type "Bool"', () => {
      const prop = { name: 'showFileList', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showFileList');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showFileList" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "expandCollapsed" with type "Bool"', () => {
      const prop = { name: 'expandCollapsed', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('expandCollapsed');
      expect(prop.type).toBeTruthy();
    });

    it('should default "expandCollapsed" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "loading" with type "Bool"', () => {
      const prop = { name: 'loading', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('loading');
      expect(prop.type).toBeTruthy();
    });

    it('should default "loading" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "additions" with type "Int"', () => {
      const prop = { name: 'additions', type: 'Int', defaultValue: "0" };
      expect(prop.name).toBe('additions');
      expect(prop.type).toBeTruthy();
    });

    it('should default "additions" to "0"', () => {
      expect("0").toBe("0");
    });

    it('should declare prop "deletions" with type "Int"', () => {
      const prop = { name: 'deletions', type: 'Int', defaultValue: "0" };
      expect(prop.name).toBe('deletions');
      expect(prop.type).toBeTruthy();
    });

    it('should default "deletions" to "0"', () => {
      expect("0").toBe("0");
    });

    it('should declare prop "onModeChange" with type "option Function"', () => {
      const prop = { name: 'onModeChange', type: 'option Function' };
      expect(prop.name).toBe('onModeChange');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "onFileSelect" with type "option Function"', () => {
      const prop = { name: 'onFileSelect', type: 'option Function' };
      expect(prop.name).toBe('onFileSelect');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","modeToggle","toolbar","changeStats","prevChangeButton","nextChangeButton","fileList","fileItem","fileItemName","fileItemStats","diffPanel","leftPane","rightPane","leftHeader","rightHeader","hunkHeader","lineNumber","lineContent","addedLine","removedLine","unchangedLine","inlineHighlight","expandButton","collapsedRange"];
      expect(parts.length).toBe(24);
      expect(parts).toContain('root');
      expect(parts).toContain('modeToggle');
      expect(parts).toContain('toolbar');
      expect(parts).toContain('changeStats');
      expect(parts).toContain('prevChangeButton');
      expect(parts).toContain('nextChangeButton');
      expect(parts).toContain('fileList');
      expect(parts).toContain('fileItem');
      expect(parts).toContain('fileItemName');
      expect(parts).toContain('fileItemStats');
      expect(parts).toContain('diffPanel');
      expect(parts).toContain('leftPane');
      expect(parts).toContain('rightPane');
      expect(parts).toContain('leftHeader');
      expect(parts).toContain('rightHeader');
      expect(parts).toContain('hunkHeader');
      expect(parts).toContain('lineNumber');
      expect(parts).toContain('lineContent');
      expect(parts).toContain('addedLine');
      expect(parts).toContain('removedLine');
      expect(parts).toContain('unchangedLine');
      expect(parts).toContain('inlineHighlight');
      expect(parts).toContain('expandButton');
      expect(parts).toContain('collapsedRange');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('widget').toBeTruthy(); // modeToggle: widget
      expect('container').toBeTruthy(); // toolbar: container
      expect('text').toBeTruthy(); // changeStats: text
      expect('action').toBeTruthy(); // prevChangeButton: action
      expect('action').toBeTruthy(); // nextChangeButton: action
      expect('container').toBeTruthy(); // fileList: container
      expect('action').toBeTruthy(); // fileItem: action
      expect('text').toBeTruthy(); // fileItemName: text
      expect('text').toBeTruthy(); // fileItemStats: text
      expect('container').toBeTruthy(); // diffPanel: container
      expect('container').toBeTruthy(); // leftPane: container
      expect('container').toBeTruthy(); // rightPane: container
      expect('container').toBeTruthy(); // leftHeader: container
      expect('container').toBeTruthy(); // rightHeader: container
      expect('container').toBeTruthy(); // hunkHeader: container
      expect('text').toBeTruthy(); // lineNumber: text
      expect('text').toBeTruthy(); // lineContent: text
      expect('container').toBeTruthy(); // addedLine: container
      expect('container').toBeTruthy(); // removedLine: container
      expect('container').toBeTruthy(); // unchangedLine: container
      expect('container').toBeTruthy(); // inlineHighlight: container
      expect('action').toBeTruthy(); // expandButton: action
      expect('container').toBeTruthy(); // collapsedRange: container
    });

  });

});
