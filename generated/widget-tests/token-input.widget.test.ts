// Auto-generated by generate-widget-tests.ts from token-input.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: TokenInput', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["static"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('static');
    });

    it('should transition from static to hovered on HOVER', () => {
      const transition = { from: 'static', event: 'HOVER', target: 'hovered' };
      expect(transition.from).toBe('static');
      expect(transition.event).toBe('HOVER');
      expect(transition.target).toBe('hovered');
      // Target state 'hovered' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from static to focused on FOCUS', () => {
      const transition = { from: 'static', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('static');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from static to selected on SELECT', () => {
      const transition = { from: 'static', event: 'SELECT', target: 'selected' };
      expect(transition.from).toBe('static');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to static on UNHOVER', () => {
      const transition = { from: 'hovered', event: 'UNHOVER', target: 'static' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('UNHOVER');
      expect(transition.target).toBe('static');
      // Target state 'static' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to focused on FOCUS', () => {
      const transition = { from: 'hovered', event: 'FOCUS', target: 'focused' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('FOCUS');
      expect(transition.target).toBe('focused');
      // Target state 'focused' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from hovered to selected on SELECT', () => {
      const transition = { from: 'hovered', event: 'SELECT', target: 'selected' };
      expect(transition.from).toBe('hovered');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to static on BLUR', () => {
      const transition = { from: 'focused', event: 'BLUR', target: 'static' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('static');
      // Target state 'static' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to removed on REMOVE', () => {
      const transition = { from: 'focused', event: 'REMOVE', target: 'removed' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('REMOVE');
      expect(transition.target).toBe('removed');
      // Target state 'removed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from focused to selected on SELECT', () => {
      const transition = { from: 'focused', event: 'SELECT', target: 'selected' };
      expect(transition.from).toBe('focused');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selected to static on DESELECT', () => {
      const transition = { from: 'selected', event: 'DESELECT', target: 'static' };
      expect(transition.from).toBe('selected');
      expect(transition.event).toBe('DESELECT');
      expect(transition.target).toBe('static');
      // Target state 'static' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selected to removed on REMOVE', () => {
      const transition = { from: 'selected', event: 'REMOVE', target: 'removed' };
      expect(transition.from).toBe('selected');
      expect(transition.event).toBe('REMOVE');
      expect(transition.target).toBe('removed');
      // Target state 'removed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selected to static on BLUR', () => {
      const transition = { from: 'selected', event: 'BLUR', target: 'static' };
      expect(transition.from).toBe('selected');
      expect(transition.event).toBe('BLUR');
      expect(transition.target).toBe('static');
      // Target state 'static' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"static","events":["HOVER","FOCUS","SELECT"]},{"name":"hovered","events":["UNHOVER","FOCUS","SELECT"]},{"name":"focused","events":["BLUR","REMOVE","SELECT"]},{"name":"selected","events":["DESELECT","REMOVE","BLUR"]},{"name":"removed","events":[]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["static","hovered","focused","selected","removed"]);
      const baseNames = new Set(["static","hovered","focused","selected","removed"]);
      expect(validNames.has('hovered') || baseNames.has('hovered')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('static') || baseNames.has('static')).toBe(true);
      expect(validNames.has('focused') || baseNames.has('focused')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('static') || baseNames.has('static')).toBe(true);
      expect(validNames.has('removed') || baseNames.has('removed')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('static') || baseNames.has('static')).toBe(true);
      expect(validNames.has('removed') || baseNames.has('removed')).toBe(true);
      expect(validNames.has('static') || baseNames.has('static')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="token" to root', () => {
      const binding = {"part":"root","dataPart":"token"};
      expect(binding.dataPart).toBe('token');
    });

    it('should bind data-type on root', () => {
      const attr = { name: 'data-type', value: "? type" };
      expect(attr.name).toBe('data-type');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "state" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-removable on root', () => {
      const attr = { name: 'data-removable', value: "if ? removable then true else false" };
      expect(attr.name).toBe('data-removable');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-disabled on root', () => {
      const attr = { name: 'data-disabled', value: "if ? disabled then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-color on root', () => {
      const attr = { name: 'data-color', value: "? color" };
      expect(attr.name).toBe('data-color');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on root', () => {
      const handler = { event: 'onClick', action: "send ( SELECT )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onDoubleClick handler on root', () => {
      const handler = { event: 'onDoubleClick', action: "send ( SELECT )" };
      expect(handler.event).toBe('onDoubleClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerEnter handler on root', () => {
      const handler = { event: 'onPointerEnter', action: "send ( HOVER )" };
      expect(handler.event).toBe('onPointerEnter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerLeave handler on root', () => {
      const handler = { event: 'onPointerLeave', action: "send ( UNHOVER )" };
      expect(handler.event).toBe('onPointerLeave');
      expect(handler.action).toBeDefined();
    });

    it('should bind onFocus handler on root', () => {
      const handler = { event: 'onFocus', action: "send ( FOCUS )" };
      expect(handler.event).toBe('onFocus');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on root', () => {
      const handler = { event: 'onBlur', action: "send ( BLUR )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Backspace handler on root', () => {
      const handler = { event: 'onKeyDown-Backspace', action: "if ? removable and not ? disabled then send ( REMOVE ) else noop" };
      expect(handler.event).toBe('onKeyDown-Backspace');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Delete handler on root', () => {
      const handler = { event: 'onKeyDown-Delete', action: "if ? removable and not ? disabled then send ( REMOVE ) else noop" };
      expect(handler.event).toBe('onKeyDown-Delete');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on root', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( SELECT )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Escape handler on root', () => {
      const handler = { event: 'onKeyDown-Escape', action: "send ( DESELECT )" };
      expect(handler.event).toBe('onKeyDown-Escape');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="label" to label', () => {
      const binding = {"part":"label","dataPart":"label"};
      expect(binding.dataPart).toBe('label');
    });

    it('should bind data-part="type-icon" to typeIcon', () => {
      const binding = {"part":"typeIcon","dataPart":"type-icon"};
      expect(binding.dataPart).toBe('type-icon');
    });

    it('should bind data-type on typeIcon', () => {
      const attr = { name: 'data-type', value: "? type" };
      expect(attr.name).toBe('data-type');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-visible on typeIcon', () => {
      const attr = { name: 'data-visible', value: "if ? type then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="remove" to removeButton', () => {
      const binding = {"part":"removeButton","dataPart":"remove"};
      expect(binding.dataPart).toBe('remove');
    });

    it('should bind data-visible on removeButton', () => {
      const attr = { name: 'data-visible', value: "if ? removable and not ? disabled then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on removeButton', () => {
      const handler = { event: 'onClick', action: "send ( REMOVE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on removeButton', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( REMOVE )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","label","typeIcon","removeButton"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('label')).toBe(true);
      expect(anatomyParts.has('typeIcon')).toBe(true);
      expect(anatomyParts.has('removeButton')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Backspace key with action: REMOVE', () => {
      const binding = { key: 'Backspace', action: 'REMOVE' };
      expect(binding.key).toBe('Backspace');
      expect(binding.action).toBe('REMOVE');
    });

    it('should handle Delete key with action: REMOVE', () => {
      const binding = { key: 'Delete', action: 'REMOVE' };
      expect(binding.key).toBe('Delete');
      expect(binding.action).toBe('REMOVE');
    });

    it('should handle Enter key with action: SELECT', () => {
      const binding = { key: 'Enter', action: 'SELECT' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('SELECT');
    });

    it('should handle Escape key with action: DESELECT', () => {
      const binding = { key: 'Escape', action: 'DESELECT' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('DESELECT');
    });

    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["HOVER","FOCUS","SELECT","UNHOVER","BLUR","REMOVE","DESELECT"]);
      const keyboardActions = ["REMOVE","REMOVE","SELECT","DESELECT","FOCUS_NEXT","FOCUS_PREV"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "option"', () => {
      expect('option').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "root"', () => {
      expect('root').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "option" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "concat ( ? label , if ? type then concat (  ( , ? type , ) ) else  )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "token" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-selected', value: "if state = = selected then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on label', () => {
      const ariaAttr = { part: 'label', name: 'role', value: "presentation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on typeIcon', () => {
      const ariaAttr = { part: 'typeIcon', name: 'role', value: "img" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on typeIcon', () => {
      const ariaAttr = { part: 'typeIcon', name: 'aria-label', value: "if ? type then concat ( Type:  , ? type ) else Untyped" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on typeIcon', () => {
      const ariaAttr = { part: 'typeIcon', name: 'aria-hidden', value: "if not ? type then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on removeButton', () => {
      const ariaAttr = { part: 'removeButton', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on removeButton', () => {
      const ariaAttr = { part: 'removeButton', name: 'aria-label', value: "concat ( Remove  , ? label ,  token )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "option" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "concat ( ? label , if ? type then concat (  ( , ? type , ) ) else  )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-roledescription', value: "token" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-selected', value: "if state = = selected then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-disabled on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-disabled', value: "if ? disabled then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part typeIcon', () => {
      const connectAria = { part: 'typeIcon', attr: 'aria-hidden', value: "if not ? type then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part typeIcon', () => {
      const connectAria = { part: 'typeIcon', attr: 'aria-label', value: "if ? type then concat ( Type:  , ? type ) else" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part removeButton', () => {
      const connectAria = { part: 'removeButton', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part removeButton', () => {
      const connectAria = { part: 'removeButton', attr: 'aria-label', value: "concat ( Remove  , ? label ,  token )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "label" with type "String"', () => {
      const prop = { name: 'label', type: 'String' };
      expect(prop.name).toBe('label');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "type" with type "option String"', () => {
      const prop = { name: 'type', type: 'option String' };
      expect(prop.name).toBe('type');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "removable" with type "Bool"', () => {
      const prop = { name: 'removable', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('removable');
      expect(prop.type).toBeTruthy();
    });

    it('should default "removable" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "disabled" with type "Bool"', () => {
      const prop = { name: 'disabled', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('disabled');
      expect(prop.type).toBeTruthy();
    });

    it('should default "disabled" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "color" with type "option String"', () => {
      const prop = { name: 'color', type: 'option String' };
      expect(prop.name).toBe('color');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "value" with type "option String"', () => {
      const prop = { name: 'value', type: 'option String' };
      expect(prop.name).toBe('value');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "id" with type "option String"', () => {
      const prop = { name: 'id', type: 'option String' };
      expect(prop.name).toBe('id');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","label","typeIcon","removeButton"];
      expect(parts.length).toBe(4);
      expect(parts).toContain('root');
      expect(parts).toContain('label');
      expect(parts).toContain('typeIcon');
      expect(parts).toContain('removeButton');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('text').toBeTruthy(); // label: text
      expect('container').toBeTruthy(); // typeIcon: container
      expect('action').toBeTruthy(); // removeButton: action
    });

  });

});
