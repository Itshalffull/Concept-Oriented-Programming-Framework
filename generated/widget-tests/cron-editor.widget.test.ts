// Auto-generated by generate-widget-tests.ts from cron-editor.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: CronEditor', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["simple","validation.valid"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('simple');
      expect(initialStates).toContain('validation.valid');
    });

    it('should transition from simple to advanced on SWITCH_ADVANCED', () => {
      const transition = { from: 'simple', event: 'SWITCH_ADVANCED', target: 'advanced' };
      expect(transition.from).toBe('simple');
      expect(transition.event).toBe('SWITCH_ADVANCED');
      expect(transition.target).toBe('advanced');
      // Target state 'advanced' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from simple to simple on CHANGE', () => {
      const transition = { from: 'simple', event: 'CHANGE', target: 'simple' };
      expect(transition.from).toBe('simple');
      expect(transition.event).toBe('CHANGE');
      expect(transition.target).toBe('simple');
      // Target state 'simple' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from advanced to simple on SWITCH_SIMPLE', () => {
      const transition = { from: 'advanced', event: 'SWITCH_SIMPLE', target: 'simple' };
      expect(transition.from).toBe('advanced');
      expect(transition.event).toBe('SWITCH_SIMPLE');
      expect(transition.target).toBe('simple');
      // Target state 'simple' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from advanced to advanced on CHANGE', () => {
      const transition = { from: 'advanced', event: 'CHANGE', target: 'advanced' };
      expect(transition.from).toBe('advanced');
      expect(transition.event).toBe('CHANGE');
      expect(transition.target).toBe('advanced');
      // Target state 'advanced' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from validation.valid to invalid on INVALIDATE', () => {
      const transition = { from: 'validation.valid', event: 'INVALIDATE', target: 'invalid' };
      expect(transition.from).toBe('validation.valid');
      expect(transition.event).toBe('INVALIDATE');
      expect(transition.target).toBe('invalid');
      // Target state 'invalid' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from validation.invalid to valid on REVALIDATE', () => {
      const transition = { from: 'validation.invalid', event: 'REVALIDATE', target: 'valid' };
      expect(transition.from).toBe('validation.invalid');
      expect(transition.event).toBe('REVALIDATE');
      expect(transition.target).toBe('valid');
      // Target state 'valid' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"simple","events":["SWITCH_ADVANCED","CHANGE"]},{"name":"advanced","events":["SWITCH_SIMPLE","CHANGE"]},{"name":"validation.valid","events":["INVALIDATE"]},{"name":"validation.invalid","events":["REVALIDATE"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["simple","advanced","validation.valid","validation.invalid"]);
      const baseNames = new Set(["simple","advanced","valid","invalid"]);
      expect(validNames.has('advanced') || baseNames.has('advanced')).toBe(true);
      expect(validNames.has('simple') || baseNames.has('simple')).toBe(true);
      expect(validNames.has('simple') || baseNames.has('simple')).toBe(true);
      expect(validNames.has('advanced') || baseNames.has('advanced')).toBe(true);
      expect(validNames.has('invalid') || baseNames.has('invalid')).toBe(true);
      expect(validNames.has('valid') || baseNames.has('valid')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "state" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-mode on root', () => {
      const attr = { name: 'data-mode', value: "? mode" };
      expect(attr.name).toBe('data-mode');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-valid on root', () => {
      const attr = { name: 'data-valid', value: "if state . validation = = valid then true else false" };
      expect(attr.name).toBe('data-valid');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-readonly on root', () => {
      const attr = { name: 'data-readonly', value: "if ? readOnly then true else false" };
      expect(attr.name).toBe('data-readonly');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="tabs" to tabs', () => {
      const binding = {"part":"tabs","dataPart":"tabs"};
      expect(binding.dataPart).toBe('tabs');
    });

    it('should bind data-active on tabs', () => {
      const attr = { name: 'data-active', value: "? mode" };
      expect(attr.name).toBe('data-active');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="simple-editor" to simpleEditor', () => {
      const binding = {"part":"simpleEditor","dataPart":"simple-editor"};
      expect(binding.dataPart).toBe('simple-editor');
    });

    it('should bind data-visible on simpleEditor', () => {
      const attr = { name: 'data-visible', value: "if ? mode = = simple then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="frequency-select" to frequencySelect', () => {
      const binding = {"part":"frequencySelect","dataPart":"frequency-select"};
      expect(binding.dataPart).toBe('frequency-select');
    });

    it('should bind data-value on frequencySelect', () => {
      const attr = { name: 'data-value', value: "? frequency" };
      expect(attr.name).toBe('data-value');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="time-input" to timeInput', () => {
      const binding = {"part":"timeInput","dataPart":"time-input"};
      expect(binding.dataPart).toBe('time-input');
    });

    it('should bind data-hour on timeInput', () => {
      const attr = { name: 'data-hour', value: "? hour" };
      expect(attr.name).toBe('data-hour');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-minute on timeInput', () => {
      const attr = { name: 'data-minute', value: "? minute" };
      expect(attr.name).toBe('data-minute');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-visible on timeInput', () => {
      const attr = { name: 'data-visible', value: "if ? frequency != minutely then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="day-select" to daySelect', () => {
      const binding = {"part":"daySelect","dataPart":"day-select"};
      expect(binding.dataPart).toBe('day-select');
    });

    it('should bind data-visible on daySelect', () => {
      const attr = { name: 'data-visible', value: "if ? frequency = = weekly or ? frequency = = monthly then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-frequency on daySelect', () => {
      const attr = { name: 'data-frequency', value: "? frequency" };
      expect(attr.name).toBe('data-frequency');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="advanced-editor" to advancedEditor', () => {
      const binding = {"part":"advancedEditor","dataPart":"advanced-editor"};
      expect(binding.dataPart).toBe('advanced-editor');
    });

    it('should bind data-visible on advancedEditor', () => {
      const attr = { name: 'data-visible', value: "if ? mode = = advanced then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="cron-input" to cronInput', () => {
      const binding = {"part":"cronInput","dataPart":"cron-input"};
      expect(binding.dataPart).toBe('cron-input');
    });

    it('should bind data-valid on cronInput', () => {
      const attr = { name: 'data-valid', value: "if state . validation = = valid then true else false" };
      expect(attr.name).toBe('data-valid');
      expect(attr.value).toBeDefined();
    });

    it('should bind onInput handler on cronInput', () => {
      const handler = { event: 'onInput', action: "send ( CHANGE , { expression : self . value } )" };
      expect(handler.event).toBe('onInput');
      expect(handler.action).toBeDefined();
    });

    it('should bind onBlur handler on cronInput', () => {
      const handler = { event: 'onBlur', action: "send ( VALIDATE )" };
      expect(handler.event).toBe('onBlur');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="preview" to preview', () => {
      const binding = {"part":"preview","dataPart":"preview"};
      expect(binding.dataPart).toBe('preview');
    });

    it('should bind data-visible on preview', () => {
      const attr = { name: 'data-visible', value: "if state . validation = = valid then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-timezone on preview', () => {
      const attr = { name: 'data-timezone', value: "? timezone" };
      expect(attr.name).toBe('data-timezone');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="next-runs" to nextRuns', () => {
      const binding = {"part":"nextRuns","dataPart":"next-runs"};
      expect(binding.dataPart).toBe('next-runs');
    });

    it('should bind data-count on nextRuns', () => {
      const attr = { name: 'data-count', value: "? nextRunCount" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-timezone on nextRuns', () => {
      const attr = { name: 'data-timezone', value: "? timezone" };
      expect(attr.name).toBe('data-timezone');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-visible on nextRuns', () => {
      const attr = { name: 'data-visible', value: "if state . validation = = valid then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","tabs","simpleEditor","frequencySelect","timeInput","daySelect","advancedEditor","cronInput","preview","nextRuns"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('tabs')).toBe(true);
      expect(anatomyParts.has('simpleEditor')).toBe(true);
      expect(anatomyParts.has('frequencySelect')).toBe(true);
      expect(anatomyParts.has('timeInput')).toBe(true);
      expect(anatomyParts.has('daySelect')).toBe(true);
      expect(anatomyParts.has('advancedEditor')).toBe(true);
      expect(anatomyParts.has('cronInput')).toBe(true);
      expect(anatomyParts.has('preview')).toBe(true);
      expect(anatomyParts.has('nextRuns')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should handle Enter key with action: APPLY', () => {
      const binding = { key: 'Enter', action: 'APPLY' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('APPLY');
    });

    it('should handle Escape key with action: ESCAPE', () => {
      const binding = { key: 'Escape', action: 'ESCAPE' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('ESCAPE');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["SWITCH_ADVANCED","CHANGE","SWITCH_SIMPLE","INVALIDATE","REVALIDATE"]);
      const keyboardActions = ["FOCUS_NEXT","FOCUS_PREV","APPLY","ESCAPE"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "group"', () => {
      expect('group').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "tabs"', () => {
      expect('tabs').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? ariaLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-roledescription on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-roledescription', value: "cron editor" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on tabs', () => {
      const ariaAttr = { part: 'tabs', name: 'role', value: "tablist" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on tabs', () => {
      const ariaAttr = { part: 'tabs', name: 'aria-label', value: "Editor mode" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on simpleEditor', () => {
      const ariaAttr = { part: 'simpleEditor', name: 'role', value: "form" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on simpleEditor', () => {
      const ariaAttr = { part: 'simpleEditor', name: 'aria-label', value: "Simple schedule editor" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on simpleEditor', () => {
      const ariaAttr = { part: 'simpleEditor', name: 'aria-hidden', value: "if state = = advanced then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on frequencySelect', () => {
      const ariaAttr = { part: 'frequencySelect', name: 'aria-label', value: "Frequency" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on timeInput', () => {
      const ariaAttr = { part: 'timeInput', name: 'aria-label', value: "Time of day" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on daySelect', () => {
      const ariaAttr = { part: 'daySelect', name: 'aria-label', value: "if ? frequency = = weekly then Day of week else Day of month" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on advancedEditor', () => {
      const ariaAttr = { part: 'advancedEditor', name: 'role', value: "form" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on advancedEditor', () => {
      const ariaAttr = { part: 'advancedEditor', name: 'aria-label', value: "Advanced cron expression editor" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-hidden on advancedEditor', () => {
      const ariaAttr = { part: 'advancedEditor', name: 'aria-hidden', value: "if state != advanced then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on cronInput', () => {
      const ariaAttr = { part: 'cronInput', name: 'role', value: "textbox" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on cronInput', () => {
      const ariaAttr = { part: 'cronInput', name: 'aria-label', value: "Cron expression" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-invalid on cronInput', () => {
      const ariaAttr = { part: 'cronInput', name: 'aria-invalid', value: "if state . validation = = invalid then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-describedby on cronInput', () => {
      const ariaAttr = { part: 'cronInput', name: 'aria-describedby', value: "cron-hint" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on preview', () => {
      const ariaAttr = { part: 'preview', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on preview', () => {
      const ariaAttr = { part: 'preview', name: 'aria-label', value: "Schedule preview" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on preview', () => {
      const ariaAttr = { part: 'preview', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on nextRuns', () => {
      const ariaAttr = { part: 'nextRuns', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on nextRuns', () => {
      const ariaAttr = { part: 'nextRuns', name: 'aria-label', value: "Next scheduled runs" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? ariaLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-roledescription on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-roledescription', value: "cron editor" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part simpleEditor', () => {
      const connectAria = { part: 'simpleEditor', attr: 'role', value: "form" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part simpleEditor', () => {
      const connectAria = { part: 'simpleEditor', attr: 'aria-label', value: "Simple schedule editor" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part simpleEditor', () => {
      const connectAria = { part: 'simpleEditor', attr: 'aria-hidden', value: "if ? mode = = advanced then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part frequencySelect', () => {
      const connectAria = { part: 'frequencySelect', attr: 'aria-label', value: "Frequency" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part timeInput', () => {
      const connectAria = { part: 'timeInput', attr: 'aria-label', value: "Time of day" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part daySelect', () => {
      const connectAria = { part: 'daySelect', attr: 'aria-label', value: "if ? frequency = = weekly then Day of week else Day of month" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part advancedEditor', () => {
      const connectAria = { part: 'advancedEditor', attr: 'role', value: "form" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part advancedEditor', () => {
      const connectAria = { part: 'advancedEditor', attr: 'aria-label', value: "Advanced cron expression editor" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part advancedEditor', () => {
      const connectAria = { part: 'advancedEditor', attr: 'aria-hidden', value: "if ? mode != advanced then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part cronInput', () => {
      const connectAria = { part: 'cronInput', attr: 'role', value: "textbox" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part cronInput', () => {
      const connectAria = { part: 'cronInput', attr: 'aria-label', value: "Cron expression" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-invalid on connect part cronInput', () => {
      const connectAria = { part: 'cronInput', attr: 'aria-invalid', value: "if state . validation = = invalid then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-describedby on connect part cronInput', () => {
      const connectAria = { part: 'cronInput', attr: 'aria-describedby', value: "cron-hint" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part preview', () => {
      const connectAria = { part: 'preview', attr: 'role', value: "region" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part preview', () => {
      const connectAria = { part: 'preview', attr: 'aria-label', value: "Schedule preview" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-live on connect part preview', () => {
      const connectAria = { part: 'preview', attr: 'aria-live', value: "polite" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part nextRuns', () => {
      const connectAria = { part: 'nextRuns', attr: 'role', value: "list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part nextRuns', () => {
      const connectAria = { part: 'nextRuns', attr: 'aria-label', value: "Next scheduled runs" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "cronExpression" with type "String"', () => {
      const prop = { name: 'cronExpression', type: 'String', defaultValue: "0 * * * *" };
      expect(prop.name).toBe('cronExpression');
      expect(prop.type).toBeTruthy();
    });

    it('should default "cronExpression" to "0 * * * *"', () => {
      expect("0 * * * *").toBe("0 * * * *");
    });

    it('should declare prop "frequency" with type "union minutely | hourly | daily | weekly | monthly"', () => {
      const prop = { name: 'frequency', type: 'union minutely | hourly | daily | weekly | monthly', defaultValue: "hourly" };
      expect(prop.name).toBe('frequency');
      expect(prop.type).toBeTruthy();
    });

    it('should default "frequency" to "hourly"', () => {
      expect("hourly").toBe("hourly");
    });

    it('should declare prop "hour" with type "Int"', () => {
      const prop = { name: 'hour', type: 'Int', defaultValue: "0" };
      expect(prop.name).toBe('hour');
      expect(prop.type).toBeTruthy();
    });

    it('should default "hour" to "0"', () => {
      expect("0").toBe("0");
    });

    it('should declare prop "minute" with type "Int"', () => {
      const prop = { name: 'minute', type: 'Int', defaultValue: "0" };
      expect(prop.name).toBe('minute');
      expect(prop.type).toBeTruthy();
    });

    it('should default "minute" to "0"', () => {
      expect("0").toBe("0");
    });

    it('should declare prop "dayOfWeek" with type "list Int"', () => {
      const prop = { name: 'dayOfWeek', type: 'list Int', defaultValue: "[ ]" };
      expect(prop.name).toBe('dayOfWeek');
      expect(prop.type).toBeTruthy();
    });

    it('should default "dayOfWeek" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "dayOfMonth" with type "Int"', () => {
      const prop = { name: 'dayOfMonth', type: 'Int', defaultValue: "1" };
      expect(prop.name).toBe('dayOfMonth');
      expect(prop.type).toBeTruthy();
    });

    it('should default "dayOfMonth" to "1"', () => {
      expect("1").toBe("1");
    });

    it('should declare prop "ariaLabel" with type "String"', () => {
      const prop = { name: 'ariaLabel', type: 'String', defaultValue: "Cron Editor" };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "ariaLabel" to "Cron Editor"', () => {
      expect("Cron Editor").toBe("Cron Editor");
    });

    it('should declare prop "timezone" with type "String"', () => {
      const prop = { name: 'timezone', type: 'String', defaultValue: "UTC" };
      expect(prop.name).toBe('timezone');
      expect(prop.type).toBeTruthy();
    });

    it('should default "timezone" to "UTC"', () => {
      expect("UTC").toBe("UTC");
    });

    it('should declare prop "nextRunCount" with type "Int"', () => {
      const prop = { name: 'nextRunCount', type: 'Int', defaultValue: "5" };
      expect(prop.name).toBe('nextRunCount');
      expect(prop.type).toBeTruthy();
    });

    it('should default "nextRunCount" to "5"', () => {
      expect("5").toBe("5");
    });

    it('should declare prop "readOnly" with type "Bool"', () => {
      const prop = { name: 'readOnly', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('readOnly');
      expect(prop.type).toBeTruthy();
    });

    it('should default "readOnly" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "mode" with type "union simple | advanced"', () => {
      const prop = { name: 'mode', type: 'union simple | advanced', defaultValue: "simple" };
      expect(prop.name).toBe('mode');
      expect(prop.type).toBeTruthy();
    });

    it('should default "mode" to "simple"', () => {
      expect("simple").toBe("simple");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","tabs","simpleEditor","frequencySelect","timeInput","daySelect","advancedEditor","cronInput","preview","nextRuns"];
      expect(parts.length).toBe(10);
      expect(parts).toContain('root');
      expect(parts).toContain('tabs');
      expect(parts).toContain('simpleEditor');
      expect(parts).toContain('frequencySelect');
      expect(parts).toContain('timeInput');
      expect(parts).toContain('daySelect');
      expect(parts).toContain('advancedEditor');
      expect(parts).toContain('cronInput');
      expect(parts).toContain('preview');
      expect(parts).toContain('nextRuns');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('widget').toBeTruthy(); // tabs: widget
      expect('container').toBeTruthy(); // simpleEditor: container
      expect('widget').toBeTruthy(); // frequencySelect: widget
      expect('widget').toBeTruthy(); // timeInput: widget
      expect('widget').toBeTruthy(); // daySelect: widget
      expect('container').toBeTruthy(); // advancedEditor: container
      expect('widget').toBeTruthy(); // cronInput: widget
      expect('container').toBeTruthy(); // preview: container
      expect('container').toBeTruthy(); // nextRuns: container
    });

  });

});
