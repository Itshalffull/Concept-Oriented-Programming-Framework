// Auto-generated by generate-widget-tests.ts from canvas-panel.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: CanvasPanel', () => {
  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","header","title","tab-bar","tab","collapse-trigger","resize-handle","body"];
      expect(parts.length).toBe(8);
      expect(parts).toContain('root');
      expect(parts).toContain('header');
      expect(parts).toContain('title');
      expect(parts).toContain('tab-bar');
      expect(parts).toContain('tab');
      expect(parts).toContain('collapse-trigger');
      expect(parts).toContain('resize-handle');
      expect(parts).toContain('body');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // header: container
      expect('text').toBeTruthy(); // title: text
      expect('container').toBeTruthy(); // tab-bar: container
      expect('action').toBeTruthy(); // tab: action
      expect('action').toBeTruthy(); // collapse-trigger: action
      expect('interactive').toBeTruthy(); // resize-handle: interactive
      expect('container').toBeTruthy(); // body: container
    });

  });

});
