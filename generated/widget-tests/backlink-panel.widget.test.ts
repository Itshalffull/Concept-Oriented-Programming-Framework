// Auto-generated by generate-widget-tests.ts from backlink-panel.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: BacklinkPanel', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["panel.expanded","linkedSection.expanded","unlinkedSection.collapsed","loading.idle"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('panel.expanded');
      expect(initialStates).toContain('linkedSection.expanded');
      expect(initialStates).toContain('unlinkedSection.collapsed');
      expect(initialStates).toContain('loading.idle');
    });

    it('should transition from panel.expanded to collapsed on COLLAPSE', () => {
      const transition = { from: 'panel.expanded', event: 'COLLAPSE', target: 'collapsed' };
      expect(transition.from).toBe('panel.expanded');
      expect(transition.event).toBe('COLLAPSE');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from panel.collapsed to expanded on EXPAND', () => {
      const transition = { from: 'panel.collapsed', event: 'EXPAND', target: 'expanded' };
      expect(transition.from).toBe('panel.collapsed');
      expect(transition.event).toBe('EXPAND');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from linkedSection.expanded to collapsed on COLLAPSE_LINKED', () => {
      const transition = { from: 'linkedSection.expanded', event: 'COLLAPSE_LINKED', target: 'collapsed' };
      expect(transition.from).toBe('linkedSection.expanded');
      expect(transition.event).toBe('COLLAPSE_LINKED');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from linkedSection.collapsed to expanded on EXPAND_LINKED', () => {
      const transition = { from: 'linkedSection.collapsed', event: 'EXPAND_LINKED', target: 'expanded' };
      expect(transition.from).toBe('linkedSection.collapsed');
      expect(transition.event).toBe('EXPAND_LINKED');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from unlinkedSection.collapsed to expanded on EXPAND_UNLINKED', () => {
      const transition = { from: 'unlinkedSection.collapsed', event: 'EXPAND_UNLINKED', target: 'expanded' };
      expect(transition.from).toBe('unlinkedSection.collapsed');
      expect(transition.event).toBe('EXPAND_UNLINKED');
      expect(transition.target).toBe('expanded');
      // Target state 'expanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from unlinkedSection.expanded to collapsed on COLLAPSE_UNLINKED', () => {
      const transition = { from: 'unlinkedSection.expanded', event: 'COLLAPSE_UNLINKED', target: 'collapsed' };
      expect(transition.from).toBe('unlinkedSection.expanded');
      expect(transition.event).toBe('COLLAPSE_UNLINKED');
      expect(transition.target).toBe('collapsed');
      // Target state 'collapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading.idle to loading on LOAD', () => {
      const transition = { from: 'loading.idle', event: 'LOAD', target: 'loading' };
      expect(transition.from).toBe('loading.idle');
      expect(transition.event).toBe('LOAD');
      expect(transition.target).toBe('loading');
      // Target state 'loading' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading.loading to idle on LOAD_COMPLETE', () => {
      const transition = { from: 'loading.loading', event: 'LOAD_COMPLETE', target: 'idle' };
      expect(transition.from).toBe('loading.loading');
      expect(transition.event).toBe('LOAD_COMPLETE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading.loading to error on LOAD_ERROR', () => {
      const transition = { from: 'loading.loading', event: 'LOAD_ERROR', target: 'error' };
      expect(transition.from).toBe('loading.loading');
      expect(transition.event).toBe('LOAD_ERROR');
      expect(transition.target).toBe('error');
      // Target state 'error' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from loading.error to loading on RETRY', () => {
      const transition = { from: 'loading.error', event: 'RETRY', target: 'loading' };
      expect(transition.from).toBe('loading.error');
      expect(transition.event).toBe('RETRY');
      expect(transition.target).toBe('loading');
      // Target state 'loading' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"panel.expanded","events":["COLLAPSE"]},{"name":"panel.collapsed","events":["EXPAND"]},{"name":"linkedSection.expanded","events":["COLLAPSE_LINKED"]},{"name":"linkedSection.collapsed","events":["EXPAND_LINKED"]},{"name":"unlinkedSection.collapsed","events":["EXPAND_UNLINKED"]},{"name":"unlinkedSection.expanded","events":["COLLAPSE_UNLINKED"]},{"name":"loading.idle","events":["LOAD"]},{"name":"loading.loading","events":["LOAD_COMPLETE","LOAD_ERROR"]},{"name":"loading.error","events":["RETRY"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["panel.expanded","panel.collapsed","linkedSection.expanded","linkedSection.collapsed","unlinkedSection.collapsed","unlinkedSection.expanded","loading.idle","loading.loading","loading.error"]);
      const baseNames = new Set(["expanded","collapsed","idle","loading","error"]);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('expanded') || baseNames.has('expanded')).toBe(true);
      expect(validNames.has('collapsed') || baseNames.has('collapsed')).toBe(true);
      expect(validNames.has('loading') || baseNames.has('loading')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('error') || baseNames.has('error')).toBe(true);
      expect(validNames.has('loading') || baseNames.has('loading')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if state . panel = = expanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="header" to header', () => {
      const binding = {"part":"header","dataPart":"header"};
      expect(binding.dataPart).toBe('header');
    });

    it('should bind data-state on header', () => {
      const attr = { name: 'data-state', value: "if state . panel = = expanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="count" to count', () => {
      const binding = {"part":"count","dataPart":"count"};
      expect(binding.dataPart).toBe('count');
    });

    it('should bind onClick handler on collapseToggle', () => {
      const handler = { event: 'onClick', action: "if state . panel = = expanded then send ( COLLAPSE ) else send ( EXPAND )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-state on linkedSection', () => {
      const attr = { name: 'data-state', value: "if state . linkedSection = = expanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="linked-section-header" to linkedSectionHeader', () => {
      const binding = {"part":"linkedSectionHeader","dataPart":"linked-section-header"};
      expect(binding.dataPart).toBe('linked-section-header');
    });

    it('should bind onClick handler on linkedSectionHeader', () => {
      const handler = { event: 'onClick', action: "if state . linkedSection = = expanded then send ( COLLAPSE_LINKED ) else send ( EXPAND_LINKED )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="linked-section-label" to linkedSectionLabel', () => {
      const binding = {"part":"linkedSectionLabel","dataPart":"linked-section-label"};
      expect(binding.dataPart).toBe('linked-section-label');
    });

    it('should bind data-part="linked-section-count" to linkedSectionCount', () => {
      const binding = {"part":"linkedSectionCount","dataPart":"linked-section-count"};
      expect(binding.dataPart).toBe('linked-section-count');
    });

    it('should bind data-count on linkedList', () => {
      const attr = { name: 'data-count', value: "? linkedReferences . length" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-source on linkedItem', () => {
      const attr = { name: 'data-source', value: "self . sourceId" };
      expect(attr.name).toBe('data-source');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on linkedItem', () => {
      const handler = { event: 'onClick', action: "send ( NAVIGATE_TO_SOURCE , { id : self . sourceId } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on linkedItem', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( NAVIGATE_TO_SOURCE , { id : self . sourceId } )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="linked-item-breadcrumb" to linkedItemBreadcrumb', () => {
      const binding = {"part":"linkedItemBreadcrumb","dataPart":"linked-item-breadcrumb"};
      expect(binding.dataPart).toBe('linked-item-breadcrumb');
    });

    it('should bind data-part="linked-item-context" to linkedItemContext', () => {
      const binding = {"part":"linkedItemContext","dataPart":"linked-item-context"};
      expect(binding.dataPart).toBe('linked-item-context');
    });

    it('should bind data-part="linked-item-highlight" to linkedItemHighlight', () => {
      const binding = {"part":"linkedItemHighlight","dataPart":"linked-item-highlight"};
      expect(binding.dataPart).toBe('linked-item-highlight');
    });

    it('should bind data-state on unlinkedSection', () => {
      const attr = { name: 'data-state', value: "if state . unlinkedSection = = expanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="unlinked-section-header" to unlinkedSectionHeader', () => {
      const binding = {"part":"unlinkedSectionHeader","dataPart":"unlinked-section-header"};
      expect(binding.dataPart).toBe('unlinked-section-header');
    });

    it('should bind onClick handler on unlinkedSectionHeader', () => {
      const handler = { event: 'onClick', action: "if state . unlinkedSection = = expanded then send ( COLLAPSE_UNLINKED ) else send ( EXPAND_UNLINKED )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="unlinked-section-label" to unlinkedSectionLabel', () => {
      const binding = {"part":"unlinkedSectionLabel","dataPart":"unlinked-section-label"};
      expect(binding.dataPart).toBe('unlinked-section-label');
    });

    it('should bind data-part="unlinked-section-count" to unlinkedSectionCount', () => {
      const binding = {"part":"unlinkedSectionCount","dataPart":"unlinked-section-count"};
      expect(binding.dataPart).toBe('unlinked-section-count');
    });

    it('should bind data-count on unlinkedList', () => {
      const attr = { name: 'data-count', value: "? unlinkedMentions . length" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-source on unlinkedItem', () => {
      const attr = { name: 'data-source', value: "self . sourceId" };
      expect(attr.name).toBe('data-source');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on unlinkedItem', () => {
      const handler = { event: 'onClick', action: "send ( NAVIGATE_TO_SOURCE , { id : self . sourceId } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="unlinked-item-context" to unlinkedItemContext', () => {
      const binding = {"part":"unlinkedItemContext","dataPart":"unlinked-item-context"};
      expect(binding.dataPart).toBe('unlinked-item-context');
    });

    it('should bind onClick handler on linkButton', () => {
      const handler = { event: 'onClick', action: "send ( LINK_MENTION , { sourceId : self . sourceId , mentionId : self . mentionId } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="empty-state" to emptyState', () => {
      const binding = {"part":"emptyState","dataPart":"empty-state"};
      expect(binding.dataPart).toBe('empty-state');
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","header","title","count","collapseToggle","linkedSection","linkedSectionHeader","linkedSectionLabel","linkedSectionCount","linkedList","linkedItem","linkedItemBreadcrumb","linkedItemContext","linkedItemHighlight","unlinkedSection","unlinkedSectionHeader","unlinkedSectionLabel","unlinkedSectionCount","unlinkedList","unlinkedItem","unlinkedItemContext","linkButton","emptyState"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('header')).toBe(true);
      expect(anatomyParts.has('title')).toBe(true);
      expect(anatomyParts.has('count')).toBe(true);
      expect(anatomyParts.has('collapseToggle')).toBe(true);
      expect(anatomyParts.has('linkedSection')).toBe(true);
      expect(anatomyParts.has('linkedSectionHeader')).toBe(true);
      expect(anatomyParts.has('linkedSectionLabel')).toBe(true);
      expect(anatomyParts.has('linkedSectionCount')).toBe(true);
      expect(anatomyParts.has('linkedList')).toBe(true);
      expect(anatomyParts.has('linkedItem')).toBe(true);
      expect(anatomyParts.has('linkedItemBreadcrumb')).toBe(true);
      expect(anatomyParts.has('linkedItemContext')).toBe(true);
      expect(anatomyParts.has('linkedItemHighlight')).toBe(true);
      expect(anatomyParts.has('unlinkedSection')).toBe(true);
      expect(anatomyParts.has('unlinkedSectionHeader')).toBe(true);
      expect(anatomyParts.has('unlinkedSectionLabel')).toBe(true);
      expect(anatomyParts.has('unlinkedSectionCount')).toBe(true);
      expect(anatomyParts.has('unlinkedList')).toBe(true);
      expect(anatomyParts.has('unlinkedItem')).toBe(true);
      expect(anatomyParts.has('unlinkedItemContext')).toBe(true);
      expect(anatomyParts.has('linkButton')).toBe(true);
      expect(anatomyParts.has('emptyState')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should handle Shift+Tab key with action: FOCUS_PREV', () => {
      const binding = { key: 'Shift+Tab', action: 'FOCUS_PREV' };
      expect(binding.key).toBe('Shift+Tab');
      expect(binding.action).toBe('FOCUS_PREV');
    });

    it('should handle Enter key with action: NAVIGATE_TO_SOURCE', () => {
      const binding = { key: 'Enter', action: 'NAVIGATE_TO_SOURCE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('NAVIGATE_TO_SOURCE');
    });

    it('should handle Space key with action: TOGGLE_SECTION', () => {
      const binding = { key: 'Space', action: 'TOGGLE_SECTION' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('TOGGLE_SECTION');
    });

    it('should handle ArrowDown key with action: NAVIGATE_NEXT_ITEM', () => {
      const binding = { key: 'ArrowDown', action: 'NAVIGATE_NEXT_ITEM' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('NAVIGATE_NEXT_ITEM');
    });

    it('should handle ArrowUp key with action: NAVIGATE_PREV_ITEM', () => {
      const binding = { key: 'ArrowUp', action: 'NAVIGATE_PREV_ITEM' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('NAVIGATE_PREV_ITEM');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["COLLAPSE","EXPAND","COLLAPSE_LINKED","EXPAND_LINKED","EXPAND_UNLINKED","COLLAPSE_UNLINKED","LOAD","LOAD_COMPLETE","LOAD_ERROR","RETRY"]);
      const keyboardActions = ["FOCUS_NEXT","FOCUS_PREV","NAVIGATE_TO_SOURCE","TOGGLE_SECTION","NAVIGATE_NEXT_ITEM","NAVIGATE_PREV_ITEM"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "complementary"', () => {
      expect('complementary').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "collapseToggle"', () => {
      expect('collapseToggle').toBeTruthy();
    });

    it('should not use roving tabindex', () => {
      expect(false).toBe(false);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "complementary" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Backlinks" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on collapseToggle', () => {
      const ariaAttr = { part: 'collapseToggle', name: 'aria-expanded', value: "if state . panel = = expanded then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-controls on collapseToggle', () => {
      const ariaAttr = { part: 'collapseToggle', name: 'aria-controls', value: "panelContent" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on linkedSection', () => {
      const ariaAttr = { part: 'linkedSection', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on linkedSection', () => {
      const ariaAttr = { part: 'linkedSection', name: 'aria-label', value: "Linked mentions" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on linkedSection', () => {
      const ariaAttr = { part: 'linkedSection', name: 'aria-expanded', value: "if state . linkedSection = = expanded then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on linkedList', () => {
      const ariaAttr = { part: 'linkedList', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on linkedList', () => {
      const ariaAttr = { part: 'linkedList', name: 'aria-label', value: "Linked references" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on linkedItem', () => {
      const ariaAttr = { part: 'linkedItem', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on linkedItem', () => {
      const ariaAttr = { part: 'linkedItem', name: 'aria-label', value: "self . sourceTitle" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on unlinkedSection', () => {
      const ariaAttr = { part: 'unlinkedSection', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on unlinkedSection', () => {
      const ariaAttr = { part: 'unlinkedSection', name: 'aria-label', value: "Unlinked mentions" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on unlinkedSection', () => {
      const ariaAttr = { part: 'unlinkedSection', name: 'aria-expanded', value: "if state . unlinkedSection = = expanded then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on unlinkedList', () => {
      const ariaAttr = { part: 'unlinkedList', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on unlinkedList', () => {
      const ariaAttr = { part: 'unlinkedList', name: 'aria-label', value: "Unlinked references" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on unlinkedItem', () => {
      const ariaAttr = { part: 'unlinkedItem', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on unlinkedItem', () => {
      const ariaAttr = { part: 'unlinkedItem', name: 'aria-label', value: "self . sourceTitle" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on linkButton', () => {
      const ariaAttr = { part: 'linkButton', name: 'aria-label', value: "concat ( Link mention from  , self . sourceTitle )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "complementary" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "concat ( Backlinks to  , ? targetTitle )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-busy on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-busy', value: "if ? loading then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part count', () => {
      const connectAria = { part: 'count', attr: 'aria-label', value: "concat ( ? linkedReferences . length ,  linked,  , ? unlinkedMentions . length ,  unlinked )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part collapseToggle', () => {
      const connectAria = { part: 'collapseToggle', attr: 'aria-expanded', value: "if state . panel = = expanded then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-controls on connect part collapseToggle', () => {
      const connectAria = { part: 'collapseToggle', attr: 'aria-controls', value: "panelContent" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part collapseToggle', () => {
      const connectAria = { part: 'collapseToggle', attr: 'aria-label', value: "if state . panel = = expanded then Collapse backlinks else Expand backlinks" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part linkedSection', () => {
      const connectAria = { part: 'linkedSection', attr: 'role', value: "region" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part linkedSection', () => {
      const connectAria = { part: 'linkedSection', attr: 'aria-label', value: "Linked mentions" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part linkedSection', () => {
      const connectAria = { part: 'linkedSection', attr: 'aria-expanded', value: "if state . linkedSection = = expanded then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part linkedSectionCount', () => {
      const connectAria = { part: 'linkedSectionCount', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part linkedList', () => {
      const connectAria = { part: 'linkedList', attr: 'role', value: "list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part linkedList', () => {
      const connectAria = { part: 'linkedList', attr: 'aria-label', value: "Linked references" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part linkedItem', () => {
      const connectAria = { part: 'linkedItem', attr: 'role', value: "listitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part linkedItem', () => {
      const connectAria = { part: 'linkedItem', attr: 'aria-label', value: "concat ( Reference from  , self . sourceTitle )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part linkedItemHighlight', () => {
      const connectAria = { part: 'linkedItemHighlight', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part unlinkedSection', () => {
      const connectAria = { part: 'unlinkedSection', attr: 'role', value: "region" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part unlinkedSection', () => {
      const connectAria = { part: 'unlinkedSection', attr: 'aria-label', value: "Unlinked mentions" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part unlinkedSection', () => {
      const connectAria = { part: 'unlinkedSection', attr: 'aria-expanded', value: "if state . unlinkedSection = = expanded then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part unlinkedSectionCount', () => {
      const connectAria = { part: 'unlinkedSectionCount', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part unlinkedList', () => {
      const connectAria = { part: 'unlinkedList', attr: 'role', value: "list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part unlinkedList', () => {
      const connectAria = { part: 'unlinkedList', attr: 'aria-label', value: "Unlinked references" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part unlinkedItem', () => {
      const connectAria = { part: 'unlinkedItem', attr: 'role', value: "listitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part unlinkedItem', () => {
      const connectAria = { part: 'unlinkedItem', attr: 'aria-label', value: "concat ( Unlinked mention from  , self . sourceTitle )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part linkButton', () => {
      const connectAria = { part: 'linkButton', attr: 'aria-label', value: "concat ( Link mention from  , self . sourceTitle )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part emptyState', () => {
      const connectAria = { part: 'emptyState', attr: 'aria-hidden', value: "if ? linkedReferences . length > 0 then true else if ? unlinkedMentions . length > 0 then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "targetId" with type "String"', () => {
      const prop = { name: 'targetId', type: 'String' };
      expect(prop.name).toBe('targetId');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "targetTitle" with type "String"', () => {
      const prop = { name: 'targetTitle', type: 'String' };
      expect(prop.name).toBe('targetTitle');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "linkedReferences" with type "list LinkedRef"', () => {
      const prop = { name: 'linkedReferences', type: 'list LinkedRef', defaultValue: "[ ]" };
      expect(prop.name).toBe('linkedReferences');
      expect(prop.type).toBeTruthy();
    });

    it('should default "linkedReferences" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "unlinkedMentions" with type "list UnlinkedRef"', () => {
      const prop = { name: 'unlinkedMentions', type: 'list UnlinkedRef', defaultValue: "[ ]" };
      expect(prop.name).toBe('unlinkedMentions');
      expect(prop.type).toBeTruthy();
    });

    it('should default "unlinkedMentions" to "[ ]"', () => {
      expect("[ ]").toBe("[ ]");
    });

    it('should declare prop "loading" with type "Bool"', () => {
      const prop = { name: 'loading', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('loading');
      expect(prop.type).toBeTruthy();
    });

    it('should default "loading" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "showUnlinked" with type "Bool"', () => {
      const prop = { name: 'showUnlinked', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showUnlinked');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showUnlinked" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "contextChars" with type "Int"', () => {
      const prop = { name: 'contextChars', type: 'Int', defaultValue: "200" };
      expect(prop.name).toBe('contextChars');
      expect(prop.type).toBeTruthy();
    });

    it('should default "contextChars" to "200"', () => {
      expect("200").toBe("200");
    });

    it('should declare prop "onNavigate" with type "option Function"', () => {
      const prop = { name: 'onNavigate', type: 'option Function' };
      expect(prop.name).toBe('onNavigate');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "onLink" with type "option Function"', () => {
      const prop = { name: 'onLink', type: 'option Function' };
      expect(prop.name).toBe('onLink');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","header","title","count","collapseToggle","linkedSection","linkedSectionHeader","linkedSectionLabel","linkedSectionCount","linkedList","linkedItem","linkedItemBreadcrumb","linkedItemContext","linkedItemHighlight","unlinkedSection","unlinkedSectionHeader","unlinkedSectionLabel","unlinkedSectionCount","unlinkedList","unlinkedItem","unlinkedItemContext","linkButton","emptyState"];
      expect(parts.length).toBe(23);
      expect(parts).toContain('root');
      expect(parts).toContain('header');
      expect(parts).toContain('title');
      expect(parts).toContain('count');
      expect(parts).toContain('collapseToggle');
      expect(parts).toContain('linkedSection');
      expect(parts).toContain('linkedSectionHeader');
      expect(parts).toContain('linkedSectionLabel');
      expect(parts).toContain('linkedSectionCount');
      expect(parts).toContain('linkedList');
      expect(parts).toContain('linkedItem');
      expect(parts).toContain('linkedItemBreadcrumb');
      expect(parts).toContain('linkedItemContext');
      expect(parts).toContain('linkedItemHighlight');
      expect(parts).toContain('unlinkedSection');
      expect(parts).toContain('unlinkedSectionHeader');
      expect(parts).toContain('unlinkedSectionLabel');
      expect(parts).toContain('unlinkedSectionCount');
      expect(parts).toContain('unlinkedList');
      expect(parts).toContain('unlinkedItem');
      expect(parts).toContain('unlinkedItemContext');
      expect(parts).toContain('linkButton');
      expect(parts).toContain('emptyState');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // header: container
      expect('text').toBeTruthy(); // title: text
      expect('text').toBeTruthy(); // count: text
      expect('action').toBeTruthy(); // collapseToggle: action
      expect('container').toBeTruthy(); // linkedSection: container
      expect('container').toBeTruthy(); // linkedSectionHeader: container
      expect('text').toBeTruthy(); // linkedSectionLabel: text
      expect('text').toBeTruthy(); // linkedSectionCount: text
      expect('container').toBeTruthy(); // linkedList: container
      expect('container').toBeTruthy(); // linkedItem: container
      expect('widget').toBeTruthy(); // linkedItemBreadcrumb: widget
      expect('text').toBeTruthy(); // linkedItemContext: text
      expect('container').toBeTruthy(); // linkedItemHighlight: container
      expect('container').toBeTruthy(); // unlinkedSection: container
      expect('container').toBeTruthy(); // unlinkedSectionHeader: container
      expect('text').toBeTruthy(); // unlinkedSectionLabel: text
      expect('text').toBeTruthy(); // unlinkedSectionCount: text
      expect('container').toBeTruthy(); // unlinkedList: container
      expect('container').toBeTruthy(); // unlinkedItem: container
      expect('text').toBeTruthy(); // unlinkedItemContext: text
      expect('action').toBeTruthy(); // linkButton: action
      expect('container').toBeTruthy(); // emptyState: container
    });

  });

});
