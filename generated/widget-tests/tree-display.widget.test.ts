// Auto-generated by generate-widget-tests.ts from tree-display.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: TreeDisplay', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["expansion.allExpanded","load.idle","selection.unselected"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('expansion.allExpanded');
      expect(initialStates).toContain('load.idle');
      expect(initialStates).toContain('selection.unselected');
    });

    it('should transition from expansion.allExpanded to partiallyExpanded on COLLAPSE_NODE', () => {
      const transition = { from: 'expansion.allExpanded', event: 'COLLAPSE_NODE', target: 'partiallyExpanded' };
      expect(transition.from).toBe('expansion.allExpanded');
      expect(transition.event).toBe('COLLAPSE_NODE');
      expect(transition.target).toBe('partiallyExpanded');
      // Target state 'partiallyExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expansion.allExpanded to allCollapsed on COLLAPSE_ALL', () => {
      const transition = { from: 'expansion.allExpanded', event: 'COLLAPSE_ALL', target: 'allCollapsed' };
      expect(transition.from).toBe('expansion.allExpanded');
      expect(transition.event).toBe('COLLAPSE_ALL');
      expect(transition.target).toBe('allCollapsed');
      // Target state 'allCollapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expansion.partiallyExpanded to partiallyExpanded on EXPAND_NODE', () => {
      const transition = { from: 'expansion.partiallyExpanded', event: 'EXPAND_NODE', target: 'partiallyExpanded' };
      expect(transition.from).toBe('expansion.partiallyExpanded');
      expect(transition.event).toBe('EXPAND_NODE');
      expect(transition.target).toBe('partiallyExpanded');
      // Target state 'partiallyExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expansion.partiallyExpanded to partiallyExpanded on COLLAPSE_NODE', () => {
      const transition = { from: 'expansion.partiallyExpanded', event: 'COLLAPSE_NODE', target: 'partiallyExpanded' };
      expect(transition.from).toBe('expansion.partiallyExpanded');
      expect(transition.event).toBe('COLLAPSE_NODE');
      expect(transition.target).toBe('partiallyExpanded');
      // Target state 'partiallyExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expansion.partiallyExpanded to allExpanded on EXPAND_ALL', () => {
      const transition = { from: 'expansion.partiallyExpanded', event: 'EXPAND_ALL', target: 'allExpanded' };
      expect(transition.from).toBe('expansion.partiallyExpanded');
      expect(transition.event).toBe('EXPAND_ALL');
      expect(transition.target).toBe('allExpanded');
      // Target state 'allExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expansion.partiallyExpanded to allCollapsed on COLLAPSE_ALL', () => {
      const transition = { from: 'expansion.partiallyExpanded', event: 'COLLAPSE_ALL', target: 'allCollapsed' };
      expect(transition.from).toBe('expansion.partiallyExpanded');
      expect(transition.event).toBe('COLLAPSE_ALL');
      expect(transition.target).toBe('allCollapsed');
      // Target state 'allCollapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expansion.allCollapsed to partiallyExpanded on EXPAND_NODE', () => {
      const transition = { from: 'expansion.allCollapsed', event: 'EXPAND_NODE', target: 'partiallyExpanded' };
      expect(transition.from).toBe('expansion.allCollapsed');
      expect(transition.event).toBe('EXPAND_NODE');
      expect(transition.target).toBe('partiallyExpanded');
      // Target state 'partiallyExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from expansion.allCollapsed to allExpanded on EXPAND_ALL', () => {
      const transition = { from: 'expansion.allCollapsed', event: 'EXPAND_ALL', target: 'allExpanded' };
      expect(transition.from).toBe('expansion.allCollapsed');
      expect(transition.event).toBe('EXPAND_ALL');
      expect(transition.target).toBe('allExpanded');
      // Target state 'allExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from load.idle to loadingChildren on LOAD_CHILDREN', () => {
      const transition = { from: 'load.idle', event: 'LOAD_CHILDREN', target: 'loadingChildren' };
      expect(transition.from).toBe('load.idle');
      expect(transition.event).toBe('LOAD_CHILDREN');
      expect(transition.target).toBe('loadingChildren');
      // Target state 'loadingChildren' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from load.idle to empty on DATA_EMPTY', () => {
      const transition = { from: 'load.idle', event: 'DATA_EMPTY', target: 'empty' };
      expect(transition.from).toBe('load.idle');
      expect(transition.event).toBe('DATA_EMPTY');
      expect(transition.target).toBe('empty');
      // Target state 'empty' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from load.idle to idle on DATA_AVAILABLE', () => {
      const transition = { from: 'load.idle', event: 'DATA_AVAILABLE', target: 'idle' };
      expect(transition.from).toBe('load.idle');
      expect(transition.event).toBe('DATA_AVAILABLE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from load.loadingChildren to idle on CHILDREN_LOADED', () => {
      const transition = { from: 'load.loadingChildren', event: 'CHILDREN_LOADED', target: 'idle' };
      expect(transition.from).toBe('load.loadingChildren');
      expect(transition.event).toBe('CHILDREN_LOADED');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from load.loadingChildren to idle on CHILDREN_ERROR', () => {
      const transition = { from: 'load.loadingChildren', event: 'CHILDREN_ERROR', target: 'idle' };
      expect(transition.from).toBe('load.loadingChildren');
      expect(transition.event).toBe('CHILDREN_ERROR');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from load.empty to idle on DATA_AVAILABLE', () => {
      const transition = { from: 'load.empty', event: 'DATA_AVAILABLE', target: 'idle' };
      expect(transition.from).toBe('load.empty');
      expect(transition.event).toBe('DATA_AVAILABLE');
      expect(transition.target).toBe('idle');
      // Target state 'idle' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selection.unselected to selected on SELECT_NODE', () => {
      const transition = { from: 'selection.unselected', event: 'SELECT_NODE', target: 'selected' };
      expect(transition.from).toBe('selection.unselected');
      expect(transition.event).toBe('SELECT_NODE');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selection.selected to unselected on DESELECT_NODE', () => {
      const transition = { from: 'selection.selected', event: 'DESELECT_NODE', target: 'unselected' };
      expect(transition.from).toBe('selection.selected');
      expect(transition.event).toBe('DESELECT_NODE');
      expect(transition.target).toBe('unselected');
      // Target state 'unselected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from selection.selected to selected on SELECT_NODE', () => {
      const transition = { from: 'selection.selected', event: 'SELECT_NODE', target: 'selected' };
      expect(transition.from).toBe('selection.selected');
      expect(transition.event).toBe('SELECT_NODE');
      expect(transition.target).toBe('selected');
      // Target state 'selected' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"expansion.allExpanded","events":["COLLAPSE_NODE","COLLAPSE_ALL"]},{"name":"expansion.partiallyExpanded","events":["EXPAND_NODE","COLLAPSE_NODE","EXPAND_ALL","COLLAPSE_ALL"]},{"name":"expansion.allCollapsed","events":["EXPAND_NODE","EXPAND_ALL"]},{"name":"load.idle","events":["LOAD_CHILDREN","DATA_EMPTY","DATA_AVAILABLE"]},{"name":"load.loadingChildren","events":["CHILDREN_LOADED","CHILDREN_ERROR"]},{"name":"load.empty","events":["DATA_AVAILABLE"]},{"name":"selection.unselected","events":["SELECT_NODE"]},{"name":"selection.selected","events":["DESELECT_NODE","SELECT_NODE"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["expansion.allExpanded","expansion.partiallyExpanded","expansion.allCollapsed","load.idle","load.loadingChildren","load.empty","selection.unselected","selection.selected"]);
      const baseNames = new Set(["allExpanded","partiallyExpanded","allCollapsed","idle","loadingChildren","empty","unselected","selected"]);
      expect(validNames.has('partiallyExpanded') || baseNames.has('partiallyExpanded')).toBe(true);
      expect(validNames.has('allCollapsed') || baseNames.has('allCollapsed')).toBe(true);
      expect(validNames.has('partiallyExpanded') || baseNames.has('partiallyExpanded')).toBe(true);
      expect(validNames.has('partiallyExpanded') || baseNames.has('partiallyExpanded')).toBe(true);
      expect(validNames.has('allExpanded') || baseNames.has('allExpanded')).toBe(true);
      expect(validNames.has('allCollapsed') || baseNames.has('allCollapsed')).toBe(true);
      expect(validNames.has('partiallyExpanded') || baseNames.has('partiallyExpanded')).toBe(true);
      expect(validNames.has('allExpanded') || baseNames.has('allExpanded')).toBe(true);
      expect(validNames.has('loadingChildren') || baseNames.has('loadingChildren')).toBe(true);
      expect(validNames.has('empty') || baseNames.has('empty')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('idle') || baseNames.has('idle')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
      expect(validNames.has('unselected') || baseNames.has('unselected')).toBe(true);
      expect(validNames.has('selected') || baseNames.has('selected')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="root" to root', () => {
      const binding = {"part":"root","dataPart":"root"};
      expect(binding.dataPart).toBe('root');
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if state . load = = empty then empty else if ? loading then loading else state . expansion" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="toolbar" to toolbar', () => {
      const binding = {"part":"toolbar","dataPart":"toolbar"};
      expect(binding.dataPart).toBe('toolbar');
    });

    it('should bind data-part="expand-all-button" to expandAllButton', () => {
      const binding = {"part":"expandAllButton","dataPart":"expand-all-button"};
      expect(binding.dataPart).toBe('expand-all-button');
    });

    it('should bind onClick handler on expandAllButton', () => {
      const handler = { event: 'onClick', action: "send ( EXPAND_ALL )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on expandAllButton', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( EXPAND_ALL )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on expandAllButton', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( EXPAND_ALL )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="collapse-all-button" to collapseAllButton', () => {
      const binding = {"part":"collapseAllButton","dataPart":"collapse-all-button"};
      expect(binding.dataPart).toBe('collapse-all-button');
    });

    it('should bind onClick handler on collapseAllButton', () => {
      const handler = { event: 'onClick', action: "send ( COLLAPSE_ALL )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on collapseAllButton', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( COLLAPSE_ALL )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Space handler on collapseAllButton', () => {
      const handler = { event: 'onKeyDown-Space', action: "send ( COLLAPSE_ALL )" };
      expect(handler.event).toBe('onKeyDown-Space');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="node-count" to nodeCount', () => {
      const binding = {"part":"nodeCount","dataPart":"node-count"};
      expect(binding.dataPart).toBe('node-count');
    });

    it('should bind data-part="tree" to tree', () => {
      const binding = {"part":"tree","dataPart":"tree"};
      expect(binding.dataPart).toBe('tree');
    });

    it('should bind data-part="tree-node" to treeNode', () => {
      const binding = {"part":"treeNode","dataPart":"tree-node"};
      expect(binding.dataPart).toBe('tree-node');
    });

    it('should bind data-state on treeNode', () => {
      const attr = { name: 'data-state', value: "if state . selection = = selected then selected else if self . isExpanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-depth on treeNode', () => {
      const attr = { name: 'data-depth', value: "self . depth" };
      expect(attr.name).toBe('data-depth');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-has-children on treeNode', () => {
      const attr = { name: 'data-has-children', value: "if self . hasChildren then true else false" };
      expect(attr.name).toBe('data-has-children');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="node-content" to nodeContent', () => {
      const binding = {"part":"nodeContent","dataPart":"node-content"};
      expect(binding.dataPart).toBe('node-content');
    });

    it('should bind onClick handler on nodeContent', () => {
      const handler = { event: 'onClick', action: "send ( ACTIVATE_NODE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on nodeContent', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( ACTIVATE_NODE )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="expand-toggle" to expandToggle', () => {
      const binding = {"part":"expandToggle","dataPart":"expand-toggle"};
      expect(binding.dataPart).toBe('expand-toggle');
    });

    it('should bind data-state on expandToggle', () => {
      const attr = { name: 'data-state', value: "if self . isExpanded then expanded else collapsed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on expandToggle', () => {
      const handler = { event: 'onClick', action: "send ( EXPAND_OR_DESCEND )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown-Enter handler on expandToggle', () => {
      const handler = { event: 'onKeyDown-Enter', action: "send ( EXPAND_OR_DESCEND )" };
      expect(handler.event).toBe('onKeyDown-Enter');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="node-icon" to nodeIcon', () => {
      const binding = {"part":"nodeIcon","dataPart":"node-icon"};
      expect(binding.dataPart).toBe('node-icon');
    });

    it('should bind data-leaf on nodeIcon', () => {
      const attr = { name: 'data-leaf', value: "if self . hasChildren then false else true" };
      expect(attr.name).toBe('data-leaf');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="node-label" to nodeLabel', () => {
      const binding = {"part":"nodeLabel","dataPart":"node-label"};
      expect(binding.dataPart).toBe('node-label');
    });

    it('should bind data-part="node-meta" to nodeMeta', () => {
      const binding = {"part":"nodeMeta","dataPart":"node-meta"};
      expect(binding.dataPart).toBe('node-meta');
    });

    it('should bind data-part="child-count-badge" to childCountBadge', () => {
      const binding = {"part":"childCountBadge","dataPart":"child-count-badge"};
      expect(binding.dataPart).toBe('child-count-badge');
    });

    it('should bind data-part="children-container" to childrenContainer', () => {
      const binding = {"part":"childrenContainer","dataPart":"children-container"};
      expect(binding.dataPart).toBe('children-container');
    });

    it('should bind data-part="leaf-indicator" to leafIndicator', () => {
      const binding = {"part":"leafIndicator","dataPart":"leaf-indicator"};
      expect(binding.dataPart).toBe('leaf-indicator');
    });

    it('should bind data-part="empty-state" to emptyState', () => {
      const binding = {"part":"emptyState","dataPart":"empty-state"};
      expect(binding.dataPart).toBe('empty-state');
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","toolbar","expandAllButton","collapseAllButton","nodeCount","tree","treeNode","nodeContent","expandToggle","nodeIcon","nodeLabel","nodeMeta","childCountBadge","childrenContainer","leafIndicator","emptyState"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('toolbar')).toBe(true);
      expect(anatomyParts.has('expandAllButton')).toBe(true);
      expect(anatomyParts.has('collapseAllButton')).toBe(true);
      expect(anatomyParts.has('nodeCount')).toBe(true);
      expect(anatomyParts.has('tree')).toBe(true);
      expect(anatomyParts.has('treeNode')).toBe(true);
      expect(anatomyParts.has('nodeContent')).toBe(true);
      expect(anatomyParts.has('expandToggle')).toBe(true);
      expect(anatomyParts.has('nodeIcon')).toBe(true);
      expect(anatomyParts.has('nodeLabel')).toBe(true);
      expect(anatomyParts.has('nodeMeta')).toBe(true);
      expect(anatomyParts.has('childCountBadge')).toBe(true);
      expect(anatomyParts.has('childrenContainer')).toBe(true);
      expect(anatomyParts.has('leafIndicator')).toBe(true);
      expect(anatomyParts.has('emptyState')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle ArrowDown key with action: NAVIGATE_NEXT', () => {
      const binding = { key: 'ArrowDown', action: 'NAVIGATE_NEXT' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('NAVIGATE_NEXT');
    });

    it('should handle ArrowUp key with action: NAVIGATE_PREV', () => {
      const binding = { key: 'ArrowUp', action: 'NAVIGATE_PREV' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('NAVIGATE_PREV');
    });

    it('should handle ArrowRight key with action: EXPAND_OR_DESCEND', () => {
      const binding = { key: 'ArrowRight', action: 'EXPAND_OR_DESCEND' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('EXPAND_OR_DESCEND');
    });

    it('should handle ArrowLeft key with action: COLLAPSE_OR_ASCEND', () => {
      const binding = { key: 'ArrowLeft', action: 'COLLAPSE_OR_ASCEND' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('COLLAPSE_OR_ASCEND');
    });

    it('should handle Home key with action: NAVIGATE_FIRST', () => {
      const binding = { key: 'Home', action: 'NAVIGATE_FIRST' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('NAVIGATE_FIRST');
    });

    it('should handle End key with action: NAVIGATE_LAST', () => {
      const binding = { key: 'End', action: 'NAVIGATE_LAST' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('NAVIGATE_LAST');
    });

    it('should handle Enter key with action: ACTIVATE_NODE', () => {
      const binding = { key: 'Enter', action: 'ACTIVATE_NODE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('ACTIVATE_NODE');
    });

    it('should handle Space key with action: SELECT_NODE', () => {
      const binding = { key: 'Space', action: 'SELECT_NODE' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('SELECT_NODE');
    });

    it('should handle Escape key with action: DESELECT_NODE', () => {
      const binding = { key: 'Escape', action: 'DESELECT_NODE' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('DESELECT_NODE');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["COLLAPSE_NODE","COLLAPSE_ALL","EXPAND_NODE","EXPAND_ALL","LOAD_CHILDREN","DATA_EMPTY","DATA_AVAILABLE","CHILDREN_LOADED","CHILDREN_ERROR","SELECT_NODE","DESELECT_NODE"]);
      const keyboardActions = ["NAVIGATE_NEXT","NAVIGATE_PREV","EXPAND_OR_DESCEND","COLLAPSE_OR_ASCEND","NAVIGATE_FIRST","NAVIGATE_LAST","ACTIVATE_NODE","SELECT_NODE","DESELECT_NODE"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "tree"', () => {
      expect('tree').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "treeNode"', () => {
      expect('treeNode').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "tree" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "? ariaLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-busy on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-busy', value: "if state . load = = loadingChildren then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-multiselectable on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-multiselectable', value: "false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on treeNode', () => {
      const ariaAttr = { part: 'treeNode', name: 'role', value: "treeitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on treeNode', () => {
      const ariaAttr = { part: 'treeNode', name: 'aria-expanded', value: "if self . hasChildren then ( if self . isExpanded then true else false ) else undefined" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-selected on treeNode', () => {
      const ariaAttr = { part: 'treeNode', name: 'aria-selected', value: "if state . selection = = selected then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-level on treeNode', () => {
      const ariaAttr = { part: 'treeNode', name: 'aria-level', value: "self . depth" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-setsize on treeNode', () => {
      const ariaAttr = { part: 'treeNode', name: 'aria-setsize', value: "self . siblingCount" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-posinset on treeNode', () => {
      const ariaAttr = { part: 'treeNode', name: 'aria-posinset', value: "self . positionInParent" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on childrenContainer', () => {
      const ariaAttr = { part: 'childrenContainer', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on childrenContainer', () => {
      const ariaAttr = { part: 'childrenContainer', name: 'aria-label', value: "concat ( self . label ,  children )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on expandToggle', () => {
      const ariaAttr = { part: 'expandToggle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on expandToggle', () => {
      const ariaAttr = { part: 'expandToggle', name: 'aria-label', value: "if self . isExpanded then Collapse else Expand" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind tabindex on expandToggle', () => {
      const ariaAttr = { part: 'expandToggle', name: 'tabindex', value: "-1" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set role on connect part root', () => {
      const connectAria = { part: 'root', attr: 'role', value: "tree" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "? ariaLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-busy on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-busy', value: "if ? loading then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part toolbar', () => {
      const connectAria = { part: 'toolbar', attr: 'aria-hidden', value: "false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part expandAllButton', () => {
      const connectAria = { part: 'expandAllButton', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part expandAllButton', () => {
      const connectAria = { part: 'expandAllButton', attr: 'aria-label', value: "Expand all nodes" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part collapseAllButton', () => {
      const connectAria = { part: 'collapseAllButton', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part collapseAllButton', () => {
      const connectAria = { part: 'collapseAllButton', attr: 'aria-label', value: "Collapse all nodes" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-live on connect part nodeCount', () => {
      const connectAria = { part: 'nodeCount', attr: 'aria-live', value: "polite" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part treeNode', () => {
      const connectAria = { part: 'treeNode', attr: 'role', value: "treeitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part treeNode', () => {
      const connectAria = { part: 'treeNode', attr: 'aria-expanded', value: "if self . hasChildren then ( if self . isExpanded then true else false ) else undefined" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-selected on connect part treeNode', () => {
      const connectAria = { part: 'treeNode', attr: 'aria-selected', value: "if ? selectable then ( if state . selection = = selected then true else false ) else undefined" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-level on connect part treeNode', () => {
      const connectAria = { part: 'treeNode', attr: 'aria-level', value: "self . depth" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-setsize on connect part treeNode', () => {
      const connectAria = { part: 'treeNode', attr: 'aria-setsize', value: "self . siblingCount" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-posinset on connect part treeNode', () => {
      const connectAria = { part: 'treeNode', attr: 'aria-posinset', value: "self . positionInParent" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part nodeContent', () => {
      const connectAria = { part: 'nodeContent', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part expandToggle', () => {
      const connectAria = { part: 'expandToggle', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part expandToggle', () => {
      const connectAria = { part: 'expandToggle', attr: 'aria-label', value: "if self . isExpanded then Collapse else Expand" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part expandToggle', () => {
      const connectAria = { part: 'expandToggle', attr: 'aria-hidden', value: "if self . hasChildren then false else true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part nodeIcon', () => {
      const connectAria = { part: 'nodeIcon', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part nodeMeta', () => {
      const connectAria = { part: 'nodeMeta', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part childCountBadge', () => {
      const connectAria = { part: 'childCountBadge', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part childrenContainer', () => {
      const connectAria = { part: 'childrenContainer', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-label on connect part childrenContainer', () => {
      const connectAria = { part: 'childrenContainer', attr: 'aria-label', value: "concat ( self . label ,  children )" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part leafIndicator', () => {
      const connectAria = { part: 'leafIndicator', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-live on connect part emptyState', () => {
      const connectAria = { part: 'emptyState', attr: 'aria-live', value: "polite" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "items" with type "list Object"', () => {
      const prop = { name: 'items', type: 'list Object' };
      expect(prop.name).toBe('items');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "parentField" with type "String"', () => {
      const prop = { name: 'parentField', type: 'String', defaultValue: "parentId" };
      expect(prop.name).toBe('parentField');
      expect(prop.type).toBeTruthy();
    });

    it('should default "parentField" to "parentId"', () => {
      expect("parentId").toBe("parentId");
    });

    it('should declare prop "idField" with type "String"', () => {
      const prop = { name: 'idField', type: 'String', defaultValue: "id" };
      expect(prop.name).toBe('idField');
      expect(prop.type).toBeTruthy();
    });

    it('should default "idField" to "id"', () => {
      expect("id").toBe("id");
    });

    it('should declare prop "labelField" with type "String"', () => {
      const prop = { name: 'labelField', type: 'String', defaultValue: "name" };
      expect(prop.name).toBe('labelField');
      expect(prop.type).toBeTruthy();
    });

    it('should default "labelField" to "name"', () => {
      expect("name").toBe("name");
    });

    it('should declare prop "iconField" with type "option String"', () => {
      const prop = { name: 'iconField', type: 'option String' };
      expect(prop.name).toBe('iconField');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "defaultExpanded" with type "Bool"', () => {
      const prop = { name: 'defaultExpanded', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('defaultExpanded');
      expect(prop.type).toBeTruthy();
    });

    it('should default "defaultExpanded" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "selectable" with type "Bool"', () => {
      const prop = { name: 'selectable', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('selectable');
      expect(prop.type).toBeTruthy();
    });

    it('should default "selectable" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "ariaLabel" with type "String"', () => {
      const prop = { name: 'ariaLabel', type: 'String', defaultValue: "Tree" };
      expect(prop.name).toBe('ariaLabel');
      expect(prop.type).toBeTruthy();
    });

    it('should default "ariaLabel" to "Tree"', () => {
      expect("Tree").toBe("Tree");
    });

    it('should declare prop "emptyMessage" with type "String"', () => {
      const prop = { name: 'emptyMessage', type: 'String', defaultValue: "No data to display" };
      expect(prop.name).toBe('emptyMessage');
      expect(prop.type).toBeTruthy();
    });

    it('should default "emptyMessage" to "No data to display"', () => {
      expect("No data to display").toBe("No data to display");
    });

    it('should declare prop "loading" with type "Bool"', () => {
      const prop = { name: 'loading', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('loading');
      expect(prop.type).toBeTruthy();
    });

    it('should default "loading" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "onItemClick" with type "option String"', () => {
      const prop = { name: 'onItemClick', type: 'option String' };
      expect(prop.name).toBe('onItemClick');
      expect(prop.type).toBeTruthy();
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","toolbar","expandAllButton","collapseAllButton","nodeCount","tree","treeNode","nodeContent","expandToggle","nodeIcon","nodeLabel","nodeMeta","childCountBadge","childrenContainer","leafIndicator","emptyState"];
      expect(parts.length).toBe(16);
      expect(parts).toContain('root');
      expect(parts).toContain('toolbar');
      expect(parts).toContain('expandAllButton');
      expect(parts).toContain('collapseAllButton');
      expect(parts).toContain('nodeCount');
      expect(parts).toContain('tree');
      expect(parts).toContain('treeNode');
      expect(parts).toContain('nodeContent');
      expect(parts).toContain('expandToggle');
      expect(parts).toContain('nodeIcon');
      expect(parts).toContain('nodeLabel');
      expect(parts).toContain('nodeMeta');
      expect(parts).toContain('childCountBadge');
      expect(parts).toContain('childrenContainer');
      expect(parts).toContain('leafIndicator');
      expect(parts).toContain('emptyState');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // toolbar: container
      expect('action').toBeTruthy(); // expandAllButton: action
      expect('action').toBeTruthy(); // collapseAllButton: action
      expect('text').toBeTruthy(); // nodeCount: text
      expect('container').toBeTruthy(); // tree: container
      expect('container').toBeTruthy(); // treeNode: container
      expect('action').toBeTruthy(); // nodeContent: action
      expect('action').toBeTruthy(); // expandToggle: action
      expect('presentation').toBeTruthy(); // nodeIcon: presentation
      expect('text').toBeTruthy(); // nodeLabel: text
      expect('text').toBeTruthy(); // nodeMeta: text
      expect('text').toBeTruthy(); // childCountBadge: text
      expect('container').toBeTruthy(); // childrenContainer: container
      expect('presentation').toBeTruthy(); // leafIndicator: presentation
      expect('container').toBeTruthy(); // emptyState: container
    });

  });

});
