// Auto-generated by generate-widget-tests.ts from menu.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: Menu', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["menu.closed","submenu.closed","highlight.none"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('menu.closed');
      expect(initialStates).toContain('submenu.closed');
      expect(initialStates).toContain('highlight.none');
    });

    it('should transition from menu.closed to open on OPEN', () => {
      const transition = { from: 'menu.closed', event: 'OPEN', target: 'open' };
      expect(transition.from).toBe('menu.closed');
      expect(transition.event).toBe('OPEN');
      expect(transition.target).toBe('open');
      // Target state 'open' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from menu.open to closed on CLOSE', () => {
      const transition = { from: 'menu.open', event: 'CLOSE', target: 'closed' };
      expect(transition.from).toBe('menu.open');
      expect(transition.event).toBe('CLOSE');
      expect(transition.target).toBe('closed');
      // Target state 'closed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from menu.open to closed on SELECT', () => {
      const transition = { from: 'menu.open', event: 'SELECT', target: 'closed' };
      expect(transition.from).toBe('menu.open');
      expect(transition.event).toBe('SELECT');
      expect(transition.target).toBe('closed');
      // Target state 'closed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from submenu.closed to open on SUBMENU_OPEN', () => {
      const transition = { from: 'submenu.closed', event: 'SUBMENU_OPEN', target: 'open' };
      expect(transition.from).toBe('submenu.closed');
      expect(transition.event).toBe('SUBMENU_OPEN');
      expect(transition.target).toBe('open');
      // Target state 'open' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from submenu.open to closed on SUBMENU_CLOSE', () => {
      const transition = { from: 'submenu.open', event: 'SUBMENU_CLOSE', target: 'closed' };
      expect(transition.from).toBe('submenu.open');
      expect(transition.event).toBe('SUBMENU_CLOSE');
      expect(transition.target).toBe('closed');
      // Target state 'closed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from submenu.open to closed on CLOSE', () => {
      const transition = { from: 'submenu.open', event: 'CLOSE', target: 'closed' };
      expect(transition.from).toBe('submenu.open');
      expect(transition.event).toBe('CLOSE');
      expect(transition.target).toBe('closed');
      // Target state 'closed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from highlight.none to highlighted on HIGHLIGHT', () => {
      const transition = { from: 'highlight.none', event: 'HIGHLIGHT', target: 'highlighted' };
      expect(transition.from).toBe('highlight.none');
      expect(transition.event).toBe('HIGHLIGHT');
      expect(transition.target).toBe('highlighted');
      // Target state 'highlighted' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from highlight.highlighted to none on UNHIGHLIGHT', () => {
      const transition = { from: 'highlight.highlighted', event: 'UNHIGHLIGHT', target: 'none' };
      expect(transition.from).toBe('highlight.highlighted');
      expect(transition.event).toBe('UNHIGHLIGHT');
      expect(transition.target).toBe('none');
      // Target state 'none' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from highlight.highlighted to highlighted on HIGHLIGHT', () => {
      const transition = { from: 'highlight.highlighted', event: 'HIGHLIGHT', target: 'highlighted' };
      expect(transition.from).toBe('highlight.highlighted');
      expect(transition.event).toBe('HIGHLIGHT');
      expect(transition.target).toBe('highlighted');
      // Target state 'highlighted' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"menu.closed","events":["OPEN"]},{"name":"menu.open","events":["CLOSE","SELECT"]},{"name":"submenu.closed","events":["SUBMENU_OPEN"]},{"name":"submenu.open","events":["SUBMENU_CLOSE","CLOSE"]},{"name":"highlight.none","events":["HIGHLIGHT"]},{"name":"highlight.highlighted","events":["UNHIGHLIGHT","HIGHLIGHT"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["menu.closed","menu.open","submenu.closed","submenu.open","highlight.none","highlight.highlighted"]);
      const baseNames = new Set(["closed","open","none","highlighted"]);
      expect(validNames.has('open') || baseNames.has('open')).toBe(true);
      expect(validNames.has('closed') || baseNames.has('closed')).toBe(true);
      expect(validNames.has('closed') || baseNames.has('closed')).toBe(true);
      expect(validNames.has('open') || baseNames.has('open')).toBe(true);
      expect(validNames.has('closed') || baseNames.has('closed')).toBe(true);
      expect(validNames.has('closed') || baseNames.has('closed')).toBe(true);
      expect(validNames.has('highlighted') || baseNames.has('highlighted')).toBe(true);
      expect(validNames.has('none') || baseNames.has('none')).toBe(true);
      expect(validNames.has('highlighted') || baseNames.has('highlighted')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "if state . menu = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on trigger', () => {
      const attr = { name: 'data-state', value: "if state . menu = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on trigger', () => {
      const handler = { event: 'onClick', action: "send ( if state . menu = = closed then OPEN else CLOSE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown handler on trigger', () => {
      const handler = { event: 'onKeyDown', action: "send ( OPEN )" };
      expect(handler.event).toBe('onKeyDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-placement on positioner', () => {
      const attr = { name: 'data-placement', value: "? placement" };
      expect(attr.name).toBe('data-placement');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on positioner', () => {
      const attr = { name: 'data-state', value: "if state . menu = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-state on content', () => {
      const attr = { name: 'data-state', value: "if state . menu = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onKeyDown handler on content', () => {
      const handler = { event: 'onKeyDown', action: "send ( NAVIGATE )" };
      expect(handler.event).toBe('onKeyDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-highlighted on item', () => {
      const attr = { name: 'data-highlighted', value: "if state . highlight = = highlighted then true else false" };
      expect(attr.name).toBe('data-highlighted');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on item', () => {
      const handler = { event: 'onClick', action: "send ( ACTIVATE )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerEnter handler on item', () => {
      const handler = { event: 'onPointerEnter', action: "send ( HIGHLIGHT )" };
      expect(handler.event).toBe('onPointerEnter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onPointerLeave handler on item', () => {
      const handler = { event: 'onPointerLeave', action: "send ( UNHIGHLIGHT )" };
      expect(handler.event).toBe('onPointerLeave');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="item-icon" to itemIcon', () => {
      const binding = {"part":"itemIcon","dataPart":"item-icon"};
      expect(binding.dataPart).toBe('item-icon');
    });

    it('should bind data-part="item-label" to itemLabel', () => {
      const binding = {"part":"itemLabel","dataPart":"item-label"};
      expect(binding.dataPart).toBe('item-label');
    });

    it('should bind data-part="item-shortcut" to itemShortcut', () => {
      const binding = {"part":"itemShortcut","dataPart":"item-shortcut"};
      expect(binding.dataPart).toBe('item-shortcut');
    });

    it('should bind data-part="group-label" to groupLabel', () => {
      const binding = {"part":"groupLabel","dataPart":"group-label"};
      expect(binding.dataPart).toBe('group-label');
    });

    it('should bind data-state on submenuTrigger', () => {
      const attr = { name: 'data-state', value: "if state . submenu = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind onPointerEnter handler on submenuTrigger', () => {
      const handler = { event: 'onPointerEnter', action: "send ( SUBMENU_OPEN )" };
      expect(handler.event).toBe('onPointerEnter');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown handler on submenuTrigger', () => {
      const handler = { event: 'onKeyDown', action: "send ( SUBMENU_OPEN )" };
      expect(handler.event).toBe('onKeyDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-state on submenuContent', () => {
      const attr = { name: 'data-state', value: "if state . submenu = = open then open else closed" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","trigger","positioner","content","item","itemIcon","itemLabel","itemShortcut","separator","group","groupLabel","submenuTrigger","submenuContent"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('trigger')).toBe(true);
      expect(anatomyParts.has('positioner')).toBe(true);
      expect(anatomyParts.has('content')).toBe(true);
      expect(anatomyParts.has('item')).toBe(true);
      expect(anatomyParts.has('itemIcon')).toBe(true);
      expect(anatomyParts.has('itemLabel')).toBe(true);
      expect(anatomyParts.has('itemShortcut')).toBe(true);
      expect(anatomyParts.has('separator')).toBe(true);
      expect(anatomyParts.has('group')).toBe(true);
      expect(anatomyParts.has('groupLabel')).toBe(true);
      expect(anatomyParts.has('submenuTrigger')).toBe(true);
      expect(anatomyParts.has('submenuContent')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: ACTIVATE', () => {
      const binding = { key: 'Enter', action: 'ACTIVATE' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('ACTIVATE');
    });

    it('should handle Space key with action: ACTIVATE', () => {
      const binding = { key: 'Space', action: 'ACTIVATE' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('ACTIVATE');
    });

    it('should handle ArrowDown key with action: NAVIGATE_NEXT', () => {
      const binding = { key: 'ArrowDown', action: 'NAVIGATE_NEXT' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('NAVIGATE_NEXT');
    });

    it('should handle ArrowUp key with action: NAVIGATE_PREV', () => {
      const binding = { key: 'ArrowUp', action: 'NAVIGATE_PREV' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('NAVIGATE_PREV');
    });

    it('should handle ArrowRight key with action: SUBMENU_OPEN', () => {
      const binding = { key: 'ArrowRight', action: 'SUBMENU_OPEN' };
      expect(binding.key).toBe('ArrowRight');
      expect(binding.action).toBe('SUBMENU_OPEN');
    });

    it('should handle ArrowLeft key with action: SUBMENU_CLOSE', () => {
      const binding = { key: 'ArrowLeft', action: 'SUBMENU_CLOSE' };
      expect(binding.key).toBe('ArrowLeft');
      expect(binding.action).toBe('SUBMENU_CLOSE');
    });

    it('should handle Home key with action: NAVIGATE_FIRST', () => {
      const binding = { key: 'Home', action: 'NAVIGATE_FIRST' };
      expect(binding.key).toBe('Home');
      expect(binding.action).toBe('NAVIGATE_FIRST');
    });

    it('should handle End key with action: NAVIGATE_LAST', () => {
      const binding = { key: 'End', action: 'NAVIGATE_LAST' };
      expect(binding.key).toBe('End');
      expect(binding.action).toBe('NAVIGATE_LAST');
    });

    it('should handle Escape key with action: CLOSE', () => {
      const binding = { key: 'Escape', action: 'CLOSE' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('CLOSE');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["OPEN","CLOSE","SELECT","SUBMENU_OPEN","SUBMENU_CLOSE","HIGHLIGHT","UNHIGHLIGHT"]);
      const keyboardActions = ["ACTIVATE","ACTIVATE","NAVIGATE_NEXT","NAVIGATE_PREV","SUBMENU_OPEN","SUBMENU_CLOSE","NAVIGATE_FIRST","NAVIGATE_LAST","CLOSE"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "menu"', () => {
      expect('menu').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "first"', () => {
      expect('first').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind aria-haspopup on trigger', () => {
      const ariaAttr = { part: 'trigger', name: 'aria-haspopup', value: "menu" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on trigger', () => {
      const ariaAttr = { part: 'trigger', name: 'aria-expanded', value: "if state . menu = = open then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-controls on trigger', () => {
      const ariaAttr = { part: 'trigger', name: 'aria-controls', value: "content" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on content', () => {
      const ariaAttr = { part: 'content', name: 'role', value: "menu" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-labelledby on content', () => {
      const ariaAttr = { part: 'content', name: 'aria-labelledby', value: "trigger" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on item', () => {
      const ariaAttr = { part: 'item', name: 'role', value: "menuitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind tabindex on item', () => {
      const ariaAttr = { part: 'item', name: 'tabindex', value: "-1" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on group', () => {
      const ariaAttr = { part: 'group', name: 'role', value: "group" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-labelledby on group', () => {
      const ariaAttr = { part: 'group', name: 'aria-labelledby', value: "groupLabel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on groupLabel', () => {
      const ariaAttr = { part: 'groupLabel', name: 'role', value: "presentation" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on submenuTrigger', () => {
      const ariaAttr = { part: 'submenuTrigger', name: 'role', value: "menuitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-haspopup on submenuTrigger', () => {
      const ariaAttr = { part: 'submenuTrigger', name: 'aria-haspopup', value: "menu" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on submenuTrigger', () => {
      const ariaAttr = { part: 'submenuTrigger', name: 'aria-expanded', value: "if state . submenu = = open then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-controls on submenuTrigger', () => {
      const ariaAttr = { part: 'submenuTrigger', name: 'aria-controls', value: "submenuContent" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on submenuContent', () => {
      const ariaAttr = { part: 'submenuContent', name: 'role', value: "menu" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-labelledby on submenuContent', () => {
      const ariaAttr = { part: 'submenuContent', name: 'aria-labelledby', value: "submenuTrigger" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on separator', () => {
      const ariaAttr = { part: 'separator', name: 'role', value: "separator" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set aria-haspopup on connect part trigger', () => {
      const connectAria = { part: 'trigger', attr: 'aria-haspopup', value: "menu" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part trigger', () => {
      const connectAria = { part: 'trigger', attr: 'aria-expanded', value: "if state . menu = = open then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-controls on connect part trigger', () => {
      const connectAria = { part: 'trigger', attr: 'aria-controls', value: "content" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part content', () => {
      const connectAria = { part: 'content', attr: 'role', value: "menu" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-labelledby on connect part content', () => {
      const connectAria = { part: 'content', attr: 'aria-labelledby', value: "trigger" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part item', () => {
      const connectAria = { part: 'item', attr: 'role', value: "menuitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part itemIcon', () => {
      const connectAria = { part: 'itemIcon', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part itemShortcut', () => {
      const connectAria = { part: 'itemShortcut', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part separator', () => {
      const connectAria = { part: 'separator', attr: 'role', value: "separator" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-hidden on connect part separator', () => {
      const connectAria = { part: 'separator', attr: 'aria-hidden', value: "true" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part group', () => {
      const connectAria = { part: 'group', attr: 'role', value: "group" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-labelledby on connect part group', () => {
      const connectAria = { part: 'group', attr: 'aria-labelledby', value: "groupLabel" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part submenuTrigger', () => {
      const connectAria = { part: 'submenuTrigger', attr: 'role', value: "menuitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-haspopup on connect part submenuTrigger', () => {
      const connectAria = { part: 'submenuTrigger', attr: 'aria-haspopup', value: "menu" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part submenuTrigger', () => {
      const connectAria = { part: 'submenuTrigger', attr: 'aria-expanded', value: "if state . submenu = = open then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-controls on connect part submenuTrigger', () => {
      const connectAria = { part: 'submenuTrigger', attr: 'aria-controls', value: "submenuContent" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part submenuContent', () => {
      const connectAria = { part: 'submenuContent', attr: 'role', value: "menu" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-labelledby on connect part submenuContent', () => {
      const connectAria = { part: 'submenuContent', attr: 'aria-labelledby', value: "submenuTrigger" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "items" with type "list MenuItem"', () => {
      const prop = { name: 'items', type: 'list MenuItem' };
      expect(prop.name).toBe('items');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "placement" with type "String"', () => {
      const prop = { name: 'placement', type: 'String', defaultValue: "bottom-start" };
      expect(prop.name).toBe('placement');
      expect(prop.type).toBeTruthy();
    });

    it('should default "placement" to "bottom-start"', () => {
      expect("bottom-start").toBe("bottom-start");
    });

    it('should declare prop "open" with type "Bool"', () => {
      const prop = { name: 'open', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('open');
      expect(prop.type).toBeTruthy();
    });

    it('should default "open" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "closeOnSelect" with type "Bool"', () => {
      const prop = { name: 'closeOnSelect', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('closeOnSelect');
      expect(prop.type).toBeTruthy();
    });

    it('should default "closeOnSelect" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "loop" with type "Bool"', () => {
      const prop = { name: 'loop', type: 'Bool', defaultValue: "false" };
      expect(prop.name).toBe('loop');
      expect(prop.type).toBeTruthy();
    });

    it('should default "loop" to "false"', () => {
      expect("false").toBe("false");
    });

    it('should declare prop "typeahead" with type "Bool"', () => {
      const prop = { name: 'typeahead', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('typeahead');
      expect(prop.type).toBeTruthy();
    });

    it('should default "typeahead" to "true"', () => {
      expect("true").toBe("true");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","trigger","positioner","content","item","itemIcon","itemLabel","itemShortcut","separator","group","groupLabel","submenuTrigger","submenuContent"];
      expect(parts.length).toBe(13);
      expect(parts).toContain('root');
      expect(parts).toContain('trigger');
      expect(parts).toContain('positioner');
      expect(parts).toContain('content');
      expect(parts).toContain('item');
      expect(parts).toContain('itemIcon');
      expect(parts).toContain('itemLabel');
      expect(parts).toContain('itemShortcut');
      expect(parts).toContain('separator');
      expect(parts).toContain('group');
      expect(parts).toContain('groupLabel');
      expect(parts).toContain('submenuTrigger');
      expect(parts).toContain('submenuContent');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('action').toBeTruthy(); // trigger: action
      expect('container').toBeTruthy(); // positioner: container
      expect('container').toBeTruthy(); // content: container
      expect('action').toBeTruthy(); // item: action
      expect('container').toBeTruthy(); // itemIcon: container
      expect('text').toBeTruthy(); // itemLabel: text
      expect('text').toBeTruthy(); // itemShortcut: text
      expect('container').toBeTruthy(); // separator: container
      expect('container').toBeTruthy(); // group: container
      expect('text').toBeTruthy(); // groupLabel: text
      expect('action').toBeTruthy(); // submenuTrigger: action
      expect('container').toBeTruthy(); // submenuContent: container
    });

  });

});
