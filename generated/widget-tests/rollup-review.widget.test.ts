// Auto-generated by generate-widget-tests.ts from rollup-review.widget
// Do NOT edit unless you add the @clef-patched marker for 3-way merge preservation.
//
// Tests cover: FSM transitions, connect bindings, keyboard bindings, ARIA attributes, prop defaults

import { describe, it, expect } from 'vitest';

describe('Widget: RollupReview', () => {
  describe('FSM Transitions', () => {
    it('should have a defined initial state', () => {
      const initialStates = ["allCollapsed"];
      expect(initialStates.length).toBeGreaterThan(0);
      expect(initialStates).toContain('allCollapsed');
    });

    it('should transition from allCollapsed to stepExpanded on EXPAND_STEP', () => {
      const transition = { from: 'allCollapsed', event: 'EXPAND_STEP', target: 'stepExpanded' };
      expect(transition.from).toBe('allCollapsed');
      expect(transition.event).toBe('EXPAND_STEP');
      expect(transition.target).toBe('stepExpanded');
      // Target state 'stepExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from stepExpanded to allCollapsed on COLLAPSE_STEP', () => {
      const transition = { from: 'stepExpanded', event: 'COLLAPSE_STEP', target: 'allCollapsed' };
      expect(transition.from).toBe('stepExpanded');
      expect(transition.event).toBe('COLLAPSE_STEP');
      expect(transition.target).toBe('allCollapsed');
      // Target state 'allCollapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from stepExpanded to stepExpanded on EXPAND_STEP', () => {
      const transition = { from: 'stepExpanded', event: 'EXPAND_STEP', target: 'stepExpanded' };
      expect(transition.from).toBe('stepExpanded');
      expect(transition.event).toBe('EXPAND_STEP');
      expect(transition.target).toBe('stepExpanded');
      // Target state 'stepExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from stepExpanded to evidenceOpen on EXPAND_EVIDENCE', () => {
      const transition = { from: 'stepExpanded', event: 'EXPAND_EVIDENCE', target: 'evidenceOpen' };
      expect(transition.from).toBe('stepExpanded');
      expect(transition.event).toBe('EXPAND_EVIDENCE');
      expect(transition.target).toBe('evidenceOpen');
      // Target state 'evidenceOpen' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from stepExpanded to actionPending on TRIGGER_ACTION', () => {
      const transition = { from: 'stepExpanded', event: 'TRIGGER_ACTION', target: 'actionPending' };
      expect(transition.from).toBe('stepExpanded');
      expect(transition.event).toBe('TRIGGER_ACTION');
      expect(transition.target).toBe('actionPending');
      // Target state 'actionPending' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from evidenceOpen to stepExpanded on COLLAPSE_EVIDENCE', () => {
      const transition = { from: 'evidenceOpen', event: 'COLLAPSE_EVIDENCE', target: 'stepExpanded' };
      expect(transition.from).toBe('evidenceOpen');
      expect(transition.event).toBe('COLLAPSE_EVIDENCE');
      expect(transition.target).toBe('stepExpanded');
      // Target state 'stepExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from evidenceOpen to allCollapsed on COLLAPSE_STEP', () => {
      const transition = { from: 'evidenceOpen', event: 'COLLAPSE_STEP', target: 'allCollapsed' };
      expect(transition.from).toBe('evidenceOpen');
      expect(transition.event).toBe('COLLAPSE_STEP');
      expect(transition.target).toBe('allCollapsed');
      // Target state 'allCollapsed' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from evidenceOpen to actionPending on TRIGGER_ACTION', () => {
      const transition = { from: 'evidenceOpen', event: 'TRIGGER_ACTION', target: 'actionPending' };
      expect(transition.from).toBe('evidenceOpen');
      expect(transition.event).toBe('TRIGGER_ACTION');
      expect(transition.target).toBe('actionPending');
      // Target state 'actionPending' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from actionPending to stepExpanded on ACTION_COMPLETE', () => {
      const transition = { from: 'actionPending', event: 'ACTION_COMPLETE', target: 'stepExpanded' };
      expect(transition.from).toBe('actionPending');
      expect(transition.event).toBe('ACTION_COMPLETE');
      expect(transition.target).toBe('stepExpanded');
      // Target state 'stepExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from actionPending to stepExpanded on ACTION_ERROR', () => {
      const transition = { from: 'actionPending', event: 'ACTION_ERROR', target: 'stepExpanded' };
      expect(transition.from).toBe('actionPending');
      expect(transition.event).toBe('ACTION_ERROR');
      expect(transition.target).toBe('stepExpanded');
      // Target state 'stepExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should transition from actionPending to stepExpanded on CANCEL', () => {
      const transition = { from: 'actionPending', event: 'CANCEL', target: 'stepExpanded' };
      expect(transition.from).toBe('actionPending');
      expect(transition.event).toBe('CANCEL');
      expect(transition.target).toBe('stepExpanded');
      // Target state 'stepExpanded' exists in the FSM
      expect(true).toBe(true);
    });

    it('should have deterministic transitions (no duplicate events per state)', () => {
      const states = [{"name":"allCollapsed","events":["EXPAND_STEP"]},{"name":"stepExpanded","events":["COLLAPSE_STEP","EXPAND_STEP","EXPAND_EVIDENCE","TRIGGER_ACTION"]},{"name":"evidenceOpen","events":["COLLAPSE_EVIDENCE","COLLAPSE_STEP","TRIGGER_ACTION"]},{"name":"actionPending","events":["ACTION_COMPLETE","ACTION_ERROR","CANCEL"]}];
      for (const state of states) {
        const uniqueEvents = new Set(state.events);
        expect(uniqueEvents.size).toBe(state.events.length);
      }
    });

    it('should have all transition targets reference valid states', () => {
      const validNames = new Set(["allCollapsed","stepExpanded","evidenceOpen","actionPending"]);
      const baseNames = new Set(["allCollapsed","stepExpanded","evidenceOpen","actionPending"]);
      expect(validNames.has('stepExpanded') || baseNames.has('stepExpanded')).toBe(true);
      expect(validNames.has('allCollapsed') || baseNames.has('allCollapsed')).toBe(true);
      expect(validNames.has('stepExpanded') || baseNames.has('stepExpanded')).toBe(true);
      expect(validNames.has('evidenceOpen') || baseNames.has('evidenceOpen')).toBe(true);
      expect(validNames.has('actionPending') || baseNames.has('actionPending')).toBe(true);
      expect(validNames.has('stepExpanded') || baseNames.has('stepExpanded')).toBe(true);
      expect(validNames.has('allCollapsed') || baseNames.has('allCollapsed')).toBe(true);
      expect(validNames.has('actionPending') || baseNames.has('actionPending')).toBe(true);
      expect(validNames.has('stepExpanded') || baseNames.has('stepExpanded')).toBe(true);
      expect(validNames.has('stepExpanded') || baseNames.has('stepExpanded')).toBe(true);
      expect(validNames.has('stepExpanded') || baseNames.has('stepExpanded')).toBe(true);
    });

  });

  describe('Connect Bindings', () => {
    it('should bind data-part="root" to root', () => {
      const binding = {"part":"root","dataPart":"root"};
      expect(binding.dataPart).toBe('root');
    });

    it('should bind data-state on root', () => {
      const attr = { name: 'data-state', value: "state" };
      expect(attr.name).toBe('data-state');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-variant on root', () => {
      const attr = { name: 'data-variant', value: "? variant" };
      expect(attr.name).toBe('data-variant');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="summary-bar" to summaryBar', () => {
      const binding = {"part":"summaryBar","dataPart":"summary-bar"};
      expect(binding.dataPart).toBe('summary-bar');
    });

    it('should bind data-part="overall-badge" to overallBadge', () => {
      const binding = {"part":"overallBadge","dataPart":"overall-badge"};
      expect(binding.dataPart).toBe('overall-badge');
    });

    it('should bind data-status on overallBadge', () => {
      const attr = { name: 'data-status', value: "overallStatus" };
      expect(attr.name).toBe('data-status');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="count-row" to countRow', () => {
      const binding = {"part":"countRow","dataPart":"count-row"};
      expect(binding.dataPart).toBe('count-row');
    });

    it('should bind data-part="passing-count" to passingCount', () => {
      const binding = {"part":"passingCount","dataPart":"passing-count"};
      expect(binding.dataPart).toBe('passing-count');
    });

    it('should bind data-count on passingCount', () => {
      const attr = { name: 'data-count', value: "passingCount" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="failing-count" to failingCount', () => {
      const binding = {"part":"failingCount","dataPart":"failing-count"};
      expect(binding.dataPart).toBe('failing-count');
    });

    it('should bind data-count on failingCount', () => {
      const attr = { name: 'data-count', value: "failingCount" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="pending-count" to pendingCount', () => {
      const binding = {"part":"pendingCount","dataPart":"pending-count"};
      expect(binding.dataPart).toBe('pending-count');
    });

    it('should bind data-count on pendingCount', () => {
      const attr = { name: 'data-count', value: "pendingCount" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="waived-count" to waivedCount', () => {
      const binding = {"part":"waivedCount","dataPart":"waived-count"};
      expect(binding.dataPart).toBe('waived-count');
    });

    it('should bind data-count on waivedCount', () => {
      const attr = { name: 'data-count', value: "waivedCount" };
      expect(attr.name).toBe('data-count');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="step-list" to stepList', () => {
      const binding = {"part":"stepList","dataPart":"step-list"};
      expect(binding.dataPart).toBe('step-list');
    });

    it('should bind data-part="step-row" to stepRow', () => {
      const binding = {"part":"stepRow","dataPart":"step-row"};
      expect(binding.dataPart).toBe('step-row');
    });

    it('should bind data-expanded on stepRow', () => {
      const attr = { name: 'data-expanded', value: "if self . isExpanded then true else false" };
      expect(attr.name).toBe('data-expanded');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-status on stepRow', () => {
      const attr = { name: 'data-status', value: "self . overallStatus" };
      expect(attr.name).toBe('data-status');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="step-toggle" to stepToggle', () => {
      const binding = {"part":"stepToggle","dataPart":"step-toggle"};
      expect(binding.dataPart).toBe('step-toggle');
    });

    it('should bind onClick handler on stepToggle', () => {
      const handler = { event: 'onClick', action: "send ( EXPAND_STEP , { stepId : self . id } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind onKeyDown handler on stepToggle', () => {
      const handler = { event: 'onKeyDown', action: "if key = = Enter or key = =   then send ( EXPAND_STEP , { stepId : self . id } ) else noop" };
      expect(handler.event).toBe('onKeyDown');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="step-label" to stepLabel', () => {
      const binding = {"part":"stepLabel","dataPart":"step-label"};
      expect(binding.dataPart).toBe('step-label');
    });

    it('should bind data-part="step-status-chips" to stepStatusChips', () => {
      const binding = {"part":"stepStatusChips","dataPart":"step-status-chips"};
      expect(binding.dataPart).toBe('step-status-chips');
    });

    it('should bind data-part="check-panel" to checkPanel', () => {
      const binding = {"part":"checkPanel","dataPart":"check-panel"};
      expect(binding.dataPart).toBe('check-panel');
    });

    it('should bind data-visible on checkPanel', () => {
      const attr = { name: 'data-visible', value: "if self . isExpanded then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="check-row" to checkRow', () => {
      const binding = {"part":"checkRow","dataPart":"check-row"};
      expect(binding.dataPart).toBe('check-row');
    });

    it('should bind data-status on checkRow', () => {
      const attr = { name: 'data-status', value: "self . status" };
      expect(attr.name).toBe('data-status');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-mode on checkRow', () => {
      const attr = { name: 'data-mode', value: "self . mode" };
      expect(attr.name).toBe('data-mode');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="check-label" to checkLabel', () => {
      const binding = {"part":"checkLabel","dataPart":"check-label"};
      expect(binding.dataPart).toBe('check-label');
    });

    it('should bind data-part="check-mode" to checkMode', () => {
      const binding = {"part":"checkMode","dataPart":"check-mode"};
      expect(binding.dataPart).toBe('check-mode');
    });

    it('should bind data-visible on checkMode', () => {
      const attr = { name: 'data-visible', value: "if ? showModes then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="check-status" to checkStatus', () => {
      const binding = {"part":"checkStatus","dataPart":"check-status"};
      expect(binding.dataPart).toBe('check-status');
    });

    it('should bind data-status on checkStatus', () => {
      const attr = { name: 'data-status', value: "self . status" };
      expect(attr.name).toBe('data-status');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="check-score" to checkScore', () => {
      const binding = {"part":"checkScore","dataPart":"check-score"};
      expect(binding.dataPart).toBe('check-score');
    });

    it('should bind data-visible on checkScore', () => {
      const attr = { name: 'data-visible', value: "if ? showScores and self . score then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="check-evidence" to checkEvidence', () => {
      const binding = {"part":"checkEvidence","dataPart":"check-evidence"};
      expect(binding.dataPart).toBe('check-evidence');
    });

    it('should bind data-visible on checkEvidence', () => {
      const attr = { name: 'data-visible', value: "if ? showEvidence and state = = evidenceOpen then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should bind onClick handler on checkEvidence', () => {
      const handler = { event: 'onClick', action: "send ( EXPAND_EVIDENCE , { checkId : self . id } )" };
      expect(handler.event).toBe('onClick');
      expect(handler.action).toBeDefined();
    });

    it('should bind data-part="check-actions" to checkActions', () => {
      const binding = {"part":"checkActions","dataPart":"check-actions"};
      expect(binding.dataPart).toBe('check-actions');
    });

    it('should bind data-disabled on checkActions', () => {
      const attr = { name: 'data-disabled', value: "if state = = actionPending then true else false" };
      expect(attr.name).toBe('data-disabled');
      expect(attr.value).toBeDefined();
    });

    it('should bind data-part="empty-state" to emptyState', () => {
      const binding = {"part":"emptyState","dataPart":"empty-state"};
      expect(binding.dataPart).toBe('empty-state');
    });

    it('should bind data-visible on emptyState', () => {
      const attr = { name: 'data-visible', value: "if steps . length = = 0 then true else false" };
      expect(attr.name).toBe('data-visible');
      expect(attr.value).toBeDefined();
    });

    it('should only reference anatomy parts that exist', () => {
      const anatomyParts = new Set(["root","summaryBar","overallBadge","countRow","passingCount","failingCount","pendingCount","waivedCount","stepList","stepRow","stepToggle","checkPanel","checkRow","checkLabel","checkMode","checkStatus","checkScore","checkEvidence","checkActions","emptyState"]);
      expect(anatomyParts.has('root')).toBe(true);
      expect(anatomyParts.has('summaryBar')).toBe(true);
      expect(anatomyParts.has('overallBadge')).toBe(true);
      expect(anatomyParts.has('countRow')).toBe(true);
      expect(anatomyParts.has('passingCount')).toBe(true);
      expect(anatomyParts.has('failingCount')).toBe(true);
      expect(anatomyParts.has('pendingCount')).toBe(true);
      expect(anatomyParts.has('waivedCount')).toBe(true);
      expect(anatomyParts.has('stepList')).toBe(true);
      expect(anatomyParts.has('stepRow')).toBe(true);
      expect(anatomyParts.has('stepToggle')).toBe(true);
      expect(anatomyParts.has('stepLabel')).toBe(true);
      expect(anatomyParts.has('stepStatusChips')).toBe(true);
      expect(anatomyParts.has('checkPanel')).toBe(true);
      expect(anatomyParts.has('checkRow')).toBe(true);
      expect(anatomyParts.has('checkLabel')).toBe(true);
      expect(anatomyParts.has('checkMode')).toBe(true);
      expect(anatomyParts.has('checkStatus')).toBe(true);
      expect(anatomyParts.has('checkScore')).toBe(true);
      expect(anatomyParts.has('checkEvidence')).toBe(true);
      expect(anatomyParts.has('checkActions')).toBe(true);
      expect(anatomyParts.has('emptyState')).toBe(true);
    });

  });

  describe('Keyboard Bindings', () => {
    it('should handle Enter key with action: EXPAND_STEP', () => {
      const binding = { key: 'Enter', action: 'EXPAND_STEP' };
      expect(binding.key).toBe('Enter');
      expect(binding.action).toBe('EXPAND_STEP');
    });

    it('should handle Space key with action: EXPAND_STEP', () => {
      const binding = { key: 'Space', action: 'EXPAND_STEP' };
      expect(binding.key).toBe('Space');
      expect(binding.action).toBe('EXPAND_STEP');
    });

    it('should handle Escape key with action: COLLAPSE_STEP', () => {
      const binding = { key: 'Escape', action: 'COLLAPSE_STEP' };
      expect(binding.key).toBe('Escape');
      expect(binding.action).toBe('COLLAPSE_STEP');
    });

    it('should handle ArrowDown key with action: FOCUS_NEXT_STEP', () => {
      const binding = { key: 'ArrowDown', action: 'FOCUS_NEXT_STEP' };
      expect(binding.key).toBe('ArrowDown');
      expect(binding.action).toBe('FOCUS_NEXT_STEP');
    });

    it('should handle ArrowUp key with action: FOCUS_PREV_STEP', () => {
      const binding = { key: 'ArrowUp', action: 'FOCUS_PREV_STEP' };
      expect(binding.key).toBe('ArrowUp');
      expect(binding.action).toBe('FOCUS_PREV_STEP');
    });

    it('should handle Tab key with action: FOCUS_NEXT', () => {
      const binding = { key: 'Tab', action: 'FOCUS_NEXT' };
      expect(binding.key).toBe('Tab');
      expect(binding.action).toBe('FOCUS_NEXT');
    });

    it('should map keyboard keys to valid FSM events', () => {
      const fsmEvents = new Set(["EXPAND_STEP","COLLAPSE_STEP","EXPAND_EVIDENCE","TRIGGER_ACTION","COLLAPSE_EVIDENCE","ACTION_COMPLETE","ACTION_ERROR","CANCEL"]);
      const keyboardActions = ["EXPAND_STEP","EXPAND_STEP","COLLAPSE_STEP","FOCUS_NEXT_STEP","FOCUS_PREV_STEP","FOCUS_NEXT"];
      for (const action of keyboardActions) {
        // Keyboard action should reference an FSM event or be a recognized action
        expect(typeof action).toBe('string');
        expect(action.length).toBeGreaterThan(0);
      }
    });

  });

  describe('ARIA Attributes', () => {
    it('should have accessibility role "region"', () => {
      expect('region').toBeTruthy();
    });

    it('should not trap focus', () => {
      expect(false).toBe(false);
    });

    it('should set initial focus to "summaryBar"', () => {
      expect('summaryBar').toBeTruthy();
    });

    it('should use roving tabindex', () => {
      expect(true).toBe(true);
    });

    it('should bind role on root', () => {
      const ariaAttr = { part: 'root', name: 'role', value: "region" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-label', value: "Check rollup review" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-busy on root', () => {
      const ariaAttr = { part: 'root', name: 'aria-busy', value: "if state = = actionPending then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on summaryBar', () => {
      const ariaAttr = { part: 'summaryBar', name: 'role', value: "status" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on summaryBar', () => {
      const ariaAttr = { part: 'summaryBar', name: 'aria-label', value: "Overall check status" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on summaryBar', () => {
      const ariaAttr = { part: 'summaryBar', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on overallBadge', () => {
      const ariaAttr = { part: 'overallBadge', name: 'role', value: "img" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on overallBadge', () => {
      const ariaAttr = { part: 'overallBadge', name: 'aria-label', value: "concat ( Overall status:  , overallStatus )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on stepList', () => {
      const ariaAttr = { part: 'stepList', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on stepList', () => {
      const ariaAttr = { part: 'stepList', name: 'aria-label', value: "Steps with checks" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on stepRow', () => {
      const ariaAttr = { part: 'stepRow', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on stepRow', () => {
      const ariaAttr = { part: 'stepRow', name: 'aria-expanded', value: "if self . isExpanded then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on stepRow', () => {
      const ariaAttr = { part: 'stepRow', name: 'aria-label', value: "concat ( Step:  , self . label )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on stepToggle', () => {
      const ariaAttr = { part: 'stepToggle', name: 'role', value: "button" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-expanded on stepToggle', () => {
      const ariaAttr = { part: 'stepToggle', name: 'aria-expanded', value: "if self . isExpanded then true else false" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-controls on stepToggle', () => {
      const ariaAttr = { part: 'stepToggle', name: 'aria-controls', value: "checkPanel" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on stepToggle', () => {
      const ariaAttr = { part: 'stepToggle', name: 'aria-label', value: "concat ( Toggle checks for  , self . label )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on checkPanel', () => {
      const ariaAttr = { part: 'checkPanel', name: 'role', value: "list" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on checkPanel', () => {
      const ariaAttr = { part: 'checkPanel', name: 'aria-label', value: "concat ( Checks for step  , self . stepLabel )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on checkRow', () => {
      const ariaAttr = { part: 'checkRow', name: 'role', value: "listitem" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on checkRow', () => {
      const ariaAttr = { part: 'checkRow', name: 'aria-label', value: "concat ( self . checkLabel ,  —  , self . status , if self . score then concat ( , score:  , self . score ) else  )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on checkActions', () => {
      const ariaAttr = { part: 'checkActions', name: 'role', value: "toolbar" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on checkActions', () => {
      const ariaAttr = { part: 'checkActions', name: 'aria-label', value: "concat ( Actions for check  , self . checkLabel )" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind role on emptyState', () => {
      const ariaAttr = { part: 'emptyState', name: 'role', value: "status" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-live on emptyState', () => {
      const ariaAttr = { part: 'emptyState', name: 'aria-live', value: "polite" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should bind aria-label on emptyState', () => {
      const ariaAttr = { part: 'emptyState', name: 'aria-label', value: "No checks registered" };
      expect(ariaAttr.name).toMatch(/^(role|aria-)/);
      expect(ariaAttr.value).toBeDefined();
    });

    it('should set aria-label on connect part root', () => {
      const connectAria = { part: 'root', attr: 'aria-label', value: "Check rollup review" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part summaryBar', () => {
      const connectAria = { part: 'summaryBar', attr: 'role', value: "status" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-live on connect part summaryBar', () => {
      const connectAria = { part: 'summaryBar', attr: 'aria-live', value: "polite" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part stepList', () => {
      const connectAria = { part: 'stepList', attr: 'role', value: "list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part stepToggle', () => {
      const connectAria = { part: 'stepToggle', attr: 'role', value: "button" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set aria-expanded on connect part stepToggle', () => {
      const connectAria = { part: 'stepToggle', attr: 'aria-expanded', value: "if self . isExpanded then true else false" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part checkPanel', () => {
      const connectAria = { part: 'checkPanel', attr: 'role', value: "list" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part checkRow', () => {
      const connectAria = { part: 'checkRow', attr: 'role', value: "listitem" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part checkActions', () => {
      const connectAria = { part: 'checkActions', attr: 'role', value: "toolbar" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

    it('should set role on connect part emptyState', () => {
      const connectAria = { part: 'emptyState', attr: 'role', value: "status" };
      expect(connectAria.attr).toMatch(/^(role|aria-)/);
      expect(connectAria.value).toBeDefined();
    });

  });

  describe('Prop Defaults', () => {
    it('should declare prop "expandedStepId" with type "option String"', () => {
      const prop = { name: 'expandedStepId', type: 'option String' };
      expect(prop.name).toBe('expandedStepId');
      expect(prop.type).toBeTruthy();
    });

    it('should declare prop "showScores" with type "Bool"', () => {
      const prop = { name: 'showScores', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showScores');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showScores" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showEvidence" with type "Bool"', () => {
      const prop = { name: 'showEvidence', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showEvidence');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showEvidence" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "showModes" with type "Bool"', () => {
      const prop = { name: 'showModes', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('showModes');
      expect(prop.type).toBeTruthy();
    });

    it('should default "showModes" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "allowWaive" with type "Bool"', () => {
      const prop = { name: 'allowWaive', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('allowWaive');
      expect(prop.type).toBeTruthy();
    });

    it('should default "allowWaive" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "allowReset" with type "Bool"', () => {
      const prop = { name: 'allowReset', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('allowReset');
      expect(prop.type).toBeTruthy();
    });

    it('should default "allowReset" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "allowReEvaluate" with type "Bool"', () => {
      const prop = { name: 'allowReEvaluate', type: 'Bool', defaultValue: "true" };
      expect(prop.name).toBe('allowReEvaluate');
      expect(prop.type).toBeTruthy();
    });

    it('should default "allowReEvaluate" to "true"', () => {
      expect("true").toBe("true");
    });

    it('should declare prop "variant" with type "union compact | full"', () => {
      const prop = { name: 'variant', type: 'union compact | full', defaultValue: "full" };
      expect(prop.name).toBe('variant');
      expect(prop.type).toBeTruthy();
    });

    it('should default "variant" to "full"', () => {
      expect("full").toBe("full");
    });

  });

  describe('Anatomy Parts', () => {
    it('should declare all anatomy parts', () => {
      const parts = ["root","summaryBar","overallBadge","countRow","passingCount","failingCount","pendingCount","waivedCount","stepList","stepRow","stepToggle","checkPanel","checkRow","checkLabel","checkMode","checkStatus","checkScore","checkEvidence","checkActions","emptyState"];
      expect(parts.length).toBe(20);
      expect(parts).toContain('root');
      expect(parts).toContain('summaryBar');
      expect(parts).toContain('overallBadge');
      expect(parts).toContain('countRow');
      expect(parts).toContain('passingCount');
      expect(parts).toContain('failingCount');
      expect(parts).toContain('pendingCount');
      expect(parts).toContain('waivedCount');
      expect(parts).toContain('stepList');
      expect(parts).toContain('stepRow');
      expect(parts).toContain('stepToggle');
      expect(parts).toContain('checkPanel');
      expect(parts).toContain('checkRow');
      expect(parts).toContain('checkLabel');
      expect(parts).toContain('checkMode');
      expect(parts).toContain('checkStatus');
      expect(parts).toContain('checkScore');
      expect(parts).toContain('checkEvidence');
      expect(parts).toContain('checkActions');
      expect(parts).toContain('emptyState');
    });

    it('should assign semantic roles to anatomy parts', () => {
      expect('container').toBeTruthy(); // root: container
      expect('container').toBeTruthy(); // summaryBar: container
      expect('container').toBeTruthy(); // overallBadge: container
      expect('container').toBeTruthy(); // countRow: container
      expect('text').toBeTruthy(); // passingCount: text
      expect('text').toBeTruthy(); // failingCount: text
      expect('text').toBeTruthy(); // pendingCount: text
      expect('text').toBeTruthy(); // waivedCount: text
      expect('container').toBeTruthy(); // stepList: container
      expect('container').toBeTruthy(); // stepRow: container
      expect('action').toBeTruthy(); // stepToggle: action
      expect('container').toBeTruthy(); // checkPanel: container
      expect('container').toBeTruthy(); // checkRow: container
      expect('text').toBeTruthy(); // checkLabel: text
      expect('text').toBeTruthy(); // checkMode: text
      expect('container').toBeTruthy(); // checkStatus: container
      expect('text').toBeTruthy(); // checkScore: text
      expect('container').toBeTruthy(); // checkEvidence: container
      expect('container').toBeTruthy(); // checkActions: container
      expect('container').toBeTruthy(); // emptyState: container
    });

  });

});
