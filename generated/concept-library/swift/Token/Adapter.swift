// generated: Token/Adapter.swift

import Foundation

class TokenAdapter: ConceptTransport {
    private let handler: any TokenHandler
    private let storage: ConceptStorage

    init(handler: any TokenHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "replace":
            let input = try decoder.decode(TokenReplaceInput.self, from: invocation.inputData)
            let output = try await handler.replace(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "getAvailableTokens":
            let input = try decoder.decode(TokenGetAvailableTokensInput.self, from: invocation.inputData)
            let output = try await handler.getAvailableTokens(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "scan":
            let input = try decoder.decode(TokenScanInput.self, from: invocation.inputData)
            let output = try await handler.scan(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "registerProvider":
            let input = try decoder.decode(TokenRegisterProviderInput.self, from: invocation.inputData)
            let output = try await handler.registerProvider(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
