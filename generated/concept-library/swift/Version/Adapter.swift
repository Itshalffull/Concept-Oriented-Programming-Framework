// generated: Version/Adapter.swift

import Foundation

class VersionAdapter: ConceptTransport {
    private let handler: any VersionHandler
    private let storage: ConceptStorage

    init(handler: any VersionHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "snapshot":
            let input = try decoder.decode(VersionSnapshotInput.self, from: invocation.inputData)
            let output = try await handler.snapshot(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "listVersions":
            let input = try decoder.decode(VersionListVersionsInput.self, from: invocation.inputData)
            let output = try await handler.listVersions(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "rollback":
            let input = try decoder.decode(VersionRollbackInput.self, from: invocation.inputData)
            let output = try await handler.rollback(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "diff":
            let input = try decoder.decode(VersionDiffInput.self, from: invocation.inputData)
            let output = try await handler.diff(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
