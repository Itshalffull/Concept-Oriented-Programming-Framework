// generated: SearchIndex/Adapter.swift

import Foundation

class SearchIndexAdapter: ConceptTransport {
    private let handler: any SearchIndexHandler
    private let storage: ConceptStorage

    init(handler: any SearchIndexHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "createIndex":
            let input = try decoder.decode(SearchIndexCreateIndexInput.self, from: invocation.inputData)
            let output = try await handler.createIndex(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "indexItem":
            let input = try decoder.decode(SearchIndexIndexItemInput.self, from: invocation.inputData)
            let output = try await handler.indexItem(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "removeItem":
            let input = try decoder.decode(SearchIndexRemoveItemInput.self, from: invocation.inputData)
            let output = try await handler.removeItem(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "search":
            let input = try decoder.decode(SearchIndexSearchInput.self, from: invocation.inputData)
            let output = try await handler.search(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "addProcessor":
            let input = try decoder.decode(SearchIndexAddProcessorInput.self, from: invocation.inputData)
            let output = try await handler.addProcessor(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "reindex":
            let input = try decoder.decode(SearchIndexReindexInput.self, from: invocation.inputData)
            let output = try await handler.reindex(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
