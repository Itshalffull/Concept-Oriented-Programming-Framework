// generated: Group/Adapter.swift

import Foundation

class GroupAdapter: ConceptTransport {
    private let handler: any GroupHandler
    private let storage: ConceptStorage

    init(handler: any GroupHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "createGroup":
            let input = try decoder.decode(GroupCreateGroupInput.self, from: invocation.inputData)
            let output = try await handler.createGroup(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "addMember":
            let input = try decoder.decode(GroupAddMemberInput.self, from: invocation.inputData)
            let output = try await handler.addMember(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "assignGroupRole":
            let input = try decoder.decode(GroupAssignGroupRoleInput.self, from: invocation.inputData)
            let output = try await handler.assignGroupRole(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "addContent":
            let input = try decoder.decode(GroupAddContentInput.self, from: invocation.inputData)
            let output = try await handler.addContent(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "checkGroupAccess":
            let input = try decoder.decode(GroupCheckGroupAccessInput.self, from: invocation.inputData)
            let output = try await handler.checkGroupAccess(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
