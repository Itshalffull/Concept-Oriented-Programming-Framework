// generated: ConfigSync/Adapter.swift

import Foundation

class ConfigSyncAdapter: ConceptTransport {
    private let handler: any ConfigSyncHandler
    private let storage: ConceptStorage

    init(handler: any ConfigSyncHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "export":
            let input = try decoder.decode(ConfigSyncExportInput.self, from: invocation.inputData)
            let output = try await handler.export(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "import":
            let input = try decoder.decode(ConfigSyncImportInput.self, from: invocation.inputData)
            let output = try await handler.import(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "override":
            let input = try decoder.decode(ConfigSyncOverrideInput.self, from: invocation.inputData)
            let output = try await handler.override(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "diff":
            let input = try decoder.decode(ConfigSyncDiffInput.self, from: invocation.inputData)
            let output = try await handler.diff(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
