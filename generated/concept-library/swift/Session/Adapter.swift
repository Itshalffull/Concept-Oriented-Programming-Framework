// generated: Session/Adapter.swift

import Foundation

class SessionAdapter: ConceptTransport {
    private let handler: any SessionHandler
    private let storage: ConceptStorage

    init(handler: any SessionHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "create":
            let input = try decoder.decode(SessionCreateInput.self, from: invocation.inputData)
            let output = try await handler.create(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "validate":
            let input = try decoder.decode(SessionValidateInput.self, from: invocation.inputData)
            let output = try await handler.validate(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "refresh":
            let input = try decoder.decode(SessionRefreshInput.self, from: invocation.inputData)
            let output = try await handler.refresh(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "destroy":
            let input = try decoder.decode(SessionDestroyInput.self, from: invocation.inputData)
            let output = try await handler.destroy(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "destroyAll":
            let input = try decoder.decode(SessionDestroyAllInput.self, from: invocation.inputData)
            let output = try await handler.destroyAll(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "getContext":
            let input = try decoder.decode(SessionGetContextInput.self, from: invocation.inputData)
            let output = try await handler.getContext(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
