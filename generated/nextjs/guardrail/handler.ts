// Guardrail — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  GuardrailStorage,
  GuardrailCreateInput,
  GuardrailCreateOutput,
  GuardrailAddRuleInput,
  GuardrailAddRuleOutput,
  GuardrailCheckInputInput,
  GuardrailCheckInputOutput,
  GuardrailCheckOutputInput,
  GuardrailCheckOutputOutput,
  GuardrailCheckInput,
  GuardrailCheckOutput,
  GuardrailGetViolationsInput,
  GuardrailGetViolationsOutput,
  GuardrailRemoveRuleInput,
  GuardrailRemoveRuleOutput,
} from './types.js';

import {
  createOk,
  createInvalid,
  addRuleOk,
  addRuleNotfound,
  checkInputPass,
  checkInputViolation,
  checkOutputPass,
  checkOutputViolation,
  checkPass,
  checkBlocked,
  checkFlagged,
  getViolationsOk,
  getViolationsEmpty,
  removeRuleOk,
  removeRuleNotfound,
} from './types.js';

export interface GuardrailError {
  readonly code: string;
  readonly message: string;
}

export interface GuardrailHandler {
  readonly create: (
    input: GuardrailCreateInput,
    storage: GuardrailStorage,
  ) => TE.TaskEither<GuardrailError, GuardrailCreateOutput>;
  readonly addRule: (
    input: GuardrailAddRuleInput,
    storage: GuardrailStorage,
  ) => TE.TaskEither<GuardrailError, GuardrailAddRuleOutput>;
  readonly checkInput: (
    input: GuardrailCheckInputInput,
    storage: GuardrailStorage,
  ) => TE.TaskEither<GuardrailError, GuardrailCheckInputOutput>;
  readonly checkOutput: (
    input: GuardrailCheckOutputInput,
    storage: GuardrailStorage,
  ) => TE.TaskEither<GuardrailError, GuardrailCheckOutputOutput>;
  readonly check: (
    input: GuardrailCheckInput,
    storage: GuardrailStorage,
  ) => TE.TaskEither<GuardrailError, GuardrailCheckOutput>;
  readonly getViolations: (
    input: GuardrailGetViolationsInput,
    storage: GuardrailStorage,
  ) => TE.TaskEither<GuardrailError, GuardrailGetViolationsOutput>;
  readonly removeRule: (
    input: GuardrailRemoveRuleInput,
    storage: GuardrailStorage,
  ) => TE.TaskEither<GuardrailError, GuardrailRemoveRuleOutput>;
}

// --- Implementation ---

export const guardrailHandler: GuardrailHandler = {
  create: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('guardrail', input.name, { name: input.name, guardrail_type: input.guardrail_type, config: input.config });
          return createOk(input.name);
        },
        (error): GuardrailError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  addRule: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('guardrail', input.guardrail, { guardrail: input.guardrail, pattern: input.pattern, severity: input.severity });
          return addRuleOk(input.guardrail);
        },
        (error): GuardrailError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  checkInput: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('guardrail', input.guardrail);
          return checkInputOk();
        },
        (error): GuardrailError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  checkOutput: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('guardrail', input.guardrail);
          return checkOutputOk();
        },
        (error): GuardrailError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  check: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('guardrail', input.guardrail);
          return checkOk();
        },
        (error): GuardrailError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getViolations: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('guardrail', input.guardrail);
          return getViolationsOk((record as any).violations);
        },
        (error): GuardrailError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  removeRule: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('guardrail', input.guardrail),
        (error): GuardrailError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(removeRuleNotfound(`${input.guardrail} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('guardrail', input.guardrail);
                  return removeRuleOk();
                },
                (error): GuardrailError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),
};
