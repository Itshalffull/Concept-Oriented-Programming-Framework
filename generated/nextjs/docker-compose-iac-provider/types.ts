// DockerComposeIacProvider — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for DockerComposeIacProvider
export interface DockerComposeIacProviderStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface DockerComposeIacProviderGenerateInput {
  readonly plan: string;
}

export interface DockerComposeIacProviderGenerateOutput {
  readonly variant: 'ok';
  readonly composeFile: string;
  readonly files: readonly string[];
}

export interface DockerComposeIacProviderPreviewInput {
  readonly composeFile: string;
}

export interface DockerComposeIacProviderPreviewOutput {
  readonly variant: 'ok';
  readonly composeFile: string;
  readonly toCreate: number;
  readonly toUpdate: number;
  readonly toDelete: number;
}

export interface DockerComposeIacProviderApplyInput {
  readonly composeFile: string;
}

export interface DockerComposeIacProviderApplyOutputOk {
  readonly variant: 'ok';
  readonly composeFile: string;
  readonly created: readonly string[];
  readonly updated: readonly string[];
}

export interface DockerComposeIacProviderApplyOutputPortConflict {
  readonly variant: 'portConflict';
  readonly port: number;
  readonly existingService: string;
}

export type DockerComposeIacProviderApplyOutput = DockerComposeIacProviderApplyOutputOk | DockerComposeIacProviderApplyOutputPortConflict;

export interface DockerComposeIacProviderTeardownInput {
  readonly composeFile: string;
}

export interface DockerComposeIacProviderTeardownOutput {
  readonly variant: 'ok';
  readonly composeFile: string;
  readonly destroyed: readonly string[];
}

// --- Variant constructors ---

export const generateOk = (composeFile: string, files: readonly string[]): DockerComposeIacProviderGenerateOutput => ({ variant: 'ok', composeFile, files } as DockerComposeIacProviderGenerateOutput);

export const previewOk = (composeFile: string, toCreate: number, toUpdate: number, toDelete: number): DockerComposeIacProviderPreviewOutput => ({ variant: 'ok', composeFile, toCreate, toUpdate, toDelete } as DockerComposeIacProviderPreviewOutput);

export const applyOk = (composeFile: string, created: readonly string[], updated: readonly string[]): DockerComposeIacProviderApplyOutput => ({ variant: 'ok', composeFile, created, updated } as DockerComposeIacProviderApplyOutput);
export const applyPortConflict = (port: number, existingService: string): DockerComposeIacProviderApplyOutput => ({ variant: 'portConflict', port, existingService } as DockerComposeIacProviderApplyOutput);

export const teardownOk = (composeFile: string, destroyed: readonly string[]): DockerComposeIacProviderTeardownOutput => ({ variant: 'ok', composeFile, destroyed } as DockerComposeIacProviderTeardownOutput);

