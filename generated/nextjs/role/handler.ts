// Role — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  RoleStorage,
  RoleCreateInput,
  RoleCreateOutput,
  RoleAssignInput,
  RoleAssignOutput,
  RoleRevokeInput,
  RoleRevokeOutput,
  RoleCheckInput,
  RoleCheckOutput,
  RoleDissolveInput,
  RoleDissolveOutput,
} from './types.js';

import {
  createCreated,
  createAlready_exists,
  assignAssigned,
  assignNot_found,
  assignFull,
  revokeRevoked,
  revokeNot_assigned,
  checkAllowed,
  checkDenied,
  dissolveDissolved,
  dissolveNot_found,
} from './types.js';

export interface RoleError {
  readonly code: string;
  readonly message: string;
}

export interface RoleHandler {
  readonly create: (
    input: RoleCreateInput,
    storage: RoleStorage,
  ) => TE.TaskEither<RoleError, RoleCreateOutput>;
  readonly assign: (
    input: RoleAssignInput,
    storage: RoleStorage,
  ) => TE.TaskEither<RoleError, RoleAssignOutput>;
  readonly revoke: (
    input: RoleRevokeInput,
    storage: RoleStorage,
  ) => TE.TaskEither<RoleError, RoleRevokeOutput>;
  readonly check: (
    input: RoleCheckInput,
    storage: RoleStorage,
  ) => TE.TaskEither<RoleError, RoleCheckOutput>;
  readonly dissolve: (
    input: RoleDissolveInput,
    storage: RoleStorage,
  ) => TE.TaskEither<RoleError, RoleDissolveOutput>;
}

// --- Implementation ---

export const roleHandler: RoleHandler = {
  create: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('role', input.role, { role: input.role, name: input.name, purpose: input.purpose, permissions: input.permissions });
          return createOk();
        },
        (error): RoleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  assign: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('role', input.role, { role: input.role, holder: input.holder });
          return assignOk();
        },
        (error): RoleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  revoke: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.delete('role', input.role);
          return revokeOk();
        },
        (error): RoleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  check: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('role', input.holder);
          return checkOk();
        },
        (error): RoleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  dissolve: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('role', input.role, { role: input.role });
          return dissolveOk();
        },
        (error): RoleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
