// Role — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for Role
export interface RoleStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface RoleCreateInput {
  readonly role: string;
  readonly name: string;
  readonly purpose: string;
  readonly permissions: ReadonlySet<string>;
}

export interface RoleCreateOutputCreated {
  readonly variant: 'created';
  readonly role: string;
}

export interface RoleCreateOutputAlready_exists {
  readonly variant: 'already_exists';
  readonly role: string;
}

export type RoleCreateOutput = RoleCreateOutputCreated | RoleCreateOutputAlready_exists;

export interface RoleAssignInput {
  readonly role: string;
  readonly holder: string;
}

export interface RoleAssignOutputAssigned {
  readonly variant: 'assigned';
  readonly role: string;
  readonly holder: string;
}

export interface RoleAssignOutputNot_found {
  readonly variant: 'not_found';
  readonly role: string;
}

export interface RoleAssignOutputFull {
  readonly variant: 'full';
  readonly role: string;
}

export type RoleAssignOutput = RoleAssignOutputAssigned | RoleAssignOutputNot_found | RoleAssignOutputFull;

export interface RoleRevokeInput {
  readonly role: string;
  readonly holder: string;
}

export interface RoleRevokeOutputRevoked {
  readonly variant: 'revoked';
  readonly role: string;
  readonly holder: string;
}

export interface RoleRevokeOutputNot_assigned {
  readonly variant: 'not_assigned';
  readonly role: string;
  readonly holder: string;
}

export type RoleRevokeOutput = RoleRevokeOutputRevoked | RoleRevokeOutputNot_assigned;

export interface RoleCheckInput {
  readonly holder: string;
  readonly permission: string;
}

export interface RoleCheckOutputAllowed {
  readonly variant: 'allowed';
  readonly holder: string;
  readonly permission: string;
}

export interface RoleCheckOutputDenied {
  readonly variant: 'denied';
  readonly holder: string;
  readonly permission: string;
}

export type RoleCheckOutput = RoleCheckOutputAllowed | RoleCheckOutputDenied;

export interface RoleDissolveInput {
  readonly role: string;
}

export interface RoleDissolveOutputDissolved {
  readonly variant: 'dissolved';
  readonly role: string;
}

export interface RoleDissolveOutputNot_found {
  readonly variant: 'not_found';
  readonly role: string;
}

export type RoleDissolveOutput = RoleDissolveOutputDissolved | RoleDissolveOutputNot_found;

// --- Variant constructors ---

export const createCreated = (role: string): RoleCreateOutput => ({ variant: 'created', role } as RoleCreateOutput);
export const createAlready_exists = (role: string): RoleCreateOutput => ({ variant: 'already_exists', role } as RoleCreateOutput);

export const assignAssigned = (role: string, holder: string): RoleAssignOutput => ({ variant: 'assigned', role, holder } as RoleAssignOutput);
export const assignNot_found = (role: string): RoleAssignOutput => ({ variant: 'not_found', role } as RoleAssignOutput);
export const assignFull = (role: string): RoleAssignOutput => ({ variant: 'full', role } as RoleAssignOutput);

export const revokeRevoked = (role: string, holder: string): RoleRevokeOutput => ({ variant: 'revoked', role, holder } as RoleRevokeOutput);
export const revokeNot_assigned = (role: string, holder: string): RoleRevokeOutput => ({ variant: 'not_assigned', role, holder } as RoleRevokeOutput);

export const checkAllowed = (holder: string, permission: string): RoleCheckOutput => ({ variant: 'allowed', holder, permission } as RoleCheckOutput);
export const checkDenied = (holder: string, permission: string): RoleCheckOutput => ({ variant: 'denied', holder, permission } as RoleCheckOutput);

export const dissolveDissolved = (role: string): RoleDissolveOutput => ({ variant: 'dissolved', role } as RoleDissolveOutput);
export const dissolveNot_found = (role: string): RoleDissolveOutput => ({ variant: 'not_found', role } as RoleDissolveOutput);

