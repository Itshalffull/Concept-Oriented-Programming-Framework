// ReWOOStrategy — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ReWOOStrategyStorage,
  ReWOOStrategyExecuteInput,
  ReWOOStrategyExecuteOutput,
  ReWOOStrategyPlanCallsInput,
  ReWOOStrategyPlanCallsOutput,
  ReWOOStrategyExecuteBatchInput,
  ReWOOStrategyExecuteBatchOutput,
  ReWOOStrategySynthesizeInput,
  ReWOOStrategySynthesizeOutput,
  ReWOOStrategyGetStateInput,
  ReWOOStrategyGetStateOutput,
} from './types.js';

import {
  executeOk,
  executeError,
  planCallsOk,
  executeBatchOk,
  executeBatchPartial,
  synthesizeOk,
  getStateOk,
} from './types.js';

export interface ReWOOStrategyError {
  readonly code: string;
  readonly message: string;
}

export interface ReWOOStrategyHandler {
  readonly execute: (
    input: ReWOOStrategyExecuteInput,
    storage: ReWOOStrategyStorage,
  ) => TE.TaskEither<ReWOOStrategyError, ReWOOStrategyExecuteOutput>;
  readonly planCalls: (
    input: ReWOOStrategyPlanCallsInput,
    storage: ReWOOStrategyStorage,
  ) => TE.TaskEither<ReWOOStrategyError, ReWOOStrategyPlanCallsOutput>;
  readonly executeBatch: (
    input: ReWOOStrategyExecuteBatchInput,
    storage: ReWOOStrategyStorage,
  ) => TE.TaskEither<ReWOOStrategyError, ReWOOStrategyExecuteBatchOutput>;
  readonly synthesize: (
    input: ReWOOStrategySynthesizeInput,
    storage: ReWOOStrategyStorage,
  ) => TE.TaskEither<ReWOOStrategyError, ReWOOStrategySynthesizeOutput>;
  readonly getState: (
    input: ReWOOStrategyGetStateInput,
    storage: ReWOOStrategyStorage,
  ) => TE.TaskEither<ReWOOStrategyError, ReWOOStrategyGetStateOutput>;
}

// --- Implementation ---

export const reWOOStrategyHandler: ReWOOStrategyHandler = {
  execute: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('rewoostrategy', input.agent_ref, { agent_ref: input.agent_ref, goal: input.goal, context: input.context, available_tools: input.available_tools, max_iterations: input.max_iterations });
          return executeOk(input.agent_ref, 0, 0);
        },
        (error): ReWOOStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  planCalls: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('rewoostrategy', input.session, { session: input.session, goal: input.goal, available_tools: input.available_tools });
          return planCallsOk([]);
        },
        (error): ReWOOStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  executeBatch: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('rewoostrategy', input.session, { session: input.session });
          return executeBatchOk([]);
        },
        (error): ReWOOStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  synthesize: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('rewoostrategy', input.session, { session: input.session, goal: input.goal });
          return synthesizeOk(input.session);
        },
        (error): ReWOOStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getState: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('rewoostrategy', input.session);
          return getStateOk((record as any).planned, (record as any).results);
        },
        (error): ReWOOStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
