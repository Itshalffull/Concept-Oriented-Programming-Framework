// KindSystem — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { kindSystemHandler } from './handler.js';
import type { KindSystemStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): KindSystemStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('KindSystem conformance', () => {
  it('invariant 1: after define, define, connect, validate, route, dependents behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = kindSystemHandler;

    const ast = 'u-test-invariant-001';
    const mfst = 'u-test-invariant-002';
    const p = 'u-test-invariant-003';
    const d = 'u-test-invariant-004';

    // setup: define -> ok
    const defineResultSetup = await pipe(
      handler.define({
      name: 'ConceptAST',
      category: 'model',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).kind).toBe(ast);
        return output;
      }),
    )();
    expect(E.isRight(defineResultSetup)).toBe(true);

    // setup: define -> ok
    const defineResultSetup = await pipe(
      handler.define({
      name: 'ConceptManifest',
      category: 'model',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).kind).toBe(mfst);
        return output;
      }),
    )();
    expect(E.isRight(defineResultSetup)).toBe(true);

    // setup: connect -> ok
    const connectResultSetup = await pipe(
      handler.connect({
      from: ast,
      to: mfst,
      relation: 'normalizes_to',
      transformName: 'SchemaGen',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(connectResultSetup)).toBe(true);

    // assert: validate -> ok
    const validateResultAssert = await pipe(
      handler.validate({
      from: ast,
      to: mfst,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(validateResultAssert)).toBe(true);

    // assert: route -> ok
    const routeResultAssert = await pipe(
      handler.route({
      from: ast,
      to: mfst,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).path).toBe(p);
        return output;
      }),
    )();
    expect(E.isRight(routeResultAssert)).toBe(true);

    // assert: dependents -> ok
    const dependentsResultAssert = await pipe(
      handler.dependents({
      kind: ast,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).downstream).toBe(d);
        return output;
      }),
    )();
    expect(E.isRight(dependentsResultAssert)).toBe(true);

  });

});
