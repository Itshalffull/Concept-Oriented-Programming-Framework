// TokenBalance — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  TokenBalanceStorage,
  TokenBalanceConfigureInput,
  TokenBalanceConfigureOutput,
  TokenBalanceSnapshotInput,
  TokenBalanceSnapshotOutput,
  TokenBalanceGetWeightInput,
  TokenBalanceGetWeightOutput,
} from './types.js';

import {
  configureConfigured,
  snapshotSnapshotted,
  getWeightWeight,
  getWeightZero_balance,
} from './types.js';

export interface TokenBalanceError {
  readonly code: string;
  readonly message: string;
}

export interface TokenBalanceHandler {
  readonly configure: (
    input: TokenBalanceConfigureInput,
    storage: TokenBalanceStorage,
  ) => TE.TaskEither<TokenBalanceError, TokenBalanceConfigureOutput>;
  readonly snapshot: (
    input: TokenBalanceSnapshotInput,
    storage: TokenBalanceStorage,
  ) => TE.TaskEither<TokenBalanceError, TokenBalanceSnapshotOutput>;
  readonly getWeight: (
    input: TokenBalanceGetWeightInput,
    storage: TokenBalanceStorage,
  ) => TE.TaskEither<TokenBalanceError, TokenBalanceGetWeightOutput>;
}

// --- Implementation ---

export const tokenBalanceHandler: TokenBalanceHandler = {
  configure: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('tokenbalance', input.tokenAddress, { tokenAddress: input.tokenAddress });
          return configureOk();
        },
        (error): TokenBalanceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  snapshot: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('tokenbalance', input.config, { config: input.config, blockRef: input.blockRef });
          return snapshotOk();
        },
        (error): TokenBalanceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getWeight: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('tokenbalance', input.config);
          return getWeightOk();
        },
        (error): TokenBalanceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
