// DisplayMode — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { displayModeHandler } from './handler.js';
import type { DisplayModeStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): DisplayModeStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('DisplayMode conformance', () => {
  it('invariant 1: after defineMode, configureFieldDisplay behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = displayModeHandler;

    const d = 'u-test-invariant-001';

    // setup: defineMode -> ok
    const defineModeResultSetup = await pipe(
      handler.defineMode({
      mode: d,
      name: 'teaser',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).mode).toBe(d);
        return output;
      }),
    )();
    expect(E.isRight(defineModeResultSetup)).toBe(true);

    // assert: configureFieldDisplay -> ok
    const configureFieldDisplayResultAssert = await pipe(
      handler.configureFieldDisplay({
      mode: d,
      field: 'title',
      config: 'truncated',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).mode).toBe(d);
        return output;
      }),
    )();
    expect(E.isRight(configureFieldDisplayResultAssert)).toBe(true);

  });

  it('invariant 2: after configureFieldDisplay, renderInMode behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = displayModeHandler;

    const d = 'u-test-invariant-001';

    // setup: configureFieldDisplay -> ok
    const configureFieldDisplayResultSetup = await pipe(
      handler.configureFieldDisplay({
      mode: d,
      field: 'title',
      config: 'truncated',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).mode).toBe(d);
        return output;
      }),
    )();
    expect(E.isRight(configureFieldDisplayResultSetup)).toBe(true);

    // assert: renderInMode -> ok
    const renderInModeResultAssert = await pipe(
      handler.renderInMode({
      mode: d,
      entity: 'article-1',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).output).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(renderInModeResultAssert)).toBe(true);

  });

});
