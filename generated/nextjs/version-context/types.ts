// VersionContext — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for VersionContext
export interface VersionContextStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

// --- push ---

export interface VersionContextPushInput {
  readonly user: string;
  readonly space_id: string;
}

export interface VersionContextPushOutputOk {
  readonly variant: 'ok';
  readonly context: string;
}

export interface VersionContextPushOutputConflict {
  readonly variant: 'conflict';
  readonly existing: string;
}

export type VersionContextPushOutput =
  | VersionContextPushOutputOk
  | VersionContextPushOutputConflict;

// --- pop ---

export interface VersionContextPopInput {
  readonly user: string;
  readonly space_id: string;
}

export interface VersionContextPopOutputOk {
  readonly variant: 'ok';
  readonly context: string;
}

export type VersionContextPopOutput = VersionContextPopOutputOk;

// --- get ---

export interface VersionContextGetInput {
  readonly user: string;
}

export interface VersionContextGetOutputOk {
  readonly variant: 'ok';
  readonly stack: readonly string[];
}

export interface VersionContextGetOutputNoContext {
  readonly variant: 'no_context';
  readonly user: string;
}

export type VersionContextGetOutput =
  | VersionContextGetOutputOk
  | VersionContextGetOutputNoContext;

// --- resolve_for ---

export interface VersionContextResolveForInput {
  readonly user: string;
  readonly entity_id: string;
}

export interface VersionContextResolveForOutputOk {
  readonly variant: 'ok';
  readonly fields: string;
  readonly source_space: string;
}

export type VersionContextResolveForOutput = VersionContextResolveForOutputOk;

// --- Variant constructors ---

export const pushOk = (context: string): VersionContextPushOutput =>
  ({ variant: 'ok', context } as VersionContextPushOutput);
export const pushConflict = (existing: string): VersionContextPushOutput =>
  ({ variant: 'conflict', existing } as VersionContextPushOutput);

export const popOk = (context: string): VersionContextPopOutput =>
  ({ variant: 'ok', context } as VersionContextPopOutput);

export const getOk = (stack: readonly string[]): VersionContextGetOutput =>
  ({ variant: 'ok', stack } as VersionContextGetOutput);
export const getNoContext = (user: string): VersionContextGetOutput =>
  ({ variant: 'no_context', user } as VersionContextGetOutput);

export const resolveForOk = (fields: string, source_space: string): VersionContextResolveForOutput =>
  ({ variant: 'ok', fields, source_space, source: source_space } as VersionContextResolveForOutput);
