// Permission — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as O from 'fp-ts/Option';

// Storage interface for Permission
export interface PermissionStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface PermissionGrantInput {
  readonly who: string;
  readonly where: string;
  readonly what: string;
  readonly condition: O.Option<string>;
  readonly grantedBy: string;
}

export interface PermissionGrantOutputGranted {
  readonly variant: 'granted';
  readonly permission: string;
}

export interface PermissionGrantOutputAlready_granted {
  readonly variant: 'already_granted';
  readonly permission: string;
}

export type PermissionGrantOutput = PermissionGrantOutputGranted | PermissionGrantOutputAlready_granted;

export interface PermissionRevokeInput {
  readonly permission: string;
}

export interface PermissionRevokeOutputRevoked {
  readonly variant: 'revoked';
  readonly permission: string;
}

export interface PermissionRevokeOutputNot_found {
  readonly variant: 'not_found';
  readonly permission: string;
}

export type PermissionRevokeOutput = PermissionRevokeOutputRevoked | PermissionRevokeOutputNot_found;

export interface PermissionCheckInput {
  readonly who: string;
  readonly where: string;
  readonly what: string;
}

export interface PermissionCheckOutputAllowed {
  readonly variant: 'allowed';
  readonly permission: string;
}

export interface PermissionCheckOutputDenied {
  readonly variant: 'denied';
  readonly who: string;
  readonly where: string;
  readonly what: string;
}

export type PermissionCheckOutput = PermissionCheckOutputAllowed | PermissionCheckOutputDenied;

// --- Variant constructors ---

export const grantGranted = (permission: string): PermissionGrantOutput => ({ variant: 'granted', permission } as PermissionGrantOutput);
export const grantAlready_granted = (permission: string): PermissionGrantOutput => ({ variant: 'already_granted', permission } as PermissionGrantOutput);

export const revokeRevoked = (permission: string): PermissionRevokeOutput => ({ variant: 'revoked', permission } as PermissionRevokeOutput);
export const revokeNot_found = (permission: string): PermissionRevokeOutput => ({ variant: 'not_found', permission } as PermissionRevokeOutput);

export const checkAllowed = (permission: string): PermissionCheckOutput => ({ variant: 'allowed', permission } as PermissionCheckOutput);
export const checkDenied = (who: string, where: string, what: string): PermissionCheckOutput => ({ variant: 'denied', who, where, what } as PermissionCheckOutput);

