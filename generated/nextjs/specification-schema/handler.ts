// SpecificationSchema — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  SpecificationSchemaStorage,
  SpecificationSchemaDefineInput,
  SpecificationSchemaDefineOutput,
  SpecificationSchemaInstantiateInput,
  SpecificationSchemaInstantiateOutput,
  SpecificationSchemaValidateInput,
  SpecificationSchemaValidateOutput,
  SpecificationSchemaList_by_categoryInput,
  SpecificationSchemaList_by_categoryOutput,
  SpecificationSchemaSearchInput,
  SpecificationSchemaSearchOutput,
} from './types.js';

import {
  defineOk,
  defineInvalid,
  instantiateOk,
  instantiateInvalid,
  validateOk,
  validateInvalid,
  list_by_categoryOk,
  searchOk,
} from './types.js';

export interface SpecificationSchemaError {
  readonly code: string;
  readonly message: string;
}

export interface SpecificationSchemaHandler {
  readonly define: (
    input: SpecificationSchemaDefineInput,
    storage: SpecificationSchemaStorage,
  ) => TE.TaskEither<SpecificationSchemaError, SpecificationSchemaDefineOutput>;
  readonly instantiate: (
    input: SpecificationSchemaInstantiateInput,
    storage: SpecificationSchemaStorage,
  ) => TE.TaskEither<SpecificationSchemaError, SpecificationSchemaInstantiateOutput>;
  readonly validate: (
    input: SpecificationSchemaValidateInput,
    storage: SpecificationSchemaStorage,
  ) => TE.TaskEither<SpecificationSchemaError, SpecificationSchemaValidateOutput>;
  readonly list_by_category: (
    input: SpecificationSchemaList_by_categoryInput,
    storage: SpecificationSchemaStorage,
  ) => TE.TaskEither<SpecificationSchemaError, SpecificationSchemaList_by_categoryOutput>;
  readonly search: (
    input: SpecificationSchemaSearchInput,
    storage: SpecificationSchemaStorage,
  ) => TE.TaskEither<SpecificationSchemaError, SpecificationSchemaSearchOutput>;
}

// --- Implementation ---

export const specificationSchemaHandler: SpecificationSchemaHandler = {
  define: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('specificationschema', input.name, { name: input.name, category: input.category, pattern_type: input.pattern_type, template_text: input.template_text, formal_language: input.formal_language, parameters: input.parameters });
          return defineOk(input.name);
        },
        (error): SpecificationSchemaError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  instantiate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('specificationschema', input.schema, { schema: input.schema, parameter_values: input.parameter_values, target_symbol: input.target_symbol });
          return instantiateOk(input.schema);
        },
        (error): SpecificationSchemaError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  validate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('specificationschema', input.schema);
          return validateOk((record as any).valid, (record as any).instantiated_preview);
        },
        (error): SpecificationSchemaError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  list_by_category: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('specificationschema');
          return list_by_categoryOk([]);
        },
        (error): SpecificationSchemaError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  search: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('specificationschema');
          return searchOk([]);
        },
        (error): SpecificationSchemaError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};