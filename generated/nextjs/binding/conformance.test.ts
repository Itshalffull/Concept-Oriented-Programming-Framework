// Binding — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { bindingHandler } from './handler.js';
import type { BindingStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): BindingStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Binding conformance', () => {
  it('invariant 1: after bind, sync, bind behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = bindingHandler;

    const b = 'u-test-invariant-001';
    const c = 'u-test-invariant-002';
    const b2 = 'u-test-invariant-003';
    const c2 = 'u-test-invariant-004';

    // setup: bind -> ok
    const bindResultSetup = await pipe(
      handler.bind({
      binding: b,
      concept: c,
      mode: 'static',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).binding).toBe(b);
        return output;
      }),
    )();
    expect(E.isRight(bindResultSetup)).toBe(true);

    // assert: sync -> ok
    const syncResultAssert = await pipe(
      handler.sync({
      binding: b,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).binding).toBe(b);
        return output;
      }),
    )();
    expect(E.isRight(syncResultAssert)).toBe(true);

    // assert: bind -> invalid
    const bindResultAssert = await pipe(
      handler.bind({
      binding: b2,
      concept: c2,
      mode: 'invalid-mode',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('invalid');
        expect((output as any).message).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(bindResultAssert)).toBe(true);

  });

});
