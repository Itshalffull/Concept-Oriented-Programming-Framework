// Conformance — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { conformanceHandler } from './handler.js';
import type { ConformanceStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): ConformanceStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Conformance conformance', () => {
  it('invariant 1: after generate, verify, matrix behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = conformanceHandler;

    let c: any = 'u-test-invariant-001';
    let vs: any = 'u-test-invariant-002';
    let reqs: any = 'u-test-invariant-003';
    let m: any = 'u-test-invariant-004';

    // setup: generate -> ok
    const generateResultSetup = await pipe(
      handler.generate({
      concept: 'password',
      specPath: './specs/password.concept',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        c = (output as any).suite;
        vs = (output as any).testVectors;
        return output;
      }),
    )();
    expect(E.isRight(generateResultSetup)).toBe(true);

    // setup: verify -> ok
    const verifyResultSetup = await pipe(
      handler.verify({
      suite: c,
      language: 'typescript',
      artifactLocation: '.clef-artifacts/ts/password',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).passed).toBe(12);
        expect((output as any).total).toBe(12);
        reqs = (output as any).coveredRequirements;
        return output;
      }),
    )();
    expect(E.isRight(verifyResultSetup)).toBe(true);

    // assert: matrix -> ok
    const matrixResultAssert = await pipe(
      handler.matrix({
      concepts: undefined,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        m = (output as any).matrix;
        return output;
      }),
    )();
    expect(E.isRight(matrixResultAssert)).toBe(true);

  });

});
