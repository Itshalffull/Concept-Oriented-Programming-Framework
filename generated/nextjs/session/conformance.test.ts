// Session — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { sessionHandler } from './handler.js';
import type { SessionStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): SessionStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Session conformance', () => {
  it('invariant 1: after create, validate behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = sessionHandler;

    const s = 'u-test-invariant-001';
    const t = 'u-test-invariant-002';

    // setup: create -> ok
    const createResultSetup = await pipe(
      handler.create({
      session: s,
      userId: 'alice',
      device: 'mobile',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).token).toBe(t);
        return output;
      }),
    )();
    expect(E.isRight(createResultSetup)).toBe(true);

    // assert: validate -> ok
    const validateResultAssert = await pipe(
      handler.validate({
      session: s,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).valid).toBe(true);
        return output;
      }),
    )();
    expect(E.isRight(validateResultAssert)).toBe(true);

  });

  it('invariant 2: after create, getContext behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = sessionHandler;

    const s = 'u-test-invariant-001';
    const t = 'u-test-invariant-002';

    // setup: create -> ok
    const createResultSetup = await pipe(
      handler.create({
      session: s,
      userId: 'alice',
      device: 'mobile',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).token).toBe(t);
        return output;
      }),
    )();
    expect(E.isRight(createResultSetup)).toBe(true);

    // assert: getContext -> ok
    const getContextResultAssert = await pipe(
      handler.getContext({
      session: s,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).userId).toBe('alice');
        expect((output as any).device).toBe('mobile');
        return output;
      }),
    )();
    expect(E.isRight(getContextResultAssert)).toBe(true);

  });

  it('invariant 3: after create, destroy, validate behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = sessionHandler;

    const s = 'u-test-invariant-001';
    const t = 'u-test-invariant-002';
    const m = 'u-test-invariant-003';

    // setup: create -> ok
    const createResultSetup = await pipe(
      handler.create({
      session: s,
      userId: 'alice',
      device: 'mobile',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).token).toBe(t);
        return output;
      }),
    )();
    expect(E.isRight(createResultSetup)).toBe(true);

    // setup: destroy -> ok
    const destroyResultSetup = await pipe(
      handler.destroy({
      session: s,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).session).toBe(s);
        return output;
      }),
    )();
    expect(E.isRight(destroyResultSetup)).toBe(true);

    // assert: validate -> notfound
    const validateResultAssert = await pipe(
      handler.validate({
      session: s,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('notfound');
        expect((output as any).message).toBe(m);
        return output;
      }),
    )();
    expect(E.isRight(validateResultAssert)).toBe(true);

  });

  it('invariant 4: after create, create, destroyAll, validate behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = sessionHandler;

    const s1 = 'u-test-invariant-001';
    const t1 = 'u-test-invariant-002';
    const s2 = 'u-test-invariant-003';
    const t2 = 'u-test-invariant-004';
    const m1 = 'u-test-invariant-005';

    // setup: create -> ok
    const createResultSetup = await pipe(
      handler.create({
      session: s1,
      userId: 'alice',
      device: 'mobile',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).token).toBe(t1);
        return output;
      }),
    )();
    expect(E.isRight(createResultSetup)).toBe(true);

    // setup: create -> ok
    const createResultSetup = await pipe(
      handler.create({
      session: s2,
      userId: 'alice',
      device: 'desktop',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).token).toBe(t2);
        return output;
      }),
    )();
    expect(E.isRight(createResultSetup)).toBe(true);

    // setup: destroyAll -> ok
    const destroyAllResultSetup = await pipe(
      handler.destroyAll({
      userId: 'alice',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).userId).toBe('alice');
        return output;
      }),
    )();
    expect(E.isRight(destroyAllResultSetup)).toBe(true);

    // assert: validate -> notfound
    const validateResultAssert = await pipe(
      handler.validate({
      session: s1,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('notfound');
        expect((output as any).message).toBe(m1);
        return output;
      }),
    )();
    expect(E.isRight(validateResultAssert)).toBe(true);

  });

});
