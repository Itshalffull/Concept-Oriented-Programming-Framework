// AgentMemory — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  AgentMemoryStorage,
  AgentMemoryRememberInput,
  AgentMemoryRememberOutput,
  AgentMemoryRecallInput,
  AgentMemoryRecallOutput,
  AgentMemoryEditWorkingMemoryInput,
  AgentMemoryEditWorkingMemoryOutput,
  AgentMemoryForgetInput,
  AgentMemoryForgetOutput,
  AgentMemoryConsolidateInput,
  AgentMemoryConsolidateOutput,
  AgentMemorySearchInput,
  AgentMemorySearchOutput,
  AgentMemoryGetWorkingMemoryInput,
  AgentMemoryGetWorkingMemoryOutput,
} from './types.js';

import {
  rememberOk,
  rememberInvalid,
  recallOk,
  recallEmpty,
  editWorkingMemoryOk,
  editWorkingMemoryNotfound,
  forgetOk,
  forgetNotfound,
  consolidateOk,
  consolidateEmpty,
  searchOk,
  searchEmpty,
  getWorkingMemoryOk,
} from './types.js';

export interface AgentMemoryError {
  readonly code: string;
  readonly message: string;
}

export interface AgentMemoryHandler {
  readonly remember: (
    input: AgentMemoryRememberInput,
    storage: AgentMemoryStorage,
  ) => TE.TaskEither<AgentMemoryError, AgentMemoryRememberOutput>;
  readonly recall: (
    input: AgentMemoryRecallInput,
    storage: AgentMemoryStorage,
  ) => TE.TaskEither<AgentMemoryError, AgentMemoryRecallOutput>;
  readonly editWorkingMemory: (
    input: AgentMemoryEditWorkingMemoryInput,
    storage: AgentMemoryStorage,
  ) => TE.TaskEither<AgentMemoryError, AgentMemoryEditWorkingMemoryOutput>;
  readonly forget: (
    input: AgentMemoryForgetInput,
    storage: AgentMemoryStorage,
  ) => TE.TaskEither<AgentMemoryError, AgentMemoryForgetOutput>;
  readonly consolidate: (
    input: AgentMemoryConsolidateInput,
    storage: AgentMemoryStorage,
  ) => TE.TaskEither<AgentMemoryError, AgentMemoryConsolidateOutput>;
  readonly search: (
    input: AgentMemorySearchInput,
    storage: AgentMemoryStorage,
  ) => TE.TaskEither<AgentMemoryError, AgentMemorySearchOutput>;
  readonly getWorkingMemory: (
    input: AgentMemoryGetWorkingMemoryInput,
    storage: AgentMemoryStorage,
  ) => TE.TaskEither<AgentMemoryError, AgentMemoryGetWorkingMemoryOutput>;
}

// --- Implementation ---

export const agentMemoryHandler: AgentMemoryHandler = {
  remember: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentmemory', input.content, { content: input.content, memory_type: input.memory_type, metadata: input.metadata });
          return rememberOk(input.content);
        },
        (error): AgentMemoryError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  recall: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentmemory', input.query, { query: input.query, memory_type: input.memory_type, k: input.k });
          return recallOk([]);
        },
        (error): AgentMemoryError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  editWorkingMemory: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('agentmemory', input.label),
        (error): AgentMemoryError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(editWorkingMemoryNotfound(`${input.label} not found`)),
            (existing) =>
              TE.tryCatch(
                async () => {
                  const updated = { ...existing, label: input.label, new_content: input.new_content };
                  await storage.put('agentmemory', input.label, updated);
                  return editWorkingMemoryOk(input.label);
                },
                (error): AgentMemoryError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  forget: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentmemory', input.entry, { entry: input.entry });
          return forgetOk();
        },
        (error): AgentMemoryError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  consolidate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentmemory', input.id, {  });
          return consolidateOk(0, 0, 0);
        },
        (error): AgentMemoryError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  search: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('agentmemory');
          return searchOk([]);
        },
        (error): AgentMemoryError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getWorkingMemory: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('agentmemory', input.id);
          return getWorkingMemoryOk((record as any).blocks);
        },
        (error): AgentMemoryError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
