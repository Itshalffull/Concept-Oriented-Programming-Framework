// BrowserPermission — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  BrowserPermissionStorage,
  BrowserPermissionResolveInput,
  BrowserPermissionResolveOutput,
  BrowserPermissionValidateInput,
  BrowserPermissionValidateOutput,
  BrowserPermissionListAvailableInput,
  BrowserPermissionListAvailableOutput,
  BrowserPermissionGetStoreWarningsInput,
  BrowserPermissionGetStoreWarningsOutput,
} from './types.js';

import {
  resolveOk,
  resolveUnsupported,
  validateOk,
  listAvailableOk,
  getStoreWarningsOk,
} from './types.js';

export interface BrowserPermissionError {
  readonly code: string;
  readonly message: string;
}

export interface BrowserPermissionHandler {
  readonly resolve: (
    input: BrowserPermissionResolveInput,
    storage: BrowserPermissionStorage,
  ) => TE.TaskEither<BrowserPermissionError, BrowserPermissionResolveOutput>;
  readonly validate: (
    input: BrowserPermissionValidateInput,
    storage: BrowserPermissionStorage,
  ) => TE.TaskEither<BrowserPermissionError, BrowserPermissionValidateOutput>;
  readonly listAvailable: (
    input: BrowserPermissionListAvailableInput,
    storage: BrowserPermissionStorage,
  ) => TE.TaskEither<BrowserPermissionError, BrowserPermissionListAvailableOutput>;
  readonly getStoreWarnings: (
    input: BrowserPermissionGetStoreWarningsInput,
    storage: BrowserPermissionStorage,
  ) => TE.TaskEither<BrowserPermissionError, BrowserPermissionGetStoreWarningsOutput>;
}

// --- Implementation ---

export const browserPermissionHandler: BrowserPermissionHandler = {
  resolve: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('browserpermission', input.permission);
          return resolveOk((record as any).browserPermissions);
        },
        (error): BrowserPermissionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  validate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('browserpermission', input.permissions);
          return validateOk((record as any).valid, (record as any).conflicts);
        },
        (error): BrowserPermissionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  listAvailable: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('browserpermission');
          return listAvailableOk(input.browser);
        },
        (error): BrowserPermissionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getStoreWarnings: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('browserpermission', input.permissions);
          return getStoreWarningsOk((record as any).warnings);
        },
        (error): BrowserPermissionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
