// VoteEscrow — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  VoteEscrowStorage,
  VoteEscrowConfigureInput,
  VoteEscrowConfigureOutput,
  VoteEscrowLockInput,
  VoteEscrowLockOutput,
  VoteEscrowExtendLockInput,
  VoteEscrowExtendLockOutput,
  VoteEscrowGetWeightInput,
  VoteEscrowGetWeightOutput,
  VoteEscrowWithdrawInput,
  VoteEscrowWithdrawOutput,
} from './types.js';

import {
  configureConfigured,
  lockLocked,
  lockExceeds_max,
  extendLockExtended,
  getWeightWeight,
  getWeightExpired,
  withdrawWithdrawn,
  withdrawStill_locked,
} from './types.js';

export interface VoteEscrowError {
  readonly code: string;
  readonly message: string;
}

export interface VoteEscrowHandler {
  readonly configure: (
    input: VoteEscrowConfigureInput,
    storage: VoteEscrowStorage,
  ) => TE.TaskEither<VoteEscrowError, VoteEscrowConfigureOutput>;
  readonly lock: (
    input: VoteEscrowLockInput,
    storage: VoteEscrowStorage,
  ) => TE.TaskEither<VoteEscrowError, VoteEscrowLockOutput>;
  readonly extendLock: (
    input: VoteEscrowExtendLockInput,
    storage: VoteEscrowStorage,
  ) => TE.TaskEither<VoteEscrowError, VoteEscrowExtendLockOutput>;
  readonly getWeight: (
    input: VoteEscrowGetWeightInput,
    storage: VoteEscrowStorage,
  ) => TE.TaskEither<VoteEscrowError, VoteEscrowGetWeightOutput>;
  readonly withdraw: (
    input: VoteEscrowWithdrawInput,
    storage: VoteEscrowStorage,
  ) => TE.TaskEither<VoteEscrowError, VoteEscrowWithdrawOutput>;
}

// --- Implementation ---

export const voteEscrowHandler: VoteEscrowHandler = {
  configure: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('voteescrow', input.maxLockDurationDays, { maxLockDurationDays: input.maxLockDurationDays });
          return configureOk();
        },
        (error): VoteEscrowError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  lock: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('voteescrow', input.participant, { participant: input.participant, amount: input.amount, lockDurationDays: input.lockDurationDays });
          return lockOk();
        },
        (error): VoteEscrowError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  extendLock: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('voteescrow', input.lock, { lock: input.lock, additionalDays: input.additionalDays });
          return extendLockOk();
        },
        (error): VoteEscrowError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getWeight: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('voteescrow', input.lock);
          return getWeightOk();
        },
        (error): VoteEscrowError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  withdraw: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('voteescrow', input.lock, { lock: input.lock });
          return withdrawOk();
        },
        (error): VoteEscrowError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
