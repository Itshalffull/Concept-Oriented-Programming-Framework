// Grouping — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { groupingHandler } from './handler.js';
import type { GroupingStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): GroupingStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Grouping conformance', () => {
  it('invariant 1: after group, classify behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = groupingHandler;

    const g = 'u-test-invariant-001';
    const gs = 'u-test-invariant-002';

    // setup: group -> ok
    const groupResultSetup = await pipe(
      handler.group({
      items: undefined,
      config: 'per-concept',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).grouping).toBe(g);
        expect((output as any).groups).toBe(gs);
        expect((output as any).groupCount).toBe(3);
        return output;
      }),
    )();
    expect(E.isRight(groupResultSetup)).toBe(true);

    // assert: classify -> ok
    const classifyResultAssert = await pipe(
      handler.classify({
      actionName: 'create',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).crudRole).toBe('create');
        expect((output as any).intent).toBe('write');
        expect((output as any).eventProducing).toBe(true);
        expect((output as any).eventVerb).toBe('created');
        expect((output as any).mcpType).toBe('tool');
        return output;
      }),
    )();
    expect(E.isRight(classifyResultAssert)).toBe(true);

  });

});
