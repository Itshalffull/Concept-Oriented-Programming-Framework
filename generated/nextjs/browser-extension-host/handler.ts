// BrowserExtensionHost — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  BrowserExtensionHostStorage,
  BrowserExtensionHostActivateInput,
  BrowserExtensionHostActivateOutput,
  BrowserExtensionHostDeactivateInput,
  BrowserExtensionHostDeactivateOutput,
  BrowserExtensionHostInjectContentScriptInput,
  BrowserExtensionHostInjectContentScriptOutput,
  BrowserExtensionHostOnTabUpdateInput,
  BrowserExtensionHostOnTabUpdateOutput,
  BrowserExtensionHostOnBrowserEventInput,
  BrowserExtensionHostOnBrowserEventOutput,
  BrowserExtensionHostGetStatusInput,
  BrowserExtensionHostGetStatusOutput,
} from './types.js';

import {
  activateOk,
  activateError,
  deactivateOk,
  deactivateNotfound,
  injectContentScriptOk,
  injectContentScriptNotfound,
  injectContentScriptForbidden,
  onTabUpdateOk,
  onBrowserEventOk,
  getStatusOk,
  getStatusNotfound,
} from './types.js';

export interface BrowserExtensionHostError {
  readonly code: string;
  readonly message: string;
}

export interface BrowserExtensionHostHandler {
  readonly activate: (
    input: BrowserExtensionHostActivateInput,
    storage: BrowserExtensionHostStorage,
  ) => TE.TaskEither<BrowserExtensionHostError, BrowserExtensionHostActivateOutput>;
  readonly deactivate: (
    input: BrowserExtensionHostDeactivateInput,
    storage: BrowserExtensionHostStorage,
  ) => TE.TaskEither<BrowserExtensionHostError, BrowserExtensionHostDeactivateOutput>;
  readonly injectContentScript: (
    input: BrowserExtensionHostInjectContentScriptInput,
    storage: BrowserExtensionHostStorage,
  ) => TE.TaskEither<BrowserExtensionHostError, BrowserExtensionHostInjectContentScriptOutput>;
  readonly onTabUpdate: (
    input: BrowserExtensionHostOnTabUpdateInput,
    storage: BrowserExtensionHostStorage,
  ) => TE.TaskEither<BrowserExtensionHostError, BrowserExtensionHostOnTabUpdateOutput>;
  readonly onBrowserEvent: (
    input: BrowserExtensionHostOnBrowserEventInput,
    storage: BrowserExtensionHostStorage,
  ) => TE.TaskEither<BrowserExtensionHostError, BrowserExtensionHostOnBrowserEventOutput>;
  readonly getStatus: (
    input: BrowserExtensionHostGetStatusInput,
    storage: BrowserExtensionHostStorage,
  ) => TE.TaskEither<BrowserExtensionHostError, BrowserExtensionHostGetStatusOutput>;
}

// --- Implementation ---

export const browserExtensionHostHandler: BrowserExtensionHostHandler = {
  activate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browserextensionhost', input.extension, { extension: input.extension });
          return activateOk(input.extension);
        },
        (error): BrowserExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  deactivate: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('browserextensionhost', input.extension),
        (error): BrowserExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(deactivateNotfound(`${input.extension} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('browserextensionhost', input.extension);
                  return deactivateOk();
                },
                (error): BrowserExtensionHostError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  injectContentScript: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browserextensionhost', input.extension, { extension: input.extension, tabId: input.tabId, scriptId: input.scriptId });
          return injectContentScriptOk(input.extension);
        },
        (error): BrowserExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  onTabUpdate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browserextensionhost', input.tabId, { tabId: input.tabId, url: input.url, status: input.status });
          return onTabUpdateOk(input.tabId);
        },
        (error): BrowserExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  onBrowserEvent: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browserextensionhost', input.eventType, { eventType: input.eventType, data: input.data });
          return onBrowserEventOk(true);
        },
        (error): BrowserExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getStatus: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('browserextensionhost', input.extension),
        (error): BrowserExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getStatusNotfound(`${input.extension} not found`)),
            (found) => TE.right(getStatusOk(input.extension, input.extension)),
          ),
        ),
      ),
    ),
};
