// WidgetResolver — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { widgetResolverHandler } from './handler.js';
import type { WidgetResolverStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): WidgetResolverStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('WidgetResolver conformance', () => {
  it('invariant 1: after resolve, explain behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = widgetResolverHandler;

    const r = 'u-test-invariant-001';

    // setup: resolve -> ok
    const resolveResultSetup = await pipe(
      handler.resolve({
      resolver: r,
      element: '{ "interactorType": "single-choice", "optionCount": 4 }',
      context: '{ "platform": "browser", "viewport": "desktop" }',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).resolver).toBe(r);
        expect((output as any).widget).toBe('radio-group');
        expect((output as any).score).toBe(_);
        expect((output as any).reason).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(resolveResultSetup)).toBe(true);

    // assert: explain -> ok
    const explainResultAssert = await pipe(
      handler.explain({
      resolver: r,
      element: '{ "interactorType": "single-choice", "optionCount": 4 }',
      context: '{ "platform": "browser", "viewport": "desktop" }',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).resolver).toBe(r);
        expect((output as any).explanation).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(explainResultAssert)).toBe(true);

  });

  it('invariant 2: after override, resolve behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = widgetResolverHandler;

    const r = 'u-test-invariant-001';

    // setup: override -> ok
    const overrideResultSetup = await pipe(
      handler.override({
      resolver: r,
      element: '{ "kind": "selection-single" }',
      widget: 'custom-picker',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).resolver).toBe(r);
        return output;
      }),
    )();
    expect(E.isRight(overrideResultSetup)).toBe(true);

    // assert: resolve -> ok
    const resolveResultAssert = await pipe(
      handler.resolve({
      resolver: r,
      element: '{ "kind": "selection-single" }',
      context: _,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).resolver).toBe(r);
        expect((output as any).widget).toBe('custom-picker');
        expect((output as any).score).toBe(_);
        expect((output as any).reason).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(resolveResultAssert)).toBe(true);

  });

});
