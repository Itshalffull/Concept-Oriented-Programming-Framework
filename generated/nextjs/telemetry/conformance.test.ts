// Telemetry — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { telemetryHandler } from './handler.js';
import type { TelemetryStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): TelemetryStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Telemetry conformance', () => {
  it('invariant 1: after configure, deployMarker behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = telemetryHandler;

    const t = 'u-test-invariant-001';
    const m = 'u-test-invariant-002';

    // setup: configure -> ok
    const configureResultSetup = await pipe(
      handler.configure({
      concept: 'User',
      endpoint: 'http://otel:4317',
      samplingRate: 0.5,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).config).toBe(t);
        return output;
      }),
    )();
    expect(E.isRight(configureResultSetup)).toBe(true);

    // assert: deployMarker -> ok
    const deployMarkerResultAssert = await pipe(
      handler.deployMarker({
      kit: 'auth',
      version: '1.0.0',
      environment: 'staging',
      status: 'started',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).marker).toBe(m);
        return output;
      }),
    )();
    expect(E.isRight(deployMarkerResultAssert)).toBe(true);

  });

});
