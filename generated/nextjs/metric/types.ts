// Metric — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as O from 'fp-ts/Option';

// Storage interface for Metric
export interface MetricStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface MetricDefineInput {
  readonly name: string;
  readonly unit: O.Option<string>;
  readonly source: string;
}

export interface MetricDefineOutput {
  readonly variant: 'defined';
  readonly metric: string;
}

export interface MetricUpdateInput {
  readonly metric: string;
  readonly value: number;
}

export interface MetricUpdateOutputUpdated {
  readonly variant: 'updated';
  readonly metric: string;
  readonly previousValue: number;
}

export interface MetricUpdateOutputThreshold_crossed {
  readonly variant: 'threshold_crossed';
  readonly metric: string;
  readonly threshold: string;
  readonly direction: string;
}

export type MetricUpdateOutput = MetricUpdateOutputUpdated | MetricUpdateOutputThreshold_crossed;

export interface MetricSetThresholdInput {
  readonly metric: string;
  readonly level: string;
  readonly operator: string;
  readonly value: number;
}

export interface MetricSetThresholdOutput {
  readonly variant: 'threshold_set';
  readonly metric: string;
}

export interface MetricEvaluateInput {
  readonly metric: string;
}

export interface MetricEvaluateOutputWithin_bounds {
  readonly variant: 'within_bounds';
  readonly metric: string;
}

export interface MetricEvaluateOutputThreshold_breached {
  readonly variant: 'threshold_breached';
  readonly metric: string;
  readonly breachedThresholds: string;
}

export type MetricEvaluateOutput = MetricEvaluateOutputWithin_bounds | MetricEvaluateOutputThreshold_breached;

// --- Variant constructors ---

export const defineDefined = (metric: string): MetricDefineOutput => ({ variant: 'defined', metric } as MetricDefineOutput);

export const updateUpdated = (metric: string, previousValue: number): MetricUpdateOutput => ({ variant: 'updated', metric, previousValue } as MetricUpdateOutput);
export const updateThreshold_crossed = (metric: string, threshold: string, direction: string): MetricUpdateOutput => ({ variant: 'threshold_crossed', metric, threshold, direction } as MetricUpdateOutput);

export const setThresholdThreshold_set = (metric: string): MetricSetThresholdOutput => ({ variant: 'threshold_set', metric } as MetricSetThresholdOutput);

export const evaluateWithin_bounds = (metric: string): MetricEvaluateOutput => ({ variant: 'within_bounds', metric } as MetricEvaluateOutput);
export const evaluateThreshold_breached = (metric: string, breachedThresholds: string): MetricEvaluateOutput => ({ variant: 'threshold_breached', metric, breachedThresholds } as MetricEvaluateOutput);

