// Pathauto — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { pathautoHandler } from './handler.js';
import type { PathautoStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): PathautoStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Pathauto conformance', () => {
  it('invariant 1: after generateAlias, cleanString behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = pathautoHandler;

    const p = 'u-test-invariant-001';
    const a = 'u-test-invariant-002';

    // setup: generateAlias -> ok
    const generateAliasResultSetup = await pipe(
      handler.generateAlias({
      pattern: p,
      entity: 'My Example Page',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).alias).toBe(a);
        return output;
      }),
    )();
    expect(E.isRight(generateAliasResultSetup)).toBe(true);

    // assert: cleanString -> ok
    const cleanStringResultAssert = await pipe(
      handler.cleanString({
      input: 'My Example Page',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).cleaned).toBe(a);
        return output;
      }),
    )();
    expect(E.isRight(cleanStringResultAssert)).toBe(true);

  });

});
