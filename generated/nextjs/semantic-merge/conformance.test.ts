// SemanticMerge — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { semanticMergeHandler } from './handler.js';
import type { SemanticMergeStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): SemanticMergeStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('SemanticMerge conformance', () => {
  it('invariant 1: after execute, execute behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = semanticMergeHandler;

    const b = 'u-test-invariant-001';
    const t = 'u-test-invariant-002';
    const o = 'u-test-invariant-003';

    // setup: execute -> clean
    const executeResultSetup = await pipe(
      handler.execute({
      base: b,
      ours: b,
      theirs: t,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('clean');
        expect((output as any).result).toBe(t);
        return output;
      }),
    )();
    expect(E.isRight(executeResultSetup)).toBe(true);

    // assert: execute -> clean
    const executeResultAssert = await pipe(
      handler.execute({
      base: b,
      ours: o,
      theirs: b,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('clean');
        expect((output as any).result).toBe(o);
        return output;
      }),
    )();
    expect(E.isRight(executeResultAssert)).toBe(true);

  });

});
