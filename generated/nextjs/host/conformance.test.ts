// Host — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { hostHandler } from './handler.js';
import type { HostStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): HostStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Host conformance', () => {
  it('invariant 1: after mount, unmount behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = hostHandler;

    const w = 'u-test-invariant-001';

    // setup: mount -> ok
    const mountResultSetup = await pipe(
      handler.mount({
      host: w,
      concept: 'urn:app/Article',
      view: 'list',
      level: 0,
      zone: 'primary',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).host).toBe(w);
        return output;
      }),
    )();
    expect(E.isRight(mountResultSetup)).toBe(true);

    // assert: unmount -> ok
    const unmountResultAssert = await pipe(
      handler.unmount({
      host: w,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).host).toBe(w);
        expect((output as any).machines).toBe(_);
        expect((output as any).binding).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(unmountResultAssert)).toBe(true);

  });

});
