// Contract — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ContractStorage,
  ContractDefineInput,
  ContractDefineOutput,
  ContractVerifyInput,
  ContractVerifyOutput,
  ContractComposeInput,
  ContractComposeOutput,
  ContractDischargeInput,
  ContractDischargeOutput,
  ContractListInput,
  ContractListOutput,
} from './types.js';

import {
  defineOk,
  defineInvalid,
  verifyOk,
  verifyIncompatible,
  composeOk,
  composeIncompatible,
  dischargeOk,
  dischargeNotfound,
  listOk,
} from './types.js';

export interface ContractError {
  readonly code: string;
  readonly message: string;
}

export interface ContractHandler {
  readonly define: (
    input: ContractDefineInput,
    storage: ContractStorage,
  ) => TE.TaskEither<ContractError, ContractDefineOutput>;
  readonly verify: (
    input: ContractVerifyInput,
    storage: ContractStorage,
  ) => TE.TaskEither<ContractError, ContractVerifyOutput>;
  readonly compose: (
    input: ContractComposeInput,
    storage: ContractStorage,
  ) => TE.TaskEither<ContractError, ContractComposeOutput>;
  readonly discharge: (
    input: ContractDischargeInput,
    storage: ContractStorage,
  ) => TE.TaskEither<ContractError, ContractDischargeOutput>;
  readonly list: (
    input: ContractListInput,
    storage: ContractStorage,
  ) => TE.TaskEither<ContractError, ContractListOutput>;
}

// --- Implementation ---

export const contractHandler: ContractHandler = {
  define: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('contract', input.name, { name: input.name, source_concept: input.source_concept, target_concept: input.target_concept, assumptions: input.assumptions, guarantees: input.guarantees });
          return defineOk(input.name);
        },
        (error): ContractError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  verify: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('contract', input.contract);
          return verifyOk(input.contract, (record as any).compatible);
        },
        (error): ContractError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  compose: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('contract', input.contracts, { contracts: input.contracts });
          return composeOk(input.contracts, []);
        },
        (error): ContractError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  discharge: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('contract', input.contract, { contract: input.contract, assumption_ref: input.assumption_ref, evidence_ref: input.evidence_ref });
          return dischargeOk(input.contract, []);
        },
        (error): ContractError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  list: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('contract');
          return listOk([]);
        },
        (error): ContractError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};