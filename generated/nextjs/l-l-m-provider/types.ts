// LLMProvider — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as O from 'fp-ts/Option';

// Storage interface for LLMProvider
export interface LLMProviderStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface LLMProviderRegisterInput {
  readonly provider_id: string;
  readonly model_id: string;
  readonly credentials: Buffer;
  readonly config: { readonly temperature: number; readonly top_p: number; readonly max_tokens: number; readonly stop_sequences: readonly string[] };
  readonly capabilities: ReadonlySet<string>;
}

export interface LLMProviderRegisterOutputOk {
  readonly variant: 'ok';
  readonly provider: string;
}

export interface LLMProviderRegisterOutputInvalid {
  readonly variant: 'invalid';
  readonly message: string;
}

export type LLMProviderRegisterOutput = LLMProviderRegisterOutputOk | LLMProviderRegisterOutputInvalid;

export interface LLMProviderGenerateInput {
  readonly provider: string;
  readonly messages: readonly { readonly role: string; readonly content: string }[];
  readonly config: O.Option<{ readonly temperature: number; readonly top_p: number; readonly max_tokens: number }>;
}

export interface LLMProviderGenerateOutputOk {
  readonly variant: 'ok';
  readonly response: { readonly content: string; readonly model: string; readonly prompt_tokens: number; readonly completion_tokens: number; readonly finish_reason: string; readonly cost: number };
}

export interface LLMProviderGenerateOutputRate_limited {
  readonly variant: 'rate_limited';
  readonly retry_after_ms: number;
}

export interface LLMProviderGenerateOutputContext_overflow {
  readonly variant: 'context_overflow';
  readonly max_tokens: number;
  readonly requested_tokens: number;
}

export interface LLMProviderGenerateOutputAuth_failure {
  readonly variant: 'auth_failure';
  readonly message: string;
}

export interface LLMProviderGenerateOutputContent_filtered {
  readonly variant: 'content_filtered';
  readonly message: string;
}

export interface LLMProviderGenerateOutputUnavailable {
  readonly variant: 'unavailable';
  readonly message: string;
}

export type LLMProviderGenerateOutput = LLMProviderGenerateOutputOk | LLMProviderGenerateOutputRate_limited | LLMProviderGenerateOutputContext_overflow | LLMProviderGenerateOutputAuth_failure | LLMProviderGenerateOutputContent_filtered | LLMProviderGenerateOutputUnavailable;

export interface LLMProviderStreamInput {
  readonly provider: string;
  readonly messages: readonly { readonly role: string; readonly content: string }[];
  readonly config: O.Option<{ readonly temperature: number; readonly top_p: number; readonly max_tokens: number }>;
}

export interface LLMProviderStreamOutputOk {
  readonly variant: 'ok';
  readonly stream_id: string;
}

export interface LLMProviderStreamOutputRate_limited {
  readonly variant: 'rate_limited';
  readonly retry_after_ms: number;
}

export interface LLMProviderStreamOutputUnavailable {
  readonly variant: 'unavailable';
  readonly message: string;
}

export type LLMProviderStreamOutput = LLMProviderStreamOutputOk | LLMProviderStreamOutputRate_limited | LLMProviderStreamOutputUnavailable;

export interface LLMProviderEmbedInput {
  readonly provider: string;
  readonly texts: readonly string[];
}

export interface LLMProviderEmbedOutputOk {
  readonly variant: 'ok';
  readonly vectors: readonly { readonly vector: readonly number[]; readonly dimensions: number }[];
}

export interface LLMProviderEmbedOutputError {
  readonly variant: 'error';
  readonly message: string;
}

export type LLMProviderEmbedOutput = LLMProviderEmbedOutputOk | LLMProviderEmbedOutputError;

export interface LLMProviderCountTokensInput {
  readonly provider: string;
  readonly content: string;
}

export interface LLMProviderCountTokensOutputOk {
  readonly variant: 'ok';
  readonly count: number;
  readonly tokenizer: string;
}

export interface LLMProviderCountTokensOutputError {
  readonly variant: 'error';
  readonly message: string;
}

export type LLMProviderCountTokensOutput = LLMProviderCountTokensOutputOk | LLMProviderCountTokensOutputError;

export interface LLMProviderHealthCheckInput {
  readonly provider: string;
}

export interface LLMProviderHealthCheckOutputOk {
  readonly variant: 'ok';
  readonly status: string;
  readonly latency_ms: number;
}

export interface LLMProviderHealthCheckOutputDegraded {
  readonly variant: 'degraded';
  readonly message: string;
  readonly latency_ms: number;
}

export interface LLMProviderHealthCheckOutputUnavailable {
  readonly variant: 'unavailable';
  readonly message: string;
}

export type LLMProviderHealthCheckOutput = LLMProviderHealthCheckOutputOk | LLMProviderHealthCheckOutputDegraded | LLMProviderHealthCheckOutputUnavailable;

export interface LLMProviderUpdateConfigInput {
  readonly provider: string;
  readonly config: { readonly temperature: number; readonly top_p: number; readonly max_tokens: number; readonly stop_sequences: readonly string[] };
}

export interface LLMProviderUpdateConfigOutputOk {
  readonly variant: 'ok';
  readonly provider: string;
}

export interface LLMProviderUpdateConfigOutputNotfound {
  readonly variant: 'notfound';
  readonly message: string;
}

export type LLMProviderUpdateConfigOutput = LLMProviderUpdateConfigOutputOk | LLMProviderUpdateConfigOutputNotfound;

// --- Variant constructors ---

export const registerOk = (provider: string): LLMProviderRegisterOutput => ({ variant: 'ok', provider } as LLMProviderRegisterOutput);
export const registerInvalid = (message: string): LLMProviderRegisterOutput => ({ variant: 'invalid', message } as LLMProviderRegisterOutput);

export const generateOk = (response: { readonly content: string; readonly model: string; readonly prompt_tokens: number; readonly completion_tokens: number; readonly finish_reason: string; readonly cost: number }): LLMProviderGenerateOutput => ({ variant: 'ok', response } as LLMProviderGenerateOutput);
export const generateRate_limited = (retry_after_ms: number): LLMProviderGenerateOutput => ({ variant: 'rate_limited', retry_after_ms } as LLMProviderGenerateOutput);
export const generateContext_overflow = (max_tokens: number, requested_tokens: number): LLMProviderGenerateOutput => ({ variant: 'context_overflow', max_tokens, requested_tokens } as LLMProviderGenerateOutput);
export const generateAuth_failure = (message: string): LLMProviderGenerateOutput => ({ variant: 'auth_failure', message } as LLMProviderGenerateOutput);
export const generateContent_filtered = (message: string): LLMProviderGenerateOutput => ({ variant: 'content_filtered', message } as LLMProviderGenerateOutput);
export const generateUnavailable = (message: string): LLMProviderGenerateOutput => ({ variant: 'unavailable', message } as LLMProviderGenerateOutput);

export const streamOk = (stream_id: string): LLMProviderStreamOutput => ({ variant: 'ok', stream_id } as LLMProviderStreamOutput);
export const streamRate_limited = (retry_after_ms: number): LLMProviderStreamOutput => ({ variant: 'rate_limited', retry_after_ms } as LLMProviderStreamOutput);
export const streamUnavailable = (message: string): LLMProviderStreamOutput => ({ variant: 'unavailable', message } as LLMProviderStreamOutput);

export const embedOk = (vectors: readonly { readonly vector: readonly number[]; readonly dimensions: number }[]): LLMProviderEmbedOutput => ({ variant: 'ok', vectors } as LLMProviderEmbedOutput);
export const embedError = (message: string): LLMProviderEmbedOutput => ({ variant: 'error', message } as LLMProviderEmbedOutput);

export const countTokensOk = (count: number, tokenizer: string): LLMProviderCountTokensOutput => ({ variant: 'ok', count, tokenizer } as LLMProviderCountTokensOutput);
export const countTokensError = (message: string): LLMProviderCountTokensOutput => ({ variant: 'error', message } as LLMProviderCountTokensOutput);

export const healthCheckOk = (status: string, latency_ms: number): LLMProviderHealthCheckOutput => ({ variant: 'ok', status, latency_ms } as LLMProviderHealthCheckOutput);
export const healthCheckDegraded = (message: string, latency_ms: number): LLMProviderHealthCheckOutput => ({ variant: 'degraded', message, latency_ms } as LLMProviderHealthCheckOutput);
export const healthCheckUnavailable = (message: string): LLMProviderHealthCheckOutput => ({ variant: 'unavailable', message } as LLMProviderHealthCheckOutput);

export const updateConfigOk = (provider: string): LLMProviderUpdateConfigOutput => ({ variant: 'ok', provider } as LLMProviderUpdateConfigOutput);
export const updateConfigNotfound = (message: string): LLMProviderUpdateConfigOutput => ({ variant: 'notfound', message } as LLMProviderUpdateConfigOutput);

