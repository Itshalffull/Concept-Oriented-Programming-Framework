// LLMProvider — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  LLMProviderStorage,
  LLMProviderRegisterInput,
  LLMProviderRegisterOutput,
  LLMProviderGenerateInput,
  LLMProviderGenerateOutput,
  LLMProviderStreamInput,
  LLMProviderStreamOutput,
  LLMProviderEmbedInput,
  LLMProviderEmbedOutput,
  LLMProviderCountTokensInput,
  LLMProviderCountTokensOutput,
  LLMProviderHealthCheckInput,
  LLMProviderHealthCheckOutput,
  LLMProviderUpdateConfigInput,
  LLMProviderUpdateConfigOutput,
} from './types.js';

import {
  registerOk,
  registerInvalid,
  generateOk,
  generateRate_limited,
  generateContext_overflow,
  generateAuth_failure,
  generateContent_filtered,
  generateUnavailable,
  streamOk,
  streamRate_limited,
  streamUnavailable,
  embedOk,
  embedError,
  countTokensOk,
  countTokensError,
  healthCheckOk,
  healthCheckDegraded,
  healthCheckUnavailable,
  updateConfigOk,
  updateConfigNotfound,
} from './types.js';

export interface LLMProviderError {
  readonly code: string;
  readonly message: string;
}

export interface LLMProviderHandler {
  readonly register: (
    input: LLMProviderRegisterInput,
    storage: LLMProviderStorage,
  ) => TE.TaskEither<LLMProviderError, LLMProviderRegisterOutput>;
  readonly generate: (
    input: LLMProviderGenerateInput,
    storage: LLMProviderStorage,
  ) => TE.TaskEither<LLMProviderError, LLMProviderGenerateOutput>;
  readonly stream: (
    input: LLMProviderStreamInput,
    storage: LLMProviderStorage,
  ) => TE.TaskEither<LLMProviderError, LLMProviderStreamOutput>;
  readonly embed: (
    input: LLMProviderEmbedInput,
    storage: LLMProviderStorage,
  ) => TE.TaskEither<LLMProviderError, LLMProviderEmbedOutput>;
  readonly countTokens: (
    input: LLMProviderCountTokensInput,
    storage: LLMProviderStorage,
  ) => TE.TaskEither<LLMProviderError, LLMProviderCountTokensOutput>;
  readonly healthCheck: (
    input: LLMProviderHealthCheckInput,
    storage: LLMProviderStorage,
  ) => TE.TaskEither<LLMProviderError, LLMProviderHealthCheckOutput>;
  readonly updateConfig: (
    input: LLMProviderUpdateConfigInput,
    storage: LLMProviderStorage,
  ) => TE.TaskEither<LLMProviderError, LLMProviderUpdateConfigOutput>;
}

// --- Implementation ---

export const lLMProviderHandler: LLMProviderHandler = {
  register: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmprovider', input.provider_id, { provider_id: input.provider_id, model_id: input.model_id, credentials: input.credentials, config: input.config, capabilities: input.capabilities });
          return registerOk(input.provider_id);
        },
        (error): LLMProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  generate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmprovider', input.provider, { provider: input.provider, messages: input.messages, config: input.config });
          return generateOk(input.provider);
        },
        (error): LLMProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  stream: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmprovider', input.provider, { provider: input.provider, messages: input.messages, config: input.config });
          return streamOk(input.provider);
        },
        (error): LLMProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  embed: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmprovider', input.provider, { provider: input.provider, texts: input.texts });
          return embedOk([]);
        },
        (error): LLMProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  countTokens: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmprovider', input.provider, { provider: input.provider, content: input.content });
          return countTokensOk(0, input.provider);
        },
        (error): LLMProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  healthCheck: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmprovider', input.provider, { provider: input.provider });
          return healthCheckOk(input.provider, 0);
        },
        (error): LLMProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  updateConfig: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('llmprovider', input.provider),
        (error): LLMProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(updateConfigNotfound(`${input.provider} not found`)),
            (existing) =>
              TE.tryCatch(
                async () => {
                  const updated = { ...existing, provider: input.provider, config: input.config };
                  await storage.put('llmprovider', input.provider, updated);
                  return updateConfigOk(input.provider);
                },
                (error): LLMProviderError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),
};
