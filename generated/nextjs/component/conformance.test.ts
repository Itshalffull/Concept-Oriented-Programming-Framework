// Component — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { componentHandler } from './handler.js';
import type { ComponentStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): ComponentStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Component conformance', () => {
  it('invariant 1: after register, place, render behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = componentHandler;

    const c = 'u-test-invariant-001';

    // setup: register -> ok
    const registerResultSetup = await pipe(
      handler.register({
      component: c,
      config: 'hero-banner',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(registerResultSetup)).toBe(true);

    // assert: place -> ok
    const placeResultAssert = await pipe(
      handler.place({
      component: c,
      region: 'header',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(placeResultAssert)).toBe(true);

    // assert: render -> ok
    const renderResultAssert = await pipe(
      handler.render({
      component: c,
      context: 'homepage',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).output).toBe('hero-banner:header:homepage');
        return output;
      }),
    )();
    expect(E.isRight(renderResultAssert)).toBe(true);

  });

});
