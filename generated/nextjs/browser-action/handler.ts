// BrowserAction — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  BrowserActionStorage,
  BrowserActionConfigureInput,
  BrowserActionConfigureOutput,
  BrowserActionSetBadgeInput,
  BrowserActionSetBadgeOutput,
  BrowserActionSetIconInput,
  BrowserActionSetIconOutput,
  BrowserActionSetPopupInput,
  BrowserActionSetPopupOutput,
  BrowserActionSetEnabledInput,
  BrowserActionSetEnabledOutput,
  BrowserActionAddContextMenuInput,
  BrowserActionAddContextMenuOutput,
  BrowserActionRemoveContextMenuInput,
  BrowserActionRemoveContextMenuOutput,
  BrowserActionOnClickedInput,
  BrowserActionOnClickedOutput,
} from './types.js';

import {
  configureOk,
  configureExists,
  setBadgeOk,
  setBadgeNotfound,
  setIconOk,
  setIconNotfound,
  setPopupOk,
  setPopupNotfound,
  setEnabledOk,
  setEnabledNotfound,
  addContextMenuOk,
  addContextMenuNotfound,
  addContextMenuExists,
  removeContextMenuOk,
  removeContextMenuNotfound,
  onClickedOk,
  onClickedNotfound,
} from './types.js';

export interface BrowserActionError {
  readonly code: string;
  readonly message: string;
}

export interface BrowserActionHandler {
  readonly configure: (
    input: BrowserActionConfigureInput,
    storage: BrowserActionStorage,
  ) => TE.TaskEither<BrowserActionError, BrowserActionConfigureOutput>;
  readonly setBadge: (
    input: BrowserActionSetBadgeInput,
    storage: BrowserActionStorage,
  ) => TE.TaskEither<BrowserActionError, BrowserActionSetBadgeOutput>;
  readonly setIcon: (
    input: BrowserActionSetIconInput,
    storage: BrowserActionStorage,
  ) => TE.TaskEither<BrowserActionError, BrowserActionSetIconOutput>;
  readonly setPopup: (
    input: BrowserActionSetPopupInput,
    storage: BrowserActionStorage,
  ) => TE.TaskEither<BrowserActionError, BrowserActionSetPopupOutput>;
  readonly setEnabled: (
    input: BrowserActionSetEnabledInput,
    storage: BrowserActionStorage,
  ) => TE.TaskEither<BrowserActionError, BrowserActionSetEnabledOutput>;
  readonly addContextMenu: (
    input: BrowserActionAddContextMenuInput,
    storage: BrowserActionStorage,
  ) => TE.TaskEither<BrowserActionError, BrowserActionAddContextMenuOutput>;
  readonly removeContextMenu: (
    input: BrowserActionRemoveContextMenuInput,
    storage: BrowserActionStorage,
  ) => TE.TaskEither<BrowserActionError, BrowserActionRemoveContextMenuOutput>;
  readonly onClicked: (
    input: BrowserActionOnClickedInput,
    storage: BrowserActionStorage,
  ) => TE.TaskEither<BrowserActionError, BrowserActionOnClickedOutput>;
}

// --- Implementation ---

export const browserActionHandler: BrowserActionHandler = {
  configure: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browseraction', input.extensionId, { extensionId: input.extensionId, icon: input.icon, popupUrl: input.popupUrl });
          return configureOk(input.extensionId);
        },
        (error): BrowserActionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  setBadge: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browseraction', input.action, { action: input.action, tabId: input.tabId, text: input.text, color: input.color });
          return setBadgeOk(input.action);
        },
        (error): BrowserActionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  setIcon: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browseraction', input.action, { action: input.action, tabId: input.tabId, icon: input.icon });
          return setIconOk(input.action);
        },
        (error): BrowserActionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  setPopup: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browseraction', input.action, { action: input.action, popupUrl: input.popupUrl });
          return setPopupOk(input.action);
        },
        (error): BrowserActionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  setEnabled: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browseraction', input.action, { action: input.action, tabId: input.tabId, enabled: input.enabled });
          return setEnabledOk(input.action);
        },
        (error): BrowserActionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  addContextMenu: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browseraction', input.action, { action: input.action, id: input.id, title: input.title, contexts: input.contexts });
          return addContextMenuOk(input.action);
        },
        (error): BrowserActionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  removeContextMenu: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('browseraction', input.action),
        (error): BrowserActionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(removeContextMenuNotfound(`${input.action} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('browseraction', input.action);
                  return removeContextMenuOk();
                },
                (error): BrowserActionError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  onClicked: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('browseraction', input.action, { action: input.action, tabId: input.tabId });
          return onClickedOk(input.action);
        },
        (error): BrowserActionError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
