// ExtensionHost — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ExtensionHostStorage,
  ExtensionHostInstallInput,
  ExtensionHostInstallOutput,
  ExtensionHostActivateInput,
  ExtensionHostActivateOutput,
  ExtensionHostDeactivateInput,
  ExtensionHostDeactivateOutput,
  ExtensionHostUninstallInput,
  ExtensionHostUninstallOutput,
  ExtensionHostGetStatusInput,
  ExtensionHostGetStatusOutput,
  ExtensionHostListInstalledInput,
  ExtensionHostListInstalledOutput,
  ExtensionHostHandleErrorInput,
  ExtensionHostHandleErrorOutput,
} from './types.js';

import {
  installOk,
  installExists,
  installInvalid,
  activateOk,
  activateNotfound,
  activateError,
  deactivateOk,
  deactivateNotfound,
  uninstallOk,
  uninstallNotfound,
  getStatusOk,
  getStatusNotfound,
  listInstalledOk,
  handleErrorOk,
  handleErrorNotfound,
} from './types.js';

export interface ExtensionHostError {
  readonly code: string;
  readonly message: string;
}

export interface ExtensionHostHandler {
  readonly install: (
    input: ExtensionHostInstallInput,
    storage: ExtensionHostStorage,
  ) => TE.TaskEither<ExtensionHostError, ExtensionHostInstallOutput>;
  readonly activate: (
    input: ExtensionHostActivateInput,
    storage: ExtensionHostStorage,
  ) => TE.TaskEither<ExtensionHostError, ExtensionHostActivateOutput>;
  readonly deactivate: (
    input: ExtensionHostDeactivateInput,
    storage: ExtensionHostStorage,
  ) => TE.TaskEither<ExtensionHostError, ExtensionHostDeactivateOutput>;
  readonly uninstall: (
    input: ExtensionHostUninstallInput,
    storage: ExtensionHostStorage,
  ) => TE.TaskEither<ExtensionHostError, ExtensionHostUninstallOutput>;
  readonly getStatus: (
    input: ExtensionHostGetStatusInput,
    storage: ExtensionHostStorage,
  ) => TE.TaskEither<ExtensionHostError, ExtensionHostGetStatusOutput>;
  readonly listInstalled: (
    input: ExtensionHostListInstalledInput,
    storage: ExtensionHostStorage,
  ) => TE.TaskEither<ExtensionHostError, ExtensionHostListInstalledOutput>;
  readonly handleError: (
    input: ExtensionHostHandleErrorInput,
    storage: ExtensionHostStorage,
  ) => TE.TaskEither<ExtensionHostError, ExtensionHostHandleErrorOutput>;
}

// --- Implementation ---

export const extensionHostHandler: ExtensionHostHandler = {
  install: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionhost', input.manifest, { manifest: input.manifest, hostType: input.hostType });
          return installOk(input.manifest);
        },
        (error): ExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  activate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionhost', input.extension, { extension: input.extension });
          return activateOk(input.extension);
        },
        (error): ExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  deactivate: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionhost', input.extension),
        (error): ExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(deactivateNotfound(`${input.extension} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('extensionhost', input.extension);
                  return deactivateOk();
                },
                (error): ExtensionHostError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  uninstall: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionhost', input.extension, { extension: input.extension });
          return uninstallOk(input.extension);
        },
        (error): ExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getStatus: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionhost', input.extension),
        (error): ExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getStatusNotfound(`${input.extension} not found`)),
            (found) => TE.right(getStatusOk(input.extension, input.extension, input.extension)),
          ),
        ),
      ),
    ),

  listInstalled: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('extensionhost');
          return listInstalledOk(input.hostType);
        },
        (error): ExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  handleError: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionhost', input.extension, { extension: input.extension, error: input.error });
          return handleErrorOk(input.extension, input.extension);
        },
        (error): ExtensionHostError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
