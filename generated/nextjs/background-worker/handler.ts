// BackgroundWorker — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  BackgroundWorkerStorage,
  BackgroundWorkerRegisterInput,
  BackgroundWorkerRegisterOutput,
  BackgroundWorkerStartInput,
  BackgroundWorkerStartOutput,
  BackgroundWorkerStopInput,
  BackgroundWorkerStopOutput,
  BackgroundWorkerSetAlarmInput,
  BackgroundWorkerSetAlarmOutput,
  BackgroundWorkerClearAlarmInput,
  BackgroundWorkerClearAlarmOutput,
  BackgroundWorkerOnAlarmInput,
  BackgroundWorkerOnAlarmOutput,
  BackgroundWorkerGetStatusInput,
  BackgroundWorkerGetStatusOutput,
} from './types.js';

import {
  registerOk,
  registerExists,
  startOk,
  startNotfound,
  startError,
  stopOk,
  stopNotfound,
  setAlarmOk,
  setAlarmNotfound,
  clearAlarmOk,
  clearAlarmNotfound,
  onAlarmOk,
  onAlarmNotfound,
  getStatusOk,
  getStatusNotfound,
} from './types.js';

export interface BackgroundWorkerError {
  readonly code: string;
  readonly message: string;
}

export interface BackgroundWorkerHandler {
  readonly register: (
    input: BackgroundWorkerRegisterInput,
    storage: BackgroundWorkerStorage,
  ) => TE.TaskEither<BackgroundWorkerError, BackgroundWorkerRegisterOutput>;
  readonly start: (
    input: BackgroundWorkerStartInput,
    storage: BackgroundWorkerStorage,
  ) => TE.TaskEither<BackgroundWorkerError, BackgroundWorkerStartOutput>;
  readonly stop: (
    input: BackgroundWorkerStopInput,
    storage: BackgroundWorkerStorage,
  ) => TE.TaskEither<BackgroundWorkerError, BackgroundWorkerStopOutput>;
  readonly setAlarm: (
    input: BackgroundWorkerSetAlarmInput,
    storage: BackgroundWorkerStorage,
  ) => TE.TaskEither<BackgroundWorkerError, BackgroundWorkerSetAlarmOutput>;
  readonly clearAlarm: (
    input: BackgroundWorkerClearAlarmInput,
    storage: BackgroundWorkerStorage,
  ) => TE.TaskEither<BackgroundWorkerError, BackgroundWorkerClearAlarmOutput>;
  readonly onAlarm: (
    input: BackgroundWorkerOnAlarmInput,
    storage: BackgroundWorkerStorage,
  ) => TE.TaskEither<BackgroundWorkerError, BackgroundWorkerOnAlarmOutput>;
  readonly getStatus: (
    input: BackgroundWorkerGetStatusInput,
    storage: BackgroundWorkerStorage,
  ) => TE.TaskEither<BackgroundWorkerError, BackgroundWorkerGetStatusOutput>;
}

// --- Implementation ---

export const backgroundWorkerHandler: BackgroundWorkerHandler = {
  register: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('backgroundworker', input.extensionId, { extensionId: input.extensionId, scriptUrl: input.scriptUrl });
          return registerOk(input.extensionId);
        },
        (error): BackgroundWorkerError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  start: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('backgroundworker', input.worker, { worker: input.worker });
          return startOk(input.worker);
        },
        (error): BackgroundWorkerError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  stop: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('backgroundworker', input.worker, { worker: input.worker });
          return stopOk(input.worker);
        },
        (error): BackgroundWorkerError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  setAlarm: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('backgroundworker', input.worker, { worker: input.worker, name: input.name, delayMs: input.delayMs, periodic: input.periodic });
          return setAlarmOk(input.worker);
        },
        (error): BackgroundWorkerError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  clearAlarm: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('backgroundworker', input.worker),
        (error): BackgroundWorkerError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(clearAlarmNotfound(`${input.worker} not found`)),
            (existing) =>
              TE.tryCatch(
                async () => {
                  const updated = { ...existing, worker: input.worker, name: input.name };
                  await storage.put('backgroundworker', input.worker, updated);
                  return clearAlarmOk(input.worker);
                },
                (error): BackgroundWorkerError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  onAlarm: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('backgroundworker', input.worker, { worker: input.worker, name: input.name });
          return onAlarmOk(input.worker);
        },
        (error): BackgroundWorkerError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getStatus: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('backgroundworker', input.worker),
        (error): BackgroundWorkerError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getStatusNotfound(`${input.worker} not found`)),
            (found) => TE.right(getStatusOk(input.worker, 0)),
          ),
        ),
      ),
    ),
};
