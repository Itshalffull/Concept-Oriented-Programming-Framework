// ScoreApi — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { scoreApiHandler } from './handler.js';
import type { ScoreApiStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): ScoreApiStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('ScoreApi conformance', () => {
  it('invariant 1: after reindex, status, listConcepts, listSyncs behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = scoreApiHandler;

    let c: any = 'u-test-invariant-001';
    let s: any = 'u-test-invariant-002';
    let f: any = 'u-test-invariant-003';
    let y: any = 'u-test-invariant-004';
    let d: any = 'u-test-invariant-005';
    let t: any = 'u-test-invariant-006';
    let concepts: any = 'u-test-invariant-007';
    let syncs: any = 'u-test-invariant-008';

    // setup: reindex -> ok
    const reindexResultSetup = await pipe(
      handler.reindex({

      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        c = (output as any).conceptCount;
        s = (output as any).symbolCount;
        f = (output as any).fileCount;
        y = (output as any).syncCount;
        d = (output as any).duration;
        return output;
      }),
    )();
    expect(E.isRight(reindexResultSetup)).toBe(true);

    // assert: status -> ok
    const statusResultAssert = await pipe(
      handler.status({

      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).indexed).toBe(true);
        expect((output as any).conceptCount).toBe(c);
        expect((output as any).symbolCount).toBe(s);
        expect((output as any).fileCount).toBe(f);
        expect((output as any).syncCount).toBe(y);
        t = (output as any).lastIndexed;
        return output;
      }),
    )();
    expect(E.isRight(statusResultAssert)).toBe(true);

    // assert: listConcepts -> ok
    const listConceptsResultAssert = await pipe(
      handler.listConcepts({

      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        concepts = (output as any).concepts;
        return output;
      }),
    )();
    expect(E.isRight(listConceptsResultAssert)).toBe(true);

    // assert: listSyncs -> ok
    const listSyncsResultAssert = await pipe(
      handler.listSyncs({

      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        syncs = (output as any).syncs;
        return output;
      }),
    )();
    expect(E.isRight(listSyncsResultAssert)).toBe(true);

  });

});
