// Flag — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { flagHandler } from './handler.js';
import type { FlagStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): FlagStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Flag conformance', () => {
  it('invariant 1: after flag, isFlagged behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = flagHandler;

    const f = 'u-test-invariant-001';
    const t = 'u-test-invariant-002';
    const e = 'u-test-invariant-003';
    const u = 'u-test-invariant-004';

    // setup: flag -> ok
    const flagResultSetup = await pipe(
      handler.flag({
      flagging: f,
      flagType: t,
      entity: e,
      user: u,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(flagResultSetup)).toBe(true);

    // assert: isFlagged -> ok
    const isFlaggedResultAssert = await pipe(
      handler.isFlagged({
      flagType: t,
      entity: e,
      user: u,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).flagged).toBe(true);
        return output;
      }),
    )();
    expect(E.isRight(isFlaggedResultAssert)).toBe(true);

  });

  it('invariant 2: after flag, unflag, isFlagged behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = flagHandler;

    const f = 'u-test-invariant-001';
    const t = 'u-test-invariant-002';
    const e = 'u-test-invariant-003';
    const u = 'u-test-invariant-004';

    // setup: flag -> ok
    const flagResultSetup = await pipe(
      handler.flag({
      flagging: f,
      flagType: t,
      entity: e,
      user: u,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(flagResultSetup)).toBe(true);

    // assert: unflag -> ok
    const unflagResultAssert = await pipe(
      handler.unflag({
      flagging: f,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(unflagResultAssert)).toBe(true);

    // assert: isFlagged -> ok
    const isFlaggedResultAssert = await pipe(
      handler.isFlagged({
      flagType: t,
      entity: e,
      user: u,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).flagged).toBe(false);
        return output;
      }),
    )();
    expect(E.isRight(isFlaggedResultAssert)).toBe(true);

  });

});
