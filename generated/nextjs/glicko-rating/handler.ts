// GlickoRating — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  GlickoRatingStorage,
  GlickoRatingConfigureInput,
  GlickoRatingConfigureOutput,
  GlickoRatingRecordOutcomeInput,
  GlickoRatingRecordOutcomeOutput,
  GlickoRatingApplyInactivityDecayInput,
  GlickoRatingApplyInactivityDecayOutput,
  GlickoRatingGetReliableWeightInput,
  GlickoRatingGetReliableWeightOutput,
} from './types.js';

import {
  configureConfigured,
  recordOutcomeUpdated,
  applyInactivityDecayDecayed,
  getReliableWeightWeight,
} from './types.js';

export interface GlickoRatingError {
  readonly code: string;
  readonly message: string;
}

export interface GlickoRatingHandler {
  readonly configure: (
    input: GlickoRatingConfigureInput,
    storage: GlickoRatingStorage,
  ) => TE.TaskEither<GlickoRatingError, GlickoRatingConfigureOutput>;
  readonly recordOutcome: (
    input: GlickoRatingRecordOutcomeInput,
    storage: GlickoRatingStorage,
  ) => TE.TaskEither<GlickoRatingError, GlickoRatingRecordOutcomeOutput>;
  readonly applyInactivityDecay: (
    input: GlickoRatingApplyInactivityDecayInput,
    storage: GlickoRatingStorage,
  ) => TE.TaskEither<GlickoRatingError, GlickoRatingApplyInactivityDecayOutput>;
  readonly getReliableWeight: (
    input: GlickoRatingGetReliableWeightInput,
    storage: GlickoRatingStorage,
  ) => TE.TaskEither<GlickoRatingError, GlickoRatingGetReliableWeightOutput>;
}

// --- Implementation ---

export const glickoRatingHandler: GlickoRatingHandler = {
  configure: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('glickorating', input.initialRating, { initialRating: input.initialRating, initialDeviation: input.initialDeviation, initialVolatility: input.initialVolatility, inactivityGrowthRate: input.inactivityGrowthRate });
          return configureOk();
        },
        (error): GlickoRatingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  recordOutcome: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('glickorating', input.config, { config: input.config, participant: input.participant, opponent: input.opponent, outcome: input.outcome });
          return recordOutcomeOk();
        },
        (error): GlickoRatingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  applyInactivityDecay: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('glickorating', input.config, { config: input.config, participant: input.participant, daysSinceActive: input.daysSinceActive });
          return applyInactivityDecayOk();
        },
        (error): GlickoRatingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getReliableWeight: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('glickorating', input.config);
          return getReliableWeightOk();
        },
        (error): GlickoRatingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
