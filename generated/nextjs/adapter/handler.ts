// Adapter — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  AdapterStorage,
  AdapterCreateInput,
  AdapterCreateOutput,
  AdapterTrainInput,
  AdapterTrainOutput,
  AdapterMergeInput,
  AdapterMergeOutput,
  AdapterSwapInput,
  AdapterSwapOutput,
  AdapterComposeInput,
  AdapterComposeOutput,
  AdapterExportInput,
  AdapterExportOutput,
} from './types.js';

import {
  createOk,
  createInvalid,
  trainOk,
  trainError,
  mergeOk,
  mergeNot_trained,
  swapOk,
  swapNot_trained,
  composeOk,
  composeIncompatible,
  exportOk,
  exportNot_trained,
} from './types.js';

export interface AdapterError {
  readonly code: string;
  readonly message: string;
}

export interface AdapterHandler {
  readonly create: (
    input: AdapterCreateInput,
    storage: AdapterStorage,
  ) => TE.TaskEither<AdapterError, AdapterCreateOutput>;
  readonly train: (
    input: AdapterTrainInput,
    storage: AdapterStorage,
  ) => TE.TaskEither<AdapterError, AdapterTrainOutput>;
  readonly merge: (
    input: AdapterMergeInput,
    storage: AdapterStorage,
  ) => TE.TaskEither<AdapterError, AdapterMergeOutput>;
  readonly swap: (
    input: AdapterSwapInput,
    storage: AdapterStorage,
  ) => TE.TaskEither<AdapterError, AdapterSwapOutput>;
  readonly compose: (
    input: AdapterComposeInput,
    storage: AdapterStorage,
  ) => TE.TaskEither<AdapterError, AdapterComposeOutput>;
  readonly export: (
    input: AdapterExportInput,
    storage: AdapterStorage,
  ) => TE.TaskEither<AdapterError, AdapterExportOutput>;
}

// --- Implementation ---

export const adapterHandler: AdapterHandler = {
  create: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('adapter', input.name, { name: input.name, base_model_id: input.base_model_id, rank: input.rank, target_modules: input.target_modules, quantization: input.quantization });
          return createOk(input.name);
        },
        (error): AdapterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  train: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('adapter', input.adapter, { adapter: input.adapter, dataset_ref: input.dataset_ref, config: input.config });
          return trainOk(input.adapter, 0, 0, 0);
        },
        (error): AdapterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  merge: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const existing = await storage.get('adapter', input.adapter);
          const updated = { ...existing, adapter: input.adapter };
          await storage.put('adapter', input.adapter, updated);
          return mergeOk(input.adapter);
        },
        (error): AdapterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  swap: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const existing = await storage.get('adapter', input.adapter);
          const updated = { ...existing, adapter: input.adapter, active: input.active };
          await storage.put('adapter', input.adapter, updated);
          return swapOk(input.adapter);
        },
        (error): AdapterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  compose: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('adapter', input.adapter_a, { adapter_a: input.adapter_a, adapter_b: input.adapter_b });
          return composeOk(input.adapter_a);
        },
        (error): AdapterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  export: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('adapter', input.adapter, { adapter: input.adapter, format: input.format });
          return exportOk(input.adapter);
        },
        (error): AdapterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
