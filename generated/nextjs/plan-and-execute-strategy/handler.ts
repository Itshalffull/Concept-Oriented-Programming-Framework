// PlanAndExecuteStrategy — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  PlanAndExecuteStrategyStorage,
  PlanAndExecuteStrategyExecuteInput,
  PlanAndExecuteStrategyExecuteOutput,
  PlanAndExecuteStrategyPlanInput,
  PlanAndExecuteStrategyPlanOutput,
  PlanAndExecuteStrategyReplanInput,
  PlanAndExecuteStrategyReplanOutput,
  PlanAndExecuteStrategyGetStateInput,
  PlanAndExecuteStrategyGetStateOutput,
} from './types.js';

import {
  executeOk,
  executeMax_iterations,
  executeError,
  planOk,
  replanOk,
  replanNo_change,
  getStateOk,
} from './types.js';

export interface PlanAndExecuteStrategyError {
  readonly code: string;
  readonly message: string;
}

export interface PlanAndExecuteStrategyHandler {
  readonly execute: (
    input: PlanAndExecuteStrategyExecuteInput,
    storage: PlanAndExecuteStrategyStorage,
  ) => TE.TaskEither<PlanAndExecuteStrategyError, PlanAndExecuteStrategyExecuteOutput>;
  readonly plan: (
    input: PlanAndExecuteStrategyPlanInput,
    storage: PlanAndExecuteStrategyStorage,
  ) => TE.TaskEither<PlanAndExecuteStrategyError, PlanAndExecuteStrategyPlanOutput>;
  readonly replan: (
    input: PlanAndExecuteStrategyReplanInput,
    storage: PlanAndExecuteStrategyStorage,
  ) => TE.TaskEither<PlanAndExecuteStrategyError, PlanAndExecuteStrategyReplanOutput>;
  readonly getState: (
    input: PlanAndExecuteStrategyGetStateInput,
    storage: PlanAndExecuteStrategyStorage,
  ) => TE.TaskEither<PlanAndExecuteStrategyError, PlanAndExecuteStrategyGetStateOutput>;
}

// --- Implementation ---

export const planAndExecuteStrategyHandler: PlanAndExecuteStrategyHandler = {
  execute: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('planandexecutestrategy', input.agent_ref, { agent_ref: input.agent_ref, goal: input.goal, context: input.context, available_tools: input.available_tools, max_iterations: input.max_iterations });
          return executeOk(input.agent_ref, 0, 0, []);
        },
        (error): PlanAndExecuteStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  plan: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('planandexecutestrategy', input.session, { session: input.session, goal: input.goal });
          return planOk([]);
        },
        (error): PlanAndExecuteStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  replan: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('planandexecutestrategy', input.session, { session: input.session, completed: input.completed, remaining: input.remaining });
          return replanOk([]);
        },
        (error): PlanAndExecuteStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getState: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('planandexecutestrategy', input.session);
          return getStateOk((record as any).plan);
        },
        (error): PlanAndExecuteStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
