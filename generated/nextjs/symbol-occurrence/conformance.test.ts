// SymbolOccurrence — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { symbolOccurrenceHandler } from './handler.js';
import type { SymbolOccurrenceStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): SymbolOccurrenceStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('SymbolOccurrence conformance', () => {
  it('invariant 1: after record, findDefinitions behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = symbolOccurrenceHandler;

    const o = 'u-test-invariant-001';

    // setup: record -> ok
    const recordResultSetup = await pipe(
      handler.record({
      symbol: 'clef/concept/Article',
      file: 'specs/article.concept',
      startRow: 2,
      startCol: 8,
      endRow: 2,
      endCol: 15,
      startByte: 30,
      endByte: 37,
      role: 'definition',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).occurrence).toBe(o);
        return output;
      }),
    )();
    expect(E.isRight(recordResultSetup)).toBe(true);

    // assert: findDefinitions -> ok
    const findDefinitionsResultAssert = await pipe(
      handler.findDefinitions({
      symbol: 'clef/concept/Article',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).occurrences).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(findDefinitionsResultAssert)).toBe(true);

  });

  it('invariant 2: after record, findAtPosition behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = symbolOccurrenceHandler;

    const o = 'u-test-invariant-001';

    // setup: record -> ok
    const recordResultSetup = await pipe(
      handler.record({
      symbol: 'clef/concept/Article',
      file: 'specs/article.concept',
      startRow: 2,
      startCol: 8,
      endRow: 2,
      endCol: 15,
      startByte: 30,
      endByte: 37,
      role: 'definition',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).occurrence).toBe(o);
        return output;
      }),
    )();
    expect(E.isRight(recordResultSetup)).toBe(true);

    // assert: findAtPosition -> ok
    const findAtPositionResultAssert = await pipe(
      handler.findAtPosition({
      file: 'specs/article.concept',
      row: 2,
      col: 10,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).occurrence).toBe(o);
        expect((output as any).symbol).toBe('clef/concept/Article');
        return output;
      }),
    )();
    expect(E.isRight(findAtPositionResultAssert)).toBe(true);

  });

});
