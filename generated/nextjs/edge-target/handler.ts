// EdgeTarget — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  EdgeTargetStorage,
  EdgeTargetGenerateInput,
  EdgeTargetGenerateOutput,
  EdgeTargetPackageInput,
  EdgeTargetPackageOutput,
  EdgeTargetValidateInput,
  EdgeTargetValidateOutput,
} from './types.js';

import {
  generateOk,
  generateInvalid,
  packageOk,
  packageNotfound,
  packageError,
  validateOk,
  validateNotfound,
} from './types.js';

export interface EdgeTargetError {
  readonly code: string;
  readonly message: string;
}

export interface EdgeTargetHandler {
  readonly generate: (
    input: EdgeTargetGenerateInput,
    storage: EdgeTargetStorage,
  ) => TE.TaskEither<EdgeTargetError, EdgeTargetGenerateOutput>;
  readonly package: (
    input: EdgeTargetPackageInput,
    storage: EdgeTargetStorage,
  ) => TE.TaskEither<EdgeTargetError, EdgeTargetPackageOutput>;
  readonly validate: (
    input: EdgeTargetValidateInput,
    storage: EdgeTargetStorage,
  ) => TE.TaskEither<EdgeTargetError, EdgeTargetValidateOutput>;
}

// --- Implementation ---

export const edgeTargetHandler: EdgeTargetHandler = {
  generate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('edgetarget', input.manifest, { manifest: input.manifest, options: input.options });
          return generateOk(input.manifest, input.manifest);
        },
        (error): EdgeTargetError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  package: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('edgetarget', input.target, { target: input.target, format: input.format });
          return packageOk(input.target, input.target);
        },
        (error): EdgeTargetError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  validate: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('edgetarget', input.target),
        (error): EdgeTargetError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(validateNotfound(`${input.target} not found`)),
            (found) => TE.right(validateOk(true, input.target, input.target)),
          ),
        ),
      ),
    ),
};
