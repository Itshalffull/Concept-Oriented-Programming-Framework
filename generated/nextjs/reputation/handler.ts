// Reputation — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ReputationStorage,
  ReputationEarnInput,
  ReputationEarnOutput,
  ReputationBurnInput,
  ReputationBurnOutput,
  ReputationDecayInput,
  ReputationDecayOutput,
  ReputationGetScoreInput,
  ReputationGetScoreOutput,
  ReputationRecalculateInput,
  ReputationRecalculateOutput,
} from './types.js';

import {
  earnEarned,
  burnBurned,
  burnInsufficient,
  decayDecayed,
  decayNo_decay_configured,
  getScoreScore,
  getScoreNot_found,
  recalculateRecalculated,
} from './types.js';

export interface ReputationError {
  readonly code: string;
  readonly message: string;
}

export interface ReputationHandler {
  readonly earn: (
    input: ReputationEarnInput,
    storage: ReputationStorage,
  ) => TE.TaskEither<ReputationError, ReputationEarnOutput>;
  readonly burn: (
    input: ReputationBurnInput,
    storage: ReputationStorage,
  ) => TE.TaskEither<ReputationError, ReputationBurnOutput>;
  readonly decay: (
    input: ReputationDecayInput,
    storage: ReputationStorage,
  ) => TE.TaskEither<ReputationError, ReputationDecayOutput>;
  readonly getScore: (
    input: ReputationGetScoreInput,
    storage: ReputationStorage,
  ) => TE.TaskEither<ReputationError, ReputationGetScoreOutput>;
  readonly recalculate: (
    input: ReputationRecalculateInput,
    storage: ReputationStorage,
  ) => TE.TaskEither<ReputationError, ReputationRecalculateOutput>;
}

// --- Implementation ---

export const reputationHandler: ReputationHandler = {
  earn: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('reputation', input.participant, { participant: input.participant, amount: input.amount, reason: input.reason });
          return earnOk();
        },
        (error): ReputationError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  burn: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('reputation', input.participant, { participant: input.participant, amount: input.amount, reason: input.reason });
          return burnOk();
        },
        (error): ReputationError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  decay: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('reputation', input.id, {  });
          return decayOk();
        },
        (error): ReputationError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getScore: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('reputation', input.participant);
          return getScoreOk();
        },
        (error): ReputationError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  recalculate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const existing = await storage.get('reputation', input.participant);
          const updated = { ...existing, participant: input.participant };
          await storage.put('reputation', input.participant, updated);
          return recalculateOk();
        },
        (error): ReputationError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
