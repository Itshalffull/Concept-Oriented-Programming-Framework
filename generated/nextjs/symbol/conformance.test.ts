// Symbol — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { symbolHandler } from './handler.js';
import type { SymbolStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): SymbolStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Symbol conformance', () => {
  it('invariant 1: after register, get behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = symbolHandler;

    const s = 'u-test-invariant-001';

    // setup: register -> ok
    const registerResultSetup = await pipe(
      handler.register({
      symbolString: 'clef/concept/Article',
      kind: 'concept',
      displayName: 'Article',
      definingFile: 'specs/article.concept',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).symbol).toBe(s);
        return output;
      }),
    )();
    expect(E.isRight(registerResultSetup)).toBe(true);

    // assert: get -> ok
    const getResultAssert = await pipe(
      handler.get({
      symbol: s,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).symbol).toBe(s);
        expect((output as any).symbolString).toBe('clef/concept/Article');
        expect((output as any).kind).toBe('concept');
        expect((output as any).displayName).toBe('Article');
        expect((output as any).visibility).toBe('public');
        expect((output as any).definingFile).toBe('specs/article.concept');
        expect((output as any).namespace).toBe('clef/concept');
        return output;
      }),
    )();
    expect(E.isRight(getResultAssert)).toBe(true);

  });

  it('invariant 2: after register, resolve behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = symbolHandler;

    const s = 'u-test-invariant-001';

    // setup: register -> ok
    const registerResultSetup = await pipe(
      handler.register({
      symbolString: 'clef/concept/Article',
      kind: 'concept',
      displayName: 'Article',
      definingFile: 'specs/article.concept',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).symbol).toBe(s);
        return output;
      }),
    )();
    expect(E.isRight(registerResultSetup)).toBe(true);

    // assert: resolve -> ok
    const resolveResultAssert = await pipe(
      handler.resolve({
      symbolString: 'clef/concept/Article',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).symbol).toBe(s);
        return output;
      }),
    )();
    expect(E.isRight(resolveResultAssert)).toBe(true);

  });

  it('invariant 3: after register, register behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = symbolHandler;

    const s = 'u-test-invariant-001';

    // setup: register -> ok
    const registerResultSetup = await pipe(
      handler.register({
      symbolString: 'clef/concept/Article',
      kind: 'concept',
      displayName: 'Article',
      definingFile: 'specs/article.concept',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).symbol).toBe(s);
        return output;
      }),
    )();
    expect(E.isRight(registerResultSetup)).toBe(true);

    // assert: register -> alreadyExists
    const registerResultAssert = await pipe(
      handler.register({
      symbolString: 'clef/concept/Article',
      kind: 'concept',
      displayName: 'Article',
      definingFile: 'specs/article.concept',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('alreadyExists');
        expect((output as any).existing).toBe(s);
        return output;
      }),
    )();
    expect(E.isRight(registerResultAssert)).toBe(true);

  });

});
