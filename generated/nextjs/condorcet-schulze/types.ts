// CondorcetSchulze — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for CondorcetSchulze
export interface CondorcetSchulzeStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface CondorcetSchulzeConfigureInput {
}

export interface CondorcetSchulzeConfigureOutput {
  readonly variant: 'configured';
  readonly config: string;
}

export interface CondorcetSchulzeCountInput {
  readonly config: string;
  readonly rankedBallots: string;
  readonly weights: string;
}

export interface CondorcetSchulzeCountOutputCondorcet_winner {
  readonly variant: 'condorcet_winner';
  readonly choice: string;
  readonly pairwiseRecord: string;
}

export interface CondorcetSchulzeCountOutputSchulze_winner {
  readonly variant: 'schulze_winner';
  readonly choice: string;
  readonly strongestPaths: string;
}

export interface CondorcetSchulzeCountOutputUnresolvable {
  readonly variant: 'unresolvable';
  readonly smithSet: string;
}

export type CondorcetSchulzeCountOutput = CondorcetSchulzeCountOutputCondorcet_winner | CondorcetSchulzeCountOutputSchulze_winner | CondorcetSchulzeCountOutputUnresolvable;

export interface CondorcetSchulzeGetPairwiseMatrixInput {
  readonly config: string;
}

export interface CondorcetSchulzeGetPairwiseMatrixOutput {
  readonly variant: 'matrix';
  readonly data: string;
}

// --- Variant constructors ---

export const configureConfigured = (config: string): CondorcetSchulzeConfigureOutput => ({ variant: 'configured', config } as CondorcetSchulzeConfigureOutput);

export const countCondorcet_winner = (choice: string, pairwiseRecord: string): CondorcetSchulzeCountOutput => ({ variant: 'condorcet_winner', choice, pairwiseRecord } as CondorcetSchulzeCountOutput);
export const countSchulze_winner = (choice: string, strongestPaths: string): CondorcetSchulzeCountOutput => ({ variant: 'schulze_winner', choice, strongestPaths } as CondorcetSchulzeCountOutput);
export const countUnresolvable = (smithSet: string): CondorcetSchulzeCountOutput => ({ variant: 'unresolvable', smithSet } as CondorcetSchulzeCountOutput);

export const getPairwiseMatrixMatrix = (data: string): CondorcetSchulzeGetPairwiseMatrixOutput => ({ variant: 'matrix', data } as CondorcetSchulzeGetPairwiseMatrixOutput);

