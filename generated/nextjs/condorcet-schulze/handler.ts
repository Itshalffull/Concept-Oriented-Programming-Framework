// CondorcetSchulze — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  CondorcetSchulzeStorage,
  CondorcetSchulzeConfigureInput,
  CondorcetSchulzeConfigureOutput,
  CondorcetSchulzeCountInput,
  CondorcetSchulzeCountOutput,
  CondorcetSchulzeGetPairwiseMatrixInput,
  CondorcetSchulzeGetPairwiseMatrixOutput,
} from './types.js';

import {
  configureConfigured,
  countCondorcet_winner,
  countSchulze_winner,
  countUnresolvable,
  getPairwiseMatrixMatrix,
} from './types.js';

export interface CondorcetSchulzeError {
  readonly code: string;
  readonly message: string;
}

export interface CondorcetSchulzeHandler {
  readonly configure: (
    input: CondorcetSchulzeConfigureInput,
    storage: CondorcetSchulzeStorage,
  ) => TE.TaskEither<CondorcetSchulzeError, CondorcetSchulzeConfigureOutput>;
  readonly count: (
    input: CondorcetSchulzeCountInput,
    storage: CondorcetSchulzeStorage,
  ) => TE.TaskEither<CondorcetSchulzeError, CondorcetSchulzeCountOutput>;
  readonly getPairwiseMatrix: (
    input: CondorcetSchulzeGetPairwiseMatrixInput,
    storage: CondorcetSchulzeStorage,
  ) => TE.TaskEither<CondorcetSchulzeError, CondorcetSchulzeGetPairwiseMatrixOutput>;
}

// --- Implementation ---

export const condorcetSchulzeHandler: CondorcetSchulzeHandler = {
  configure: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('condorcetschulze', input.id, {  });
          return configureOk();
        },
        (error): CondorcetSchulzeError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  count: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('condorcetschulze', input.config, { config: input.config, rankedBallots: input.rankedBallots, weights: input.weights });
          return countOk();
        },
        (error): CondorcetSchulzeError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getPairwiseMatrix: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('condorcetschulze', input.config);
          return getPairwiseMatrixOk();
        },
        (error): CondorcetSchulzeError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
