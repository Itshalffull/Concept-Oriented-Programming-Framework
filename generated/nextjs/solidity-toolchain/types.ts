// SolidityToolchain — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as O from 'fp-ts/Option';

// Storage interface for SolidityToolchain
export interface SolidityToolchainStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface SolidityToolchainResolveInput {
  readonly platform: string;
  readonly versionConstraint: O.Option<string>;
}

export interface SolidityToolchainResolveOutputOk {
  readonly variant: 'ok';
  readonly toolchain: string;
  readonly solcPath: string;
  readonly version: string;
  readonly capabilities: readonly string[];
}

export interface SolidityToolchainResolveOutputNotInstalled {
  readonly variant: 'notInstalled';
  readonly installHint: string;
}

export interface SolidityToolchainResolveOutputEvmVersionUnsupported {
  readonly variant: 'evmVersionUnsupported';
  readonly requested: string;
  readonly supported: readonly string[];
}

export type SolidityToolchainResolveOutput = SolidityToolchainResolveOutputOk | SolidityToolchainResolveOutputNotInstalled | SolidityToolchainResolveOutputEvmVersionUnsupported;

export interface SolidityToolchainRegisterInput {
}

export interface SolidityToolchainRegisterOutput {
  readonly variant: 'ok';
  readonly name: string;
  readonly language: string;
  readonly capabilities: readonly string[];
}

// --- Variant constructors ---

export const resolveOk = (toolchain: string, solcPath: string, version: string, capabilities: readonly string[]): SolidityToolchainResolveOutput => ({ variant: 'ok', toolchain, solcPath, version, capabilities } as SolidityToolchainResolveOutput);
export const resolveNotInstalled = (installHint: string): SolidityToolchainResolveOutput => ({ variant: 'notInstalled', installHint } as SolidityToolchainResolveOutput);
export const resolveEvmVersionUnsupported = (requested: string, supported: readonly string[]): SolidityToolchainResolveOutput => ({ variant: 'evmVersionUnsupported', requested, supported } as SolidityToolchainResolveOutput);

export const registerOk = (name: string, language: string, capabilities: readonly string[]): SolidityToolchainRegisterOutput => ({ variant: 'ok', name, language, capabilities } as SolidityToolchainRegisterOutput);

