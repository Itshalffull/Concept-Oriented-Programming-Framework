// ActionEntity — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { actionEntityHandler } from './handler.js';
import type { ActionEntityStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): ActionEntityStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('ActionEntity conformance', () => {
  it('invariant 1: after register, get behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = actionEntityHandler;

    const a = 'u-test-invariant-001';

    // setup: register -> ok
    const registerResultSetup = await pipe(
      handler.register({
      concept: 'Article',
      name: 'create',
      params: '[]',
      variantRefs: '[]',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).action).toBe(a);
        return output;
      }),
    )();
    expect(E.isRight(registerResultSetup)).toBe(true);

    // assert: get -> ok
    const getResultAssert = await pipe(
      handler.get({
      action: a,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).action).toBe(a);
        expect((output as any).concept).toBe('Article');
        expect((output as any).name).toBe('create');
        expect((output as any).params).toBe('[]');
        expect((output as any).variantCount).toBe(0);
        return output;
      }),
    )();
    expect(E.isRight(getResultAssert)).toBe(true);

  });

});
