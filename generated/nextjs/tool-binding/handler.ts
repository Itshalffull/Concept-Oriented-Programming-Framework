// ToolBinding — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import type {
  ToolBindingStorage,
  ToolBindingDefineInput,
  ToolBindingDefineOutput,
  ToolBindingInvokeInput,
  ToolBindingInvokeOutput,
  ToolBindingToProviderFormatInput,
  ToolBindingToProviderFormatOutput,
  ToolBindingDiscoverInput,
  ToolBindingDiscoverOutput,
  ToolBindingSearchInput,
  ToolBindingSearchOutput,
} from './types.js';

import {
  defineOk,
  defineInvalid,
  invokeOk,
  invokeValidation_error,
  invokeTimeout,
  invokeExecution_error,
  invokeApproval_required,
  toProviderFormatOk,
  toProviderFormatNotfound,
  discoverOk,
  searchOk,
  searchEmpty,
} from './types.js';

export interface ToolBindingError {
  readonly code: string;
  readonly message: string;
}

export interface ToolBindingHandler {
  readonly define: (
    input: ToolBindingDefineInput,
    storage: ToolBindingStorage,
  ) => TE.TaskEither<ToolBindingError, ToolBindingDefineOutput>;
  readonly invoke: (
    input: ToolBindingInvokeInput,
    storage: ToolBindingStorage,
  ) => TE.TaskEither<ToolBindingError, ToolBindingInvokeOutput>;
  readonly toProviderFormat: (
    input: ToolBindingToProviderFormatInput,
    storage: ToolBindingStorage,
  ) => TE.TaskEither<ToolBindingError, ToolBindingToProviderFormatOutput>;
  readonly discover: (
    input: ToolBindingDiscoverInput,
    storage: ToolBindingStorage,
  ) => TE.TaskEither<ToolBindingError, ToolBindingDiscoverOutput>;
  readonly search: (
    input: ToolBindingSearchInput,
    storage: ToolBindingStorage,
  ) => TE.TaskEither<ToolBindingError, ToolBindingSearchOutput>;
}

// --- Implementation ---

export const toolBindingHandler: ToolBindingHandler = {
  define: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('toolbinding', input.name, { name: input.name, description: input.description, input_schema: input.input_schema, output_schema: input.output_schema, handler: input.handler, annotations: input.annotations, timeout_ms: input.timeout_ms, requires_approval: input.requires_approval });
          return defineOk(input.name);
        },
        (error): ToolBindingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  invoke: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('toolbinding', input.tool, { tool: input.tool, arguments: input.arguments });
          return invokeOk(input.tool, 0);
        },
        (error): ToolBindingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  toProviderFormat: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('toolbinding', input.tool, { tool: input.tool, provider: input.provider });
          return toProviderFormatOk(input.tool);
        },
        (error): ToolBindingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  discover: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('toolbinding');
          return discoverOk([]);
        },
        (error): ToolBindingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  search: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('toolbinding');
          return searchOk([]);
        },
        (error): ToolBindingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
