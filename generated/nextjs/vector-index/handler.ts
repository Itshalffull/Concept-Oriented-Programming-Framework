// VectorIndex — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  VectorIndexStorage,
  VectorIndexCreateInput,
  VectorIndexCreateOutput,
  VectorIndexEmbedInput,
  VectorIndexEmbedOutput,
  VectorIndexEmbedBatchInput,
  VectorIndexEmbedBatchOutput,
  VectorIndexAddInput,
  VectorIndexAddOutput,
  VectorIndexAddBatchInput,
  VectorIndexAddBatchOutput,
  VectorIndexSearchInput,
  VectorIndexSearchOutput,
  VectorIndexHybridSearchInput,
  VectorIndexHybridSearchOutput,
  VectorIndexMmrSearchInput,
  VectorIndexMmrSearchOutput,
  VectorIndexDeleteInput,
  VectorIndexDeleteOutput,
} from './types.js';

import {
  createOk,
  createInvalid,
  embedOk,
  embedError,
  embedBatchOk,
  embedBatchPartial,
  addOk,
  addDimension_mismatch,
  addBatchOk,
  addBatchPartial,
  searchOk,
  searchEmpty,
  hybridSearchOk,
  hybridSearchEmpty,
  mmrSearchOk,
  mmrSearchEmpty,
  deleteOk,
  deleteNotfound,
} from './types.js';

export interface VectorIndexError {
  readonly code: string;
  readonly message: string;
}

export interface VectorIndexHandler {
  readonly create: (
    input: VectorIndexCreateInput,
    storage: VectorIndexStorage,
  ) => TE.TaskEither<VectorIndexError, VectorIndexCreateOutput>;
  readonly embed: (
    input: VectorIndexEmbedInput,
    storage: VectorIndexStorage,
  ) => TE.TaskEither<VectorIndexError, VectorIndexEmbedOutput>;
  readonly embedBatch: (
    input: VectorIndexEmbedBatchInput,
    storage: VectorIndexStorage,
  ) => TE.TaskEither<VectorIndexError, VectorIndexEmbedBatchOutput>;
  readonly add: (
    input: VectorIndexAddInput,
    storage: VectorIndexStorage,
  ) => TE.TaskEither<VectorIndexError, VectorIndexAddOutput>;
  readonly addBatch: (
    input: VectorIndexAddBatchInput,
    storage: VectorIndexStorage,
  ) => TE.TaskEither<VectorIndexError, VectorIndexAddBatchOutput>;
  readonly search: (
    input: VectorIndexSearchInput,
    storage: VectorIndexStorage,
  ) => TE.TaskEither<VectorIndexError, VectorIndexSearchOutput>;
  readonly hybridSearch: (
    input: VectorIndexHybridSearchInput,
    storage: VectorIndexStorage,
  ) => TE.TaskEither<VectorIndexError, VectorIndexHybridSearchOutput>;
  readonly mmrSearch: (
    input: VectorIndexMmrSearchInput,
    storage: VectorIndexStorage,
  ) => TE.TaskEither<VectorIndexError, VectorIndexMmrSearchOutput>;
  readonly delete: (
    input: VectorIndexDeleteInput,
    storage: VectorIndexStorage,
  ) => TE.TaskEither<VectorIndexError, VectorIndexDeleteOutput>;
}

// --- Implementation ---

export const vectorIndexHandler: VectorIndexHandler = {
  create: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('vectorindex', input.dimensions, { dimensions: input.dimensions, distance_metric: input.distance_metric, index_type: input.index_type, backend: input.backend, embedding_model: input.embedding_model });
          return createOk(input.dimensions);
        },
        (error): VectorIndexError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  embed: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('vectorindex', input.index, { index: input.index, text: input.text });
          return embedOk([]);
        },
        (error): VectorIndexError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  embedBatch: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('vectorindex', input.index, { index: input.index, texts: input.texts });
          return embedBatchOk([], 0);
        },
        (error): VectorIndexError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  add: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('vectorindex', input.index, { index: input.index, id: input.id, vector: input.vector, metadata: input.metadata });
          return addOk();
        },
        (error): VectorIndexError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  addBatch: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('vectorindex', input.index, { index: input.index, items: input.items });
          return addBatchOk(0);
        },
        (error): VectorIndexError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  search: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('vectorindex');
          return searchOk([]);
        },
        (error): VectorIndexError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  hybridSearch: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('vectorindex', input.index, { index: input.index, query_vector: input.query_vector, keyword_query: input.keyword_query, vector_weight: input.vector_weight, k: input.k });
          return hybridSearchOk([]);
        },
        (error): VectorIndexError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  mmrSearch: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('vectorindex', input.index, { index: input.index, query_vector: input.query_vector, k: input.k, diversity: input.diversity });
          return mmrSearchOk([]);
        },
        (error): VectorIndexError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  delete: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('vectorindex', input.index),
        (error): VectorIndexError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(deleteNotfound(`${input.index} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('vectorindex', input.index);
                  return deleteOk();
                },
                (error): VectorIndexError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),
};
