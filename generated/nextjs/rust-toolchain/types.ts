// RustToolchain — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as O from 'fp-ts/Option';

// Storage interface for RustToolchain
export interface RustToolchainStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface RustToolchainResolveInput {
  readonly platform: string;
  readonly versionConstraint: O.Option<string>;
}

export interface RustToolchainResolveOutputOk {
  readonly variant: 'ok';
  readonly toolchain: string;
  readonly rustcPath: string;
  readonly version: string;
  readonly capabilities: readonly string[];
}

export interface RustToolchainResolveOutputNotInstalled {
  readonly variant: 'notInstalled';
  readonly installHint: string;
}

export interface RustToolchainResolveOutputTargetMissing {
  readonly variant: 'targetMissing';
  readonly target: string;
  readonly installHint: string;
}

export type RustToolchainResolveOutput = RustToolchainResolveOutputOk | RustToolchainResolveOutputNotInstalled | RustToolchainResolveOutputTargetMissing;

export interface RustToolchainRegisterInput {
}

export interface RustToolchainRegisterOutput {
  readonly variant: 'ok';
  readonly name: string;
  readonly language: string;
  readonly capabilities: readonly string[];
}

// --- Variant constructors ---

export const resolveOk = (toolchain: string, rustcPath: string, version: string, capabilities: readonly string[]): RustToolchainResolveOutput => ({ variant: 'ok', toolchain, rustcPath, version, capabilities } as RustToolchainResolveOutput);
export const resolveNotInstalled = (installHint: string): RustToolchainResolveOutput => ({ variant: 'notInstalled', installHint } as RustToolchainResolveOutput);
export const resolveTargetMissing = (target: string, installHint: string): RustToolchainResolveOutput => ({ variant: 'targetMissing', target, installHint } as RustToolchainResolveOutput);

export const registerOk = (name: string, language: string, capabilities: readonly string[]): RustToolchainRegisterOutput => ({ variant: 'ok', name, language, capabilities } as RustToolchainRegisterOutput);

