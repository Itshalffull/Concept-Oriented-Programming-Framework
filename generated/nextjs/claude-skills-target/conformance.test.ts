// ClaudeSkillsTarget — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { claudeSkillsTargetHandler } from './handler.js';
import type { ClaudeSkillsTargetStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): ClaudeSkillsTargetStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('ClaudeSkillsTarget conformance', () => {
  it('invariant 1: after generate, listSkills behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = claudeSkillsTargetHandler;

    let s: any = 'u-test-invariant-001';
    let f: any = 'u-test-invariant-002';
    let all: any = 'u-test-invariant-003';
    let e: any = 'u-test-invariant-004';
    let fl: any = 'u-test-invariant-005';

    // setup: generate -> ok
    const generateResultSetup = await pipe(
      handler.generate({
      projection: 'spec-parser-projection',
      config: '{"progressive":true}',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        s = (output as any).skills;
        f = (output as any).files;
        return output;
      }),
    )();
    expect(E.isRight(generateResultSetup)).toBe(true);

    // assert: listSkills -> ok
    const listSkillsResultAssert = await pipe(
      handler.listSkills({
      kit: 'test-kit',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        all = (output as any).skills;
        e = (output as any).enriched;
        fl = (output as any).flat;
        return output;
      }),
    )();
    expect(E.isRight(listSkillsResultAssert)).toBe(true);

  });

});
