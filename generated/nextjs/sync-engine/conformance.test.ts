// SyncEngine — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { syncEngineHandler } from './handler.js';
import type { SyncEngineStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): SyncEngineStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('SyncEngine conformance', () => {
  it('invariant 1: after registerSync, onCompletion behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = syncEngineHandler;

    const inv = 'u-test-invariant-001';

    // setup: registerSync -> ok
    const registerSyncResultSetup = await pipe(
      handler.registerSync({
      sync: undefined,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(registerSyncResultSetup)).toBe(true);

    // assert: onCompletion -> ok
    const onCompletionResultAssert = await pipe(
      handler.onCompletion({
      completion: undefined,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).invocations).toBe(inv);
        return output;
      }),
    )();
    expect(E.isRight(onCompletionResultAssert)).toBe(true);

  });

});
