// Attribution — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { attributionHandler } from './handler.js';
import type { AttributionStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): AttributionStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Attribution conformance', () => {
  it('invariant 1: after attribute, blame behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = attributionHandler;

    const c = 'u-test-invariant-001';
    const r = 'u-test-invariant-002';
    const a = 'u-test-invariant-003';
    const ch = 'u-test-invariant-004';
    const id = 'u-test-invariant-005';
    const m = 'u-test-invariant-006';

    // setup: attribute -> ok
    const attributeResultSetup = await pipe(
      handler.attribute({
      contentRef: c,
      region: r,
      agent: a,
      changeRef: ch,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).attributionId).toBe(id);
        return output;
      }),
    )();
    expect(E.isRight(attributeResultSetup)).toBe(true);

    // assert: blame -> ok
    const blameResultAssert = await pipe(
      handler.blame({
      contentRef: c,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).map).toBe(m);
        return output;
      }),
    )();
    expect(E.isRight(blameResultAssert)).toBe(true);

  });

});
