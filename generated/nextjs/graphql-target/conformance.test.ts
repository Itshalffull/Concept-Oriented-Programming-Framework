// GraphqlTarget — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { graphqlTargetHandler } from './handler.js';
import type { GraphqlTargetStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): GraphqlTargetStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('GraphqlTarget conformance', () => {
  it('invariant 1: after generate, listOperations behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = graphqlTargetHandler;

    let t: any = 'u-test-invariant-001';
    let f: any = 'u-test-invariant-002';
    let q: any = 'u-test-invariant-003';
    let m: any = 'u-test-invariant-004';
    let s: any = 'u-test-invariant-005';

    // setup: generate -> ok
    const generateResultSetup = await pipe(
      handler.generate({
      projection: 'order-projection',
      config: '{}',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        t = (output as any).types;
        f = (output as any).files;
        return output;
      }),
    )();
    expect(E.isRight(generateResultSetup)).toBe(true);

    // assert: listOperations -> ok
    const listOperationsResultAssert = await pipe(
      handler.listOperations({
      concept: 'Order',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        q = (output as any).queries;
        m = (output as any).mutations;
        s = (output as any).subscriptions;
        return output;
      }),
    )();
    expect(E.isRight(listOperationsResultAssert)).toBe(true);

  });

});
