// Motion — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { motionHandler } from './handler.js';
import type { MotionStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): MotionStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Motion conformance', () => {
  it('invariant 1: after defineDuration, defineTransition, defineDuration behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = motionHandler;

    const o = 'u-test-invariant-001';
    const o2 = 'u-test-invariant-002';
    const o3 = 'u-test-invariant-003';

    // setup: defineDuration -> ok
    const defineDurationResultSetup = await pipe(
      handler.defineDuration({
      motion: o,
      name: 'normal',
      ms: 200,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).motion).toBe(o);
        return output;
      }),
    )();
    expect(E.isRight(defineDurationResultSetup)).toBe(true);

    // assert: defineTransition -> ok
    const defineTransitionResultAssert = await pipe(
      handler.defineTransition({
      motion: o2,
      name: 'fade',
      config: '{ "property": "opacity", "duration": "normal", "easing": "ease-out" }',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).motion).toBe(o2);
        return output;
      }),
    )();
    expect(E.isRight(defineTransitionResultAssert)).toBe(true);

    // assert: defineDuration -> invalid
    const defineDurationResultAssert = await pipe(
      handler.defineDuration({
      motion: o3,
      name: 'bad',
      ms: -1,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('invalid');
        expect((output as any).message).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(defineDurationResultAssert)).toBe(true);

  });

});
