// FormBuilder — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { formBuilderHandler } from './handler.js';
import type { FormBuilderStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): FormBuilderStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('FormBuilder conformance', () => {
  it('invariant 1: after buildForm, buildForm behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = formBuilderHandler;

    const f = 'u-test-invariant-001';
    const d = 'u-test-invariant-002';

    // setup: buildForm -> ok
    const buildFormResultSetup = await pipe(
      handler.buildForm({
      form: f,
      schema: 'user-profile',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).definition).toBe(d);
        return output;
      }),
    )();
    expect(E.isRight(buildFormResultSetup)).toBe(true);

    // assert: buildForm -> ok
    const buildFormResultAssert = await pipe(
      handler.buildForm({
      form: f,
      schema: 'user-profile',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).definition).toBe(d);
        return output;
      }),
    )();
    expect(E.isRight(buildFormResultAssert)).toBe(true);

  });

});
