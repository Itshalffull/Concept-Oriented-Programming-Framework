// RestTarget — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for RestTarget
export interface RestTargetStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface RestTargetGenerateInput {
  readonly projection: string;
  readonly config: string;
}

export interface RestTargetGenerateOutputOk {
  readonly variant: 'ok';
  readonly routes: readonly string[];
  readonly files: readonly string[];
}

export interface RestTargetGenerateOutputAmbiguousMapping {
  readonly variant: 'ambiguousMapping';
  readonly action: string;
  readonly reason: string;
}

export type RestTargetGenerateOutput = RestTargetGenerateOutputOk | RestTargetGenerateOutputAmbiguousMapping;

export interface RestTargetValidateInput {
  readonly route: string;
}

export interface RestTargetValidateOutputOk {
  readonly variant: 'ok';
  readonly route: string;
}

export interface RestTargetValidateOutputPathConflict {
  readonly variant: 'pathConflict';
  readonly route: string;
  readonly conflicting: string;
  readonly reason: string;
}

export type RestTargetValidateOutput = RestTargetValidateOutputOk | RestTargetValidateOutputPathConflict;

export interface RestTargetListRoutesInput {
  readonly concept: string;
}

export interface RestTargetListRoutesOutput {
  readonly variant: 'ok';
  readonly routes: readonly string[];
  readonly methods: readonly string[];
}

// --- Variant constructors ---

export const generateOk = (routes: readonly string[], files: readonly string[]): RestTargetGenerateOutput => ({ variant: 'ok', routes, files } as RestTargetGenerateOutput);
export const generateAmbiguousMapping = (action: string, reason: string): RestTargetGenerateOutput => ({ variant: 'ambiguousMapping', action, reason } as RestTargetGenerateOutput);

export const validateOk = (route: string): RestTargetValidateOutput => ({ variant: 'ok', route } as RestTargetValidateOutput);
export const validatePathConflict = (route: string, conflicting: string, reason: string): RestTargetValidateOutput => ({ variant: 'pathConflict', route, conflicting, reason } as RestTargetValidateOutput);

export const listRoutesOk = (routes: readonly string[], methods: readonly string[]): RestTargetListRoutesOutput => ({ variant: 'ok', routes, methods } as RestTargetListRoutesOutput);

