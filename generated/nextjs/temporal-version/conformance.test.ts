// TemporalVersion — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { temporalVersionHandler } from './handler.js';
import type { TemporalVersionStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): TemporalVersionStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('TemporalVersion conformance', () => {
  it('invariant 1: after record, asOf behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = temporalVersionHandler;

    const h = 'u-test-invariant-001';
    const vf = 'u-test-invariant-002';
    const v = 'u-test-invariant-003';

    // setup: record -> ok
    const recordResultSetup = await pipe(
      handler.record({
      contentHash: h,
      validFrom: vf,
      validTo: _,
      metadata: _,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).versionId).toBe(v);
        return output;
      }),
    )();
    expect(E.isRight(recordResultSetup)).toBe(true);

    // assert: asOf -> ok
    const asOfResultAssert = await pipe(
      handler.asOf({
      systemTime: _,
      validTime: vf,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).versionId).toBe(v);
        expect((output as any).contentHash).toBe(h);
        return output;
      }),
    )();
    expect(E.isRight(asOfResultAssert)).toBe(true);

  });

  it('invariant 2: after record, current behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = temporalVersionHandler;

    const h = 'u-test-invariant-001';
    const v = 'u-test-invariant-002';

    // setup: record -> ok
    const recordResultSetup = await pipe(
      handler.record({
      contentHash: h,
      validFrom: _,
      validTo: _,
      metadata: _,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).versionId).toBe(v);
        return output;
      }),
    )();
    expect(E.isRight(recordResultSetup)).toBe(true);

    // assert: current -> ok
    const currentResultAssert = await pipe(
      handler.current({

      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).versionId).toBe(v);
        expect((output as any).contentHash).toBe(h);
        return output;
      }),
    )();
    expect(E.isRight(currentResultAssert)).toBe(true);

  });

});
