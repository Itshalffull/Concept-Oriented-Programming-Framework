// ClaudeMdTarget — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ClaudeMdTargetStorage,
  ClaudeMdTargetGenerateInput,
  ClaudeMdTargetGenerateOutput,
  ClaudeMdTargetValidateInput,
  ClaudeMdTargetValidateOutput,
  ClaudeMdTargetPreviewInput,
  ClaudeMdTargetPreviewOutput,
} from './types.js';

import {
  generateOk,
  generateNoProjections,
  validateOk,
  validateMissingSection,
  previewOk,
} from './types.js';

export interface ClaudeMdTargetError {
  readonly code: string;
  readonly message: string;
}

export interface ClaudeMdTargetHandler {
  readonly generate: (
    input: ClaudeMdTargetGenerateInput,
    storage: ClaudeMdTargetStorage,
  ) => TE.TaskEither<ClaudeMdTargetError, ClaudeMdTargetGenerateOutput>;
  readonly validate: (
    input: ClaudeMdTargetValidateInput,
    storage: ClaudeMdTargetStorage,
  ) => TE.TaskEither<ClaudeMdTargetError, ClaudeMdTargetValidateOutput>;
  readonly preview: (
    input: ClaudeMdTargetPreviewInput,
    storage: ClaudeMdTargetStorage,
  ) => TE.TaskEither<ClaudeMdTargetError, ClaudeMdTargetPreviewOutput>;
}

// --- Implementation ---

export const claudeMdTargetHandler: ClaudeMdTargetHandler = {
  generate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('claudemdtarget', input.projection, { projection: input.projection, config: input.config });
          return generateOk(input.projection, []);
        },
        (error): ClaudeMdTargetError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  validate: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('claudemdtarget', input.document);
          return validateOk(input.document);
        },
        (error): ClaudeMdTargetError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  preview: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('claudemdtarget', input.config, { config: input.config });
          return previewOk([], 0);
        },
        (error): ClaudeMdTargetError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
