// VerificationRun — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { verificationRunHandler } from './handler.js';
import type { VerificationRunStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): VerificationRunStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('VerificationRun conformance', () => {
  it('invariant 1: after start, complete behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = verificationRunHandler;

    let r: any = 'u-test-invariant-001';
    const res = 'u-test-invariant-002';
    const usage = 'u-test-invariant-003';

    // setup: start -> ok
    const startResultSetup = await pipe(
      handler.start({
      target_symbol: 'clef/concept/Password',
      properties: undefined,
      solver: 'z3',
      timeout_ms: 10000,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        r = (output as any).run;
        return output;
      }),
    )();
    expect(E.isRight(startResultSetup)).toBe(true);

    // assert: complete -> ok
    const completeResultAssert = await pipe(
      handler.complete({
      run: r,
      results: res,
      resource_usage: usage,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).run).toBe(r);
        expect((output as any).proved).toBe(2);
        expect((output as any).refuted).toBe(0);
        expect((output as any).unknown).toBe(0);
        return output;
      }),
    )();
    expect(E.isRight(completeResultAssert)).toBe(true);

  });

});
