// Interactor — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { interactorHandler } from './handler.js';
import type { InteractorStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): InteractorStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Interactor conformance', () => {
  it('invariant 1: after define, classify behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = interactorHandler;

    const i = 'u-test-invariant-001';

    // setup: define -> ok
    const defineResultSetup = await pipe(
      handler.define({
      interactor: i,
      name: 'single-choice',
      category: 'selection',
      properties: '{ "cardinality": "one", "comparison": true }',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).interactor).toBe(i);
        return output;
      }),
    )();
    expect(E.isRight(defineResultSetup)).toBe(true);

    // assert: classify -> ok
    const classifyResultAssert = await pipe(
      handler.classify({
      interactor: _,
      fieldType: 'T -> T',
      constraints: '{ "enum": ["A","B","C"] }',
      intent: _,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).interactor).toBe(_);
        expect((output as any).confidence).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(classifyResultAssert)).toBe(true);

  });

});
