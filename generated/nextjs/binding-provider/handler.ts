// BindingProvider — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  BindingProviderStorage,
  BindingProviderInitializeInput,
  BindingProviderInitializeOutput,
  BindingProviderBindInput,
  BindingProviderBindOutput,
  BindingProviderSyncInput,
  BindingProviderSyncOutput,
  BindingProviderInvokeInput,
  BindingProviderInvokeOutput,
  BindingProviderUnbindInput,
  BindingProviderUnbindOutput,
} from './types.js';

import {
  initializeOk,
  initializeConfigError,
  bindOk,
  bindInvalid,
  syncOk,
  syncError,
  invokeOk,
  invokeError,
  unbindOk,
  unbindNotfound,
} from './types.js';

export interface BindingProviderError {
  readonly code: string;
  readonly message: string;
}

export interface BindingProviderHandler {
  readonly initialize: (
    input: BindingProviderInitializeInput,
    storage: BindingProviderStorage,
  ) => TE.TaskEither<BindingProviderError, BindingProviderInitializeOutput>;
  readonly bind: (
    input: BindingProviderBindInput,
    storage: BindingProviderStorage,
  ) => TE.TaskEither<BindingProviderError, BindingProviderBindOutput>;
  readonly sync: (
    input: BindingProviderSyncInput,
    storage: BindingProviderStorage,
  ) => TE.TaskEither<BindingProviderError, BindingProviderSyncOutput>;
  readonly invoke: (
    input: BindingProviderInvokeInput,
    storage: BindingProviderStorage,
  ) => TE.TaskEither<BindingProviderError, BindingProviderInvokeOutput>;
  readonly unbind: (
    input: BindingProviderUnbindInput,
    storage: BindingProviderStorage,
  ) => TE.TaskEither<BindingProviderError, BindingProviderUnbindOutput>;
}

// --- Implementation ---

export const bindingProviderHandler: BindingProviderHandler = {
  initialize: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('bindingprovider', input.provider, { provider: input.provider, config: input.config });
          return initializeOk(input.provider, input.provider);
        },
        (error): BindingProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  bind: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('bindingprovider', input.provider, { provider: input.provider, concept: input.concept, mode: input.mode });
          return bindOk(input.provider);
        },
        (error): BindingProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  sync: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('bindingprovider', input.provider),
        (error): BindingProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(syncError(`${input.provider} not found`)),
            (existing) =>
              TE.tryCatch(
                async () => {
                  const updated = { ...existing, provider: input.provider };
                  await storage.put('bindingprovider', input.provider, updated);
                  return syncOk(input.provider);
                },
                (error): BindingProviderError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  invoke: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('bindingprovider', input.provider, { provider: input.provider, action: input.action, input: input.input });
          return invokeOk(input.provider, input.provider);
        },
        (error): BindingProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  unbind: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('bindingprovider', input.provider, { provider: input.provider });
          return unbindOk(input.provider);
        },
        (error): BindingProviderError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
