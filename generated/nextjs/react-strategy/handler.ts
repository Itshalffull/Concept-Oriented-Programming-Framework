// ReactStrategy — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ReactStrategyStorage,
  ReactStrategyExecuteInput,
  ReactStrategyExecuteOutput,
  ReactStrategyStepOnceInput,
  ReactStrategyStepOnceOutput,
  ReactStrategyAddObservationInput,
  ReactStrategyAddObservationOutput,
  ReactStrategyGetStateInput,
  ReactStrategyGetStateOutput,
} from './types.js';

import {
  executeOk,
  executeMax_iterations,
  executeError,
  stepOnceThought,
  stepOnceAction_request,
  stepOnceFinal_answer,
  addObservationOk,
  getStateOk,
} from './types.js';

export interface ReactStrategyError {
  readonly code: string;
  readonly message: string;
}

export interface ReactStrategyHandler {
  readonly execute: (
    input: ReactStrategyExecuteInput,
    storage: ReactStrategyStorage,
  ) => TE.TaskEither<ReactStrategyError, ReactStrategyExecuteOutput>;
  readonly stepOnce: (
    input: ReactStrategyStepOnceInput,
    storage: ReactStrategyStorage,
  ) => TE.TaskEither<ReactStrategyError, ReactStrategyStepOnceOutput>;
  readonly addObservation: (
    input: ReactStrategyAddObservationInput,
    storage: ReactStrategyStorage,
  ) => TE.TaskEither<ReactStrategyError, ReactStrategyAddObservationOutput>;
  readonly getState: (
    input: ReactStrategyGetStateInput,
    storage: ReactStrategyStorage,
  ) => TE.TaskEither<ReactStrategyError, ReactStrategyGetStateOutput>;
}

// --- Implementation ---

export const reactStrategyHandler: ReactStrategyHandler = {
  execute: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('reactstrategy', input.agent_ref, { agent_ref: input.agent_ref, goal: input.goal, context: input.context, available_tools: input.available_tools, max_iterations: input.max_iterations });
          return executeOk(input.agent_ref, 0, 0, []);
        },
        (error): ReactStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  stepOnce: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('reactstrategy', input.session, { session: input.session });
          return stepOnceOk();
        },
        (error): ReactStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  addObservation: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('reactstrategy', input.session, { session: input.session, observation: input.observation });
          return addObservationOk(input.session);
        },
        (error): ReactStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getState: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('reactstrategy', input.session);
          return getStateOk((record as any).scratchpad);
        },
        (error): ReactStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
