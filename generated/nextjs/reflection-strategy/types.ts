// ReflectionStrategy — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for ReflectionStrategy
export interface ReflectionStrategyStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface ReflectionStrategyExecuteInput {
  readonly agent_ref: string;
  readonly goal: string;
  readonly context: string;
  readonly available_tools: readonly string[];
  readonly max_iterations: number;
}

export interface ReflectionStrategyExecuteOutputOk {
  readonly variant: 'ok';
  readonly result: string;
  readonly rounds: number;
  readonly history: readonly { readonly draft: string; readonly critique: string }[];
}

export interface ReflectionStrategyExecuteOutputMax_rounds {
  readonly variant: 'max_rounds';
  readonly best_draft: string;
  readonly rounds: number;
}

export type ReflectionStrategyExecuteOutput = ReflectionStrategyExecuteOutputOk | ReflectionStrategyExecuteOutputMax_rounds;

export interface ReflectionStrategyCritiqueInput {
  readonly session: string;
  readonly draft: string;
}

export interface ReflectionStrategyCritiqueOutput {
  readonly variant: 'ok';
  readonly critique: string;
  readonly satisfactory: boolean;
}

export interface ReflectionStrategyReviseInput {
  readonly session: string;
  readonly draft: string;
  readonly critique: string;
}

export interface ReflectionStrategyReviseOutput {
  readonly variant: 'ok';
  readonly revised: string;
}

export interface ReflectionStrategyGetStateInput {
  readonly session: string;
}

export interface ReflectionStrategyGetStateOutput {
  readonly variant: 'ok';
  readonly history: readonly { readonly draft: string; readonly critique: string; readonly round: number }[];
}

// --- Variant constructors ---

export const executeOk = (result: string, rounds: number, history: readonly { readonly draft: string; readonly critique: string }[]): ReflectionStrategyExecuteOutput => ({ variant: 'ok', result, rounds, history } as ReflectionStrategyExecuteOutput);
export const executeMax_rounds = (best_draft: string, rounds: number): ReflectionStrategyExecuteOutput => ({ variant: 'max_rounds', best_draft, rounds } as ReflectionStrategyExecuteOutput);

export const critiqueOk = (critique: string, satisfactory: boolean): ReflectionStrategyCritiqueOutput => ({ variant: 'ok', critique, satisfactory } as ReflectionStrategyCritiqueOutput);

export const reviseOk = (revised: string): ReflectionStrategyReviseOutput => ({ variant: 'ok', revised } as ReflectionStrategyReviseOutput);

export const getStateOk = (history: readonly { readonly draft: string; readonly critique: string; readonly round: number }[]): ReflectionStrategyGetStateOutput => ({ variant: 'ok', history } as ReflectionStrategyGetStateOutput);

