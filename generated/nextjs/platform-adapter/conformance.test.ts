// PlatformAdapter — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { platformAdapterHandler } from './handler.js';
import type { PlatformAdapterStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): PlatformAdapterStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('PlatformAdapter conformance', () => {
  it('invariant 1: after register, mapNavigation behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = platformAdapterHandler;

    const d = 'u-test-invariant-001';

    // setup: register -> ok
    const registerResultSetup = await pipe(
      handler.register({
      adapter: d,
      platform: 'browser',
      config: '{}',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).adapter).toBe(d);
        return output;
      }),
    )();
    expect(E.isRight(registerResultSetup)).toBe(true);

    // assert: mapNavigation -> ok
    const mapNavigationResultAssert = await pipe(
      handler.mapNavigation({
      adapter: d,
      transition: '{ "type": "push" }',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).adapter).toBe(d);
        expect((output as any).platformAction).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(mapNavigationResultAssert)).toBe(true);

  });

});
