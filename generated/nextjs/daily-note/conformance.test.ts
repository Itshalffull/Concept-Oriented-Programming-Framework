// DailyNote — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { dailyNoteHandler } from './handler.js';
import type { DailyNoteStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): DailyNoteStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('DailyNote conformance', () => {
  it('invariant 1: after getOrCreateToday, getOrCreateToday behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = dailyNoteHandler;

    const n = 'u-test-invariant-001';

    // setup: getOrCreateToday -> ok
    const getOrCreateTodayResultSetup = await pipe(
      handler.getOrCreateToday({
      note: n,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).note).toBe(n);
        expect((output as any).created).toBe(true);
        return output;
      }),
    )();
    expect(E.isRight(getOrCreateTodayResultSetup)).toBe(true);

    // assert: getOrCreateToday -> ok
    const getOrCreateTodayResultAssert = await pipe(
      handler.getOrCreateToday({
      note: n,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).note).toBe(n);
        expect((output as any).created).toBe(false);
        return output;
      }),
    )();
    expect(E.isRight(getOrCreateTodayResultAssert)).toBe(true);

  });

});
