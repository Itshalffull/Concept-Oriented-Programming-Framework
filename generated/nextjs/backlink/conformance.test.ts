// Backlink — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { backlinkHandler } from './handler.js';
import type { BacklinkStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): BacklinkStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Backlink conformance', () => {
  it('invariant 1: after reindex, getBacklinks behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = backlinkHandler;

    const n = 'u-test-invariant-001';
    const x = 'u-test-invariant-002';
    const s = 'u-test-invariant-003';

    // setup: reindex -> ok
    const reindexResultSetup = await pipe(
      handler.reindex({

      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).count).toBe(n);
        return output;
      }),
    )();
    expect(E.isRight(reindexResultSetup)).toBe(true);

    // assert: getBacklinks -> ok
    const getBacklinksResultAssert = await pipe(
      handler.getBacklinks({
      entity: x,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).sources).toBe(s);
        return output;
      }),
    )();
    expect(E.isRight(getBacklinksResultAssert)).toBe(true);

  });

});
