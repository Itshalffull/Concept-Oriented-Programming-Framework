// Health — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { healthHandler } from './handler.js';
import type { HealthStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): HealthStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Health conformance', () => {
  it('invariant 1: after checkConcept, checkKit behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = healthHandler;

    let h: any = 'u-test-invariant-001';
    let h2: any = 'u-test-invariant-002';
    let cr: any = 'u-test-invariant-003';
    let sr: any = 'u-test-invariant-004';

    // setup: checkConcept -> ok
    const checkConceptResultSetup = await pipe(
      handler.checkConcept({
      concept: 'User',
      runtime: 'server',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        h = (output as any).check;
        expect((output as any).latencyMs).toBe(15);
        return output;
      }),
    )();
    expect(E.isRight(checkConceptResultSetup)).toBe(true);

    // assert: checkKit -> ok
    const checkKitResultAssert = await pipe(
      handler.checkKit({
      kit: 'auth',
      environment: 'staging',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        h2 = (output as any).check;
        cr = (output as any).conceptResults;
        sr = (output as any).syncResults;
        return output;
      }),
    )();
    expect(E.isRight(checkKitResultAssert)).toBe(true);

  });

});
