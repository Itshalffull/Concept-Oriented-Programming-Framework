// ChainMonitor — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { chainMonitorHandler } from './handler.js';
import type { ChainMonitorStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): ChainMonitorStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('ChainMonitor conformance', () => {
  it('invariant 1: after awaitFinality, status behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = chainMonitorHandler;

    const tx = 'u-test-invariant-001';

    // setup: awaitFinality -> ok
    const awaitFinalityResultSetup = await pipe(
      handler.awaitFinality({
      txHash: tx,
      level: 'confirmations',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).chain).toBe('ethereum');
        expect((output as any).block).toBe(100);
        expect((output as any).confirmations).toBe(12);
        return output;
      }),
    )();
    expect(E.isRight(awaitFinalityResultSetup)).toBe(true);

    // assert: status -> ok
    const statusResultAssert = await pipe(
      handler.status({
      txHash: tx,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).chain).toBe('ethereum');
        expect((output as any).block).toBe(100);
        expect((output as any).confirmations).toBe(12);
        return output;
      }),
    )();
    expect(E.isRight(statusResultAssert)).toBe(true);

  });

  it('invariant 2: after awaitFinality, status behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = chainMonitorHandler;

    const tx = 'u-test-invariant-001';

    // setup: awaitFinality -> reorged
    const awaitFinalityResultSetup = await pipe(
      handler.awaitFinality({
      txHash: tx,
      level: 'confirmations',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('reorged');
        expect((output as any).txHash).toBe(tx);
        expect((output as any).depth).toBe(3);
        return output;
      }),
    )();
    expect(E.isRight(awaitFinalityResultSetup)).toBe(true);

    // assert: status -> ok
    const statusResultAssert = await pipe(
      handler.status({
      txHash: tx,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).chain).toBe('ethereum');
        expect((output as any).block).toBe(100);
        expect((output as any).confirmations).toBe(0);
        return output;
      }),
    )();
    expect(E.isRight(statusResultAssert)).toBe(true);

  });

});
