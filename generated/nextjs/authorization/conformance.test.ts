// Authorization — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { authorizationHandler } from './handler.js';
import type { AuthorizationStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): AuthorizationStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Authorization conformance', () => {
  it('invariant 1: after grantPermission, assignRole, checkPermission behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = authorizationHandler;

    const x = 'u-test-invariant-001';

    // setup: grantPermission -> ok
    const grantPermissionResultSetup = await pipe(
      handler.grantPermission({
      role: 'admin',
      permission: 'write',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).role).toBe('admin');
        expect((output as any).permission).toBe('write');
        return output;
      }),
    )();
    expect(E.isRight(grantPermissionResultSetup)).toBe(true);

    // setup: assignRole -> ok
    const assignRoleResultSetup = await pipe(
      handler.assignRole({
      user: x,
      role: 'admin',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).user).toBe(x);
        expect((output as any).role).toBe('admin');
        return output;
      }),
    )();
    expect(E.isRight(assignRoleResultSetup)).toBe(true);

    // assert: checkPermission -> ok
    const checkPermissionResultAssert = await pipe(
      handler.checkPermission({
      user: x,
      permission: 'write',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).granted).toBe(true);
        return output;
      }),
    )();
    expect(E.isRight(checkPermissionResultAssert)).toBe(true);

  });

  it('invariant 2: after grantPermission, assignRole, revokePermission, checkPermission behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = authorizationHandler;

    const x = 'u-test-invariant-001';

    // setup: grantPermission -> ok
    const grantPermissionResultSetup = await pipe(
      handler.grantPermission({
      role: 'editor',
      permission: 'publish',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).role).toBe('editor');
        expect((output as any).permission).toBe('publish');
        return output;
      }),
    )();
    expect(E.isRight(grantPermissionResultSetup)).toBe(true);

    // setup: assignRole -> ok
    const assignRoleResultSetup = await pipe(
      handler.assignRole({
      user: x,
      role: 'editor',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).user).toBe(x);
        expect((output as any).role).toBe('editor');
        return output;
      }),
    )();
    expect(E.isRight(assignRoleResultSetup)).toBe(true);

    // setup: revokePermission -> ok
    const revokePermissionResultSetup = await pipe(
      handler.revokePermission({
      role: 'editor',
      permission: 'publish',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).role).toBe('editor');
        expect((output as any).permission).toBe('publish');
        return output;
      }),
    )();
    expect(E.isRight(revokePermissionResultSetup)).toBe(true);

    // assert: checkPermission -> ok
    const checkPermissionResultAssert = await pipe(
      handler.checkPermission({
      user: x,
      permission: 'publish',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).granted).toBe(false);
        return output;
      }),
    )();
    expect(E.isRight(checkPermissionResultAssert)).toBe(true);

  });

});
