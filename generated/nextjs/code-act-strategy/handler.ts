// CodeActStrategy — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  CodeActStrategyStorage,
  CodeActStrategyExecuteInput,
  CodeActStrategyExecuteOutput,
  CodeActStrategyGenerateCodeInput,
  CodeActStrategyGenerateCodeOutput,
  CodeActStrategyExecuteCodeInput,
  CodeActStrategyExecuteCodeOutput,
  CodeActStrategyGetStateInput,
  CodeActStrategyGetStateOutput,
} from './types.js';

import {
  executeOk,
  executeMax_iterations,
  executeSandbox_error,
  generateCodeOk,
  executeCodeOk,
  executeCodeError,
  executeCodeTimeout,
  getStateOk,
} from './types.js';

export interface CodeActStrategyError {
  readonly code: string;
  readonly message: string;
}

export interface CodeActStrategyHandler {
  readonly execute: (
    input: CodeActStrategyExecuteInput,
    storage: CodeActStrategyStorage,
  ) => TE.TaskEither<CodeActStrategyError, CodeActStrategyExecuteOutput>;
  readonly generateCode: (
    input: CodeActStrategyGenerateCodeInput,
    storage: CodeActStrategyStorage,
  ) => TE.TaskEither<CodeActStrategyError, CodeActStrategyGenerateCodeOutput>;
  readonly executeCode: (
    input: CodeActStrategyExecuteCodeInput,
    storage: CodeActStrategyStorage,
  ) => TE.TaskEither<CodeActStrategyError, CodeActStrategyExecuteCodeOutput>;
  readonly getState: (
    input: CodeActStrategyGetStateInput,
    storage: CodeActStrategyStorage,
  ) => TE.TaskEither<CodeActStrategyError, CodeActStrategyGetStateOutput>;
}

// --- Implementation ---

export const codeActStrategyHandler: CodeActStrategyHandler = {
  execute: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('codeactstrategy', input.agent_ref, { agent_ref: input.agent_ref, goal: input.goal, context: input.context, available_tools: input.available_tools, max_iterations: input.max_iterations });
          return executeOk(input.agent_ref, 0, input.agent_ref);
        },
        (error): CodeActStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  generateCode: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('codeactstrategy', input.session, { session: input.session, goal: input.goal, previous_error: input.previous_error });
          return generateCodeOk(input.session, input.session);
        },
        (error): CodeActStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  executeCode: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('codeactstrategy', input.session, { session: input.session, code: input.code });
          return executeCodeOk(input.session);
        },
        (error): CodeActStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getState: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('codeactstrategy', input.session);
          return getStateOk((record as any).history);
        },
        (error): CodeActStrategyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
