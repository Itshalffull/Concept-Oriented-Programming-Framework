// EloRating — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  EloRatingStorage,
  EloRatingConfigureInput,
  EloRatingConfigureOutput,
  EloRatingRecordOutcomeInput,
  EloRatingRecordOutcomeOutput,
  EloRatingRecordDrawInput,
  EloRatingRecordDrawOutput,
  EloRatingGetRatingInput,
  EloRatingGetRatingOutput,
} from './types.js';

import {
  configureConfigured,
  recordOutcomeUpdated,
  recordDrawUpdated,
  getRatingRating,
  getRatingUnrated,
} from './types.js';

export interface EloRatingError {
  readonly code: string;
  readonly message: string;
}

export interface EloRatingHandler {
  readonly configure: (
    input: EloRatingConfigureInput,
    storage: EloRatingStorage,
  ) => TE.TaskEither<EloRatingError, EloRatingConfigureOutput>;
  readonly recordOutcome: (
    input: EloRatingRecordOutcomeInput,
    storage: EloRatingStorage,
  ) => TE.TaskEither<EloRatingError, EloRatingRecordOutcomeOutput>;
  readonly recordDraw: (
    input: EloRatingRecordDrawInput,
    storage: EloRatingStorage,
  ) => TE.TaskEither<EloRatingError, EloRatingRecordDrawOutput>;
  readonly getRating: (
    input: EloRatingGetRatingInput,
    storage: EloRatingStorage,
  ) => TE.TaskEither<EloRatingError, EloRatingGetRatingOutput>;
}

// --- Implementation ---

export const eloRatingHandler: EloRatingHandler = {
  configure: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('elorating', input.kFactor, { kFactor: input.kFactor, initialRating: input.initialRating, kFactorDecay: input.kFactorDecay });
          return configureOk();
        },
        (error): EloRatingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  recordOutcome: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('elorating', input.config, { config: input.config, winner: input.winner, loser: input.loser });
          return recordOutcomeOk();
        },
        (error): EloRatingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  recordDraw: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('elorating', input.config, { config: input.config, participantA: input.participantA, participantB: input.participantB });
          return recordDrawOk();
        },
        (error): EloRatingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getRating: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('elorating', input.config);
          return getRatingOk();
        },
        (error): EloRatingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
