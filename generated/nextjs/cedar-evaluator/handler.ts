// CedarEvaluator — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  CedarEvaluatorStorage,
  CedarEvaluatorLoadPoliciesInput,
  CedarEvaluatorLoadPoliciesOutput,
  CedarEvaluatorAuthorizeInput,
  CedarEvaluatorAuthorizeOutput,
  CedarEvaluatorVerifyInput,
  CedarEvaluatorVerifyOutput,
} from './types.js';

import {
  loadPoliciesLoaded,
  loadPoliciesValidation_error,
  authorizeAllow,
  authorizeDeny,
  verifyVerified,
  verifyCounterexample,
} from './types.js';

export interface CedarEvaluatorError {
  readonly code: string;
  readonly message: string;
}

export interface CedarEvaluatorHandler {
  readonly loadPolicies: (
    input: CedarEvaluatorLoadPoliciesInput,
    storage: CedarEvaluatorStorage,
  ) => TE.TaskEither<CedarEvaluatorError, CedarEvaluatorLoadPoliciesOutput>;
  readonly authorize: (
    input: CedarEvaluatorAuthorizeInput,
    storage: CedarEvaluatorStorage,
  ) => TE.TaskEither<CedarEvaluatorError, CedarEvaluatorAuthorizeOutput>;
  readonly verify: (
    input: CedarEvaluatorVerifyInput,
    storage: CedarEvaluatorStorage,
  ) => TE.TaskEither<CedarEvaluatorError, CedarEvaluatorVerifyOutput>;
}

// --- Implementation ---

export const cedarEvaluatorHandler: CedarEvaluatorHandler = {
  loadPolicies: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('cedarevaluator', input.policies);
          return loadPoliciesOk();
        },
        (error): CedarEvaluatorError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  authorize: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('cedarevaluator', input.store, { store: input.store, principal: input.principal, action: input.action, resource: input.resource, context: input.context });
          return authorizeOk();
        },
        (error): CedarEvaluatorError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  verify: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('cedarevaluator', input.store);
          return verifyOk();
        },
        (error): CedarEvaluatorError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
