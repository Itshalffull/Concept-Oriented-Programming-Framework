// Comment — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { commentHandler } from './handler.js';
import type { CommentStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): CommentStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Comment conformance', () => {
  it('invariant 1: after addComment, reply behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = commentHandler;

    const c = 'u-test-invariant-001';
    const e = 'u-test-invariant-002';
    const r = 'u-test-invariant-003';

    // setup: addComment -> ok
    const addCommentResultSetup = await pipe(
      handler.addComment({
      comment: c,
      entity: e,
      content: 'Hello',
      author: 'alice',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).comment).toBe(c);
        return output;
      }),
    )();
    expect(E.isRight(addCommentResultSetup)).toBe(true);

    // assert: reply -> ok
    const replyResultAssert = await pipe(
      handler.reply({
      comment: r,
      parent: c,
      content: 'Reply',
      author: 'bob',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).comment).toBe(r);
        return output;
      }),
    )();
    expect(E.isRight(replyResultAssert)).toBe(true);

  });

});
