// CliTarget — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { cliTargetHandler } from './handler.js';
import type { CliTargetStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): CliTargetStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('CliTarget conformance', () => {
  it('invariant 1: after generate, listCommands behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = cliTargetHandler;

    let c: any = 'u-test-invariant-001';
    let f: any = 'u-test-invariant-002';
    let cmds: any = 'u-test-invariant-003';
    let subs: any = 'u-test-invariant-004';

    // setup: generate -> ok
    const generateResultSetup = await pipe(
      handler.generate({
      projection: 'task-projection',
      config: '{}',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        c = (output as any).commands;
        f = (output as any).files;
        return output;
      }),
    )();
    expect(E.isRight(generateResultSetup)).toBe(true);

    // assert: listCommands -> ok
    const listCommandsResultAssert = await pipe(
      handler.listCommands({
      concept: 'Task',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        cmds = (output as any).commands;
        subs = (output as any).subcommands;
        return output;
      }),
    )();
    expect(E.isRight(listCommandsResultAssert)).toBe(true);

  });

});
