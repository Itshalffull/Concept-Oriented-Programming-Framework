// DeploymentValidator — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { deploymentValidatorHandler } from './handler.js';
import type { DeploymentValidatorStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): DeploymentValidatorStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('DeploymentValidator conformance', () => {
  it('invariant 1: after parse, validate behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = deploymentValidatorHandler;

    const m = 'u-test-invariant-001';
    const i = 'u-test-invariant-002';

    // setup: parse -> ok
    const parseResultSetup = await pipe(
      handler.parse({
      raw: '{"app":{"name":"myapp","version":"1.0","uri":"urn:app/myapp"},"runtimes":{},"concepts":{},"syncs":[]}',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).manifest).toBe(m);
        return output;
      }),
    )();
    expect(E.isRight(parseResultSetup)).toBe(true);

    // assert: validate -> error
    const validateResultAssert = await pipe(
      handler.validate({
      manifest: m,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('error');
        expect((output as any).issues).toBe(i);
        return output;
      }),
    )();
    expect(E.isRight(validateResultAssert)).toBe(true);

  });

  it('invariant 2: after parse, parse behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = deploymentValidatorHandler;

    const m = 'u-test-invariant-001';
    const e = 'u-test-invariant-002';

    // setup: parse -> ok
    const parseResultSetup = await pipe(
      handler.parse({
      raw: '{"app":{"name":"t","version":"1","uri":"u"},"runtimes":{},"concepts":{},"syncs":[]}',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).manifest).toBe(m);
        return output;
      }),
    )();
    expect(E.isRight(parseResultSetup)).toBe(true);

    // assert: parse -> error
    const parseResultAssert = await pipe(
      handler.parse({
      raw: 'not json',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('error');
        expect((output as any).message).toBe(e);
        return output;
      }),
    )();
    expect(E.isRight(parseResultAssert)).toBe(true);

  });

});
