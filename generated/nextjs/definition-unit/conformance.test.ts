// DefinitionUnit — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { definitionUnitHandler } from './handler.js';
import type { DefinitionUnitStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): DefinitionUnitStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('DefinitionUnit conformance', () => {
  it('invariant 1: after extract, findBySymbol behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = definitionUnitHandler;

    const u = 'u-test-invariant-001';

    // setup: extract -> ok
    const extractResultSetup = await pipe(
      handler.extract({
      tree: 't1',
      startByte: 0,
      endByte: 100,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).unit).toBe(u);
        return output;
      }),
    )();
    expect(E.isRight(extractResultSetup)).toBe(true);

    // assert: findBySymbol -> notfound
    const findBySymbolResultAssert = await pipe(
      handler.findBySymbol({
      symbol: 'sym-u',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('notfound');
        return output;
      }),
    )();
    expect(E.isRight(findBySymbolResultAssert)).toBe(true);

  });

});
