// Notification — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { notificationHandler } from './handler.js';
import type { NotificationStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): NotificationStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Notification conformance', () => {
  it('invariant 1: after registerChannel, defineTemplate, subscribe, notify, getUnread behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = notificationHandler;

    const c = 'u-test-invariant-001';
    const cfg = 'u-test-invariant-002';
    const n = 'u-test-invariant-003';
    const t = 'u-test-invariant-004';
    const u = 'u-test-invariant-005';
    const e = 'u-test-invariant-006';
    const d = 'u-test-invariant-007';

    // setup: registerChannel -> ok
    const registerChannelResultSetup = await pipe(
      handler.registerChannel({
      name: c,
      config: cfg,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(registerChannelResultSetup)).toBe(true);

    // assert: defineTemplate -> ok
    const defineTemplateResultAssert = await pipe(
      handler.defineTemplate({
      notification: n,
      template: t,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(defineTemplateResultAssert)).toBe(true);

    // assert: subscribe -> ok
    const subscribeResultAssert = await pipe(
      handler.subscribe({
      user: u,
      eventType: e,
      channel: c,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(subscribeResultAssert)).toBe(true);

    // assert: notify -> ok
    const notifyResultAssert = await pipe(
      handler.notify({
      notification: n,
      user: u,
      template: t,
      data: d,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(notifyResultAssert)).toBe(true);

    // assert: getUnread -> ok
    const getUnreadResultAssert = await pipe(
      handler.getUnread({
      user: u,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).notifications).toBe(n);
        return output;
      }),
    )();
    expect(E.isRight(getUnreadResultAssert)).toBe(true);

  });

  it('invariant 2: after notify, markRead, getUnread behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = notificationHandler;

    const n = 'u-test-invariant-001';
    const u = 'u-test-invariant-002';
    const t = 'u-test-invariant-003';
    const d = 'u-test-invariant-004';

    // setup: notify -> ok
    const notifyResultSetup = await pipe(
      handler.notify({
      notification: n,
      user: u,
      template: t,
      data: d,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(notifyResultSetup)).toBe(true);

    // assert: markRead -> ok
    const markReadResultAssert = await pipe(
      handler.markRead({
      notification: n,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        return output;
      }),
    )();
    expect(E.isRight(markReadResultAssert)).toBe(true);

    // assert: getUnread -> ok
    const getUnreadResultAssert = await pipe(
      handler.getUnread({
      user: u,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).notifications).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(getUnreadResultAssert)).toBe(true);

  });

});
