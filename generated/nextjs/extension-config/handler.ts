// ExtensionConfig — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ExtensionConfigStorage,
  ExtensionConfigInitializeInput,
  ExtensionConfigInitializeOutput,
  ExtensionConfigGetInput,
  ExtensionConfigGetOutput,
  ExtensionConfigSetInput,
  ExtensionConfigSetOutput,
  ExtensionConfigResetInput,
  ExtensionConfigResetOutput,
  ExtensionConfigGetSchemaInput,
  ExtensionConfigGetSchemaOutput,
  ExtensionConfigOnChangeInput,
  ExtensionConfigOnChangeOutput,
  ExtensionConfigDestroyInput,
  ExtensionConfigDestroyOutput,
} from './types.js';

import {
  initializeOk,
  initializeExists,
  initializeInvalid,
  getOk,
  getNotfound,
  setOk,
  setNotfound,
  setInvalid,
  resetOk,
  resetNotfound,
  getSchemaOk,
  getSchemaNotfound,
  onChangeOk,
  onChangeNotfound,
  destroyOk,
  destroyNotfound,
} from './types.js';

export interface ExtensionConfigError {
  readonly code: string;
  readonly message: string;
}

export interface ExtensionConfigHandler {
  readonly initialize: (
    input: ExtensionConfigInitializeInput,
    storage: ExtensionConfigStorage,
  ) => TE.TaskEither<ExtensionConfigError, ExtensionConfigInitializeOutput>;
  readonly get: (
    input: ExtensionConfigGetInput,
    storage: ExtensionConfigStorage,
  ) => TE.TaskEither<ExtensionConfigError, ExtensionConfigGetOutput>;
  readonly set: (
    input: ExtensionConfigSetInput,
    storage: ExtensionConfigStorage,
  ) => TE.TaskEither<ExtensionConfigError, ExtensionConfigSetOutput>;
  readonly reset: (
    input: ExtensionConfigResetInput,
    storage: ExtensionConfigStorage,
  ) => TE.TaskEither<ExtensionConfigError, ExtensionConfigResetOutput>;
  readonly getSchema: (
    input: ExtensionConfigGetSchemaInput,
    storage: ExtensionConfigStorage,
  ) => TE.TaskEither<ExtensionConfigError, ExtensionConfigGetSchemaOutput>;
  readonly onChange: (
    input: ExtensionConfigOnChangeInput,
    storage: ExtensionConfigStorage,
  ) => TE.TaskEither<ExtensionConfigError, ExtensionConfigOnChangeOutput>;
  readonly destroy: (
    input: ExtensionConfigDestroyInput,
    storage: ExtensionConfigStorage,
  ) => TE.TaskEither<ExtensionConfigError, ExtensionConfigDestroyOutput>;
}

// --- Implementation ---

export const extensionConfigHandler: ExtensionConfigHandler = {
  initialize: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionconfig', input.extensionId, { extensionId: input.extensionId, schema: input.schema, defaults: input.defaults });
          return initializeOk(input.extensionId);
        },
        (error): ExtensionConfigError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  get: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionconfig', input.config),
        (error): ExtensionConfigError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getNotfound(`${input.config} not found`)),
            (found) => TE.right(getOk(input.config)),
          ),
        ),
      ),
    ),

  set: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionconfig', input.config, { config: input.config, key: input.key, value: input.value });
          return setOk(input.config);
        },
        (error): ExtensionConfigError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  reset: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionconfig', input.config),
        (error): ExtensionConfigError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(resetNotfound(`${input.config} not found`)),
            (existing) =>
              TE.tryCatch(
                async () => {
                  const updated = { ...existing, config: input.config, key: input.key };
                  await storage.put('extensionconfig', input.config, updated);
                  return resetOk(input.config);
                },
                (error): ExtensionConfigError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  getSchema: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionconfig', input.config),
        (error): ExtensionConfigError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getSchemaNotfound(`${input.config} not found`)),
            (found) => TE.right(getSchemaOk(input.config)),
          ),
        ),
      ),
    ),

  onChange: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionconfig', input.config, { config: input.config, key: input.key, newValue: input.newValue, oldValue: input.oldValue });
          return onChangeOk(input.config);
        },
        (error): ExtensionConfigError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  destroy: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionconfig', input.config),
        (error): ExtensionConfigError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(destroyNotfound(`${input.config} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('extensionconfig', input.config);
                  return destroyOk();
                },
                (error): ExtensionConfigError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),
};
