// FluxProvider — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { fluxProviderHandler } from './handler.js';
import type { FluxProviderStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): FluxProviderStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('FluxProvider conformance', () => {
  it('invariant 1: after emit, reconciliationStatus behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = fluxProviderHandler;

    const k = 'u-test-invariant-001';
    const f = 'u-test-invariant-002';
    const rev = 'u-test-invariant-003';
    const t = 'u-test-invariant-004';

    // setup: emit -> ok
    const emitResultSetup = await pipe(
      handler.emit({
      plan: 'dp-001',
      repo: 'git@github.com:org/deploy.git',
      path: 'envs/prod',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).kustomization).toBe(k);
        expect((output as any).files).toBe(f);
        return output;
      }),
    )();
    expect(E.isRight(emitResultSetup)).toBe(true);

    // assert: reconciliationStatus -> ok
    const reconciliationStatusResultAssert = await pipe(
      handler.reconciliationStatus({
      kustomization: k,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).kustomization).toBe(k);
        expect((output as any).readyStatus).toBe('True');
        expect((output as any).appliedRevision).toBe(rev);
        expect((output as any).reconciledAt).toBe(t);
        return output;
      }),
    )();
    expect(E.isRight(reconciliationStatusResultAssert)).toBe(true);

  });

});
