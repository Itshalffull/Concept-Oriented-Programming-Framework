// SpecParser — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { specParserHandler } from './handler.js';
import type { SpecParserStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): SpecParserStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('SpecParser conformance', () => {
  it('invariant 1: after parse, parse behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = specParserHandler;

    const s = 'u-test-invariant-001';
    const a = 'u-test-invariant-002';
    const m = 'u-test-invariant-003';

    // setup: parse -> ok
    const parseResultSetup = await pipe(
      handler.parse({
      source: 'concept Tiny [X] { purpose { A test. } state { items: set X } actions { action get(x: X) { -> ok(item: X) { Return. } } } }',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).spec).toBe(s);
        expect((output as any).ast).toBe(a);
        return output;
      }),
    )();
    expect(E.isRight(parseResultSetup)).toBe(true);

    // assert: parse -> error
    const parseResultAssert = await pipe(
      handler.parse({
      source: '',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('error');
        expect((output as any).message).toBe(m);
        expect((output as any).line).toBe(0);
        return output;
      }),
    )();
    expect(E.isRight(parseResultAssert)).toBe(true);

  });

});
