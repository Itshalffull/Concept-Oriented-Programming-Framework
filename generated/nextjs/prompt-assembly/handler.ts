// PromptAssembly — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  PromptAssemblyStorage,
  PromptAssemblyCreateInput,
  PromptAssemblyCreateOutput,
  PromptAssemblyAddSectionInput,
  PromptAssemblyAddSectionOutput,
  PromptAssemblySetVariableInput,
  PromptAssemblySetVariableOutput,
  PromptAssemblyAssembleInput,
  PromptAssemblyAssembleOutput,
  PromptAssemblyToMessagesInput,
  PromptAssemblyToMessagesOutput,
  PromptAssemblyEstimateTokensInput,
  PromptAssemblyEstimateTokensOutput,
  PromptAssemblyRemoveSectionInput,
  PromptAssemblyRemoveSectionOutput,
} from './types.js';

import {
  createOk,
  createInvalid,
  addSectionOk,
  addSectionNotfound,
  setVariableOk,
  setVariableNotfound,
  assembleOk,
  assembleOver_budget,
  toMessagesOk,
  toMessagesOver_budget,
  estimateTokensOk,
  estimateTokensNotfound,
  removeSectionOk,
  removeSectionNotfound,
} from './types.js';

export interface PromptAssemblyError {
  readonly code: string;
  readonly message: string;
}

export interface PromptAssemblyHandler {
  readonly create: (
    input: PromptAssemblyCreateInput,
    storage: PromptAssemblyStorage,
  ) => TE.TaskEither<PromptAssemblyError, PromptAssemblyCreateOutput>;
  readonly addSection: (
    input: PromptAssemblyAddSectionInput,
    storage: PromptAssemblyStorage,
  ) => TE.TaskEither<PromptAssemblyError, PromptAssemblyAddSectionOutput>;
  readonly setVariable: (
    input: PromptAssemblySetVariableInput,
    storage: PromptAssemblyStorage,
  ) => TE.TaskEither<PromptAssemblyError, PromptAssemblySetVariableOutput>;
  readonly assemble: (
    input: PromptAssemblyAssembleInput,
    storage: PromptAssemblyStorage,
  ) => TE.TaskEither<PromptAssemblyError, PromptAssemblyAssembleOutput>;
  readonly toMessages: (
    input: PromptAssemblyToMessagesInput,
    storage: PromptAssemblyStorage,
  ) => TE.TaskEither<PromptAssemblyError, PromptAssemblyToMessagesOutput>;
  readonly estimateTokens: (
    input: PromptAssemblyEstimateTokensInput,
    storage: PromptAssemblyStorage,
  ) => TE.TaskEither<PromptAssemblyError, PromptAssemblyEstimateTokensOutput>;
  readonly removeSection: (
    input: PromptAssemblyRemoveSectionInput,
    storage: PromptAssemblyStorage,
  ) => TE.TaskEither<PromptAssemblyError, PromptAssemblyRemoveSectionOutput>;
}

// --- Implementation ---

export const promptAssemblyHandler: PromptAssemblyHandler = {
  create: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('promptassembly', input.strategy, { strategy: input.strategy, format: input.format, tokenizer_id: input.tokenizer_id, context_window: input.context_window });
          return createOk(input.strategy);
        },
        (error): PromptAssemblyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  addSection: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('promptassembly', input.assembly, { assembly: input.assembly, name: input.name, role: input.role, priority: input.priority, max_tokens: input.max_tokens, required: input.required, content: input.content, template_ref: input.template_ref });
          return addSectionOk(input.assembly);
        },
        (error): PromptAssemblyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  setVariable: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('promptassembly', input.assembly, { assembly: input.assembly, name: input.name, value: input.value });
          return setVariableOk(input.assembly);
        },
        (error): PromptAssemblyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  assemble: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('promptassembly', input.assembly, { assembly: input.assembly });
          return assembleOk(input.assembly, [], [], 0, 0);
        },
        (error): PromptAssemblyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  toMessages: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('promptassembly', input.assembly, { assembly: input.assembly });
          return toMessagesOk([]);
        },
        (error): PromptAssemblyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  estimateTokens: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('promptassembly', input.assembly, { assembly: input.assembly });
          return estimateTokensOk(0, []);
        },
        (error): PromptAssemblyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  removeSection: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('promptassembly', input.assembly),
        (error): PromptAssemblyError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(removeSectionNotfound(`${input.assembly} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('promptassembly', input.assembly);
                  return removeSectionOk();
                },
                (error): PromptAssemblyError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),
};
