// ExtensionManifest — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ExtensionManifestStorage,
  ExtensionManifestRegisterInput,
  ExtensionManifestRegisterOutput,
  ExtensionManifestGetInput,
  ExtensionManifestGetOutput,
  ExtensionManifestUpdateInput,
  ExtensionManifestUpdateOutput,
  ExtensionManifestExportInput,
  ExtensionManifestExportOutput,
  ExtensionManifestListDependenciesInput,
  ExtensionManifestListDependenciesOutput,
  ExtensionManifestCheckCompatibilityInput,
  ExtensionManifestCheckCompatibilityOutput,
} from './types.js';

import {
  registerOk,
  registerExists,
  getOk,
  getNotfound,
  updateOk,
  updateNotfound,
  updateInvalid,
  exportOk,
  exportNotfound,
  exportUnsupported,
  listDependenciesOk,
  listDependenciesNotfound,
  checkCompatibilityOk,
  checkCompatibilityNotfound,
} from './types.js';

export interface ExtensionManifestError {
  readonly code: string;
  readonly message: string;
}

export interface ExtensionManifestHandler {
  readonly register: (
    input: ExtensionManifestRegisterInput,
    storage: ExtensionManifestStorage,
  ) => TE.TaskEither<ExtensionManifestError, ExtensionManifestRegisterOutput>;
  readonly get: (
    input: ExtensionManifestGetInput,
    storage: ExtensionManifestStorage,
  ) => TE.TaskEither<ExtensionManifestError, ExtensionManifestGetOutput>;
  readonly update: (
    input: ExtensionManifestUpdateInput,
    storage: ExtensionManifestStorage,
  ) => TE.TaskEither<ExtensionManifestError, ExtensionManifestUpdateOutput>;
  readonly export: (
    input: ExtensionManifestExportInput,
    storage: ExtensionManifestStorage,
  ) => TE.TaskEither<ExtensionManifestError, ExtensionManifestExportOutput>;
  readonly listDependencies: (
    input: ExtensionManifestListDependenciesInput,
    storage: ExtensionManifestStorage,
  ) => TE.TaskEither<ExtensionManifestError, ExtensionManifestListDependenciesOutput>;
  readonly checkCompatibility: (
    input: ExtensionManifestCheckCompatibilityInput,
    storage: ExtensionManifestStorage,
  ) => TE.TaskEither<ExtensionManifestError, ExtensionManifestCheckCompatibilityOutput>;
}

// --- Implementation ---

export const extensionManifestHandler: ExtensionManifestHandler = {
  register: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionmanifest', input.name, { name: input.name, version: input.version, author: input.author, entryPoints: input.entryPoints, permissions: input.permissions, capabilities: input.capabilities });
          return registerOk(input.name);
        },
        (error): ExtensionManifestError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  get: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionmanifest', input.manifest),
        (error): ExtensionManifestError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getNotfound(`${input.manifest} not found`)),
            (found) => TE.right(getOk(input.manifest, input.manifest, input.manifest, input.manifest, input.manifest, input.manifest)),
          ),
        ),
      ),
    ),

  update: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionmanifest', input.manifest),
        (error): ExtensionManifestError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(updateNotfound(`${input.manifest} not found`)),
            (existing) =>
              TE.tryCatch(
                async () => {
                  const updated = { ...existing, manifest: input.manifest, fields: input.fields };
                  await storage.put('extensionmanifest', input.manifest, updated);
                  return updateOk(input.manifest);
                },
                (error): ExtensionManifestError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  export: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionmanifest', input.manifest, { manifest: input.manifest, hostType: input.hostType });
          return exportOk(input.manifest, input.manifest);
        },
        (error): ExtensionManifestError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  listDependencies: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const records = await storage.find('extensionmanifest');
          return listDependenciesOk(input.manifest);
        },
        (error): ExtensionManifestError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  checkCompatibility: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionmanifest', input.manifest),
        (error): ExtensionManifestError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(checkCompatibilityNotfound(`${input.manifest} not found`)),
            (found) => TE.right(checkCompatibilityOk(true, input.manifest)),
          ),
        ),
      ),
    ),
};
