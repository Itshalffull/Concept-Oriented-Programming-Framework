// LLMCall — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for LLMCall
export interface LLMCallStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

// Status: pending -> requesting -> validating -> accepted | rejected | repairing

export interface LLMCallRequestInput {
  readonly call_id: string;
  readonly step_ref: string;
  readonly model: string;
  readonly prompt: string;
  readonly output_schema?: string;
  readonly max_attempts: number;
}

export interface LLMCallRequestOutput {
  readonly variant: 'ok';
  readonly call_id: string;
}

export interface LLMCallRecordResponseInput {
  readonly call_id: string;
  readonly raw_output: string;
  readonly prompt_tokens: number;
  readonly completion_tokens: number;
}

export interface LLMCallRecordResponseOutputOk {
  readonly variant: 'ok';
  readonly call_id: string;
  readonly status: string;
}

export interface LLMCallRecordResponseOutputNotfound {
  readonly variant: 'notfound';
  readonly message: string;
}

export interface LLMCallRecordResponseOutputInvalidStatus {
  readonly variant: 'invalid_status';
  readonly message: string;
}

export type LLMCallRecordResponseOutput =
  | LLMCallRecordResponseOutputOk
  | LLMCallRecordResponseOutputNotfound
  | LLMCallRecordResponseOutputInvalidStatus;

export interface LLMCallValidateInput {
  readonly call_id: string;
}

export interface LLMCallValidateOutputValid {
  readonly variant: 'valid';
  readonly call_id: string;
  readonly parsed_output: string;
}

export interface LLMCallValidateOutputInvalid {
  readonly variant: 'invalid';
  readonly call_id: string;
  readonly errors: string;
}

export interface LLMCallValidateOutputNotfound {
  readonly variant: 'notfound';
  readonly message: string;
}

export interface LLMCallValidateOutputInvalidStatus {
  readonly variant: 'invalid_status';
  readonly message: string;
}

export type LLMCallValidateOutput =
  | LLMCallValidateOutputValid
  | LLMCallValidateOutputInvalid
  | LLMCallValidateOutputNotfound
  | LLMCallValidateOutputInvalidStatus;

export interface LLMCallRepairInput {
  readonly call_id: string;
}

export interface LLMCallRepairOutputOk {
  readonly variant: 'ok';
  readonly call_id: string;
  readonly attempt_count: number;
}

export interface LLMCallRepairOutputMaxAttemptsReached {
  readonly variant: 'max_attempts_reached';
  readonly call_id: string;
  readonly attempt_count: number;
}

export interface LLMCallRepairOutputNotfound {
  readonly variant: 'notfound';
  readonly message: string;
}

export interface LLMCallRepairOutputInvalidStatus {
  readonly variant: 'invalid_status';
  readonly message: string;
}

export type LLMCallRepairOutput =
  | LLMCallRepairOutputOk
  | LLMCallRepairOutputMaxAttemptsReached
  | LLMCallRepairOutputNotfound
  | LLMCallRepairOutputInvalidStatus;

export interface LLMCallAcceptInput {
  readonly call_id: string;
}

export interface LLMCallAcceptOutputOk {
  readonly variant: 'ok';
  readonly call_id: string;
}

export interface LLMCallAcceptOutputNotfound {
  readonly variant: 'notfound';
  readonly message: string;
}

export interface LLMCallAcceptOutputInvalidStatus {
  readonly variant: 'invalid_status';
  readonly message: string;
}

export type LLMCallAcceptOutput =
  | LLMCallAcceptOutputOk
  | LLMCallAcceptOutputNotfound
  | LLMCallAcceptOutputInvalidStatus;

export interface LLMCallRejectInput {
  readonly call_id: string;
}

export interface LLMCallRejectOutputOk {
  readonly variant: 'ok';
  readonly call_id: string;
}

export interface LLMCallRejectOutputNotfound {
  readonly variant: 'notfound';
  readonly message: string;
}

export interface LLMCallRejectOutputInvalidStatus {
  readonly variant: 'invalid_status';
  readonly message: string;
}

export type LLMCallRejectOutput =
  | LLMCallRejectOutputOk
  | LLMCallRejectOutputNotfound
  | LLMCallRejectOutputInvalidStatus;

// --- Variant constructors ---

export const requestOk = (call_id: string): LLMCallRequestOutput =>
  ({ variant: 'ok', call_id } as LLMCallRequestOutput);

export const recordResponseOk = (call_id: string, status: string): LLMCallRecordResponseOutput =>
  ({ variant: 'ok', call_id, status } as LLMCallRecordResponseOutput);
export const recordResponseNotfound = (message: string): LLMCallRecordResponseOutput =>
  ({ variant: 'notfound', message } as LLMCallRecordResponseOutput);
export const recordResponseInvalidStatus = (message: string): LLMCallRecordResponseOutput =>
  ({ variant: 'invalid_status', message } as LLMCallRecordResponseOutput);

export const validateValid = (call_id: string, parsed_output: string): LLMCallValidateOutput =>
  ({ variant: 'valid', call_id, parsed_output } as LLMCallValidateOutput);
export const validateInvalid = (call_id: string, errors: string): LLMCallValidateOutput =>
  ({ variant: 'invalid', call_id, errors } as LLMCallValidateOutput);
export const validateNotfound = (message: string): LLMCallValidateOutput =>
  ({ variant: 'notfound', message } as LLMCallValidateOutput);
export const validateInvalidStatus = (message: string): LLMCallValidateOutput =>
  ({ variant: 'invalid_status', message } as LLMCallValidateOutput);

export const repairOk = (call_id: string, attempt_count: number): LLMCallRepairOutput =>
  ({ variant: 'ok', call_id, attempt_count } as LLMCallRepairOutput);
export const repairMaxAttemptsReached = (call_id: string, attempt_count: number): LLMCallRepairOutput =>
  ({ variant: 'max_attempts_reached', call_id, attempt_count } as LLMCallRepairOutput);
export const repairNotfound = (message: string): LLMCallRepairOutput =>
  ({ variant: 'notfound', message } as LLMCallRepairOutput);
export const repairInvalidStatus = (message: string): LLMCallRepairOutput =>
  ({ variant: 'invalid_status', message } as LLMCallRepairOutput);

export const acceptOk = (call_id: string): LLMCallAcceptOutput =>
  ({ variant: 'ok', call_id } as LLMCallAcceptOutput);
export const acceptNotfound = (message: string): LLMCallAcceptOutput =>
  ({ variant: 'notfound', message } as LLMCallAcceptOutput);
export const acceptInvalidStatus = (message: string): LLMCallAcceptOutput =>
  ({ variant: 'invalid_status', message } as LLMCallAcceptOutput);

export const rejectOk = (call_id: string): LLMCallRejectOutput =>
  ({ variant: 'ok', call_id } as LLMCallRejectOutput);
export const rejectNotfound = (message: string): LLMCallRejectOutput =>
  ({ variant: 'notfound', message } as LLMCallRejectOutput);
export const rejectInvalidStatus = (message: string): LLMCallRejectOutput =>
  ({ variant: 'invalid_status', message } as LLMCallRejectOutput);
