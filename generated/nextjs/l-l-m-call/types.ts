// LLMCall — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for LLMCall
export interface LLMCallStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface LLMCallRequestInput {
  readonly step_ref: string;
  readonly model: string;
  readonly prompt: Buffer;
  readonly output_schema: string;
  readonly max_attempts: number;
}

export interface LLMCallRequestOutput {
  readonly variant: 'ok';
  readonly call: string;
  readonly step_ref: string;
  readonly model: string;
}

export interface LLMCallRecord_responseInput {
  readonly call: string;
  readonly raw_output: Buffer;
  readonly input_tokens: number;
  readonly output_tokens: number;
}

export interface LLMCallRecord_responseOutputOk {
  readonly variant: 'ok';
  readonly call: string;
}

export interface LLMCallRecord_responseOutputProvider_error {
  readonly variant: 'provider_error';
  readonly call: string;
  readonly message: string;
}

export type LLMCallRecord_responseOutput = LLMCallRecord_responseOutputOk | LLMCallRecord_responseOutputProvider_error;

export interface LLMCallValidateInput {
  readonly call: string;
}

export interface LLMCallValidateOutputValid {
  readonly variant: 'valid';
  readonly call: string;
  readonly step_ref: string;
  readonly validated_output: Buffer;
}

export interface LLMCallValidateOutputInvalid {
  readonly variant: 'invalid';
  readonly call: string;
  readonly errors: string;
  readonly attempt_count: number;
  readonly max_attempts: number;
}

export type LLMCallValidateOutput = LLMCallValidateOutputValid | LLMCallValidateOutputInvalid;

export interface LLMCallRepairInput {
  readonly call: string;
  readonly errors: string;
}

export interface LLMCallRepairOutputOk {
  readonly variant: 'ok';
  readonly call: string;
}

export interface LLMCallRepairOutputMax_attempts_reached {
  readonly variant: 'max_attempts_reached';
  readonly call: string;
  readonly step_ref: string;
}

export type LLMCallRepairOutput = LLMCallRepairOutputOk | LLMCallRepairOutputMax_attempts_reached;

export interface LLMCallAcceptInput {
  readonly call: string;
}

export interface LLMCallAcceptOutput {
  readonly variant: 'ok';
  readonly call: string;
  readonly step_ref: string;
  readonly output: Buffer;
}

export interface LLMCallRejectInput {
  readonly call: string;
  readonly reason: string;
}

export interface LLMCallRejectOutput {
  readonly variant: 'ok';
  readonly call: string;
  readonly step_ref: string;
  readonly reason: string;
}

// --- Variant constructors ---

export const requestOk = (call: string, step_ref: string, model: string): LLMCallRequestOutput => ({ variant: 'ok', call, step_ref, model } as LLMCallRequestOutput);

export const record_responseOk = (call: string): LLMCallRecord_responseOutput => ({ variant: 'ok', call } as LLMCallRecord_responseOutput);
export const record_responseProvider_error = (call: string, message: string): LLMCallRecord_responseOutput => ({ variant: 'provider_error', call, message } as LLMCallRecord_responseOutput);

export const validateValid = (call: string, step_ref: string, validated_output: Buffer): LLMCallValidateOutput => ({ variant: 'valid', call, step_ref, validated_output } as LLMCallValidateOutput);
export const validateInvalid = (call: string, errors: string, attempt_count: number, max_attempts: number): LLMCallValidateOutput => ({ variant: 'invalid', call, errors, attempt_count, max_attempts } as LLMCallValidateOutput);

export const repairOk = (call: string): LLMCallRepairOutput => ({ variant: 'ok', call } as LLMCallRepairOutput);
export const repairMax_attempts_reached = (call: string, step_ref: string): LLMCallRepairOutput => ({ variant: 'max_attempts_reached', call, step_ref } as LLMCallRepairOutput);

export const acceptOk = (call: string, step_ref: string, output: Buffer): LLMCallAcceptOutput => ({ variant: 'ok', call, step_ref, output } as LLMCallAcceptOutput);

export const rejectOk = (call: string, step_ref: string, reason: string): LLMCallRejectOutput => ({ variant: 'ok', call, step_ref, reason } as LLMCallRejectOutput);

