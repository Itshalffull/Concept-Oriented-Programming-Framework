// ModelRouter — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ModelRouterStorage,
  ModelRouterAddRouteInput,
  ModelRouterAddRouteOutput,
  ModelRouterRouteInput,
  ModelRouterRouteOutput,
  ModelRouterFallbackInput,
  ModelRouterFallbackOutput,
  ModelRouterRecordOutcomeInput,
  ModelRouterRecordOutcomeOutput,
  ModelRouterGetHealthInput,
  ModelRouterGetHealthOutput,
} from './types.js';

import {
  addRouteOk,
  addRouteDuplicate,
  routeOk,
  routeNo_route,
  fallbackOk,
  fallbackExhausted,
  recordOutcomeOk,
  recordOutcomeNotfound,
  getHealthOk,
} from './types.js';

export interface ModelRouterError {
  readonly code: string;
  readonly message: string;
}

export interface ModelRouterHandler {
  readonly addRoute: (
    input: ModelRouterAddRouteInput,
    storage: ModelRouterStorage,
  ) => TE.TaskEither<ModelRouterError, ModelRouterAddRouteOutput>;
  readonly route: (
    input: ModelRouterRouteInput,
    storage: ModelRouterStorage,
  ) => TE.TaskEither<ModelRouterError, ModelRouterRouteOutput>;
  readonly fallback: (
    input: ModelRouterFallbackInput,
    storage: ModelRouterStorage,
  ) => TE.TaskEither<ModelRouterError, ModelRouterFallbackOutput>;
  readonly recordOutcome: (
    input: ModelRouterRecordOutcomeInput,
    storage: ModelRouterStorage,
  ) => TE.TaskEither<ModelRouterError, ModelRouterRecordOutcomeOutput>;
  readonly getHealth: (
    input: ModelRouterGetHealthInput,
    storage: ModelRouterStorage,
  ) => TE.TaskEither<ModelRouterError, ModelRouterGetHealthOutput>;
}

// --- Implementation ---

export const modelRouterHandler: ModelRouterHandler = {
  addRoute: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('modelrouter', input.name, { name: input.name, model_id: input.model_id, conditions: input.conditions, priority: input.priority, weight: input.weight });
          return addRouteOk(input.name);
        },
        (error): ModelRouterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  route: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('modelrouter', input.task_type, { task_type: input.task_type, complexity: input.complexity, cost_limit: input.cost_limit, latency_limit: input.latency_limit });
          return routeOk(input.task_type, input.task_type);
        },
        (error): ModelRouterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  fallback: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('modelrouter', input.failed_model_id, { failed_model_id: input.failed_model_id, error_type: input.error_type });
          return fallbackOk(input.failed_model_id);
        },
        (error): ModelRouterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  recordOutcome: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('modelrouter', input.route, { route: input.route, success: input.success, latency_ms: input.latency_ms, tokens: input.tokens, cost: input.cost });
          return recordOutcomeOk(input.route);
        },
        (error): ModelRouterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getHealth: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          const record = await storage.get('modelrouter', input.id);
          return getHealthOk((record as any).statuses);
        },
        (error): ModelRouterError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
