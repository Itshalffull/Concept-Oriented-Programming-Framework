// SearchSpace — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for SearchSpace
export interface SearchSpaceStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

// --- index ---

export interface SearchSpaceIndexInput {
  readonly scope_id: string;
  readonly provider: string;
  readonly entity_id: string;
  readonly data: string;
}

export interface SearchSpaceIndexOutputOk {
  readonly variant: 'ok';
  readonly entry: string;
}

export type SearchSpaceIndexOutput = SearchSpaceIndexOutputOk;

// --- tombstone ---

export interface SearchSpaceTombstoneInput {
  readonly scope_id: string;
  readonly provider: string;
  readonly entity_id: string;
}

export interface SearchSpaceTombstoneOutputOk {
  readonly variant: 'ok';
  readonly entry: string;
}

export type SearchSpaceTombstoneOutput = SearchSpaceTombstoneOutputOk;

// --- query ---

export interface SearchSpaceQueryInput {
  readonly scope_id: string;
  readonly provider: string;
  readonly query_expr: string;
}

export interface SearchSpaceQueryOutputOk {
  readonly variant: 'ok';
  readonly results: readonly string[];
}

export interface SearchSpaceQueryOutputNoScope {
  readonly variant: 'no_scope';
  readonly scope_id: string;
}

export type SearchSpaceQueryOutput =
  | SearchSpaceQueryOutputOk
  | SearchSpaceQueryOutputNoScope;

// --- clear ---

export interface SearchSpaceClearInput {
  readonly scope_id: string;
}

export interface SearchSpaceClearOutputOk {
  readonly variant: 'ok';
}

export type SearchSpaceClearOutput = SearchSpaceClearOutputOk;

// --- materialize ---

export interface SearchSpaceMaterializeInput {
  readonly scope_id: string;
}

export interface SearchSpaceMaterializeOutputOk {
  readonly variant: 'ok';
  readonly count: number;
}

export type SearchSpaceMaterializeOutput = SearchSpaceMaterializeOutputOk;

// --- Variant constructors ---

export const indexOk = (entry: string): SearchSpaceIndexOutput =>
  ({ variant: 'ok', entry } as SearchSpaceIndexOutput);

export const tombstoneOk = (entry: string): SearchSpaceTombstoneOutput =>
  ({ variant: 'ok', entry } as SearchSpaceTombstoneOutput);

export const queryOk = (results: readonly string[]): SearchSpaceQueryOutput =>
  ({ variant: 'ok', results } as SearchSpaceQueryOutput);
export const queryNoScope = (scope_id: string): SearchSpaceQueryOutput =>
  ({ variant: 'no_scope', scope_id } as SearchSpaceQueryOutput);

export const clearOk = (): SearchSpaceClearOutput =>
  ({ variant: 'ok' } as SearchSpaceClearOutput);

export const materializeOk = (count: number): SearchSpaceMaterializeOutput =>
  ({ variant: 'ok', count } as SearchSpaceMaterializeOutput);
