// AgentLoop — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import type {
  AgentLoopStorage,
  AgentLoopCreateInput,
  AgentLoopCreateOutput,
  AgentLoopRunInput,
  AgentLoopRunOutput,
  AgentLoopStepInput,
  AgentLoopStepOutput,
  AgentLoopObserveInput,
  AgentLoopObserveOutput,
  AgentLoopInterruptInput,
  AgentLoopInterruptOutput,
  AgentLoopResumeInput,
  AgentLoopResumeOutput,
} from './types.js';

import {
  createOk,
  createInvalid,
  runOk,
  runMax_iterations,
  runError,
  runWaiting_for_human,
  stepThought,
  stepAction_request,
  stepFinal_answer,
  stepError,
  observeOk,
  observeNotfound,
  interruptOk,
  interruptNotfound,
  resumeOk,
  resumeNotfound,
} from './types.js';

export interface AgentLoopError {
  readonly code: string;
  readonly message: string;
}

export interface AgentLoopHandler {
  readonly create: (
    input: AgentLoopCreateInput,
    storage: AgentLoopStorage,
  ) => TE.TaskEither<AgentLoopError, AgentLoopCreateOutput>;
  readonly run: (
    input: AgentLoopRunInput,
    storage: AgentLoopStorage,
  ) => TE.TaskEither<AgentLoopError, AgentLoopRunOutput>;
  readonly step: (
    input: AgentLoopStepInput,
    storage: AgentLoopStorage,
  ) => TE.TaskEither<AgentLoopError, AgentLoopStepOutput>;
  readonly observe: (
    input: AgentLoopObserveInput,
    storage: AgentLoopStorage,
  ) => TE.TaskEither<AgentLoopError, AgentLoopObserveOutput>;
  readonly interrupt: (
    input: AgentLoopInterruptInput,
    storage: AgentLoopStorage,
  ) => TE.TaskEither<AgentLoopError, AgentLoopInterruptOutput>;
  readonly resume: (
    input: AgentLoopResumeInput,
    storage: AgentLoopStorage,
  ) => TE.TaskEither<AgentLoopError, AgentLoopResumeOutput>;
}

// --- Implementation ---

export const agentLoopHandler: AgentLoopHandler = {
  create: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentloop', input.available_tools, { available_tools: input.available_tools, max_iterations: input.max_iterations });
          return createOk(input.available_tools);
        },
        (error): AgentLoopError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  run: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentloop', input.agent, { agent: input.agent, goal: input.goal, context: input.context, strategy: input.strategy });
          return runOk(input.agent, 0, 0);
        },
        (error): AgentLoopError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  step: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentloop', input.agent, { agent: input.agent });
          return stepOk();
        },
        (error): AgentLoopError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  observe: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentloop', input.agent, { agent: input.agent, observation: input.observation });
          return observeOk(input.agent);
        },
        (error): AgentLoopError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  interrupt: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentloop', input.agent, { agent: input.agent });
          return interruptOk(input.agent, 0);
        },
        (error): AgentLoopError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  resume: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('agentloop', input.agent, { agent: input.agent, human_input: input.human_input });
          return resumeOk(input.agent);
        },
        (error): AgentLoopError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
