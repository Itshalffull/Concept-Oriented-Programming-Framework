// FewShotExample — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  FewShotExampleStorage,
  FewShotExampleCreatePoolInput,
  FewShotExampleCreatePoolOutput,
  FewShotExampleAddInput,
  FewShotExampleAddOutput,
  FewShotExampleSelectInput,
  FewShotExampleSelectOutput,
  FewShotExampleOptimizeInput,
  FewShotExampleOptimizeOutput,
  FewShotExampleEmbedInput,
  FewShotExampleEmbedOutput,
  FewShotExampleRemoveInput,
  FewShotExampleRemoveOutput,
} from './types.js';

import {
  createPoolOk,
  createPoolInvalid,
  addOk,
  addNotfound,
  selectOk,
  selectEmpty,
  optimizeOk,
  optimizeError,
  embedOk,
  embedError,
  removeOk,
  removeNotfound,
} from './types.js';

export interface FewShotExampleError {
  readonly code: string;
  readonly message: string;
}

export interface FewShotExampleHandler {
  readonly createPool: (
    input: FewShotExampleCreatePoolInput,
    storage: FewShotExampleStorage,
  ) => TE.TaskEither<FewShotExampleError, FewShotExampleCreatePoolOutput>;
  readonly add: (
    input: FewShotExampleAddInput,
    storage: FewShotExampleStorage,
  ) => TE.TaskEither<FewShotExampleError, FewShotExampleAddOutput>;
  readonly select: (
    input: FewShotExampleSelectInput,
    storage: FewShotExampleStorage,
  ) => TE.TaskEither<FewShotExampleError, FewShotExampleSelectOutput>;
  readonly optimize: (
    input: FewShotExampleOptimizeInput,
    storage: FewShotExampleStorage,
  ) => TE.TaskEither<FewShotExampleError, FewShotExampleOptimizeOutput>;
  readonly embed: (
    input: FewShotExampleEmbedInput,
    storage: FewShotExampleStorage,
  ) => TE.TaskEither<FewShotExampleError, FewShotExampleEmbedOutput>;
  readonly remove: (
    input: FewShotExampleRemoveInput,
    storage: FewShotExampleStorage,
  ) => TE.TaskEither<FewShotExampleError, FewShotExampleRemoveOutput>;
}

// --- Implementation ---

export const fewShotExampleHandler: FewShotExampleHandler = {
  createPool: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('fewshotexample', input.strategy, { strategy: input.strategy, k: input.k, diversity_weight: input.diversity_weight });
          return createPoolOk(input.strategy);
        },
        (error): FewShotExampleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  add: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('fewshotexample', input.pool, { pool: input.pool, input: input.input, output: input.output, metadata: input.metadata });
          return addOk(input.pool);
        },
        (error): FewShotExampleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  select: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('fewshotexample', input.pool, { pool: input.pool, input: input.input, k: input.k });
          return selectOk([]);
        },
        (error): FewShotExampleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  optimize: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('fewshotexample', input.pool, { pool: input.pool, metric: input.metric, training_set: input.training_set });
          return optimizeOk(0, 0);
        },
        (error): FewShotExampleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  embed: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('fewshotexample', input.pool, { pool: input.pool, model_id: input.model_id });
          return embedOk(0);
        },
        (error): FewShotExampleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  remove: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('fewshotexample', input.pool),
        (error): FewShotExampleError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(removeNotfound(`${input.pool} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('fewshotexample', input.pool);
                  return removeOk();
                },
                (error): FewShotExampleError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),
};
