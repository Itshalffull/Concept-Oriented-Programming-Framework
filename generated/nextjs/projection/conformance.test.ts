// Projection — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { projectionHandler } from './handler.js';
import type { ProjectionStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): ProjectionStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Projection conformance', () => {
  it('invariant 1: after project, validate, inferResources behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = projectionHandler;

    const p = 'u-test-invariant-001';
    const w = 'u-test-invariant-002';
    const r = 'u-test-invariant-003';

    // setup: project -> ok
    const projectResultSetup = await pipe(
      handler.project({
      manifest: 'valid-manifest',
      annotations: 'valid-annotations',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).projection).toBe(p);
        expect((output as any).shapes).toBe(3);
        expect((output as any).actions).toBe(4);
        expect((output as any).traits).toBe(2);
        return output;
      }),
    )();
    expect(E.isRight(projectResultSetup)).toBe(true);

    // assert: validate -> ok
    const validateResultAssert = await pipe(
      handler.validate({
      projection: p,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).projection).toBe(p);
        expect((output as any).warnings).toBe(w);
        return output;
      }),
    )();
    expect(E.isRight(validateResultAssert)).toBe(true);

    // assert: inferResources -> ok
    const inferResourcesResultAssert = await pipe(
      handler.inferResources({
      projection: p,
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).projection).toBe(p);
        expect((output as any).resources).toBe(r);
        return output;
      }),
    )();
    expect(E.isRight(inferResourcesResultAssert)).toBe(true);

  });

});
