// Secret — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { secretHandler } from './handler.js';
import type { SecretStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): SecretStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('Secret conformance', () => {
  it('invariant 1: after resolve, exists behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = secretHandler;

    const s = 'u-test-invariant-001';

    // setup: resolve -> ok
    const resolveResultSetup = await pipe(
      handler.resolve({
      name: 'DB_PASSWORD',
      provider: 'vault',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).secret).toBe(s);
        expect((output as any).version).toBe('v1');
        return output;
      }),
    )();
    expect(E.isRight(resolveResultSetup)).toBe(true);

    // assert: exists -> ok
    const existsResultAssert = await pipe(
      handler.exists({
      name: 'DB_PASSWORD',
      provider: 'vault',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).name).toBe('DB_PASSWORD');
        expect((output as any).exists).toBe(true);
        return output;
      }),
    )();
    expect(E.isRight(existsResultAssert)).toBe(true);

  });

});
