// SymbolRelationship — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { symbolRelationshipHandler } from './handler.js';
import type { SymbolRelationshipStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): SymbolRelationshipStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('SymbolRelationship conformance', () => {
  it('invariant 1: after add, findFrom behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = symbolRelationshipHandler;

    const r = 'u-test-invariant-001';

    // setup: add -> ok
    const addResultSetup = await pipe(
      handler.add({
      source: 'ts/class/Handler',
      target: 'ts/interface/IHandler',
      kind: 'implements',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).relationship).toBe(r);
        return output;
      }),
    )();
    expect(E.isRight(addResultSetup)).toBe(true);

    // assert: findFrom -> ok
    const findFromResultAssert = await pipe(
      handler.findFrom({
      source: 'ts/class/Handler',
      kind: 'implements',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).relationships).toBe(_);
        return output;
      }),
    )();
    expect(E.isRight(findFromResultAssert)).toBe(true);

  });

  it('invariant 2: after add, add behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = symbolRelationshipHandler;

    const r = 'u-test-invariant-001';

    // setup: add -> ok
    const addResultSetup = await pipe(
      handler.add({
      source: 'ts/class/Handler',
      target: 'ts/interface/IHandler',
      kind: 'implements',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).relationship).toBe(r);
        return output;
      }),
    )();
    expect(E.isRight(addResultSetup)).toBe(true);

    // assert: add -> alreadyExists
    const addResultAssert = await pipe(
      handler.add({
      source: 'ts/class/Handler',
      target: 'ts/interface/IHandler',
      kind: 'implements',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('alreadyExists');
        expect((output as any).existing).toBe(r);
        return output;
      }),
    )();
    expect(E.isRight(addResultAssert)).toBe(true);

  });

});
