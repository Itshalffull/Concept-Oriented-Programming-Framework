// VercelRuntime — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for VercelRuntime
export interface VercelRuntimeStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface VercelRuntimeProvisionInput {
  readonly concept: string;
  readonly teamId: string;
  readonly framework: string;
}

export interface VercelRuntimeProvisionOutputOk {
  readonly variant: 'ok';
  readonly project: string;
  readonly projectId: string;
  readonly endpoint: string;
}

export interface VercelRuntimeProvisionOutputDomainConflict {
  readonly variant: 'domainConflict';
  readonly domain: string;
  readonly existingProject: string;
}

export type VercelRuntimeProvisionOutput = VercelRuntimeProvisionOutputOk | VercelRuntimeProvisionOutputDomainConflict;

export interface VercelRuntimeDeployInput {
  readonly project: string;
  readonly sourceDirectory: string;
}

export interface VercelRuntimeDeployOutputOk {
  readonly variant: 'ok';
  readonly project: string;
  readonly deploymentId: string;
  readonly deploymentUrl: string;
}

export interface VercelRuntimeDeployOutputBuildFailed {
  readonly variant: 'buildFailed';
  readonly project: string;
  readonly errors: readonly string[];
}

export type VercelRuntimeDeployOutput = VercelRuntimeDeployOutputOk | VercelRuntimeDeployOutputBuildFailed;

export interface VercelRuntimeSetTrafficWeightInput {
  readonly project: string;
  readonly weight: number;
}

export interface VercelRuntimeSetTrafficWeightOutput {
  readonly variant: 'ok';
  readonly project: string;
}

export interface VercelRuntimeRollbackInput {
  readonly project: string;
  readonly targetDeploymentId: string;
}

export interface VercelRuntimeRollbackOutput {
  readonly variant: 'ok';
  readonly project: string;
  readonly restoredDeploymentId: string;
}

export interface VercelRuntimeDestroyInput {
  readonly project: string;
}

export interface VercelRuntimeDestroyOutput {
  readonly variant: 'ok';
  readonly project: string;
}

// --- Variant constructors ---

export const provisionOk = (project: string, projectId: string, endpoint: string): VercelRuntimeProvisionOutput => ({ variant: 'ok', project, projectId, endpoint } as VercelRuntimeProvisionOutput);
export const provisionDomainConflict = (domain: string, existingProject: string): VercelRuntimeProvisionOutput => ({ variant: 'domainConflict', domain, existingProject } as VercelRuntimeProvisionOutput);

export const deployOk = (project: string, deploymentId: string, deploymentUrl: string): VercelRuntimeDeployOutput => ({ variant: 'ok', project, deploymentId, deploymentUrl } as VercelRuntimeDeployOutput);
export const deployBuildFailed = (project: string, errors: readonly string[]): VercelRuntimeDeployOutput => ({ variant: 'buildFailed', project, errors } as VercelRuntimeDeployOutput);

export const setTrafficWeightOk = (project: string): VercelRuntimeSetTrafficWeightOutput => ({ variant: 'ok', project } as VercelRuntimeSetTrafficWeightOutput);

export const rollbackOk = (project: string, restoredDeploymentId: string): VercelRuntimeRollbackOutput => ({ variant: 'ok', project, restoredDeploymentId } as VercelRuntimeRollbackOutput);

export const destroyOk = (project: string): VercelRuntimeDestroyOutput => ({ variant: 'ok', project } as VercelRuntimeDestroyOutput);

