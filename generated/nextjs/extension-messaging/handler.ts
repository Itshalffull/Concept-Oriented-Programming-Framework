// ExtensionMessaging — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ExtensionMessagingStorage,
  ExtensionMessagingRegisterChannelInput,
  ExtensionMessagingRegisterChannelOutput,
  ExtensionMessagingSendInput,
  ExtensionMessagingSendOutput,
  ExtensionMessagingRequestInput,
  ExtensionMessagingRequestOutput,
  ExtensionMessagingRespondInput,
  ExtensionMessagingRespondOutput,
  ExtensionMessagingBroadcastInput,
  ExtensionMessagingBroadcastOutput,
  ExtensionMessagingSubscribeInput,
  ExtensionMessagingSubscribeOutput,
  ExtensionMessagingUnsubscribeInput,
  ExtensionMessagingUnsubscribeOutput,
} from './types.js';

import {
  registerChannelOk,
  registerChannelExists,
  sendOk,
  sendNotfound,
  sendInvalid,
  requestOk,
  requestNotfound,
  requestTimeout,
  respondOk,
  respondNotfound,
  broadcastOk,
  broadcastNotfound,
  subscribeOk,
  subscribeNotfound,
  subscribeExists,
  unsubscribeOk,
  unsubscribeNotfound,
} from './types.js';

export interface ExtensionMessagingError {
  readonly code: string;
  readonly message: string;
}

export interface ExtensionMessagingHandler {
  readonly registerChannel: (
    input: ExtensionMessagingRegisterChannelInput,
    storage: ExtensionMessagingStorage,
  ) => TE.TaskEither<ExtensionMessagingError, ExtensionMessagingRegisterChannelOutput>;
  readonly send: (
    input: ExtensionMessagingSendInput,
    storage: ExtensionMessagingStorage,
  ) => TE.TaskEither<ExtensionMessagingError, ExtensionMessagingSendOutput>;
  readonly request: (
    input: ExtensionMessagingRequestInput,
    storage: ExtensionMessagingStorage,
  ) => TE.TaskEither<ExtensionMessagingError, ExtensionMessagingRequestOutput>;
  readonly respond: (
    input: ExtensionMessagingRespondInput,
    storage: ExtensionMessagingStorage,
  ) => TE.TaskEither<ExtensionMessagingError, ExtensionMessagingRespondOutput>;
  readonly broadcast: (
    input: ExtensionMessagingBroadcastInput,
    storage: ExtensionMessagingStorage,
  ) => TE.TaskEither<ExtensionMessagingError, ExtensionMessagingBroadcastOutput>;
  readonly subscribe: (
    input: ExtensionMessagingSubscribeInput,
    storage: ExtensionMessagingStorage,
  ) => TE.TaskEither<ExtensionMessagingError, ExtensionMessagingSubscribeOutput>;
  readonly unsubscribe: (
    input: ExtensionMessagingUnsubscribeInput,
    storage: ExtensionMessagingStorage,
  ) => TE.TaskEither<ExtensionMessagingError, ExtensionMessagingUnsubscribeOutput>;
}

// --- Implementation ---

export const extensionMessagingHandler: ExtensionMessagingHandler = {
  registerChannel: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionmessaging', input.name, { name: input.name, schema: input.schema });
          return registerChannelOk(input.name);
        },
        (error): ExtensionMessagingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  send: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionmessaging', input.channel, { channel: input.channel, sender: input.sender, payload: input.payload });
          return sendOk(input.channel);
        },
        (error): ExtensionMessagingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  request: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionmessaging', input.channel, { channel: input.channel, sender: input.sender, payload: input.payload });
          return requestOk(input.channel, input.channel);
        },
        (error): ExtensionMessagingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  respond: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionmessaging', input.channel, { channel: input.channel, requestId: input.requestId, payload: input.payload });
          return respondOk(input.channel);
        },
        (error): ExtensionMessagingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  broadcast: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionmessaging', input.channel, { channel: input.channel, sender: input.sender, payload: input.payload });
          return broadcastOk(input.channel, 0);
        },
        (error): ExtensionMessagingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  subscribe: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionmessaging', input.channel, { channel: input.channel, subscriber: input.subscriber });
          return subscribeOk(input.channel);
        },
        (error): ExtensionMessagingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  unsubscribe: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionmessaging', input.channel),
        (error): ExtensionMessagingError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(unsubscribeNotfound(`${input.channel} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('extensionmessaging', input.channel);
                  return unsubscribeOk();
                },
                (error): ExtensionMessagingError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),
};
