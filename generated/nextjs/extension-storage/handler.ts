// ExtensionStorage — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  ExtensionStorageStorage,
  ExtensionStorageProvisionInput,
  ExtensionStorageProvisionOutput,
  ExtensionStorageSetInput,
  ExtensionStorageSetOutput,
  ExtensionStorageGetInput,
  ExtensionStorageGetOutput,
  ExtensionStorageRemoveInput,
  ExtensionStorageRemoveOutput,
  ExtensionStorageClearInput,
  ExtensionStorageClearOutput,
  ExtensionStorageGetQuotaInput,
  ExtensionStorageGetQuotaOutput,
  ExtensionStorageSyncInput,
  ExtensionStorageSyncOutput,
  ExtensionStorageDestroyInput,
  ExtensionStorageDestroyOutput,
} from './types.js';

import {
  provisionOk,
  provisionExists,
  setOk,
  setNotfound,
  setQuotaExceeded,
  getOk,
  getNotfound,
  removeOk,
  removeNotfound,
  clearOk,
  clearNotfound,
  getQuotaOk,
  getQuotaNotfound,
  syncOk,
  syncNotfound,
  syncDisabled,
  destroyOk,
  destroyNotfound,
} from './types.js';

export interface ExtensionStorageError {
  readonly code: string;
  readonly message: string;
}

export interface ExtensionStorageHandler {
  readonly provision: (
    input: ExtensionStorageProvisionInput,
    storage: ExtensionStorageStorage,
  ) => TE.TaskEither<ExtensionStorageError, ExtensionStorageProvisionOutput>;
  readonly set: (
    input: ExtensionStorageSetInput,
    storage: ExtensionStorageStorage,
  ) => TE.TaskEither<ExtensionStorageError, ExtensionStorageSetOutput>;
  readonly get: (
    input: ExtensionStorageGetInput,
    storage: ExtensionStorageStorage,
  ) => TE.TaskEither<ExtensionStorageError, ExtensionStorageGetOutput>;
  readonly remove: (
    input: ExtensionStorageRemoveInput,
    storage: ExtensionStorageStorage,
  ) => TE.TaskEither<ExtensionStorageError, ExtensionStorageRemoveOutput>;
  readonly clear: (
    input: ExtensionStorageClearInput,
    storage: ExtensionStorageStorage,
  ) => TE.TaskEither<ExtensionStorageError, ExtensionStorageClearOutput>;
  readonly getQuota: (
    input: ExtensionStorageGetQuotaInput,
    storage: ExtensionStorageStorage,
  ) => TE.TaskEither<ExtensionStorageError, ExtensionStorageGetQuotaOutput>;
  readonly sync: (
    input: ExtensionStorageSyncInput,
    storage: ExtensionStorageStorage,
  ) => TE.TaskEither<ExtensionStorageError, ExtensionStorageSyncOutput>;
  readonly destroy: (
    input: ExtensionStorageDestroyInput,
    storage: ExtensionStorageStorage,
  ) => TE.TaskEither<ExtensionStorageError, ExtensionStorageDestroyOutput>;
}

// --- Implementation ---

export const extensionStorageHandler: ExtensionStorageHandler = {
  provision: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionstorage', input.extensionId, { extensionId: input.extensionId, quotaLimit: input.quotaLimit, syncEnabled: input.syncEnabled });
          return provisionOk(input.extensionId);
        },
        (error): ExtensionStorageError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  set: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('extensionstorage', input.store, { store: input.store, key: input.key, value: input.value });
          return setOk(input.store);
        },
        (error): ExtensionStorageError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  get: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionstorage', input.store),
        (error): ExtensionStorageError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getNotfound(`${input.store} not found`)),
            (found) => TE.right(getOk(input.store)),
          ),
        ),
      ),
    ),

  remove: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionstorage', input.store),
        (error): ExtensionStorageError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(removeNotfound(`${input.store} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('extensionstorage', input.store);
                  return removeOk();
                },
                (error): ExtensionStorageError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  clear: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionstorage', input.store),
        (error): ExtensionStorageError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(clearNotfound(`${input.store} not found`)),
            (existing) =>
              TE.tryCatch(
                async () => {
                  const updated = { ...existing, store: input.store };
                  await storage.put('extensionstorage', input.store, updated);
                  return clearOk(input.store);
                },
                (error): ExtensionStorageError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  getQuota: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionstorage', input.store),
        (error): ExtensionStorageError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getQuotaNotfound(`${input.store} not found`)),
            (found) => TE.right(getQuotaOk(0, 0)),
          ),
        ),
      ),
    ),

  sync: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionstorage', input.store),
        (error): ExtensionStorageError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(syncNotfound(`${input.store} not found`)),
            (existing) =>
              TE.tryCatch(
                async () => {
                  const updated = { ...existing, store: input.store };
                  await storage.put('extensionstorage', input.store, updated);
                  return syncOk(input.store);
                },
                (error): ExtensionStorageError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),

  destroy: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('extensionstorage', input.store),
        (error): ExtensionStorageError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(destroyNotfound(`${input.store} not found`)),
            () =>
              TE.tryCatch(
                async () => {
                  await storage.delete('extensionstorage', input.store);
                  return destroyOk();
                },
                (error): ExtensionStorageError => ({
                  code: 'STORAGE_ERROR',
                  message: error instanceof Error ? error.message : String(error),
                }),
              ),
          ),
        ),
      ),
    ),
};
