// QualitySignal — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import type {
  QualitySignalStorage,
  QualitySignalRecordInput,
  QualitySignalRecordOutput,
  QualitySignalLatestInput,
  QualitySignalLatestOutput,
  QualitySignalRollupInput,
  QualitySignalRollupOutput,
  QualitySignalExplainInput,
  QualitySignalExplainOutput,
} from './types.js';

import {
  recordOk,
  recordInvalid,
  latestOk,
  latestNotfound,
  rollupOk,
  explainOk,
} from './types.js';

export interface QualitySignalError {
  readonly code: string;
  readonly message: string;
}

export interface QualitySignalHandler {
  readonly record: (
    input: QualitySignalRecordInput,
    storage: QualitySignalStorage,
  ) => TE.TaskEither<QualitySignalError, QualitySignalRecordOutput>;
  readonly latest: (
    input: QualitySignalLatestInput,
    storage: QualitySignalStorage,
  ) => TE.TaskEither<QualitySignalError, QualitySignalLatestOutput>;
  readonly rollup: (
    input: QualitySignalRollupInput,
    storage: QualitySignalStorage,
  ) => TE.TaskEither<QualitySignalError, QualitySignalRollupOutput>;
  readonly explain: (
    input: QualitySignalExplainInput,
    storage: QualitySignalStorage,
  ) => TE.TaskEither<QualitySignalError, QualitySignalExplainOutput>;
}

// --- Implementation ---

export const qualitySignalHandler: QualitySignalHandler = {
  record: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('qualitysignal', input.target_symbol, { target_symbol: input.target_symbol, dimension: input.dimension, status: input.status, severity: input.severity, summary: input.summary, artifact_path: input.artifact_path, artifact_hash: input.artifact_hash, run_ref: input.run_ref });
          return recordOk(input.target_symbol);
        },
        (error): QualitySignalError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  latest: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('qualitysignal', input.target_symbol, { target_symbol: input.target_symbol, dimension: input.dimension });
          return latestOk(input.target_symbol, input.target_symbol, input.target_symbol, input.target_symbol, input.target_symbol);
        },
        (error): QualitySignalError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  rollup: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('qualitysignal', input.target_symbols, { target_symbols: input.target_symbols, dimensions: input.dimensions });
          return rollupOk([]);
        },
        (error): QualitySignalError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  explain: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('qualitysignal', input.target_symbol, { target_symbol: input.target_symbol, dimensions: input.dimensions });
          return explainOk([]);
        },
        (error): QualitySignalError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};