// QualitySignal — conformance.test.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import { describe, it, expect } from 'vitest';
import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import { qualitySignalHandler } from './handler.js';
import type { QualitySignalStorage } from './types.js';

// In-memory storage for conformance tests
const createTestStorage = (): QualitySignalStorage => {
  const store = new Map<string, Map<string, Record<string, unknown>>>();
  return {
    get: async (relation, key) => store.get(relation)?.get(key) ?? null,
    put: async (relation, key, value) => {
      if (!store.has(relation)) store.set(relation, new Map());
      store.get(relation)!.set(key, value);
    },
    delete: async (relation, key) => store.get(relation)?.delete(key) ?? false,
    find: async (relation) => [...(store.get(relation)?.values() ?? [])],
  };
};

describe('QualitySignal conformance', () => {
  it('invariant 1: after record, latest behaves correctly', async () => {
    const storage = createTestStorage();
    const handler = qualitySignalHandler;

    const nullVar = 'u-test-invariant-001';
    let q: any = 'u-test-invariant-002';
    let t: any = 'u-test-invariant-003';

    // setup: record -> ok
    const recordResultSetup = await pipe(
      handler.record({
      target_symbol: 'clef/concept/Password',
      dimension: 'formal',
      status: 'pass',
      severity: 'gate',
      summary: 'Proved 3 properties',
      artifact_path: nullVar,
      artifact_hash: nullVar,
      run_ref: 'run-1',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        q = (output as any).signal;
        return output;
      }),
    )();
    expect(E.isRight(recordResultSetup)).toBe(true);

    // assert: latest -> ok
    const latestResultAssert = await pipe(
      handler.latest({
      target_symbol: 'clef/concept/Password',
      dimension: 'formal',
      }, storage),
      TE.map((output) => {
        expect(output.variant).toBe('ok');
        expect((output as any).signal).toBe(q);
        expect((output as any).status).toBe('pass');
        expect((output as any).severity).toBe('gate');
        expect((output as any).summary).toBe('Proved 3 properties');
        t = (output as any).observed_at;
        return output;
      }),
    )();
    expect(E.isRight(latestResultAssert)).toBe(true);

  });

});
