// LLMTrace — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import * as O from 'fp-ts/Option';
import { pipe } from 'fp-ts/function';

import type {
  LLMTraceStorage,
  LLMTraceStartTraceInput,
  LLMTraceStartTraceOutput,
  LLMTraceStartSpanInput,
  LLMTraceStartSpanOutput,
  LLMTraceEndSpanInput,
  LLMTraceEndSpanOutput,
  LLMTraceAddMetricInput,
  LLMTraceAddMetricOutput,
  LLMTraceGetCostInput,
  LLMTraceGetCostOutput,
  LLMTraceGetTraceInput,
  LLMTraceGetTraceOutput,
  LLMTraceExportInput,
  LLMTraceExportOutput,
} from './types.js';

import {
  startTraceOk,
  startTraceError,
  startSpanOk,
  startSpanNotfound,
  endSpanOk,
  endSpanNotfound,
  addMetricOk,
  addMetricNotfound,
  getCostOk,
  getCostNotfound,
  getTraceOk,
  getTraceNotfound,
  exportOk,
  exportNotfound,
} from './types.js';

export interface LLMTraceError {
  readonly code: string;
  readonly message: string;
}

export interface LLMTraceHandler {
  readonly startTrace: (
    input: LLMTraceStartTraceInput,
    storage: LLMTraceStorage,
  ) => TE.TaskEither<LLMTraceError, LLMTraceStartTraceOutput>;
  readonly startSpan: (
    input: LLMTraceStartSpanInput,
    storage: LLMTraceStorage,
  ) => TE.TaskEither<LLMTraceError, LLMTraceStartSpanOutput>;
  readonly endSpan: (
    input: LLMTraceEndSpanInput,
    storage: LLMTraceStorage,
  ) => TE.TaskEither<LLMTraceError, LLMTraceEndSpanOutput>;
  readonly addMetric: (
    input: LLMTraceAddMetricInput,
    storage: LLMTraceStorage,
  ) => TE.TaskEither<LLMTraceError, LLMTraceAddMetricOutput>;
  readonly getCost: (
    input: LLMTraceGetCostInput,
    storage: LLMTraceStorage,
  ) => TE.TaskEither<LLMTraceError, LLMTraceGetCostOutput>;
  readonly getTrace: (
    input: LLMTraceGetTraceInput,
    storage: LLMTraceStorage,
  ) => TE.TaskEither<LLMTraceError, LLMTraceGetTraceOutput>;
  readonly export: (
    input: LLMTraceExportInput,
    storage: LLMTraceStorage,
  ) => TE.TaskEither<LLMTraceError, LLMTraceExportOutput>;
}

// --- Implementation ---

export const lLMTraceHandler: LLMTraceHandler = {
  startTrace: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmtrace', input.name, { name: input.name, tags: input.tags });
          return startTraceOk(input.name);
        },
        (error): LLMTraceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  startSpan: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmtrace', input.trace, { trace: input.trace, operation: input.operation, parent_span_id: input.parent_span_id });
          return startSpanOk(input.trace);
        },
        (error): LLMTraceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  endSpan: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmtrace', input.trace, { trace: input.trace, span_id: input.span_id, status: input.status, metrics: input.metrics });
          return endSpanOk(input.span_id);
        },
        (error): LLMTraceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  addMetric: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmtrace', input.trace, { trace: input.trace, span_id: input.span_id, key: input.key, value: input.value });
          return addMetricOk();
        },
        (error): LLMTraceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  getCost: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('llmtrace', input.trace),
        (error): LLMTraceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getCostNotfound(`${input.trace} not found`)),
            (found) => TE.right(getCostOk(0, [])),
          ),
        ),
      ),
    ),

  getTrace: (input, storage) =>
    pipe(
      TE.tryCatch(
        () => storage.get('llmtrace', input.trace),
        (error): LLMTraceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
      TE.chain((record) =>
        pipe(
          O.fromNullable(record),
          O.fold(
            () => TE.right(getTraceNotfound(`${input.trace} not found`)),
            (found) => TE.right(getTraceOk([])),
          ),
        ),
      ),
    ),

  export: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('llmtrace', input.trace, { trace: input.trace, format: input.format });
          return exportOk(input.trace);
        },
        (error): LLMTraceError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
