// Retriever — handler.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

import * as TE from 'fp-ts/TaskEither';
import * as E from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';

import type {
  RetrieverStorage,
  RetrieverCreateInput,
  RetrieverCreateOutput,
  RetrieverRetrieveInput,
  RetrieverRetrieveOutput,
  RetrieverMultiQueryRetrieveInput,
  RetrieverMultiQueryRetrieveOutput,
  RetrieverSelfQueryRetrieveInput,
  RetrieverSelfQueryRetrieveOutput,
  RetrieverRerankInput,
  RetrieverRerankOutput,
  RetrieverCompressInput,
  RetrieverCompressOutput,
  RetrieverSetRerankerInput,
  RetrieverSetRerankerOutput,
} from './types.js';

import {
  createOk,
  createInvalid,
  retrieveOk,
  retrieveEmpty,
  multiQueryRetrieveOk,
  multiQueryRetrieveEmpty,
  selfQueryRetrieveOk,
  selfQueryRetrieveEmpty,
  rerankOk,
  rerankError,
  compressOk,
  compressError,
  setRerankerOk,
  setRerankerInvalid,
} from './types.js';

export interface RetrieverError {
  readonly code: string;
  readonly message: string;
}

export interface RetrieverHandler {
  readonly create: (
    input: RetrieverCreateInput,
    storage: RetrieverStorage,
  ) => TE.TaskEither<RetrieverError, RetrieverCreateOutput>;
  readonly retrieve: (
    input: RetrieverRetrieveInput,
    storage: RetrieverStorage,
  ) => TE.TaskEither<RetrieverError, RetrieverRetrieveOutput>;
  readonly multiQueryRetrieve: (
    input: RetrieverMultiQueryRetrieveInput,
    storage: RetrieverStorage,
  ) => TE.TaskEither<RetrieverError, RetrieverMultiQueryRetrieveOutput>;
  readonly selfQueryRetrieve: (
    input: RetrieverSelfQueryRetrieveInput,
    storage: RetrieverStorage,
  ) => TE.TaskEither<RetrieverError, RetrieverSelfQueryRetrieveOutput>;
  readonly rerank: (
    input: RetrieverRerankInput,
    storage: RetrieverStorage,
  ) => TE.TaskEither<RetrieverError, RetrieverRerankOutput>;
  readonly compress: (
    input: RetrieverCompressInput,
    storage: RetrieverStorage,
  ) => TE.TaskEither<RetrieverError, RetrieverCompressOutput>;
  readonly setReranker: (
    input: RetrieverSetRerankerInput,
    storage: RetrieverStorage,
  ) => TE.TaskEither<RetrieverError, RetrieverSetRerankerOutput>;
}

// --- Implementation ---

export const retrieverHandler: RetrieverHandler = {
  create: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('retriever', input.retriever_type, { retriever_type: input.retriever_type, source_ids: input.source_ids, top_k: input.top_k, score_threshold: input.score_threshold });
          return createOk(input.retriever_type);
        },
        (error): RetrieverError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  retrieve: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('retriever', input.retriever, { retriever: input.retriever, query: input.query });
          return retrieveOk([]);
        },
        (error): RetrieverError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  multiQueryRetrieve: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('retriever', input.retriever, { retriever: input.retriever, query: input.query });
          return multiQueryRetrieveOk([]);
        },
        (error): RetrieverError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  selfQueryRetrieve: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('retriever', input.retriever, { retriever: input.retriever, query: input.query });
          return selfQueryRetrieveOk([], input.retriever);
        },
        (error): RetrieverError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  rerank: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('retriever', input.retriever, { retriever: input.retriever, query: input.query, documents: input.documents });
          return rerankOk([]);
        },
        (error): RetrieverError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  compress: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('retriever', input.retriever, { retriever: input.retriever, query: input.query, documents: input.documents });
          return compressOk([]);
        },
        (error): RetrieverError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),

  setReranker: (input, storage) =>
    pipe(
      TE.tryCatch(
        async () => {
          await storage.put('retriever', input.retriever, { retriever: input.retriever, model: input.model, top_n: input.top_n });
          return setRerankerOk(input.retriever);
        },
        (error): RetrieverError => ({
          code: 'STORAGE_ERROR',
          message: error instanceof Error ? error.message : String(error),
        }),
      ),
    ),
};
