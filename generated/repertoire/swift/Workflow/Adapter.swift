// generated: Workflow/Adapter.swift

import Foundation

class WorkflowAdapter: ConceptTransport {
    private let handler: any WorkflowHandler
    private let storage: ConceptStorage

    init(handler: any WorkflowHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "defineState":
            let input = try decoder.decode(WorkflowDefineStateInput.self, from: invocation.inputData)
            let output = try await handler.defineState(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "defineTransition":
            let input = try decoder.decode(WorkflowDefineTransitionInput.self, from: invocation.inputData)
            let output = try await handler.defineTransition(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "transition":
            let input = try decoder.decode(WorkflowTransitionInput.self, from: invocation.inputData)
            let output = try await handler.transition(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "getCurrentState":
            let input = try decoder.decode(WorkflowGetCurrentStateInput.self, from: invocation.inputData)
            let output = try await handler.getCurrentState(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
