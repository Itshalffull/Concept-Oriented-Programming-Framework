// generated: Reference/Adapter.swift

import Foundation

class ReferenceAdapter: ConceptTransport {
    private let handler: any ReferenceHandler
    private let storage: ConceptStorage

    init(handler: any ReferenceHandler, storage: ConceptStorage) {
        self.handler = handler
        self.storage = storage
    }

    func invoke(invocation: ActionInvocation) async throws -> ActionCompletion {
        let decoder = JSONDecoder()
        let encoder = JSONEncoder()

        switch invocation.action {
        case "addRef":
            let input = try decoder.decode(ReferenceAddRefInput.self, from: invocation.inputData)
            let output = try await handler.addRef(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "removeRef":
            let input = try decoder.decode(ReferenceRemoveRefInput.self, from: invocation.inputData)
            let output = try await handler.removeRef(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "getRefs":
            let input = try decoder.decode(ReferenceGetRefsInput.self, from: invocation.inputData)
            let output = try await handler.getRefs(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        case "resolveTarget":
            let input = try decoder.decode(ReferenceResolveTargetInput.self, from: invocation.inputData)
            let output = try await handler.resolveTarget(input: input, storage: storage)
            let outputData = try encoder.encode(output)
            return ActionCompletion(
                id: invocation.id,
                concept: invocation.concept,
                action: invocation.action,
                input: invocation.inputData,
                output: outputData,
                flow: invocation.flow
            )
        default:
            throw ConceptError.unknownAction(invocation.action)
        }
    }

    func query(request: ConceptQuery) async throws -> [Data] {
        try await storage.find(relation: request.relation, args: request.args)
    }

    func health() async throws -> (healthy: Bool, latencyMs: UInt64) {
        (true, 0)
    }
}
