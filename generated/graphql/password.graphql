type PasswordState {
  """All us in this concept"""
  us: [ID!]!
}

type PasswordEntry {
  u: ID!
  hash: String!
  salt: String!
}

extend type Query {
  password_entry(u: ID!): PasswordEntry
  password_entries: [PasswordEntry!]!
}
