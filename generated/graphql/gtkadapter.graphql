type GTKAdapterState {
  """All as in this concept"""
  as: [ID!]!
}

type GTKAdapterEntry {
  a: ID!
  outputs: String!
}

extend type Query {
  gtkadapter_entry(a: ID!): GTKAdapterEntry
  gtkadapter_entries: [GTKAdapterEntry!]!
}
