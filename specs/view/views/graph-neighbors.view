view "graph-neighbors" {
  shell: "graph-neighbors"

  features {
    projection
  }

  purpose {
    Graph visualization of directly connected entities in the
    knowledge graph. Read-only.
  }

  invariants {
    always "purity is read-only": {
      purity = "read-only"
    }
  }
}
