view "dashboard-concepts" {
  shell: "dashboard-concepts"

  features {
    sort
    projection
  }

  purpose {
    Registered concepts displayed as a grid on the dashboard.
  }

  invariants {
    always "purity is read-only": { purity = "read-only" }
    always "no invoke instructions": { invokeCount = 0 }
  }
}
