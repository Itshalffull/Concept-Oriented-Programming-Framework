view "entity-content" {
  shell: "entity-content"

  features {
    projection
  }

  purpose {
    Unstructured content zone of the entity detail page showing
    the content body. Read-only.
  }

  invariants {
    always "purity is read-only": {
      purity = "read-only"
    }

    always "no invoke instructions": {
      invokeCount = 0
    }
  }
}
