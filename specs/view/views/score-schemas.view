view "score-schemas" {
  shell: "score-schemas"

  features {
    filter
    sort
    projection
  }

  purpose {
    Browse parsed schema definitions from Score. Filterable and
    sortable card grid of all schema types. Read-only.
  }

  invariants {
    always "purity is read-only": {
      purity = "read-only"
    }

    always "no invoke instructions": {
      invokeCount = 0
    }
  }
}
