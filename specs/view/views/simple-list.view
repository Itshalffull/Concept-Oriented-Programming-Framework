view "simple-list" {
  shell: "simple-list"

  features {
    sort
  }

  purpose {
    Minimal data listing with no filtering or actions.
  }

  invariants {
    always "read-only": {
      purity = "read-only"
    }
  }
}
