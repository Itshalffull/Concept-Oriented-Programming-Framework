view "syncs-list" {
  shell: "syncs-list"

  features {
    filter
    sort
    projection
  }

  purpose {
    Browse synchronization rules wiring concepts together through
    pattern matching. Filterable by suite tier. Read-only.
  }

  invariants {
    always "purity is read-only": {
      purity = "read-only"
    }

    always "no invoke instructions": {
      invokeCount = 0
    }
  }
}
