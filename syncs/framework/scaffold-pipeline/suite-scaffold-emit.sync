# Route KitScaffoldGen file output through Emitter

sync EmitKitScaffoldFiles [eager]
  purpose { Write generated suite manifest and stubs through Emitter. }
when {
  KitScaffoldGen/generate: [ name: ?name ]
    => [ files: ?files ]
}
then {
  Emitter/writeBatch: [ files: ?files ]
}
