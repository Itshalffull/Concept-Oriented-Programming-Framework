# Auto-register the Ink render-interpreter provider on first use

sync RegisterInkInterpreterProvider [eager]
  purpose: "Self-register the Ink target interpreter in plugin-registry so the RenderInterpreter dispatcher can discover it."
when {
  RenderInterpreterInk/interpret: []
    => []
}
then {
  RenderInterpreterInk/initialize: []
}
