# ObserveRunComplete — generation suite recommended sync
sync ObserveRunComplete [eager]
  purpose { Mark a generation run as complete when pipeline quiesces. }
when {
  SyncEngine/quiesced: []
    => []
}
where {
  GenerationPlan: { activeRun: ?run }
}
then {
  GenerationPlan/complete: []
}
