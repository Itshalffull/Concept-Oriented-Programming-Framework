# PluginRegistry resolves the "react" strategy to ReactStrategy
# for ReAct reasoning loop execution.

sync ReactRoutes [integration]
when {
  PluginRegistry/resolve: [plugin_type: "agent_strategy"; plugin_id: "react"] => _
}
then {
  ReactStrategy/execute: [agent_ref: ?a; goal: ?g; context: ?c]
}
