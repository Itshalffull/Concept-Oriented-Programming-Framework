# Tool approval requests trigger HITL notifications for human
# review of destructive or sensitive tool invocations.

sync HITLNotification [eager]
when {
  ToolBinding/invoke => [approval_required: true; tool: ?t; arguments: ?args]
}
then {
  Notification/send: [channel: "hitl"; message: ?args]
}
