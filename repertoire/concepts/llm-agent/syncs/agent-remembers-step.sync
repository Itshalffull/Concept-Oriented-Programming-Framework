# AgentLoop reasoning steps are stored in AgentMemory as episodic
# memories for future context and learning.

sync AgentRemembersStep [recommended]
when {
  AgentLoop/step => [reasoning: ?r; step: ?s]
}
then {
  AgentMemory/remember: [content: ?r; memory_type: "episodic"]
}
