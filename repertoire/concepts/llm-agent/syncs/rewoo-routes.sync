# PluginRegistry resolves "rewoo" strategy to ReWOOStrategy
# for batch tool planning and execution.

sync ReWOORoutes [integration]
when {
  PluginRegistry/resolve: [plugin_type: "agent_strategy"; plugin_id: "rewoo"] => _
}
then {
  ReWOOStrategy/execute: [agent_ref: ?a; goal: ?g; context: ?c]
}
