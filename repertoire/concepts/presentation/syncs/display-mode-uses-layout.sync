# Layout delegation — when DisplayMode has a layout set, delegate rendering
# to Component/renderLayout with the entity context.

sync DisplayModeUsesLayout [eager]
when {
  DisplayMode/get: [ mode: ?dm; layout: ?layout ]
    => [ ok ]
}
where {
  filter(?layout != null)
  DisplayMode: { ?dm schema: ?schema; mode_id: ?mode }
}
then {
  Component/renderLayout: [ layout: ?layout;
                            context: { schema: ?schema,
                                       mode_id: ?mode } ]
}
