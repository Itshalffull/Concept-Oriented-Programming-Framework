# Deploy completion marker — Architecture doc Section 2.4
# Emit a telemetry marker when rollout completes.

sync MarkerOnDeployComplete [eventual]
when {
  Rollout/advance: [ rollout: ?r ]
    => [ rollout: ?r ]
}
then {
  Telemetry/deployMarker: [
    suite: ?suite; version: ?version;
    environment: ?env; status: "completed" ]
}
