# Route Builder/build to RustBuilder — Architecture doc Section 3.8

sync RouteBuilderToRust [eager]
when {
  Builder/build: [ concept: ?concept; source: ?src;
    language: "rust"; platform: ?plat; config: ?cfg ]
    => [ build: ?b; artifactHash: ?hash;
         artifactLocation: ?loc; duration: ?dur ]
}
then {
  RustBuilder/build: [ source: ?src; toolchainPath: ?toolPath;
    platform: ?plat; config: ?cfg ]
}
