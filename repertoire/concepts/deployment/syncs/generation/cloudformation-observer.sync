# Record CloudFormationProvider completion in GenerationPlan — Section 3.8

sync ObserveCloudFormation [eager]
when {
  CloudFormationProvider/generate: [ plan: ?plan ]
    => [ files: ?files ]
}
then {
  GenerationPlan/recordStep: [
    stepKey: ?plan;
    status: "done";
    filesProduced: 1;
    cached: false
  ]
}
