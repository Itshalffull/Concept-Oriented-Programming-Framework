# Route ArgoCDProvider file output through Emitter — Section 3.8

sync EmitArgoCDFiles [eager]
when {
  ArgoCDProvider/emit: [ plan: ?plan; repo: ?repo; path: ?path ]
    => [ files: ?files ]
}
then {
  Emitter/writeBatch: [ files: ?files ]
}
