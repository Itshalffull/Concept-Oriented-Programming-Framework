# TrainingRun start creates an LLMTrace for tracking compute
# cost and resource usage throughout the training process.

sync TrainingTracksCost [recommended]
when {
  TrainingRun/start: [] => [run: ?j]
}
then {
  LLMTrace/startTrace: [name: ?j]
}
