# Conversation instances register as a Collection provider for
# cross-conversation search, filtering, and clustering.

sync ConversationCollectionProvider [eager]
when {
  Conversation/create => [conversation: ?c]
}
then {
  Collection/add: [item: ?c; collection_type: "conversation"]
}
