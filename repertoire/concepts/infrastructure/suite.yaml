# Infrastructure Suite Manifest
name: infrastructure
version: 0.2.0
description: "System infrastructure — caching, configuration sync, path aliases, plugin discovery, event dispatching, validation, file catalog, runtime registry, and entity reflection."

concepts:
  - name: Cache
    spec: cache.concept
  - name: ConfigSync
    spec: config-sync.concept
  - name: Pathauto
    spec: pathauto.concept
  - name: PluginRegistry
    spec: plugin-registry.concept
  - name: EventBus
    spec: event-bus.concept
  - name: AppInstallation
    spec: app-installation.concept
  - name: Validator
    spec: validator.concept
  - name: FileCatalog
    spec: file-catalog.concept
  - name: RuntimeRegistry
    spec: runtime-registry.concept
  - name: EntityReflector
    spec: entity-reflector.concept

syncs:
  - name: event-validation
    file: syncs/event-validation.sync
    tier: required

dependencies: []
