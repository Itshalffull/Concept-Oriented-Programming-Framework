# Media Suite Manifest
name: media
version: 0.1.0
description: "Media and file handling — file uploads, media asset creation, metadata extraction, and usage tracking."

concepts:
  - name: FileManagement
    spec: file-management.concept
  - name: MediaAsset
    spec: media-asset.concept

syncs:
  - name: media-lifecycle
    file: syncs/media-lifecycle.sync
    tier: required

dependencies: []
