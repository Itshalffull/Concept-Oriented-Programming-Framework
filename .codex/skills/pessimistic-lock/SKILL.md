---
name: pessimistic-lock
description: Prevent conflicts by granting exclusive write access to a resource , 
 serializing edits rather than reconciling them after the fact . 
 Complementary to ConflictResolution use locking for non mergeable 
 content ( binary files , legal documents ) and resolution for mergeable 
 content ( text , structured data ) . checkOut may complete after an 
 arbitrarily long wait if the resource is locked and the requester 
 is queued .
argument-hint: [command] [resource] [holder] [duration] [reason]
---

<!-- Auto-generated by Clef Clef Bind — claude-skills target -->
<!-- Concept: PessimisticLock -->
<!-- Do not edit manually; regenerate with: clef interface generate -->

# PessimisticLock

Prevent conflicts by granting exclusive write access to a resource , 
 serializing edits rather than reconciling them after the fact . 
 Complementary to ConflictResolution use locking for non mergeable 
 content ( binary files , legal documents ) and resolution for mergeable 
 content ( text , structured data ) . checkOut may complete after an 
 arbitrarily long wait if the resource is locked and the requester 
 is queued .

## Commands

### checkOut
Exclusive lock granted to holder .

**Arguments:** `$0` **resource** (string), `$1` **holder** (string), `$2` **duration** (int?), `$3` **reason** (string?)

### checkIn
Lock released . Next requester in queue is notified .

**Arguments:** `$0` **lockId** (L)

### breakLock
Lock forcibly released . Reason is mandatory and recorded .

**Arguments:** `$0` **lockId** (L), `$1` **breaker** (string), `$2` **reason** (string)

### renew
Lock expiry extended by the additional duration .

**Arguments:** `$0` **lockId** (L), `$1` **additionalDuration** (int)

### queryLocks
All active locks , optionally filtered to a specific resource .

**Arguments:** `$0` **resource** (string?)

### queryQueue
Wait queue for the resource .

**Arguments:** `$0` **resource** (string)
