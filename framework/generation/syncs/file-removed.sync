# FileRemoved — generation suite required sync
sync FileRemoved [eager]
  purpose { Track file deletions. }
when {
  FileWatcher/detected: [ path: ?path; event: "delete" ]
    => []
}
then {
  Resource/remove: [ locator: ?path ]
}
