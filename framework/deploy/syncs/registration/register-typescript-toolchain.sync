# Register TypeScriptToolchain with PluginRegistry — Architecture doc Section 3.8

sync RegisterTypeScriptToolchain [eager]
when {
  TypeScriptToolchain/register: []
    => [ name: ?name; language: ?lang; capabilities: ?caps ]
}
then {
  PluginRegistry/register: [
    type: "toolchain";
    name: ?name;
    metadata: {
      language: ?lang;
      concept: "TypeScriptToolchain";
      action: "resolve";
      capabilities: ?caps
    }
  ]
}
