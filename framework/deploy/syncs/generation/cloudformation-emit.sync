# Route CloudFormationProvider file output through Emitter — Section 3.8

sync EmitCloudFormationFiles [eager]
when {
  CloudFormationProvider/generate: [ plan: ?plan ]
    => [ files: ?files ]
}
then {
  Emitter/writeBatch: [ files: ?files ]
}
