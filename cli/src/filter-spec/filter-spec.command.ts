// Auto-generated by Clef Clef Bind — cli target
// Concept: FilterSpec
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const filterSpecCommand = new Command('filter-spec')
  .description('Represent reusable , composable predicate trees that determine which records pass a filter . Each filter holds a serialized FilterNode AST , the set of fields it references ( for pushdown analysis ) , the bound context variables it expects , and its source classification . Filters can be composed algebraically into AND trees , evaluated directly against JSON row arrays , normalized to canonical forms , validated against a schema , and have parameter nodes substituted with concrete values at runtime');

filterSpecCommand
  .command('create')
  .description('Register a new named filter with its serialized FilterNode tree and source classification . The filter is immediately available for composition , evaluation , and validation .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--tree <tree>', 'Tree')
  .requiredOption('--source-type <sourceType>', 'Source Type')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterSpec', 'create', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterSpecCommand
  .command('get')
  .description('Return the filter s tree , source classification , and the field references extracted from the tree at registration time .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterSpec', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterSpecCommand
  .command('compose')
  .description('Create a new unnamed filter whose tree is { type : and , conditions : [ a . tree , b . tree ] } . The resulting FilterNode preserves both sub trees . Composing with the identity filter ( type : true ) produces a tree semantically equal to the other operand . Composition is commutative and idempotent .')
  .requiredOption('--a <a>', 'A')
  .requiredOption('--b <b>', 'B')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterSpec', 'compose', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterSpecCommand
  .command('evaluate')
  .description('Apply the filter s predicate tree to the provided JSON array of row objects and return only the rows that match the predicate . The result is a JSON array string containing the matching rows .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--rows <rows>', 'Rows')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterSpec', 'evaluate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterSpecCommand
  .command('normalize')
  .description('Return the filter tree converted to the requested normal form ( cnf or dnf ) . The normalized tree is semantically equivalent to the original and can be substituted anywhere the original tree would be used .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--form <form>', 'Form')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterSpec', 'normalize', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterSpecCommand
  .command('validate')
  .description('All field references in the filter tree exist in the provided schema . The filter is safe to apply against data matching that schema .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--schema <schema>', 'Schema')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterSpec', 'validate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterSpecCommand
  .command('bind')
  .description('Replace every { type : param } node in the filter tree with the concrete value from the bindings map that matches the node s parameter name . The bound tree is returned as a serialized FilterNode string ready for evaluation .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--bindings <bindings>', 'Bindings')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterSpec', 'bind', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterSpecCommand
  .command('list')
  .description('Return all registered filters as a JSON array , each entry containing the filter s name , tree , sourceType , and fieldRefs .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterSpec', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const filterSpecCommandTree = {
  group: 'filter-spec',
  description: 'Represent reusable , composable predicate trees that determine which records pass a filter . Each filter holds a serialized FilterNode AST , the set of fields it references ( for pushdown analysis ) , the bound context variables it expects , and its source classification . Filters can be composed algebraically into AND trees , evaluated directly against JSON row arrays , normalized to canonical forms , validated against a schema , and have parameter nodes substituted with concrete values at runtime',
  commands: [{ action: 'create', command: 'create' }, { action: 'get', command: 'get' }, { action: 'compose', command: 'compose' }, { action: 'evaluate', command: 'evaluate' }, { action: 'normalize', command: 'normalize' }, { action: 'validate', command: 'validate' }, { action: 'bind', command: 'bind' }, { action: 'list', command: 'list' }],
};
