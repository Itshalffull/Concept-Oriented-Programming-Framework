// Auto-generated by Clef Clef Bind — cli target
// Concept: CompletionCoverage
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const completionCoverageCommand = new Command('completion-coverage')
  .description('Verify that every variant a handler can produce is matched by at least one sync when pattern , and that every sync when pattern with a variant filter matches a variant that actually exists the static bridge between handler behavior and sync wiring');

completionCoverageCommand
  .command('check')
  .description('Every declared variant has at least one sync when pattern matching it , and every sync when pattern for this concept action references an existing variant . Full bidirectional coverage no sync wiring gaps .')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--action <action>', 'Action')
  .requiredOption('--declared-variants <declaredVariants>', 'Declared Variants')
  .requiredOption('--extracted-variants <extractedVariants>', 'Extracted Variants')
  .requiredOption('--sync-patterns <syncPatterns>', 'Sync Patterns')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/CompletionCoverage', 'check', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

completionCoverageCommand
  .command('report')
  .description('Return all coverage reports for the given concept , serialized as a JSON array of report objects .')
  .requiredOption('--concept <concept>', 'Concept')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/CompletionCoverage', 'report', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

completionCoverageCommand
  .command('list-uncovered')
  .description('Return all concept action pairs across the entire system that have at least one uncovered variant .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/CompletionCoverage', 'listUncovered', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const completionCoverageCommandTree = {
  group: 'completion-coverage',
  description: 'Verify that every variant a handler can produce is matched by at least one sync when pattern , and that every sync when pattern with a variant filter matches a variant that actually exists the static bridge between handler behavior and sync wiring',
  commands: [{ action: 'check', command: 'check' }, { action: 'report', command: 'report' }, { action: 'listUncovered', command: 'list-uncovered' }],
};
