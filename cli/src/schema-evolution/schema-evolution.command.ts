// Auto-generated by Clef Clef Bind — cli target
// Concept: SchemaEvolution
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const schemaEvolutionCommand = new Command('schema-evolution')
  .description('Manage versioned structural definitions with compatibility guarantees . Supports backward , forward , and full compatibility modes with upcast transformations between schema versions');

schemaEvolutionCommand
  .command('register')
  .description('Schema registered for subject . Version auto incremented .')
  .requiredOption('--subject <subject>', 'Subject')
  .requiredOption('--schema <schema>', 'Schema')
  .requiredOption('--compatibility <compatibility>', 'Compatibility')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SchemaEvolution', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

schemaEvolutionCommand
  .command('check')
  .description('newSchema is compatible with oldSchema under the given mode .')
  .requiredOption('--old-schema <oldSchema>', 'Old Schema')
  .requiredOption('--new-schema <newSchema>', 'New Schema')
  .requiredOption('--mode <mode>', 'Mode')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SchemaEvolution', 'check', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

schemaEvolutionCommand
  .command('upcast')
  .description('Data transformed from fromVersion schema to toVersion schema .')
  .requiredOption('--data <data>', 'Data')
  .requiredOption('--from-version <fromVersion>', 'From Version')
  .requiredOption('--to-version <toVersion>', 'To Version')
  .requiredOption('--subject <subject>', 'Subject')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SchemaEvolution', 'upcast', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

schemaEvolutionCommand
  .command('resolve')
  .description('Merged schema allowing reader to consume writer serialized data .')
  .requiredOption('--reader-schema <readerSchema>', 'Reader Schema')
  .requiredOption('--writer-schema <writerSchema>', 'Writer Schema')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SchemaEvolution', 'resolve', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

schemaEvolutionCommand
  .command('get-schema')
  .description('Returns the schema at the specified version .')
  .requiredOption('--subject <subject>', 'Subject')
  .requiredOption('--version <version>', 'Version')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SchemaEvolution', 'getSchema', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const schemaEvolutionCommandTree = {
  group: 'schema-evolution',
  description: 'Manage versioned structural definitions with compatibility guarantees . Supports backward , forward , and full compatibility modes with upcast transformations between schema versions',
  commands: [{ action: 'register', command: 'register' }, { action: 'check', command: 'check' }, { action: 'upcast', command: 'upcast' }, { action: 'resolve', command: 'resolve' }, { action: 'getSchema', command: 'get-schema' }],
};
