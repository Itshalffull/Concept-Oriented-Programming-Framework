// Auto-generated by Clef Clef Bind — cli target
// Concept: ActionGuide
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const actionGuideCommand = new Command('action-guide')
  .description('Organize concept actions into ordered , annotated workflow sequences for interface targets . The concept owns step ordering ( structural ) and opaque content passthrough for decorations . Each target reads the content keys it understands ( checklists , design principles , anti patterns , validation commands , etc . ) and ignores the rest . New decoration kinds require only a new YAML key and a renderer in the target provider zero concept changes . Renamed from Workflow to avoid collision with suites automation workflow . concept ( FSM with guarded transitions ) . See Architecture doc Section 1.8');

actionGuideCommand
  .command('define')
  .description('Create an action guide from a concept s actions . Steps are ordered action names with titles and prose the concept owns this structure . The content string is JSON from the interface manifest containing decorations whose internal structure is not constrained by this concept . Targets interpret keys they recognize ( e . g . checklists , references , examples , anti patterns , design principles , content sections , validation commands , quick reference , related workflows ) and pass through the rest .')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--steps <steps>', 'Steps')
  .requiredOption('--content <content>', 'Content')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ActionGuide', 'define', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

actionGuideCommand
  .command('render')
  .description('Render an action guide in the requested format . Supported formats include skill md , cli help , rest guide , and generic . Each format produces output suited to its target audience . The renderer deserializes the content JSON and picks out keys it understands for the given format .')
  .requiredOption('--workflow <workflow>', 'Workflow')
  .requiredOption('--format <format>', 'Format')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ActionGuide', 'render', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const actionGuideCommandTree = {
  group: 'action-guide',
  description: 'Organize concept actions into ordered , annotated workflow sequences for interface targets . The concept owns step ordering ( structural ) and opaque content passthrough for decorations . Each target reads the content keys it understands ( checklists , design principles , anti patterns , validation commands , etc . ) and ignores the rest . New decoration kinds require only a new YAML key and a renderer in the target provider zero concept changes . Renamed from Workflow to avoid collision with suites automation workflow . concept ( FSM with guarded transitions ) . See Architecture doc Section 1.8',
  commands: [{ action: 'define', command: 'define' }, { action: 'render', command: 'render' }],
};
