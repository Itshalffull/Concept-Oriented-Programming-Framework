// Auto-generated by Clef Clef Bind — cli target
// Concept: TestEntity
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const testEntityCommand = new Command('test-entity')
  .description('Queryable representation of test suites , conformance checks , and validation rules . Links tests to the concepts , syncs , handlers , and widgets they validate . Enables queries like what tests cover User/create? , which invariants are tested? , what\'s the test coverage for this concept? , and which tests are failing?');

testEntityCommand
  .command('register')
  .description('Register a test . kind is one of : conformance , unit , integration , invariant , sync chain , widget render , theme contrast , e2e . targetEntity is the concept , sync , handler , or widget being tested .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--source-file <sourceFile>', 'Source File')
  .requiredOption('--kind <kind>', 'Kind')
  .requiredOption('--target-entity <targetEntity>', 'Target Entity')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

testEntityCommand
  .command('get')
  .description('Retrieve a test entity by name .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

testEntityCommand
  .command('find-by-entity')
  .description('Return all tests targeting a given entity . Result as serialized JSON array of { name , kind , sourceFile , lastResult } .')
  .requiredOption('--entity <entity>', 'Entity')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'findByEntity', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

testEntityCommand
  .command('find-by-action')
  .description('Return all tests covering a specific concept action . Result as serialized JSON array .')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'findByAction', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

testEntityCommand
  .command('find-by-kind')
  .description('Return all tests of a given kind . Result as serialized JSON array .')
  .requiredOption('--kind <kind>', 'Kind')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'findByKind', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

testEntityCommand
  .command('find-failing')
  .description('Return all tests whose last result was failure . Result as serialized JSON array of { name , kind , targetEntity , sourceFile , errorMessage } .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'findFailing', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

testEntityCommand
  .command('coverage-report')
  .description('Return test coverage for an entity which actions , variants , and invariants are tested vs untested . Result as serialized JSON { totalActions , testedActions , totalVariants , testedVariants , totalInvariants , testedInvariants , coveragePct } .')
  .requiredOption('--entity <entity>', 'Entity')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'coverageReport', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

testEntityCommand
  .command('untested-actions')
  .description('Return all concept actions that have no test coverage . Result as serialized JSON array of { concept , action , declaredIn } .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'untestedActions', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

testEntityCommand
  .command('untested-invariants')
  .description('Return all declared invariants that have no test . Result as serialized JSON array of { concept , invariant , declaredIn } .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'untestedInvariants', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

testEntityCommand
  .command('record-result')
  .description('Record a test execution result . result is one of : pass , fail , skip , error .')
  .requiredOption('--test <test>', 'Test')
  .requiredOption('--result <result>', 'Result')
  .requiredOption('--duration <duration>', 'Duration')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TestEntity', 'recordResult', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const testEntityCommandTree = {
  group: 'test-entity',
  description: 'Queryable representation of test suites , conformance checks , and validation rules . Links tests to the concepts , syncs , handlers , and widgets they validate . Enables queries like what tests cover User/create? , which invariants are tested? , what\'s the test coverage for this concept? , and which tests are failing?',
  commands: [{ action: 'register', command: 'register' }, { action: 'get', command: 'get' }, { action: 'findByEntity', command: 'find-by-entity' }, { action: 'findByAction', command: 'find-by-action' }, { action: 'findByKind', command: 'find-by-kind' }, { action: 'findFailing', command: 'find-failing' }, { action: 'coverageReport', command: 'coverage-report' }, { action: 'untestedActions', command: 'untested-actions' }, { action: 'untestedInvariants', command: 'untested-invariants' }, { action: 'recordResult', command: 'record-result' }],
};
