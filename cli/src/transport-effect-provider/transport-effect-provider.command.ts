// Auto-generated by Clef Clef Bind — cli target
// Concept: TransportEffectProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const transportEffectProviderCommand = new Command('transport-effect-provider')
  .description('Extract the set of transport effects ( protocol : operation pairs ) from a StorageProgram s instruction tree enabling static verification that every declared transport effect has a registered EffectHandler before execution');

transportEffectProviderCommand
  .command('analyze')
  .description('Walk the instruction tree . For each perform performFrom instruction , extract the protocol : operation tag . For branch instructions , recurse into both arms and union the sets . Return all reachable transport effects as a serialized JSON array , plus the total perform count .')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TransportEffectProvider', 'analyze', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const transportEffectProviderCommandTree = {
  group: 'transport-effect-provider',
  description: 'Extract the set of transport effects ( protocol : operation pairs ) from a StorageProgram s instruction tree enabling static verification that every declared transport effect has a registered EffectHandler before execution',
  commands: [{ action: 'analyze', command: 'analyze' }],
};
