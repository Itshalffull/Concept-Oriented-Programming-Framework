// Auto-generated by Clef Clef Bind — cli target
// Concept: ScoreIndex
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const scoreIndexCommand = new Command('score-index')
  .description('Materialized index backing ScoreApi queries . Maintains denormalized views of the five Score layers ( parse , symbol , semantic , analysis , discovery ) optimized for fast LLM friendly lookups . Rebuilt incrementally as project files change . Auto registered as a built in concept in every Clef runtime');

scoreIndexCommand
  .command('upsert-concept')
  .description('Insert or update a concept entry in the index .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--purpose <purpose>', 'Purpose')
  .requiredOption('--actions <actions>', 'Actions')
  .requiredOption('--state-fields <stateFields>', 'State Fields')
  .requiredOption('--file <file>', 'File')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertConcept', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('upsert-sync')
  .description('Insert or update a sync entry in the index .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--annotation <annotation>', 'Annotation')
  .requiredOption('--triggers <triggers>', 'Triggers')
  .requiredOption('--effects <effects>', 'Effects')
  .requiredOption('--file <file>', 'File')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertSync', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('upsert-symbol')
  .description('Insert or update a symbol entry in the index .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--kind <kind>', 'Kind')
  .requiredOption('--file <file>', 'File')
  .requiredOption('--line <line>', 'Line')
  .requiredOption('--scope <scope>', 'Scope')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertSymbol', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('upsert-file')
  .description('Insert or update a file entry in the index .')
  .requiredOption('--path <path>', 'Path')
  .requiredOption('--language <language>', 'Language')
  .requiredOption('--role <role>', 'Role')
  .requiredOption('--definitions <definitions>', 'Definitions')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertFile', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('upsert-handler')
  .description('Insert or update a handler entry in the index .')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--language <language>', 'Language')
  .requiredOption('--file <file>', 'File')
  .requiredOption('--actions <actions>', 'Actions')
  .requiredOption('--line-count <lineCount>', 'Line Count')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertHandler', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('upsert-widget-impl')
  .description('Insert or update a widget implementation entry in the index .')
  .requiredOption('--widget <widget>', 'Widget')
  .requiredOption('--framework <framework>', 'Framework')
  .requiredOption('--file <file>', 'File')
  .requiredOption('--component <component>', 'Component')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertWidgetImpl', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('upsert-theme-impl')
  .description('Insert or update a theme implementation entry in the index .')
  .requiredOption('--theme <theme>', 'Theme')
  .requiredOption('--platform <platform>', 'Platform')
  .requiredOption('--file <file>', 'File')
  .requiredOption('--token-count <tokenCount>', 'Token Count')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertThemeImpl', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('upsert-deployment')
  .description('Insert or update a deployment entry in the index .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--app <app>', 'App')
  .requiredOption('--runtimes <runtimes>', 'Runtimes')
  .requiredOption('--file <file>', 'File')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertDeployment', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('upsert-suite-manifest')
  .description('Insert or update a suite manifest entry in the index .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--version <version>', 'Version')
  .requiredOption('--concepts <concepts>', 'Concepts')
  .requiredOption('--syncs <syncs>', 'Syncs')
  .requiredOption('--file <file>', 'File')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertSuiteManifest', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('upsert-interface')
  .description('Insert or update an interface entry in the index .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--targets <targets>', 'Targets')
  .requiredOption('--endpoint-count <endpointCount>', 'Endpoint Count')
  .requiredOption('--file <file>', 'File')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'upsertInterface', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('remove-by-file')
  .description('Remove all index entries associated with a file .')
  .requiredOption('--path <path>', 'Path')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'removeByFile', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('clear')
  .description('Clear all index entries . Used before a full reindex .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'clear', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreIndexCommand
  .command('stats')
  .description('Return current index statistics .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreIndex', 'stats', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const scoreIndexCommandTree = {
  group: 'score-index',
  description: 'Materialized index backing ScoreApi queries . Maintains denormalized views of the five Score layers ( parse , symbol , semantic , analysis , discovery ) optimized for fast LLM friendly lookups . Rebuilt incrementally as project files change . Auto registered as a built in concept in every Clef runtime',
  commands: [{ action: 'upsertConcept', command: 'upsert-concept' }, { action: 'upsertSync', command: 'upsert-sync' }, { action: 'upsertSymbol', command: 'upsert-symbol' }, { action: 'upsertFile', command: 'upsert-file' }, { action: 'upsertHandler', command: 'upsert-handler' }, { action: 'upsertWidgetImpl', command: 'upsert-widget-impl' }, { action: 'upsertThemeImpl', command: 'upsert-theme-impl' }, { action: 'upsertDeployment', command: 'upsert-deployment' }, { action: 'upsertSuiteManifest', command: 'upsert-suite-manifest' }, { action: 'upsertInterface', command: 'upsert-interface' }, { action: 'removeByFile', command: 'remove-by-file' }, { action: 'clear', command: 'clear' }, { action: 'stats', command: 'stats' }],
};
