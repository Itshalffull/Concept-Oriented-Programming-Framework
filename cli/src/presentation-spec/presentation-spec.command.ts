// Auto-generated by Clef Clef Bind — cli target
// Concept: PresentationSpec
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const presentationSpecCommand = new Command('presentation-spec')
  .description('Name a display strategy and carry opaque , display specific hints for rendering a view . Each presentation captures a display type ( an open , uninterpreted string such as table , card-grid , board , or any registered custom type ) , a JSON hints blob interpreted only by the matching display component , and a display mode policy that controls whether a DisplayMode system drives the render or is bypassed entirely . PresentationSpec does not enumerate or constrain which display types are valid that is the responsibility of the display component registry');

presentationSpecCommand
  .command('create')
  .description('Register a new named presentation with the given display type , opaque JSON hints , display mode policy , and default display mode . The presentation is immediately available for retrieval and composition with view and layout specs .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--display-type <displayType>', 'Display Type')
  .requiredOption('--hints <hints>', 'Hints')
  .requiredOption('--display-mode-policy <displayModePolicy>', 'Display Mode Policy')
  .requiredOption('--default-display-mode <defaultDisplayMode>', 'Default Display Mode')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PresentationSpec', 'create', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

presentationSpecCommand
  .command('get')
  .description('Return the presentation s display type , opaque hints blob , display mode policy , and default display mode exactly as registered .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PresentationSpec', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

presentationSpecCommand
  .command('list')
  .description('Return all registered presentations as a JSON array , each entry containing name , displayType , hints , displayModePolicy , and defaultDisplayMode .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PresentationSpec', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const presentationSpecCommandTree = {
  group: 'presentation-spec',
  description: 'Name a display strategy and carry opaque , display specific hints for rendering a view . Each presentation captures a display type ( an open , uninterpreted string such as table , card-grid , board , or any registered custom type ) , a JSON hints blob interpreted only by the matching display component , and a display mode policy that controls whether a DisplayMode system drives the render or is bypassed entirely . PresentationSpec does not enumerate or constrain which display types are valid that is the responsibility of the display component registry',
  commands: [{ action: 'create', command: 'create' }, { action: 'get', command: 'get' }, { action: 'list', command: 'list' }],
};
