// Auto-generated by Clef Clef Bind — cli target
// Concept: RemoteQueryProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const remoteQueryProviderCommand = new Command('remote-query-provider')
  .description('Execute query programs against external APIs by mapping query instructions to HTTP calls via Projection and FieldTransform . Handles planPushdown by identifying which predicates the remote API supports natively ( via query params or request body ) and which must run as residual in memory filtering after the response');

remoteQueryProviderCommand
  .command('register')
  .description('Register a remote query provider for an external API source . pushdownOps is a JSON array of instruction types the remote API can handle natively ( e . g . , [ filter , sort , limit ] for APIs with query param support ) .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--source <source>', 'Source')
  .requiredOption('--manifest-ref <manifestRef>', 'Manifest Ref')
  .requiredOption('--projection-ref <projectionRef>', 'Projection Ref')
  .requiredOption('--pushdown-ops <pushdownOps>', 'Pushdown Ops')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RemoteQueryProvider', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

remoteQueryProviderCommand
  .command('execute')
  .description('Execute a query program against the remote API : 1 . planPushdown : split program into pushdown residual 2 . Map pushdown instructions to API params via FieldTransform 3 . Resolve HTTP call via Projection resolveForward 4 . Execute HTTP call via perform ( http , ... ) 5 . Transform response via FieldTransform 6 . Apply residual instructions in memory 7 . Return combined results')
  .requiredOption('--provider <provider>', 'Provider')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RemoteQueryProvider', 'execute', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

remoteQueryProviderCommand
  .command('plan-pushdown')
  .description('Split a query program into pushdown ( handled by API ) and residual ( handled in memory ) . Also returns the mapped API params ( query string params , request body fields ) that the pushdown instructions translate to .')
  .requiredOption('--provider <provider>', 'Provider')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RemoteQueryProvider', 'planPushdown', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

remoteQueryProviderCommand
  .command('get')
  .description('Return the full provider configuration .')
  .requiredOption('--provider <provider>', 'Provider')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RemoteQueryProvider', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

remoteQueryProviderCommand
  .command('list')
  .description('Return all registered remote providers .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RemoteQueryProvider', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const remoteQueryProviderCommandTree = {
  group: 'remote-query-provider',
  description: 'Execute query programs against external APIs by mapping query instructions to HTTP calls via Projection and FieldTransform . Handles planPushdown by identifying which predicates the remote API supports natively ( via query params or request body ) and which must run as residual in memory filtering after the response',
  commands: [{ action: 'register', command: 'register' }, { action: 'execute', command: 'execute' }, { action: 'planPushdown', command: 'plan-pushdown' }, { action: 'get', command: 'get' }, { action: 'list', command: 'list' }],
};
