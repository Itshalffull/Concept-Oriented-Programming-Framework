// Auto-generated by Clef Clef Bind — cli target
// Concept: Generator
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const generatorCommand = new Command('generator')
  .description('Orchestrate multi target interface generation from concept projections . Plans which targets and SDK languages to generate and coordinates the generation pipeline . History tracking is handled by GenerationPlan ; file output by Emitter');

generatorCommand
  .command('plan')
  .description('Parse the interface manifest . Resolve which targets , SDK languages , and spec formats are configured . Validate that required provider concepts are loaded . Estimate output file count .')
  .requiredOption('--suite <suite>', 'Suite')
  .requiredOption('--interface-manifest <interfaceManifest>', 'Interface Manifest')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Generator', 'plan', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

generatorCommand
  .command('generate')
  .description('All configured targets generated successfully . Unchanged files ( content addressed ) were skipped .')
  .requiredOption('--plan <plan>', 'Plan')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Generator', 'generate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

generatorCommand
  .command('regenerate')
  .description('Regenerate only the specified targets . Uses cached projections if concept specs have not changed .')
  .requiredOption('--plan <plan>', 'Plan')
  .requiredOption('--targets <targets>', 'Targets')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Generator', 'regenerate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const generatorCommandTree = {
  group: 'generator',
  description: 'Orchestrate multi target interface generation from concept projections . Plans which targets and SDK languages to generate and coordinates the generation pipeline . History tracking is handled by GenerationPlan ; file output by Emitter',
  commands: [{ action: 'plan', command: 'plan' }, { action: 'generate', command: 'generate' }, { action: 'regenerate', command: 'regenerate' }],
};
