// Auto-generated by Clef Clef Bind — cli target
// Concept: EffectHandler
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const effectHandlerCommand = new Command('effect-handler')
  .description('Register and resolve protocol level effect handlers that the interpreter uses to execute abstract perform instructions the bridge between a handler s declared transport effects and concrete transport implementations');

effectHandlerCommand
  .command('register')
  .description('Register a handler for the given protocol : operation pair , making it available for the interpreter to resolve perform instructions at execution time .')
  .requiredOption('--protocol <protocol>', 'Protocol')
  .requiredOption('--operation <operation>', 'Operation')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/EffectHandler', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

effectHandlerCommand
  .command('resolve')
  .description('Return the registered handler for this protocol : operation pair , confirming the effect can be interpreted .')
  .requiredOption('--protocol <protocol>', 'Protocol')
  .requiredOption('--operation <operation>', 'Operation')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/EffectHandler', 'resolve', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

effectHandlerCommand
  .command('list-by-protocol')
  .description('Return all registered handlers for the given protocol as a serialized JSON array of handler objects .')
  .requiredOption('--protocol <protocol>', 'Protocol')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/EffectHandler', 'listByProtocol', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

effectHandlerCommand
  .command('deregister')
  .description('Remove the handler registration for this protocol : operation pair .')
  .requiredOption('--protocol <protocol>', 'Protocol')
  .requiredOption('--operation <operation>', 'Operation')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/EffectHandler', 'deregister', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const effectHandlerCommandTree = {
  group: 'effect-handler',
  description: 'Register and resolve protocol level effect handlers that the interpreter uses to execute abstract perform instructions the bridge between a handler s declared transport effects and concrete transport implementations',
  commands: [{ action: 'register', command: 'register' }, { action: 'resolve', command: 'resolve' }, { action: 'listByProtocol', command: 'list-by-protocol' }, { action: 'deregister', command: 'deregister' }],
};
