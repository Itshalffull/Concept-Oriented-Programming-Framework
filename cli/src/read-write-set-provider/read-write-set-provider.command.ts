// Auto-generated by Clef Clef Bind — cli target
// Concept: ReadWriteSetProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const readWriteSetProviderCommand = new Command('read-write-set-provider')
  .description('Extract the set of storage relations a program reads and writes by walking its instruction sequence , and classify the program s purity as pure , read only , or read write');

readWriteSetProviderCommand
  .command('analyze')
  .description('Walk the instruction tree . Get and Find instructions contribute to the readSet . Put and Del instructions contribute to the writeSet . Branch nodes union both sub branches conservatively . Purity : no storage ops is pure , only reads is read only , any write is read write .')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ReadWriteSetProvider', 'analyze', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const readWriteSetProviderCommandTree = {
  group: 'read-write-set-provider',
  description: 'Extract the set of storage relations a program reads and writes by walking its instruction sequence , and classify the program s purity as pure , read only , or read write',
  commands: [{ action: 'analyze', command: 'analyze' }],
};
