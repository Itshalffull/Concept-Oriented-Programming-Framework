// Auto-generated by Clef Clef Bind — cli target
// Concept: EnrichmentRenderer
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const enrichmentRendererCommand = new Command('enrichment-renderer')
  .description('Render opaque enrichment JSON into formatted output strings using data driven templates . Handlers are declarative each maps an enrichment key to a built in render pattern ( list , checklist , code list , callout , heading body , bad good , etc . ) plus a template config with { { field } } interpolation . Patterns are the small code surface shipped once ; handlers are pure data that can live in YAML manifests . New enrichment kinds need only a YAML entry zero code changes . Renamed from Renderer to avoid collision with suites presentation renderer . concept ( cache aware rendering pipeline ) . See Architecture doc Section 1.8');

enrichmentRendererCommand
  .command('register')
  .description('Register a handler for an enrichment key in a given format . Order determines rendering position lower numbers render first . Pattern names a built in render pattern ( list , checklist , code list , link list , callout , heading body , bad good , scaffold list , slash list , keyed checklist , inline list ) . Template is a JSON config for the pattern with { { field } } interpolation placeholders . If a handler already exists for the ( key , format ) pair , it is replaced .')
  .requiredOption('--key <key>', 'Key')
  .requiredOption('--format <format>', 'Format')
  .requiredOption('--order <order>', 'Order')
  .requiredOption('--pattern <pattern>', 'Pattern')
  .requiredOption('--template <template>', 'Template')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/EnrichmentRenderer', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

enrichmentRendererCommand
  .command('render')
  .description('Render enrichment content in the given format . Parses the content JSON , walks top level keys , dispatches each to the registered handler s pattern with its template config . Keys without a handler are collected in unhandledKeys for transparency . Output sections are ordered by handler order .')
  .requiredOption('--content <content>', 'Content')
  .requiredOption('--format <format>', 'Format')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/EnrichmentRenderer', 'render', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

enrichmentRendererCommand
  .command('list-handlers')
  .description('Handler keys for the format , in render order .')
  .requiredOption('--format <format>', 'Format')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/EnrichmentRenderer', 'listHandlers', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

enrichmentRendererCommand
  .command('list-patterns')
  .description('List all available render patterns .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/EnrichmentRenderer', 'listPatterns', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const enrichmentRendererCommandTree = {
  group: 'enrichment-renderer',
  description: 'Render opaque enrichment JSON into formatted output strings using data driven templates . Handlers are declarative each maps an enrichment key to a built in render pattern ( list , checklist , code list , callout , heading body , bad good , etc . ) plus a template config with { { field } } interpolation . Patterns are the small code surface shipped once ; handlers are pure data that can live in YAML manifests . New enrichment kinds need only a YAML entry zero code changes . Renamed from Renderer to avoid collision with suites presentation renderer . concept ( cache aware rendering pipeline ) . See Architecture doc Section 1.8',
  commands: [{ action: 'register', command: 'register' }, { action: 'render', command: 'render' }, { action: 'listHandlers', command: 'list-handlers' }, { action: 'listPatterns', command: 'list-patterns' }],
};
