// Auto-generated by Clef Clef Bind — cli target
// Concept: VariantEntity
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const variantEntityCommand = new Command('variant-entity')
  .description('Action return variant as a first class branching point in 
 sync chains . Enables dead variant detection and sync coverage 
 analysis identifying variants that no sync pattern matches on .');

variantEntityCommand
  .command('register')
  .description('Register a variant extracted from an action in a concept 
 spec . Fields is serialized JSON array of { name , typeExpr } .')
  .requiredOption('--action <action>', 'Action')
  .requiredOption('--tag <tag>', 'Tag')
  .requiredOption('--fields <fields>', 'Fields')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'register', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

variantEntityCommand
  .command('matching-syncs')
  .description('Return all syncs whose when pattern matches this variant s 
 tag . Results as serialized JSON array .')
  .requiredOption('--variant <variant>', 'Variant')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'matchingSyncs', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

variantEntityCommand
  .command('is-dead')
  .description('This variant has no sync matching it and or no runtime 
 occurrences . Both values are true or false .')
  .requiredOption('--variant <variant>', 'Variant')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'isDead', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

variantEntityCommand
  .command('get')
  .description('Retrieve variant entity metadata .')
  .requiredOption('--variant <variant>', 'Variant')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'get', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

export const variantEntityCommandTree = {
  group: 'variant-entity',
  description: 'Action return variant as a first class branching point in 
 sync chains . Enables dead variant detection and sync coverage 
 analysis identifying variants that no sync pattern matches on .',
  commands: [{ action: 'register', command: 'register' }, { action: 'matchingSyncs', command: 'matching-syncs' }, { action: 'isDead', command: 'is-dead' }, { action: 'get', command: 'get' }],
};
