// Auto-generated by Clef Clef Bind — cli target
// Concept: FunctionalHandler
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const functionalHandlerCommand = new Command('functional-handler')
  .description('Register concept handlers that produce StoragePrograms instead of directly executing storage effects . A FunctionalHandler is the migration point between imperative and monadic handler models');

functionalHandlerCommand
  .command('register')
  .description('Register a functional handler for the given concept and action , declaring its purity level ( pure , read only , or read write ) and the set of completion variant tags ( serialized JSON array ) that the handler may produce .')
  .requiredOption('--handler <handler>', 'Handler')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--action <action>', 'Action')
  .requiredOption('--purity <purity>', 'Purity')
  .requiredOption('--variants <variants>', 'Variants')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FunctionalHandler', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

functionalHandlerCommand
  .command('build')
  .description('Invoke the handler s factory function with the given input and return the resulting StorageProgram as serialized data . No storage side effects occur during build .')
  .requiredOption('--handler <handler>', 'Handler')
  .requiredOption('--input <input>', 'Input')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FunctionalHandler', 'build', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

functionalHandlerCommand
  .command('list')
  .description('Return all registered functional handlers for the named concept , including their action names and declared purity levels .')
  .requiredOption('--concept <concept>', 'Concept')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FunctionalHandler', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

functionalHandlerCommand
  .command('validate-purity')
  .description('The program s structural effects are consistent with the handler s declared purity level . The declared purity is at least as permissive as the actual effects .')
  .requiredOption('--handler <handler>', 'Handler')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FunctionalHandler', 'validatePurity', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const functionalHandlerCommandTree = {
  group: 'functional-handler',
  description: 'Register concept handlers that produce StoragePrograms instead of directly executing storage effects . A FunctionalHandler is the migration point between imperative and monadic handler models',
  commands: [{ action: 'register', command: 'register' }, { action: 'build', command: 'build' }, { action: 'list', command: 'list' }, { action: 'validatePurity', command: 'validate-purity' }],
};
