// Auto-generated by Clef Clef Bind — cli target
// Concept: IntegrationTestGen
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const integrationTestGenCommand = new Command('generate-integration-test')
  .description('Generate a ProcessSpec integration test from fixture chains and scenario invariants.');

integrationTestGenCommand
  .command('integration-test')
  .description('Generate a ProcessSpec from a concept spec and ingest manifest.')
  .requiredOption('--concept-spec <conceptSpec>', 'Concept Spec')
  .requiredOption('--ingest-manifest <ingestManifest>', 'Ingest Manifest')
  .requiredOption('-s, --source <source>', 'Source name')
  .option('--json', 'Output as JSON')
  .addHelpText('after', '\nExamples:')
  .addHelpText('after', '  clef generate integration-test --concept-spec ./specs/app/charge.concept --ingest-manifest ./ingest.json --source payments  # Generate an integration ProcessSpec for Charge')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/IntegrationTestGen', 'generate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

integrationTestGenCommand
  .command('run')
  .description('Run the generated ProcessSpec against the live external API.')
  .requiredOption('--process-spec <processSpec>', 'Process Spec')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/IntegrationTestGen', 'run', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

integrationTestGenCommand
  .command('preview')
  .description('List the ordered steps that would be emitted, with expected variants.')
  .requiredOption('--concept-spec <conceptSpec>', 'Concept Spec')
  .requiredOption('-s, --source <source>', 'Source name under which steps will run')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/IntegrationTestGen', 'preview', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

integrationTestGenCommand
  .command('list')
  .description('Return all stored generation records ordered by generatedAt descending . Each record includes the concept name , source path , step count , and generation timestamp .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/IntegrationTestGen', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

integrationTestGenCommand
  .command('register')
  .description('Return static metadata for PluginRegistry . name : IntegrationTestGen inputKind : ConceptSpec outputKind : ProcessSpec capabilities : [ fixture-walk , after-chain , check-verification , external-call ]')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/IntegrationTestGen', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const integrationTestGenCommandTree = {
  group: 'generate-integration-test',
  description: 'Generate a ProcessSpec integration test from fixture chains and scenario invariants.',
  commands: [{ action: 'generate', command: 'integration-test' }, { action: 'run', command: 'run' }, { action: 'preview', command: 'preview' }, { action: 'list', command: 'list' }, { action: 'register', command: 'register' }],
};
