// Auto-generated by Clef Clef Bind — cli target
// Concept: Credential
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const credentialCommand = new Command('credential')
  .description('Manage named client side connection profiles , each binding a kernel endpoint to stored authentication credentials . Supports multiple profiles for different environments and multiple auth methods , enabling users and agents to switch between connections without re entering credentials each time');

credentialCommand
  .command('create')
  .description('Add the profile to the profiles set with the given name , endpoint , and method . Set status to unset with no token stored .')
  .requiredOption('--profile <profile>', 'Profile')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--endpoint <endpoint>', 'Endpoint')
  .requiredOption('--method <method>', 'Method')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Credential', 'create', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

credentialCommand
  .command('store')
  .description('Save the token and optional expiration timestamp for the profile . Set status to valid .')
  .requiredOption('--profile <profile>', 'Profile')
  .requiredOption('--token <token>', 'Token')
  .requiredOption('--expires-at <expiresAt>', 'Expires At')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Credential', 'store', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

credentialCommand
  .command('load')
  .description('Return the stored token and endpoint for the profile , ready to pass to a connection action .')
  .requiredOption('--profile <profile>', 'Profile')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Credential', 'load', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

credentialCommand
  .command('refresh')
  .description('Replace the stored token and expiresAt with fresh values from the auth provider . Set status to valid .')
  .requiredOption('--profile <profile>', 'Profile')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Credential', 'refresh', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

credentialCommand
  .command('remove')
  .description('Delete the profile and all associated credentials from the store .')
  .requiredOption('--profile <profile>', 'Profile')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Credential', 'remove', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

credentialCommand
  .command('list')
  .description('Return a summary of all profiles name , endpoint , method , and status without exposing any stored token values .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Credential', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const credentialCommandTree = {
  group: 'credential',
  description: 'Manage named client side connection profiles , each binding a kernel endpoint to stored authentication credentials . Supports multiple profiles for different environments and multiple auth methods , enabling users and agents to switch between connections without re entering credentials each time',
  commands: [{ action: 'create', command: 'create' }, { action: 'store', command: 'store' }, { action: 'load', command: 'load' }, { action: 'refresh', command: 'refresh' }, { action: 'remove', command: 'remove' }, { action: 'list', command: 'list' }],
};
