// Auto-generated by Clef Clef Bind — cli target
// Concept: RenderTransform
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const renderTransformCommand = new Command('render-transform')
  .description('Registry and dispatcher for composable render program transformations . Manages named transforms with kind based routing to provider concepts through sync wiring the functorial mapping hub that makes theme switching , a11y variants , and style adaptations composable first class operations without hardcoding transform logic');

renderTransformCommand
  .command('register-kind')
  .description('Register a transform kind provider . Called by provider registration syncs to declare that a provider exists for this kind ( token remap , a11y adapt , bind rewrite , custom ) .')
  .requiredOption('--kind <kind>', 'Kind')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RenderTransform', 'registerKind', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

renderTransformCommand
  .command('register')
  .description('Register a named transform with a kind and a serialized rewrite specification . The spec structure is defined by the provider for that kind .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--kind <kind>', 'Kind')
  .requiredOption('--spec <spec>', 'Spec')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RenderTransform', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

renderTransformCommand
  .command('apply')
  .description('Dispatch the transform application to the appropriate provider based on kind . The provider applies the spec to the program and returns the transformed result . Satisfies the functor identity law : apply ( p , kind , emptySpec ) = p .')
  .requiredOption('--program <program>', 'Program')
  .requiredOption('--kind <kind>', 'Kind')
  .requiredOption('--spec <spec>', 'Spec')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RenderTransform', 'apply', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

renderTransformCommand
  .command('compose')
  .description('Combine multiple named transforms into a single composite transform . The result applies all transforms in sequence . Satisfies the functor composition law : apply ( p , compose ( [ f , g ] ) ) = apply ( apply ( p , f ) , g ) .')
  .requiredOption('--transforms <transforms>', 'Transforms')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RenderTransform', 'compose', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

renderTransformCommand
  .command('list')
  .description('Return all registered transforms as a serialized JSON array of { name , kind } objects .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RenderTransform', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

renderTransformCommand
  .command('get')
  .description('Return the full details of a registered transform .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RenderTransform', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const renderTransformCommandTree = {
  group: 'render-transform',
  description: 'Registry and dispatcher for composable render program transformations . Manages named transforms with kind based routing to provider concepts through sync wiring the functorial mapping hub that makes theme switching , a11y variants , and style adaptations composable first class operations without hardcoding transform logic',
  commands: [{ action: 'registerKind', command: 'register-kind' }, { action: 'register', command: 'register' }, { action: 'apply', command: 'apply' }, { action: 'compose', command: 'compose' }, { action: 'list', command: 'list' }, { action: 'get', command: 'get' }],
};
