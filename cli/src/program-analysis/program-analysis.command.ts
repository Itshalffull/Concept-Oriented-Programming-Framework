// Auto-generated by Clef Clef Bind — cli target
// Concept: ProgramAnalysis
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const programAnalysisCommand = new Command('program-analysis')
  .description('Dispatch named analysis queries against StoragePrograms to registered providers and return structured results . Each provider implements a specific analysis strategy ; this concept provides the uniform dispatch and result tracking interface');

programAnalysisCommand
  .command('register-provider')
  .description('Analysis provider registered under the given name and kind classification .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--kind <kind>', 'Kind')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProgramAnalysis', 'registerProvider', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

programAnalysisCommand
  .command('run')
  .description('Dispatch the analysis to the named provider and return the structured result . The result format depends on the provider .')
  .requiredOption('--program <program>', 'Program')
  .requiredOption('--provider <provider>', 'Provider')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProgramAnalysis', 'run', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

programAnalysisCommand
  .command('run-all')
  .description('Run every registered provider against the program and return all results keyed by provider name .')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProgramAnalysis', 'runAll', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

programAnalysisCommand
  .command('list-providers')
  .description('Return all registered provider names and their kind classifications .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProgramAnalysis', 'listProviders', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const programAnalysisCommandTree = {
  group: 'program-analysis',
  description: 'Dispatch named analysis queries against StoragePrograms to registered providers and return structured results . Each provider implements a specific analysis strategy ; this concept provides the uniform dispatch and result tracking interface',
  commands: [{ action: 'registerProvider', command: 'register-provider' }, { action: 'run', command: 'run' }, { action: 'runAll', command: 'run-all' }, { action: 'listProviders', command: 'list-providers' }],
};
