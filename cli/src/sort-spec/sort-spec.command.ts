// Auto-generated by Clef Clef Bind — cli target
// Concept: SortSpec
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const sortSpecCommand = new Command('sort-spec')
  .description('Manage named , ordered sequences of sort keys that determine record ordering in a view . Each sort spec captures one or more sort keys ( field , direction , null handling , collation ) as a serialized JSON array . Sort specs can be composed to produce a combined ordering where the secondary spec acts as a tiebreaker , and can be evaluated directly against a JSON row array to return the rows in sorted order');

sortSpecCommand
  .command('create')
  .description('Register a new named sort spec with the given key array . The keys string is a JSON serialized SortKey [ ] ( e . g . [ { field : node , direction : asc } ] ) . An empty array [ ] is valid and represents natural ( identity ) ordering .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--keys <keys>', 'Keys')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SortSpec', 'create', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

sortSpecCommand
  .command('get')
  .description('Return the sort spec identifier and its serialized key array for the given name .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SortSpec', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

sortSpecCommand
  .command('compose')
  .description('Concatenate the key arrays of the primary and secondary sort specs , producing a new sort spec whose name is primary+secondary . The primary keys appear first ; the secondary keys serve as a tiebreaker . This operation is not commutative : compose ( a , b ) differs from compose ( b , a ) . Composing any sort with an identity sort ( keys : [ ] ) leaves the keys unchanged .')
  .requiredOption('--primary <primary>', 'Primary')
  .requiredOption('--secondary <secondary>', 'Secondary')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SortSpec', 'compose', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

sortSpecCommand
  .command('evaluate')
  .description('Apply the named sort spec s keys to the JSON serialized row array , returning a stable sorted JSON array . Rows with the same key values preserve their original relative order .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--rows <rows>', 'Rows')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SortSpec', 'evaluate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

sortSpecCommand
  .command('list')
  .description('Return a JSON serialized array of all registered sort spec names .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SortSpec', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const sortSpecCommandTree = {
  group: 'sort-spec',
  description: 'Manage named , ordered sequences of sort keys that determine record ordering in a view . Each sort spec captures one or more sort keys ( field , direction , null handling , collation ) as a serialized JSON array . Sort specs can be composed to produce a combined ordering where the secondary spec acts as a tiebreaker , and can be evaluated directly against a JSON row array to return the rows in sorted order',
  commands: [{ action: 'create', command: 'create' }, { action: 'get', command: 'get' }, { action: 'compose', command: 'compose' }, { action: 'evaluate', command: 'evaluate' }, { action: 'list', command: 'list' }],
};
