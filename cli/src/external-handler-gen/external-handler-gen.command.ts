// Auto-generated by Clef Clef Bind — cli target
// Concept: ExternalHandlerGen
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const externalHandlerGenCommand = new Command('generate-external-handler')
  .description('Generate a handler that calls an external REST API from an ingest manifest.');

externalHandlerGenCommand
  .command('external-handler')
  .description('Generate a handler for one concept declared in the ingest manifest.')
  .requiredOption('-m, --manifest <manifest>', 'Path to ingest manifest JSON')
  .requiredOption('-s, --source <source>', 'Source name')
  .requiredOption('-c, --concept <concept>', 'Concept name')
  .option('--json', 'Output as JSON')
  .addHelpText('after', '\nExamples:')
  .addHelpText('after', '  clef generate external-handler --manifest ./ingest.json --source payments --concept Charge  # Generate a Charge handler from a payments source')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ExternalHandlerGen', 'generate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

externalHandlerGenCommand
  .command('generate-all')
  .description('Generate handlers for all concepts declared across all sources in the manifest . Iterates every source entry , then every concept within each source , and calls the same generation logic as generate . The returned handlers string is a newline delimited concatenation of all generated handler files , each preceded by a comment header identifying its source and concept . count is the total number of concept handlers emitted .')
  .requiredOption('--manifest <manifest>', 'Manifest')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ExternalHandlerGen', 'generateAll', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

externalHandlerGenCommand
  .command('preview')
  .description('Dry-run: return the handler source that would be generated without writing.')
  .requiredOption('-m, --manifest <manifest>', 'Path to ingest manifest JSON')
  .requiredOption('-s, --source <source>', 'Source name (must match a manifest entry)')
  .requiredOption('-c, --concept <concept>', 'Concept name under the source')
  .option('-a, --action <action>', 'Specific action to preview (defaults to all)')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ExternalHandlerGen', 'preview', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

externalHandlerGenCommand
  .command('list')
  .description('Return all recorded generation entries , each carrying its source , concept , and generatedAt timestamp .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ExternalHandlerGen', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

externalHandlerGenCommand
  .command('register')
  .description('Return static metadata for PluginRegistry . name : ExternalHandlerGen inputKind : IngestManifest outputKind : HandlerImpl capabilities : [ http-perform , field-transform , auth-headers , multi-source ]')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ExternalHandlerGen', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const externalHandlerGenCommandTree = {
  group: 'generate-external-handler',
  description: 'Generate a handler that calls an external REST API from an ingest manifest.',
  commands: [{ action: 'generate', command: 'external-handler' }, { action: 'generateAll', command: 'generate-all' }, { action: 'preview', command: 'preview' }, { action: 'list', command: 'list' }, { action: 'register', command: 'register' }],
};
