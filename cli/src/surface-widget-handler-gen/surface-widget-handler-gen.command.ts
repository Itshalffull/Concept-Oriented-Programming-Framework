// Auto-generated by Clef Clef Bind — cli target
// Concept: SurfaceWidgetHandlerGen
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const surfaceWidgetHandlerGenCommand = new Command('surface-widget-handler-gen')
  .description('Generate functional StorageProgram handlers for Clef Surface widgets . Takes a parsed WidgetManifest and produces a handler that builds RenderPrograms from widget specs , dispatches to framework specific interpreters , and integrates with WidgetComponentTest for automated conformance testing');

surfaceWidgetHandlerGenCommand
  .command('generate')
  .description('Parse widget manifest , generate functional handler with RenderProgram construction and interpreter dispatch . Also generate component conformance tests via WidgetComponentTest .')
  .requiredOption('--widget-ref <widget_ref>', 'Widget_ref')
  .requiredOption('--widget-data <widget_data>', 'Widget_data')
  .requiredOption('--output-dir <output_dir>', 'Output_dir')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SurfaceWidgetHandlerGen', 'generate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

surfaceWidgetHandlerGenCommand
  .command('list-targets')
  .description('Return available interpreter targets for widget code generation .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/SurfaceWidgetHandlerGen', 'listTargets', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const surfaceWidgetHandlerGenCommandTree = {
  group: 'surface-widget-handler-gen',
  description: 'Generate functional StorageProgram handlers for Clef Surface widgets . Takes a parsed WidgetManifest and produces a handler that builds RenderPrograms from widget specs , dispatches to framework specific interpreters , and integrates with WidgetComponentTest for automated conformance testing',
  commands: [{ action: 'generate', command: 'generate' }, { action: 'listTargets', command: 'list-targets' }],
};
