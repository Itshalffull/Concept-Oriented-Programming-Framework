// Auto-generated by Clef Clef Bind — cli target
// Concept: ActionEntity
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const actionEntityCommand = new Command('action-entity')
  .description('Action declaration with full lifecycle tracing from spec 
 through sync participation , implementation , interface exposure , 
 to runtime invocation . Enables queries like what syncs trigger
    on this action? and where is this action implemented?');

actionEntityCommand
  .command('register')
  .description('Register an action extracted from a concept spec . 
 Params and variantRefs are serialized JSON .')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--params <params>', 'Params')
  .requiredOption('--variant-refs <variantRefs>', 'Variant Refs')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'register', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

actionEntityCommand
  .command('find-by-concept')
  .description('Return all actions for a given concept . 
 Results as serialized JSON array .')
  .requiredOption('--concept <concept>', 'Concept')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'findByConcept', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

actionEntityCommand
  .command('triggering-syncs')
  .description('Return all syncs whose when clause matches this action . 
 Results as serialized JSON array .')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'triggeringSyncs', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

actionEntityCommand
  .command('invoking-syncs')
  .description('Return all syncs whose then clause invokes this action . 
 Results as serialized JSON array .')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'invokingSyncs', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

actionEntityCommand
  .command('implementations')
  .description('Return implementation symbols ( handler functions ) for 
 this action across target languages . 
 Results as serialized JSON array .')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'implementations', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

actionEntityCommand
  .command('interface-exposures')
  .description('Return all interface exposures for this action 
 REST endpoints , CLI commands , GraphQL mutations , etc . 
 Results as serialized JSON array .')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'interfaceExposures', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

actionEntityCommand
  .command('get')
  .description('Retrieve action entity metadata .')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'get', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

export const actionEntityCommandTree = {
  group: 'action-entity',
  description: 'Action declaration with full lifecycle tracing from spec 
 through sync participation , implementation , interface exposure , 
 to runtime invocation . Enables queries like what syncs trigger
    on this action? and where is this action implemented?',
  commands: [{ action: 'register', command: 'register' }, { action: 'findByConcept', command: 'find-by-concept' }, { action: 'triggeringSyncs', command: 'triggering-syncs' }, { action: 'invokingSyncs', command: 'invoking-syncs' }, { action: 'implementations', command: 'implementations' }, { action: 'interfaceExposures', command: 'interface-exposures' }, { action: 'get', command: 'get' }],
};
