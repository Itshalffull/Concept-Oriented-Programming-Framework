// Auto-generated by Clef Clef Bind — cli target
// Concept: TokenRemapProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const tokenRemapProviderCommand = new Command('token-remap-provider')
  .description('Rewrite theme token path references in RenderProgram instruction sequences mapping old token paths to new ones so that theme switching becomes a pure instruction rewrite over the program');

tokenRemapProviderCommand
  .command('register')
  .description('Return static metadata for PluginRegistry . name : TokenRemapProvider kind : token-remap capabilities : [ token-path-rewrite , theme-switching ]')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TokenRemapProvider', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

tokenRemapProviderCommand
  .command('apply')
  .description('Walk the instruction sequence and rewrite token path references per the mappings in spec . Instructions with tag token have their path field remapped if it appears in the mapping . Non token instructions pass through unchanged . Returns the new serialized program and the updated list of applied transforms .')
  .requiredOption('--program <program>', 'Program')
  .requiredOption('--spec <spec>', 'Spec')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TokenRemapProvider', 'apply', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const tokenRemapProviderCommandTree = {
  group: 'token-remap-provider',
  description: 'Rewrite theme token path references in RenderProgram instruction sequences mapping old token paths to new ones so that theme switching becomes a pure instruction rewrite over the program',
  commands: [{ action: 'register', command: 'register' }, { action: 'apply', command: 'apply' }],
};
