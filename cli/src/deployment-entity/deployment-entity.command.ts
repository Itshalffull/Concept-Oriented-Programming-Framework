// Auto-generated by Clef Clef Bind — cli target
// Concept: DeploymentEntity
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const deploymentEntityCommand = new Command('deployment-entity')
  .description('Queryable representation of a parsed deployment manifest ( deploy . yaml ) 
 the runtime topology , concept to runtime assignments , storage adapter 
 bindings , transport adapter bindings , sync engine configurations , and 
 environment overlays . Enables queries like which runtime hosts User? , 
 what storage backend does Article use? , what transport connects
    api to worker? , and show me the full deployment topology.');

deploymentEntityCommand
  .command('register')
  .description('Register a parsed deployment manifest . Extracts runtimes , 
 concept assignments , storage transport bindings , sync engine 
 configurations , environment overlays , and IaC provider from 
 the manifest .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--source <source>', 'Source')
  .requiredOption('--manifest <manifest>', 'Manifest')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'register', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentEntityCommand
  .command('get')
  .description('Retrieve a deployment entity by name .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'get', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentEntityCommand
  .command('list-runtimes')
  .description('Return all runtimes in this deployment with their type , 
 transport , and storage config . Result as serialized JSON 
 array of { name , type , transport , storage , concepts : [ ] } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'listRuntimes', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentEntityCommand
  .command('find-concept-runtime')
  .description('Find which runtime a concept is assigned to and what 
 transport storage it uses .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .requiredOption('--concept <concept>', 'Concept')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'findConceptRuntime', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentEntityCommand
  .command('find-sync-engine')
  .description('Find which sync engine and runtime a sync is assigned to .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .requiredOption('--sync <sync>', 'Sync')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'findSyncEngine', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentEntityCommand
  .command('topology')
  .description('Return the full deployment topology as a graph showing 
 runtimes , their concepts , transports between them , and 
 storage backends . Result as serialized JSON { nodes : 
 [ { id , kind , label } ] , edges : [ { from , to , transport } ] } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'topology', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentEntityCommand
  .command('transport-routes')
  .description('Trace the transport path between two concepts through 
 which runtimes and transports does a sync fire travel 
 Result as serialized JSON array of { hop , runtime , 
 transport , latencyEstimate } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .requiredOption('--from-concept <fromConcept>', 'From Concept')
  .requiredOption('--to-concept <toConcept>', 'To Concept')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'transportRoutes', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentEntityCommand
  .command('storage-topology')
  .description('Return all concept to storage bindings . Result as serialized 
 JSON array of { concept , runtime , storageBackend , config } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'storageTopology', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentEntityCommand
  .command('environment-diff')
  .description('Compare two deployments ( e . g . , staging vs production ) . 
 Result as serialized JSON array of { path , aValue , bValue } .')
  .requiredOption('--deployment-a <deploymentA>', 'Deployment A')
  .requiredOption('--deployment-b <deploymentB>', 'Deployment B')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'environmentDiff', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentEntityCommand
  .command('validate-against-specs')
  .description('Validate that all concepts referenced in the deployment 
 exist as ConceptEntities , all syncs exist as SyncEntities , 
 and all storage transport capabilities are satisfied .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'validateAgainstSpecs', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

export const deploymentEntityCommandTree = {
  group: 'deployment-entity',
  description: 'Queryable representation of a parsed deployment manifest ( deploy . yaml ) 
 the runtime topology , concept to runtime assignments , storage adapter 
 bindings , transport adapter bindings , sync engine configurations , and 
 environment overlays . Enables queries like which runtime hosts User? , 
 what storage backend does Article use? , what transport connects
    api to worker? , and show me the full deployment topology.',
  commands: [{ action: 'register', command: 'register' }, { action: 'get', command: 'get' }, { action: 'listRuntimes', command: 'list-runtimes' }, { action: 'findConceptRuntime', command: 'find-concept-runtime' }, { action: 'findSyncEngine', command: 'find-sync-engine' }, { action: 'topology', command: 'topology' }, { action: 'transportRoutes', command: 'transport-routes' }, { action: 'storageTopology', command: 'storage-topology' }, { action: 'environmentDiff', command: 'environment-diff' }, { action: 'validateAgainstSpecs', command: 'validate-against-specs' }],
};
