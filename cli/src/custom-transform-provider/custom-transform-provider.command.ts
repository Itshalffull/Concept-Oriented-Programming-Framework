// Auto-generated by Clef Clef Bind — cli target
// Concept: CustomTransformProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const customTransformProviderCommand = new Command('custom-transform-provider')
  .description('Apply generic pattern match and replace transformations to RenderProgram instruction sequences matching arbitrary instruction field patterns and merging replacement fields , serving as the extensibility escape hatch for transform kinds not covered by specialized providers');

customTransformProviderCommand
  .command('register')
  .description('Return static metadata for PluginRegistry . name : CustomTransformProvider kind : custom capabilities : [ pattern-match , field-replace , generic-transform ]')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/CustomTransformProvider', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

customTransformProviderCommand
  .command('apply')
  .description('Walk the instruction sequence . For each instruction matching all fields in the match pattern , merge the replace fields into the instruction . Non matching instructions pass through unchanged . Returns the new serialized program and the updated list of applied transforms .')
  .requiredOption('--program <program>', 'Program')
  .requiredOption('--spec <spec>', 'Spec')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/CustomTransformProvider', 'apply', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const customTransformProviderCommandTree = {
  group: 'custom-transform-provider',
  description: 'Apply generic pattern match and replace transformations to RenderProgram instruction sequences matching arbitrary instruction field patterns and merging replacement fields , serving as the extensibility escape hatch for transform kinds not covered by specialized providers',
  commands: [{ action: 'register', command: 'register' }, { action: 'apply', command: 'apply' }],
};
