// Auto-generated by Clef Clef Bind — cli target
// Concept: Lens
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const lensCommand = new Command('lens')
  .description('Typed , composable reference to a location within concept state , replacing untyped string based storage access with first class optics that can be composed into paths , decomposed into segments , and validated against concept state declarations');

lensCommand
  .command('create')
  .description('Construct a lens from up to three path segments : relation ( required ) , key ( optional focus on a specific record ) , and field ( optional focus on a field within that record ) . The kind is inferred from which segments are provided : relation only , record , or field .')
  .requiredOption('--lens <lens>', 'Lens')
  .requiredOption('--relation <relation>', 'Relation')
  .requiredOption('--key <key>', 'Key')
  .requiredOption('--field <field>', 'Field')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Lens', 'create', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

lensCommand
  .command('from-relation')
  .description('Construct a relation level lens that focuses on an entire storage relation ( a set or map of records ) . This is the broadest focus composing with at ( ) or field ( ) narrows it .')
  .requiredOption('--lens <lens>', 'Lens')
  .requiredOption('--relation <relation>', 'Relation')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Lens', 'fromRelation', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

lensCommand
  .command('compose')
  .description('Create a new lens by composing outer and inner , concatenating their path segments . The resulting lens focuses through the outer lens first , then applies the inner lens to the result . Composition is associative : compose ( a , compose ( b , c ) ) equals compose ( compose ( a , b ) , c ) .')
  .requiredOption('--outer <outer>', 'Outer')
  .requiredOption('--inner <inner>', 'Inner')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Lens', 'compose', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

lensCommand
  .command('get')
  .description('Retrieve the lens metadata including its path segments , source and focus types , and kind classification .')
  .requiredOption('--lens <lens>', 'Lens')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Lens', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

lensCommand
  .command('decompose')
  .description('Break the lens into its constituent path segments as a JSON array of { type , value } objects . Each segment is one of : relation , key , or field .')
  .requiredOption('--lens <lens>', 'Lens')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Lens', 'decompose', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

lensCommand
  .command('validate')
  .description('The lens path segments reference valid state declarations in the given concept specification . The relation exists , the key type is compatible , and the field is declared .')
  .requiredOption('--lens <lens>', 'Lens')
  .requiredOption('--concept-spec <conceptSpec>', 'Concept Spec')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Lens', 'validate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

lensCommand
  .command('list')
  .description('Return all registered lenses with their metadata as a serialized JSON array .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Lens', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const lensCommandTree = {
  group: 'lens',
  description: 'Typed , composable reference to a location within concept state , replacing untyped string based storage access with first class optics that can be composed into paths , decomposed into segments , and validated against concept state declarations',
  commands: [{ action: 'create', command: 'create' }, { action: 'fromRelation', command: 'from-relation' }, { action: 'compose', command: 'compose' }, { action: 'get', command: 'get' }, { action: 'decompose', command: 'decompose' }, { action: 'validate', command: 'validate' }, { action: 'list', command: 'list' }],
};
