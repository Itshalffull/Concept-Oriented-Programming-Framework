// Auto-generated by Clef Clef Bind — cli target
// Concept: PageMap
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const pageMapCommand = new Command('page-map')
  .description('Maintain a labeled inventory of interactive elements within a hosted view , addressable by semantic label , role , or concept binding . Independently useful for accessibility auditing , integration testing , dev tools , and agent driven UI navigation');

pageMapCommand
  .command('register')
  .description('Add a new interactive element to the page map with its label , semantic role , machine reference , widget name , FSM state , and host membership . The element is now addressable by label within its host .')
  .requiredOption('--entry <entry>', 'Entry')
  .requiredOption('--label <label>', 'Label')
  .requiredOption('--role <role>', 'Role')
  .requiredOption('--machine-ref <machineRef>', 'Machine Ref')
  .requiredOption('--widget-name <widgetName>', 'Widget Name')
  .requiredOption('--current-state <currentState>', 'Current State')
  .requiredOption('--valid-events <validEvents>', 'Valid Events')
  .requiredOption('--concept-binding <conceptBinding>', 'Concept Binding')
  .requiredOption('--affordance-serves <affordanceServes>', 'Affordance Serves')
  .requiredOption('--host-ref <hostRef>', 'Host Ref')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PageMap', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

pageMapCommand
  .command('update')
  .description('Overwrite the FSM state and valid event list for an existing entry , reflecting a widget state transition . All other fields remain unchanged .')
  .requiredOption('--entry <entry>', 'Entry')
  .requiredOption('--current-state <currentState>', 'Current State')
  .requiredOption('--valid-events <validEvents>', 'Valid Events')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PageMap', 'update', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

pageMapCommand
  .command('find')
  .description('Locate an element by label using fuzzy matching within the current inventory and return its identifier and machine reference for direct interaction .')
  .requiredOption('--entry <entry>', 'Entry')
  .requiredOption('--label <label>', 'Label')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PageMap', 'find', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

pageMapCommand
  .command('find-by-role')
  .description('Return a JSON array of all entries whose role matches the given value . Returns an empty array if no entries match .')
  .requiredOption('--role <role>', 'Role')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PageMap', 'findByRole', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

pageMapCommand
  .command('find-by-concept')
  .description('Return a JSON array of all entries whose conceptBinding matches the given concept name . Returns an empty array if no entries match .')
  .requiredOption('--concept <concept>', 'Concept')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PageMap', 'findByConcept', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

pageMapCommand
  .command('find-by-widget')
  .description('Return a JSON array of all entries whose widgetName matches the given widget spec name . Returns an empty array if no entries match .')
  .requiredOption('--widget-name <widgetName>', 'Widget Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PageMap', 'findByWidget', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

pageMapCommand
  .command('list')
  .description('Return a JSON array of all entries belonging to the given host . Returns an empty array if the host has no registered entries .')
  .requiredOption('--host-ref <hostRef>', 'Host Ref')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PageMap', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

pageMapCommand
  .command('clear')
  .description('Remove all entries belonging to the given host . Called by syncs when a Host unmounts to prevent stale element references from persisting in the inventory .')
  .requiredOption('--host-ref <hostRef>', 'Host Ref')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/PageMap', 'clear', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const pageMapCommandTree = {
  group: 'page-map',
  description: 'Maintain a labeled inventory of interactive elements within a hosted view , addressable by semantic label , role , or concept binding . Independently useful for accessibility auditing , integration testing , dev tools , and agent driven UI navigation',
  commands: [{ action: 'register', command: 'register' }, { action: 'update', command: 'update' }, { action: 'find', command: 'find' }, { action: 'findByRole', command: 'find-by-role' }, { action: 'findByConcept', command: 'find-by-concept' }, { action: 'findByWidget', command: 'find-by-widget' }, { action: 'list', command: 'list' }, { action: 'clear', command: 'clear' }],
};
