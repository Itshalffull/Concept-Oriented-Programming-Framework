// Auto-generated by Clef Clef Bind — cli target
// Concept: RuntimeInfrastructure
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const runtimeInfrastructureCommand = new Command('runtime-infrastructure')
  .description('Complete runtime infrastructure monadic execution pipeline ( StorageProgram , interpreter , effects ) , storage and transport adapters ( in-memory , SQL , cloud , HTTP , WebSocket , serverless ) , and application-level infrastructure primitives ( cache , plugin registry , event bus ) . The layer between concept handlers and the physical world .');

runtimeInfrastructureCommand
  .command('create-program')
  .description('Surface action: createProgram')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeInfrastructure', 'createProgram', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeInfrastructureCommand
  .command('ensure-index')
  .description('Surface action: ensureIndex')
  .requiredOption('--relation <relation>', 'Relation')
  .requiredOption('--field <field>', 'Field')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeInfrastructure', 'ensureIndex', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeInfrastructureCommand
  .command('get-effect-handlers')
  .description('Surface query: getEffectHandlers')
  .requiredOption('--protocol <protocol>', 'Protocol')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeInfrastructure', 'getEffectHandlers', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeInfrastructureCommand
  .command('get-cached')
  .description('Surface query: getCached')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeInfrastructure', 'getCached', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const runtimeInfrastructureCommandTree = {
  group: 'runtime-infrastructure',
  description: 'Complete runtime infrastructure monadic execution pipeline ( StorageProgram , interpreter , effects ) , storage and transport adapters ( in-memory , SQL , cloud , HTTP , WebSocket , serverless ) , and application-level infrastructure primitives ( cache , plugin registry , event bus ) . The layer between concept handlers and the physical world .',
  commands: [{ action: 'createProgram', command: 'create-program' }, { action: 'ensureIndex', command: 'ensure-index' }, { action: 'getEffectHandlers', command: 'get-effect-handlers' }, { action: 'getCached', command: 'get-cached' }],
};
