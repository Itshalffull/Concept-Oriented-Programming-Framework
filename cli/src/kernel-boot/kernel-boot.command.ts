// Auto-generated by Clef Clef Bind — cli target
// Concept: KernelBoot
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const kernelBootCommand = new Command('kernel-boot')
  .description('Bootstrap a Clef kernel instance by discovering concept handlers , compiling routing syncs , and exposing the kernel for action dispatch . Provides the runtime foundation that all CLI commands , API endpoints , and MCP tools require before they can invoke concept actions');

kernelBootCommand
  .command('boot')
  .description('Kernel booted : all concept handlers registered , routing syncs compiled , and kernel instance ready for action dispatch .')
  .requiredOption('--project-root <projectRoot>', 'Project Root')
  .requiredOption('--manifest-path <manifestPath>', 'Manifest Path')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/KernelBoot', 'boot', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

kernelBootCommand
  .command('status')
  .description('Current kernel state including registered concepts and compiled syncs .')
  .requiredOption('--kernel <kernel>', 'Kernel')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/KernelBoot', 'status', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

kernelBootCommand
  .command('shutdown')
  .description('Kernel instance torn down and all registrations cleared .')
  .requiredOption('--kernel <kernel>', 'Kernel')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/KernelBoot', 'shutdown', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const kernelBootCommandTree = {
  group: 'kernel-boot',
  description: 'Bootstrap a Clef kernel instance by discovering concept handlers , compiling routing syncs , and exposing the kernel for action dispatch . Provides the runtime foundation that all CLI commands , API endpoints , and MCP tools require before they can invoke concept actions',
  commands: [{ action: 'boot', command: 'boot' }, { action: 'status', command: 'status' }, { action: 'shutdown', command: 'shutdown' }],
};
