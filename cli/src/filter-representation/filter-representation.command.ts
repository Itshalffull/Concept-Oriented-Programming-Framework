// Auto-generated by Clef Clef Bind — cli target
// Concept: FilterRepresentation
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const filterRepresentationCommand = new Command('filter-representation')
  .description('Registry and dispatcher for bidirectional conversion between authoring representations and canonical FilterNode IR . Manages named representation providers with kind based routing through sync wiring the coordination hub that lets toggle groups , text DSL inputs , URL param codecs , and visual builders all speak the same FilterNode language without coupling to one another');

filterRepresentationCommand
  .command('register-kind')
  .description('Register a representation kind provider so that parse and print dispatches can route to it . Called by provider registration syncs to declare that a handler exists for this kind ( toggle group , text dsl , url params , visual builder , odata ) .')
  .requiredOption('--kind <kind>', 'Kind')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterRepresentation', 'registerKind', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterRepresentationCommand
  .command('register')
  .description('Register a named representation provider of a given kind . The name identifies the provider and must be unique across all registered representations .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--kind <kind>', 'Kind')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterRepresentation', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterRepresentationCommand
  .command('parse')
  .description('Forward direction : convert a representation string authored in the given kind s format into a canonical FilterNode IR JSON string . Dispatches to the appropriate kind provider through sync wiring .')
  .requiredOption('--repr <repr>', 'Repr')
  .requiredOption('--kind <kind>', 'Kind')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterRepresentation', 'parse', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterRepresentationCommand
  .command('print')
  .description('Reverse direction : convert a canonical FilterNode IR JSON string into the authoring representation string for the given kind . The produced repr can be fed back into parse to recover the original tree ( GetPut round trip law ) .')
  .requiredOption('--tree <tree>', 'Tree')
  .requiredOption('--kind <kind>', 'Kind')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterRepresentation', 'print', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterRepresentationCommand
  .command('can-print')
  .description('Check whether the kind provider can faithfully represent the given FilterNode tree without loss . Returns true when the round trip parse ( print ( tree , kind ) , kind ) would recover the original tree ; false when the kind s format is not expressive enough .')
  .requiredOption('--tree <tree>', 'Tree')
  .requiredOption('--kind <kind>', 'Kind')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterRepresentation', 'canPrint', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

filterRepresentationCommand
  .command('list')
  .description('Return all registered representations as a JSON array , each entry containing the representation s name and kind .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/FilterRepresentation', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const filterRepresentationCommandTree = {
  group: 'filter-representation',
  description: 'Registry and dispatcher for bidirectional conversion between authoring representations and canonical FilterNode IR . Manages named representation providers with kind based routing through sync wiring the coordination hub that lets toggle groups , text DSL inputs , URL param codecs , and visual builders all speak the same FilterNode language without coupling to one another',
  commands: [{ action: 'registerKind', command: 'register-kind' }, { action: 'register', command: 'register' }, { action: 'parse', command: 'parse' }, { action: 'print', command: 'print' }, { action: 'canPrint', command: 'can-print' }, { action: 'list', command: 'list' }],
};
