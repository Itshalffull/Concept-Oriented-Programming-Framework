// Auto-generated by Clef Clef Bind — cli target
// Concept: ConceptEntity
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const conceptEntityCommand = new Command('concept-entity')
  .description('Queryable representation of a parsed concept , linking its declaration to generated artifacts and runtime behavior . Enables semantic queries like what syncs participate in this concept? and what artifacts were generated from it?');

conceptEntityCommand
  .command('register')
  .description('Register a parsed concept as a semantic entity . Extracts name , purpose , actions , variants , state fields , and capabilities from the AST .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--source <source>', 'Source')
  .requiredOption('--ast <ast>', 'Ast')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ConceptEntity', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

conceptEntityCommand
  .command('get')
  .description('Retrieve a concept entity by name .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ConceptEntity', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

conceptEntityCommand
  .command('find-by-capability')
  .description('Return all concepts that declare the given capability . Results as serialized JSON array .')
  .requiredOption('--capability <capability>', 'Capability')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ConceptEntity', 'findByCapability', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

conceptEntityCommand
  .command('find-by-suite')
  .description('Return all concepts belonging to the given suite . Results as serialized JSON array .')
  .requiredOption('--suite <suite>', 'Suite')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ConceptEntity', 'findBySuite', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

conceptEntityCommand
  .command('generated-artifacts')
  .description('Return all files generated from this concept via Provenance lookup . Results as serialized JSON array .')
  .requiredOption('--entity <entity>', 'Entity')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ConceptEntity', 'generatedArtifacts', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

conceptEntityCommand
  .command('participating-syncs')
  .description('Return all syncs that reference any of this concept s actions in when , where , or then clauses . Results as serialized JSON array .')
  .requiredOption('--entity <entity>', 'Entity')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ConceptEntity', 'participatingSyncs', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

conceptEntityCommand
  .command('check-compatibility')
  .description('The two concepts share type parameters that can be unified in a suite composition .')
  .requiredOption('--a <a>', 'A')
  .requiredOption('--b <b>', 'B')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ConceptEntity', 'checkCompatibility', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const conceptEntityCommandTree = {
  group: 'concept-entity',
  description: 'Queryable representation of a parsed concept , linking its declaration to generated artifacts and runtime behavior . Enables semantic queries like what syncs participate in this concept? and what artifacts were generated from it?',
  commands: [{ action: 'register', command: 'register' }, { action: 'get', command: 'get' }, { action: 'findByCapability', command: 'find-by-capability' }, { action: 'findBySuite', command: 'find-by-suite' }, { action: 'generatedArtifacts', command: 'generated-artifacts' }, { action: 'participatingSyncs', command: 'participating-syncs' }, { action: 'checkCompatibility', command: 'check-compatibility' }],
};
