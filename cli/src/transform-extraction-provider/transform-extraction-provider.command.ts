// Auto-generated by Clef Clef Bind — cli target
// Concept: TransformExtractionProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const transformExtractionProviderCommand = new Command('transform-extraction-provider')
  .description('Extract the list of transforms that have been applied to a RenderProgram by reading its appliedTransforms metadata enabling provenance queries ( what transformations produced this output? ) and compliance verification ( was the a11y transform applied before rendering? )');

transformExtractionProviderCommand
  .command('analyze')
  .description('Parse the program and extract its appliedTransforms metadata . Return the list of transform names that were applied , in order of application , plus the total count .')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/TransformExtractionProvider', 'analyze', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const transformExtractionProviderCommandTree = {
  group: 'transform-extraction-provider',
  description: 'Extract the list of transforms that have been applied to a RenderProgram by reading its appliedTransforms metadata enabling provenance queries ( what transformations produced this output? ) and compliance verification ( was the a11y transform applied before rendering? )',
  commands: [{ action: 'analyze', command: 'analyze' }],
};
