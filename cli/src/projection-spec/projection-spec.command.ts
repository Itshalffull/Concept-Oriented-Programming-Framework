// Auto-generated by Clef Clef Bind — cli target
// Concept: ProjectionSpec
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const projectionSpecCommand = new Command('projection-spec')
  .description('Manage named field selection specifications that control which columns appear in a view , in what order , and how they are formatted or computed . Each projection captures a JSON serialized ProjectionField [ ] describing key , optional label , visibility , formatter , computed expression , and weight . Projections can be merged so that overlay fields win on key conflict and new fields are appended , and can be evaluated directly against a JSON row array to produce projected rows containing only the specified fields in the specified order');

projectionSpecCommand
  .command('create')
  .description('Register a new named projection spec with the given field array . The fields string is a JSON serialized ProjectionField [ ] where each entry may carry key , label , visible , formatter , computed , and weight properties . An empty array [ ] is valid and represents a zero field projection .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--fields <fields>', 'Fields')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProjectionSpec', 'create', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionSpecCommand
  .command('get')
  .description('Return the projection spec identifier and its serialized field array for the given name .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProjectionSpec', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionSpecCommand
  .command('merge')
  .description('Combine two projection specs : overlay fields win on key conflict ( matching by the key property ) , and fields present only in the overlay are appended after the base fields . The resulting projection is stored under the name base+overlay and its field array is returned .')
  .requiredOption('--base <base>', 'Base')
  .requiredOption('--overlay <overlay>', 'Overlay')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProjectionSpec', 'merge', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionSpecCommand
  .command('evaluate')
  .description('Apply the named projection spec to a JSON serialized row array . Each row is reduced to only the fields declared in the projection , in the order they appear in the spec . Fields marked visible : false are excluded . The result is a JSON serialized array of projected row objects .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--rows <rows>', 'Rows')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProjectionSpec', 'evaluate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionSpecCommand
  .command('list')
  .description('Return a JSON serialized array of all registered projection spec names .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProjectionSpec', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const projectionSpecCommandTree = {
  group: 'projection-spec',
  description: 'Manage named field selection specifications that control which columns appear in a view , in what order , and how they are formatted or computed . Each projection captures a JSON serialized ProjectionField [ ] describing key , optional label , visibility , formatter , computed expression , and weight . Projections can be merged so that overlay fields win on key conflict and new fields are appended , and can be evaluated directly against a JSON row array to produce projected rows containing only the specified fields in the specified order',
  commands: [{ action: 'create', command: 'create' }, { action: 'get', command: 'get' }, { action: 'merge', command: 'merge' }, { action: 'evaluate', command: 'evaluate' }, { action: 'list', command: 'list' }],
};
