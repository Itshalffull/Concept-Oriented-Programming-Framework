// Auto-generated by Clef Clef Bind — cli target
// Concept: AnatomyPartEntity
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const anatomyPartEntityCommand = new Command('anatomy-part-entity')
  .description('Named part within a widget s anatomy each carries a semantic 
 role and connects to props via the connect section . Enables 
 tracing from rendered UI elements back to concept state fields 
 and actions .');

anatomyPartEntityCommand
  .command('register')
  .description('Register an anatomy part extracted from a widget spec . 
 Role is one of : container , action , text , overlay , widget .')
  .requiredOption('--widget <widget>', 'Widget')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--role <role>', 'Role')
  .requiredOption('--required <required>', 'Required')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'register', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

anatomyPartEntityCommand
  .command('find-by-role')
  .description('Return all anatomy parts with the given semantic role 
 across all widgets . Results as serialized JSON array .')
  .requiredOption('--role <role>', 'Role')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'findByRole', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

anatomyPartEntityCommand
  .command('find-bound-to-field')
  .description('Return all anatomy parts that present the given concept 
 state field . Results as serialized JSON array .')
  .requiredOption('--field <field>', 'Field')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'findBoundToField', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

anatomyPartEntityCommand
  .command('find-bound-to-action')
  .description('Return all anatomy parts that trigger the given concept 
 action . Results as serialized JSON array .')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'findBoundToAction', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

anatomyPartEntityCommand
  .command('get')
  .description('Retrieve anatomy part metadata .')
  .requiredOption('--part <part>', 'Part')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'get', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

export const anatomyPartEntityCommandTree = {
  group: 'anatomy-part-entity',
  description: 'Named part within a widget s anatomy each carries a semantic 
 role and connects to props via the connect section . Enables 
 tracing from rendered UI elements back to concept state fields 
 and actions .',
  commands: [{ action: 'register', command: 'register' }, { action: 'findByRole', command: 'find-by-role' }, { action: 'findBoundToField', command: 'find-bound-to-field' }, { action: 'findBoundToAction', command: 'find-bound-to-action' }, { action: 'get', command: 'get' }],
};
