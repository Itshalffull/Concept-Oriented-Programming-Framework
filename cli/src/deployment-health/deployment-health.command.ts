// Auto-generated by Clef Clef Bind — cli target
// Concept: DeploymentHealth
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const deploymentHealthCommand = new Command('deployment-health')
  .description('Live deployment monitoring runtime health , transport connectivity , 
 storage adapter status , sync delivery rates , concept instance counts , 
 and cross runtime latency . Connects live operational data to the 
 static deployment topology ( DeploymentEntity ) and concept structure 
 ( ConceptEntity , SyncEntity ) . Enables queries like is the api runtime
    healthy? , what's the sync delivery failure rate? , which storage
    adapters are degraded? , and what's the p99 cross-runtime latency?');

deploymentHealthCommand
  .command('record')
  .description('Record a health check snapshot for a deployment . The snapshot 
 includes runtime statuses , transport connectivity , storage 
 adapter health , sync delivery metrics , concept instance 
 counts , and cross runtime latency measurements . Automatically 
 correlates with DeploymentEntity to map metrics to the 
 static deployment topology .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .requiredOption('--snapshot <snapshot>', 'Snapshot')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'record', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('runtime-health')
  .description('Return current health status for all runtimes in a deployment . 
 Result as serialized JSON array of { name , status , uptime , 
 conceptCount , lastChecked , memoryUsage , cpuUsage } . 
 status is one of : healthy , degraded , unhealthy , unreachable .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'runtimeHealth', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('transport-health')
  .description('Return current connectivity status for all transports . 
 Result as serialized JSON array of { from , to , protocol , 
 status , latencyP50Ms , latencyP99Ms , errorRate , lastChecked } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'transportHealth', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('storage-health')
  .description('Return current status for all storage adapters . Result as 
 serialized JSON array of { name , backend , runtime , status , 
 latencyP50Ms , latencyP99Ms , connectionPoolUsage , lastChecked } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'storageHealth', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('sync-delivery')
  .description('Return sync delivery metrics success failure rates , retry 
 counts , and delivery latency per sync . Result as serialized 
 JSON array of { sync , tier , deliveredCount , failedCount , 
 retryCount , successRate , avgLatencyMs , p99LatencyMs } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .requiredOption('--since <since>', 'Since')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'syncDelivery', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('concept-instances')
  .description('Return instance counts per concept per runtime . Result as 
 serialized JSON array of { concept , runtime , instanceCount , 
 activeCount , storageBytes } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'conceptInstances', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('cross-runtime-latency')
  .description('Return the latency matrix between all runtimes . Result as 
 serialized JSON array of { from , to , p50Ms , p90Ms , p99Ms , 
 sampleCount } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'crossRuntimeLatency', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('hotspots')
  .description('Return the top N operational hotspots concepts , syncs , 
 or transports with the highest error rates , latency , or 
 resource usage . Result as serialized JSON array of { entity , 
 kind , metric , value , trend } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .requiredOption('--since <since>', 'Since')
  .requiredOption('--top-n <topN>', 'Top N')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'hotspots', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('correlate-with-errors')
  .description('Cross reference deployment health with ErrorCorrelation 
 entities . Return errors grouped by failing runtime , transport , 
 or storage adapter . Result as serialized JSON array of 
 { infrastructure , errorCount , topErrors : [ { kind , message , 
 count } ] } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .requiredOption('--since <since>', 'Since')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'correlateWithErrors', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('compare-windows')
  .description('Compare deployment health between two time windows ( e . g . , 
 before and after a deploy ) . Result as serialized JSON of 
 { runtimeChanges , latencyChanges , errorRateChanges , 
 syncDeliveryChanges , regressions : [ ] } .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .requiredOption('--window-a <windowA>', 'Window A')
  .requiredOption('--window-b <windowB>', 'Window B')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'compareWindows', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

deploymentHealthCommand
  .command('slo-status')
  .description('Return SLO compliance status availability , latency , 
 error budgets per runtime and per concept . Result as 
 serialized JSON array of { entity , slo , target , actual , 
 budget , budgetRemaining , status } . 
 status is one of : met , at risk , breached .')
  .requiredOption('--deployment <deployment>', 'Deployment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    const result = await globalThis.kernel.handleRequest({ method: 'sloStatus', ...opts });
    console.log(opts.json ? JSON.stringify(result) : result);
  });

export const deploymentHealthCommandTree = {
  group: 'deployment-health',
  description: 'Live deployment monitoring runtime health , transport connectivity , 
 storage adapter status , sync delivery rates , concept instance counts , 
 and cross runtime latency . Connects live operational data to the 
 static deployment topology ( DeploymentEntity ) and concept structure 
 ( ConceptEntity , SyncEntity ) . Enables queries like is the api runtime
    healthy? , what's the sync delivery failure rate? , which storage
    adapters are degraded? , and what's the p99 cross-runtime latency?',
  commands: [{ action: 'record', command: 'record' }, { action: 'runtimeHealth', command: 'runtime-health' }, { action: 'transportHealth', command: 'transport-health' }, { action: 'storageHealth', command: 'storage-health' }, { action: 'syncDelivery', command: 'sync-delivery' }, { action: 'conceptInstances', command: 'concept-instances' }, { action: 'crossRuntimeLatency', command: 'cross-runtime-latency' }, { action: 'hotspots', command: 'hotspots' }, { action: 'correlateWithErrors', command: 'correlate-with-errors' }, { action: 'compareWindows', command: 'compare-windows' }, { action: 'sloStatus', command: 'slo-status' }],
};
