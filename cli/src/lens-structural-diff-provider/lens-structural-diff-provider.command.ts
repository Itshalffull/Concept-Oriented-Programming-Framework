// Auto-generated by Clef Clef Bind — cli target
// Concept: LensStructuralDiffProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const lensStructuralDiffProviderCommand = new Command('lens-structural-diff-provider')
  .description('Compare two versions of a concept s state declarations and produce a structural diff expressed as lens operations , enabling the Diff Patch pipeline to generate invertible schema migrations');

lensStructuralDiffProviderCommand
  .command('analyze')
  .description('Parse both schemas as JSON arrays of state field declarations , compute structural differences , and express each change as a lens operation : addField , removeField , renameField , changeType , or changeCardinality . The edit script is a JSON array of these operations suitable for the Patch concept .')
  .requiredOption('--old-schema <oldSchema>', 'Old Schema')
  .requiredOption('--new-schema <newSchema>', 'New Schema')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/LensStructuralDiffProvider', 'analyze', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const lensStructuralDiffProviderCommandTree = {
  group: 'lens-structural-diff-provider',
  description: 'Compare two versions of a concept s state declarations and produce a structural diff expressed as lens operations , enabling the Diff Patch pipeline to generate invertible schema migrations',
  commands: [{ action: 'analyze', command: 'analyze' }],
};
