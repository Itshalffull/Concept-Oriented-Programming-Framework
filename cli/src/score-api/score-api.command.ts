// Auto-generated by Clef Clef Bind — cli target
// Concept: ScoreApi
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const scoreApiCommand = new Command('score-api')
  .description('Unified facade over the five Score suites ( parse , symbol , semantic , analysis , discovery ) providing a single LLM friendly surface for querying any Clef project s structure , symbols , semantics , data flows , and search indexes . Every Clef app gets ScoreApi registered automatically LLMs can immediately ask questions about the codebase without configuration . Actions are designed for natural language invocation : parameter names read as English , results are structured for tool use consumption , and error variants include actionable suggestions');

scoreApiCommand
  .command('list-files')
  .description('List all files in the project matching a glob pattern . Returns path , detected language , role ( source , generated , config , test , doc ) , and file size . Use * for all files .')
  .requiredOption('--pattern <pattern>', 'Pattern')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'listFiles', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-file-tree')
  .description('Get a hierarchical tree view of the project starting at the given path . Depth controls recursion level ( 0 = full ) . Tree is formatted as an indented text tree suitable for display in chat or tool output .')
  .requiredOption('--path <path>', 'Path')
  .requiredOption('--depth <depth>', 'Depth')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getFileTree', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-file-content')
  .description('Read a file s content along with its detected language and the names of top level definitions it contains .')
  .requiredOption('--path <path>', 'Path')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getFileContent', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-definitions')
  .description('Extract all definition units from a file functions , classes , types , concept actions , sync rules , etc . Each entry includes the definition name , its kind , the line number , and the source span .')
  .requiredOption('--path <path>', 'Path')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getDefinitions', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('match-pattern')
  .description('Run a structural pattern query ( tree sitter query syntax ) across all files of the given language . Returns matching locations with surrounding context .')
  .requiredOption('--pattern <pattern>', 'Pattern')
  .requiredOption('--language <language>', 'Language')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'matchPattern', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('find-symbol')
  .description('Find all symbols matching the given name . Returns the symbol kind ( function , type , concept , action , field , etc . ) , defining file , line number , and enclosing scope .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'findSymbol', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-references')
  .description('Find all references to a symbol across the project . Returns the definition location and all reference locations with their reference kind ( read , write , call , import , type use ) .')
  .requiredOption('--symbol <symbol>', 'Symbol')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getReferences', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-scope')
  .description('Get the scope at a specific file location . Returns the scope name , all symbols visible in that scope , and the parent scope if any .')
  .requiredOption('--file <file>', 'File')
  .requiredOption('--line <line>', 'Line')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getScope', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-relationships')
  .description('Get typed relationships involving a symbol implements , extends , overrides , calls , imports , depends on , etc .')
  .requiredOption('--symbol <symbol>', 'Symbol')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getRelationships', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('list-concepts')
  .description('List all concepts in the project with their purpose , action names , state field names , and defining file path . This is the primary entry point for understanding a Clef application s architecture .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'listConcepts', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-concept')
  .description('Get complete details of a concept its purpose , type parameters , actions with params and variants , state fields with types and relation groups , invariants , and source file .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getConcept', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-action')
  .description('Get full details of a concept action parameters with types , all variants with their fields and prose , and the action description .')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getAction', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('list-syncs')
  .description('List all sync rules in the project with their annotation ( eager , eventual , local ) , trigger patterns , effect actions , and source file .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'listSyncs', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-sync')
  .description('Get full details of a sync rule the when clause with pattern matched concept action bindings , optional where clause with queries and filters , and then clause with invoked concept action bindings .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getSync', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-flow')
  .description('Trace the static flow graph starting from a concept action . Shows the chain of sync firings and concept action invocations that would occur . Useful for understanding side effects and cross concept coordination .')
  .requiredOption('--start-concept <startConcept>', 'Start Concept')
  .requiredOption('--start-action <startAction>', 'Start Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getFlow', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-handler')
  .description('Get the handler implementation for a concept in a given language . Returns source file , action method locations , dependency list , and storage collection usage .')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--language <language>', 'Language')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getHandler', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-action-source')
  .description('Get the source code of a specific action method in the concept s handler . Returns the method body and its exact file location .')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getActionSource', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('list-handlers')
  .description('List all registered handlers with their concept , language , source file , number of implemented actions , and line count .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'listHandlers', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('implementation-gaps')
  .description('Find all concept actions that are declared in specs but have no handler implementation . Returns concept name , action name , and the spec file where it s declared .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'implementationGaps', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('resolve-stack-trace')
  .description('Parse a stack trace and resolve each frame to handler entities , concept names , action methods , and exact AST nodes where possible . For frames within handlers , astNode is the precise code construct at that position ( e . g . , AwaitExpression , ReturnStatement , IfStatement ) and astAncestors is the chain up to the method root .')
  .requiredOption('--stack-trace <stackTrace>', 'Stack Trace')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'resolveStackTrace', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-widget-impl')
  .description('Get the generated widget implementation for a given framework . Returns component name , rendered parts mapping , and props interface .')
  .requiredOption('--widget <widget>', 'Widget')
  .requiredOption('--framework <framework>', 'Framework')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getWidgetImpl', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-theme-impl')
  .description('Get the generated theme implementation for a given platform . Returns source file and token count .')
  .requiredOption('--theme <theme>', 'Theme')
  .requiredOption('--platform <platform>', 'Platform')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getThemeImpl', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-deployment')
  .description('Get a deployment manifest s topology runtimes , concept assignments , and transport storage bindings .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getDeployment', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-deployment-topology')
  .description('Get the full deployment topology as a traversable graph .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getDeploymentTopology', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-deployment-health')
  .description('Get current operational health of a deployment .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getDeploymentHealth', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('list-suites')
  .description('List all registered suites .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'listSuites', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-suite')
  .description('Get full details of a suite manifest .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getSuite', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('list-interfaces')
  .description('List all registered interface manifests .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'listInterfaces', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-endpoints')
  .description('List all generated endpoints for a target ( rest , graphql , grpc ) .')
  .requiredOption('--interface <interface>', 'Interface')
  .requiredOption('--target <target>', 'Target')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getEndpoints', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('trace-endpoint')
  .description('Trace an API endpoint back to the concept , action , and handler that implements it .')
  .requiredOption('--target <target>', 'Target')
  .requiredOption('--path <path>', 'Path')
  .requiredOption('--method <method>', 'Method')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'traceEndpoint', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-dependencies')
  .description('Get direct and transitive dependencies of a symbol . Shows what a symbol depends on ( imports , calls , reads ) .')
  .requiredOption('--symbol <symbol>', 'Symbol')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getDependencies', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-dependents')
  .description('Get direct and transitive dependents of a symbol . Shows what depends on this symbol ( reverse dependencies ) . Useful for impact analysis .')
  .requiredOption('--symbol <symbol>', 'Symbol')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getDependents', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-impact')
  .description('Analyze the impact of changing a file . Returns directly affected files and transitively affected files with the reason for impact ( import , sync reference , type dependency , generated from , etc . ) .')
  .requiredOption('--file <file>', 'File')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getImpact', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-data-flow')
  .description('Find data flow paths between two symbols . Shows how data moves from one point to another through the program . Useful for understanding action chains and sync propagation .')
  .requiredOption('--from <from>', 'From')
  .requiredOption('--to <to>', 'To')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getDataFlow', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('search')
  .description('Search the project using natural language . Combines semantic embedding similarity , trigram text search , and symbol aware indexing for best results . Returns ranked matches with relevance score and context snippet .')
  .requiredOption('--query <query>', 'Query')
  .requiredOption('--limit <limit>', 'Limit')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'search', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('explain')
  .description('Generate a natural language explanation of a symbol what it is , where it s defined , what uses it , and its key relationships . Designed for LLM consumption : the summary is a complete sentence suitable for embedding in a response .')
  .requiredOption('--symbol <symbol>', 'Symbol')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'explain', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('list-views')
  .description('Return all indexed ViewEntity entries as a JSON array , each containing name , shellRef , purposeText , and purity .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'listViews', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('get-view')
  .description('Return the full ViewEntity record for the named view as JSON , including all analysis fields ( purity , invokedActions , projectedFields , filterFields , invariants ) .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'getView', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('find-views-by-shell')
  .description('Return all views referencing this ViewShell as a JSON array .')
  .requiredOption('--shell-ref <shellRef>', 'Shell Ref')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'findViewsByShell', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('find-views-by-purity')
  .description('Return all views with the given purity classification .')
  .requiredOption('--purity <purity>', 'Purity')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'findViewsByPurity', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('find-views-by-invoked-action')
  .description('Return all views whose invokedActions include the given concept action pair .')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'findViewsByInvokedAction', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('status')
  .description('Get the current status of the Score index . Returns counts of indexed entities and the last indexing time .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'status', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

scoreApiCommand
  .command('reindex')
  .description('Force a full reindex of the project . Parses all files , extracts symbols , builds semantic graph , indexes handler implementations , widget implementations , theme implementations , view manifests , computes analysis overlays , and generates embeddings . Returns counts and duration in milliseconds .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ScoreApi', 'reindex', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const scoreApiCommandTree = {
  group: 'score-api',
  description: 'Unified facade over the five Score suites ( parse , symbol , semantic , analysis , discovery ) providing a single LLM friendly surface for querying any Clef project s structure , symbols , semantics , data flows , and search indexes . Every Clef app gets ScoreApi registered automatically LLMs can immediately ask questions about the codebase without configuration . Actions are designed for natural language invocation : parameter names read as English , results are structured for tool use consumption , and error variants include actionable suggestions',
  commands: [{ action: 'listFiles', command: 'list-files' }, { action: 'getFileTree', command: 'get-file-tree' }, { action: 'getFileContent', command: 'get-file-content' }, { action: 'getDefinitions', command: 'get-definitions' }, { action: 'matchPattern', command: 'match-pattern' }, { action: 'findSymbol', command: 'find-symbol' }, { action: 'getReferences', command: 'get-references' }, { action: 'getScope', command: 'get-scope' }, { action: 'getRelationships', command: 'get-relationships' }, { action: 'listConcepts', command: 'list-concepts' }, { action: 'getConcept', command: 'get-concept' }, { action: 'getAction', command: 'get-action' }, { action: 'listSyncs', command: 'list-syncs' }, { action: 'getSync', command: 'get-sync' }, { action: 'getFlow', command: 'get-flow' }, { action: 'getHandler', command: 'get-handler' }, { action: 'getActionSource', command: 'get-action-source' }, { action: 'listHandlers', command: 'list-handlers' }, { action: 'implementationGaps', command: 'implementation-gaps' }, { action: 'resolveStackTrace', command: 'resolve-stack-trace' }, { action: 'getWidgetImpl', command: 'get-widget-impl' }, { action: 'getThemeImpl', command: 'get-theme-impl' }, { action: 'getDeployment', command: 'get-deployment' }, { action: 'getDeploymentTopology', command: 'get-deployment-topology' }, { action: 'getDeploymentHealth', command: 'get-deployment-health' }, { action: 'listSuites', command: 'list-suites' }, { action: 'getSuite', command: 'get-suite' }, { action: 'listInterfaces', command: 'list-interfaces' }, { action: 'getEndpoints', command: 'get-endpoints' }, { action: 'traceEndpoint', command: 'trace-endpoint' }, { action: 'getDependencies', command: 'get-dependencies' }, { action: 'getDependents', command: 'get-dependents' }, { action: 'getImpact', command: 'get-impact' }, { action: 'getDataFlow', command: 'get-data-flow' }, { action: 'search', command: 'search' }, { action: 'explain', command: 'explain' }, { action: 'listViews', command: 'list-views' }, { action: 'getView', command: 'get-view' }, { action: 'findViewsByShell', command: 'find-views-by-shell' }, { action: 'findViewsByPurity', command: 'find-views-by-purity' }, { action: 'findViewsByInvokedAction', command: 'find-views-by-invoked-action' }, { action: 'status', command: 'status' }, { action: 'reindex', command: 'reindex' }],
};
