// Auto-generated by Clef Clef Bind — cli target
// Concept: ProgramInterpreter
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const programInterpreterCommand = new Command('program-interpreter')
  .description('Execute a StorageProgram against a ConceptStorage backend , managing transaction boundaries , producing completions , and recording execution traces for rollback');

programInterpreterCommand
  .command('register')
  .description('Register an interpreter bound to the named storage backend operating in the given mode ( live , dry run , or replay ) .')
  .requiredOption('--interpreter <interpreter>', 'Interpreter')
  .requiredOption('--backend <backend>', 'Backend')
  .requiredOption('--mode <mode>', 'Mode')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProgramInterpreter', 'register', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

programInterpreterCommand
  .command('execute')
  .description('Run the program instruction by instruction against the backend . In live mode mutations are applied to storage ; in dry run mode they are applied to an in memory snapshot only . Returns an executionId usable for rollback , the terminal variant , output fields , and a full execution trace .')
  .requiredOption('--interpreter <interpreter>', 'Interpreter')
  .requiredOption('--program <program>', 'Program')
  .requiredOption('--snapshot <snapshot>', 'Snapshot')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProgramInterpreter', 'execute', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

programInterpreterCommand
  .command('dry-run')
  .description('Execute the program against a snapshot without persisting any mutations . Returns the terminal variant , output , and the list of mutations that would have been applied .')
  .requiredOption('--interpreter <interpreter>', 'Interpreter')
  .requiredOption('--program <program>', 'Program')
  .requiredOption('--snapshot <snapshot>', 'Snapshot')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProgramInterpreter', 'dryRun', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

programInterpreterCommand
  .command('rollback')
  .description('Reverse the mutations from a previous execution by applying compensating operations derived from its execution trace .')
  .requiredOption('--interpreter <interpreter>', 'Interpreter')
  .requiredOption('--execution-id <executionId>', 'Execution Id')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ProgramInterpreter', 'rollback', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const programInterpreterCommandTree = {
  group: 'program-interpreter',
  description: 'Execute a StorageProgram against a ConceptStorage backend , managing transaction boundaries , producing completions , and recording execution traces for rollback',
  commands: [{ action: 'register', command: 'register' }, { action: 'execute', command: 'execute' }, { action: 'dryRun', command: 'dry-run' }, { action: 'rollback', command: 'rollback' }],
};
