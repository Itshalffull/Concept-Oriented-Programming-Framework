// Auto-generated by Clef Clef Bind — cli target
// Concept: InvariantExtractionProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const invariantExtractionProviderCommand = new Command('invariant-extraction-provider')
  .description('Extract verifiable formal properties from a StorageProgram given its concept specification . Produces property candidates relating pre state to post state for the formal verification pipeline');

invariantExtractionProviderCommand
  .command('extract')
  .description('Analyze the program s instruction sequence against the concept s declared invariants and state constraints . For each invariant , derive a verifiable property of the form if pre-state satisfies P then post-state satisfies Q.')
  .requiredOption('--program <program>', 'Program')
  .requiredOption('--concept-spec <conceptSpec>', 'Concept Spec')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InvariantExtractionProvider', 'extract', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const invariantExtractionProviderCommandTree = {
  group: 'invariant-extraction-provider',
  description: 'Extract verifiable formal properties from a StorageProgram given its concept specification . Produces property candidates relating pre state to post state for the formal verification pipeline',
  commands: [{ action: 'extract', command: 'extract' }],
};
