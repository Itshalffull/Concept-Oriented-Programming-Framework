// Auto-generated by Clef Clef Bind — cli target
// Concept: ParallelismProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const parallelismProviderCommand = new Command('parallelism-provider')
  .description('Analyze a StorageProgram s instruction list to identify independent instruction groups that can execute concurrently . Builds a dependency graph from binding writes ( bindAs ) to binding reads ( closure functions ) , partitions instructions into parallel layers via topological sort , and reports the maximum parallelism and theoretical speedup ratio');

parallelismProviderCommand
  .command('analyze')
  .description('Walk the instruction list . Track which instructions write bindings ( get find mapBindings via bindAs ) . Track which instructions read bindings ( branch condition , pureFrom fn , putFrom valueFn , mergeFrom fieldsFn , delFrom keyFn conservatively assume these read ALL previously written bindings ) . Build dependency edges . Partition into parallel layers via topological sort . Report layers , critical path length , max parallelism , and speedup ratio .')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/ParallelismProvider', 'analyze', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const parallelismProviderCommandTree = {
  group: 'parallelism-provider',
  description: 'Analyze a StorageProgram s instruction list to identify independent instruction groups that can execute concurrently . Builds a dependency graph from binding writes ( bindAs ) to binding reads ( closure functions ) , partitions instructions into parallel layers via topological sort , and reports the maximum parallelism and theoretical speedup ratio',
  commands: [{ action: 'analyze', command: 'analyze' }],
};
