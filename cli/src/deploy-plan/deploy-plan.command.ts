// Auto-generated by Clef Clef Bind — cli target
// Concept: DeployPlan
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const deployPlanCommand = new Command('deploy-plan')
  .description('Compute , validate , and execute deployment plans for suites . Constructs a dependency graph from concept specs , syncs , and the deploy manifest , then executes operations in topological order with parallelism on independent branches');

deployPlanCommand
  .command('plan')
  .description('Parse the deploy manifest , resolve environment overlays , construct the deploy DAG . Return the plan for inspection before execution . Graph returned as serialized JSON .')
  .requiredOption('--manifest <manifest>', 'Manifest')
  .requiredOption('--environment <environment>', 'Environment')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/DeployPlan', 'plan', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

deployPlanCommand
  .command('validate')
  .description('All pre deployment invariants pass : sync completeness , transport compatibility , storage migration safety , dependency ordering . Warnings for non blocking issues .')
  .requiredOption('--plan <plan>', 'Plan')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/DeployPlan', 'validate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

deployPlanCommand
  .command('execute')
  .description('All nodes in the deploy graph executed successfully . Progressive delivery completed if configured .')
  .requiredOption('--plan <plan>', 'Plan')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/DeployPlan', 'execute', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

deployPlanCommand
  .command('rollback')
  .description('Compensating actions executed in reverse dependency order .')
  .requiredOption('--plan <plan>', 'Plan')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/DeployPlan', 'rollback', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

deployPlanCommand
  .command('status')
  .description('Current execution status of an in progress deployment .')
  .requiredOption('--plan <plan>', 'Plan')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/DeployPlan', 'status', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const deployPlanCommandTree = {
  group: 'deploy-plan',
  description: 'Compute , validate , and execute deployment plans for suites . Constructs a dependency graph from concept specs , syncs , and the deploy manifest , then executes operations in topological order with parallelism on independent branches',
  commands: [{ action: 'plan', command: 'plan' }, { action: 'validate', command: 'validate' }, { action: 'execute', command: 'execute' }, { action: 'rollback', command: 'rollback' }, { action: 'status', command: 'status' }],
};
