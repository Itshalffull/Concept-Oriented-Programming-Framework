// Auto-generated by Clef Clef Bind — cli target
// Concept: LensExtractionProvider
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const lensExtractionProviderCommand = new Command('lens-extraction-provider')
  .description('Extract implicit lens references from StorageProgram instruction trees , converting string based storage access patterns into first class typed StateLens values for traceability and analysis');

lensExtractionProviderCommand
  .command('analyze')
  .description('Walk the instruction tree and convert each get put del merge instruction into an explicit StateLens reference . Instructions that already use getLens putLens modifyLens are passed through . Returns extracted lenses as JSON and a per lens access pattern classification ( read , write , or read write ) .')
  .requiredOption('--program <program>', 'Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/LensExtractionProvider', 'analyze', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const lensExtractionProviderCommandTree = {
  group: 'lens-extraction-provider',
  description: 'Extract implicit lens references from StorageProgram instruction trees , converting string based storage access patterns into first class typed StateLens values for traceability and analysis',
  commands: [{ action: 'analyze', command: 'analyze' }],
};
