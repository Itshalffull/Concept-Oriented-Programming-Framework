// Auto-generated by Clef Clef Bind — cli target
// Concept: Connection
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const connectionCommand = new Command('connection')
  .description('Establish and manage a session with a running Clef kernel instance , providing discovery , invocation , and observation of registered concepts . Transport agnostic : the same concept works over WebSocket , HTTP , IPC , or in process . Each connection carries a session identity that the kernel s existing auth concepts evaluate on every invocation');

connectionCommand
  .command('connect')
  .description('Kernel reached , handshake completed , session token issued , and the full set of registered concept names discovered and stored . The connection is now ready for invoke and observe calls .')
  .requiredOption('--connection <connection>', 'Connection')
  .requiredOption('--endpoint <endpoint>', 'Endpoint')
  .requiredOption('--transport-adapter <transportAdapter>', 'Transport Adapter')
  .requiredOption('--credentials <credentials>', 'Credentials')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Connection', 'connect', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

connectionCommand
  .command('discover')
  .description('Returns a JSON encoded discovery result at the requested depth : list yields concept names only , manifest adds actions , inputs , and variants for each concept , full adds syncs , affordances , and widgets ( requires Score to be available on the kernel ) .')
  .requiredOption('--connection <connection>', 'Connection')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--depth <depth>', 'Depth')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Connection', 'discover', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

connectionCommand
  .command('invoke')
  .description('The action was executed on the kernel . variant is the completion variant tag returned by the handler ( e . g . ok , notfound ) , and output is the JSON encoded output fields of that completion .')
  .requiredOption('--connection <connection>', 'Connection')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--action <action>', 'Action')
  .requiredOption('--input <input>', 'Input')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Connection', 'invoke', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

connectionCommand
  .command('observe')
  .description('A completion stream subscription was opened . streamId identifies this subscription ; completions matching the concept ( and optional action ) will be delivered on this stream until unsubscribed or the connection is closed .')
  .requiredOption('--connection <connection>', 'Connection')
  .requiredOption('--concept <concept>', 'Concept')
  .requiredOption('--action <action>', 'Action')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Connection', 'observe', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

connectionCommand
  .command('disconnect')
  .description('The session token was invalidated , the transport was closed cleanly , and the connection s status is now disconnected .')
  .requiredOption('--connection <connection>', 'Connection')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Connection', 'disconnect', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const connectionCommandTree = {
  group: 'connection',
  description: 'Establish and manage a session with a running Clef kernel instance , providing discovery , invocation , and observation of registered concepts . Transport agnostic : the same concept works over WebSocket , HTTP , IPC , or in process . Each connection carries a session identity that the kernel s existing auth concepts evaluate on every invocation',
  commands: [{ action: 'connect', command: 'connect' }, { action: 'discover', command: 'discover' }, { action: 'invoke', command: 'invoke' }, { action: 'observe', command: 'observe' }, { action: 'disconnect', command: 'disconnect' }],
};
