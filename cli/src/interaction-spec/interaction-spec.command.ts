// Auto-generated by Clef Clef Bind — cli target
// Concept: InteractionSpec
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const interactionSpecCommand = new Command('interaction-spec')
  .description('Manage named interaction configurations that govern controls , row actions , navigation , and picker behaviors for a view . Each interaction spec captures whether a create form is available ( as a serialized CreateFormConfig JSON ) , a row click navigation rule ( as a serialized RowClickConfig JSON ) , a list of per row action buttons ( as a serialized RowActionConfig [ ] JSON ) , and whether the view operates in picker mode . Interaction specs are independent of data source , filter , sort , and presentation concerns they describe only what a user can do with the rows displayed');

interactionSpecCommand
  .command('create')
  .description('Register a new named interaction spec with the given create form config , row click rule , row action list , and picker mode flag . The spec is immediately available for retrieval and composition with view and layout specs .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--create-form <createForm>', 'Create Form')
  .requiredOption('--row-click <rowClick>', 'Row Click')
  .requiredOption('--row-actions <rowActions>', 'Row Actions')
  .requiredOption('--picker-mode <pickerMode>', 'Picker Mode')
  .requiredOption('--create-program <createProgram>', 'Create Program')
  .requiredOption('--action-program <actionProgram>', 'Action Program')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InteractionSpec', 'create', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

interactionSpecCommand
  .command('get')
  .description('Return the interaction spec s create form config , row click rule , row action list , picker mode flag , and optional invoke bearing QueryProgram references exactly as registered .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InteractionSpec', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

interactionSpecCommand
  .command('list')
  .description('Return all registered interaction specs as a JSON array , each entry containing name , createForm , rowClick , rowActions , and pickerMode .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InteractionSpec', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const interactionSpecCommandTree = {
  group: 'interaction-spec',
  description: 'Manage named interaction configurations that govern controls , row actions , navigation , and picker behaviors for a view . Each interaction spec captures whether a create form is available ( as a serialized CreateFormConfig JSON ) , a row click navigation rule ( as a serialized RowClickConfig JSON ) , a list of per row action buttons ( as a serialized RowActionConfig [ ] JSON ) , and whether the view operates in picker mode . Interaction specs are independent of data source , filter , sort , and presentation concerns they describe only what a user can do with the rows displayed',
  commands: [{ action: 'create', command: 'create' }, { action: 'get', command: 'get' }, { action: 'list', command: 'list' }],
};
