// Auto-generated by Clef Clef Bind — cli target
// Concept: GroupSpec
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const groupSpecCommand = new Command('group-spec')
  .description('Define named grouping specifications that cluster records into hierarchical groups and compute aggregations over each group . Each group spec captures a serialized GroupingConfig ( type , fields , and optional set memberships ) , an AggregationDef [ ] describing functions and aliases to compute per group , and an optional post aggregation FilterNode for HAVING style pruning . Group specs can be evaluated directly against a JSON row array , returning a structured result containing the group key , the matching rows , and the computed aggregates for each bucket');

groupSpecCommand
  .command('create')
  .description('Register a new named group spec with its serialized GroupingConfig and AggregationDef array . The spec is immediately available for evaluation and retrieval .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--grouping <grouping>', 'Grouping')
  .requiredOption('--aggregations <aggregations>', 'Aggregations')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/GroupSpec', 'create', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

groupSpecCommand
  .command('get')
  .description('Return the group spec identifier , its serialized GroupingConfig , AggregationDef array , and optional HAVING filter for the given name .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/GroupSpec', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

groupSpecCommand
  .command('evaluate')
  .description('Apply the named group spec s GroupingConfig to the JSON serialized row array , compute each AggregationDef per bucket , and apply the optional HAVING filter to prune groups . The result is a JSON array where each entry contains the group key , the matching rows , and the computed aggregates : [ { key , rows , aggregates } ] .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--rows <rows>', 'Rows')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/GroupSpec', 'evaluate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

groupSpecCommand
  .command('list')
  .description('Return a JSON serialized array of all registered group spec names .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/GroupSpec', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const groupSpecCommandTree = {
  group: 'group-spec',
  description: 'Define named grouping specifications that cluster records into hierarchical groups and compute aggregations over each group . Each group spec captures a serialized GroupingConfig ( type , fields , and optional set memberships ) , an AggregationDef [ ] describing functions and aliases to compute per group , and an optional post aggregation FilterNode for HAVING style pruning . Group specs can be evaluated directly against a JSON row array , returning a structured result containing the group key , the matching rows , and the computed aggregates for each bucket',
  commands: [{ action: 'create', command: 'create' }, { action: 'get', command: 'get' }, { action: 'evaluate', command: 'evaluate' }, { action: 'list', command: 'list' }],
};
