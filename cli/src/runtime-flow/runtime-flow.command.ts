// Auto-generated by Clef Clef Bind — cli target
// Concept: RuntimeFlow
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const runtimeFlowCommand = new Command('runtime-flow')
  .description('Enriched execution flow that correlates ActionLog events with static semantic entities the resolved version of FlowTrace . Enables comparing actual runtime paths against static FlowGraph predictions to find deviations , missing syncs , and unexpected branches');

runtimeFlowCommand
  .command('correlate')
  .description('Build an enriched flow by resolving all ActionLog entries for this flow ID to their static semantic entities ( ConceptEntity , ActionEntity , VariantEntity , SyncEntity ) .')
  .requiredOption('--flow-id <flowId>', 'Flow Id')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeFlow', 'correlate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeFlowCommand
  .command('find-by-action')
  .description('Return all flows that include the given action . Results as serialized JSON array .')
  .requiredOption('--action <action>', 'Action')
  .requiredOption('--since <since>', 'Since')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeFlow', 'findByAction', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeFlowCommand
  .command('find-by-sync')
  .description('Return all flows that triggered the given sync . Results as serialized JSON array .')
  .requiredOption('--sync <sync>', 'Sync')
  .requiredOption('--since <since>', 'Since')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeFlow', 'findBySync', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeFlowCommand
  .command('find-by-variant')
  .description('Return all flows that produced the given variant . Results as serialized JSON array .')
  .requiredOption('--variant <variant>', 'Variant')
  .requiredOption('--since <since>', 'Since')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeFlow', 'findByVariant', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeFlowCommand
  .command('find-failures')
  .description('Return all flows with status failed or timeout . Results as serialized JSON array .')
  .requiredOption('--since <since>', 'Since')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeFlow', 'findFailures', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeFlowCommand
  .command('compare-to-static')
  .description('The runtime execution path matches the predicted static FlowGraph path exactly .')
  .requiredOption('--flow <flow>', 'Flow')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeFlow', 'compareToStatic', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeFlowCommand
  .command('source-locations')
  .description('Return source file locations for each step in the flow . Results as serialized JSON array of { step , file , line , col , symbol } .')
  .requiredOption('--flow <flow>', 'Flow')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeFlow', 'sourceLocations', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

runtimeFlowCommand
  .command('get')
  .description('Retrieve runtime flow metadata .')
  .requiredOption('--flow <flow>', 'Flow')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/RuntimeFlow', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const runtimeFlowCommandTree = {
  group: 'runtime-flow',
  description: 'Enriched execution flow that correlates ActionLog events with static semantic entities the resolved version of FlowTrace . Enables comparing actual runtime paths against static FlowGraph predictions to find deviations , missing syncs , and unexpected branches',
  commands: [{ action: 'correlate', command: 'correlate' }, { action: 'findByAction', command: 'find-by-action' }, { action: 'findBySync', command: 'find-by-sync' }, { action: 'findByVariant', command: 'find-by-variant' }, { action: 'findFailures', command: 'find-failures' }, { action: 'compareToStatic', command: 'compare-to-static' }, { action: 'sourceLocations', command: 'source-locations' }, { action: 'get', command: 'get' }],
};
