// Auto-generated by Clef Clef Bind — cli target
// Concept: DataSourceSpec
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const dataSourceSpecCommand = new Command('data-source-spec')
  .description('Declare the base dataset for a view : what concept action to invoke , what external source to query , or what inline data to use . Each source records its kind , serialized configuration , and the template variables it expects at bind time , so the view layer can resolve the right data without knowing the source s internal structure');

dataSourceSpecCommand
  .command('create')
  .description('Register a new named data source with the given kind and serialized DataSourceConfig JSON . The source is immediately available for get , bind , and list operations .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--kind <kind>', 'Kind')
  .requiredOption('--config <config>', 'Config')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/DataSourceSpec', 'create', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

dataSourceSpecCommand
  .command('get')
  .description('Return the source s kind , serialized config , and the list of template variable names extracted from the config at registration time , serialized as a JSON array string .')
  .requiredOption('--name <name>', 'Name')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/DataSourceSpec', 'get', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

dataSourceSpecCommand
  .command('bind')
  .description('Resolve every template variable of the form { { varName } } in the source s config with the corresponding value from the bindings map , and return the resulting config string with all placeholders substituted . For example , a config containing { params : { id : {{entityId}} } } bound with { entityId : abc } yields { params : { id : abc } } .')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--bindings <bindings>', 'Bindings')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/DataSourceSpec', 'bind', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

dataSourceSpecCommand
  .command('list')
  .description('Return all registered data sources as a JSON array , each entry containing the source s name , kind , config , and parameters .')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/DataSourceSpec', 'list', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const dataSourceSpecCommandTree = {
  group: 'data-source-spec',
  description: 'Declare the base dataset for a view : what concept action to invoke , what external source to query , or what inline data to use . Each source records its kind , serialized configuration , and the template variables it expects at bind time , so the view layer can resolve the right data without knowing the source s internal structure',
  commands: [{ action: 'create', command: 'create' }, { action: 'get', command: 'get' }, { action: 'bind', command: 'bind' }, { action: 'list', command: 'list' }],
};
