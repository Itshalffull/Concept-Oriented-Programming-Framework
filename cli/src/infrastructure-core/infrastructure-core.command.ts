// Auto-generated by Clef Clef Bind — cli target
// Concept: InfrastructureCore
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const infrastructureCoreCommand = new Command('infrastructure-core')
  .description('Platform infrastructure primitives that underpin every clef-base feature : plugin discovery and registration ( PluginRegistry ) , tag-based caching ( Cache ) , URL alias generation ( Pathauto ) , configuration synchronization ( ConfigSync ) , event pub / sub ( EventBus ) , input validation ( Validator ) , entity reflection to the content system ( EntityReflector ) , and file system artifact discovery ( FileCatalog ) .');

infrastructureCoreCommand
  .command('register-plugin')
  .description('Surface action: registerPlugin')
  .requiredOption('--type <type>', 'Type')
  .requiredOption('--name <name>', 'Name')
  .requiredOption('--metadata <metadata>', 'Metadata')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InfrastructureCore', 'registerPlugin', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

infrastructureCoreCommand
  .command('discover-plugins')
  .description('Surface action: discoverPlugins')
  .requiredOption('--type <type>', 'Type')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InfrastructureCore', 'discoverPlugins', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

infrastructureCoreCommand
  .command('invalidate-cache')
  .description('Surface action: invalidateCache')
  .requiredOption('--bin <bin>', 'Bin')
  .requiredOption('--key <key>', 'Key')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InfrastructureCore', 'invalidateCache', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

infrastructureCoreCommand
  .command('publish-event')
  .description('Surface action: publishEvent')
  .requiredOption('--event <event>', 'Event')
  .requiredOption('--payload <payload>', 'Payload')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InfrastructureCore', 'publishEvent', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

infrastructureCoreCommand
  .command('validate')
  .description('Surface action: validate')
  .requiredOption('--validator <validator>', 'Validator')
  .requiredOption('--data <data>', 'Data')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InfrastructureCore', 'validate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

infrastructureCoreCommand
  .command('get-cached')
  .description('Surface query: getCached')
  .requiredOption('--bin <bin>', 'Bin')
  .requiredOption('--key <key>', 'Key')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InfrastructureCore', 'getCached', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

infrastructureCoreCommand
  .command('get-plugin-definitions')
  .description('Surface query: getPluginDefinitions')
  .requiredOption('--type <type>', 'Type')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/InfrastructureCore', 'getPluginDefinitions', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const infrastructureCoreCommandTree = {
  group: 'infrastructure-core',
  description: 'Platform infrastructure primitives that underpin every clef-base feature : plugin discovery and registration ( PluginRegistry ) , tag-based caching ( Cache ) , URL alias generation ( Pathauto ) , configuration synchronization ( ConfigSync ) , event pub / sub ( EventBus ) , input validation ( Validator ) , entity reflection to the content system ( EntityReflector ) , and file system artifact discovery ( FileCatalog ) .',
  commands: [{ action: 'registerPlugin', command: 'register-plugin' }, { action: 'discoverPlugins', command: 'discover-plugins' }, { action: 'invalidateCache', command: 'invalidate-cache' }, { action: 'publishEvent', command: 'publish-event' }, { action: 'validate', command: 'validate' }, { action: 'getCached', command: 'get-cached' }, { action: 'getPluginDefinitions', command: 'get-plugin-definitions' }],
};
