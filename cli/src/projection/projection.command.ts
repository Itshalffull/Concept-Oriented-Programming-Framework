// Auto-generated by Clef Clef Bind — cli target
// Concept: Projection
// Do not edit manually; regenerate with: clef interface generate

import { Command } from 'commander';

export const projectionCommand = new Command('projection')
  .description('Bidirectional interface mapping between concepts and external APIs . Forward ( outbound ) : enrich ConceptManifests with interface generation metadata for producing REST GraphQL CLI MCP endpoints . Reverse ( inbound ) : map external API endpoints back to concept actions for consuming third party APIs as concept backends . The same Projection data structure serves both directions the mapping between concept actions and API endpoints is inherently bidirectional');

projectionCommand
  .command('project')
  .description('Parse interface annotations . Merge with ConceptManifest . Compute resource mappings from state relations and action signatures . Bind traits to actions . Resolve cross concept type references within the suite . Enrichment data from workflows , annotations , and target configs is serialized as opaque JSON into the content field .')
  .requiredOption('--manifest <manifest>', 'Manifest')
  .requiredOption('--annotations <annotations>', 'Annotations')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Projection', 'project', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionCommand
  .command('validate')
  .description('All annotations resolve . Resource mappings are consistent . No breaking changes from previous generation ( if history exists ) .')
  .requiredOption('--projection <projection>', 'Projection')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Projection', 'validate', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionCommand
  .command('diff')
  .description('Compare two projections . Used for breaking change detection and changelog generation .')
  .requiredOption('--projection <projection>', 'Projection')
  .requiredOption('--previous <previous>', 'Previous')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Projection', 'diff', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionCommand
  .command('infer-resources')
  .description('Auto derive REST resource mappings from state relations and action signatures . Actions named create add produce POST , delete remove produce DELETE , list find produce GET , update edit produce PUT . Non CRUD actions produce POST to resource { id } action name .')
  .requiredOption('--projection <projection>', 'Projection')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Projection', 'inferResources', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionCommand
  .command('ingest')
  .description('Create a reverse Projection from an ingest manifest . Parses the manifest s action mappings for the specified source and concept , stores them as actionMappings . Sets direction to reverse and externalSource to the source name . This is the inverse of project ( ) instead of enriching a concept for API generation , it maps an external API back to a concept .')
  .requiredOption('--manifest <manifest>', 'Manifest')
  .requiredOption('--source <source>', 'Source')
  .requiredOption('--concept <concept>', 'Concept')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Projection', 'ingest', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionCommand
  .command('resolve-forward')
  .description('Given a concept action invocation , resolve the external API call from the Projection s action mappings . Applies request field transforms to produce the API request body . Returns the HTTP method , path , serialized body , and auth headers .')
  .requiredOption('--projection <projection>', 'Projection')
  .requiredOption('--action <action>', 'Action')
  .requiredOption('--input <input>', 'Input')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Projection', 'resolveForward', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

projectionCommand
  .command('resolve-reverse')
  .description('Given an incoming API call ( method , path , body ) , resolve which concept action it maps to . Applies response field transforms to produce the concept action input . Used for webhook routing and inbound API bridging .')
  .requiredOption('--projection <projection>', 'Projection')
  .requiredOption('--method <method>', 'Method')
  .requiredOption('--path <path>', 'Path')
  .requiredOption('--body <body>', 'Body')
  .option('--json', 'Output as JSON')
  .action(async (opts) => {
    try {
      const result = await globalThis.kernel.invokeConcept('urn:clef/Projection', 'resolveReverse', opts);
      if (result.variant !== 'ok') {
        console.error(opts.json ? JSON.stringify(result) : `Error [${result.variant}]: ${JSON.stringify(result)}`);
        process.exitCode = 1;
      } else {
        console.log(opts.json ? JSON.stringify(result) : result);
      }
    } catch (err) {
      console.error(err instanceof Error ? err.message : err);
      process.exitCode = 1;
    }
  });

export const projectionCommandTree = {
  group: 'projection',
  description: 'Bidirectional interface mapping between concepts and external APIs . Forward ( outbound ) : enrich ConceptManifests with interface generation metadata for producing REST GraphQL CLI MCP endpoints . Reverse ( inbound ) : map external API endpoints back to concept actions for consuming third party APIs as concept backends . The same Projection data structure serves both directions the mapping between concept actions and API endpoints is inherently bidirectional',
  commands: [{ action: 'project', command: 'project' }, { action: 'validate', command: 'validate' }, { action: 'diff', command: 'diff' }, { action: 'inferResources', command: 'infer-resources' }, { action: 'ingest', command: 'ingest' }, { action: 'resolveForward', command: 'resolve-forward' }, { action: 'resolveReverse', command: 'resolve-reverse' }],
};
