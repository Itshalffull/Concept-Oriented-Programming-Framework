// ============================================================
// Clef Surface Compose Widgets — Primitives Index
//
// Package-level documentation for all 14 primitive widget
// Compose implementations.
//
// In Kotlin, all public @Composable functions within the same
// package are automatically accessible without explicit
// re-exports. This file serves as a reference index.
// ============================================================

package clef.surface.compose.components.widgets.primitives

// All @Composable functions in this package:
//
// Avatar          — User/entity identity as initials in a circular surface
// Button          — Generic action trigger with filled/outline/text/danger variants
// Checkbox        — Boolean toggle with checked/unchecked/indeterminate states
// Chip            — Compact interactive tag with selection and dismiss support
// FocusTrap       — Focus-constraining wrapper using Compose focus modifiers
// ClefIcon        — Named icon via Material Icons with fallback glyph
// Label           — Accessible form-control label with required indicator
// Portal          — Overlay rendering via Popup composable
// Presence        — Conditional rendering with animated enter/exit transitions
// ScrollLock      — Scroll interception via nested scroll connection
// Separator       — Horizontal or vertical divider line
// Spinner         — Indeterminate circular progress with optional label
// TextInput       — Single-line text field with OutlinedTextField styling
// VisuallyHidden  — Screen-reader-only content at zero visual size
