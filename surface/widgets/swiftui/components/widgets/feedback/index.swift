// ============================================================
// Clef Surface SwiftUI Widgets — Feedback Index
//
// Reference index for all 10 feedback widget SwiftUI
// implementations.
// ============================================================

// All SwiftUI Views in this category:
//
// AlertView              — Inline, persistent status message with icon and close
// AlertDialogView        — Confirmation dialog requiring explicit action
// ClefContextMenuView    — Contextual action menu anchored to trigger
// DialogView             — Modal overlay with title, description, and body
// DrawerView             — Slide-in panel overlay with close button
// HoverCardView          — Preview card shown on trigger hover/tap
// PopoverView            — Non-modal floating content panel
// ToastView              — Ephemeral notification with variant styling
// ToastManagerView       — Container managing a stack of toast notifications
// TooltipView            — Lightweight floating label for supplementary text
// RichTooltipView        — Rich tooltip with title, description, and action
