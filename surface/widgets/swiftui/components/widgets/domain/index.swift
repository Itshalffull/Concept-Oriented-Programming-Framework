// ============================================================
// Clef Surface SwiftUI Widgets — Domain Index
//
// Reference index for all 25 domain widget SwiftUI
// implementations.
// ============================================================

// All SwiftUI Views in this category:
//
// AutomationBuilderView   — Linear step-sequence automation rule builder
// BlockEditorView         — Block-based document editor
// ClefCanvasView          — Infinite two-dimensional spatial canvas
// CanvasConnectorView     — Edge/arrow connecting two canvas nodes
// CanvasNodeView          — Draggable node on a canvas surface
// CodeBlockView           — Syntax-highlighted code display block
// ColorLabelPickerView    — Colored tag and label selector grid
// ConditionBuilderView    — Condition row builder for filter expressions
// CronEditorView          — Visual cron expression builder
// DragHandleView          — Drag reorder handle icon
// FieldMapperView         — Two-column field mapping interface
// GraphViewWidget         — Node-and-edge graph visualization
// ImageGalleryView        — Thumbnail grid image gallery
// InlineEditView          — Click-to-edit inline text display
// MarkdownPreviewView     — Live markdown rendering widget
// MinimapView             — Scaled-down document overview minimap
// OutlinerView            — Nested bullet-list outliner
// PluginDetailPageView    — Plugin marketplace detail page
// PolicyEditorView        — Access-control policy rule editor
// SlashMenuView           — Filterable slash command menu
// StateMachineDiagramView — State-and-transition diagram
// StepIndicatorView       — Stepper/wizard progress indicator
// TokenInputView          — Token pill input with suggestions
// WorkflowEditorView      — Node-graph workflow editor
// WorkflowNodeView        — Single workflow node with ports
