// ============================================================
// Clef Surface SwiftUI Widgets — Primitives Index
//
// Reference index for all 14 primitive widget SwiftUI
// implementations.
//
// In Swift, all public structs within the same module are
// automatically accessible. This file serves as a reference.
// ============================================================

// All SwiftUI Views in this category:
//
// AvatarView          — User/entity identity as initials in a circular surface
// ClefButtonView      — Generic action trigger with filled/outline/text/danger variants
// CheckboxView        — Boolean toggle with checked/unchecked/indeterminate states
// ChipView            — Compact interactive tag with selection and dismiss support
// FocusTrapView       — Focus-constraining wrapper using SwiftUI focus modifiers
// ClefIconView        — Named icon via SF Symbols with fallback glyph
// LabelView           — Accessible form-control label with required indicator
// PortalView          — Overlay rendering via SwiftUI overlay modifier
// PresenceView        — Conditional rendering with animated enter/exit transitions
// ScrollLockView      — Scroll interception via gesture-consuming layer
// SeparatorView       — Horizontal or vertical divider line
// SpinnerView         — Indeterminate circular progress with optional label
// TextInputView       — Single-line text field with TextField styling
// VisuallyHiddenView  — Screen-reader-only content at zero visual size
