// ============================================================
// Clef Surface SwiftUI Widgets — Composites Index
//
// Reference index for all 16 composite widget SwiftUI
// implementations.
// ============================================================

// All SwiftUI Views in this category:
//
// BacklinkPanelView      — Panel showing incoming backlink references
// CacheDashboardView     — Cache statistics and management dashboard
// DiffViewerView         — Side-by-side or unified text diff viewer
// FacetedSearchView      — Search interface with filter facets
// FileBrowserView        — File system browser with tree navigation
// FilterBuilderView      — Visual filter rule builder
// MasterDetailView       — Two-pane list-detail layout
// NotificationCenterView — Notification list with read/unread states
// PermissionMatrixView   — Grid-based permission assignment matrix
// PluginCardView         — Plugin/extension card with actions
// PreferenceMatrixView   — Grid of preference toggle settings
// PropertyPanelView      — Editable property inspector panel
// QueueDashboardView     — Job/task queue monitoring dashboard
// SchemaEditorView       — Visual schema/model field editor
// SortBuilderView        — Visual sort rule builder
// ViewSwitcherView       — Named view/layout switcher
