# TypeScriptToolchain resolution completion propagates back to Toolchain — Architecture doc Section 3.8

sync TypeScriptToolchainComplete [eager]
when {
  TypeScriptToolchain/resolve: [ platform: ?plat ]
    => [ toolchain: ?n; tscPath: ?path; version: ?ver;
         capabilities: ?caps ]
}
then {
  Toolchain/resolve: [ language: "typescript"; platform: ?plat;
    versionConstraint: ?constraint ]
}
