# Route Builder/build to SolidityBuilder — Architecture doc Section 3.8

sync RouteBuilderToSolidity [eager]
when {
  Builder/build: [ concept: ?concept; source: ?src;
    language: "solidity"; platform: ?plat; config: ?cfg ]
    => [ build: ?b; artifactHash: ?hash;
         artifactLocation: ?loc; duration: ?dur ]
}
then {
  SolidityBuilder/build: [ source: ?src; toolchainPath: ?toolPath;
    platform: ?plat; config: ?cfg ]
}
