# Route Toolchain/resolve to SolidityToolchain — Architecture doc Section 3.8

sync RouteToolchainToSolidity [eager]
when {
  Toolchain/resolve: [ language: "solidity"; platform: ?plat;
    versionConstraint: ?ver; category: ?cat; toolName: ?name ]
    => [ tool: ?tool; version: ?v; path: ?path;
         capabilities: ?caps; invocation: ?invoc ]
}
then {
  SolidityToolchain/resolve: [ platform: ?plat;
    versionConstraint: ?ver ]
}
