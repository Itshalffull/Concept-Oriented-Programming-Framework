# SwiftToolchain resolution completion propagates back to Toolchain — Architecture doc Section 3.8

sync SwiftToolchainComplete [eager]
when {
  SwiftToolchain/resolve: [ platform: ?plat ]
    => [ toolchain: ?s; swiftcPath: ?path; version: ?ver;
         capabilities: ?caps ]
}
then {
  Toolchain/resolve: [ language: "swift"; platform: ?plat;
    versionConstraint: ?constraint ]
}
