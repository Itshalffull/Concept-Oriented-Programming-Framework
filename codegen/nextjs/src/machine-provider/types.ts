// MachineProvider — types.ts
// Generated by NextjsGen. Do not edit manually.
// Uses fp-ts for purely functional, composable concept implementations.

// Storage interface for MachineProvider
export interface MachineProviderStorage {
  readonly get: (relation: string, key: string) => Promise<Record<string, unknown> | null>;
  readonly put: (relation: string, key: string, value: Record<string, unknown>) => Promise<void>;
  readonly delete: (relation: string, key: string) => Promise<boolean>;
  readonly find: (relation: string, filter?: Record<string, unknown>) => Promise<readonly Record<string, unknown>[]>;
}

export interface MachineProviderInitializeInput {
  readonly provider: string;
  readonly domain: string;
}

export interface MachineProviderInitializeOutputOk {
  readonly variant: 'ok';
  readonly provider: string;
  readonly pluginRef: string;
}

export interface MachineProviderInitializeOutputAlreadyExists {
  readonly variant: 'already_exists';
  readonly provider: string;
  readonly pluginRef: string;
}

export type MachineProviderInitializeOutput =
  | MachineProviderInitializeOutputOk
  | MachineProviderInitializeOutputAlreadyExists;

export interface MachineProviderSpawnInput {
  readonly machineId: string;
  readonly definition: Record<string, unknown>;
  readonly initialState: string;
}

export interface MachineProviderSpawnOutputOk {
  readonly variant: 'ok';
  readonly machineId: string;
  readonly currentState: string;
}

export interface MachineProviderSpawnOutputAlreadyExists {
  readonly variant: 'already_exists';
  readonly machineId: string;
  readonly message: string;
}

export type MachineProviderSpawnOutput =
  | MachineProviderSpawnOutputOk
  | MachineProviderSpawnOutputAlreadyExists;

export interface MachineProviderSendInput {
  readonly machineId: string;
  readonly event: string;
  readonly payload?: Record<string, unknown>;
}

export interface MachineProviderSendOutputOk {
  readonly variant: 'ok';
  readonly machineId: string;
  readonly previousState: string;
  readonly currentState: string;
}

export interface MachineProviderSendOutputNotFound {
  readonly variant: 'not_found';
  readonly machineId: string;
  readonly message: string;
}

export interface MachineProviderSendOutputInvalidTransition {
  readonly variant: 'invalid_transition';
  readonly machineId: string;
  readonly currentState: string;
  readonly event: string;
  readonly message: string;
}

export type MachineProviderSendOutput =
  | MachineProviderSendOutputOk
  | MachineProviderSendOutputNotFound
  | MachineProviderSendOutputInvalidTransition;

export interface MachineProviderConnectInput {
  readonly sourceMachineId: string;
  readonly targetMachineId: string;
  readonly eventMapping: Record<string, string>;
}

export interface MachineProviderConnectOutputOk {
  readonly variant: 'ok';
  readonly connectionId: string;
  readonly sourceMachineId: string;
  readonly targetMachineId: string;
}

export interface MachineProviderConnectOutputNotFound {
  readonly variant: 'not_found';
  readonly machineId: string;
  readonly message: string;
}

export type MachineProviderConnectOutput =
  | MachineProviderConnectOutputOk
  | MachineProviderConnectOutputNotFound;

export interface MachineProviderDestroyInput {
  readonly machineId: string;
}

export interface MachineProviderDestroyOutputOk {
  readonly variant: 'ok';
  readonly machineId: string;
}

export interface MachineProviderDestroyOutputNotFound {
  readonly variant: 'not_found';
  readonly machineId: string;
  readonly message: string;
}

export type MachineProviderDestroyOutput =
  | MachineProviderDestroyOutputOk
  | MachineProviderDestroyOutputNotFound;

// --- Variant constructors ---

export const initializeOk = (provider: string, pluginRef: string): MachineProviderInitializeOutput =>
  ({ variant: 'ok', provider, pluginRef } as MachineProviderInitializeOutput);

export const initializeAlreadyExists = (provider: string, pluginRef: string): MachineProviderInitializeOutput =>
  ({ variant: 'already_exists', provider, pluginRef } as MachineProviderInitializeOutput);

export const spawnOk = (machineId: string, currentState: string): MachineProviderSpawnOutput =>
  ({ variant: 'ok', machineId, currentState } as MachineProviderSpawnOutput);

export const spawnAlreadyExists = (machineId: string, message: string): MachineProviderSpawnOutput =>
  ({ variant: 'already_exists', machineId, message } as MachineProviderSpawnOutput);

export const sendOk = (machineId: string, previousState: string, currentState: string): MachineProviderSendOutput =>
  ({ variant: 'ok', machineId, previousState, currentState } as MachineProviderSendOutput);

export const sendNotFound = (machineId: string, message: string): MachineProviderSendOutput =>
  ({ variant: 'not_found', machineId, message } as MachineProviderSendOutput);

export const sendInvalidTransition = (machineId: string, currentState: string, event: string, message: string): MachineProviderSendOutput =>
  ({ variant: 'invalid_transition', machineId, currentState, event, message } as MachineProviderSendOutput);

export const connectOk = (connectionId: string, sourceMachineId: string, targetMachineId: string): MachineProviderConnectOutput =>
  ({ variant: 'ok', connectionId, sourceMachineId, targetMachineId } as MachineProviderConnectOutput);

export const connectNotFound = (machineId: string, message: string): MachineProviderConnectOutput =>
  ({ variant: 'not_found', machineId, message } as MachineProviderConnectOutput);

export const destroyOk = (machineId: string): MachineProviderDestroyOutput =>
  ({ variant: 'ok', machineId } as MachineProviderDestroyOutput);

export const destroyNotFound = (machineId: string, message: string): MachineProviderDestroyOutput =>
  ({ variant: 'not_found', machineId, message } as MachineProviderDestroyOutput);
