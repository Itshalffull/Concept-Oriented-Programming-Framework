---
name: retention-policy
description: Govern how long versions and records must be kept and when they may 
 be disposed , including legal hold suspension of normal disposition . 
 A record under active legal hold can never be disposed regardless 
 of retention period expiration .
argument-hint: [command] [recordType] [period] [unit] [dispositionAction]
---

<!-- Auto-generated by Clef Clef Bind — claude-skills target -->
<!-- Concept: RetentionPolicy -->
<!-- Do not edit manually; regenerate with: clef interface generate -->

# RetentionPolicy

Govern how long versions and records must be kept and when they may 
 be disposed , including legal hold suspension of normal disposition . 
 A record under active legal hold can never be disposed regardless 
 of retention period expiration .

## Commands

### setRetention
Retention policy created for the given record type .

**Arguments:** `$0` **recordType** (string), `$1` **period** (int), `$2` **unit** (string), `$3` **dispositionAction** (string)

### applyHold
Legal hold applied . All records matching scope pattern are held .

**Arguments:** `$0` **name** (string), `$1` **scope** (string), `$2` **reason** (string), `$3` **issuer** (string)

### releaseHold
Hold released . Records revert to normal retention rules .

**Arguments:** `$0` **holdId** (h), `$1` **releasedBy** (string), `$2` **reason** (string)

### checkDisposition
Record has passed its retention period and has no active holds .

**Arguments:** `$0` **record** (string)

### dispose
Record disposed per its policy . Disposition logged .

**Arguments:** `$0` **record** (string), `$1` **disposedBy** (string)

### auditLog
Returns disposition log , filtered to specific record if provided .

**Arguments:** `$0` **record** (string?)
