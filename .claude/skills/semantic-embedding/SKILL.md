---
name: semantic-embedding
description: Vector representation of DefinitionUnits for similarity search and natural language code search . Cached by content digest so embeddings are recomputed only when the underlying code changes
argument-hint: [command] [unit] [model]
allowed-tools: Read, Grep, Glob, Bash
---

<!-- Auto-generated by Clef Clef Bind — claude-skills target -->
<!-- Concept: SemanticEmbedding -->
<!-- Do not edit manually; regenerate with: clef interface generate -->

# SemanticEmbedding

Vector representation of DefinitionUnits for similarity search and natural language code search . Cached by content digest so embeddings are recomputed only when the underlying code changes

## Commands

### compute
Compute a vector embedding for a definition unit using 
 the specified model ( codeBERT , unixcoder , openai code , 
 voyage code ) .

**Arguments:** `$0` **unit** (string), `$1` **model** (string)

### searchSimilar
Find definition units most similar to the query vector , 
 optionally filtered by language and kind . Results as 
 serialized JSON array of { unit , score } .

**Arguments:** `$0` **queryVector** (string), `$1` **topK** (int), `$2` **language** (string), `$3` **kind** (string)

### searchNaturalLanguage
Search for code matching a natural language description . 
 Embeds the query and finds nearest definition units . 
 Results as serialized JSON array of { unit , score } .

**Arguments:** `$0` **query** (string), `$1` **topK** (int)

### get
Retrieve embedding metadata ( vector omitted for size ) .

**Arguments:** `$0` **embedding** (B)
