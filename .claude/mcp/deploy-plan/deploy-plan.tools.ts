// Auto-generated by Clef Clef Bind — mcp target
// Concept: DeployPlan
// Do not edit manually; regenerate with: clef interface generate

export const deployPlanTools = [
  {
    "type": "tool",
    "name": "deploy_plan_plan",
    "description": "Plan deployplan — Parse the deploy manifest , resolve environment overlays , \n construct the deploy DAG . Return the plan for inspection \n before execution . Graph returned as serialized JSON .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "manifest": {
          "type": "string"
        },
        "environment": {
          "type": "string"
        }
      },
      "required": [
        "manifest",
        "environment"
      ]
    }
  },
  {
    "type": "tool",
    "name": "deploy_plan_validate",
    "description": "Validate deployplan — All pre deployment invariants pass : sync completeness , \n transport compatibility , storage migration safety , \n dependency ordering . Warnings for non blocking issues .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "plan": {
          "type": "string"
        }
      },
      "required": [
        "plan"
      ]
    }
  },
  {
    "type": "tool",
    "name": "deploy_plan_execute",
    "description": "Execute deployplan — All nodes in the deploy graph executed successfully . \n Progressive delivery completed if configured .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "plan": {
          "type": "string"
        }
      },
      "required": [
        "plan"
      ]
    }
  },
  {
    "type": "tool",
    "name": "deploy_plan_rollback",
    "description": "Rollback deployplan — Compensating actions executed in reverse dependency order .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "plan": {
          "type": "string"
        }
      },
      "required": [
        "plan"
      ]
    }
  },
  {
    "type": "tool",
    "name": "deploy_plan_status",
    "description": "Status deployplan — Current execution status of an in progress deployment .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "plan": {
          "type": "string"
        }
      },
      "required": [
        "plan"
      ]
    }
  }
];
