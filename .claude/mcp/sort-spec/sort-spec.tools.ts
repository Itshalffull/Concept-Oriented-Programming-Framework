// Auto-generated by Clef Clef Bind — mcp target
// Concept: SortSpec
// Do not edit manually; regenerate with: clef interface generate

export const sortSpecTools = [
  {
    "type": "tool",
    "name": "sort_spec_create",
    "description": "Create sortspec — Register a new named sort spec with the given key array . The keys string \n is a JSON serialized SortKey [ ] ( e . g . [ { field : node , direction : asc } ] ) . \n An empty array [ ] is valid and represents natural ( identity ) ordering .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "keys": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "keys"
      ]
    }
  },
  {
    "type": "resource",
    "name": "sort_spec_get",
    "uri": "urn:clef://sort-specs/{name}",
    "description": "Get sortspec — Return the sort spec identifier and its serialized key array for the given name ."
  },
  {
    "type": "tool",
    "name": "sort_spec_compose",
    "description": "Compose sortspec — Concatenate the key arrays of the primary and secondary sort specs , \n producing a new sort spec whose name is primary+secondary . The primary \n keys appear first ; the secondary keys serve as a tiebreaker . This \n operation is not commutative : compose ( a , b ) differs from compose ( b , a ) . \n Composing any sort with an identity sort ( keys : [ ] ) leaves the keys \n unchanged .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "primary": {
          "type": "string"
        },
        "secondary": {
          "type": "string"
        }
      },
      "required": [
        "primary",
        "secondary"
      ]
    }
  },
  {
    "type": "tool",
    "name": "sort_spec_evaluate",
    "description": "Evaluate sortspec — Apply the named sort spec s keys to the JSON serialized row array , returning \n a stable sorted JSON array . Rows with the same key values preserve their \n original relative order .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "rows": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "rows"
      ]
    }
  },
  {
    "type": "resource-template",
    "name": "sort_spec_list",
    "uriTemplate": "urn:clef://sort-specs",
    "description": "List sortspec — Return a JSON serialized array of all registered sort spec names ."
  }
];
