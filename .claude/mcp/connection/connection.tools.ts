// Auto-generated by Clef Clef Bind — mcp target
// Concept: Connection
// Do not edit manually; regenerate with: clef interface generate

export const connectionTools = [
  {
    "type": "tool",
    "name": "connection_connect",
    "description": "Connect connection — Kernel reached , handshake completed , session token issued , and the \n full set of registered concept names discovered and stored . The \n connection is now ready for invoke and observe calls .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "connection": {
          "type": "string"
        },
        "endpoint": {
          "type": "string"
        },
        "transportAdapter": {
          "type": "string"
        },
        "credentials": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "connection",
        "endpoint",
        "transportAdapter",
        "credentials"
      ]
    }
  },
  {
    "type": "tool",
    "name": "connection_discover",
    "description": "Discover connection — Returns a JSON encoded discovery result at the requested depth : \n list yields concept names only , manifest adds actions , inputs , \n and variants for each concept , full adds syncs , affordances , and \n widgets ( requires Score to be available on the kernel ) .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "connection": {
          "type": "string"
        },
        "concept": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "depth": {
          "type": "string"
        }
      },
      "required": [
        "connection",
        "concept",
        "depth"
      ]
    }
  },
  {
    "type": "tool",
    "name": "connection_invoke",
    "description": "Invoke connection — The action was executed on the kernel . variant is the completion \n variant tag returned by the handler ( e . g . ok , notfound ) , \n and output is the JSON encoded output fields of that completion .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "connection": {
          "type": "string"
        },
        "concept": {
          "type": "string"
        },
        "action": {
          "type": "string"
        },
        "input": {
          "type": "string"
        }
      },
      "required": [
        "connection",
        "concept",
        "action",
        "input"
      ]
    }
  },
  {
    "type": "tool",
    "name": "connection_observe",
    "description": "Observe connection — A completion stream subscription was opened . streamId identifies \n this subscription ; completions matching the concept ( and optional \n action ) will be delivered on this stream until unsubscribed or \n the connection is closed .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "connection": {
          "type": "string"
        },
        "concept": {
          "type": "string"
        },
        "action": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "connection",
        "concept",
        "action"
      ]
    }
  },
  {
    "type": "tool",
    "name": "connection_disconnect",
    "description": "Disconnect connection — The session token was invalidated , the transport was closed \n cleanly , and the connection s status is now disconnected .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "connection": {
          "type": "string"
        }
      },
      "required": [
        "connection"
      ]
    }
  }
];
