// Auto-generated by Clef Clef Bind — mcp target
// Concept: ViewShell
// Do not edit manually; regenerate with: clef interface generate

export const viewShellTools = [
  {
    "type": "tool",
    "name": "view_shell_create",
    "description": "Create viewshell — Register a new named view shell with the given title , description , and \n child spec references . The view is immediately addressable by name and \n ready for resolution into a complete ViewConfig .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "dataSource": {
          "type": "string"
        },
        "filter": {
          "type": "string"
        },
        "sort": {
          "type": "string"
        },
        "group": {
          "type": "string"
        },
        "projection": {
          "type": "string"
        },
        "presentation": {
          "type": "string"
        },
        "interaction": {
          "type": "string"
        },
        "features": {
          "type": "string"
        },
        "pagination": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "title",
        "description",
        "dataSource",
        "filter",
        "sort",
        "group",
        "projection",
        "presentation",
        "interaction",
        "features",
        "pagination"
      ]
    }
  },
  {
    "type": "resource",
    "name": "view_shell_get",
    "uri": "urn:clef://view-shells/{name}",
    "description": "Get viewshell — Return the view shell s title , description , and all child spec reference \n names exactly as registered ."
  },
  {
    "type": "tool",
    "name": "view_shell_update",
    "description": "Update viewshell — Replace the title , description , and all child spec references for the \n named view . The view s name remains unchanged ; all other fields are \n overwritten with the supplied values .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "dataSource": {
          "type": "string"
        },
        "filter": {
          "type": "string"
        },
        "sort": {
          "type": "string"
        },
        "group": {
          "type": "string"
        },
        "projection": {
          "type": "string"
        },
        "presentation": {
          "type": "string"
        },
        "interaction": {
          "type": "string"
        },
        "features": {
          "type": "string"
        },
        "pagination": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "title",
        "description",
        "dataSource",
        "filter",
        "sort",
        "group",
        "projection",
        "presentation",
        "interaction",
        "features",
        "pagination"
      ]
    }
  },
  {
    "type": "tool",
    "name": "view_shell_resolve",
    "description": "Resolve viewshell — Hydrate all referenced child specs into a complete ViewConfig JSON string . \n If the view s legacyConfig is set and all child spec references are empty , \n the legacyConfig is returned verbatim to preserve backward compatibility \n during migration . Otherwise the config is assembled from the registered \n child spec names .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ]
    }
  },
  {
    "type": "resource-template",
    "name": "view_shell_list",
    "uriTemplate": "urn:clef://view-shells",
    "description": "List viewshell — Return all registered view shells as a JSON array , each entry containing \n the view s name , title , description , and all child spec reference names ."
  },
  {
    "type": "tool",
    "name": "view_shell_resolve_hydrated",
    "description": "Resolve hydrated viewshell — Load the ViewShell entry by name , then fetch each non empty child spec \n by calling the corresponding concept s get action . Only fetches child specs \n whose feature is listed in the features set ; disabled features are returned \n as empty strings . Returns fully hydrated data actual FilterNode trees as \n JSON strings , SortKey arrays , ProjectionField arrays , DataSourceConfig \n objects , PresentationSpec fields , InteractionSpec fields , GroupSpec fields , \n and PaginationSpec fields not just reference name strings . Child specs \n that are not registered ( empty ref or notfound ) are returned as empty strings .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ]
    }
  }
];
