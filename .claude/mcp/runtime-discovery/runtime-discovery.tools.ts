// Auto-generated by Clef Clef Bind — mcp target
// Concept: RuntimeDiscovery
// Do not edit manually; regenerate with: clef interface generate

export const runtimeDiscoveryTools = [
  {
    "type": "tool",
    "name": "runtime_discovery_scan",
    "description": "Scan runtimediscovery — Walk a project directory for deploy manifests (*.deploy.yaml),\nextract declared runtimes, and register the project with its\ndiscovered topology.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "directory": {
          "type": "string",
          "description": "Path to the Clef project directory to scan for deploy manifests"
        }
      },
      "required": [
        "directory"
      ]
    }
  },
  {
    "type": "resource-template",
    "name": "runtime_discovery_list_projects",
    "uriTemplate": "urn:clef://runtime-discovery/projects",
    "description": "List projects runtimediscovery — Return all previously scanned projects\nas a JSON array with their directories and runtime counts."
  },
  {
    "type": "resource-template",
    "name": "runtime_discovery_list_runtimes",
    "uriTemplate": "urn:clef://runtime-discovery/projects/{project}/runtimes",
    "description": "List runtimes runtimediscovery — Return the runtimes declared in the\nproject's deploy manifests as a JSON array with name, type,\ntransport protocol, and storage backend for each."
  },
  {
    "type": "resource",
    "name": "runtime_discovery_resolve_endpoint",
    "uri": "urn:clef://runtime-discovery/projects/{project}/runtimes/{runtime}/endpoint",
    "description": "Resolve endpoint runtimediscovery — Extract the transport endpoint URL\nfor a specific runtime from the project's deploy manifests,\nresolving environment variable references where possible."
  },
  {
    "type": "tool",
    "name": "runtime_discovery_resolve_credentials",
    "description": "Resolve credentials runtimediscovery — Extract and resolve credential\nreferences (API tokens, secrets) for a runtime from the\ninfrastructure.secrets section of its deploy manifest.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "project": {
          "type": "string",
          "description": "Project identifier from a previous scan"
        },
        "runtime": {
          "type": "string",
          "description": "Runtime name to resolve credentials for"
        }
      },
      "required": [
        "project",
        "runtime"
      ]
    }
  },
  {
    "type": "tool",
    "name": "runtime_discovery_select_runtime",
    "description": "Select runtime runtimediscovery — Mark a runtime as selected for this\nproject and return its resolved endpoint and protocol, ready\nfor connection to ScoreBridge.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "project": {
          "type": "string",
          "description": "Project identifier from a previous scan"
        },
        "runtime": {
          "type": "string",
          "description": "Runtime name to select for Score connection"
        }
      },
      "required": [
        "project",
        "runtime"
      ]
    }
  }
];
