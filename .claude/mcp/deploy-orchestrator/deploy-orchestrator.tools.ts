// Auto-generated by Clef Clef Bind — mcp target
// Concept: DeployOrchestrator
// Do not edit manually; regenerate with: clef interface generate

export const deployOrchestratorTools = [
  {
    "type": "tool",
    "name": "deploy_orchestrator_deploy",
    "description": "Deploy deployorchestrator — Full deployment pipeline completed : manifest parsed , \n plan created , validated , runtime provisioned , and deployed .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "manifestPath": {
          "type": "string"
        },
        "environment": {
          "type": "string"
        }
      },
      "required": [
        "manifestPath",
        "environment"
      ]
    }
  },
  {
    "type": "tool",
    "name": "deploy_orchestrator_deploy_all",
    "description": "Deploy all deployorchestrator — All discoverable clef apps deployed to their configured runtimes , \n then cross app dependencies configured via env vars .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "projectRoot": {
          "type": "string"
        },
        "environment": {
          "type": "string"
        }
      },
      "required": [
        "projectRoot",
        "environment"
      ]
    }
  },
  {
    "type": "tool",
    "name": "deploy_orchestrator_status",
    "description": "Status deployorchestrator — Current status of a deployment run .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "run": {
          "type": "string"
        }
      },
      "required": [
        "run"
      ]
    }
  }
];
