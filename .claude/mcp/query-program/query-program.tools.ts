// Auto-generated by Clef Clef Bind — mcp target
// Concept: QueryProgram
// Do not edit manually; regenerate with: clef interface generate

export const queryProgramTools = [
  {
    "type": "tool",
    "name": "query_program_create",
    "description": "Create queryprogram — Initialize an empty query program ready to receive instructions . \n The program begins with an empty instruction list , no bindings , \n and terminated set to false .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        }
      },
      "required": [
        "program"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_scan",
    "description": "Scan queryprogram — Append a Scan instruction that reads all records from the named \n source relation . The resulting record set is bound to bindAs \n for use by subsequent instructions .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "source": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "source",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_filter",
    "description": "Filter queryprogram — Append a Filter instruction that applies a serialized FilterNode \n predicate to the current record set . Only records satisfying the \n predicate are kept . The filtered set is bound to bindAs .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "node": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "node",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_sort",
    "description": "Sort queryprogram — Append a Sort instruction that orders the current record set \n by the given JSON serialized SortKey array . An empty key array \n preserves the existing order .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "keys": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "keys",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_group",
    "description": "Group queryprogram — Append a Group instruction that partitions the current record set \n by the given key fields . The config string is a JSON object \n specifying aggregation options ( e . g . count , sum , list ) . The \n grouped result is bound to bindAs .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "keys": {
          "type": "string"
        },
        "config": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "keys",
        "config",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_project",
    "description": "Project queryprogram — Append a Project instruction that selects a subset of fields \n from each record . The fields parameter is a JSON array of \n field names to retain . All other fields are dropped from the \n output . The projected set is bound to bindAs .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "fields": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "fields",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_limit",
    "description": "Limit queryprogram — Append a Limit instruction that truncates the current record set \n to at most count records . A count of zero returns an empty set . \n The truncated set is bound to output .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "output": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "count",
        "output"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_pure",
    "description": "Pure queryprogram — Terminate the program with a return value consisting of a \n variant tag and output binding . No further instructions may \n be appended after pure . The program s terminated flag is set \n to true .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "variant": {
          "type": "string"
        },
        "output": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "variant",
        "output"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_join",
    "description": "Join queryprogram — Append a Join instruction that enriches the current record set \n by joining against a second relation . For each record in the \n current set , finds matching records in source where localField \n equals foreignField , and attaches the matched data under bindAs . \n Used for schema membership joins : join membership relation where \n entity_id matches node , attaching schemas array to each record .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "source": {
          "type": "string"
        },
        "localField": {
          "type": "string"
        },
        "foreignField": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "source",
        "localField",
        "foreignField",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_compose",
    "description": "Compose queryprogram — Monadic bind : create a new program that runs first , binds its \n terminal output value to bindAs , then continues with second . \n The composed program inherits the union of both programs \n readFields .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "first": {
          "type": "string"
        },
        "second": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "first",
        "second",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_invoke",
    "description": "Invoke queryprogram — Append an Invoke instruction that declares a concept action \n invocation . The concept and action identify the target ; input \n is a JSON serialized action input . The completion ( variant tag \n and output fields ) is bound to bindAs for use by subsequent \n instructions . The program s purity is promoted to read write .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "concept": {
          "type": "string"
        },
        "action": {
          "type": "string"
        },
        "input": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "concept",
        "action",
        "input",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_match",
    "description": "Match queryprogram — Append a Match instruction that inspects the variant tag bound \n at binding . cases is a JSON object mapping variant tags to \n sub program IDs . The * key is a wildcard default . The matched \n sub program s terminal output is bound to bindAs . All referenced \n sub programs must exist and be sealed .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "binding": {
          "type": "string"
        },
        "cases": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "binding",
        "cases",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_traverse_invoke",
    "description": "Traverse invoke queryprogram — Append a TraverseInvoke instruction that iterates over the \n record set bound at sourceBinding . For each record , binds \n the record to itemBinding , interpolates inputTemplate \n ( replacing $ < itemBinding > . field with actual values ) , and \n invokes concept action with the interpolated input . Each \n invocation dispatches through the sync engine sequentially . \n The list of completions ( variant and output per item ) is bound \n to bindAs . The program s purity is promoted to read write .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "sourceBinding": {
          "type": "string"
        },
        "itemBinding": {
          "type": "string"
        },
        "concept": {
          "type": "string"
        },
        "action": {
          "type": "string"
        },
        "inputTemplate": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "sourceBinding",
        "itemBinding",
        "concept",
        "action",
        "inputTemplate",
        "bindAs"
      ]
    }
  },
  {
    "type": "tool",
    "name": "query_program_traverse",
    "description": "Traverse queryprogram — Append a Traverse instruction that iterates over the record \n set bound at sourceBinding . For each record , binds the record \n to itemBinding , then executes bodyProgram in a nested scope . \n The bodyProgram must be sealed . Results are collected into a \n list bound to bindAs . declaredEffects is a JSON object with \n optional keys : readFields , invokedActions , completionVariants . \n If provided , static analysis uses declared effects instead of \n analyzing the body .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "program": {
          "type": "string"
        },
        "sourceBinding": {
          "type": "string"
        },
        "itemBinding": {
          "type": "string"
        },
        "bodyProgram": {
          "type": "string"
        },
        "bindAs": {
          "type": "string"
        },
        "declaredEffects": {
          "type": "string"
        }
      },
      "required": [
        "program",
        "sourceBinding",
        "itemBinding",
        "bodyProgram",
        "bindAs",
        "declaredEffects"
      ]
    }
  }
];
