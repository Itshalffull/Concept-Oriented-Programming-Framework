// Auto-generated by Clef Clef Bind — mcp target
// Concept: FilterSpec
// Do not edit manually; regenerate with: clef interface generate

export const filterSpecTools = [
  {
    "type": "tool",
    "name": "filter_spec_create",
    "description": "Create filterspec — Register a new named filter with its serialized FilterNode tree and \n source classification . The filter is immediately available for \n composition , evaluation , and validation .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "tree": {
          "type": "string"
        },
        "sourceType": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "tree",
        "sourceType"
      ]
    }
  },
  {
    "type": "resource",
    "name": "filter_spec_get",
    "uri": "urn:clef://filter-specs/{name}",
    "description": "Get filterspec — Return the filter s tree , source classification , and the field \n references extracted from the tree at registration time ."
  },
  {
    "type": "tool",
    "name": "filter_spec_compose",
    "description": "Compose filterspec — Create a new unnamed filter whose tree is \n { type : and , conditions : [ a . tree , b . tree ] } . The resulting \n FilterNode preserves both sub trees . Composing with the identity \n filter ( type : true ) produces a tree semantically equal to the \n other operand . Composition is commutative and idempotent .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "a": {
          "type": "string"
        },
        "b": {
          "type": "string"
        }
      },
      "required": [
        "a",
        "b"
      ]
    }
  },
  {
    "type": "tool",
    "name": "filter_spec_evaluate",
    "description": "Evaluate filterspec — Apply the filter s predicate tree to the provided JSON array of \n row objects and return only the rows that match the predicate . \n The result is a JSON array string containing the matching rows .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "rows": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "rows"
      ]
    }
  },
  {
    "type": "tool",
    "name": "filter_spec_normalize",
    "description": "Normalize filterspec — Return the filter tree converted to the requested normal form \n ( cnf or dnf ) . The normalized tree is semantically equivalent \n to the original and can be substituted anywhere the original \n tree would be used .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "form": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "form"
      ]
    }
  },
  {
    "type": "tool",
    "name": "filter_spec_validate",
    "description": "Validate filterspec — All field references in the filter tree exist in the provided \n schema . The filter is safe to apply against data matching that \n schema .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "schema": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "schema"
      ]
    }
  },
  {
    "type": "tool",
    "name": "filter_spec_bind",
    "description": "Bind filterspec — Replace every { type : param } node in the filter tree with the \n concrete value from the bindings map that matches the node s \n parameter name . The bound tree is returned as a serialized \n FilterNode string ready for evaluation .",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "bindings": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "bindings"
      ]
    }
  },
  {
    "type": "resource-template",
    "name": "filter_spec_list",
    "uriTemplate": "urn:clef://filter-specs",
    "description": "List filterspec — Return all registered filters as a JSON array , each entry \n containing the filter s name , tree , sourceType , and fieldRefs ."
  }
];
