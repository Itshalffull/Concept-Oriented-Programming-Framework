# Generator dispatches to Target for each configured target type
# Architecture doc: Clef Bind, Sync Section 3.3

sync DispatchToTarget [eager]
when {
  Generator/generate: [ plan: ?plan ]
    => [ plan: ?plan ]
}
then {
  Target/generate: [ projection: ?projection; targetType: ?targetType; config: ?config ]
}
