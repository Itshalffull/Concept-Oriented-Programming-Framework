# Projection complete triggers validation
# Architecture doc: Clef Bind, Sync Section 3.2

sync ValidateProjection [eager]
when {
  Projection/project: [ manifest: ?manifest ]
    => [ projection: ?p ]
}
then {
  Projection/validate: [ projection: ?p ]
}
