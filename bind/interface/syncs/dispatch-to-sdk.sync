# Generator dispatches to Sdk for each configured language
# Architecture doc: Clef Bind, Sync Section 3.4

sync DispatchToSdk [eager]
when {
  Generator/generate: [ plan: ?plan ]
    => [ plan: ?plan ]
}
then {
  Sdk/generate: [ projection: ?projection; language: ?lang; config: ?config ]
}
