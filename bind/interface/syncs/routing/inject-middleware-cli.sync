# CLI target complete triggers middleware resolution
# Architecture doc: Clef Bind, Sync Section 3.6

sync InjectMiddlewareCli [eager]
when {
  CliTarget/generate: [ projection: ?p ]
    => [ commands: ?commands; files: ?files ]
}
then {
  Middleware/resolve: [ traits: ?traits; target: "cli" ]
}
