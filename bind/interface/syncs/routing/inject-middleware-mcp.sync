# MCP target complete triggers middleware resolution
# Architecture doc: Clef Bind, Sync Section 3.6

sync InjectMiddlewareMcp [eager]
when {
  McpTarget/generate: [ projection: ?p ]
    => [ tools: ?tools; files: ?files ]
}
then {
  Middleware/resolve: [ traits: ?traits; target: "mcp" ]
}
