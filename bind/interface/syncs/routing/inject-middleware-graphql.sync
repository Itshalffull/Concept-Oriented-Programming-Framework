# GraphQL target complete triggers middleware resolution
# Architecture doc: Clef Bind, Sync Section 3.6

sync InjectMiddlewareGraphql [eager]
when {
  GraphqlTarget/generate: [ projection: ?p ]
    => [ types: ?types; files: ?files ]
}
then {
  Middleware/resolve: [ traits: ?traits; target: "graphql" ]
}
